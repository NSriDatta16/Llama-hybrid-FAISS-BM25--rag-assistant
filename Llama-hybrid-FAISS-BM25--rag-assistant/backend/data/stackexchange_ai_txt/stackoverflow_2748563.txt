[site]: stackoverflow
[post_id]: 2748563
[parent_id]: 2748551
[tags]: 
Looking through the docs, it doesn't look like the HTML subset that QTextBrowser supports would allow you to adjust the line space (though should be able to adjust the paragraph spacing). It's a much heavier weight solution, but you could try using QWebView instead.
