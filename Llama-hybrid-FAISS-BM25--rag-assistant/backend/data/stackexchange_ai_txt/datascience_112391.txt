[site]: datascience
[post_id]: 112391
[parent_id]: 112112
[tags]: 
A common way to evaluate machine learning models is performance on unseen data. Thus, "randomly sample 100 unrated (by the test user) items and append the test item to it and then they build the top 10 rank list" is popular. That process is similar to using precision@k for search engine result pages .
