[site]: stackoverflow
[post_id]: 5339381
[parent_id]: 5332714
[tags]: 
While this is a workaround, it might do the trick for you... wherever you're displaying these, I'll be assuming you're checking in the view so... session->flashdata('status_messege'): ?> session->flashdata('status_message') ?> you COULD add an elseif to that and check for the referrer being your logout function... session->flashdata('status_messege'): ?> session->flashdata('status_message') ?> agent->referrer() == site_url('path/to/logout'): ?> lang->line('auth_message_logged_out') ?> A bit of a hackish way to overcome this issue, but probably a way nonetheless.
