[site]: crossvalidated
[post_id]: 509391
[parent_id]: 405933
[tags]: 
Yes, you can do so, assuming the expected counts in each cell are large enough (all larger than one, not more than 20% smaller than 5). But it seems that your wealth category variable is ordinal, the usual chi-squared test would not use that information. To use it you can try ordinal logistic regression (search this site).
