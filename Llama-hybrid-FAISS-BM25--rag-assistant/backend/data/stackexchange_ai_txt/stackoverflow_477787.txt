[site]: stackoverflow
[post_id]: 477787
[parent_id]: 
[tags]: 
C++, windows->linux porting, mapfile problem

I'm porting a small C++ console application from windows to linux, GCC 4.3.2. When compiling I get strange error that I'm unable to solve. Labels.cpp: In function ‘void DumpSymbols()’: Labels.cpp:68: error: invalid conversion from ‘int’ to ‘std::_Ios_Openmode’ Labels.cpp:68: error: initializing argument 2 of ‘std::basic_ofstream ::basic_ofstream(const char*, std::_Ios_Openmode) [with _CharT = char, _Traits = std::char_traits ]’ Labels.cpp: #include #include #include #include "stdafx.h" using namespace std; #include "Opcodes.h" #include "Labels.h" Label LABELS[1024]; int labelcounter = 0; int OffsetCounter = 0; void AddLabel(string name, int line) { LABELS[labelcounter].name = name; LABELS[labelcounter].line = line; LABELS[labelcounter].offset = OffsetCounter; printf("Adding label: %s[0x%X]\n", name.c_str(), OffsetCounter); labelcounter++; } bool IsLabel(string name) { for(int i=0;i stdafx.h: #pragma once #include #include #include #include Thanks.
