[site]: stackoverflow
[post_id]: 4878145
[parent_id]: 
[tags]: 
Javascript and WebGL, external scripts

Just curious; How do I place my webgl shaders, in an external file? Currently I'm having; #ifdef GL_ES precision highp float; #endif void main(void) { gl_FragColor = vec4(1.0, 1.0, 1.0, 1.0); } attribute vec3 aVertexPosition; uniform mat4 uMVMatrix; uniform mat4 uPMatrix; void main(void) { gl_Position = uPMatrix * uMVMatrix * vec4(aVertexPosition, 1.0); } In my html header, how do I link in this from an external file? - I tried the usual javascript approach;
