[site]: stackoverflow
[post_id]: 5692685
[parent_id]: 5692662
[tags]: 
I'm not sure about the links, but for the divs, if you want to put every paragraph in its own div, you can use .wrap() : $('p').wrap(function(i) { return ' ' }); If you'd post the before and after structure of the HTML, we could help more.
