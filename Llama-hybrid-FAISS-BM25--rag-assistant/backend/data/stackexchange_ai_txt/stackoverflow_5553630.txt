[site]: stackoverflow
[post_id]: 5553630
[parent_id]: 5553398
[tags]: 
Yes, that's exactly how it should be used. Perhaps the following syntax would be more elegant: Node oldTop = null; Node newTop = null; do { oldTop=topOfStack.get(); newTop=new Node (e,oldTop); } while (!topOfStack.compareAndSet(oldTop, newTop));
