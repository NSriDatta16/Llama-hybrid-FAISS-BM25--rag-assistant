[site]: stackoverflow
[post_id]: 2701315
[parent_id]: 2701286
[tags]: 
The simplest way I can think of is to create a table with values 1000-1999 then do the following: Select MIN(Values.Key) From Main Left Join Values on Values.Key = Main.Key Where Main.Key is Null But someone else can probably come up with a more elegant solution
