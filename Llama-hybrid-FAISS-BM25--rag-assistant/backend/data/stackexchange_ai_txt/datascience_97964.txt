[site]: datascience
[post_id]: 97964
[parent_id]: 26060
[tags]: 
That is a common approach when using embeddings, especially word embeddings. One choice is between mean and medoid. Mean calculates a central tendency vector that might not map to an observed image. Medoid adds the constraint the central tendency vector as to be an observed image.
