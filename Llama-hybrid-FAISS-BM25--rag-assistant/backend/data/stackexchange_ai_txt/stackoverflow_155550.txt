[site]: stackoverflow
[post_id]: 155550
[parent_id]: 155435
[tags]: 
Note: other CFML engines do support nulls. If we really are dealing with nulls (and not empty strings), here is a function that will work for Railo and OpenBlueDragon: Then to use the function is as simple as:
