[site]: stackoverflow
[post_id]: 4803472
[parent_id]: 4803205
[tags]: 
Your code looks mostly working. I think the only thing you need to change is that in your showParagraphs function you just have to... replace scrollToElement(elementId) with scrollToElement('# ') ...since you're pulling the id for the element you want to affect from the server and not a passed in argument. If that doesn't do it, here's a working demo that I threw together that you can try implementing as an alternative.
