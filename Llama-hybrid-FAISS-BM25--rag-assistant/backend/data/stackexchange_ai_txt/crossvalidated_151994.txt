[site]: crossvalidated
[post_id]: 151994
[parent_id]: 
[tags]: 
Interpretation of logistic regression output with an interaction effect

I'm currently working on a research problem that requires the addition of an interaction term for each continuous covariate. In other words, I need to determine how the continuous variable changes across seasons (A = Fall/Winter, B = Preincubation, and C = Summer). I ran the following full model: `results_full summary(results_full)` The results are as follows: I'm struggling with the interpretation of the results. How would you interpret results from an interaction between a continuous (MP_Scaled) and categorical variable (season)? Example snippet of data:
