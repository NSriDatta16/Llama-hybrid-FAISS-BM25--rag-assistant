[site]: stackoverflow
[post_id]: 569248
[parent_id]: 569162
[tags]: 
If you provide your own lambda expression inside the sum you should be able to group them by a standard sum. I think this should work: var grouped = from item in items orderby item.Group group item by item.Group into grp select new { Average= grp.Sum(row => row.Value * row.Weight) };
