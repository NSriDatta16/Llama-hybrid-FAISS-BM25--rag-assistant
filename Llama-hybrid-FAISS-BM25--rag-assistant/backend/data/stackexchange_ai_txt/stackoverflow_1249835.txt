[site]: stackoverflow
[post_id]: 1249835
[parent_id]: 1249794
[tags]: 
Since you're using prepared statements, you shouldn't also escape your strings. String escaping is for when you're embedding the values into the SQL query string itself, but by using prepared statements, you're quite rightly avoiding doing that.
