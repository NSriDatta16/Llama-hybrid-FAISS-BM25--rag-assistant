[site]: stackoverflow
[post_id]: 3597570
[parent_id]: 3597508
[tags]: 
If you just want to run your tests from the command line, there is MSTest . You can also run your VS Unit Tests if your using Team Foundation Server (TFS) and its build agents. However this may not be possible or even desirable for you. The TeamCity build server now also has built-in support for running MSTests.
