[site]: stackoverflow
[post_id]: 3675606
[parent_id]: 3675574
[tags]: 
try foreach(array_unique($information) as $info) plus, pay attention to Ross's comment: it might be better to optimize your db query in the first place plus, variable names "$information" and "$info" just suck. I hope it's only an example!
