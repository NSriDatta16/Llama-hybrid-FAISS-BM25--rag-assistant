[site]: stackoverflow
[post_id]: 5107329
[parent_id]: 5106021
[tags]: 
The procedure of choice depends on many factors. If you have an empty tinymce editor you can initialize your editor with the following content Please update your question regarding your use case. EDIT: This might work better for you. Add this handler to one of your own plugins. It checks for all paragraphs (if your tinymce uses divs you need to alter this piece of code) and wraps the inner HTML into b-tags (you might want to use strong-tags). ed.onInit.add(function(ed){ ps = ed.getDoc().getElementsByTagName('p'); for (var i=0; i '+ps[i].innerHTML+' '; } }); Another option you have is to not use an own plugin, but to use the setup parameter when initializing tinymce: setup : function(ed) { ed.onInit.add(function(ed){ ps = ed.getDoc().getElementsByTagName('p'); for (var i=0; i '+ps[i].innerHTML+' '; } }); },
