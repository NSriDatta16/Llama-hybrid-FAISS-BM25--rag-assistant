[site]: stackoverflow
[post_id]: 3343871
[parent_id]: 3343841
[tags]: 
Put the images in a persistent storage (MySQL, memcache, xcache...) along with the time when they were put into storage. When you read them, determine if the persistent storage is older than the cache limit (in this example, 5 seconds) and if it is, rebuild it before reading the images.
