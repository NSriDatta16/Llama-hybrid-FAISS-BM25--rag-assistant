[site]: stackoverflow
[post_id]: 4880597
[parent_id]: 4880394
[tags]: 
Use e4x. private function SliceXMLList(xmllist : XMLList, startIndex : int, endIndex : int) : XMLList { return xmllist.(childIndex() >= startIndex && childIndex() Update: There's a problem, if you're going to use this function after some e4x-sorting, 'cause the childIndex() function returns the old values of the nodes' indexes and it cannot be changed. So, I have another idea: private function SliceXMLList(xmllist : XMLList, startIndex : int, endIndex : int) : XMLList { for (var i : int = 0; i = startIndex && @realIndex or just private function SliceXMLList(xmllist : XMLList, startIndex : int, endIndex : int) : XMLList { var newXMLList : XMLList = new XMLList(); var currIndex : int = 0; for (var i : int = startIndex; i This is the best variant, i think. Of course one-line e4x statement is much more elegant, but unfortunately it's not reusable.
