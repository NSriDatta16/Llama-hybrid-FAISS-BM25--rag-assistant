[site]: crossvalidated
[post_id]: 486082
[parent_id]: 486075
[tags]: 
Random Forest K-Nearest Neighbors Support Vector Machines Naïve Bayes The reason why softmax is popular for the neural networks, however, is because it is the multi-class analogue of the inverse link function in logistic regression. Even in logistic regression, though, you can use other link functions, and I have my suspicions that this is generalizable to multi-class problems, too.
