[site]: datascience
[post_id]: 97331
[parent_id]: 97330
[tags]: 
You are correct that it does not affect the choice of which split to make. Instead, it affects the choice of whether to make any split. If every $\mathcal{L}_{\text{split}}$ is negative, then no split will be made at the node, pre-pruning the tree. See also Tree complexity in xgboost
