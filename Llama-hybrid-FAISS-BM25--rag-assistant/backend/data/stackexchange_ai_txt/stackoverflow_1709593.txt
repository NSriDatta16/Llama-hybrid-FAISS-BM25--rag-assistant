[site]: stackoverflow
[post_id]: 1709593
[parent_id]: 1709509
[tags]: 
NSD is right. You could also use try as a short cut: request.user_agent.try(:include?, "iPhone") try will not error when its receiver is nil, so you can chain them together, like this: foo.try(:bar).try(:baz)
