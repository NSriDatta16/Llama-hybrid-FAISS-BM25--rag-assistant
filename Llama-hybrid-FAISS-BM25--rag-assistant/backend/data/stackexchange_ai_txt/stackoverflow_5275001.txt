[site]: stackoverflow
[post_id]: 5275001
[parent_id]: 5274945
[tags]: 
You can manually implement it processing the mouse move and down events. On mouse down mark object as being dragged On mouse move calculate the offset from the last cursor position and move the dragged object but checking before that the new position is inside the container box. You can get inspiration from the jquery ui code in case you need.
