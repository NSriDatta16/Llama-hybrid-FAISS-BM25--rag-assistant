[site]: stackoverflow
[post_id]: 4229423
[parent_id]: 
[tags]: 
Recommended pattern for managing data in Session object

I need to store a "POCO" class in the Session object. Which pattern would you recommend in terms of storage and performance? (I understand that pattern 2 requires serialization). Thank you. Pattern 1 (simplified): class Location { string Country { get { return Session["Country"]; } set { Session["Country]" = value }; string City { get { return Session["City"]; } set { Session["City]" = value }; } Pattern 2 (simplified): class Location { string Country { get; set; } string City { get; set; } } Session.Add("Location", new Location() { ... } );
