[site]: stackoverflow
[post_id]: 2294942
[parent_id]: 2294933
[tags]: 
One inelegant workaround is to "hide" the comma inside another macro #define ARGUMENT std::map YOUR_MACRO(ARGUMENT) #undef ARGUMENT However, if YOUR_MACRO itself needs to propagate it another level down into another macro, it will run into the same problem.
