[site]: stackoverflow
[post_id]: 5252113
[parent_id]: 5252067
[tags]: 
If you follow the single responsibility principle , which states that a class should only change for one reason, the answer would be clear: create 3 layers (BLL/DAL/BO) As for where to place them - for organizational reasons, I would create a different folder for each layer.
