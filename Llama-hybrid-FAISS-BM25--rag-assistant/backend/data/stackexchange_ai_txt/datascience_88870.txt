[site]: datascience
[post_id]: 88870
[parent_id]: 80663
[tags]: 
If you understand how a particular type of Layer works, you can simply add them as at the end all of these are Tensor operations. But you must know what are you doing. May do this way from keras.models import Sequential from keras import layers model = Sequential() model.add(layers.LSTM(30, return_sequences=True, input_shape=(30,3))) model.add(layers.GlobalAveragePooling1D()) model.add(layers.Dense(20)) model.add(layers.Dense(1)) model.compile(optimizer='adam', loss='mse', metrics=['mae']) batch_size=30 # For the case - x = (1800, 30, 3) and y=1800,1 history = model.fit(datagen(batch_size), steps_per_epoch=len(target)/batch_size, epochs=5)
