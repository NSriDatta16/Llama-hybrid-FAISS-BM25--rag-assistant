[site]: stackoverflow
[post_id]: 3751478
[parent_id]: 3751372
[tags]: 
The IsolatedStorageFile.CreateDirectory will call Directory.CreateDirectory internally. The documentation of Directory.CreateDirectory states: If the directory already exists, this method does nothing. In other words, you don't need to check if that directory exists. The method already does that for you.
