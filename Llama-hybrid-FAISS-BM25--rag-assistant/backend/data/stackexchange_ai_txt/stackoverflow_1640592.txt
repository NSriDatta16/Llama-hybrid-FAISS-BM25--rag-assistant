[site]: stackoverflow
[post_id]: 1640592
[parent_id]: 1640567
[tags]: 
Probably a more elegant approach would be to define them with 5 different names (e.g. f1, f2, f3, f4, f5), but always call the function using the same different name (e.g. f). Then compile with something like: gcc -Df=f1 That will compile any calls to f() as a call to f1().
