[site]: stackoverflow
[post_id]: 1766547
[parent_id]: 1766351
[tags]: 
void MegaNumber::multiplyAssign(int operand, int index) { data[index] *=operand; if(index 9) carry(index); } index is 0 based, while data.size() is 1 based so to say, meaning data.size() returns number 1 greater than the largest valid index . So looks like you intention was if( index Then it works. P.S. break your code into lines, whoever has to maintain your code will thank you for that: if (index
