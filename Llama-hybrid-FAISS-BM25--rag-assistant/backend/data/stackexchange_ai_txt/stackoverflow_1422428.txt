[site]: stackoverflow
[post_id]: 1422428
[parent_id]: 
[tags]: 
yield: why can't i write: p test_method { i.upcase }

def test_method ["a", "b", "c"].map {|i| yield(i) } end If I call test_method like this: p test_method {|i| i.upcase } # => ["A", "B", "C"] Why do I need the {|i|} inside the block, instead of just saying this: p test_method { i.upcase } The reason I think so is because when yield is called in test_method, we already have an {|i|} ["a", "b", "c"].map {|i| yield(i) }
