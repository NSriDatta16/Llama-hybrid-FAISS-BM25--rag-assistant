[site]: stackoverflow
[post_id]: 4582852
[parent_id]: 
[tags]: 
MySQL - finding average number in WHERE

I am trying to find the average number of the sum of some rows according to a WHERE clause. Here's what I have so far: SELECT SUM(rating), id FROM rating WHERE id=$id;"); So something like "sum of rows" / "number of rows returned"
