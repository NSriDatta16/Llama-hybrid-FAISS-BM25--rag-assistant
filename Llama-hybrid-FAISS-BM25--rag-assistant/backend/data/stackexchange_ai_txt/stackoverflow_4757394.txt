[site]: stackoverflow
[post_id]: 4757394
[parent_id]: 
[tags]: 
How to load Total Address collection of customer in magento

I want to load the customer address collection in a custom script. This code loads only billing address, but I need to load all the addresses(I have 3 addresses under this customer) $customerAddressId = Mage::getSingleton('customer/session')->getCustomer()->getDefaultBilling(); if ($customerAddressId){ $address = Mage::getModel('customer/address')->load($customerAddressId); // echo " "; print_r($address->getData());echo " "; Zend_Debug::dump($address->debug()); $street = $address->getData('street'); } Tried this but this is not returning the shipping address. Mage::getSingleton('customer/session')->getCustomer()->getDefaultBilling(); Please help me to load all the addresses from Magento customer table for particular user.
