[site]: stackoverflow
[post_id]: 5493587
[parent_id]: 
[tags]: 
How do I extract IP address and port number from NSData

- (void)readData{ int err; int sock; struct sockaddr_storage addr; socklen_t addrLen; uint8_t buffer[65536]; ssize_t bytesRead; sock = CFSocketGetNative(self->_cfSocket); addrLen = sizeof(addr); bytesRead = recvfrom(sock, buffer, sizeof(buffer), 0, (struct sockaddr *) &addr, &addrLen); if (bytesRead addrObj is NSData, How do I extract IP address and port number from addrObj?
