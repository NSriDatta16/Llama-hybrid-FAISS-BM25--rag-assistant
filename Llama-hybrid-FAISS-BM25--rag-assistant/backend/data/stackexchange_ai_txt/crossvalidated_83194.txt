[site]: crossvalidated
[post_id]: 83194
[parent_id]: 
[tags]: 
Using Moving-average smoothing in forecast package

I tried to use the non-centred moving average, that means just using past values by setting the option centre = FALSE, but unfortunately you get the centred results. Can anyone detect the failure here in the ma function? getAnywhere(ma) A single object matching ‘ma’ was found It was found in the following places package:forecast namespace:forecast with value function (x, order, centre = TRUE) { tt
