[site]: stackoverflow
[post_id]: 2307277
[parent_id]: 2307161
[tags]: 
You could try using a handle instead. So the user would drag and drop the h2 element instead of the whole of #dragMe. $('#dragMe').draggable({ containment: '#dragContain', cursor: 'move', zIndex: 20000, handle: 'h2' }); . Title goes here! Content goes here!
