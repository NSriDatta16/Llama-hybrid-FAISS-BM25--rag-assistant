[site]: stackoverflow
[post_id]: 1450017
[parent_id]: 1449953
[tags]: 
Browser-specific methods have been discussed here: onpropertychange for a textbox in Firefox? However, after various experiments I found the only cross-browser method was to use window.setInterval() to check for changes (every 20 milliseconds, for example). Not elegant, but very effective.
