[site]: stackoverflow
[post_id]: 620191
[parent_id]: 
[tags]: 
How can I get an XML fragment as a string from an XElement?

I have an XElement and inside that element I have another XML fragment. How do I retrieve the XML? If I try the following, I get only the value: string str = " value1 "; XElement elem = XElement.Parse(str); string innerXml = elem.value;
