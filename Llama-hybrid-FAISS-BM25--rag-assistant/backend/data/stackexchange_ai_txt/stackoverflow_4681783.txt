[site]: stackoverflow
[post_id]: 4681783
[parent_id]: 4680996
[tags]: 
Why you need to perform this using agents? because you want to copy them asynchronously? But if you still want to do this, you can use something like: (do-all (for [x file-names] (send-off agent-name copy-function x))) although, maybe it's better to use futures?
