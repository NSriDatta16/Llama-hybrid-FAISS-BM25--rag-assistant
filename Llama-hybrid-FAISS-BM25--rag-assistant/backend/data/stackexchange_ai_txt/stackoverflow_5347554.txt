[site]: stackoverflow
[post_id]: 5347554
[parent_id]: 5347295
[tags]: 
Here's a working version of your code: http://jsfiddle.net/marcosfromero/YRfVd/ I added this HTML: with this associated JavaScript: if(o.droppableDiv!=undefined){ $(o.droppableDiv).droppable({ drop: function( event, ui ) { // clone the original draggable, not the ui.helper // and append it to the ul element inside new-dropcontainer jQuery('#new-dropcontainer ul').append(ui.draggable.clone()); // some extra code for proper widget creation } }); }
