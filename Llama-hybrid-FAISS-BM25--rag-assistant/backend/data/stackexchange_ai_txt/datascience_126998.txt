[site]: datascience
[post_id]: 126998
[parent_id]: 
[tags]: 
Details about Pre-existing knowledge database in RAG for LLMs

In RAG, As part of retriever model- we are retrieving the relevant information from external knowledge source (i.e. vector database) and this database is always updating with new updates. In application implementation, are we storing data in databse (with relevant information)? if 'yes' then how we maintating this database as updated?
