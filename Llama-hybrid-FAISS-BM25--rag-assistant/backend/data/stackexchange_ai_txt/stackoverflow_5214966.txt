[site]: stackoverflow
[post_id]: 5214966
[parent_id]: 
[tags]: 
How to unload/remove/replace a Jquery Script if useragent is IE6?

This is tricky. I have done a script with jQuery, where an gets "shaked" if the user puts incorrect values: $('#input-name').addClass('input-bad').effect('shake',{ direction: 'left', distance: 10, times: 3} , 100 ); The problem is that this effect (thanks to jQuery UI) makes the input shake but at the very far right of the page in Internet Explorer 6 (dunno 7/8). The point is remove the shake effect if the user uses this browser. I'm using for browser detection purposes.
