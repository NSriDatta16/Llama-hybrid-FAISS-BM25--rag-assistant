[site]: stackoverflow
[post_id]: 4286715
[parent_id]: 
[tags]: 
#define ISR(x) #pragma isr=x

I have problems with this attempt at a macro: #define ISR(x) #pragma isr=x doesn't compile because it's trying to replace #pragma with a nonexistent parameter. Is there any way to achieve what I'm trying to do? I'd like to get ISR(VEC1) expanded as #pragma isr=VEC1 .
