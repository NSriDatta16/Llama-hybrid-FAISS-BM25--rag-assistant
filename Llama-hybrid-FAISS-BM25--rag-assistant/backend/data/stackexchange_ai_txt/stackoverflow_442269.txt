[site]: stackoverflow
[post_id]: 442269
[parent_id]: 
[tags]: 
jQuery scripts in the response of Ajax does not work

I am using jQuery $.load() function to get content from server. the loaded content contain this: 123 $(document).ready(function(){ $('#p1').html('ABC'); alert($('#p1').html()); }); changing text of paragraph to 'ABC' not affected to page but alert() display 'ABC'.
