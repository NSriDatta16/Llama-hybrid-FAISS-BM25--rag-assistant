[site]: stackoverflow
[post_id]: 5377278
[parent_id]: 5377257
[tags]: 
Don't execute a query for each date, rather you should do something like: SELECT DATE(date_column), other_column FROM your_table ORDER BY DATE(date_column) And when you are processing your result set pay attention to whether the date result has changed. This way you only dispatch one query.
