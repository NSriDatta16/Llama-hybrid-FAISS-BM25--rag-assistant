[site]: stackoverflow
[post_id]: 708565
[parent_id]: 708555
[tags]: 
readVector( in ); // error C2783 This should be: readVector ( in ); Argument dependent lookup does not help deduce the templates, hence explicit specification is required. in >> tmp; This line requires you have a function like: istream& operator>>(ostream& is, InventoryItem& item) { // parse and read in the data to item return is; } Put in the definition (I think strager provided this in your other question).
