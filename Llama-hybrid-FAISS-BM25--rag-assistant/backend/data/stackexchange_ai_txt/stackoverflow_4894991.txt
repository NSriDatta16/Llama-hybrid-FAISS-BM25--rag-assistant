[site]: stackoverflow
[post_id]: 4894991
[parent_id]: 4894885
[tags]: 
I haven't had a chance to run this (so it might not work 100%), but it should be what you're looking for: File files[] = Environment.getExternalStorageDirectory().listFiles(); for (File f : files) { String fullPath = f.getAbsolutePath(); int dot = fullPath.lastIndexOf("."); String ext = fullPath.substring(dot + 1); if (ext.equalsIgnoreCase("txt")) // Make sure case is irrelevant // for your use case; otherwise // use ext.equals("txt"). { // Do something with f here } }
