[site]: stackoverflow
[post_id]: 587515
[parent_id]: 587351
[tags]: 
Try this (based on your partial solution): foreach (var group in dlstPlantillas.SelectedItems) { var groupCast = groupCast = group as System.Linq.IGrouping if(groupCast == null) return; item = groupCast.FirstOrDefault (); if(item == null) return; // do stuff with item }
