[site]: stackoverflow
[post_id]: 881084
[parent_id]: 
[tags]: 
Is there a difference between SetupResult and Stub in RhinoMocks?

Is there, if any? : var storage = mocks.DynamicMock (); ... SetupResult.For(storage.GetCustomers()) .Return(new Collection { cust1, cust2 }); // and storage.Stub(x => x.Customers) .Return(new Collection { cust1, cust2 });
