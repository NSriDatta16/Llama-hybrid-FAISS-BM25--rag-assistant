[site]: stackoverflow
[post_id]: 2024344
[parent_id]: 2024332
[tags]: 
Define the functions in the correct scope and it's not a big deal. var generateHTML = function() { ... }; var somethingElse = function() { ... }; ... some more ... $('#dialog').html( generateHTML() )...etc There's not much else you can do except reorganize your code in general (say, using an object oriented style ) to encapsulate code so it isn't so messy.
