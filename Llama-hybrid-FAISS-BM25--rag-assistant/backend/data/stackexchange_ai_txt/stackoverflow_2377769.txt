[site]: stackoverflow
[post_id]: 2377769
[parent_id]: 2377733
[tags]: 
It won't convert automatically float to integer. Because both has different format of storage. So if you want to convert, use (int) typecasting. #include int main() { float a = 1234.5f; printf("%d\n", (int)a); return 0; }
