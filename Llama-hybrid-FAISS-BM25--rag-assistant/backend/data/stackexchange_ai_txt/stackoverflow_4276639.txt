[site]: stackoverflow
[post_id]: 4276639
[parent_id]: 4276563
[tags]: 
pList is really just an xml file so the advantages (such is both easily readable by human and computer) and disadvantages of xml (efficiency of storage/retrieval) apply. While SQLite gives you the benefit of SQL obviously. For large data SQLite would be the better choice
