[site]: stackoverflow
[post_id]: 756838
[parent_id]: 756443
[tags]: 
I like to solve some challenges in silly ways. :D This is a very unelegant, inefficient and not very safe solution. But i had fun writing it. $(function () { $(".button").each(function(){ var div = $(this) div.css({"border":"1px solid black"}); var classes = div.attr('class').split(' ').slice(1); classes.forEach( function (element) { div.append(/(width|height)(\d+)|(icon|color)(\S+)/.exec(element).filter(function (element) {return !!element} ).slice(1).join(": ").concat(" " ) ) }) }) }); Ohh, and it doesn't work in every browser. ;)
