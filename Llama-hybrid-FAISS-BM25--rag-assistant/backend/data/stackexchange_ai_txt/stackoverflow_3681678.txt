[site]: stackoverflow
[post_id]: 3681678
[parent_id]: 3681639
[tags]: 
Try changing your OnClientClick to something like this: this.OnClientClick = "if (!DerivedButton_OnClick()) return false"; This should work, although I suspect there's probably a more elegant way to do what you want.
