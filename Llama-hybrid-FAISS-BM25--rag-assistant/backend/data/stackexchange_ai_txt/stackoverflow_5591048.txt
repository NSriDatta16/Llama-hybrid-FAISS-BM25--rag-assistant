[site]: stackoverflow
[post_id]: 5591048
[parent_id]: 5590949
[tags]: 
The other answers are around changing things to static, you should avoid having mutable state in static variables this is 101 of programming - it's essentially global variables. import java.util.Scanner; public class CH6PA { private double difficulty; private int score; private double[] average; public static void main(String[]args) { CH6PA ch6pa = new CH6PA(); ch6pa.doSomething(); } private void doSomething() { Scanner keyboard = new Scanner (System.in); do { System.out.println("Enter the level of difficulty (1.2-3.8)"); difficulty = keyboard.nextDouble; } while (difficulty>1.2 || difficulty
