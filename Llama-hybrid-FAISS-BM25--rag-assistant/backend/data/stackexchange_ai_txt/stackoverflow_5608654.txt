[site]: stackoverflow
[post_id]: 5608654
[parent_id]: 
[tags]: 
Double tap on a UIButton

Possible Duplicate: Double tap on a button Hi, I find on foruns that to me detect 2 taps I use this code: - (void) touchesBegan:(NSSet *) touches withEvent:(UIEvent *) event { UITouch *touch = [touches anyObject]; if (touch.tapCount == 2) { nosi=nosi-1; if (nosi But, how I detect 2 taps in a button ?
