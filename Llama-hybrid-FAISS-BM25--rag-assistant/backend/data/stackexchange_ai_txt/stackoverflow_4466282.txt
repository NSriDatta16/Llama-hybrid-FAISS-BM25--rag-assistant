[site]: stackoverflow
[post_id]: 4466282
[parent_id]: 4466259
[tags]: 
With UTF-8 you can store any character supported by Unicode, With UTF-8 you can store any character supported by Unicode, so you shouldn't have any problem using it to store just latin1 characters (which are just a really small subset of what Unicode supports). So, for information storage you're ok; if you need to do any conversions when retrieving the data, that depends on the connector you use to get the data from the DB and on how your programming language handles string. For the update: assuming that you're using PHP to produce web pages, can't you just send the right HTTP header specifying that your page is encoded in UTF8?
