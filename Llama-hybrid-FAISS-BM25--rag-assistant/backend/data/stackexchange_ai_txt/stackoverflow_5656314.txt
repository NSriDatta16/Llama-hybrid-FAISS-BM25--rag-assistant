[site]: stackoverflow
[post_id]: 5656314
[parent_id]: 5646405
[tags]: 
Is it possible that the user agent could have been behind a firewall that truncated the ViewState? Rex Lin mentions that possibility in this thread , along with a solution based on splitting up the ViewState into separate fields. Another approach would be to implement server-side ViewState as suggested by @Nissan Fan here: Proxy server truncating View State Neither of which strictly answer the question, but may remove the underlying problem.
