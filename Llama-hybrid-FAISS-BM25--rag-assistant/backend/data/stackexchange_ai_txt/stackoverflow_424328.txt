[site]: stackoverflow
[post_id]: 424328
[parent_id]: 424311
[tags]: 
Interactive code coverage test would be great. m = 1; if (m > 0) { // do something } else { // do something else Something like this would throw up a red flag. But the code coverage test would have to be optional or unobtrusive. Since some people use Preprocessors, and DEBUG = 1;
