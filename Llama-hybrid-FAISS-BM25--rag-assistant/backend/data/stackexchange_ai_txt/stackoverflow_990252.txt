[site]: stackoverflow
[post_id]: 990252
[parent_id]: 989861
[tags]: 
MiscUtil has generic aggregate functions ( Sum , Average , etc) built in, using .NET 3.5 to provide generic operator support at runtime. Additionally, C# 4.0 makes this possible via dynamic (which supports operators), but the above approach is usually faster (less indirection).
