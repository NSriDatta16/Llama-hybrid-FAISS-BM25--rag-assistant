[site]: crossvalidated
[post_id]: 45277
[parent_id]: 
[tags]: 
Forecast of IMA(1,1) process using SES

When a time series follows an IMA(1,1) process and single exponential smoothing (SES) is used as a forecasting method, should we select a low smoothing constant ($\alpha 0.5$)? What is the intuitive interpretation?
