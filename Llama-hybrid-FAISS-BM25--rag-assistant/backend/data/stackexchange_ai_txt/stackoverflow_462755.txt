[site]: stackoverflow
[post_id]: 462755
[parent_id]: 462641
[tags]: 
It's not elegant, but it works. can anyone come up with a better solution? ("Next" functionality shown) int index = 0; foreach (TreeViewItem i in ParentItem.Items) { if (i.Equals(MyTreeView.SelectedItem)) { break; } index++; } (ParentItem.Items[index + 1] as TreeViewItem).IsSelected = true;
