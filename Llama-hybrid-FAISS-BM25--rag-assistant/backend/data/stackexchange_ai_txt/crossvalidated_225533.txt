[site]: crossvalidated
[post_id]: 225533
[parent_id]: 225528
[tags]: 
May be you can search for function approximation using neural network . Where you try to treat the function to be approximated as a black box, feed input and collect output. Then use these data to train a NN, which is more "transparent" and can be optimized.
