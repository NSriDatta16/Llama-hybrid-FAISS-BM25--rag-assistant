[site]: crossvalidated
[post_id]: 384546
[parent_id]: 384541
[tags]: 
PCA uses orthogonal transformation to generate new features on the directions that maximize the data variance, so it does not reveal the importance of the original features. Instead of PCA, random forest can be used to study the feature importance. The example using sklearn is presented here .
