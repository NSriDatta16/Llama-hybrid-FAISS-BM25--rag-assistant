[site]: stackoverflow
[post_id]: 3536693
[parent_id]: 3535655
[tags]: 
A slightly nicer way if doing this (if you don't need output from your script block) would probably be: 1..$loopcount | ForEach-Object { Measure-Command { # do some processing here } } | Measure-Object -Average TotalSeconds
