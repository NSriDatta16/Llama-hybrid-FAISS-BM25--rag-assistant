[site]: stackoverflow
[post_id]: 2238969
[parent_id]: 2236165
[tags]: 
How are you embedding the swf file? It could be the difference in and with IE causing the problem. An easy fix may be to use SWFObject http://code.google.com/p/swfobject/ This is a JavaScript file that automates the swf embed based on browser. It may be worth a try because it can fix browser issues with swf files. It is used as follows: // Thus the script places the swf in the appropriate tags for the browser being used.
