[site]: stackoverflow
[post_id]: 4449620
[parent_id]: 4448823
[tags]: 
You can try using the ArrayAccess. meaning foreach ($pager->getResults() as $data) { echo $data['username'] ; //outputs 'username' from sfGuardUser table echo ' ' ; echo $data['Profile'][0]['org_name'] ; //outputs 'Organization name' from sfGuardUserProfile table Or maybe the problem is the lacking [0] in your code, meaning echo $data->Profile[0]->org_name ; //outputs 'Organization name' from sfGuardUserProfile table
