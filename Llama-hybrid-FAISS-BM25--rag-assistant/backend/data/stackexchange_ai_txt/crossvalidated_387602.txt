[site]: crossvalidated
[post_id]: 387602
[parent_id]: 387596
[tags]: 
There isn't a single canonical captioning model, or a "correct" way to do image captioning. That's like asking whether logistic regression or SVM is the correct method to solve a binary classification task. It is possible that both models are capable of solving whatever image captioning task you might have in mind.
