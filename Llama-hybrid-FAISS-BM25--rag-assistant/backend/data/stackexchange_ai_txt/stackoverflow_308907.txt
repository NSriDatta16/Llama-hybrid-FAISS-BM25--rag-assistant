[site]: stackoverflow
[post_id]: 308907
[parent_id]: 308850
[tags]: 
There isn't strictly an average width for fonts as kerning can have an effect depending upon what letters are before and after any given letter. If you want to non-fixed sized fonts used in a fixed-width scenario, your main option is to space the characters by the width of the upper case "W" character. Others have already given an example of getting the width of a specified string, but to answer your question, to get a true average of printable characters you might want to do something like: StringBuilder sb = new StringBuilder(); // Using the typical printable range for(char i=32;i
