[site]: stackoverflow
[post_id]: 5600670
[parent_id]: 
[tags]: 
Magento- How do I echo selected product attribute (color)

I am running magento 1.4+. On the product view page, using a configurable product, How can I get the selected attribute to echo it's title. For example - I have color thumbnails and when you click it updates my dropdown, too. I would like to just echo text onclick, instead of changing the dropdown. Any tips? here is my code- getProduct(); $_attributes = Mage::helper('core')->decorateArray($this->getAllowAttributes()); ?> isSaleable() && count($_attributes)):?> getLabel() ?> * decoratedIsLast){?> class="last" > showIcons()):?>style="" name="super_attribute[ getAttributeId() ?>]" id="attribute getAttributeId() ?>" class="required-entry super-attribute-select"> __('Choose an Option...') ?> var spConfig = new Product.Config( getJsonConfig() ?>, '', optionsPrice, showIcons() ?>, ' getIconsDir() ?>', ' getIconExt() ?>', reloadeImage() ?>);
