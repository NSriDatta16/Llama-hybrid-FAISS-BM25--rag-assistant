[site]: stackoverflow
[post_id]: 3905071
[parent_id]: 3903317
[tags]: 
This is maybe a more elegant way, but it requires the times to be in seconds (that also makes it more flexible). function time_range( $start, $end, $step = 1800 ) { $return = array(); for( $time = $start; $time
