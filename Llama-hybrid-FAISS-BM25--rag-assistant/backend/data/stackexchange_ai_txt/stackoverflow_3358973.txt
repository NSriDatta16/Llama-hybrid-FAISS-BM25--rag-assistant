[site]: stackoverflow
[post_id]: 3358973
[parent_id]: 3358954
[tags]: 
You're missing some parenthesis on your if() , this: if window.localStorage["TextEditorData"] { Should be: if (window.localStorage["TextEditorData"]) { Currently it's throwing a syntax error. Here's a version of your code with the above fix , working :)
