[site]: stackoverflow
[post_id]: 2912467
[parent_id]: 2912266
[tags]: 
From Iulian's link, the source for aptly-named WTF::Vector says that the second template parameter is named inlineCapacity . So, it has something to do with some kind of optimization which should have nothing to do with functionality. Ignore it. The size of the buffer is fragmentEnd * 3 + 1 , nothing more or less.
