[site]: stackoverflow
[post_id]: 5005944
[parent_id]: 5005874
[tags]: 
Er -- inside a try/catch block? Am I missing something? try { Process p = new Process(); StringBuilder sb = new StringBuilder("/COVERAGE "); sb.Append("hello.exe"); p.StartInfo.FileName = "vsinstr.exe"; p.StartInfo.Arguments = sb.ToString(); p.Start(); p.WaitForExit(); } catch(Exception ex) { // Handle exception } ...keep in mind that the ex.Message may not contain the exeption that you specified above, while the inner exception, e.g. ex.InnerException may.
