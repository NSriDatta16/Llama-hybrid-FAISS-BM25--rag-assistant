[site]: crossvalidated
[post_id]: 338385
[parent_id]: 338245
[tags]: 
Plots like this do suggest to use k=1. And that usually is correct. It means the data - or the way it was preprocessed and prepared - is unsuitable for clustering. When visualized e.g. with PCA, such data usually looks like one (hence k=1) big blob.
