[site]: stackoverflow
[post_id]: 4921404
[parent_id]: 2927020
[tags]: 
The following piece of code worked for me well. There isn't any method that cancels only one "dragging", but you can set the position of the object using ui.position $('#draggablediv').draggable({ drag: function (event, ui) { if (your_condition) { // for example: ui.position.left
