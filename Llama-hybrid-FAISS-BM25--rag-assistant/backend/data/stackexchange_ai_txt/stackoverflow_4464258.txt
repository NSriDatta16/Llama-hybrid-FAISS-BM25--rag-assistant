[site]: stackoverflow
[post_id]: 4464258
[parent_id]: 4464236
[tags]: 
$("div.text").draggable(); is called immedately after your JSON request starts. Then later, the server responds with JSON and executes the function you pass to it. So you are making something draggable that doesnt exist yet. Put the draggable call in the callback function and you should be good.
