[site]: stackoverflow
[post_id]: 4638030
[parent_id]: 4637985
[tags]: 
(I'm assuming from the PHP tag that the following would be useful...) strip_tags() does exactly this. The sample code Test paragraph. Other text '; echo strip_tags($text, ' '); echo "\n"; ?> produces the output Test paragraph. Other text
