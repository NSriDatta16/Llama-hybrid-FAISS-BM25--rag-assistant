[site]: stackoverflow
[post_id]: 2486833
[parent_id]: 2485748
[tags]: 
I don't know why you use $('#pnganim').html(' ); . Just place this in your . $('#pnganim').append(myFish); doesn't make any sense. The draw function doesn't return a html element which could be appended to #pnganim Instead your code probably should look like this function addFish(){ $('#pnganim').html(' var fishAnim = new AnimatedPNG("fish", "fish01.png", 3, 100);fishAnim.draw(false); '); }
