[site]: stackoverflow
[post_id]: 4249753
[parent_id]: 
[tags]: 
Converting repeated subqueries to JOINS

I have the following query that repeat a select subquery with different column selections , i hope i could write it in a more elegant way. Notice that the first select query is repeated twice in the code but with different column selections. SET @date1 = '2010-01-06'; SET @date2 = '2010-07-01'; SET @qdate = '2010-07-01'; SET @period = 'WEEK'; (SELECT S.date1, S.date2, S.period, Q.market, Q.ticker, Q.close * EXP(S.ratio) AS scenario FROM portfolio.scenario S , portfolio.quote Q WHERE S.date1 >= @date1 AND S.date2 = @date1 AND S.date2 = @date1 AND S.date2 = @date1 AND S.date2
