[site]: crossvalidated
[post_id]: 204616
[parent_id]: 
[tags]: 
How can the Cross-correlation of two time series be used to measure similarity between vectors

Because cross correlation methods (like numpy.correlate) typically return vectors, how can they be used to determine the similarity of two input vectors
