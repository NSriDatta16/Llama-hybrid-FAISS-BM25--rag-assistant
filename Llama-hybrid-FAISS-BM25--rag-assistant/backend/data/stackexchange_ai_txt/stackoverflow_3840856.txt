[site]: stackoverflow
[post_id]: 3840856
[parent_id]: 3840658
[tags]: 
You can avoid using regex, saving CPU, here is an elegant and tricky way: $text_parts = explode("\n", $original_string); Now you can join it replacing line breaks with something elese: $new_string = implode(" ", $text_parts); I hope this helps
