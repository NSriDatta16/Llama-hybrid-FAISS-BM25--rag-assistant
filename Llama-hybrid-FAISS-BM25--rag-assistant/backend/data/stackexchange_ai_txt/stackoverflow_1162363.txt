[site]: stackoverflow
[post_id]: 1162363
[parent_id]: 1162306
[tags]: 
Two ways: Improve the formatting of that string Install event handlers programatically Eg: function Reply(postId) { if (!document.getElementById("reply" + postId)) { // Generate HTML var html = " "; html += " "; html += " "; html += " "; html += " "; html += " "; html += " "; // Insert into DOM $("#root" + postId).after(html); // Install event handlers $("#root .cancelButton").bind("click", function() { $("#reply" + postId).remove(); }); } } Using the DOM is methods directly is specifically discouraged by jQuery since it's so slow. Update: As Chris says, move those styles to a CSS file to further tidy this up.
