[site]: stackoverflow
[post_id]: 1669816
[parent_id]: 367617
[tags]: 
I wrote about how to achieve queries which search for a key value within a set on my blog . Here are the relevant links. Contains Operations in ADO.NET Data Services Part I Contains Operations in ADO.NET Data Services Part II Using this , you can write queries which look like this //The set in which we have to search for a match List citiesIWillVisit = new List () {"London","Berlin","Prague"}; var customersAround = nwContext.Customers .IsIn (citiesIWillVisit, c=> c.City); foreach (Customers localCustomer in customersAround) { System.Console.WriteLine(localCustomer.ContactName); }
