[site]: stackoverflow
[post_id]: 2944798
[parent_id]: 2944760
[tags]: 
Use an ArrayList. This will dynamically resize when it hits its capacity limit as you rightly say. However this should not be a problem as it does so using a sufficiently clever algorithm that the average cost per append operation is still only O(1).
