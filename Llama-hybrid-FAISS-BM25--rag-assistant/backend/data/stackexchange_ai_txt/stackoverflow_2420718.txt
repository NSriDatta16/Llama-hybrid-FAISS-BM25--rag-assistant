[site]: stackoverflow
[post_id]: 2420718
[parent_id]: 2420578
[tags]: 
This is one way, bot not terribly elegant.... Dim dataKeyNames As String = "" For Each fld As IEntityField In Me.llbDataSource.EntityCollection(0).PrimaryKeyFields If dataKeyNames <> "" Then dataKeyNames &= "," dataKeyNames &= fld.Name Next Me.grid.DataKeyNames = dataKeyNames.Split(",".ToCharArray())
