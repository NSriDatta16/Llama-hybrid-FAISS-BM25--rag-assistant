[site]: stackoverflow
[post_id]: 3876053
[parent_id]: 3876010
[tags]: 
Don't. Either Use conditional comments instead. That's the proper way to target versions of IE. Output this directly to the web page: This page may not behave correctly in your browser. I suggest you update your browser. --> Or Use the browser update javascript: var $buoop = {} $buoop.ol = window.onload; window.onload=function(){ if ($buoop.ol) $buoop.ol(); var e = document.createElement("script"); e.setAttribute("type", "text/javascript"); e.setAttribute("src", "http://browser-update.org/update.js"); document.body.appendChild(e); } It's generally agreed that parsing the User-Agent string is evil.
