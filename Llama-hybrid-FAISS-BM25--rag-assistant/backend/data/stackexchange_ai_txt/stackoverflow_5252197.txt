[site]: stackoverflow
[post_id]: 5252197
[parent_id]: 5252145
[tags]: 
Is this what you're after? It's not very elegant to read, but it's small (I haven't tested it, so tell me if it doesn't work): $('input[name=Save]').fadeTo('fast', (X > 0) ? '1' : '0') That random jargon stuff in the middle is basically what you originally wrote, but in a compact syntax. Here's a Wikipedia article about it, if you want to read it: http://en.wikipedia.org/wiki/%3F:#JavaScript
