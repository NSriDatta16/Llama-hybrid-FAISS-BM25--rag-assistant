[site]: stackoverflow
[post_id]: 1040300
[parent_id]: 1040215
[tags]: 
You can do one of these: Change the App Pool identity to a user that has SQL rights Change to SQL authentication (username + password) Add the domain\webserver$ machine account to SQL eg BOBSGARAGE\WEBSERVER1$ Enable pass through authentication so the credentials are passed from browser to SQL Server Host SQL and IIS on the same server (bad idea)
