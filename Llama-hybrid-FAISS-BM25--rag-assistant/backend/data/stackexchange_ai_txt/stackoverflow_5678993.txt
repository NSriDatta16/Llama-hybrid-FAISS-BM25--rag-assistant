[site]: stackoverflow
[post_id]: 5678993
[parent_id]: 5678890
[tags]: 
If you haven't defined any specific width for your grid columns, the text won't write (default value is Auto, so the column will increase according to the textblock width) I'd suggest you specify a fixed width to the columns(and I'd encourage you to use * and not finite pixels )
