[site]: crossvalidated
[post_id]: 383782
[parent_id]: 
[tags]: 
What's the equivalent function for mLogit() if random effects are involved?

mLogit is the function for running multinomial logistic regression in R. What if we have random effects (mixed-effects model), what's the adequate function (and package) for this purpose?
