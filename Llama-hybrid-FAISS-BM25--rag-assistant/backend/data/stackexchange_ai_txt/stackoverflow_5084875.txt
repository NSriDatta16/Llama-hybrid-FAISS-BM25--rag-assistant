[site]: stackoverflow
[post_id]: 5084875
[parent_id]: 5084156
[tags]: 
I believe the layout file's root node should be and not . P.S. If you wanted to do without jQuery - to avoid bloat - Magento already has Prototype as standard and can do exactly the same. var url = " getUrl('featured/featured/display') ?>"; document.observer('dom:loaded', function(){ new Ajax.Updater('featured-products', url); });
