[site]: crossvalidated
[post_id]: 391469
[parent_id]: 
[tags]: 
Theoretical foundation for dropout in neural networks

Can someone point me to a thorough theoretical foundation for dropout in training neural networks? So far I have found only handwaving explanations (e.g. Goodfellow's textbook and the original paper) as way to approximate an ensemble model and many empirical papers.
