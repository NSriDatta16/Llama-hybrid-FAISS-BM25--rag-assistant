[site]: stackoverflow
[post_id]: 4477414
[parent_id]: 1259489
[tags]: 
Add the following code to OnPreInit method of base page. protected override void OnPreInit(EventArgs e) { if (Request.UserAgent != null && (Request.UserAgent.IndexOf("AppleWebKit") > 0)) // added for compatibility issues with chrome { this.ClientTarget = "uplevel"; } base.OnPreInit(e); }
