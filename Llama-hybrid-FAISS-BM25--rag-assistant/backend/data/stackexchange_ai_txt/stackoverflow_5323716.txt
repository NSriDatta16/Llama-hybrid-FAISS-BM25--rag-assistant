[site]: stackoverflow
[post_id]: 5323716
[parent_id]: 5323386
[tags]: 
If you are only dragging the map, have you tried the dragend event? Otherwise, I think the use of a shared Timer is a common way to overcome this. You probably have the right idea reading your code snippet. Every trigger of the bounds_changed event resets the timer, when time's up, the timer triggers your desired function. You just need to move the timer out and share it amongs all the bounds_changed events. the APIv3 link Updates You can try: function fireEvent() { if (lastEvent.getTime() + 500 taken from here
