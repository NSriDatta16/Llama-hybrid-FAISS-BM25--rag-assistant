[site]: stackoverflow
[post_id]: 3777470
[parent_id]: 140012
[tags]: 
I like the solution from Anurag, for the bytes transfered problem I have extended the command with grep -v "bytes" ie grep "^530" ftp_out2.txt | grep -v "byte" -instead of 530 you can use all the error codes as Anurag did.
