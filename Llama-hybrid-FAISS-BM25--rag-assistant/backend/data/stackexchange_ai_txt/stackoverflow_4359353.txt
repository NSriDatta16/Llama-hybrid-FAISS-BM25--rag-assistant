[site]: stackoverflow
[post_id]: 4359353
[parent_id]: 
[tags]: 
Do something after tab is visible in jquery

I'm trying to load a function in javascript (i'm using jquery) after that my tab is it showed. So this it he code that i'm testing: $("#tabsL").html(data).tabs("destroy").tabs({ select:function(event, ui){ var section = $(ui.tab).attr("href"); var foo = section.split("-"); var looking_for = "#slideshow-carousel-"+foo[1]+"-"+foo[2]+" ul li img"; var imgangefd = $(looking_for)[0]; $(imgangefd).click(); } }); but imgangefd need to be visible to get a click. So my question is what can i do something after the tab is clicked and loaded and visible? Thanks for any clue! Roberto
