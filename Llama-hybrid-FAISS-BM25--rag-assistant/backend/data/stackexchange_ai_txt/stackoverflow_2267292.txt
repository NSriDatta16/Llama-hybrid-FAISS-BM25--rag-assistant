[site]: stackoverflow
[post_id]: 2267292
[parent_id]: 2267262
[tags]: 
Do you mean persistent storage or allocation in memory? In memory, the datastructure that you define, the way you allocate memory for your data structure (heap or stack) the compiler you use, and the C++ standard together determine how memory is allocated. Persistent storage is an altogether different story.
