[site]: stackoverflow
[post_id]: 4540372
[parent_id]: 4540304
[tags]: 
$result = preg_replace('% .*? %s', '', $subject); should work. It will remove the entire paragraph from the opening to the closing p tag. It expects that the p tag is properly closed. But you already seem to know about the drawbacks of handling HTML with regexes...
