[site]: stackoverflow
[post_id]: 473903
[parent_id]: 472040
[tags]: 
Also, other factors are probably coming into play. Filesystem caching on the machine, buffering of recently used data, etc. Best to run several tests (or several hundred!) and average out across the set, unless you're specifically measuring cold boot times.
