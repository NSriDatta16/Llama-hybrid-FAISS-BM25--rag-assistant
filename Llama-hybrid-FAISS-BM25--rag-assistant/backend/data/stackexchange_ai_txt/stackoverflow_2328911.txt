[site]: stackoverflow
[post_id]: 2328911
[parent_id]: 
[tags]: 
Select random item in unknown list

How can this be done more elegant and without creating new objects? List playerMoves = clone.getValidSuccessorMoves(0); List opponentMoves = clone.getValidSuccessorMoves(1); Move pmove = playerMoves[rand.Next(playerMoves.Count)]; Move omove = opponentMoves[rand.Next(opponentMoves.Count)]; MapManipulator.applyMove(clone.Map, pmove); MapManipulator.applyMove(clone.Map, omove); clone.Player = pmove.Destination; clone.Opponent = omove.Destination;
