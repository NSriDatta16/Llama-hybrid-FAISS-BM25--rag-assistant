[site]: crossvalidated
[post_id]: 578510
[parent_id]: 
[tags]: 
Bayesian Prior definition

The prior of an inference problem where we try to infer $x$ from observations $y$ is defined as $P(X)$ . Often ( e.g. ) I see another definition where the prior is defined as $P(X|Q)$ , what exactly is $Q$ in that context, are those the hyperparameters as shown here ?
