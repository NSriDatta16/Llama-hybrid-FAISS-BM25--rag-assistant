[site]: stackoverflow
[post_id]: 3060708
[parent_id]: 3054245
[tags]: 
There's a much more elegant and reliable approach to checking if a post is private; if ( post_password_required() ) { // It's protected and they haven't entered a password, so ask for one: the_content(); } else { // It's not protected or they have entered a password }
