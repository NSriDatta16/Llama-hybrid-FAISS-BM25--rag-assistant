[site]: stackoverflow
[post_id]: 2282400
[parent_id]: 2252030
[tags]: 
You could do as the drawing packages of old did, and display a stretchable ruler. Have your users drag the virtual ruler until it matches a physical ruler they've put against the screen. Not a serious suggestion for production use, but probably the only way to actually get the right answer :(.
