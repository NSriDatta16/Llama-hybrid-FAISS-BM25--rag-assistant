[site]: stackoverflow
[post_id]: 2251683
[parent_id]: 2250866
[tags]: 
The use of a dynamic library is similar to a Framework Apple doc on dynamic libraries The library will need to be in a known place In the target build options set the header and library search paths. To use the library drag it into Xcode (These set the -I -L and -l options to the compiler)
