[site]: datascience
[post_id]: 100178
[parent_id]: 97894
[tags]: 
Nevermind. You just flatten the cnn layers and then use concatenate function to join the labels. input_img = Input(shape=[input_shape], name='encoder_input') x = Conv2D(32, 3, padding='same', activation='relu')(input_img) x = Conv2D(64, 3, padding='same', activation='relu', strides=(2, 2))(x) x = Conv2D(64, 3, padding='same', activation='relu')(x) x = Conv2D(64, 3, padding='same', activation='relu')(x) flat = Flatten()(x) from tensorflow.keras.layers import concatenate inputs = concatenate([flat, l]) where l is one-hot encoded labels
