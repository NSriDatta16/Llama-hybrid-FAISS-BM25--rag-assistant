[site]: crossvalidated
[post_id]: 180790
[parent_id]: 
[tags]: 
Dimensions of Neural Network Output > Input

I would like to build a neural network which takes in $n$ rows and each row has $d$ columns. I'm wondering if a neural network can output $n$ rows with $p$ columns where $p > d$? How would I configure my network to do this?
