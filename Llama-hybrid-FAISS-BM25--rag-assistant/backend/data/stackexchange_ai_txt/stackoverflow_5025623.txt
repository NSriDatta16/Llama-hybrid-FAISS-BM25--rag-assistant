[site]: stackoverflow
[post_id]: 5025623
[parent_id]: 
[tags]: 
Is there a 'standard way' to parse about:blank URL's in Java?

With the following code fragment, if docurl = "about:blank", then I get a java.net.MalformedURLException thrown. I cant believe I am the first person to have to handle such urls so Im wondering if there is a different api than java.net I can use? try { URL url; url = new URL(docurl); docuri = new URI(url.getProtocol(), url.getHost(), url.getPath(), url.getQuery(), null); } catch (MalformedURLException e) { e.printStackTrace(); } catch (URISyntaxException e) { e.printStackTrace(); }
