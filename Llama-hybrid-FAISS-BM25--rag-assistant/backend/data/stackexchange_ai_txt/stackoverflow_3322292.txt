[site]: stackoverflow
[post_id]: 3322292
[parent_id]: 3322267
[tags]: 
You wouldn't really use jQuery to do the replace. Instead just use javascript's .replace() method. (I gave the paragraph an ID to specify the correct paragraph.) Try it out: http://jsfiddle.net/yRCjN/1/ HTML His veins burned gasoline. It kept his motor running but it never kept him clean. javascript/jQuery // Cache the selected element so it only runs once. var $paragraph = $('#myparagraph'); // Get the text and update it with the tag var newcontent = $paragraph.text().replace(/(gasoline)/,' $1 '); // Set the new content $paragraph.html( newcontent ); You get the content of the paragraph using jQuery's .text() Do a .replace() on the text content replacing "gasoline" with the same text wrapped in an element Use jQuery's .html() method to update the paragraph with the new content. http://api.jquery.com/text/ http://api.jquery.com/html/ Park Avenue leads to...
