[site]: datascience
[post_id]: 114760
[parent_id]: 108365
[tags]: 
You have to convert your DataFrame to a numpy array first. Try this: train_dane_rnn = np.reshape(np.array(train_dane), (train_dane.shape[0], train_dane.shape[1], 1)) test_dane_rnn = np.reshape(np.array(test_dane), (test_dane.shape[0], test_dane.shape[1], 1))
