[site]: stackoverflow
[post_id]: 4482128
[parent_id]: 4481346
[tags]: 
Since there's clearly a problem somewhere else... and since you're not fully using the potential of JQuery, I've taken the liberty of rewriting the code you've provided. Here it is : var _comment_counter = 0; function Comment(msg){ var comment = $(' ') .attr('id', "comment" + (_comment_counter++)) .text(msg); $(' ').val('reply') .appendTo(comment); $(' ').val('delete') .click(function() { deleteComment(comment); }) .appendTo(comment); $(' ') .appendTo(comment); return comment; } function addComment(comment) { $("ul#wall").prepend( $(' ').hide().append(comment).fadeIn() ); } function deleteComment(comment){ comment.parent().fadeOut('normal', function() { $(this).remove(); }); } $(document).ready(function(){ var _defaultValue = "Write a message"; $("#newMessageTxt") .height("200px") // ??? use a textarea ? .val(_defaultValue) .focus(function() { if ($(this).val() == _defaultValue) { $(this).val(''); } }) .blur(function() { if(/^\s*$/.test($(this).val())) { $(this).val(_defaultValue); } }) ; $("form#newMessage").submit(function(){ var msg = $("input#newMessageTxt").val(); if (msg != _defaultValue) { var newComment = new Comment(msg); addComment(newComment); $("input#newMessageTxt").val(_defaultValue); } return false; }); }); ** TIPS ** adopt a coding style, and use it everywhere if you're using a library, use it or else you're not respecting the point above separate code and layout; HTML is a layout, Javascript is code. You shouldn't put socks in your t-shirt drawer... if you have a delete function, you should have an add function. This goes for all CRUD operations Now, this code works, you should perhaps start from there.
