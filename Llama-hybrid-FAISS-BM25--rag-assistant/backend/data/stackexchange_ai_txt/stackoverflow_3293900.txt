[site]: stackoverflow
[post_id]: 3293900
[parent_id]: 3293429
[tags]: 
FromConfigurationSetting uses the role runtime API, which tries to read configuration settings. That will not work when running outside Windows Azure. Use CloudStorageAccount.Parse(" ") instead. (Or fetch the config setting from app.config or somewhere else and then pass it into Parse()).
