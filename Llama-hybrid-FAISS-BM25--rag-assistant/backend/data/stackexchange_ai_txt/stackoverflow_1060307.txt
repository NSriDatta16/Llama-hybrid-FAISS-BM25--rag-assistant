[site]: stackoverflow
[post_id]: 1060307
[parent_id]: 973875
[tags]: 
The most reliable way would be to actually feature-test it. Browser sniffing is fragile and unreliable. I have an example of such test in CFT http://kangax.github.com/cft/#IS_POSITION_FIXED_SUPPORTED . Note that the test should be run after document.body is loaded.
