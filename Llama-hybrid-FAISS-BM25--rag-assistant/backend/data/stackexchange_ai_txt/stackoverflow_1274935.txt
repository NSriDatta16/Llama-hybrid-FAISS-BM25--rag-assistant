[site]: stackoverflow
[post_id]: 1274935
[parent_id]: 1273718
[tags]: 
Save the Draggables in a collection and call destroy() on them when one of them is dragged (ie, onEnd is fired): var draggables = []; var products = document.getElementsByClassName('box'); for (var i = 0; i
