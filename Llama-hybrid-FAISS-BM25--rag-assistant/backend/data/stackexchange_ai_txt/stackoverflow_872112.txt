[site]: stackoverflow
[post_id]: 872112
[parent_id]: 872021
[tags]: 
There are two solutions: 1. Add to load function callback that setups events on loaded content. 2. Use live events: $("p").live("click", function(){ $(this).after("Another paragraph!"); }); http://docs.jquery.com/Events/live
