[site]: stackoverflow
[post_id]: 3746455
[parent_id]: 
[tags]: 
using map erase - Deleting from a map which contains pointer to another map

when i try to remove an element: i go to secondMap which contains in second field a pointer to the first map, now when i try to erase it it gives me problems: multimap firstMap; multimap *> secondMap; template T GarageDataBase ::Remove(SearchKey toRemove) { multimap *>::iterator it; it=secondMap.find(toRemove); multimap ::const_iterator itr; itr=(it->second);//pointer to a pair in the first map firstMap.erase(itr); } i get: error C2664: 'std::_Tree_iterator std::_Tree ::erase(std::_Tree_const_iterator )' : cannot convert parameter 1 from 'std::_Tree_const_iterator ' to 'std::_Tree_const_iterator ' error C2678: binary '=' : no operator found which takes a left-hand operand of type 'const std::pair ' (or there is no acceptable conversion) any idea?
