[site]: stackoverflow
[post_id]: 5731489
[parent_id]: 5731416
[tags]: 
Parent is a property inherited from control which is used to describe embedding relationships ( a label has as parent the form). I do not think it is set when using ShowDialog(). I assume that Owner is the correct property to check. hth Mario
