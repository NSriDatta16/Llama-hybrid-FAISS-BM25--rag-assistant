[site]: datascience
[post_id]: 107110
[parent_id]: 31491
[tags]: 
Decision trees can handle continuous variables, so this shouldn't be a disadvantage. They just make it categorical by defining thresholds to separate continuous variables into classes. However, if you have a model with continuous variables only, neural networks will probably be the better solution for such an use-case.
