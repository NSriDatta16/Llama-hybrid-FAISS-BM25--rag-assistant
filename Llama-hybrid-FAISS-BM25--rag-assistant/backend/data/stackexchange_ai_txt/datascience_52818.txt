[site]: datascience
[post_id]: 52818
[parent_id]: 
[tags]: 
How is computed the train data set score and why?

In a lot of machine learning blogs or review, the training dataset accuracy (or other metric) is given alongside the test dataset score. Is this score calculated through the training, or is the training data given at the final model to see if it gives a good score ? Moreover, what is the point of computing such a score, as the model has been trained with this data ?
