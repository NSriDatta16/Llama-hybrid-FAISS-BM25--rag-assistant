[site]: stackoverflow
[post_id]: 4605430
[parent_id]: 4605388
[tags]: 
Something like this? for (int i = 0; i Much more elegantly, public int showClassRoomDetails(String teacherName) { for (int i = 0; i Alternatively, keep a counter that gets incremented for each not true condition (on your if statement).
