[site]: crossvalidated
[post_id]: 422051
[parent_id]: 421661
[tags]: 
Goodfellow's example seems like a specific example of the bias-variance tradeoff. Fitting training data is pretty easy with modern machine learning methods; generalizing to unseen data is much harder. Striking the balance is where most of the work happens. We have some threads about overfitting which might help. What exactly is overfitting? Definition of Overfitting
