[site]: stackoverflow
[post_id]: 4543923
[parent_id]: 4540470
[tags]: 
I guess you can do this using the DragOver event private void UserControl_DragOver(object sender, DragEventArgs e) { //Verify that this is a valid drop if (!Validate()) { e.Effects = DragDropEffects.None; e.Handled = true; } }
