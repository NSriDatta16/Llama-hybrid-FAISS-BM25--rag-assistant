[site]: stackoverflow
[post_id]: 5421125
[parent_id]: 5421085
[tags]: 
var running_chrome_11 = navigator.userAgent.toLowerCase().indexOf('chrome/11') > -1; This will either be true or false. However as suggested, it is highy recommend to check against the current features, rather than the version number. An example might be: if(window.localStorage){ // you can run local storage }
