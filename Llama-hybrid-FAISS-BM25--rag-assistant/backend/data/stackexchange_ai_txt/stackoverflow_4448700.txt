[site]: stackoverflow
[post_id]: 4448700
[parent_id]: 4448644
[tags]: 
In the first script fragment, you need to refer to xml.d to access the contents of the return from your GET. success: function (xml) { var $temp_code = $(xml.d).find("test[id='" + i+ "']").find("html_code").html(); I think that this will also allow you click handler to work.
