[site]: stackoverflow
[post_id]: 697491
[parent_id]: 697370
[tags]: 
Get the server time, subtract N hours, set the local time. There is no problem. Time is a real number. Hours, minutes, seconds etc are just a presentation format. In case you have to include time zones in your calculation, use my GpTimezone ( description ).
