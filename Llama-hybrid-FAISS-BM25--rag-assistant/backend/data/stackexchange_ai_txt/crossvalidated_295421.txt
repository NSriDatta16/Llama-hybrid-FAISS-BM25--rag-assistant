[site]: crossvalidated
[post_id]: 295421
[parent_id]: 
[tags]: 
Why is it desirable to have linear separability in SVM?

Ref to above image, clearly a circle can separate the two classes(left image). Why then take so much pain to map it to a function to make it linearly separable (right image) ? Can anyone please explain ? I really couldn't find anything on the web or youtube lectures on the why
