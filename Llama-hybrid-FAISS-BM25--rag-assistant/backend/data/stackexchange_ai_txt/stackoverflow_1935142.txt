[site]: stackoverflow
[post_id]: 1935142
[parent_id]: 1935120
[tags]: 
The Colt library has a sparse matrix implementation for Java. You could alternatively use Berkeley DB as your storage engine. Now if your machine has enough actual RAM (at least 9 gigabytes free), you can increase the heap size in the Java command-line.
