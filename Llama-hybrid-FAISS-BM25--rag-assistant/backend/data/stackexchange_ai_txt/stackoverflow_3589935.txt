[site]: stackoverflow
[post_id]: 3589935
[parent_id]: 3589928
[tags]: 
Yes, it is much more efficient to use numbers, and that is the standard convention. SQL and relational databases are built for this kind of storage. For example: select students.studentid, assessments.assesName AS assesType from students inner join assessments on (students.assesType = assesments.assesType)
