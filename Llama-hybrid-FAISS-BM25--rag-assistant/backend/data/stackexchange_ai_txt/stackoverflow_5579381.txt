[site]: stackoverflow
[post_id]: 5579381
[parent_id]: 5579322
[tags]: 
Since the screen resolution is different for both devices, maybe you'd want to route them to completely different pages based on the user-agent string. That way, you can design the optimal UI for both devices.
