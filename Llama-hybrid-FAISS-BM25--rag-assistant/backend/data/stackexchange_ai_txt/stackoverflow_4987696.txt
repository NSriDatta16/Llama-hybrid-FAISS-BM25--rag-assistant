[site]: stackoverflow
[post_id]: 4987696
[parent_id]: 
[tags]: 
Why is there only ONE NoSQL Platform in .NET vs dozens in Java?

Here's a list http://en.wikipedia.org/wiki/Structured_storage I can only see one for .NET (and it doesn't even work as expected ! Sones GraphDB Query Returning Error ) Is it because .NET just don't get it yet ?
