[site]: stackoverflow
[post_id]: 2383185
[parent_id]: 2383160
[tags]: 
Try having a value greater than 0 for the amount of data to lock. Also, is there any data in the stream, if there is nothing to lock that might be the problem.... lockStream = new IsolatedStorageFileStream("my.lck", FileMode.OpenOrCreate, isoStore); lockStream.Write(.....) lockStream.Lock(0, 10);
