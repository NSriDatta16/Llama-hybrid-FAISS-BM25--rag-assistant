[site]: stackoverflow
[post_id]: 2758647
[parent_id]: 2758608
[tags]: 
A suggestion from someone's blog is to use this: if (navigator.userAgent.indexOf('Chrome/') > 0) { if (window.detwin) { window.detwin.close(); window.detwin = null; } } window.detwin = window.open(URL, 'windowname', '...'); window.detwin.focus(); Following this bug might be useful.
