[site]: stackoverflow
[post_id]: 2226573
[parent_id]: 2226562
[tags]: 
represents a paragraph and represents a 'division', I suppose the main difference is that divs are semantically 'meaningless', where as a is supposed to represent something relating to the text itself. You wouldn't want to have nested s for example, since that wouldn't make much semantic sense (except in the sense of quotations) Whereas people use nested s for page layout. According to Wikipedia In HTML, the span and div elements are used where parts of a document cannot be semantically described by other HTML elements.
