[site]: stackoverflow
[post_id]: 4639097
[parent_id]: 4638994
[tags]: 
Relying on the java.version string for anything else than showing to a human is fragile and will break if running on another Java implementation. The only reliable way programatically is to use reflection to carefully ask if a given facility is available, and then select the appropriate code accordingly.
