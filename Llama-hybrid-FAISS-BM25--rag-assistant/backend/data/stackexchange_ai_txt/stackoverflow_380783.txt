[site]: stackoverflow
[post_id]: 380783
[parent_id]: 380603
[tags]: 
I doubt ElementTree is the thing to use for this. But assuming you have strong reasons for using it maybe you could try stripping the root tag from the fragment: re.sub(r'(^ | $)' % (element.tag, element.tag), '', ElementTree.tostring(element))
