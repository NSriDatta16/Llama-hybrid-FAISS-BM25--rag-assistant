[site]: stackoverflow
[post_id]: 3466492
[parent_id]: 
[tags]: 
NHibertnate mapping HasManyToMany results in CROSS JOIN

I'm trying to add a mapping to the table IdentityGroup which has ManyToMany IdentityUsers thought the table IdentityGroupIdentitiy. However when I add the mapping. The result seems to be a something like a cross join. Expected result for a group with two users: Group 1: User 1 User 2 Current result for a group with two users: Group 1: User 1 User 2 Group 1: User 1 User 2 My mapping looks like this: mapping.HasManyToMany (x => x.Users) .Table("IdentityGroupIdentity") .ParentKeyColumn("identityGroup_cid") .ChildKeyColumn("identity_cid"); Code to fetch the data: public IQueryable Find (params string[] propertiesToLoad) { var query = session.Linq (); foreach (var propName in propertiesToLoad) query.Expand(propName); if (typeof(T) is ISoftDeletable) query.Where(x => !(x as ISoftDeletable).IsDeleted); return query; } [...] public IQueryable Groups { get { return Find (new string[] { "DefaultOrganization", "DefaultLocation", "Users" }); } } Ant ideas?
