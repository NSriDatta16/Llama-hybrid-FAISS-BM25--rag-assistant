[site]: datascience
[post_id]: 122357
[parent_id]: 
[tags]: 
feature engineering mechanism

why do we need to rescale some feature having large range I know we do it for faster rate of gradient descent ,but still how does rescaling works? and it doesn't break the model and does rescaling just decreases the time or it even improves the accuracy of the model
