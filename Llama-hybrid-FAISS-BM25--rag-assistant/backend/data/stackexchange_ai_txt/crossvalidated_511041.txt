[site]: crossvalidated
[post_id]: 511041
[parent_id]: 
[tags]: 
What's the loss function of neutral network with a vector output?

We know that usually the output of neutral network is a value. If the output is a vector such as the multi-steps prediction in time series, what's the loss function of vector? Is it simply the sum of the loss functions of each component?
