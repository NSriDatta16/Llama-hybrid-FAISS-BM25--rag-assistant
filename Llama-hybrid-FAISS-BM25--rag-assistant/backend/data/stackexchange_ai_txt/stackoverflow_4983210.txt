[site]: stackoverflow
[post_id]: 4983210
[parent_id]: 4983198
[tags]: 
It sounds like you want something like jQuery UI's .draggable feature. Through it you can let the user drag an object on the screen, access where it is, when it's stopped being moved, and so on. You can also drag according to ('snap' to) a grid.
