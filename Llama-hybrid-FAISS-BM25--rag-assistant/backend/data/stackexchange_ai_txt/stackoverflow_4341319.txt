[site]: stackoverflow
[post_id]: 4341319
[parent_id]: 4340440
[tags]: 
You need to define your own handler, which takes the key as part of the URL, or a query string, and returns the appropriate object from memcache. Remember that memcache is intended to be a cache , not a reliable storage mechanism, so you should have some way to retrieve an object even if it's been evicted from the cache.
