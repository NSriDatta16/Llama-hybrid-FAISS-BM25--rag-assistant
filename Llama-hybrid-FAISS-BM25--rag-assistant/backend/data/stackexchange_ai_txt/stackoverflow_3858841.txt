[site]: stackoverflow
[post_id]: 3858841
[parent_id]: 
[tags]: 
Input text box not editable with jQuery Draggable?

I call a javascript function that dynamically builds 's with a text input box and drop down inside. The drop down works fine with jQuery's Draggable but I cannot edit the input text box? function addField(type){ var html = ''; html += ' '; if(type == 'text'){ html += ' Text Field '; html += 'Field Name: &nbsp;&nbsp;&nbsp;'; html += 'Field Size: Small Medium Large Large 2 Lines Large 3 Lines Large 4 Lines '; } html += ' '; $("#sortableFields").append(html); $(function() { $( "#sortableFields" ).sortable({ revert: true }); $( "#draggable" ).draggable({ connectToSortable: "#sortableFields", helper: "clone", revert: "invalid", cancel: ':input,option' }); $( "ul, li" ).disableSelection(); }); } I have played around with the cancel option but it doesn't help. What do you suggest?
