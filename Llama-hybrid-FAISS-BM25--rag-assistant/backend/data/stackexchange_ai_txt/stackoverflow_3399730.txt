[site]: stackoverflow
[post_id]: 3399730
[parent_id]: 3399684
[tags]: 
You can use .closest() to make it a bit shorter, like this: $(function() { $("td.commandText pre").each(function(index) { $(this).closest('tr').prev().attr('Title', $(this).text()); }); }); Other than that, there's not much more elegance to gain here.
