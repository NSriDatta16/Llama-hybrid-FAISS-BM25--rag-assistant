[site]: stackoverflow
[post_id]: 4276580
[parent_id]: 4254250
[tags]: 
Even if you solve you compilation problems, embedding a SQLite database in the qrc file will not work. The best solution would be IMHO to include a dump of a database in the qrc file, create a memory SQLite db and rebuild the database from the SQL statements in the resource.
