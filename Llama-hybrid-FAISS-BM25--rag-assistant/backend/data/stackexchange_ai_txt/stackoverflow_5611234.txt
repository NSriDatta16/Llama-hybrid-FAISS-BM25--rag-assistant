[site]: stackoverflow
[post_id]: 5611234
[parent_id]: 5601125
[tags]: 
When you load the silverlight object, the browser is told to download your XAP file, the XAML splash file, and then the Logo.png. This PNG is loaded last in the queue. To fix that, try preloading an invisible image before embedding the tag. ... // your xap stuff goes here
