[site]: stackoverflow
[post_id]: 4301188
[parent_id]: 
[tags]: 
How to truncate a HTML fragment to a given length(for preview) in Java?

Possible Duplicate: html truncator in java How to truncate a HTML fragment to a given length(for preview) in Java ? I'm develop a personal weblog. I want to use some html parser to do that, e.g. use Xerces-J ?
