[site]: stackoverflow
[post_id]: 5665195
[parent_id]: 5665097
[tags]: 
Try this: public void DoRequestLog(HttpRequest request) { // We don’t need to log anything if there is no HTTP request. if (request == null) return; string UserAgent = request.Headers["User-Agent"]; string Date = "4/14/2011";//request.Headers["Date"]; ...
