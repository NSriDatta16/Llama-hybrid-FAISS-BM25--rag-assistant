[site]: stackoverflow
[post_id]: 3619510
[parent_id]: 
[tags]: 
jquery delete label on response

In the below code i send a json request for delete_st() on response i need that particular label and the two to be removed.How can this be done function manage_profiles() { var html = ' Profile '; {% for groupid,empname,uid in response_dict.emp_arr%} if('{{empid}}' == id) { html+=' {{empname}}&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; '; } {% endfor %} html += ' '; html+=' '; html+=' '; $dialog.html(html) .dialog({ autoOpen: true, position: 'center' , title: 'profile', draggable: false, width : 550, height : 300, resizable : false, modal : true, buttons: { "Update" : function() { var ret=validate(2,this);return ret; },"Cancel" : function() { $(this).dialog("close"); }} }); $dialog.dialog('open'); }
