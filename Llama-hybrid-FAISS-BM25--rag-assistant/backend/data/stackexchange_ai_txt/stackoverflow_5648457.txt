[site]: stackoverflow
[post_id]: 5648457
[parent_id]: 
[tags]: 
When deploying iPad App to device, it just shows a blank screen

I just created a simple iPad app and dragged a few components on its mainform. When I debug it in the Simulator it works fine, but when debugging it on an actual iPad I only get a blank screen. What may I do wrong?
