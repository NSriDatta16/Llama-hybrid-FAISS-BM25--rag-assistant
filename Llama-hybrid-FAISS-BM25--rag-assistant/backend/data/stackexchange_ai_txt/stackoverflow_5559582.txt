[site]: stackoverflow
[post_id]: 5559582
[parent_id]: 
[tags]: 
Issue inserting text into table from c# proc parameter

SqlConnection connection = new SqlConnection(cnnString); try { connection.Open(); SqlCommand command = new SqlCommand("[dbo].[tblMessages_Insert]", connection); command.CommandType = CommandType.StoredProcedure; // params SqlParameter messageText = new SqlParameter("@messageText", SqlDbType.VarChar); messageText.Value = message; // add params command.Parameters.Add(messageText); rows = command.ExecuteNonQuery(); } So this works, the row gets inserted, however, my message was "test message" , and what ends up in the db is "t" . Whats happening?
