[site]: stackoverflow
[post_id]: 2344921
[parent_id]: 2343609
[tags]: 
Try this: Step #1: Have the LinearLayout watch for touch events, and consume them Step #2: Have the thing under the LinearLayout watch for touch events -- if it gets one, you know it wasn't from the LinearLayout , so you dismiss the LinearLayout and consume that event, then perhaps unregister for touch events (or just only pay attention to them if the LinearLayout is on-screen) Step #3: There is no step #3...I think
