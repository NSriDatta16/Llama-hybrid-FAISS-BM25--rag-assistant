[site]: stackoverflow
[post_id]: 2776748
[parent_id]: 2776621
[tags]: 
Not elegant or tested but this should work... function Comma( Num ) { var period = Num.indexOf('.'); // if you want to just fail... if ( Num.length > (period + 6)) throw "too many after decimal point"; if ( period != -1 ) { Num += '00000'; Num = Num.substr( 0, (period + 6)); } // might want to replace all commas->'' before parsing, // this will remove all trailing zeros Num = parseFloat( Num.replace( ',', '') ); ....your stuff }
