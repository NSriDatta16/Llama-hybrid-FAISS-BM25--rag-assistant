[site]: stackoverflow
[post_id]: 2184986
[parent_id]: 2184840
[tags]: 
var prm = Sys.WebForms.PageRequestManager.getInstance(); prm.add_endRequest(function() { bindPageEvents(); /// insert bind function here! [I have it as an actual function() to avoid writing it out twice when I use this method] }); Will re-bind your events to elements created in an UpdatePanel. So, you just put function bindPageEvents(){ $(document).ready(function() { $("#clickableImage").click(function() { $("#specialMessage").show(); }); $("#specialMessage").draggable(); }); }); in the code at some point outside the update panel.
