[site]: stackoverflow
[post_id]: 2608201
[parent_id]: 2589922
[tags]: 
ALIAS_TO_ENTITY_MAP map worked: criteria.setResultTransformer(Criteria.ALIAS_TO_ENTITY_MAP); List itemsList = criteria.list(); if (itemsList == null || itemsList.isEmpty()) { throw new EntityNotFoundException(); } List content = new ArrayList (); Iterator iter = itemsList.iterator(); while ( iter.hasNext() ) { Map map = (Map) iter.next(); content.add((Content) map.get("contentAlias")); } return content;
