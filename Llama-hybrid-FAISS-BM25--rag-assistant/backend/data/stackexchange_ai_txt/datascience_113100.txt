[site]: datascience
[post_id]: 113100
[parent_id]: 52311
[tags]: 
use a dense layer neural network. Feed the network as many features of the used car possible as 1 or 0 for each feature that is non quantative. bin the prices into multiple categories creating a multiple input and multiple output dense layer network https://www.kaggle.com/austinreese/craigslist-carstrucks-data use the kaggle dataset to predict price.
