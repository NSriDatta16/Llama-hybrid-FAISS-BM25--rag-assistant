[site]: stackoverflow
[post_id]: 4650791
[parent_id]: 4650744
[tags]: 
Since you didn't specify what your table looks like and what would be the outcome that you would like, this is just a guess. SELECT a.post_id, a.organisation_id, b.organisation_id FROM your_table a LEFT JOIN your_table b ON a.post_id = b.post_id AND a.organisation_id
