[site]: crossvalidated
[post_id]: 392645
[parent_id]: 335793
[tags]: 
For Keras Embedding Layer, You are using supervised learning. My guess is embedding learned here for independent variable will directly map to the dependent variable. However, word2vec or glove is unsupervised learning problem. Here, embedding learned depends on data you are feeding to model.
