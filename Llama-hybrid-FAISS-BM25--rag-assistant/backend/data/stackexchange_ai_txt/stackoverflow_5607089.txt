[site]: stackoverflow
[post_id]: 5607089
[parent_id]: 5607046
[tags]: 
The obvious approach would be something like: Speed = factor / steeringAngle; Edit: oops -- I misread your request. If you want to reduce the steering angle as the speed increases, you'd want something like: if (speed > 0) steeringAngle -= factor / speed;
