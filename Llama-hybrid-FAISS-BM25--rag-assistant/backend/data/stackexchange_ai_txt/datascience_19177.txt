[site]: datascience
[post_id]: 19177
[parent_id]: 17134
[tags]: 
Remember that CNNs are feature detectors. The output of a convolutional layer is a matrix that signals where certain feature was detected. Therefore, recurrent CNNs are recurrent neural networks that learn sequences of features, where those features are also learnt during the training.
