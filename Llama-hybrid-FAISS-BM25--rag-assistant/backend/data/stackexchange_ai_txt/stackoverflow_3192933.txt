[site]: stackoverflow
[post_id]: 3192933
[parent_id]: 3192869
[tags]: 
Use zip and a frontend that unpacks the files (if needed). 10 million files is generated code. Don't. Just create on demand. [edit] Then you don't need to store the pages at all. There are data structures that let you reconstruct the original page while being fast searchable combined with using little extra storage space.
