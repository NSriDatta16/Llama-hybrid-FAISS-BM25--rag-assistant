[site]: stackoverflow
[post_id]: 1850725
[parent_id]: 1800675
[tags]: 
Well car(0) didn't work out as the NULL value will most likely choke most csv parsers. I ended using fputcsv() to build the initial file, then went through and removed all quotes. Elegant? Maybe not, but it got the job done :).
