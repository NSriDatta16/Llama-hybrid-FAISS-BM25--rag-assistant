[site]: stackoverflow
[post_id]: 5431274
[parent_id]: 
[tags]: 
Script for call routing through asterisk server

How can I get a script in which I can route a call using an asterisk server (script may be in PHP or CGI). Actually, I have an agent interface in PHP and I want to make a call using this agent interface, e.g. on a button click. How can I do this?
