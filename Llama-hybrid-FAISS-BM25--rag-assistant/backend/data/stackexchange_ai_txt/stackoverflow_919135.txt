[site]: stackoverflow
[post_id]: 919135
[parent_id]: 
[tags]: 
Checking if no elements in IEnumerable(Of T) - Linq element and quantifier operators

For my function IEnumerable LoadOpenListToProcess(CallbackSearchParams usp); This line errors when the sequence contains no elements (as it should) CallbackListRecord nextRecord = CallbackSearch.LoadOpenListToProcess(p).First(); I have changed it to the following CallbackListRecord nextRecord = null; IEnumerable nextRecords = CallbackSearch.LoadOpenListToProcess(p); if (nextRecords.Any()) { nextRecord = nextRecords.First(); } Are there better, easier or more elegant ways to determine if the IEnumerable sequence has no elements?
