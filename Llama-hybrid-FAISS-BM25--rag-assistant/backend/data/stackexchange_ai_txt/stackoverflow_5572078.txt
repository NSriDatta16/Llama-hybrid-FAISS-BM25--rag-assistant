[site]: stackoverflow
[post_id]: 5572078
[parent_id]: 
[tags]: 
SingleResult .ToList or .Count throws an InvalidCastException

I have a stored procedure GetTopRecords(). The sp returns the top 500 records. I dragged the sp from the server explorer to the LinqtoSql designer surface. I changed the return type to Record In code I have: var x = myDataContext.GetTopRecords(); var y = x.Count(); var z = x.ToList(); either one of the last two lines above throws an InvalidCastException the type of x is System.Data.Linq.SqlClient.SqlProvider.SingleResult Why am I getting an exception? What am I doing wrong? Please help.
