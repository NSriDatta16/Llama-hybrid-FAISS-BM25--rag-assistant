[site]: stackoverflow
[post_id]: 5562244
[parent_id]: 
[tags]: 
Unable to store selected fields from dropdown list to variables

I'm unable to store multiple fields selected from dropdown lists in form to variables in PHP using GET syntax. I am doing a search query list data in field based on the selection. HTML code: Transaction: Buy Sell Rent PropertyType: Land Apartment Commercial Other Location: Mumbai Thane NaviMumbai Other PHP code: $Var2 = $_GET['PropertyType']; $Var3 = $_GET['Location']; $var1 = $_GET['Transaction']; echo $var1; echo $var2; echo $var3; $trimmed1 = trim($var1); //trim whitespace from the stored variable; $trimmed2 = trim($var2); //trim whitespace from the stored variable; $trimmed3 = trim($var3); //trim whitespace from the stored variable; $db = "landshop_clientdata"; $link = mysql_connect('localhost','root'); if (!$link) { die("Couldn't connect to MySQL"); } mysql_select_db($db, $link) or die("Couldn't open $db: ".mysql_error()); $result = mysql_query("SELECT Agent_Ind,Title,firstname,lastname,Phone,email,Transaction,propertyType,Location,Area,Units,AdditionalData FROM clientdata where Transaction ='$trimmed1' AND PropertyType = '$trimmed2' AND Location = '$trimmed3' AND '$trimmed2' AND '$trimmed3") or die("SELECT Error: ".mysql_error()); //$describe = mysql_query("SHOW COLUMNS FROM $table") //$meta = mysql_fetch_field($result); //$fields_array[] = $meta->name; $num_rows = mysql_num_rows($result); print "There are $num_rows records. "; print "These are records for $trimmed1. "; //$row = mysql_fetch_assoc($result); print " \n"; //while ($get_info = mysql_fetch_row($result)) //print " \n"; //foreach ($get_info as $field) //print " name : " //print " $field "; Print " Agent or Individual "; Print " Title "; Print " First Name "; Print " Last Name "; Print " Phone "; Print " Email "; Print " Transaction "; Print " Location "; Print " Area "; Print " Units "; Print " Additional_Data_of_Property "; while($info = mysql_fetch_array( $result )) { print " \n"; Print " log in to see \n"; Print " log in to see \n"; Print " log in to see \n"; Print " log in to see \n"; Print " log in to see \n"; Print " log in to see \n"; Print " ".$info['Transaction'] . " \n"; Print " ".$info['Location'] . " \n"; Print " ".$info['Area'] . " \n"; Print " ".$info['Units'] . " \n"; Print " ".$info['AdditionalData'] . " \n"; print " \n"; } print " \n"; mysql_close($link); echo ("Want to view full contact details\n"); print( ' Login or Register ' ); echo ("Click to return to landshoppe\n"); print( ' Click Me ' );
