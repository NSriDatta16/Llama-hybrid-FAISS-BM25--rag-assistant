[site]: stackoverflow
[post_id]: 3992880
[parent_id]: 3988975
[tags]: 
To get rid of that error: The JNLP will need to declare all-permissions within a security element. The code will need to be digitally signed. Note that the JNLP file has elements out of the correct order. Use JaNeLA to check the validity of the file as well as other aspects of the launch.
