[site]: stackoverflow
[post_id]: 5550525
[parent_id]: 
[tags]: 
dropdown onchange event not working

my dropdown list event onchange doesn't work in server while it works fine on my localhost. I'm using jquery. what would be the reason. html: Basel Bern Fribourg Geneva Lugano Zurich jQuery: function change_city(){ var home_city =$("#cities option:selected").val(); $.cookie("home_city", home_city, { expires: 3650, path: '/' }); location.reload( true ); }
