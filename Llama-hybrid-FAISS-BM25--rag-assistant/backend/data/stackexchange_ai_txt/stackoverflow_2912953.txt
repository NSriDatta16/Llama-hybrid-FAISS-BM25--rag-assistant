[site]: stackoverflow
[post_id]: 2912953
[parent_id]: 2912898
[tags]: 
If the only use for the private variable is as a storage container, you might use: public string Name {get; set;} IMHO one should never make variables public - always use properties so you can add constraints or change behaviours later on whitout changing the interface.
