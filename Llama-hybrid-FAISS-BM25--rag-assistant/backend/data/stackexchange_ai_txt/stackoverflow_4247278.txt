[site]: stackoverflow
[post_id]: 4247278
[parent_id]: 4247180
[tags]: 
enums works like that by design As msdn says : The underlying type specifies how much storage is allocated for each enumerator. However, an explicit cast is necessary to convert from enum type to an integral type.
