[site]: stackoverflow
[post_id]: 2757438
[parent_id]: 2756873
[tags]: 
Parsing ls output is discouraged. Consider using a glob(): #!/usr/bin/env python import os import glob def check_distro(): print os.path.basename(glob.glob('/etc/*-release')[0]).replace('-release', '') if __name__ == '__main__': check_distro()
