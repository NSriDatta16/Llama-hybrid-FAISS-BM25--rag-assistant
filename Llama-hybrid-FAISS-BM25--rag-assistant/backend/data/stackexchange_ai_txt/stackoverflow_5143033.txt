[site]: stackoverflow
[post_id]: 5143033
[parent_id]: 5142710
[tags]: 
A trick may be placing a span after the meter tag, shifting it to the left using some arithmetic somevalue aritmetic could be something like: var average = [somevalue, say 0.45]; document.getElementById('average').style['margin-left'] = (Math.floor((average-1)*document.getElementById('m').offsetWidth))+'px'
