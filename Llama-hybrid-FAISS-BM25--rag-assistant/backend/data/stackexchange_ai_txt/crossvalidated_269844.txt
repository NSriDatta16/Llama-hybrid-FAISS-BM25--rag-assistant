[site]: crossvalidated
[post_id]: 269844
[parent_id]: 269815
[tags]: 
That depends on what you mean by "optimal". You need to choose a loss function . That said, as mentioned in the comments, logistic regression is a method for probabilistic classification rather than discrete classification, so if all you need as predicted output is a class, is logistic regression really what you want?
