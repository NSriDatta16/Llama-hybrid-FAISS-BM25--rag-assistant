[site]: crossvalidated
[post_id]: 620128
[parent_id]: 571401
[tags]: 
The data is not paired (so a paired test is not appropriate), and the predictor fare is continuous, so turning it into a large/small binary throws away useful information. Instead, this calls for a logistic regression with fare as continuous predictor and alone as binary response.
