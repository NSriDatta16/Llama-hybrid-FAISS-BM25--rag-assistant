[site]: stackoverflow
[post_id]: 4103982
[parent_id]: 
[tags]: 
Shorter way to return a variable if it exists in Ruby?

Is this a shorter, more elegant, more DRY way to write this in Ruby? if first_variable first_variable # return variable if it exists else second_variable # otherwise, return something else end Or this? if first_variable first_variable.method_name else second_variable end
