[site]: stackoverflow
[post_id]: 3360955
[parent_id]: 3360755
[tags]: 
A simple way to do it is by using the RadioButtons CheckChanged event to set a variable that specifies the file that they have chosen by using the RadioButtons text or Tag property which you could set to be the file itself? e.g. private File f = null; for (int i = 0; i It's certainly not the most elegant way but it would work.
