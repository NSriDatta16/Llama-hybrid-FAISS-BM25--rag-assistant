[site]: stackoverflow
[post_id]: 2215998
[parent_id]: 2215818
[tags]: 
You can try of using nested omp parallel fors (after omp_set_nested(1) call), but they a not supported on all openmp implementations. So I guess to make some 2D grid and start all thread on grid from single for (example for fixed 4x4 thread grid): #pragma omp parallel for for(k = 0; k
