[site]: stackoverflow
[post_id]: 4981456
[parent_id]: 4981403
[tags]: 
Do you need the number of scores or the score average? If you need the score average, you need to replace the "count" with "avg" SELECT s.matchresultid, COUNT(s.matchscoredid) FROM matches m INNER JOIN scored s ON m.matchresultid = s.matchresultid WHERE m.teamnumber = GROUP BY s.matchresultid
