[site]: stackoverflow
[post_id]: 4667957
[parent_id]: 4667438
[tags]: 
If you want to use a directory within IsolatedStorage you must first create the directory before trying to use it. using (var isoStore = IsolatedStorageFile.GetUserStoreForApplication()) { if (!isoStore.DirectoryExists("test")) { isoStore.CreateDirectory("test"); } isoStore.CreateFile("test\\" + param + ".txt"); }
