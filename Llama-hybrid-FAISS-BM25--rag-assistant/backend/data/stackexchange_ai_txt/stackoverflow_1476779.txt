[site]: stackoverflow
[post_id]: 1476779
[parent_id]: 1476722
[tags]: 
From your debug log: row: Else row.FRML_FUNC_OPTN is : Elsedecrementing indent row.FRML_FUNC_OPTN is : Elseincrementing indent I suspect you already have indentation before "Else" when you enter the code fragment supplied. Try adding: rowtext = rowtext.strip() just before the first if Or if rowtext is blank, and you're adding it to something else later on, try calling strip on that .
