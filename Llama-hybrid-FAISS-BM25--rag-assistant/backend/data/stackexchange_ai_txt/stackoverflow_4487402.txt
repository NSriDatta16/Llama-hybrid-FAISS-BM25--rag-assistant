[site]: stackoverflow
[post_id]: 4487402
[parent_id]: 4487384
[tags]: 
Either modify Person::Store() so that it calls $this->behaviourStorage->store($this) , and then examine the fields in the object passed to that method, or have it call $this->behaviourStorage->store() with the field values to store.
