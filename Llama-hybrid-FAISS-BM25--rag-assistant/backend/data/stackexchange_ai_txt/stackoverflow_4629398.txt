[site]: stackoverflow
[post_id]: 4629398
[parent_id]: 4629258
[tags]: 
In general you use sqlite like any other DB - as long as you have a provider for it. I don't know the iphone environment provider for sqlite, but it should be compatile with regular providers. (use TEXT to store the paragraph...)
