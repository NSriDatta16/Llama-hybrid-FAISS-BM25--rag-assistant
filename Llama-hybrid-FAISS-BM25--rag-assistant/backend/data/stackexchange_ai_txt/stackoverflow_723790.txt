[site]: stackoverflow
[post_id]: 723790
[parent_id]: 723008
[tags]: 
You can read html into a string, and spit it out with a placeholder / literal control. the function is like so: Dim tmp as string = GetHtmlFragement() Placeholder1.controls.add(New LiteralControl(tmp))
