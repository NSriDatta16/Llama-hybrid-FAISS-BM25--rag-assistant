[site]: stackoverflow
[post_id]: 4827596
[parent_id]: 
[tags]: 
multiple definition and namespace

Is that the right way to have functions in namespace that i will #include in multiple files? test.h #pragma once #ifndef TEST #define TEST namespace test{ namespace { bool test(){ return true; } } } #endif //TEST
