[site]: stackoverflow
[post_id]: 4955461
[parent_id]: 
[tags]: 
How to Store bulk data in 'Local Storage' using phonegap in Iphone

Im developing an appication using phonegap. The local storage of data using phonegap is done using key-value pairs and same in the case of retrieving. I want to store and retrieve bulk data. How to do it using Phonegap in Iphone. I am now using the methods to retrieve- var value = window.localStorage.getItem("key"); to store-- window.localStorage.setItem("key", "value"); Plz help me in this, Thanks in advance,
