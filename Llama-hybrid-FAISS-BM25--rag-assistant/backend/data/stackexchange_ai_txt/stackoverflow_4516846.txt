[site]: stackoverflow
[post_id]: 4516846
[parent_id]: 4516747
[tags]: 
keegan3d's answer is right as long as ur div is just after the textarea. if any other element happens to come in between them in the future , u could use this: $(function() { $(".content").focus(function() { $(this).height("50px"); //animate({"height": "50px",}, "fast" ); if ($(this).height() == 50) { $(this).nextAll(".button_block:first").slideDown("fast"); return false; });
