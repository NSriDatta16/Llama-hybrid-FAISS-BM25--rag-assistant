[site]: stackoverflow
[post_id]: 3117092
[parent_id]: 3117003
[tags]: 
var eventids = GetEventIdsByEventDate(DateTime.Now); var result = eventsdb.Where(e => eventids.Contains(e)); If you are returnning List inside the method, you should change the method return type from IEnumerable to List .
