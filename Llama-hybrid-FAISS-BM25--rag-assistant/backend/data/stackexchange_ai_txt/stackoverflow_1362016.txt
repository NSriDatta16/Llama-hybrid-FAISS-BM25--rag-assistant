[site]: stackoverflow
[post_id]: 1362016
[parent_id]: 1361394
[tags]: 
If you in 4.5 and lower, extend HorizontalFieldManager, add color property, use it on paint event and invalidate on focus change: class Scr extends MainScreen { HorizontalFieldManager mMainPanel; VerticalFieldManager mVerticalPanel; public Scr() { mMainPanel = new HorizontalFieldManager(); add(mMainPanel); mVerticalPanel = new VerticalFieldManager(USE_ALL_HEIGHT | USE_ALL_WIDTH); mMainPanel.add(mVerticalPanel); for (int i = 0; i If you in 4.6 and higher, use setBackground for VISUAL STATE FOCUS: class Scr extends MainScreen { HorizontalFieldManager mMainPanel; VerticalFieldManager mVerticalPanel; public Scr() { RadioInfo.isDataServiceOperational(); CoverageInfo.isOutOfCoverage(); WLANInfo.getWLANState(); mMainPanel = new HorizontalFieldManager(); add(mMainPanel); mVerticalPanel = new VerticalFieldManager(USE_ALL_HEIGHT | USE_ALL_WIDTH); mMainPanel.add(mVerticalPanel); for (int i = 0; i And here is a case if you want to highlight all controls (button, checkbox, choice and edit) if horizontal manager is focused: class Scr extends MainScreen { HorizontalFieldManager mMainPanel; VerticalFieldManager mVerticalPanel; public Scr() { mMainPanel = new HorizontalFieldManager(); add(mMainPanel); mVerticalPanel = new VerticalFieldManager(USE_ALL_HEIGHT | USE_ALL_WIDTH); mMainPanel.add(mVerticalPanel); for (int i = 0; i
