[site]: stackoverflow
[post_id]: 3877097
[parent_id]: 3837629
[tags]: 
You can automate your task with a bash script, and without the use of temporary files, like in this example: #!/bin/bash id="$1" file1="$2" file2="$3" # get a filename as a parameter # read first line of file to get $id position get_pos() { awk -v id="$id" '{ for (i = 1; i It do not do any error check, and maybe can be realized with other tools, like python, in a more elegant way, but I hope it help.
