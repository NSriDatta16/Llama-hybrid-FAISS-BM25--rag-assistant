[site]: datascience
[post_id]: 55307
[parent_id]: 55303
[tags]: 
You need to transform your timestamp column into several numerical/categorical columns representing that specific timestamp. After converting that timestamp column into features, you can simply throw supervised algorithms on the problem. You could leverage for example the fast.ai function add_datepart that you can find here :
