[site]: stackoverflow
[post_id]: 5665553
[parent_id]: 5665489
[tags]: 
GetCloseTeammateToTeammate returns a reference to a constant vector. Simply use: const std::vector & x = agent.GetInfoState().GetPositionInfo().GetCloseTeammateToTeammate(2); to get the reference. Note that you can only call const member functions of vector. To get a copy of the vector, do: std::vector x = agent.GetInfoState().GetPositionInfo().GetCloseTeammateToTeammate(2); Only copy if you really need to.
