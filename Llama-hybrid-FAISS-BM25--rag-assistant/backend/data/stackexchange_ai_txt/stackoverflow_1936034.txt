[site]: stackoverflow
[post_id]: 1936034
[parent_id]: 494120
[tags]: 
To make NTLM authentication work in WWW::Mechanize you need to use this format use URI; my $u = URI->new($url); my $host = $u->host; my $port = $u->port; my $hostport = "$host:$port"; $agent->$self->credentials($hostport, $realm, $user, $password);
