[site]: stackoverflow
[post_id]: 4371785
[parent_id]: 4371767
[tags]: 
In a word, the overloaded (a very common c++ feature). Just like other methods, operators may be overloaded, and as hinted by Martin York in his response, operators are little more than methods which the compiler invokes when it parses an operand and operator expression. What happens is that when applied to operands of type integer and such cout is a object of type ostream . See details for its ostream::operator Effectively the ostream::operator The reason why cout this is thought to be easier to read, and is also less error prone than say printf("Found %ld cats in the %s barn.", nbOfCats, barnName);
