[site]: datascience
[post_id]: 37216
[parent_id]: 17388
[tags]: 
Consider reusing an existing framework and adding the missing activation functions. For example, here you can see how to do that in sklearn . However, notice that there might be other differences (consider this example ) in how the algorithm is implemented, as with the rise of deep learning and convolution networks, neural networks have hugely grown in variety and complexity.
