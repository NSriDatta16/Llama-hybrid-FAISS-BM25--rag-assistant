[site]: stackoverflow
[post_id]: 2982133
[parent_id]: 
[tags]: 
Python - Blackjack

def showCards(): #SUM sum = playerCards[0] + playerCards[1] #Print cards print "Player's Hand: " + str(playerCards) + " : " + "sum" print "Dealer's Hand: " + str(compCards[0]) + " : " + "sum" compCards = [Deal(),Deal()] playerCards = [Deal(),Deal()] How can i add up the integer element of a list containing to values? under #SUM error is can combine lists like ints...
