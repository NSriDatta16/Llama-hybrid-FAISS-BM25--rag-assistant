[site]: stackoverflow
[post_id]: 2071908
[parent_id]: 2071717
[tags]: 
There is of course an unelegant and dishonorificabilitudinitatible way to achieve this, by setting a timer: var oldVal, el; function setTimer(){ setTimeout(function(){ if(el.value != oldVal){ console.log('something happened'); oldVal = el.value; } setTimer(); }, 5000); }; This will compare the value every five seconds to the value of five seconds ago.
