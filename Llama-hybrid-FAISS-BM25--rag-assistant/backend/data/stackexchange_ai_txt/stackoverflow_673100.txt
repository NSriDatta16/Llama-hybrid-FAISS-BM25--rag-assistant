[site]: stackoverflow
[post_id]: 673100
[parent_id]: 672649
[tags]: 
If I set up the bands and the data for the root band before everything, then the InitializeRowsCollection event is not raised until I do ultraGrid1.DataSource = ultraDataSource1; But once again it is raised for the Child and GrandChild Band.
