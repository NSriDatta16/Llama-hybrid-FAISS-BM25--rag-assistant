[site]: stackoverflow
[post_id]: 4233324
[parent_id]: 4232084
[tags]: 
Lets try to isolate the problem better: try running the following code: Whats the result? The reason I'm asking is this piece of code in PHPUnit/TextUI/Command.php (version 3.4.15 of PHPUnit) case '--coverage-clover': { if (extension_loaded('tokenizer') && extension_loaded('xdebug')) { $this->arguments['coverageClover'] = $option[1]; } else { if (!extension_loaded('tokenizer')) { $this->showMessage( 'The tokenizer extension is not loaded.' ); } else { $this->showMessage( 'The Xdebug extension is not loaded.' ); } } } break; And the fact that you get true if you try extension_loaded('xdebug') in a separate test script makes this a complete mystery to me. Why would the code work in separate script but not in PHPUnit?
