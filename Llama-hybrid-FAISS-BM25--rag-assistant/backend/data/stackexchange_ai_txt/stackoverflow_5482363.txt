[site]: stackoverflow
[post_id]: 5482363
[parent_id]: 
[tags]: 
modify how emacs auto-fill function works

I'm trying to edit documents (MultiMarkdown), that will do an auto-fill on paragraphs that are not indented 4 spaces, and will NOT do an auto-fill on paragraphs that ARE indented 4 spaces. Where should I start to look to figure out how to code this?
