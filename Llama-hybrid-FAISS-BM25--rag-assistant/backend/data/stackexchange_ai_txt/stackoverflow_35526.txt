[site]: stackoverflow
[post_id]: 35526
[parent_id]: 35407
[tags]: 
I'd drop the '-l' If all you plan to run the the system is this build I think the -j 4 does what you want. Based on my memory, if you have anything else running (crond?), that can push the load average over 4. GNU make ref
