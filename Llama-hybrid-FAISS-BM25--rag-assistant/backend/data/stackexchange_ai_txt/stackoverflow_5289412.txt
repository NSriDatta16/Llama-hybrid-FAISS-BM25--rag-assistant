[site]: stackoverflow
[post_id]: 5289412
[parent_id]: 5287321
[tags]: 
The collapse directive is probably what you're looking for, as described here . This will essentially form a single loop, which is then parallized, and is designed for exactly these sorts of situations. So you'd do: #pragma omp parallel for collapse(N) for(int i0=0; i0 } ... } } and be all set.
