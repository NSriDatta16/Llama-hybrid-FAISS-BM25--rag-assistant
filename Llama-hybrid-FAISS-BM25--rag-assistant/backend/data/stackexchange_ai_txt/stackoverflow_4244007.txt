[site]: stackoverflow
[post_id]: 4244007
[parent_id]: 
[tags]: 
How to make the style is applied

How to make the style is applied only to the chosen object, and not all at once? $('#add_text').mousedown ( function() { $('#add_text').css('background-color', 'blue'), $('#template').prepend(' '), $('div#text_frame').attr("class", function (arr) {return "nomber" + arr;}), $('div#text_frame').mousedown(function(){$('div#text_frame').addClass('select_text_frame');}), $('#text_frame').draggable('enable'), $('#text_frame').draggable({containment: '#template', distance: 1}); } );
