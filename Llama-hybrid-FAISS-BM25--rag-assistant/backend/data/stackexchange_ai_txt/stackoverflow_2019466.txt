[site]: stackoverflow
[post_id]: 2019466
[parent_id]: 2019455
[tags]: 
Inline elements must be enclosed inside block level elements, so this is the basic approach: Back to top Usually though the element is already inside a tag so the isn't absolutely necessary but it is more semantically correct – it's still a paragraph of text even if there's only a few words in it.
