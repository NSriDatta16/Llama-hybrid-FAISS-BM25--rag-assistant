[site]: stackoverflow
[post_id]: 2464310
[parent_id]: 2463876
[tags]: 
Applications performing backup operations can request to open files in the backup mode. What that means is that the application has the SeBackupPrivilege privilege and it opens files with the FILE_FLAG_BACKUP_SEMANTICS flag set: SeBackupPrivilege —allows file content retrieval, even if the security descriptor on the file might not grant such access. A caller with SeBackupPrivilege enabled obviates the need for any ACL-based security check. While this sounds technical and complicated, what it boils down to in practice is to copy the files using ROBOCOPY /B
