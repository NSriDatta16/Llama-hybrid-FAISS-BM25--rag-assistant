[site]: stackoverflow
[post_id]: 4774077
[parent_id]: 4774068
[tags]: 
The most obvious solution would be to use the built in SQLite support rather than a JSON based solution. This will allow you to create SQL tables that are processed "in memory" and backed to standard text files on disk for storage, hence removing the need for a MySQL, etc. database to be set up and configured.
