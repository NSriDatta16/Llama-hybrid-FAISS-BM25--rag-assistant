[site]: stackoverflow
[post_id]: 3458592
[parent_id]: 3452816
[tags]: 
the comment of musicfreak was correct. Because this feature requires domain, I had to use it only through a live url (at least localhost) and not by opening it as a file from a disk. There is no need to add window.localStorage as IE8 recognizes just localStorage as well.
