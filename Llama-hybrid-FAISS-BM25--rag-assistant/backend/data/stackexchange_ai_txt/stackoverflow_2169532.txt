[site]: stackoverflow
[post_id]: 2169532
[parent_id]: 2169509
[tags]: 
In terms of code simplicity, you might want to check out the realloc() function; you can allocate an initial array of some size, and if the user enters too many numbers call realloc() to get yourself a bigger array and continue from there. You don't, however, actually need to keep the numbers as you go along at all, at least if you only care about the average: int input; int sum = 0; int count = 0; int average; while (1) { scanf("%d", &input); if (input
