[site]: stackoverflow
[post_id]: 1175252
[parent_id]: 
[tags]: 
Can you check through JQuery if a tab of the same ID has already been created?

I have this script that creates a new tab in JQuery. Can you check through code on whether that particular tab has been generated? function createTab(name) { var tabName = "#fragment-" + name; $("#container-1 > ul").tabs("add", tabName, name); var newTab = $(tabName).css("display", "block"); newTab.html(" "); };
