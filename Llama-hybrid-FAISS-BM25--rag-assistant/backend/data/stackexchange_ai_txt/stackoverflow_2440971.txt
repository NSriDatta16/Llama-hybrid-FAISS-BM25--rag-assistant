[site]: stackoverflow
[post_id]: 2440971
[parent_id]: 2440963
[tags]: 
Deep copy wont work with clone, try using the jQuery live $(".b").live("draggable", function() { //impl }); the live event means that every object created will be checked if the selector matches and have the event added.
