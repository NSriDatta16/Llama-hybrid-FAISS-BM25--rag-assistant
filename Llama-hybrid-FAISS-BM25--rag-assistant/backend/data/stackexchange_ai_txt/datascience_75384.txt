[site]: datascience
[post_id]: 75384
[parent_id]: 
[tags]: 
What does mtry < 1 in TuneRF mean?

I'm running a random forest model using the randomForest package in R, using the TuneRF function. I have the option to set the 'step factor', which is how much the mtry parameter is changed at each interaction. It allows me to set an step factor of
