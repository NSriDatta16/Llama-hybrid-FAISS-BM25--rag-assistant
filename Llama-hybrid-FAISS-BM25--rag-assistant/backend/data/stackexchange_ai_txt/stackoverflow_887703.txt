[site]: stackoverflow
[post_id]: 887703
[parent_id]: 887689
[tags]: 
that's going to be tricky since file I/O works with bytes as the underlying unit of storage, and not lines. I mean you could just fseek() back to the beginning and clobber the earlier data, but I have a hunch that's not what you want.
