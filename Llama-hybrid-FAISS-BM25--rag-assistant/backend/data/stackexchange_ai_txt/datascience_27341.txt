[site]: datascience
[post_id]: 27341
[parent_id]: 27337
[tags]: 
Actually, I can add in a string column and should still be able to do the row selection trick even with PCA. In the file widget you set that column as text / meta and it shows up in the forward row selection widget. I think this will work.
