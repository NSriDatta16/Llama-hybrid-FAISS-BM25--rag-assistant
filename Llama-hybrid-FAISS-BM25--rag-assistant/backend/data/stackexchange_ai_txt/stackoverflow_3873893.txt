[site]: stackoverflow
[post_id]: 3873893
[parent_id]: 3873873
[tags]: 
You can use .offset() for this, for example: var offset = $("p:eq(4)").offset(); //top is offset.top //left is offset.left Or, if you need it to be more dynamic on the "which" paragraph, use .eq() like this: $("p").eq(4).offset(); You can test it out here .
