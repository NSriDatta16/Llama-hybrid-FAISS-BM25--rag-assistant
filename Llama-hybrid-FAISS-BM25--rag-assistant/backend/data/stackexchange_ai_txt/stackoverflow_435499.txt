[site]: stackoverflow
[post_id]: 435499
[parent_id]: 435446
[tags]: 
You should only pass the 'handle' param once, and it can either be a selector or an elements(/s). So, to make it work with more than one handle: $(elem).draggable({ handle: '#handle1, #handle2', }); More info / source: http://docs.jquery.com/UI/Draggables/draggable
