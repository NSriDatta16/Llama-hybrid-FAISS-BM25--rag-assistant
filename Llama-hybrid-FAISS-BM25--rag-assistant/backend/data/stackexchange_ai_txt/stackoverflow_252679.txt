[site]: stackoverflow
[post_id]: 252679
[parent_id]: 252671
[tags]: 
No, it's assumed if you omit the class specifier. The only reasonable uses I can think of would be to call attention to a particular local variable that overrides, say, a global variable with the same name, or as an interview question. Chances are, you'll confuse the poor programmer who's stuck maintaining the code!
