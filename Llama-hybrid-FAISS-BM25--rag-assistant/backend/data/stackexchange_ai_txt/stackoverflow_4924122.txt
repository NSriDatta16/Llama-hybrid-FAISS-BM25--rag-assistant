[site]: stackoverflow
[post_id]: 4924122
[parent_id]: 4923231
[tags]: 
Looks OK to me, though I think PLINQ would be a little more elegant: public IEnumerable GetAvailability (IList codes, DateTime startDate, int numNights) { return codes.AsParallel().AsOrdered().Select(code => new AvailService().GetAvailability(code, startDate, numNights)) .ToList(); }
