[site]: stackoverflow
[post_id]: 5224436
[parent_id]: 5224152
[tags]: 
Actually, I think $_SERVER['SERVER_NAME'] is more what you're looking for. HTTP_REFERER is where the user came from (as set by the useragent) See: PHP:$_SERVER - Manual You'll also need to check for both www.domain.com and domain.com (Unless you're set up to use either one or the other)
