[site]: crossvalidated
[post_id]: 325471
[parent_id]: 
[tags]: 
Which algorithm is used for the H2O AutoML metalearner

We know that h2o.automl() uses GBMs, Random Forest, DNNs, GLMs, and Extremely-Randomized Forest as base-learners, but what algorithm is used as the metalearner to combine base-learners?
