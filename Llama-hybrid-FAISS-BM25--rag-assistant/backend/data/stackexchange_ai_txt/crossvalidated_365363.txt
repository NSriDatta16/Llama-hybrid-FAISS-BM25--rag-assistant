[site]: crossvalidated
[post_id]: 365363
[parent_id]: 365056
[tags]: 
I think it is fine to include coordinates in the random Forest model. Each decision tree would split according to the loss function, therefore, observations with "high demand" and those with "low demand" would be separated. If necessary, there would be multiple splits.
