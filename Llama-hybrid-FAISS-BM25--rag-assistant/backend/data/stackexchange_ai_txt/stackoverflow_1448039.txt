[site]: stackoverflow
[post_id]: 1448039
[parent_id]: 
[tags]: 
jquery drag and drop clone

i am using following code var $albumcover = $('#albumcover'); $albumcover.droppable({ accept: '#trash2 li', activeClass: 'ui-state-highlight', drop: function(ev, ui) { $(this).append($(ui.draggable).clone().attr('alt', 'nat')); $(this).find('img').css('width','100px'); $(this).find('img').css('height','100px'); } }); how can i add events to clone element when it's created so it should be drag able again...
