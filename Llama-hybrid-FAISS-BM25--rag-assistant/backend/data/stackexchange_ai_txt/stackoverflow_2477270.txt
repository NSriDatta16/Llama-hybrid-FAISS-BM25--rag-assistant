[site]: stackoverflow
[post_id]: 2477270
[parent_id]: 2477229
[tags]: 
Fancybox allows you to load content dynamically using jQuery's Ajax. It shouldn't be different than creating your own PHP/HTML page that takes care of embedding the FLV or SWF, and dynamically loading it inside the Fancybox.
