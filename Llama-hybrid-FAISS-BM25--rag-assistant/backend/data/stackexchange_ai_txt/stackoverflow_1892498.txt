[site]: stackoverflow
[post_id]: 1892498
[parent_id]: 1892474
[tags]: 
Just use integers and division will just work for you: int xSnap = (xMouse / gridWidth) * gridWidth; int ySnap = (yMouse / gridHeight) * gridHeight; Except of course the modulo solution is so much more elegant.
