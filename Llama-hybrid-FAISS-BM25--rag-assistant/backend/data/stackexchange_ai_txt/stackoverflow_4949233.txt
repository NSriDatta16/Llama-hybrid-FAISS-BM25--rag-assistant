[site]: stackoverflow
[post_id]: 4949233
[parent_id]: 4949217
[tags]: 
Put "as SomeAlias" after the subquery: Select rating_id, average_rating From (Select rating_id, avg(rating_num) as average_rating from ratings group by rating_id having count(*) > 50) as A HAVING average_rating > 4 ;
