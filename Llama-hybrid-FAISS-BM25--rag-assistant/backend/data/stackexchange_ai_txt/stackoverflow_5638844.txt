[site]: stackoverflow
[post_id]: 5638844
[parent_id]: 5638787
[tags]: 
you could rename your webapp to ROOT (the default app for tomcat), or you can use a web server that can handle domain names and redirects (like apache httpserver), and configure your site there. The first approach is not very elegant, but quick and efficient though.
