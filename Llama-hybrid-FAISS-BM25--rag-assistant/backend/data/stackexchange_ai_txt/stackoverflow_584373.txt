[site]: stackoverflow
[post_id]: 584373
[parent_id]: 
[tags]: 
Using Firefox XUL, is is possible to eliminate the listbox border with a style?

I have the following XUL fragment, which works fine, but puts a 1 pixel border around the entire listbox. I've tried a number of CSS styles, but cannot eliminate the border. Any ideas?
