[site]: stackoverflow
[post_id]: 4399529
[parent_id]: 4399424
[tags]: 
You can use the replaceAll method instead, which returns the new content instead of the original content: $(document).ready(function() { $("a").each(function() { alert($(' New Paragraph ').replaceAll($(this)).text()); }); });
