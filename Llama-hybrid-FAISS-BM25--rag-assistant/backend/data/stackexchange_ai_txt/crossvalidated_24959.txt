[site]: crossvalidated
[post_id]: 24959
[parent_id]: 
[tags]: 
A priori selection of SVM class weights

I remember seeing/reading somewhere that for multiclass SVMs with unbalanced data, there was a way to determine the class weights from the training data (rather than X validation). Does anyone know what the method is or what paper its from? Thanks
