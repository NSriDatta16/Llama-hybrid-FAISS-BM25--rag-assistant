[site]: stackoverflow
[post_id]: 1132338
[parent_id]: 
[tags]: 
Get individual elements matching multiple selectors in jQuery

If I am selecting as follows: $('li', "#container").draggable({ /* blah */ }); How do I get the individual class attributes for those elements selected so that I can something along the lines of $('li', "#container").draggable({ /* blah */ }).attr("name", "insert name of individual img"); The markup is as follows:
