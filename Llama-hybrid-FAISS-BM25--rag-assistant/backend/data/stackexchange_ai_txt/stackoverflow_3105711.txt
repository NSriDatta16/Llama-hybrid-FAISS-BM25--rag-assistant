[site]: stackoverflow
[post_id]: 3105711
[parent_id]: 
[tags]: 
Can the .NET ReportViewer render HTML?

One of my report's text data sources contains some formatting in HTML, just the basic formatting tags such as paragraph, bold, etc. Can the ReportViewer be made to render the formatted text? If not, can anyone recommend a work-around?
