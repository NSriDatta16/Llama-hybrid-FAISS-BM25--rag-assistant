[site]: stackoverflow
[post_id]: 3389895
[parent_id]: 3389832
[tags]: 
Billing Rate * hours worked = cost price * customers gained = income if(cost >= income) don't convert. else convert. I would generally encourage the client (if its only one) to upgrade to 3.5 framework. They won't have to change any of their existing code (or, so little it should be trivial) and it's a relatively light-weight download.
