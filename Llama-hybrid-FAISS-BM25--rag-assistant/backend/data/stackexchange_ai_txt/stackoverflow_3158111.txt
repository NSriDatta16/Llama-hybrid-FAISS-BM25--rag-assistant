[site]: stackoverflow
[post_id]: 3158111
[parent_id]: 3157271
[tags]: 
I have not used this before but try below query: SELECT id, ( 3959 * acos( cos( radians(37) ) * cos( radians( lat ) ) * cos( radians( lng ) - radians(-122) ) + sin( radians(37) ) * sin( radians( lat ) ) ) ) AS distance FROM markers HAVING distance This query is working fine in SQL so just try this.. Hope it will work in SQLite. Cheers, Pragnesh
