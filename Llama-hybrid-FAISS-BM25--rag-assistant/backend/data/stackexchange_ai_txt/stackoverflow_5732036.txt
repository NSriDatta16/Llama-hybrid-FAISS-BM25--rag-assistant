[site]: stackoverflow
[post_id]: 5732036
[parent_id]: 5731851
[tags]: 
you can just forward the touch events - (void) touchesBegan:(NSSet*) touches withEvent:(UIEvent*)event{ [otherView touchesBegan:touches withEvent:event]; } and than handle the touch in the other View. Before yout touch cancels/ends the touchfunction of the superview aren't called
