[site]: stackoverflow
[post_id]: 4094581
[parent_id]: 3852985
[tags]: 
I find this solution its very simple: just go to system->import/export and then u export all product by csv now enter all other fields or attribute data in csv which u required. setCurrentStore(Mage_Core_Model_App::ADMIN_STORE_ID); $product = Mage::getModel('catalog/product'); $product->setSku($data[5]); $product->setName($data[7]); . . . try { $product->save(); echo ' hi product save successfully in magento uniqe mobile store'; } catch (Exception $ex) { echo 'Handle the error'; } }// end of if statment store }// end of while loop fclose($handle); ?> use this code and set all products but all fields set acourding to ur CSV file. then run this code . its working i test this code. its my own creation
