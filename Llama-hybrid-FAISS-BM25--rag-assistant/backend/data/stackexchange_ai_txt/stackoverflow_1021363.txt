[site]: stackoverflow
[post_id]: 1021363
[parent_id]: 1021306
[tags]: 
This works but sure you will have to tweak it some what :) html: Thai Turkish Portuguese American English Arabic Canadian French Chinese Simplified Chinese Traditional Czech Danish Dutch English Finnish French German Greek Hebrew Hungarian Italian Japanese Norweigan Polish Russian Spanish More javascript: var len = 0; var curStart = 0; var count = 5; var items=new Array(); function BackupList() { var lst = $("ul#list"); len= $("ul#list li").length; if (len " + $(items[curStart]).html() + " "; } $(html).appendTo($(lst)); } function ShowMore() { if (curStart >= len) { curStart = 0; } $("ul#list li").each(function() { $(this).remove(); }); var l = curStart; var html = ""; for (curStart; curStart " + items[curStart].html() + " "; } $(html).appendTo($("ul#list")); } Wire jquery for event: $(document).ready(function() { BackupList(); $("a#lnkMore").click(function() { ShowMore(); }); });
