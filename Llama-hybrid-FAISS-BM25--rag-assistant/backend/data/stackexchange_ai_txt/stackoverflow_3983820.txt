[site]: stackoverflow
[post_id]: 3983820
[parent_id]: 3983800
[tags]: 
This works for me: struct A { enum E { X, Y, Z }; }; template struct B { typedef typename T::E E; }; // basically "import" the A::E enum into B. int main(void) { std::cout ::E::X Output is 0 I do get a warning about non-standard extension in that qualified name so perhaps there is a more elegant solution.
