[site]: stackoverflow
[post_id]: 2968390
[parent_id]: 2865344
[tags]: 
From http://railsforum.com/viewtopic.php?pid=88198#p88198 @categories = Categories.find(:all, :joins => :animals, :select => "categories.*, SUM(animals.weight) as weight_sum", :group => "categories.id") # ATTENTION: weight_sum is now a temporary attribute of the categories returned! # and the animals are NOT eager-loaded` Category: Sum of Weight in this category: It isn't ActiveRecord.sum, but it should do the trick.
