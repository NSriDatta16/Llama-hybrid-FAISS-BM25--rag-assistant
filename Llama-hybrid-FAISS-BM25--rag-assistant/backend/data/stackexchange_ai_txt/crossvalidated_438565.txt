[site]: crossvalidated
[post_id]: 438565
[parent_id]: 
[tags]: 
What is the best way of appending dataframes in R in case column names are not known beforehand?

I usually do either this initflag if (!initflag){ mydf } or assigning a zero-length copy of first fragment to mydf before entering the loop, effectively copying all column names.
