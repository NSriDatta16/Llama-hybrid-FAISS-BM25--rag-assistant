[site]: stackoverflow
[post_id]: 4881406
[parent_id]: 
[tags]: 
How to get values of checkbox jquery in two separate arrays one contaings checked values and other unchecked

Below is the code through which I get the values of checked input boxes: $('#agent_region').click(function() { var ids = $('#agent_favourites_form input:checked').map(function(){ return this.value; }).get(); $.ajax({ type: "POST", url: "set_agent_favourites.php", data: {map: ids}, success: function(msg){ $.jnotify(msg, 3000); } }); return false; }); I want to get the values of unchecked checkboxes as well and pass it, I don't know how to pass two data: {map : ids}, {map1 : ids1} or something else. Please help.
