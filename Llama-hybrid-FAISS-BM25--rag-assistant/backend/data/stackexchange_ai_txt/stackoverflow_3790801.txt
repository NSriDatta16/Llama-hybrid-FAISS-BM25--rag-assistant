[site]: stackoverflow
[post_id]: 3790801
[parent_id]: 3790619
[tags]: 
try like this if (navigator.userAgent.indexOf("Opera")>=0) { // your code for opera } if (navigator.userAgent.indexOf("MSIE")>=0) { // for IE } if (navigator.userAgent.indexOf("Mozilla")>=0) { //for firefox } or if (document.all) { //for IE } else { // or other }
