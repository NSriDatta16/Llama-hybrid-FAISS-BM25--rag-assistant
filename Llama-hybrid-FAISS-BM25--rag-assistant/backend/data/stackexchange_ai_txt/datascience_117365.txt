[site]: datascience
[post_id]: 117365
[parent_id]: 117360
[tags]: 
It would be useful to munge the data into a typical machine learning data organization. The data should be reorganized into a tidy data frame where each individual instance is a row. From your question, it is unclear what an instance is. Only the features (i.e., 'Property') need to be encoded.
