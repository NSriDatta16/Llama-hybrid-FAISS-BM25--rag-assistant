[site]: crossvalidated
[post_id]: 405890
[parent_id]: 405660
[tags]: 
You used fit_transform() function which fits then transforms the training data. Instead, you can just do fit() to fit the pca object to train data, then apply transform() to a new input data. Obviously, make sure that the new dataframe has the same exact structure as the one you used to fit the PCA
