[site]: stackoverflow
[post_id]: 2678062
[parent_id]: 
[tags]: 
Javascript opens new window and loads _self

This code loads the href into a new window and into self, why is this? Shouldn't return false; stop the default click? //get links in paragraphs var first_p = $$('#client_work p a'); //get first link var client_link = first_p[0]; if(client_link) { $(client_link).observe('click', function() { var mylink = window.open(client_link.href, 'new_window', '_self'); return false; }) }
