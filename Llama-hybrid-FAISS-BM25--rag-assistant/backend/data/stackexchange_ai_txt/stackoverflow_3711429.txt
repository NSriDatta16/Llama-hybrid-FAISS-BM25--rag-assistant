[site]: stackoverflow
[post_id]: 3711429
[parent_id]: 3711377
[tags]: 
Group 1 will say it is horrible practice since it is hard to follow. Group 2 will say do it. Group 3 will say do it, but in 1 line Group 4 will say do not use the else Group 5 will say to do not use the return, just use the if around the code you want to run. AKA: if (navigator.userAgent.match(/iPad/i) === null) { //Usual script here }
