[site]: datascience
[post_id]: 85387
[parent_id]: 85384
[tags]: 
Their weights are also trained as well as the weights of the CNN filters. So, the gradient of the loss is computed with respect to these weights also, and at each gradient step, both of the dense (fully connected) and CNN layers' weights are updated.
