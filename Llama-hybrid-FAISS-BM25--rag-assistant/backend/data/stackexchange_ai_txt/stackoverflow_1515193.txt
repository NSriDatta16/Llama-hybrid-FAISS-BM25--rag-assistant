[site]: stackoverflow
[post_id]: 1515193
[parent_id]: 
[tags]: 
converting a matrix of lists to a regular matrix

Take the following code: foo In this case, result[,'a'] shows a list. Is there a more elegant way such that result is a "regular" matrix of vectors? I imagine there are manual ways of going about this, but I was wondering if there was an obvious step that I was missing.
