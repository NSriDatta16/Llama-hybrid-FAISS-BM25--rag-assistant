[site]: stackoverflow
[post_id]: 3639200
[parent_id]: 3638952
[tags]: 
This would my solution, althoug is not elegant at all query(" SELECT p.Group as 'group', GROUP_CONCAT(name) as names FROM prueba p GROUP BY p.Group "); ?> Group Name fetch_assoc()){ $names = split(",",$row["names"]); ?>
