[site]: stackoverflow
[post_id]: 1894928
[parent_id]: 1894846
[tags]: 
A version that doesn't require extra storage: std::deque bfs; bfs.push_back(start); int nodesInThisLayer = 1; int nodesInNextLayer = 0; while (!bfs.empty()) { Node front = bfs.front(); bfs.pop_front(); for (/*iterate over front's children*/) { ++nodesInNextLayer; nodes.push_back(child); } std::cout P.S. sorry for the C++ code, I'm not very fluent in Python yet.
