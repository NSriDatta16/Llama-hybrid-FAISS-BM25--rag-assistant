[site]: stackoverflow
[post_id]: 1722933
[parent_id]: 1722774
[tags]: 
Check that the page you are fetching is an HTML fragment, not a normal page with , and everything else. ColorBox would probably choke on a full page. e.g. check the source of the example page used on the official Colorbox site.
