[site]: stackoverflow
[post_id]: 5515796
[parent_id]: 3657721
[tags]: 
Using the minutes is a better solution, even if you have to line them up on the front-end. It's cleaner from a storage perspective (one column rather than being redirected to the second column for manual settings) and allows more flexibility if the requirements change later (e.g. 1/4 day, 2 days).
