[site]: stackoverflow
[post_id]: 5183278
[parent_id]: 5183185
[tags]: 
Try: for k in [w for w in W[a] if w You need to use the if clause within a list comprehension (but as noted in the comment, this is not the optimal way to do this). Edit: If you are looking to learn some of the 'awesome' features of python, you could also try using itertools : for k in itertools.ifilter(lambda x: x There are many other solutions, some more or less elegant and efficient than others. If you want to get really awesome and you are basically using array structures, look into numpy to unlock a whole world of elegance and speed.
