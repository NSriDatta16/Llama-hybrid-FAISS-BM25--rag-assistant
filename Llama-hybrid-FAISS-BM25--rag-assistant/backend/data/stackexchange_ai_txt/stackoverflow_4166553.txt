[site]: stackoverflow
[post_id]: 4166553
[parent_id]: 4166507
[tags]: 
The engine is per table, not per database. Any tables that you created before changing the default storage engine will need to be converted over to the new type. Only new tables will use the new default type unless you manually change the old ones. You can change the storage engine using ALTER TABLE TableName ENGINE=MyISAM
