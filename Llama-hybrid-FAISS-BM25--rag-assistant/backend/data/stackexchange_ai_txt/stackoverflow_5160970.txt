[site]: stackoverflow
[post_id]: 5160970
[parent_id]: 
[tags]: 
DB URL to a Record? How-to?

Is it plausible to think we can access the contents of a database field or fields via a URL? For ie, can "http://dbms_node:port/database/tablename?query" work? I guess the limitation may be where we can specify the selectors? Can that be done via url fragments?
