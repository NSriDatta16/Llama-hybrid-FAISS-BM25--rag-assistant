[site]: stackoverflow
[post_id]: 5104854
[parent_id]: 5104482
[tags]: 
Magento have changed the way the prices are calculated in the cart which makes it very difficult to do this in v1.4 onwards. If you do set the price using an Observer or other device, it will almost certainly be overwritten back to the catalog price. Effectively, you need to use Shopping Cart rules to implement this.
