[site]: stackoverflow
[post_id]: 4419121
[parent_id]: 4210131
[tags]: 
The value $frags is declared outside of the code you've pasted (in the calling scope of the call to ::clause(), probably). This happens quite often in a variable name conflict. Consider this: next()) { //... } // the last value returned from $query->next() was false in this example // .. more stuff $this->clause($frags); // actually passes false, not the apple,orange array This happens to the best of us (-:
