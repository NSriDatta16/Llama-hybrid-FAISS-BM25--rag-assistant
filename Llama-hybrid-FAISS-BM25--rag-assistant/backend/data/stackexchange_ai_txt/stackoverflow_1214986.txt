[site]: stackoverflow
[post_id]: 1214986
[parent_id]: 1213813
[tags]: 
If that is all you want to do with the grid, you could emulate it: $("#myobj").draggable({ ... stop: function(event, ui) { t = parseInt($(this).css(top); l = parseInt($(this).css(left); $(this).css(top , t - t % 30); $(this).css(left, l - l % 30); } ... });
