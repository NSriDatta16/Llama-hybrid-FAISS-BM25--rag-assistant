[site]: stackoverflow
[post_id]: 895347
[parent_id]: 885258
[tags]: 
For a basic NLP approach, you could represent each document as a vector based on word frequencies, then cluster the document vectors using Bayesian or other methods (SVM, k-means, etc). For related answers, see this somewhat similar SO question .
