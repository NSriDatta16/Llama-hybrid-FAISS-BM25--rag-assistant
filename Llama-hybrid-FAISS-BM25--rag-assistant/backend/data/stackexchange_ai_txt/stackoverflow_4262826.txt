[site]: stackoverflow
[post_id]: 4262826
[parent_id]: 
[tags]: 
DIO Control Code Needed

Does any one have the constant value of IOCTL_STORAGE_GET_MEDIA_SERIAL_NUMBER for use with the dll import in a C# class? I was told the constants are in WinIoCtl.h, but they are nowhere to be found. If someone could provide it, it would be appreciated greatly.
