[site]: stackoverflow
[post_id]: 2515390
[parent_id]: 2515254
[tags]: 
I second the opinion of Dan, too and Fredrik and to add to it - an attempt to multithread IO against a single disk can potentially instead of improving performance make things worse. Access requests from parallel threads can increase disk thrashing which will make data retrieval from the disk slower than it is now
