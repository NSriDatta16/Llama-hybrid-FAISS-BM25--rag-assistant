[site]: stackoverflow
[post_id]: 2214447
[parent_id]: 2213740
[tags]: 
You could do it by moving everything into a DataTemplate and using Trigger s there. I would probably consider Robert's suggestion that this be fixed in a ViewModel or some other bound object because it sounds more like it might be more business logic than just UI. That said: R1 R2 R3 R4
