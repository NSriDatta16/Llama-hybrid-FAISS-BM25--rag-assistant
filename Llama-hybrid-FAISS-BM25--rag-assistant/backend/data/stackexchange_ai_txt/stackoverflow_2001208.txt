[site]: stackoverflow
[post_id]: 2001208
[parent_id]: 2001014
[tags]: 
Given this XML USA,UK,Spain India,USA,China Here's an XSLT2 stylesheet that will do what you want It first builds a temporary tree (document fragment) containing one element for each token, then uses distinct-values() to remove duplicates. I coded and tested this in Oxygen/XML.
