[site]: stackoverflow
[post_id]: 1438614
[parent_id]: 1438542
[tags]: 
Why not just use Python's urllib2 module to create a POST request, like they show in an example for PHP. It would be something like this: import urrlib, urllib2 data = ( ('name', 'torrent'), ('type', 'application/x-bittorrent'), ('file', '/path/to/your/file.torrent'), ) request = urllib2.urlopen('http://torrage.com/autoupload.php', urllib.urlencode(data))
