[site]: datascience
[post_id]: 107733
[parent_id]: 94680
[tags]: 
Mean squared error (MSE) is a metric for the difference between observed and actual target values, typically used for regression. There are no actual target values (aka, labels) in embeddings, thus MSE is not appropriate. Cosine similarity can compare vectors so it is appropriate embeddings.
