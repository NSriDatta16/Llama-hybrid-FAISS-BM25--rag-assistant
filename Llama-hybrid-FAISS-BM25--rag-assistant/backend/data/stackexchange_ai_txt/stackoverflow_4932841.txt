[site]: stackoverflow
[post_id]: 4932841
[parent_id]: 
[tags]: 
NHibernate - setting long string as a parameter for query

I'm constructing a query like this: private void Test() { ISession session = sessionFactory.OpenSession(); var query = session.GetNamedQuery("testQuery"); string s = BuildVeryLongString(); // length more that 4000 chracters query.SetParameter("Param", s, NHibernateUtil.String); query .SetResultTransformer(new AliasToBeanResultTransformer(type)) .List (); } In the DB-profiler I can see that the type of Param is nvarchar(4000) that is not enough. Is there a way to say NHibernate that I'm going to use longer strings? P.S. DB is MSSQL 2005. Thanks in advance!
