[site]: stackoverflow
[post_id]: 5732344
[parent_id]: 5732027
[tags]: 
The keys in a dictionary are unique. So if agentrefattr is a string representing a key, yuu will only ever have one object in the dictionary with that key, so there is no need for a loop at all. Just do this: aPerson = [personDict objectForKey: agentrefattr];
