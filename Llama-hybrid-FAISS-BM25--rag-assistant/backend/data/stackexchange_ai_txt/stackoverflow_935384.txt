[site]: stackoverflow
[post_id]: 935384
[parent_id]: 935299
[tags]: 
It seems very bizarre to me that you'd be getting that error (though my main experience isn't with mysql). I'd try with aliasing your tables. SELECT s.systemName, o.officeSymbol FROM system as s LEFT JOIN (organization as o) ON (s.idOrganization = o.id)
