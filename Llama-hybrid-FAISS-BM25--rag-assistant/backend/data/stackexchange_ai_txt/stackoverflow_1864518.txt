[site]: stackoverflow
[post_id]: 1864518
[parent_id]: 1864477
[tags]: 
There's probably no non-ugly way to do this. That said, using the user-agent detection provided by a library like YUI ( relevant YUI doc ) will arguably result in slightly clearer and more explicit code than the above hack. Something like: if (YAHOO.env.ua.ie >= 6 && YAHOO.env.ua.ie Ugly, yes. But it's pretty clear what the intent is.
