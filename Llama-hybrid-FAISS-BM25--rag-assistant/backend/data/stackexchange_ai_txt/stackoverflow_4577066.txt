[site]: stackoverflow
[post_id]: 4577066
[parent_id]: 
[tags]: 
this program strip would output a searched directory but if not found must output a message

this program must output a directory that you searched for how ever if its not found a message must appear that the org_name is not found,i don't know how to do that, i keep trying on some if-else but it just won't output it. '; $result1 = $result; echo ' '.' '.'Name'.' '.' '.'Organization Name'.' '.' '.'Position'.' '.' '.'Cell Number'.' '.' '.'Email-Add'.' '; echo ' '; while ( $row = mysql_fetch_array($result1) ){ echo ' '.' '.$row['name'].' '.' '.$row['org_name'].' '; echo ' '.$row['position'].' '.' '.$row['cell_num'].' '.' '.$row['email_add'].' '; echo ' '; } echo ' '; ?>
