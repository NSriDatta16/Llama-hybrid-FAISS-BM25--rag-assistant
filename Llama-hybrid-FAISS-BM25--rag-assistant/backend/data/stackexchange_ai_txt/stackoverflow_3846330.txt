[site]: stackoverflow
[post_id]: 3846330
[parent_id]: 3845524
[tags]: 
you can use xml dom parser under packages org.w3c.dom and javax.xml with that you can easily parse the document and get the node contents Document doc = DocumentBuilder.parse(file); and then get the elements by using NodeList nl = doc.getElementsByTagName("p"); // for paragraph tags and then get the content from nodelist, it'll give u whole content in paragraph tag, like that you can apply for any tag
