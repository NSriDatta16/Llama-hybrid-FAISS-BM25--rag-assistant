[site]: stackoverflow
[post_id]: 2230016
[parent_id]: 2229551
[tags]: 
This is a little kludgy, but it works: vim -c "wincmd J" -O base diff1 diff2 (That's a verbatim control-W there) Maybe there's a more elegant method, but this simply loads all of them as vertical and then moves the active (first) one to the bottom.
