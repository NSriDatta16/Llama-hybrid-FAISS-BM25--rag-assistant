[site]: stackoverflow
[post_id]: 1055100
[parent_id]: 1055078
[tags]: 
When using AES, the output data will be rounded up to have a specific length (e.g a length divisible trough 16). If you want to transfer secret data to another website, a HTTP post may do better than embedding the data into the URL.
