[site]: crossvalidated
[post_id]: 642867
[parent_id]: 
[tags]: 
What's the best way to do cross-validation?

Assume that 3-fold CV is used for comparing performance of logistic regression, SVM, and KNN for a 2-class problem. For each fold, the logistic regression provides the following confusion matrices: Is it common to add confusion matrices together (shown below) in order to determine accuracy?
