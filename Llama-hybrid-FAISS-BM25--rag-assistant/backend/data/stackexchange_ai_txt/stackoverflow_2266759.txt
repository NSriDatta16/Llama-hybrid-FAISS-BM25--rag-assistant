[site]: stackoverflow
[post_id]: 2266759
[parent_id]: 2266594
[tags]: 
here's the logic. go through the file, check for at the start of the line, if not there, construct a new string with the and and echo it out. There's really no need for complicated regex with bash #!/bin/bash shopt -s extglob while read -r line do case "$line" in " $line ";; esac done with awk $ awk '!/^ "$0" "}{print}' file output $ awk '!/^ "$0" "}1' file Paragraph text one one line [... more ... ] Other paragraph text on the next line [... more ... ] html element thrown in on its own line More paragraph text [... more ... ] There is still more text!
