[site]: stackoverflow
[post_id]: 4804956
[parent_id]: 2933284
[tags]: 
The storage must to be bigger, I thought that cvMat isn't dinamically allocated so you have to for example change the line: storage = cv.CreateMat(1, 2, cv.CV_32FC3) to: storage = cv.CreateMat(1, img.rows * img.cols, cv.CV_32FC3)
