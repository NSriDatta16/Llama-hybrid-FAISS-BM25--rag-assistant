[site]: stackoverflow
[post_id]: 3358056
[parent_id]: 
[tags]: 
Multiple 's in a row using Markdown Syntax?

I'm trying to include multiple blockquotes using markdown, but I'm stuck on the syntax for closing out the initial blockquote and starting a new one instead of continuing in the current blockquote and adding additional paragraphs... === Current syntax === > Review1 > -- Person1 > Review2 > -- Person2 === Current result === Review1 -- Person1 Review2 -- Person2 === Wanted result === Review1 -- Person1 Review2 -- Person2
