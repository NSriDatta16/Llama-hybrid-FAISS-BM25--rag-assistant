[site]: crossvalidated
[post_id]: 587855
[parent_id]: 438917
[tags]: 
In both GLMs and nnet::multinom it is the case that covariates should be scaled to make sure that the fit converges. So just adding scale() in your model formula's will solve your problem, and will then result in similar SEs on the scaled covariate: library(MASS) example(birthwt) ######## Base Line glm logistic Regression ############# glm_model
