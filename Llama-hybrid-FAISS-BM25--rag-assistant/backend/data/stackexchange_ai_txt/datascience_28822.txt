[site]: datascience
[post_id]: 28822
[parent_id]: 
[tags]: 
Why do we have multiple neurons in the output layer of a neural network?

Why do we have two neurons in the output layer? What does each neuron mean? If our classifier is a binary classifier, will we have only one neuron in the output layer? Here is a picture of the neural network:
