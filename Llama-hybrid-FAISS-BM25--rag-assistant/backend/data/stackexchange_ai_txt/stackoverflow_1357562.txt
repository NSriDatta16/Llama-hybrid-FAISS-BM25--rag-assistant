[site]: stackoverflow
[post_id]: 1357562
[parent_id]: 1357427
[tags]: 
If you can guess what it's original position was in the fail block, just use a selector to find it and the manipulation functions to remove it from its current position and move it to the old one. If you need to, perhaps you can store the original position (parent and child-index) when it is dragged, then move it back there when it fails
