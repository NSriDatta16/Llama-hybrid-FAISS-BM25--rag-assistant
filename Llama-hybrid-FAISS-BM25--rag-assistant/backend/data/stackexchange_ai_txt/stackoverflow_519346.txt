[site]: stackoverflow
[post_id]: 519346
[parent_id]: 517137
[tags]: 
some cotent more content yet more content drop it here $(document).ready(function() { $('.draggableDiv').draggable({helper: 'clone'}); $('.droppableDiv').droppable({ accept: '.draggableDiv', //changed this, it was originally 'draggableRecipe' drop: function(ev, ui) { //get the index of dropped draggable div var divIndex = $('div').index(ui.draggable) alert(divIndex) //alert the divIndex to see the result ui.draggable.clone().fadeOut("fast", function() { $(this).fadeIn("fast") }).appendTo($(this).empty()); } }); }); Tested this one, it works for me and hope will work for you too. good luck
