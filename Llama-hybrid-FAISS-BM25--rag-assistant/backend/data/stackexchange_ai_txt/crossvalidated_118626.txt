[site]: crossvalidated
[post_id]: 118626
[parent_id]: 
[tags]: 
In convolutional neural network, what does fully-connected layer mean?

There are convolution layers, pooling layers, and possibly a classifier layer (e.g. softmax layer) in a convolutional neural network (CNN). I heard that there is also a fully-connected layer. What is that?
