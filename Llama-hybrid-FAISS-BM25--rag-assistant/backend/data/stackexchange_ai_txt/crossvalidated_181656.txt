[site]: crossvalidated
[post_id]: 181656
[parent_id]: 
[tags]: 
Problems in finding outliers and leverage points in non-linear regression

I'm implementing diagnostic of non-linear regression model $(y=ax^b)$. I'm trying to find out where outliers and leverage points in my model (Regr.nls To find outliers I use the following code: library(plyr) library(ggvis) Regr %>% mutate(rstud = ifelse(abs(rstudent(Regr.nls)) > 3, 'Больше 3', 'Не больше 3'), label = row.names(Regr), label = ifelse(rstud == 'Больше 3', label, '')) %>% ggvis(~NV, ~ PIR, fill = ~rstud) %>% layer_points() %>% add_axis("x", title = "Километров на литр") %>% add_axis("y", title = "Лошадиных сил") %>% add_legend("fill", title = "Стьюд.остатки") %>% layer_model_predictions(stroke := "red", model = 'lm') %>% layer_text(text := ~label, dy := -5, dx := 5) However, there is an error: Error in UseMethod("rstudent") : no applicable method for 'rstudent' applied to an object of class "nls" How can I solve this problem?
