[site]: crossvalidated
[post_id]: 324687
[parent_id]: 324680
[tags]: 
Without looking at your data, it seems that the only method you have used is stl(). There are other models available in the forecast package, including ets() (exponential smoothing methods), auto.arima() (arima models), nnetar() (neural networks). That would be the first thing to try to see if your can improve your forecasts.
