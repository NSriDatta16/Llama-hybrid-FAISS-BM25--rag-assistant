[site]: stackoverflow
[post_id]: 3443704
[parent_id]: 3443677
[tags]: 
No, unless: The attacker had access to the storage of the session variables (usually the filesystem of the server, but could also be e.g. a database) The attacker intercepted a session cookie of a more privileged user. The attacker successful fixated the session of a more privileged user (see session fixation attacks).
