[site]: stackoverflow
[post_id]: 4803914
[parent_id]: 4802253
[tags]: 
The AssociationSet is a container for an association. Lets began from a simle case when we have a Contract and Address types. Its relationship could be described in xml like: As you can see here, one Contact can have many Addresses. We here describe that there exists a reference between two types. But how we can describe a case when in Contract type we can have, actually, two or more addresses. For example, WorkAddress and HomeAddress. Within Association we can describe only fact that two types have referenced each other, but within AssociationSet we can also describe that one type could use two equal references to other type. For such case we can define next xml:
