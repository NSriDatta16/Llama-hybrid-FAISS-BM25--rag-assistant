[site]: stackoverflow
[post_id]: 2298185
[parent_id]: 2297025
[tags]: 
Why don't you try the direct mode? '124','in1'=>'1'); $result = $client->createOrder( $parms ); print_r($result); Instead of using $client->__SoapCall('createOrder',array($params)); As the manual says: "Usually, in WSDL mode, SOAP functions can be called as methods of the SoapClient object"
