[site]: stackoverflow
[post_id]: 3207325
[parent_id]: 3207278
[tags]: 
You are quite limited in HTML. You can use iframe tag but it's the same type of embedding as embedding of flash in html pages. OT: It would be quite easy in PHP. Can you use it? Or do you need static web page?
