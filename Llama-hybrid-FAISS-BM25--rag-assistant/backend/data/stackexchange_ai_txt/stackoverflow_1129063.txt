[site]: stackoverflow
[post_id]: 1129063
[parent_id]: 1129048
[tags]: 
Your code looks plausible, but in the following line; Fragment *frag=malloc(10*sizeof(Fragment)); Are you sure you need 10* ? If you need to allocate 10 Fragments, then you should take responsibility for initializing all 10.
