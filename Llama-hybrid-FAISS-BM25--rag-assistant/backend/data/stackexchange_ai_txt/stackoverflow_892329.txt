[site]: stackoverflow
[post_id]: 892329
[parent_id]: 
[tags]: 
check if div contains an element in jQuery

I'm bulding some drag and drop widgets in jQuery, once they've been dropped I need to check if my drag and droppable widget are inside another div. I'm a widget! I've had a look at $('#droptarget').each but can't seem to figure it out. Any ideas?
