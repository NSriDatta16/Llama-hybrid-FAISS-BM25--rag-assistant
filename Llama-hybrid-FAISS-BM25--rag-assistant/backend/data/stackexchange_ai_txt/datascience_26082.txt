[site]: datascience
[post_id]: 26082
[parent_id]: 12055
[tags]: 
A recurrent neural network can take unstructured data, such as video or raw text, to make a prediction. For example, the model can use as input the text from a large set of emails, and from there try to forecast something.
