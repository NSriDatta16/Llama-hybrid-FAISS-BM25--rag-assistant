[site]: stackoverflow
[post_id]: 3107385
[parent_id]: 3107342
[tags]: 
Don't use rand() , use mt_rand() . It uses a random number generator with known characteristics using the Mersenne Twister , which will produce random numbers four times faster than what the average libc rand() provides.
