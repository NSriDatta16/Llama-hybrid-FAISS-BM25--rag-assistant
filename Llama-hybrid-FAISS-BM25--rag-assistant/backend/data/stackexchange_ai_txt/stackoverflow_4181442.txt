[site]: stackoverflow
[post_id]: 4181442
[parent_id]: 3599119
[tags]: 
Override ScrollView with one that you can enable/disable //A scrollview which can be disabled during drag and drop public static class OnOffScrollView extends ScrollView { private boolean on = true; public OnOffScrollView(Context context) { super(context); } public OnOffScrollView(Context context, AttributeSet attrs, int defStyle) { super(context, attrs, defStyle); } public OnOffScrollView(Context context, AttributeSet attrs) { super(context, attrs); } //turn on the scroll view public void enable() { on=true; } //turn off the scroll view public void disable() { on = false; } @Override public boolean onInterceptTouchEvent(MotionEvent ev) { if (on) { return super.onInterceptTouchEvent(ev); } else { return false; } } } Disable it in your MotionEvent.ACTION_DOWN case, enable it again in the MotionEvent.ACTION_CANCEL and MotionEvent.ACTION_UP cases
