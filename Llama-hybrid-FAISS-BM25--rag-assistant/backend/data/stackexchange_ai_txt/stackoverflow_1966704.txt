[site]: stackoverflow
[post_id]: 1966704
[parent_id]: 1966696
[tags]: 
Figuring this out manually wouldn't too hard in this case. Determine the average net speed of your users (you should do this anyway). Determine (based on that speed) how long it would take to load a file the size of the large file. Determine (based on that speed) how long it would take to load a file the size of the small file. Subtract.
