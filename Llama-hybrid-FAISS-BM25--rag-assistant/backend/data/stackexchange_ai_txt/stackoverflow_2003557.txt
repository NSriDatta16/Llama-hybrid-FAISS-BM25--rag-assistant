[site]: stackoverflow
[post_id]: 2003557
[parent_id]: 2003313
[tags]: 
You haven't named any parameters in your callback function. You need to name them in order to use them: $(document).ready(function(){ $('ul#sortable').sortable({ handle:'.drag', axis:'y', update:function(event, ui){ alert($(ui.item).text()); } }); });
