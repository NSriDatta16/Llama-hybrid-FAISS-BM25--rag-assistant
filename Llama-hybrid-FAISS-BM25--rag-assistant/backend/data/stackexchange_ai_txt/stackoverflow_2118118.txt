[site]: stackoverflow
[post_id]: 2118118
[parent_id]: 2116493
[tags]: 
You can also do something like this /* (non-Javadoc) * @see android.app.Activity#onCreateOptionsMenu(android.view.Menu) */ @Override public boolean onCreateOptionsMenu(Menu menu) { final MenuInflater inflater = new MenuInflater(this); final Intent[] menuIntents = new Intent[] { new Intent(this, AddWebsite.class) }; inflater.inflate(R.menu.mymenu, menu); final int ms = menu.size(); for (int i=0; i avoiding some method calls, however you need to pay attention to the mapping between menu ids, menu order and intents but this is almost always known.
