[site]: stackoverflow
[post_id]: 5268681
[parent_id]: 
[tags]: 
Data retrieval from database using perl with a foreach

$dbh_source2 = DBI->connect("dbi:Oracle:host=.......;port=......;sid=......",'..........','..........'); foreach $data_line (@raw_data) { $SEL = "SELECT arg1,arg2 FROM TABLE_NAME WHERE DATA_NAME = '$data_line'"; $sth = $dbh_source2->prepare($SEL); $sth->execute(); while (my @row = $sth->fetchrow_array() ) { print @row; print "\n"; } } END { $dbh_source2->disconnect if defined($dbh_source2); } I am trying to grab several lines of data from a user. I want to take that data and use it to query a database and grab ARG1 and ARG2 WHERE USER_DATA = $data_line . It will not display anything.
