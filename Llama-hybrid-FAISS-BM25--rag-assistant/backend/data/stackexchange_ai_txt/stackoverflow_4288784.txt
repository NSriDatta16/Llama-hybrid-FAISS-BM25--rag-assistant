[site]: stackoverflow
[post_id]: 4288784
[parent_id]: 4288733
[tags]: 
Something like: $description = current(explode("\n", $string)); should work fine. Pay special attention to the double instead of single quotes. If you're doing more of this kind of parsing, you should consider a HTML parser, such as a simplehtmldom .
