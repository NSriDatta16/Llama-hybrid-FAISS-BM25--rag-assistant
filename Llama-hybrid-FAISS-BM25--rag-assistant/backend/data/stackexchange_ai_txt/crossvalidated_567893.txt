[site]: crossvalidated
[post_id]: 567893
[parent_id]: 567886
[tags]: 
It's easy to make your own. I'll do it in R . set.seed(2022) # Simulate some values of a predictor variable # x If you follow this rationale, you can make your plot for as complicated of a true function as you desire and as complicated of a model as you desire. For instance, I can use a neural network model of that same true linear function. library(nnet) # Simulate some values of a predictor variable # x
