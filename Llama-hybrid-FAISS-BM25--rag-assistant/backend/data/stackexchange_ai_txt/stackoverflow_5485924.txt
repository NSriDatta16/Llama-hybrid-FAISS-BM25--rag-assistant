[site]: stackoverflow
[post_id]: 5485924
[parent_id]: 5479504
[tags]: 
You can make use of Canvas as a base control and then add any number of WPF control to the base Canvas. Then capture the MouseX position and reset the selected child control's Left position using Canvas.SetLeft when user click and drag the control. Here is one good article link which will help you get started.
