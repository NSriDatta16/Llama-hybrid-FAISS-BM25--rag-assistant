[site]: stackoverflow
[post_id]: 994045
[parent_id]: 994005
[tags]: 
use IP address, user agent string, cookie - prolong time that is needed to get the page displayed when page is often accessed to that particular combination of IP, user agent and cookie. you can do: sleep(n) where n increases exponentially when page is requested many times in a row...
