[site]: stackoverflow
[post_id]: 5740762
[parent_id]: 
[tags]: 
Label a checkbox using jquery

i'm not familiar with jquery. What i'm trying to do is, drag and drop a text which will be displayed as a checkbox and the checkbox should have the id and tooltip same to the dropped-text. For that i used the following code.but it's not working.please help $(init); function init() { function addColumn(column) { var iHtml; //Labeling and Tool Tip the Checkbox iHtml = " "+ " ' type='checkbox' name=' ' />"+ " '>MyCheckBox "; return iHtml }
