[site]: stackoverflow
[post_id]: 3552747
[parent_id]: 3552741
[tags]: 
You would need to concatenate # into the selector with the ID. $('#' + ui.draggable.attr("id")).appendTo(this); Or, I believe ui.draggable is a jQuery object already, so try: ui.draggable.appendTo(this);
