[site]: crossvalidated
[post_id]: 232763
[parent_id]: 228304
[tags]: 
Typically the meaning of joint multi-lingual word embeddings is that the embeddings for each of the languages lie in the same space. For example, the vector for the English word "apple" will be near the vector for whatever the Chinese word for "apple" is.
