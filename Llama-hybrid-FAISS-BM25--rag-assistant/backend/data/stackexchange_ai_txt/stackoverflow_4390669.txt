[site]: stackoverflow
[post_id]: 4390669
[parent_id]: 4390520
[tags]: 
var banco = from c in bancos where headerarquivos .Select(o => o.codibanco) .Where(o.MONTH(datahorageracao)=12) and (o.YEAR(datahorageracao)=2010)) .Contains(c.codibanco) select c; Pretty sure that should do it.
