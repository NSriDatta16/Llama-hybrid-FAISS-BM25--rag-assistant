[site]: crossvalidated
[post_id]: 637889
[parent_id]: 
[tags]: 
Nonlinear PCA vs Encoder in Autoencoders

I see that encoders have the benefit over PCA that they can transform both linear and non-linear data. However, isn't non-linear PCA designed to work with non-linear data? So, why do we still prefer to use encoders instead of non-linear PCA?
