[site]: stackoverflow
[post_id]: 1092497
[parent_id]: 1092329
[tags]: 
The most important meta tag you should use is: adjusted to suit. This ensures that, if you ever need to transmit that HTML document via something other than a web server (e.g. working on it locally, sending as an attachment), the user-agent is aware of its mime-type and character set. Just make sure your server headers agree.
