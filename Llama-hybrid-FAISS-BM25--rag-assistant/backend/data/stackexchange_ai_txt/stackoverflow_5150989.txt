[site]: stackoverflow
[post_id]: 5150989
[parent_id]: 
[tags]: 
When i use draggable for an element then the click function is not working

I am using the draggable function for the element using jquery..when I use this the click function is not working on that..why is that and how to remove this bug? pls help me // Reizable and draggable for the YOUTUBE widget************** $(function() { $( "#youtube" ).draggable( {cursor: 'move'} ); $( "#youtube" ).resizable({ ghost: true, alsoResize: '#player', handles: "n, e, s, w" }); });
