[site]: stackoverflow
[post_id]: 3308045
[parent_id]: 3307938
[tags]: 
RobertPitt is correct in that the Child class is not a child class unless it extends the Parent class. But from your original code snippet, the following should be true: if (method_exists('Parent', 'myFunction') { // True } Note the 'Parent' is in quotes, you had it unquoted. Passing the class name as a string.
