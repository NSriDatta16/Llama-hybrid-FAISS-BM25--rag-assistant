[site]: stackoverflow
[post_id]: 3065094
[parent_id]: 3064803
[tags]: 
They are sharing the instance variable. You need to allocate it inside the package using the role. # find storage for instance my $iref = \${ "${class}::_instance" }; # an instance already exists; return it instead of creating a new one return $$iref if defined $$iref; # no instance yet, create a new one ...
