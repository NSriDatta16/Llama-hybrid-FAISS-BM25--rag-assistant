[site]: stackoverflow
[post_id]: 42326
[parent_id]: 42212
[tags]: 
LINQ to SQL is simply a ORM layer leveraging the power of expressions to make it easy to construct queries in your code. If you are just calling adhoc queries for your tool, there is little need to use LINQ, it just adds an extra layer of abstraction to your code.
