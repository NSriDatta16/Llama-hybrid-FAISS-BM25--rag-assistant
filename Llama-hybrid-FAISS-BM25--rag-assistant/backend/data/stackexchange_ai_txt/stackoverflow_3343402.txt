[site]: stackoverflow
[post_id]: 3343402
[parent_id]: 
[tags]: 
XSL Output Text

Here is my XSL, take notice to the following line: I tried this because the g:paragraph is coming from a WYSIWYG and it was printing out the tags and whatever else. This process of encapsulating it within xsl:text tags caused an error. What is the proper way to either hide the tags (because I want the styles to still be applied if included (i.e. bold, underlined)? Edit: The output currently is whatever I want it to be whatever
