[site]: stackoverflow
[post_id]: 4160127
[parent_id]: 4160085
[tags]: 
I'm not sure if you need to use assembly. Since you are already embedding assembly in C, you can make a simple lookup table in C and skip the assembly altogether... byte const mirror[256] = { 0x00, 0x80, 0x40, 0xc0, ... };
