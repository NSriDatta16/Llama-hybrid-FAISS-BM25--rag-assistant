[site]: crossvalidated
[post_id]: 160910
[parent_id]: 
[tags]: 
Repeating the same Random Forests through gbm is inconsistent?

I've noticed that when running a piece multiple times, the gbm produced (see below) produces slightly different results when viewing the summary. Should that be expected? I.e. running gbm regression is not consistent? file.gbm
