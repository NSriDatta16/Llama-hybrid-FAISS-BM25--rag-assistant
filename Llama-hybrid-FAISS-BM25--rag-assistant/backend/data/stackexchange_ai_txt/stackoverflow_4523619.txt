[site]: stackoverflow
[post_id]: 4523619
[parent_id]: 4522139
[tags]: 
The problem might be that you actually don't mean to display a box (div) but a paragraph (p). You can do the following and it should work. In your styles: .filter-field span{ padding-right:10px; } In your markup: Number From Hello To Goodbye Also remember that CSS inherits rules, so the problem could be somewhere else. Using Firebug or any other browser inspection tool could de helpful to determine what's going on.
