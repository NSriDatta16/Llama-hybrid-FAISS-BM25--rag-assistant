[site]: stackoverflow
[post_id]: 3809388
[parent_id]: 3809253
[tags]: 
A workaround is to check what is the target when mouse drag start. Something like this, if (event.target.tagName.toLowerCase() != 'div' ) { return; } or, in all events that happened. OP's demo, updated
