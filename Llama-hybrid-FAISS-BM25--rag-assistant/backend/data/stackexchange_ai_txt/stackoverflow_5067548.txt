[site]: stackoverflow
[post_id]: 5067548
[parent_id]: 5067449
[tags]: 
Why not embedding your so called main method in a Launcher main method ? Your launcher class uses a SecurityManager that forbids calls to System.exit. This way, when initial maint ries to exit, a SecurityException is throwed (and catched in Launcher). The Launcher then restarts the initial main.
