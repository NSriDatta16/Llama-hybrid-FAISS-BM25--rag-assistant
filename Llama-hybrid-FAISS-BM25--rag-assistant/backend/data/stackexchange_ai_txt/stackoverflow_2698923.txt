[site]: stackoverflow
[post_id]: 2698923
[parent_id]: 2697474
[tags]: 
How are you running the packages? What I found works, is to create a SQL Agent job and assign needed config file there, in step properties. You would end up with one job per environment with different paths to the config files.
