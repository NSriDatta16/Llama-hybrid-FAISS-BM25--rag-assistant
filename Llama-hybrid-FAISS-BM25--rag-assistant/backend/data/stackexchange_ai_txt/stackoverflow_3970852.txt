[site]: stackoverflow
[post_id]: 3970852
[parent_id]: 3970814
[tags]: 
are you trying to order the entire collection? if so, I think a parenthesis is placed in the wrong spot (fragment below is untested): Coll.Where(a => a.Selected).Concat(Coll.Where(a => !a.Selected)).OrderBy(a => a.CreateDate)
