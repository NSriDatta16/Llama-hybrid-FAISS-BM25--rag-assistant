[site]: stackoverflow
[post_id]: 5281613
[parent_id]: 5277221
[tags]: 
XForms is one of the most elegant way, but still in its beta... Otherwise you just need to add a Controller between HTML and your model, read more about MVC design pattern. The Controller will convert *.Text into your model incoming parameters. In other words it will decouple your view from model.
