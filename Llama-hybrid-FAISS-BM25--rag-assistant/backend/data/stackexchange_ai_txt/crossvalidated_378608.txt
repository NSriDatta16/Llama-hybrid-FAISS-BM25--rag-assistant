[site]: crossvalidated
[post_id]: 378608
[parent_id]: 378189
[tags]: 
Weights, and averaging, make sense only for >1 parameters (each from one of the component models, not the averaged one, as I suppose you did in your example). You ask to "average" a single parameter so your weight is normalized to sum to 1. If what you want is CI for the averaged parameters, use confint .
