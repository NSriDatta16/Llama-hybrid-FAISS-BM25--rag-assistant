[site]: stackoverflow
[post_id]: 4640481
[parent_id]: 4632111
[tags]: 
I think if you don’t use .netrc , that will work. It will prompt you for your login name as well as the password, though. If you want to keep your .netrc for other purposes and just not use it in Vim, use :let g:netrw_ignorenetrc = 1 A better solution, of course, would be to switch to some SSH-based access method. Then you can make use of the SSH agent to evade the whole issue entirely.
