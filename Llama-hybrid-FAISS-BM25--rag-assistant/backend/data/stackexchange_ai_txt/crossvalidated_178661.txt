[site]: crossvalidated
[post_id]: 178661
[parent_id]: 
[tags]: 
How to calculate the scores in correlation-based PCA?

I am doing correlation-based principal component analysis (PCA) where the eigenvalues are extracted from the correlation matrix instead of the covariance matrix. Now, the issue is to understand how the scores are being calculated in correlation-based PCA?
