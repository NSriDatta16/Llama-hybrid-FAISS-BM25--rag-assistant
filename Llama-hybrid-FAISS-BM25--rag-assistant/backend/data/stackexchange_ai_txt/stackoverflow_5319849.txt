[site]: stackoverflow
[post_id]: 5319849
[parent_id]: 5319831
[tags]: 
By "every second paragraph," I assume you mean the odd-numbered paragraphs (zero-indexed), e.g., 1, 3, 5, ... . I assume this because you have 4 images and 6 paragraphs. If I assume wrong, then you'd change the :odd selector to something like :nth-child(3n+1) , which would select paragraphs 1, 4, 7, ... $(function () { var $images = $('#images > img'); $('#interview > p:odd').each(function (i) { $(this).after($images.get(i)); }); }); API docs: :odd selector .after() Oh also check out the demo →
