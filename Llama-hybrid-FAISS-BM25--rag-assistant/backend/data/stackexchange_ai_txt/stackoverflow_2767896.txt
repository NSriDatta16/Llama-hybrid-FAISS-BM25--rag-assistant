[site]: stackoverflow
[post_id]: 2767896
[parent_id]: 2767872
[tags]: 
The first one is not a string. And it is not works fine: error_reporting(E_ALL); if ($s == Yes) {} It's a legacy from the times when PHP were just a "Pretty home page" form interpreter and strongly discouraged nowadays.
