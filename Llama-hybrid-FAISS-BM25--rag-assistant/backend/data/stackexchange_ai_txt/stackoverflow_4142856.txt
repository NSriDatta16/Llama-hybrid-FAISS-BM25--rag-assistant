[site]: stackoverflow
[post_id]: 4142856
[parent_id]: 
[tags]: 
Java RegEx: Just get a part of the matcher group

I have a regex in Java: Pattern pattern = Pattern.compile( text .+ ); Matcher matcher = pattern.matcher(ganzeDatei); while (matcher.find()) { String string = matcher.group(); ... This works fine, but the output is something like text Name But I just want this: Name How can I do this?
