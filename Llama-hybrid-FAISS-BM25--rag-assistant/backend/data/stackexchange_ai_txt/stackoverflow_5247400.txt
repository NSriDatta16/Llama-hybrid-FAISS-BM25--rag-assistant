[site]: stackoverflow
[post_id]: 5247400
[parent_id]: 
[tags]: 
Can I get my minimized Java app to react to files being dropped on it (on Windows)?

Can I get my minimized Java app to react to files being dropped on it (on Windows) so that a user can drag files onto the icon on the toolbar and that will cause my application to reopen and process the the files that were dropped on it. Im using Java 6
