[site]: stackoverflow
[post_id]: 4171372
[parent_id]: 4171173
[tags]: 
Something like this should work: File sdcard = Environment.getExternalStorageDirectory(); StatFs stat = new StatFs(sdcard.getAbsolutePath()); long available = stat.getAvailableBlocks() * (long) stat.getBlockSize();
