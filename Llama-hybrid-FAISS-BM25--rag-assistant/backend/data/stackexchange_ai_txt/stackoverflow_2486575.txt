[site]: stackoverflow
[post_id]: 2486575
[parent_id]: 
[tags]: 
memcached never expired in rails?

It's very strange that the session will never expire if I use memcached store even I set config.action_controller.session :session_expires => 1.seconds.from_now . And I use extended_fragment_cache to cache fragment, I meet the same problem 1.minutes} do ... %> never expired! Anyone know where's the problem?
