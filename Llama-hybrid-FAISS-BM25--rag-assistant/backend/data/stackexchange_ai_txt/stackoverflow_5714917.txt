[site]: stackoverflow
[post_id]: 5714917
[parent_id]: 5709773
[tags]: 
You can use fragment caching for the part of your view template that displays the categories. This means that the categories will be served from the cache store and the query will only be executed once until the cache is expired (by using the expire_fragment method). Fragment Caching Rails Guide
