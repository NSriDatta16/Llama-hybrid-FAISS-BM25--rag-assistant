[site]: stackoverflow
[post_id]: 7282
[parent_id]: 7277
[tags]: 
I think the only real problem is that it makes the XML harder to read. e.g. 15 Sample St Example Bay Some Country If pretty XML isn't a concern, I'd probably not worry about it, so long as it's working. If pretty XML is a concern, I'd convert the explicit newlines into tags or \n before embedding them in the XML.
