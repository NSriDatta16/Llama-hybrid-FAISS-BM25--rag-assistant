[site]: stackoverflow
[post_id]: 3812277
[parent_id]: 
[tags]: 
How to display data from XML file to ListView using LINQ to XML?

I'm having an xml file like i need to display the data from the Xml file to a Listview like (Added A to index value) Now i'm using like 1.Stores the data in an XmlNodesList 2.Then iterate through the nodeslist and add the attribute value to the list view Here i can not use Dictionary as a temporary storage because there exist multiple keys with same name. Is there any idea to do this using LINQ to XML .?
