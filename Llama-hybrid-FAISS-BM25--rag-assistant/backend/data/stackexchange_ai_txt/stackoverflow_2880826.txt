[site]: stackoverflow
[post_id]: 2880826
[parent_id]: 2855101
[tags]: 
For Windows SNMP agent, you need to check the SET community name. It must be used to set objects to values you want. Otherwise, operations can fail. Besides, not all SNMP agents are 100% compliant to RFCs. You need to test them out and never assume "they work that way by default".
