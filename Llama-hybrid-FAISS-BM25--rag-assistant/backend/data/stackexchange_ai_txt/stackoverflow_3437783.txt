[site]: stackoverflow
[post_id]: 3437783
[parent_id]: 3437746
[tags]: 
1). Use SWFObject to do embedding, otherwise you will drive yourself absolutely bonkers. 2). wmode="transparent" is the devil - only use it if you really, REALLY need to. It kills performance, doesn't play nice with the rest of the page, and generally angers your users. Use "window" or "opaque" if you can get away with it. 3). This line looks wrong: "> Probably don't want to be setting it to the user's fish name. 4). Or the problem might be here: " Hard to know without knowing what those PHP directives evaluate to, but unless $entry_user_fish is "something.swf" it's not going to work.
