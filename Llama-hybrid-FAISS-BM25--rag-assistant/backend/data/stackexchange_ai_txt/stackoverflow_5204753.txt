[site]: stackoverflow
[post_id]: 5204753
[parent_id]: 5204709
[tags]: 
Use dynamic content through Javascript or simply use an iframe. There is nothing that will otherwise tell a spider not to index a block of code or content. For a server side distinction use $_SERVER['HTTP_USER_AGENT'] but like I've stated it's not necessarily the most reliable. Ex. "Googlebot", "MsnBot"=>"msnbot", "Yahoo! Slurp"=>"Slurp", "YahooSeeker"=>"YahooSeeker" //more search terms from //http://www.useragentstring.com/pages/Crawlerlist/ ); $isSpider = false; foreach($spiderSearch as $name=>$val){ if(preg_match('/'.$val.'/i',$_SERVER['HTTP_USER_AGENT'])){ $isSpider = true; break; } } if(!$isSpider){ //SHOW COMMENTS } ?>
