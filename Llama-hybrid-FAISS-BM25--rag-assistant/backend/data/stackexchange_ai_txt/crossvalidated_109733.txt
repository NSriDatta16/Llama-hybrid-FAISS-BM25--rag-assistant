[site]: crossvalidated
[post_id]: 109733
[parent_id]: 109730
[tags]: 
You can turn this into a binary classification problem (worrisome behaviour vs non-worrisome behaviour). For such problems you can use a plethora of methods (logistic regression, SVM, neural networks, random forest, decision tree, ...).
