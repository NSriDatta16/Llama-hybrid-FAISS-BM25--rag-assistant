[site]: stackoverflow
[post_id]: 4328401
[parent_id]: 4259854
[tags]: 
As a counter question: Is there any reason to use javascript embedding methods in 2011? I find that the flash satay method works best cross-browser, and follows a mostly DRY policy. The markup is as follows: This works great so long as you don't care about checking the user's flash version (a work-around is provided in the article).
