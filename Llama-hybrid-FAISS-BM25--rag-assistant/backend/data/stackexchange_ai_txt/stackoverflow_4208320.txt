[site]: stackoverflow
[post_id]: 4208320
[parent_id]: 4207545
[tags]: 
ok, my answer will sound like a hack - but short or writing your own distribution - maybe you can map an uniform distribution and leverage boost (check out http://www.boost.org/doc/libs/1_44_0/doc/html/boost_random/reference.html#boost_random.reference.distributions ) so following your example: 1 -> 1 2,3 ->2 4,5,6 ->3 7,8,9,10 ->4 (etc...) then generate random number between 1 and 10 and return the mapped element. and then use boost's uniform_int distribution to get a number which you then map. here is an example of generating the numbers; you would then need to map the results: #include #include #include using namespace std; using namespace boost; int main ( ) { uniform_int<> distribution(0, 10) ; mt19937 engine; engine.seed(time(NULL)); variate_generator > myrandom (engine, distribution); cout
