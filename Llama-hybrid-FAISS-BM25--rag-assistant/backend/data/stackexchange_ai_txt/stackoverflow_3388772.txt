[site]: stackoverflow
[post_id]: 3388772
[parent_id]: 
[tags]: 
NullReferenceException on code that should work

I have the following controller Action: public ActionResult Details(int id) { Seguimiento seguimiento = repo.GetSeguimiento(id); if (seguimiento == null) { return View("NotFound"); } else { return View("Details", seguimiento); } } And in the details view: Informacion General ID: Materia: Docente: Informacion Tiempo Fecha: Hora: Evaluador: Desarrollo Objetivo: | Objetivo: | Objetivo: | The first two fieldsets work; the last one doesn't. Dominio, Contenido, Organizacion are bit type is the database, so maybe that's a factor?
