[site]: stackoverflow
[post_id]: 5455225
[parent_id]: 5454579
[tags]: 
Why would hardcoding three different paths be a problem? use Sys::Hostname; my $host = hostname(); $host eq 'foo' and push @INC, '/foo'; $host eq 'bar' and push @INC, '/bar'; $host eq 'baz' and push @INC, '/baz'; require 'common.pl'; Granted, it's not the most elegant solution, but if it works...
