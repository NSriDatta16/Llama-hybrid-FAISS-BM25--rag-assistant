[site]: stackoverflow
[post_id]: 1408574
[parent_id]: 1007223
[tags]: 
if this evaluates to true: !!document.createElement('audio').canPlayType , then you have audio tag support in your browser. Doing a feature detect like this is a lot better than parsing userAgent strings. Alternatively, you can use something like Modernizr to handle this detect and many others for you.
