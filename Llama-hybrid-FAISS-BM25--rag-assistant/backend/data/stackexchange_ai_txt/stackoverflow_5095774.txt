[site]: stackoverflow
[post_id]: 5095774
[parent_id]: 5095738
[tags]: 
Keep the first if get rid of the first else and condense the last into a single line if/else if (liRec == null) { throw new Exception(string.Format("Could NOT find LastIntervalUpdated record for muiMethod: {0}", muiMethod)); } liRec.dbPostTime_EST == null || liRec.retrievalTime_EST > liRec.dbPostTime_EST ? return true : return false
