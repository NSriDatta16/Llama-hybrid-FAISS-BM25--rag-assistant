[site]: stackoverflow
[post_id]: 663825
[parent_id]: 653014
[tags]: 
LINQ is great. But just in case you are stuck on a system with .NET 2.x you might have to do it the "old" (XPath) way (where xmlFragment is your string of XML above): XPathDocument doc = new XPathDocument(new StringReader(xmlFragment)); XPathNavigator n = doc.CreateNavigator().SelectSingleNode("//Name[@id='4']"); if(n==null){//Node does not exist}
