[site]: crossvalidated
[post_id]: 167117
[parent_id]: 
[tags]: 
PCA to reduce dimensionality then classifier of choice?

I read about PCA online and the way it computes a covariance matrix, computes eigenvalues, and then transforms the matrix to reduce the dimensionality of the matrix to a certain number k
