[site]: stackoverflow
[post_id]: 4069475
[parent_id]: 4069348
[tags]: 
The helper function you're applying is invoked the following way : $(o.helper.apply(this.element[0], [event])) That means that this refers to the .draggable you want inside that function, for example: $('.draggable').draggable({ helper: function(event) { return $(' ') .text($(this).text() + " helper"); } }); You can test it out here .
