[site]: datascience
[post_id]: 86759
[parent_id]: 78388
[tags]: 
It's because you wrote: x = tf.keras.layers.LSTM(256)(in_) and: y = tf.keras.layers.Bidirectional(tf.keras.layers.LSTM(128))(in_) The number of parameters is different: 256 vs 128. Their output shape is the same because Bidirectional RNN layers are technically couples of paired RNN layers.
