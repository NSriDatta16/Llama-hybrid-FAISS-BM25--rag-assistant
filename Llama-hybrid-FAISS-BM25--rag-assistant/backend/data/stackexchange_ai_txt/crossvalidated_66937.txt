[site]: crossvalidated
[post_id]: 66937
[parent_id]: 
[tags]: 
In R, why are residuals of lm() and summary(lm()) different when using weights for the regression?

I am using a decay weighting scheme for time series regression in financial markets (idea is that more recent data is more relevant). However lm() residuals are different from summary(lm()) residuals. Is there a reason for this? rr here is my decay function, by the way: decay
