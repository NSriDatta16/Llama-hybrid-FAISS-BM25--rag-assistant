[site]: stackoverflow
[post_id]: 4561825
[parent_id]: 4561434
[tags]: 
This should work, but a bit ugly $doc->loadHTML($fragment); echo simplexml_import_dom( $doc->getElementsByTagName('div')->item(0) )->asXML(); output: Lorem ipsum dolor sit amet, luptate . Excepteur proident, sunt in culpa officia est laborum. slightly more elegant $xpath = new DOMXPath($doc); $query = '/html/body/*'; ... $entries = $xpath->query($query); foreach ($entries as $entry) { echo simplexml_import_dom($entry)->asxml(); }
