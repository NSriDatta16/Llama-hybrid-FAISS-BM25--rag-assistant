[site]: stackoverflow
[post_id]: 1758347
[parent_id]: 1733730
[tags]: 
I figured it out. I have to use MemoryStream : MemoryStream ms = new MemoryStream(File.ReadAllBytes(image)); TiffBitmapDecoder decoder = new TiffBitmapDecoder(ms, BitmapCreateOptions.None, BitmapCacheOption.None); List images = new List (); foreach (BitmapFrame frame in decoder.Frames) images.Add(frame);
