[site]: stackoverflow
[post_id]: 4637162
[parent_id]: 4634368
[tags]: 
What if you used PHP to detect the browser in the functions.php file and then put a simple if/else statement to it? if (isset($_SERVER['HTTP_USER_AGENT']) && (strpos($_SERVER['HTTP_USER_AGENT'], 'MSIE') !== false)){ wp_enqueue_script('jquery'); } else { return false; }
