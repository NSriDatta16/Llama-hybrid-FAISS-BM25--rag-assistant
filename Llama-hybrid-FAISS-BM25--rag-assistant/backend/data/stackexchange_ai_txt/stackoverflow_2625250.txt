[site]: stackoverflow
[post_id]: 2625250
[parent_id]: 2625220
[tags]: 
The only real difference is readability and organization. It's technically the same as $( function() { $('#btn').click( showMessage ); function showMessage() { $('#message').fadeIn('slow'); } }); which is shorthand for $(document).ready(...)
