[site]: stackoverflow
[post_id]: 5651280
[parent_id]: 5651203
[tags]: 
Using Javascript Note comment by user DA - IDs cannot start with a number before HTML 5, so assume that the ID is spelled out (two) instead of the numeral (2) if not using HTML 5. If it is only for the tag with id=two: document.getElementById("two").innerHTML = "NotFound"; If it is for all tags: var paragraphs = document.getElementsByTagName("p"); for(var i = 0; i The key is the innerHTML tag of the DOM elements.
