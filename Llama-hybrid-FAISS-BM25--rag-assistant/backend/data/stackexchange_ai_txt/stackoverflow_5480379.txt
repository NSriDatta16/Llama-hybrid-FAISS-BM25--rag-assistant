[site]: stackoverflow
[post_id]: 5480379
[parent_id]: 
[tags]: 
IE8 won't style figcaption element using JQuery and html5shiv

I have a JQuery script that manipulates the DOM to create a and . Unfortunately I don't seem to be able to style the using CSS, in IE8. The same code works fine for Chrome and Firefox. Any ideas why IE8 won't style the elements? I'm using the HTML5Shiv script with IE8, so it should recognize the new HTML5 elements and allow me to style them. Here is the code, and thanks for checking it out: Untitled body { width: 600px; margin-left: auto; margin-right: auto; } figure { float: left; } figcaption { display: block; text-align: center; font-size: 0.5em; font-style: italic; font-weight: bold; padding: 5px 0; } This is a regular paragraph. $(document).ready(function() { $('img').wrap(' '); $('img').after(' This should be bold, italic, centered and small '); });
