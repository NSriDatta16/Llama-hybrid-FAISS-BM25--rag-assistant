[site]: stackoverflow
[post_id]: 3901569
[parent_id]: 3901447
[tags]: 
But storing XML in database in the intermediate time will make it slow. No, it won't. This is what databases are for - storing data for later retrieval. TypeWith.me uses EtherPad, which uses the MySQL database as a backend.
