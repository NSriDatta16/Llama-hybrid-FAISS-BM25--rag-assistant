[site]: datascience
[post_id]: 48005
[parent_id]: 
[tags]: 
Which is better: GPT or RelGAN for text generation?

Based on my understanding, gpt or gpt-2 are using language model loss to train and generate text, which do not contains GAN. So which is better: GPT vs RelGAN/LeakGAN/SeqGAN/TextGAN I am so confused about this question.
