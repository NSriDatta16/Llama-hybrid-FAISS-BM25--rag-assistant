[site]: stackoverflow
[post_id]: 2368150
[parent_id]: 2368085
[tags]: 
If you want to restrict all access to the /files directory you could simply use a location element in your web.config to restrict access. E.g. I should add that I agree with Robert and Rob for advanced security, but if you just want a simple solution this should do the trick. :-) HTHs, Charles
