[site]: crossvalidated
[post_id]: 363512
[parent_id]: 156117
[tags]: 
The fitting criterion is another synonym for loss function. Training a neural network proceeds by adjusting the weights and biases of a neural network to minimize the loss function (fitting criterion) with respect to the training data. See also: Objective function, cost function, loss function: are they the same thing?
