[site]: crossvalidated
[post_id]: 192073
[parent_id]: 191963
[tags]: 
Suppose we have inputs $x, y, z$, a linear combination of inputs simply means: $ax+by+cz$. Ref: http://mathworld.wolfram.com/LinearCombination.html That would be undesirable for neural networks since usually we want the network to approximate some non-linear functions.
