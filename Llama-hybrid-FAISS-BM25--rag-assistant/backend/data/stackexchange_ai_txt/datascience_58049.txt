[site]: datascience
[post_id]: 58049
[parent_id]: 57697
[tags]: 
Introduction to Data Science with Python book says However, if a feature has a low feature_importance , it doesn’t mean that this feature is uninformative. It only means that the feature was not picked by the tree, likely because another feature encodes the same information. So in that specific model, you should be able to discard because there is an other variable encodes that information. But another model may use it.
