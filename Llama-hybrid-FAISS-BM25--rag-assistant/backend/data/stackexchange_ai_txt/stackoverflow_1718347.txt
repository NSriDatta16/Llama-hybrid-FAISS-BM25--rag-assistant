[site]: stackoverflow
[post_id]: 1718347
[parent_id]: 1708273
[tags]: 
Replace the instead, so the link ends up in the Flash movie, then use a:hover for the colors. The selectors in the css parameter are relative to the replaced element, so a span.organisation won't work because you've replaced span.organisation .
