[site]: crossvalidated
[post_id]: 278663
[parent_id]: 
[tags]: 
Complexity of BPR (Bayesian Personalized Ranking)

I am using lightFM framework and it uses BPR as logistic loss. When looking at the paper that describes BPR , it says that it takes m · |S| single update steps until convergence. However, the complexity of every step is not described. Moreover in order to know when the SGD converges, the algorithm needs to compute the BPR loss function at every step too and this seems to be a massive computation. I am interested in the matrix factorization modelization of the problem. If anyone has an idea of how I can tackle this problem, I would really appreciate.
