[site]: crossvalidated
[post_id]: 380445
[parent_id]: 380410
[tags]: 
What SVM does is just to decide the best separating hyperplane. In 1D, it functions very similar to a one-level decision tree, thresholding with something like $x=a$ . If your dataset looks like XOR problem in 1D, SVM's result will be bad even if the feature is useful.
