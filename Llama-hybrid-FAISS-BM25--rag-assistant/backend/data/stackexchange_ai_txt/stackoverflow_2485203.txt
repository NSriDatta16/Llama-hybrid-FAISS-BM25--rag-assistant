[site]: stackoverflow
[post_id]: 2485203
[parent_id]: 2117616
[tags]: 
svmon -P will list the process memory segments by type. You can also use -S to see what PIDs are attached to a segment. with -S, first run ipcs -bmS, then take the SID w/o the 0x, and use it with svmon -lS [SID] That will return the PIDs attached.
