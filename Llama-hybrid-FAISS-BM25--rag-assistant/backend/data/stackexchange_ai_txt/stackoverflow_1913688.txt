[site]: stackoverflow
[post_id]: 1913688
[parent_id]: 1908429
[tags]: 
I think I have found the answer to my own question. ListEntry aListEntry = (ListEntry) aParagraph; ListData listData = listTables.getListData(aListEntry.getIlfo()); int numberFormat = listData.getLevel(listData.numLevels()).getNumberFormat(); The number format returns 23 for bullet points and 0 for numbered lists. I dare say that there are multiple format numbers that can be interpreted as either bullet points or numbered lists but at least I can now identify them!
