[site]: stackoverflow
[post_id]: 3699642
[parent_id]: 
[tags]: 
Authentication in asp.net

protected void Page_Load(object sender, EventArgs e) { if (!Page.IsPostBack) { Response.Cache.SetCacheability(HttpCacheability.NoCache ); Response.Cache.SetAllowResponseInBrowserHistory(false); } This code i have used to authenticate the website,its working but only when the user copies the url and paste it in a new window,but opening in the new window leads to the next page without any login credentials.
