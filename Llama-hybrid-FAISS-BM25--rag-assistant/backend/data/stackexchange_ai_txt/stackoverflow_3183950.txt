[site]: stackoverflow
[post_id]: 3183950
[parent_id]: 3182927
[tags]: 
Have a look at http://www.complang.org/ragel/ It's an external DSL so not technically C++. However as it generates pure C++ / C from the regular expressions it should be much faster than anything that is built at runtime./ For example. action dgt { printf("DGT: %c\n", fc); } action dec { printf("DEC: .\n"); } action exp { printf("EXP: %c\n", fc); } action exp_sign { printf("SGN: %c\n", fc); } action number { /*NUMBER*/ } number = ( [0-9]+ $dgt ( '.' @dec [0-9]+ $dgt )? ( [eE] ( [+\-] $exp_sign )? [0-9]+ $exp )? ) %number; main := ( number '\n' )*; get's compiled down to st0: if ( ++p == pe ) goto out0; if ( 48
