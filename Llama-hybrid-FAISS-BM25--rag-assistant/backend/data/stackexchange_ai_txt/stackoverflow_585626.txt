[site]: stackoverflow
[post_id]: 585626
[parent_id]: 566401
[tags]: 
There are some good screen casts on this topic http://railslab.newrelic.com/scaling-rails Things like fragmet caching and using :include (to avoid n+1) can help. It sounds like you're already using memcached, so why not curl the url to prefetch the cache?
