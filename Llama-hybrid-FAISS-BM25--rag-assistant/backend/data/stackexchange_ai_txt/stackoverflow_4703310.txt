[site]: stackoverflow
[post_id]: 4703310
[parent_id]: 923809
[tags]: 
You can just add the ElementDragBehavior (something like that) to the Data Template. Another option (which i'm not sure of it exists in WPF, but it does in Silverlight) in the silverlight toolkit you can add this above the ListBox: this makes every item in the list draggable into other ListBoxes that has that
