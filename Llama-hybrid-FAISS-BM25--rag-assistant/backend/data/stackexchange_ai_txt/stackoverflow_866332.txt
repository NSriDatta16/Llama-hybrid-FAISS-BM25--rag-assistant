[site]: stackoverflow
[post_id]: 866332
[parent_id]: 866325
[tags]: 
IF (@Result = '') BEGIN SELECT @Result = 'Unknown' END RETURN @Result Notice that the way you do assignment in T-SQL is the SELECT statement. You can also use the SET statement although that is discouraged.
