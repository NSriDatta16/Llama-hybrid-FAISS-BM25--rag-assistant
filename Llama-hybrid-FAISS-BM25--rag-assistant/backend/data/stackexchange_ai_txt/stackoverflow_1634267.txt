[site]: stackoverflow
[post_id]: 1634267
[parent_id]: 1634261
[tags]: 
It's probably referring to the fact that if draggingNode.Nodes has no items in it then it will fall out of the else and exit the function without returning anything. Maybe do this: foreach (TreeNode node in draggingNode.Nodes) return IsDestinationNodeAChildOfDraggingNode(node, destinationNode); return false
