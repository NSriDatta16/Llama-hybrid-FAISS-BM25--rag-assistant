[site]: stackoverflow
[post_id]: 3178882
[parent_id]: 3178798
[tags]: 
Something like this maybe? public string Get(string text, int maxWordCount) { int wordCounter = 0; int stringIndex = 0; char[] delimiters = new[] { '\n', ' ', ',', '.' }; while (wordCounter It's quite simplified and doesnt handle if multiple delimiters comes after each other (for instance ", "). you might just want to use space as a delimiter. If you want to get just the first paragraph, simply search after "\r\n\r\n" public string GetFirstParagraph(string text) { int pos = text.IndexOf("\r\n\r\n"); return pos == -1 ? text : text.Substring(0, pos); } Edit: A very simplistic way to strip HTML: return Regex.Replace(text, @” ”, string.Empty);
