[site]: stackoverflow
[post_id]: 4995025
[parent_id]: 
[tags]: 
Django question: Want to display only one number

Hello I have a set of numbers in Django Defined as: def order_list_service(request, type_id = 1): orders = models.Order.objects.filter(is_hot = False, is_storage = False, orderservicelist__service__type = type_id).order_by('pk').distinct() request.session['orders']= orders def service_order(request, client_id = 0, request_type = 1): orders = request.session['orders'] return render_to_response('service_step1.html', {'orders':orders }, context_instance = RequestContext(request)) In my service order view I have a template. In this template It display the list of order numbers. {%for order in orders%} {{order.pk}} {%endfor%} This is good However I just want to display just one order number. It could be any number from that list.
