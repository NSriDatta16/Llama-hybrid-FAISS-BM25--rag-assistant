[site]: crossvalidated
[post_id]: 343825
[parent_id]: 
[tags]: 
Can Minhashing be used as a Dimensionality Reduction Method?

Let's say I have high dimensional data where the number of data points $N$ is $1,000,000$ and the dimensionality $D$ of the data is $1,000$. For each data point, a vector $v = (v_0, ..., v_n)$ of hashes is computed, where $n
