[site]: stackoverflow
[post_id]: 5680084
[parent_id]: 
[tags]: 
sdcard,images,folder,path

I want to display images from specific folder in sdcard String path = Environment.getExternalStorageDirectory().toString() + "/pictures"; What changes should be made to the below query to get the images from pictures folder. `String[] img = { MediaStore.Images.Thumbnails._ID }; imagecursor = managedQuery(MediaStore.Images.Thumbnails.EXTERNAL_CONTENT_URI, img,null,null, MediaStore.Images.Thumbnails.IMAGE_ID + ""); image_column_index = imagecursor.getColumnIndexOrThrow(MediaStore.Images.Thumbnails._ID);`
