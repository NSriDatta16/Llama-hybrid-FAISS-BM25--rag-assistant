[site]: stackoverflow
[post_id]: 2185034
[parent_id]: 2184935
[tags]: 
You could forget about exceptions and avoid such a performance concern. This makes your API speak for itself. int getUserAge(String userName) { int age = 0; UserSearchResult result = getUser(userName); if (result.getFoundUser()) { User user = result.getUser(); age = user.getAge(); } return age; }
