[site]: stackoverflow
[post_id]: 5073481
[parent_id]: 4963073
[tags]: 
Although the answers above seems solid, It really didnt work for me. Finally I got what I needed to work by doing the following: row); for($i = 0; $i row[$i]->PCatID == "1") || ($data->row[$i]->PCatID == "3") || ($data->row[$i]->PCatID == "4") ) { unset($data->row[$i]); } } file_put_contents($file_xml, $data->saveXML()); ?> So in this case please see above for the information for "row", "PCatID"
