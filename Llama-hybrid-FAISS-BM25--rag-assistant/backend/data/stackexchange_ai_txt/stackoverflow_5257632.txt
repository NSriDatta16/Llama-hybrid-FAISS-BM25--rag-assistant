[site]: stackoverflow
[post_id]: 5257632
[parent_id]: 5257417
[tags]: 
To count the number of words within the paragraph excluding HTML tags, use: $tagless_content=strip_tags($content); str_word_count($tagless_content); Update Here is code to increase text-indent via jQuery jQuery.fn.stripTags = function() { return this.replaceWith( this.html().replace(/ ]+>/gi, '') ); }; then use the String.length JavaScript property var len = $(' ').stripTags().length(); for(var i=0;i ').css('text-indent',+i+'px'); Reference Strip tag via jQuery strip_tag PHP function str_word_count PHP function
