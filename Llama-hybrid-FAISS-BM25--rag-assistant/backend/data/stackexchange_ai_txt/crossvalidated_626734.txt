[site]: crossvalidated
[post_id]: 626734
[parent_id]: 626732
[tags]: 
Once you estimate the effective sample size (ESS) per subject you can use a univariate (e.g., $t$ -test) sample size calculation and divide the sample size by ESS. An elegant paper showing how to compute ESS for some generalized least squares models is here .
