[site]: stackoverflow
[post_id]: 4342940
[parent_id]: 4342929
[tags]: 
Copy the this keyword into a variable outside of the callback function, and use that variable inside the callback: anotherFunction: function() { var self = this; globalObject.dosomethingandusecallback( function() { // this is the callback self.myFunction(); } ); }
