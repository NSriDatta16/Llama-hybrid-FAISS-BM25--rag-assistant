[site]: stackoverflow
[post_id]: 4467309
[parent_id]: 
[tags]: 
jQuery: repositioned relative to the viewport

When the div has been dragged to the point when the scrolling appears and then is being resized, it forgets its actual position and is repositioned relative to the viewport. How to preserve its actual position? (function($){$(document).ready(function(){ $('#theDiv').draggable().resizable(); })})(jQuery);
