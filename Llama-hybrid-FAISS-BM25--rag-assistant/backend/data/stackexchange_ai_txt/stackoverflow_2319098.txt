[site]: stackoverflow
[post_id]: 2319098
[parent_id]: 
[tags]: 
JSF/IceFaces Conditional Rendering

I am using Icefaces to conditonally render a component but it cant pick up the boolean: BeanCode: public boolean isEmpty(){ return true; } public int getCount(){ if (isEmpty()){ return 0; } return 1; } IceFaces //this doesnt work //this does work Error message: Error Parsing: #{coverage.empty} Why is IceFaces not recognising the boolean?
