[site]: stackoverflow
[post_id]: 3425960
[parent_id]: 3425780
[tags]: 
I think this ought to do it: sim(i,j) { item1 = 0 item2 = 0 // calculate the sums for the ith and jth items // minus each users' avg rating. for (k = 0; k You'll still need to implement the average function but I suppose a simple mean will do for that.
