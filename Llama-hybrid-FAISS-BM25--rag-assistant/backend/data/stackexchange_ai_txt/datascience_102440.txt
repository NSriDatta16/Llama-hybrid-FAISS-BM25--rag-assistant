[site]: datascience
[post_id]: 102440
[parent_id]: 97410
[tags]: 
There are two options: A pass-through network with direct connections with weights set to one. The activations of the input nodes into the system would be the same as the activations of the out nodes. Autoencoder - An autoencoder learns the encoding of data. In this case, there is no encoding.
