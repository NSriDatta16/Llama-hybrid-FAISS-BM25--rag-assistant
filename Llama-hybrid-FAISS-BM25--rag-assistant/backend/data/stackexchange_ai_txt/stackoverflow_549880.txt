[site]: stackoverflow
[post_id]: 549880
[parent_id]: 410039
[tags]: 
How did you do this when select() doesn't work on message queues? Whats it Sys V or POSIX? Why take the extra effort in creating GUID to PID lookup table when PID is guaranteed to be unique, and is smaller storage (integer)? /blee/
