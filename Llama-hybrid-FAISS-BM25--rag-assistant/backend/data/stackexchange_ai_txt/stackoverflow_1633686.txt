[site]: stackoverflow
[post_id]: 1633686
[parent_id]: 1633535
[tags]: 
I think what Joe and Agent_9191 are suggesting is essentially the same: const int NUM_STRINGS = 1024; string[] strings = new string[NUM_STRINGS]; StringBuilder[] stringBuilders = new StringBuilder[NUM_STRINGS]; // ensure that all StringBuilder objects are initialized for (int i = 0; i [] pairs = new KeyValuePair [50]; for (int i = 0; i
