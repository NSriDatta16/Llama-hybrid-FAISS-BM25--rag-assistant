[site]: stackoverflow
[post_id]: 4075601
[parent_id]: 
[tags]: 
PDF file is opening in same window, but I want to open it as a seperate file

PDF file is opening in same window, but I want to open it as a seperate file without Save As dialog box. Here is a relevant piece of servlet code: response.setHeader("Expires", "0"); response.setHeader("Cache-Control", "must-revalidate, post-check=0, pre-check=0"); response.setHeader("Pragma", "public"); response.setHeader("Content-Disposition", "attachment;filename=_blank_"); response.setContentType("application/pdf"); response.setContentLength(baos.size()); ServletOutputStream out = response.getOutputStream(); baos.writeTo(out); out.flush(); Any suggestions?
