[site]: stackoverflow
[post_id]: 5388964
[parent_id]: 5388796
[tags]: 
By using a html editor the result is in html code. So i suppose all you want to do is to get rid of the editor content tags. This can be done using the strip_tags php function. $editor_content = ' Test paragraph. Other text '; echo strip_tags( $editor_content );
