[site]: crossvalidated
[post_id]: 420898
[parent_id]: 420882
[tags]: 
It's for the network to learn more global features in deeper layers. The example image you have is a classification problem so the network needs to move from local features (from pixels) to global ones (encoded in channels) This might be a useful visualisation that is also mentioned in the deep learning book:
