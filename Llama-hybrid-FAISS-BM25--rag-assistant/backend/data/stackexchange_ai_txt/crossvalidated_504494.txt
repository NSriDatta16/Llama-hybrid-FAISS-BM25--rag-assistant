[site]: crossvalidated
[post_id]: 504494
[parent_id]: 
[tags]: 
Are bipolar function and sign function same?

we know about bipolar activation function in neural networks as: $f(n) = \begin{cases} 1, & \text{if $n$ > = 0} \\ -1, & \text{if $n$ and we know about sign activation function as: $f(n) = \begin{cases} 1, & \text{if $n$ > 0} \\ 0, & \text{if $n$ = 0} \\ -1, & \text{if $n$ can we say bipolar is a special case of sign ?
