[site]: stackoverflow
[post_id]: 526853
[parent_id]: 268048
[tags]: 
Drag and drop the return expression into a watch window. For example, in the statement return someTableAdapter.getSomeData(); drag and drop someTableAdapter.getSomeData() into a watch window, and you'll see the value. You can do this for any expression.
