[site]: stackoverflow
[post_id]: 4542539
[parent_id]: 4542210
[tags]: 
I have the following in my style.xml in order to make a link orange: #FF9900 So I assume in code you just need to do this (for some reason the method name is inconsistent with the XML property): textView.setLinkTextColor(Color.MAGENTA);
