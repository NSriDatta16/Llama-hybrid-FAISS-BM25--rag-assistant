[site]: stackoverflow
[post_id]: 4553399
[parent_id]: 4553382
[tags]: 
It's because .boxgrid isn't an immediate child, do you need .find() instead of .children() (which only looks at immediate children) here, like this: $('tr.row-1').find('.boxgrid').toggleClass('active_labBeitrag');
