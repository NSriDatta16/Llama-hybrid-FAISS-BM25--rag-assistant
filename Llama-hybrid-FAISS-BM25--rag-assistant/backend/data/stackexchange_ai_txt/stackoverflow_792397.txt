[site]: stackoverflow
[post_id]: 792397
[parent_id]: 792377
[tags]: 
This is my PHP function I use to determine the appropriate stylesheet: function stylesheet() { $ua = $_SERVER["HTTP_USER_AGENT"]; $iepos = strpos($ua,"MSIE"); if ($iepos) { $vers = substr($ua,$iepos+5,1); if ($vers >= 8) return "style.css"; else return "iestyle.css"; } else return "style.css"; } I was having a really odd problem where I needed IE8 to be the "good" version, so most people can probably change... if ($vers >= 8) to... if ($vers >= 7) Then I just have, in place of a normal CSS link: "; ?> Good luck.
