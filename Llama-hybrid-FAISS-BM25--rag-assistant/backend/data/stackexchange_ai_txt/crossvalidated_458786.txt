[site]: crossvalidated
[post_id]: 458786
[parent_id]: 458785
[tags]: 
Train the model as usual with those three components. We have to do the same thing on test data too. Your classification job can be done. Note: By training a model with PCA components. You don't get feature importance. If that is fine, you can proceed with those three components. Else, those components can be useful for just visualization purpose.
