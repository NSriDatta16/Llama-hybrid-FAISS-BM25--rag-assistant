[site]: crossvalidated
[post_id]: 199393
[parent_id]: 190609
[tags]: 
I have seen it used for prediction of multiple (very many) responses in missing data imputation. In this particular case, I think 1-nearest-neighbor is potentially a more elegant solution than the prediction of thousands of variables via (for example) a neural network.
