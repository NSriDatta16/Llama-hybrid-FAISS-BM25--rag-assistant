[site]: stackoverflow
[post_id]: 2952995
[parent_id]: 
[tags]: 
How to properly set URL rewrite rules for a php script to only accept POST

I have a rewrite rule for GET and POST: RewriteEngine on RewriteCond %{REQUEST_METHOD} ^(HEAD|GET)$ RewriteRule .* /resources/scripts/storage/admin/list.php Script POST /resources/scripts/storage/admin/create.php How would I modify it to only allow POST?
