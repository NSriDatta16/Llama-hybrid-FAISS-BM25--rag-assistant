[site]: datascience
[post_id]: 98398
[parent_id]: 77044
[tags]: 
Not sure what it meant -> rather than using [CLS] token for classification? The Authoer did use [CLS] for classification tasks. BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding The first token of every sequence is always a special classification token ([CLS]). The final hidden state corresponding to this token is used as the aggregate sequence representation for classification tasks . For instance
