[site]: stackoverflow
[post_id]: 2993413
[parent_id]: 
[tags]: 
check all values match using prototype

Using prototype, is there a simple method of checking that a group of values match, for example - can this code be refined to a single line or something otherwise more elegant? var val = null; var fail = false; $('form').select('.class').each(function(e){ if(!val){ val = $F(e); }else{ if(val != $F(e)) fail = true; } });
