[site]: datascience
[post_id]: 68930
[parent_id]: 
[tags]: 
Trouble with plotting time series data

I am doing something wrong with X in these time series plots, but what? This code using plotly yields the following ridiculous time series line plot: import plotly.graph_objects as go fig = go.Figure([go.Scatter(x=df.index, y=df['Weekly_Sales'])]) plt.savefig('plot2.png', dpi=300, bbox_inches='tight') fig.show() Here is the basic data: To show the error on my part (whatever it is) is ongoing, here is a second time series plot: # Spec the chart size plt.figure(figsize=(12,6)) x = df.index #Plot the lines plt.plot(x, weekly_roll) plt.plot(x, monthly_roll) plt.plot(x, quarterly_roll) plt.plot(x, yearly_roll) # Spec the x and y labels, chart title, and axis ticks plt.yticks(fontsize=18) plt.xticks(fontsize=18) ax = plt.axes() ax.set(xlabel='Time', ylabel='Revenue', title='Rolling Averages - Weekly Sales'); plt.savefig('plot4.png', dpi=300, bbox_inches='tight') plt.show(); Here is the resulting image: While both plots have a certain modern flair, it's not what I am going for in a time series plot. Help? Here is the basic data from the head() of the df: Date Store Dept Weekly_Sales IsHoliday 2010-02-05 1 1 24924.50 False 2010-02-12 1 1 46039.49 True 2010-02-19 1 1 41595.55 False 2010-02-26 1 1 19403.54 False 2010-03-05 1 1 21827.90 False
