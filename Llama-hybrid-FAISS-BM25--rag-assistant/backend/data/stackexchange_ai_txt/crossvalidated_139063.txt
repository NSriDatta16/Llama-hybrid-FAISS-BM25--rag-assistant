[site]: crossvalidated
[post_id]: 139063
[parent_id]: 125767
[tags]: 
It does not require numeric values of predictors. In fact as a logistic regression you should feed FACTORS as predictors where it applies (they can be labeled). Otherwise ordinal as well as interval scales are accepted by polr.
