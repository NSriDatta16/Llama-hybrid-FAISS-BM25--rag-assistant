[site]: stackoverflow
[post_id]: 949029
[parent_id]: 948961
[tags]: 
One naive solution would be to drag along a $done variable and check against that before outputting anything: However, I have a hunch that the whole problem can be solved more elegantly, but without the input XML this is kind of hard.
