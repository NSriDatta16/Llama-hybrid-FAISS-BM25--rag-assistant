[site]: stackoverflow
[post_id]: 5574703
[parent_id]: 5574669
[tags]: 
eventDragStop: function(event, jsEvent, ui, view) { if (isElemOverDiv(ui, $('div.event-delete'))) { calendar.fullCalendar('removeEvents', event.id); } } I got this from sitepoint if it helps at all. I think it has something to do with $('#calendar') .
