[site]: stackoverflow
[post_id]: 858710
[parent_id]: 808916
[tags]: 
Also it is not a good solution, setting the cacheability to nocache resolved my problem. I worte this on my pageload Response.Cache.SetCacheability(HttpCacheability.NoCache); It also works by setting this code var prm = Sys.WebForms.PageRequestManager.getInstance(); prm.add_pageLoaded(pageLoaded); var c = 0; function pageLoaded(sender, args) { var img = document.getElementById("ctl00_ctl00_MainContent_MainContent_chartExchange"); c++; img.src = img.src + "?" + c; }
