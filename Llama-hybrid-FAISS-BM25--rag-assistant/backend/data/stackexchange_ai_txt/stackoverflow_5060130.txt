[site]: stackoverflow
[post_id]: 5060130
[parent_id]: 5060111
[tags]: 
You can store the error messages within a session, which would then be present for all subsequent page loads. The down side that without some care and attention, if the user has multiple copies of the form open, submitting copy A will get the errors from copy B, and vice versa. The other option is have your server-side processing redisplay the form if there's any errors:
