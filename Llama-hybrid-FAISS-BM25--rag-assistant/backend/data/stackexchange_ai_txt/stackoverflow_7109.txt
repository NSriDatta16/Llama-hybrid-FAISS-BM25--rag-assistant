[site]: stackoverflow
[post_id]: 7109
[parent_id]: 4752
[tags]: 
I suspect these are related to the SQL Server Agent trying to login to a database that no longer exists. To clear it up you need to: 1. Go to SQL Server Management Studio 2. Disable the job called _job_deleteExpiredSessions If that works, then you should be all clear to delete it.
