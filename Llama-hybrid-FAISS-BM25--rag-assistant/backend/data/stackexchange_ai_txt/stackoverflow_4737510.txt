[site]: stackoverflow
[post_id]: 4737510
[parent_id]: 4721909
[tags]: 
It seems that you are scaling the color you get from the texture by the blend factor, which is not how alpha is performed (this would just make it darker). I believe you need something along the lines of the following gl_FragColor = vec4(texture2d(texture, coord).rgb, blend); See if that works
