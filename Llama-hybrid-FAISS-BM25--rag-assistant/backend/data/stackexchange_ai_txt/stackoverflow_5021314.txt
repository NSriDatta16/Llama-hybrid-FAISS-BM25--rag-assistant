[site]: stackoverflow
[post_id]: 5021314
[parent_id]: 5021138
[tags]: 
Cut the Interface and paste it into the class where you want to embed it Add the static keyword if the inner class should be reachable without an instance of the outer class. In the Package Explorer, select your project and right click > source > organize imports I think this will get most, if not all, of the compilation errors.
