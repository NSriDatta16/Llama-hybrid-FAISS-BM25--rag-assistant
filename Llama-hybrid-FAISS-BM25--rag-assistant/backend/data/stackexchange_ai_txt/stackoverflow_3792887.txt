[site]: stackoverflow
[post_id]: 3792887
[parent_id]: 3792833
[tags]: 
Why not just just check if the variable is set, if the checkbox is checked when the form is submitted, it will be available in $_POST['checkboxName'] , otherwise, isset($_POST['checkboxName']) will return false. Basic script to test it View it in action http://robertsquared.com/so.php
