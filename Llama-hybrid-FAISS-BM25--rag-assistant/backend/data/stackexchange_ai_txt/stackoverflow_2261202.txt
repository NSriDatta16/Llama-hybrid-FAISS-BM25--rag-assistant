[site]: stackoverflow
[post_id]: 2261202
[parent_id]: 
[tags]: 
Why is scanf results different from user input?

char* ReadNumericFormat = "%i"; int Read(void) { int Storage; __asm { LEA EAX, [Storage] PUSH EAX PUSH DWORD PTR [ReadNumericFormat] CALL DWORD PTR [scanf] ADD ESP, 8 MOV EAX, DWORD PTR [Storage] } } when the user enters "023919" the procedure returns 19. this is a feature or darkness is a standard?
