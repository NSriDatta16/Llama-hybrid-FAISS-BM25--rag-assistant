[site]: stackoverflow
[post_id]: 2168080
[parent_id]: 2168066
[tags]: 
A data-reader is a hose of data. It becomes "empty" (or a better analogy: runs dry) when you consume all the data it is reading. It is not a storage device (it isn't a bucket). If you want the data afterwards, store it somewhere - a DataTable or a class model, for example.
