[site]: stackoverflow
[post_id]: 3536433
[parent_id]: 3536385
[tags]: 
You don't have the @selector for touchesBegan. You instead have it for KeyboardRefresh. Don't check for button pressage in touchesBegan. Instead replaces your touchesBegan with: -(void)KeyboardRefresh:(id) sender { NSLog(@"yes"); }
