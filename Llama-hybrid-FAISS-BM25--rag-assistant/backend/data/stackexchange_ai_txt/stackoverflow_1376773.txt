[site]: stackoverflow
[post_id]: 1376773
[parent_id]: 
[tags]: 
How can debug messages in erb be supressed in production mode

I want to print out some debug messages in my erb template when it is called in development mode, but not when it is called in production mode (i.e.) However, this will be printed in any mode. I found I can do which seems to work. Is this the proper way, or is there a more elegant solution?
