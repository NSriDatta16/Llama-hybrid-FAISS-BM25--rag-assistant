[site]: stackoverflow
[post_id]: 4530889
[parent_id]: 4529486
[tags]: 
Don't reinvent the wheel. Since you already probably have the data in SQL, use the built-in db-mail functionality: Database Mail . Simply have a daily SQL Agent job that scans the table for delinquents and calls sp_send_dbmail for each.
