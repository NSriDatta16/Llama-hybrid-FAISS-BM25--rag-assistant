[site]: stackoverflow
[post_id]: 5212455
[parent_id]: 
[tags]: 
Unable to open "Print Dialog" with JQueryUI Dialog on Google Chrome

I'm actually using JQueryUI Dialog plugin to show some information to the users of my site. The content of this dialog can be printed, so I need to open a print dialog (from browser) when I click on a button. I'm creating this dialog like this (this is just an example; I modified content and size): var box = $(' blablabla.... '); box.dialog({ width: 100, height: 100, modal: true, draggable: false, resizable: false, close: function(event,ui){ $('#print-dialog').remove(); }, buttons: { "Print this!": function(){ window.print(); } } }); But when I click on 'Print' button, nothing happens. I tried the same example on Firefox and IE and works fine. You can try to execute my code on JSBin right here: http://jsbin.com/osoro3 What do I need? Am I doing something wrong? Thanks!
