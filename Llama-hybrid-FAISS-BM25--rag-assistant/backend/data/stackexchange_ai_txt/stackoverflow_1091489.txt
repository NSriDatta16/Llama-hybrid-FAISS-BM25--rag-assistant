[site]: stackoverflow
[post_id]: 1091489
[parent_id]: 
[tags]: 
Converting an untyped Arraylist to a typed Arraylist

Is there a more elegant solution to convert an ' Arraylist ' into a ' Arraylist '? Current code: ArrayList productsArrayList=getProductsList(); ArrayList productList = new ArrayList (); for (Object item : productsArrayList) { ProductListBean product = (ProductListBean)item; productList.add(product); }
