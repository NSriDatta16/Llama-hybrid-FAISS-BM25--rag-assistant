[site]: stackoverflow
[post_id]: 5320945
[parent_id]: 5320764
[tags]: 
Make sure that you have depth testing turned on. Even the fragments composing lines and points get depth tested, so if you have shapes drawn in front of lines, the shapes should occlude the lines. You want to look at the glEnable function with parameter GL_DEPTH_TEST .
