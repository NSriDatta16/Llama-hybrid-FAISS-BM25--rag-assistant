[site]: stackoverflow
[post_id]: 1437418
[parent_id]: 1437368
[tags]: 
This is what im using and gnarf's code returns 0 RageZ's returned an empty array Kills \(K\) ([\d,]+) /", $string, $matches)) { $kills = str_replace(",","",$matches[1]); } else { $kills = 0; } echo $kills; ?> Coming up 0
