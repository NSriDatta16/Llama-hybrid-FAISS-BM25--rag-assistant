[site]: stackoverflow
[post_id]: 1294853
[parent_id]: 1294756
[tags]: 
I suspect that "using the stack for storage purposes" means use the stack instead of the heap. That is, don't call malloc, and don't use global variables. Edit: Or maybe not. The more I think about this, the more I suspect it's a poorly worded, or poorly thought out assignment.
