[site]: stackoverflow
[post_id]: 2793313
[parent_id]: 
[tags]: 
Jquery Sortable and mouseenter problem

I have sortable divs and in this divs - paragraphs. If I use mouseenter event to check if the mouse is in this divs - it doesn't work. I.e Sortable + mouseenter on the sortable elements = doesn't work. How can I fix it? Here you are some code $("#sort").click(function() { $("#ps").sortable(); $("#ps").disableSelection(); }); When click on div with id sort - the divs in #ps become sortable. dasdasdasdas 1234 And $(".happy").mouseenter(function() { alert("OK"); });
