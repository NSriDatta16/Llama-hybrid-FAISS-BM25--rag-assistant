[site]: crossvalidated
[post_id]: 451293
[parent_id]: 451285
[tags]: 
In linear regression the R squared measurement tells you if the overall model is able to explain a relevant amount of the overall variance. As long as R^2 is small, factors with tiny p-values are not of particular benefit. A t-test can be regarded as a regression with one categorical predictor, so the story is the same. Also, a (Pearson) correlation of 0.15 could be regarded as small even if significant (p Another possibility can be to go with Bayesian approaches.
