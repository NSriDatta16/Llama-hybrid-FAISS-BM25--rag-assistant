[site]: stackoverflow
[post_id]: 1101190
[parent_id]: 1101165
[tags]: 
Loop through the array of words with foreach and check if the current word exists in the UA string using strpos() : foreach ($words as $word) { if (strpos($row['user_agent'], $word) !== FALSE) { // word exists in string } }
