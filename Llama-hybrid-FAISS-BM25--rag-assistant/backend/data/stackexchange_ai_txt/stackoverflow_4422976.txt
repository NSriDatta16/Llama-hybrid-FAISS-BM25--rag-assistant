[site]: stackoverflow
[post_id]: 4422976
[parent_id]: 4422915
[tags]: 
I think you should be importing the "PIE.js" file in an IE-only conditional comment: The CSS3Pie project is an Internet Explorer facility - it only works on IE, and is completely useless for all other browsers. edit — note that this is if you're importing the stuff as a .js file in the first place. As far as I can tell, the CSS3Pie docs all encourage the functionality to be imported via the IE "behavior" CSS attribute. That attribute will just be ignored by Chrome and Firefox and Safari and everything else that's not IE.
