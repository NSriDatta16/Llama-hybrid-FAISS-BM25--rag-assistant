[site]: stackoverflow
[post_id]: 4098976
[parent_id]: 
[tags]: 
Possible JQuery class selector bug

I have an ajax call that returns a HTML fragment. I am trying to select a div in that fragment before rendering. An example of the HTML: ..... .... .... ...., ... Now the problem: function ajaxReturn(data) { alert($(data).find('.event-detail').length); Is this a bug or am I doing something wrong?
