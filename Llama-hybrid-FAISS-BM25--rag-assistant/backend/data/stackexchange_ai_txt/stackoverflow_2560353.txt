[site]: stackoverflow
[post_id]: 2560353
[parent_id]: 2560299
[tags]: 
Generally it's better to have multiple small files. Keeps memory usage low and performance is much better when searching through it. But it depends on the amount of operations you'll need, because filesystem calls are much more expensive when compared to memory storage for instance.
