[site]: stackoverflow
[post_id]: 1180711
[parent_id]: 
[tags]: 
linking to google analytics

We want to add google analytics a fairly large .NET web site. The site uses several master pages (4 or 5) and so I was going to just paste the requisite javascript into each master page. // // Is there a better way to encapsulate this? Just linking to an external js file would not do the trick due to the . We could also create a static method and dump it into an asp:literal. Is there a more elegant solution?
