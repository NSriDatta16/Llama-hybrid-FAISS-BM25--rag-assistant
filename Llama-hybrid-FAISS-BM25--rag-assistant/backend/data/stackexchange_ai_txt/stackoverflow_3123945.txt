[site]: stackoverflow
[post_id]: 3123945
[parent_id]: 3123939
[tags]: 
No, the whole point of thread local storage is that it's local to the thread - if you access the same slot in a different thread, you'll get the value for that thread instead of the other one. If you need to share state between threads, don't use thread local storage.
