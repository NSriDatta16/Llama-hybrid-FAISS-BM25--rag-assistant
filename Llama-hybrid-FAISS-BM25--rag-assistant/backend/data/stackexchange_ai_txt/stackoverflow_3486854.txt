[site]: stackoverflow
[post_id]: 3486854
[parent_id]: 
[tags]: 
Size of structure with a char, a double, an int and a t

When I run only the code fragment int *t; std::cout it gives me a result like this: 1 8 4 4 Total: 17. But when I test sizeof struct which contains these data types it gives me 24, and I am confused. What are the additional 7 bytes? This is the code #include #include struct struct_type{ int i; char ch; int *p; double d; } s; int main(){ int *t; //std::cout :EDIT I have updated my code like this #include #include struct struct_type{ double d_attribute; int i__attribute__(int(packed)); int * p__attribute_(int(packed));; char ch; } s; int main(){ int *t; //std::cout and now it shows me 16 bytes. Is it good, or have I lost some important bytes?
