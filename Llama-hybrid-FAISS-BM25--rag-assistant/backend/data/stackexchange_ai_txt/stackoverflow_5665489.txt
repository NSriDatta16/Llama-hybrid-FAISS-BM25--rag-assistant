[site]: stackoverflow
[post_id]: 5665489
[parent_id]: 
[tags]: 
how to save this vector

I want to know how I can define vector variables to save output of this code in vector? /*what should be here?*/=agent.GetInfoState().GetPositionInfo().GetCloseTeammateToTeammate(2); GetcloseTeammateToTeammate is defined as: const std::vector & GetCloseTeammateToTeammate(Unum i) { Assert(i > 0); return GetCloseTeammateToPlayer(i); }
