[site]: stackoverflow
[post_id]: 4946211
[parent_id]: 4946042
[tags]: 
You can force your app to start with admin rights (UAC will show it's dialog box anyway) by embedding custom manifest (project properties -> build -> Manifest). Manifest example ( requestedExecutionLevel part is importaint): -->
