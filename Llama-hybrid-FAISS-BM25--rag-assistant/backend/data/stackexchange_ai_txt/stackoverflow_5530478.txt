[site]: stackoverflow
[post_id]: 5530478
[parent_id]: 5530451
[tags]: 
Depending on what you are trying to do, the wysiwyg editor built into wordpress uses paragraph tags. Also I've noticed that most editors use a line break for a single return, but use a paragraph tag for two returns.
