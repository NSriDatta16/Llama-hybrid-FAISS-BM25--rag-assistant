[site]: stackoverflow
[post_id]: 3181192
[parent_id]: 3181169
[tags]: 
It doesn't look as if you need the each() function there. You may be applying the event handlers to the objects multiple times. Just: $('.close').click(function(){ $(this).parent().hide(); }); $('.colpase').click(function(){ $(this).siblings('.dragbox_content').toggle(); }); Should do the trick.
