[site]: stackoverflow
[post_id]: 5568818
[parent_id]: 5568578
[tags]: 
Change your draggable init code to include helper: "clone" $("selector").draggable({ ... helper: "clone", ... }); Otherwise you are dragging the original item which just gets left where you finished up the drag operation.
