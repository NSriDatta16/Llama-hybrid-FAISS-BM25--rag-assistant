[site]: stackoverflow
[post_id]: 4948118
[parent_id]: 4947919
[tags]: 
HI, try this..in your webview. mWebView.setWebChromeClient(new WebChromeClient() { // this will be called on page loading progress @Override public void onProgressChanged(WebView view, int newProgress) { super.onProgressChanged(view, newProgress); loadingProgressBar.setProgress(newProgress); // hide the progress bar if the loading is complete if (newProgress == 100) { loadingProgressBar.setVisibility(View.INVISIBLE); } else { loadingProgressBar.setVisibility(View.VISIBLE); } } }); refer this link https://sites.google.com/site/cganapathi/progressbar-in-webview-ac
