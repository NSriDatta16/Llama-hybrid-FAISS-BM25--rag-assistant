[site]: crossvalidated
[post_id]: 174647
[parent_id]: 174643
[tags]: 
I suggest looking into modeling this problem as an hierarchical Bayesian model and implementing it with pymc3 (out of the box). After sampling the posterior distribution, you could use a threshold on the higher levels, to default to a higher level class.
