[site]: crossvalidated
[post_id]: 386473
[parent_id]: 
[tags]: 
Does it Matter if Categorical Features are Standardized?

I have a dataset I am scaling in order to run a machine learning algorithm. I know it's bad practice to standardize binary features in regression, but I was wondering if it actually makes a difference in classification problems whether if categorical variables are standardized or not.
