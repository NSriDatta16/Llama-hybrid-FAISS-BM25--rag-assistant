[site]: stackoverflow
[post_id]: 730948
[parent_id]: 730926
[tags]: 
No, this will not be safe, as the only code that's locked is the retrieval code. What you need to do is lock(MyDict) { if(MyDict.TryGetValue()... } and lock(MyDict) { foreach(var item in MyDict.Values) ... } The basic idea is to enclose your working code within the lock() block.
