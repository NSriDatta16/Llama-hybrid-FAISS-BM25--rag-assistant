[site]: stackoverflow
[post_id]: 2755931
[parent_id]: 2755787
[tags]: 
I'd use SQLite for storage + have a last invalidated timestamp in Application subclass. It would get compared against System.currentTimeMillis() on each data access call to decide whether a new set should be fetched. Have a thin proxy layer for that.
