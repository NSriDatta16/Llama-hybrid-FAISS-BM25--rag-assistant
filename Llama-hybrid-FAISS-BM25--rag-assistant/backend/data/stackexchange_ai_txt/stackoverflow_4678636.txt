[site]: stackoverflow
[post_id]: 4678636
[parent_id]: 4678607
[tags]: 
You could concatenate the path with DateFormat.getDateInstance().format(new Date()); That is, use something like String time = DateFormat.getDateInstance().format(new Date()); dy_path = Environment.getExternalStorageDirectory() + "\\" + time + "\\5.jpg";
