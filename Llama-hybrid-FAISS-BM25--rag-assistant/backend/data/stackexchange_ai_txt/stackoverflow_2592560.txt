[site]: stackoverflow
[post_id]: 2592560
[parent_id]: 2592459
[tags]: 
@policy = File.read(RAILS_ROOT + '/config/my_policy.txt') To also cache the content (if you don't want to read it every time the variable is used): def policy @@policy ||= File.read(RAILS_ROOT + '/config/my_policy.txt') end If you need something more elegant for configuration, check configatronic .
