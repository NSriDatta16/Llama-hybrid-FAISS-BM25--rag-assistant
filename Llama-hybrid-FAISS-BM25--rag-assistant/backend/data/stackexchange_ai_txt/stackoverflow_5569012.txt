[site]: stackoverflow
[post_id]: 5569012
[parent_id]: 5568663
[tags]: 
Something like this: http://jsbin.com/orene3/2/edit ? A few improvements: only append to the document once; use a document fragment to create a temporary holder; use a sprite instead of individual images; and input the text inside the span and then text-indent to move them off the screen (for accessibility).
