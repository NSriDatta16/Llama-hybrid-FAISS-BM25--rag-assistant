[site]: crossvalidated
[post_id]: 291856
[parent_id]: 
[tags]: 
When would one use a hard margin SVM?

Is there ever a good case to use a hard margin SVM? It seems far more likely to overfit and doesn't seem computationally faster than a soft margin SVM, so there doesn't seem to be any reason to ever use it.
