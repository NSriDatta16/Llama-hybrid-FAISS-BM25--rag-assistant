[site]: stackoverflow
[post_id]: 1893674
[parent_id]: 1893636
[tags]: 
Jumm wrote: If I use fgets in the place of the fscanf above I get "\n" in the variable. Which is a far easier problem to solve so go with it: fgets( ap->name, MAX, fp ) ; nlptr = strrchr ( ap->name, '\n' ) ; if( nlptr != 0 ) { *nlptr = '\0' ; }
