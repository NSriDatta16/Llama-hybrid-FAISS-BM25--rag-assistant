[site]: stackoverflow
[post_id]: 589771
[parent_id]: 589717
[tags]: 
Or use some of the libraries like JQuery. Using JQuery, it's simple. Having Catch the key down event $("input#my-text").keydown(function(event) { switch (event.keyCode) { // space case 32: $(this).css("color","#f00"); //or set any other property break; } } If you want to highlight single word, you'll need to wrap it into span and change property just for this element. And yes, to highlight a sigle word you need to use paragraph.
