[site]: stackoverflow
[post_id]: 490235
[parent_id]: 
[tags]: 
hostname not translated into an IP address using Winsock

getaddrinfo() does not translate a hostname into an IP address and consequently does not connect() to the server. Is something wrong with my implementation - compiles with no warning messages? Is this function call to connect correct? connect(client, result->ai_addr, result->ai_addrlen) Full implementation listed below: #include #include #include #include #include #pragma comment(lib, "Ws2_32.lib") using namespace std; int main ( int argc, char* argv[]) { if (argc != 3) { cerr ai_addr, result->ai_addrlen)) { case 0: cerr
