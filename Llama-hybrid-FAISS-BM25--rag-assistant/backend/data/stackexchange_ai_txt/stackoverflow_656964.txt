[site]: stackoverflow
[post_id]: 656964
[parent_id]: 656951
[tags]: 
select * from table where Locate('rid ', FieldToSearch) > 0 or Locate(' rid', FieldToSearch) > 0 This will handle finding rid where it is preceded or followed by a space, you could extend the approach to take account of .,?! and so on, not elegant but easy.
