[site]: stackoverflow
[post_id]: 1346249
[parent_id]: 
[tags]: 
HTML/CSS - Paragraphs display line breaks from the source code

For some reason my paragraphs are displaying line breaks when there is no tag being used! So if I type content into the HTML and hit return a few times it gets formatted that way. How can I prevent that? Can I not just get it to flow?
