[site]: stackoverflow
[post_id]: 2705519
[parent_id]: 2703229
[tags]: 
Since you are adding a column you need to reset ActiveRecord's information about your model. The section Using a model after changing its table in ActiveRecord::Migration shows an example. This might work for you class AddTitleToPosts post.name.upcase) end end def self.down end end
