[site]: datascience
[post_id]: 114336
[parent_id]: 
[tags]: 
Predict time series classes 15 min before

I have multivariate time-series as input and I need to predict an event ("active" or "inactive"). The occurrence of class needs to be predicted (at least) 15 minutes before it occurs, so that take appropriate action can be taken to avoid that event, kind of fault prediction. So, the difference with classic time-series classification problems, is that I need the prediction of the time-series class to be done in advance. How could I do this? This is sample dataset:
