[site]: stackoverflow
[post_id]: 4095581
[parent_id]: 4095287
[tags]: 
Use saxon. offtop: if you use the same stylesheet to transform many XML files, you might want to consider templates (pre-compiled stylesheets): javax.xml.transform.Templates style = tFactory.newTemplates(xslSource); style.newTransformer().transform(...);
