[site]: stackoverflow
[post_id]: 1839565
[parent_id]: 
[tags]: 
How can I get nokogiri to select node attributes and add them to other nodes?

Is it possible to grab a following element's attributes and use them in the preceding one like this?: Section X Stuff Section Y Stuff into: 1. Section X Stuff 2. Section Y Stuff I have something like this but get nodeset or string errors: frag = Nokogiri::XML(File.open("test.xml")) frag.css('title').each { |text| text.set_attribute('id', "ID" + frag.css("title > paragraph['number']"))}
