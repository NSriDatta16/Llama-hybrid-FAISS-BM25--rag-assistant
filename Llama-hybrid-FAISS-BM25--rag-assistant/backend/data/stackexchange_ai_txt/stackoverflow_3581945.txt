[site]: stackoverflow
[post_id]: 3581945
[parent_id]: 
[tags]: 
Open pdf file from jsp page

I am trying to open a pdf file using ServletOutputStream in JSP. The code is: response.setContentLength(statementVO.getOutputStream().size()); response.setContentType("application/pdf"); response.setHeader("Expires", "0"); response.setHeader("Cache-Control", "must-revalidate, post-check=0, pre-check=0"); response.setHeader("Pragma", "public"); ServletOutputStream out = response.getOutputStream(); out.write(statementVO.getOutputStream().toByteArray()); out.flush(); out.close(); I am getting the following error WAS 6.0 - Response already committed / OutputStream already obtained
