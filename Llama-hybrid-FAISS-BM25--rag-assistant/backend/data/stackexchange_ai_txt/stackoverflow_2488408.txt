[site]: stackoverflow
[post_id]: 2488408
[parent_id]: 2487622
[tags]: 
Seems to me a perfect Factory Design Pattern Candidate e.g. you seem to want to call something like this ( in pseudocode: ´) //get the writer responsible for storing to any media ... Writer objWriter = new Writer ( configForCsvOrDb ) ; //perform the actual write according to the type of storage objWriter.Write(objToWrite ) ;
