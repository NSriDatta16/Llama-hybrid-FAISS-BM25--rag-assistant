[site]: stackoverflow
[post_id]: 5351427
[parent_id]: 5351342
[tags]: 
dont bother! why use iframes at all if you are using ajax. just load the login using $("where you want to load the content here").load("src of your iframe here"); //load via ajax $("div").load("login.php"); much simpler and alot more elegant.
