[site]: crossvalidated
[post_id]: 324953
[parent_id]: 324944
[tags]: 
There are several approaches. One simple approach would be to adapt binomial regression (such as logistic regression). Record a 1 every time a y-value is between the two limits, 0 when it is not, and model that as some convenient function of x (depending on what kinds of relationship you want to entertain)
