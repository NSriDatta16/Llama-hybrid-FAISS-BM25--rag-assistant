[site]: stackoverflow
[post_id]: 4765967
[parent_id]: 
[tags]: 
How NSValueTransformer works?

In sample iPhoneCoreDataRecipes , UIImage can be saved to database with core data tech. NSValueTransformer is used to do conversion from UIImage to NSData, and vice verse. But the question is that there is no obvious invocation of NSValueTransformer to do such converting in source codes. So just wonder that how this works ? A interesting discussion can be referred here
