[site]: crossvalidated
[post_id]: 415282
[parent_id]: 
[tags]: 
Why bayesian needs prior and neural net does not?

Bayesian requires prior distribution that should be magically taken from somewhere. Neural nets does not require that magical foreknowledge. Why Bayesian requires it, why can't it work without it like neural net?
