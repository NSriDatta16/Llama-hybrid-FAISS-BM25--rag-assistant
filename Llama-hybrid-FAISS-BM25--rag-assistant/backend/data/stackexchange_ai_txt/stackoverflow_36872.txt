[site]: stackoverflow
[post_id]: 36872
[parent_id]: 32709
[tags]: 
Robert S. is completely correct; for example:- class A { static void Main() { Func func = i => i > 100; Predicate pred = i => i > 100; Test (pred, 150); Test (func, 150); // Error } static void Test (Predicate pred, T val) { Console.WriteLine(pred(val) ? "true" : "false"); } }
