[site]: stackoverflow
[post_id]: 586504
[parent_id]: 586487
[tags]: 
I think page creators should use semantic markup, meaning that the markup they create should communicate meaning (and not presentation). and have different meanings. The former is used to define a division (or section) of an HTML page, the latter to define a paragraph of text.
