[site]: datascience
[post_id]: 32987
[parent_id]: 32955
[tags]: 
If you have apriori known labels, then use the labels, not clustering. Clustering is quite difficulty and fragile, so always prefer a simpler solution if you can. It's not very clear what you are trying to solve. Select columns to split the data into rooms? Just select the columns then.
