[site]: stackoverflow
[post_id]: 4278336
[parent_id]: 4278211
[tags]: 
Since Schema is XML too, you could try DOM to extract the parts of the Schema that would validate the complex type. Then load the fragment into a DOMDocument and use DOMDocument::schemaValidateSource .
