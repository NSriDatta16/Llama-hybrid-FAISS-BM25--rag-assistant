[site]: stackoverflow
[post_id]: 2578732
[parent_id]: 2578582
[tags]: 
import doesn't play well with Rake, so it's discouraged. java_import is the newest, and recommended. include_class has been deprecated. Apart from that they all do the same thing, and are actually aliases of each other. import is an alias for java_import and java_import calls include_class .
