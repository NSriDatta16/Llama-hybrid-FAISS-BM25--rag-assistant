[site]: crossvalidated
[post_id]: 167453
[parent_id]: 
[tags]: 
Can we do a T-Test when we have two columns of average data?

I have a data set assume as shown below: I wanted to know that can I use a paired T-Test to test if the average time spent is different Pre and Post change? (I am a little unsure because the each data point is an average in itself)
