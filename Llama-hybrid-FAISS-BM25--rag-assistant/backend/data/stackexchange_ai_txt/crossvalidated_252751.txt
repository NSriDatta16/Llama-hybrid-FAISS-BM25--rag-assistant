[site]: crossvalidated
[post_id]: 252751
[parent_id]: 252600
[tags]: 
Yes. A simple confirmation is just to implement it. Here is an implementation in R Tree = hclust(DistMatrix, method="average") plot(Tree) plot(Tree, hang=-1) n = 8 Points = 2^(1:n) Points [1] 2 4 8 16 32 64 128 256 DistMatrix = dist(Points) Tree = hclust(DistMatrix, method="average") plot(Tree, hang=-1)
