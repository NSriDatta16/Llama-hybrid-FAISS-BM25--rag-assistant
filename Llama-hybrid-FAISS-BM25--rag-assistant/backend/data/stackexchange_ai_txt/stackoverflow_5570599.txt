[site]: stackoverflow
[post_id]: 5570599
[parent_id]: 5570005
[tags]: 
Have you considered rewriting your functions as MSBuild tasks? That way you can leverage the pre-existing msbuild tasks, and write your script as an msbuild file (which fits nicely in any build process). You can even debug MSBuild tasks
