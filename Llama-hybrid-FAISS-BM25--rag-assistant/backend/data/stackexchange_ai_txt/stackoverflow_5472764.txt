[site]: stackoverflow
[post_id]: 5472764
[parent_id]: 
[tags]: 
Exclude ie6 from a php include

I have a script that works fine in all browsers except ie6 (including ie7 and ie8). Is there any semi reliable way I can exclude this nefarious browser. I've tried this: and this: $user_agent = getenv("HTTP_USER_AGENT"); if (preg_match("MSIE 6", $user_agent)) { include("fconditionals.php"); }
