[site]: stackoverflow
[post_id]: 1757182
[parent_id]: 1757159
[tags]: 
If by Agent theAgent(void); you meant function declaration then you probably forgot to add () to the function call int m = theAgent().loadSAG(); If you wanted to define a variable with the name theAgent then you have added extra (void) . and instead you should wrote Agent theAgent;
