[site]: stackoverflow
[post_id]: 3800387
[parent_id]: 3800347
[tags]: 
One way might be to place the co-ordinates, on drag stop, into a hidden field that you can then read in the code behind and store in the db. When you re-render the page, you would also need to fill the field and use javascript to place the objects back in their places. Also, it might be handy to mention what code behind language you are using. C#, PHP, Java, What?
