[site]: stackoverflow
[post_id]: 3484874
[parent_id]: 
[tags]: 
How to split a HTML document using Nokogiri?

Right now, I'm splitting the HTML document to small pieces like this: (regular expression simplified - skipping header tag content and closing tag) document.at('body').inner_html.split(/ ]*>/i).collect do |fragment| Nokogiri::HTML(fragment) end Is there an easier way to perform that splitting? The document is very simple, just headers, paragraphs and formatted text in it. For example: Main Sub 1 Text ----- Sub 2 Text ----- Sub 2.1 Text ----- Sub 2.2 Text For that sample, I need to get four pieces.
