[site]: stackoverflow
[post_id]: 5559572
[parent_id]: 5559523
[tags]: 
You want to do: This way, if you have no ratings, it will default to 0, thus not throwing an error but also providing the desired results of setting the width to 0. If you do have reviews, it will short circuit to the average rating for that venue. The problem here was that .average() was returning nil when there wasn't any reviews, so it was doing (nil * 20)... which obviously doesn't work.
