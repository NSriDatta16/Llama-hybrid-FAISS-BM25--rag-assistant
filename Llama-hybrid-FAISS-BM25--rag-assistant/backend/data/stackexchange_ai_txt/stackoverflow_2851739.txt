[site]: stackoverflow
[post_id]: 2851739
[parent_id]: 2851386
[tags]: 
to answer your question Is it possible to have a MVC organization structure to your coding without using cake, codeigniter, etc? Basically just separate say profile.php into 3 different files(the view, controller, model) absolutely... first file profile.php ( the view, what gets hit by the browser ) profile(); $pageData = $controller->data; ?> the controller model = new UserModel(); } public function profile(){ $this->data = $this->model->findUser(); } } the model
