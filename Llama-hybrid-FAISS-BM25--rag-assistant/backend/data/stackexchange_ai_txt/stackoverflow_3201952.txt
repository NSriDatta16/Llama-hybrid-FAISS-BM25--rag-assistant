[site]: stackoverflow
[post_id]: 3201952
[parent_id]: 3201870
[tags]: 
From the HTML Compatibility Guidelines : Include a space before the trailing / and > of empty elements, e.g. , and . Also, use the minimized tag syntax for empty elements, e.g. , as the alternative syntax allowed by XML gives uncertain results in many existing user agents.
