[site]: stackoverflow
[post_id]: 433021
[parent_id]: 432226
[tags]: 
Scott's got it, although it's even better (and cleaner!) to use an anonymous function: oneCent.addEventListener(MouseEvent.MOUSE_MOVE, function(e:MouseEvent):void{ ... }); Which is better if you're not gonna use dragIt() later in your code.
