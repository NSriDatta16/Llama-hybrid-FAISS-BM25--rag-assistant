[site]: stackoverflow
[post_id]: 226715
[parent_id]: 226689
[tags]: 
In javascript, you could attach a custom ID field to the node if(node.id) { node.myId = node.id; } else { node.myId = createId(); } // store myId It's a bit of hack, but it'll give each and every node an id you can use. Of course, document.getElementById() won't pay attention to it.
