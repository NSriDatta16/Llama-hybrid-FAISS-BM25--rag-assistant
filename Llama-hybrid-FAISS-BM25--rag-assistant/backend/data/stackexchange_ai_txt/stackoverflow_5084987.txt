[site]: stackoverflow
[post_id]: 5084987
[parent_id]: 5084921
[tags]: 
You have to maintain the same assembly version (i.e. don't increment it across builds) — see the AssemblyVersionAttribute in MSDN . Also, you could leverage assembly binding redirects, but that involves config file changes which I don't expect to be desirable in your case.
