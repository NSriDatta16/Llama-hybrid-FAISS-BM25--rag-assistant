[site]: crossvalidated
[post_id]: 383954
[parent_id]: 383952
[tags]: 
I would check if the features are correlated. That could be a possible explanation. Do PCA on the dataset and set the n_components to a small number and see the explained variance. I assume it would be high.
