[site]: stackoverflow
[post_id]: 807808
[parent_id]: 
[tags]: 
simple cout then cin allowing whitespace example?

Looking for a simple getline example which works correctly. I want to input something on the keyboard and assign it to a std::string, allowing for whitespace and tabs. The delimiter is the carriage return. TIA, Bert
