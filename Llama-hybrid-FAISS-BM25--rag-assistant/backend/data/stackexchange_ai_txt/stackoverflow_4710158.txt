[site]: stackoverflow
[post_id]: 4710158
[parent_id]: 4710057
[tags]: 
Ancestor * Derived::foo { // Do whatever return new Descendant ( ); } If you can use pointers, then just return an upcasted Decendant pointer. The returned pointer will point to a Descendant and, assuming methods are virtual, act like a Descendant as well.
