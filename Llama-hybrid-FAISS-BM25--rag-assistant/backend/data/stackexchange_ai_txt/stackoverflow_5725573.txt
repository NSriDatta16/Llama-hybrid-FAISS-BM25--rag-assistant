[site]: stackoverflow
[post_id]: 5725573
[parent_id]: 5725535
[tags]: 
WHy not check what $_POST is before blindly inserting it into your database? I could hack up a script that sets id = 'Robert\'; drop table students -- and kill your database. $id = intval($_POST['id']); $sql = "UPDATE `table` SET x=x+1 WHERE id=$id"; $result = mysql_query($sql); if ($result === FALSE) { die("query failed: " . mysql_error()); }
