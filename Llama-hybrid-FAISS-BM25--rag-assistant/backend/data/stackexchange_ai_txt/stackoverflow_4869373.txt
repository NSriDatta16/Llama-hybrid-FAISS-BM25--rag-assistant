[site]: stackoverflow
[post_id]: 4869373
[parent_id]: 
[tags]: 
How to compile Templates located in different files in C++?

When I put all the source in one file, the program successfully builds. However when I split them into header files, I get a link error. The main of my program: //C++_Class_Templates.cpp #include #include #include "Queue.h" using namespace std; //Usage for C++ class templates void main() { MyQueue q; q.Add(1); q.Add(2); } The Queue.h looks like this #pragma once #include template class MyQueue { std::vector data; public: void Add(T const &); void Remove(); void Print(); }; and the Queue.cpp looks like this: #include "Queue.h" template void MyQueue ::Add(T const &d) { data.push_back(d); } When I try to build it, I get this error: 1>main.obj : error LNK2019: unresolved external symbol "public: void __thiscall MyQueue ::Add(int const &)" (?Add@?$MyQueue@H@@QAEXABH@Z) referenced in function _main
