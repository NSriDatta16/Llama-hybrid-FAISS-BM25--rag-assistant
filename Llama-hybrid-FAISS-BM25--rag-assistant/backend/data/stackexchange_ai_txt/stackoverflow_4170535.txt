[site]: stackoverflow
[post_id]: 4170535
[parent_id]: 4170457
[tags]: 
You can use nested class approach. Probably not the most eligant one since tightly coupled but will do the trick. public class DataBase { private class Group { private Group() {} } private Group group = null; public DataBase() { this.group = new Group(); } public Group { get { return this.group; } }
