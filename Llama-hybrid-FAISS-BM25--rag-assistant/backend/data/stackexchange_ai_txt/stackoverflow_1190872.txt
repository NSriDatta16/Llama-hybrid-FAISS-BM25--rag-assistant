[site]: stackoverflow
[post_id]: 1190872
[parent_id]: 1190743
[tags]: 
This MSDN forum post uses this example as a solution. using (MemoryStream stream = new MemoryStream(abyteArray0)) { image.Source = BitmapFrame.Create(stream, BitmapCreateOptions.None, BitmapCacheOption.OnLoad); } //The field image should be of type System.Windows.Controls.Image. I used the BitmapFrame.Create method that takes only the stream as a parameter, and it worked like a charm.
