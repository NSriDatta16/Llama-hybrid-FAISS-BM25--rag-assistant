[site]: stackoverflow
[post_id]: 4347949
[parent_id]: 4347904
[tags]: 
Good question, but I'd actually start with indexes on both began_at and finished_at , because one reasonable way you might rewrite this query is like so SELECT * FROM runs WHERE began_at NOW() Which makes it a bit more clear (to me) that each column needs its own index.
