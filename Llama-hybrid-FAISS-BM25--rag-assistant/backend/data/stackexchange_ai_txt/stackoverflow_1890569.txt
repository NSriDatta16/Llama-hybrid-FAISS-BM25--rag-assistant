[site]: stackoverflow
[post_id]: 1890569
[parent_id]: 1890350
[tags]: 
The easiest way to leverage existing work, is to use Interop to call a native library capable of handling PAR2 archives, and just write a .NET wrapper for it. Avoid reinventing the wheel, unless there is some reason NIH line of thought is required?
