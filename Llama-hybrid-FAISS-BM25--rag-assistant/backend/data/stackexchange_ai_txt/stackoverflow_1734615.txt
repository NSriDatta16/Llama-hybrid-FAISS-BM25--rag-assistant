[site]: stackoverflow
[post_id]: 1734615
[parent_id]: 1734446
[tags]: 
this reacts only to the div it is in more text in this div that will be overlapped by the paragraph with the class of &quot;watermark.&quot; can't style all &lt;p&gt; in the dive. or each block would overlap each other, and only the top one could be read. this one is not affected by the paragraph above Basically, the div gets relative positioning, the p gets absolute (which is relative to it's relative container, the div). make sure the p is it's own class, not a child of the div.
