[site]: stackoverflow
[post_id]: 5000968
[parent_id]: 5000684
[tags]: 
Unfortunately there’s no CFAutorelease . How about changing the name of the cache retrieval method to make it obvious you are returning a copy? - (CGLayerRef) copyLayerForCacheKey: (CacheKey) key andProperty: (id) property; Then even the static analyzer should understand the difference.
