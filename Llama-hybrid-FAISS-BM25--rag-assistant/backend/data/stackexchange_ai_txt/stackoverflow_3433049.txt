[site]: stackoverflow
[post_id]: 3433049
[parent_id]: 3431674
[tags]: 
var content = 'asdf', fragment = document.createRange().createContextualFragment(content), parent = document.getElementById("contents-table"); parent.replaceChild(fragment, parent.firstElementChild); considering that there is only one node in the parent node, this should work
