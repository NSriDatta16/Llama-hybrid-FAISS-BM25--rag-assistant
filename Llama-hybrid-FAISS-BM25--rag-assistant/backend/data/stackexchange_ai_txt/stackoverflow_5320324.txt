[site]: stackoverflow
[post_id]: 5320324
[parent_id]: 
[tags]: 
testing if a numpy array is symmetric?

Is there a better pythonic way of checking if a ndarray is diagonally symmetric in a particular dimension? i.e for all of x (arr[:,:,x].T==arr[:,:,x]).all() I'm sure I'm missing an (duh) answer but its 2:15 here... :) EDIT: to clarify, I'm looking for a more 'elegant' way to do : for x in range(xmax): assert (arr[:,:,x].T==arr[:,:,x]).all()
