[site]: stackoverflow
[post_id]: 3599979
[parent_id]: 
[tags]: 
Unable to change comments form using comment_form_default_fields hook

Hi Im having trouble adding my custom fields to the default twentyten comment form. This is what im doing in the functions.php file of my child theme- am i forgetting something? // Customise comment form for garage-sales page if ( is_page('my-page') ): add_filter('comment_form_default_fields','my_comment'); function my_comment($fields) { // New fields $fields['date'] = ' Date: '; $fields['start_time'] = ' Start time: '; $fields['end_time'] = ' End time: '; $fields['early_birds'] = ' No early birds?: '; $fields['bad_weather'] = ' Bad weather?: '; // Remove unwanted default fields unset($fields['url']); return $fields; } endif;
