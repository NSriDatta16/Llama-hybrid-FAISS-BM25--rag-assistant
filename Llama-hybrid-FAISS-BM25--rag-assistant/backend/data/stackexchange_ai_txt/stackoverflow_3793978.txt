[site]: stackoverflow
[post_id]: 3793978
[parent_id]: 
[tags]: 
php strip_tags: allows ?

How it is possible to allow in strip_tags() or any way I can get around to it? Test paragraph. Other text '; echo strip_tags($text); echo "\n"; // Allow , , echo strip_tags($text, ' '); echo "\n"; // Allow only echo strip_tags($text, ' '); ?> result: Test paragraph. Other text Test paragraph. Other text Test paragraph. Other text Thanks, Lau
