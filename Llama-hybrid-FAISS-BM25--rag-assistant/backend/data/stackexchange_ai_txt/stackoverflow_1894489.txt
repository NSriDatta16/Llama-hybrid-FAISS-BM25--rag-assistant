[site]: stackoverflow
[post_id]: 1894489
[parent_id]: 1894451
[tags]: 
The diagnostic here is not great. You can fix the code like so: let stuff = [for a in 1 .. 10 do yield (p [text ("some String " + a.ToString())]) :> HtmlString] where I've inserted an explicit upcast to the base type.
