[site]: stackoverflow
[post_id]: 5156187
[parent_id]: 5156046
[tags]: 
First you should get the root directory with the static method Environment.getExternalStorageDirectory(); which returns a File. Then you can work like in traditional Java with it's I/O API. Inside the emulator, in your code, you can navigate through files and directories with no problem whatsoever.
