[site]: stackoverflow
[post_id]: 5148683
[parent_id]: 4835694
[tags]: 
It depends on the container. Fragmented H.264 can contain live streams. It can be transmitted thru HTTP. Google's WebM can also be streamed over HTTP. It is unlikely that HTML5 will add any delivery methods other than HTTP. So no RTP or UDP on the horizon.
