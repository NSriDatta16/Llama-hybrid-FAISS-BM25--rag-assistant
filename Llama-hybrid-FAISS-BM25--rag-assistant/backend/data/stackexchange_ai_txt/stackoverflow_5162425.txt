[site]: stackoverflow
[post_id]: 5162425
[parent_id]: 5161263
[tags]: 
You have to open it from the local server. If it is located on your server you can do FileStream fs = new FileStream(Server.MapPath("/example.pdf"),FileMode.Open) If you want to load it from an URL you have to download the PDF first. WebClient client = new WebClient (); // Add a user agent header in case the // requested URI contains a query. client.Headers.Add ("user-agent", "Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.2; .NET CLR 1.0.3705;)"); Stream data = client.OpenRead (args[0]); StreamReader reader = new StreamReader (data); string s = reader.ReadToEnd ();`
