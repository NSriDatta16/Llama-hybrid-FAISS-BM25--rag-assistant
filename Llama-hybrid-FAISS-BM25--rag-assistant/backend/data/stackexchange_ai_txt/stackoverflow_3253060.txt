[site]: stackoverflow
[post_id]: 3253060
[parent_id]: 
[tags]: 
WinPCap - how can I get protocol (e.g. HTTP) within TCP packet & HTTP fields?

As background I'm want to be able to, within a capture access: what is the protocol within the TCP packets, e.g. HTTP? (specifically I'm after filter on web traffic) what is the Length of the HTTP part Q1 - Does WinPCap support getting this? Q2 - If no, any recommendations re how to? thanks
