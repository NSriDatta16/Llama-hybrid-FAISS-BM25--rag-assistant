[site]: stackoverflow
[post_id]: 4088331
[parent_id]: 4088013
[tags]: 
Not really an elegant solution but works for me. if ($this->Main->saveAll($this->data)) { $this->Main->query(sprintf( 'DELETE ' . 'FROM extraneous ' . 'WHERE main_id = \'%s\' AND modified Main->id) )); } Note that your tables need to have a modified field.
