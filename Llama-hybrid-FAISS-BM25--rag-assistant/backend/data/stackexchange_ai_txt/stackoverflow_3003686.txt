[site]: stackoverflow
[post_id]: 3003686
[parent_id]: 3003668
[tags]: 
You do not need to use div's to do this, you can style the elements themselves. The tag by default does not understand text-align, as it is an inline element, so I have placed the link in a paragraph, which accepts text-align. I have placed the two lines in a tag with a small width so it is easy to see what is going on. Sample Heading sample link
