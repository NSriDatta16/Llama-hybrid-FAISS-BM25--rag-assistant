[site]: stackoverflow
[post_id]: 4880675
[parent_id]: 4880603
[tags]: 
What technology are you using? If you have control over what you are rendering (server side) to the page you can check for the USER-AGENT header in the request. If it contains the string iPad, then render out the css tag, if not, don't include it. If you are just delivering static html, this will not work.
