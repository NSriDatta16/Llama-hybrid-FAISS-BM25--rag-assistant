[site]: stackoverflow
[post_id]: 622875
[parent_id]: 597664
[tags]: 
They should almost always be discouraged. The only type of code that I can think of where it would be required is low-level code that requires some pointer voodoo. And to answer your question, C supports code like that (except of course for not having a string type), and that sounds like something PHP or Perl would have (but I could be totally wrong on that).
