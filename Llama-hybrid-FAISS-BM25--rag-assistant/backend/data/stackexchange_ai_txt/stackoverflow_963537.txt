[site]: stackoverflow
[post_id]: 963537
[parent_id]: 963125
[tags]: 
UIImage 's can have borders at the sides and bottom that don't resize as the image is stretched. The leftCapWidth and topCapHeight are the 2 properties you need to modify to achieve this. Then create a UIImageView (using the image) of the necessary size behind your text.
