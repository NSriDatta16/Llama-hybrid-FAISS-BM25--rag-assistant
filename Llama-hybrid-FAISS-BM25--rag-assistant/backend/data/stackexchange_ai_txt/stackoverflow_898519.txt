[site]: stackoverflow
[post_id]: 898519
[parent_id]: 
[tags]: 
Regex Pattern for Whitespace

I am creating a regex library to work with HTML (I'll post it on MSDN Code when it's done). One of the methods removes any whitespace before a closing tag. See the dog run It would eliminate the space before the closing paragraph. I am using this: public static string RemoveWhiteSpaceBeforeClosingTag(string text) { string pattern = @"(\s+)(?: As you can see I am replacing the spaces with
