[site]: crossvalidated
[post_id]: 235518
[parent_id]: 
[tags]: 
stacked autoencoder decoder and deconvolutional network

The convolution-deconvolutional architecture can be used for image segmentation or re-construction. These types of tasks can also be done using the architecture of stacked autoencoder-decoder. But what are the underlying differences between these two architecture? Are they generating different types feature hierarchy? What are their respective advantages and disadvantages?
