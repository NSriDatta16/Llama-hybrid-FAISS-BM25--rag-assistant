[site]: stackoverflow
[post_id]: 2823360
[parent_id]: 2823330
[tags]: 
header files usually define a unique symbol so that they are only included once. e.g. #ifndef _myheader_h #define _myheader_h // rest of header goes here #endif Some compilers support #pragma once See Pragma Once on wikipedia.
