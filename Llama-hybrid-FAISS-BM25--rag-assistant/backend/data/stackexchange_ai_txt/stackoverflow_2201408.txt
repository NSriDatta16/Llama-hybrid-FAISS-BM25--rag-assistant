[site]: stackoverflow
[post_id]: 2201408
[parent_id]: 
[tags]: 
Jquery UI tabs - Custom "Loader" for AJAX tabs

I have the following code to generate Jquery UI tabs: Loading, Please wait.. Animals Birds $(function() { $("#tabs-loading-message").show(); $('#fragment-2').tabs( { cache:false, spinner:'', selected: 0 , select: function(event,ui) { //show spinner $("#tabs-loading-message").show(); }, load: function() { // hide spinner $("#tabs-loading-message").hide(); } } ); }); I am able to display the loading message, but how can I hide the contents of the tab panel, when it's selected and show the contents when loaded?
