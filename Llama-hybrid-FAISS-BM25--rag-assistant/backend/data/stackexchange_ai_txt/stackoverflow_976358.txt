[site]: stackoverflow
[post_id]: 976358
[parent_id]: 976275
[tags]: 
Another option is to add this #pragma warning (defualt) at the top of the file right after the #include s This resets the warning to the defualt, discarding any ignore which might have been called. On a different note, I find it highly unlikely that an Microsoft header would disable a warning.
