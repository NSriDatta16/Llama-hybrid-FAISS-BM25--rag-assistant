[site]: stackoverflow
[post_id]: 2440975
[parent_id]: 2440963
[tags]: 
Looks like what you are trying to do is "revert" .b back to its original place after dropped. Try doing this: $(".b").draggable({ revert: true, revertDuration: 0 }); $("#map_canvas").droppable({ drop: function(event,ui) { //console.log(ui.offset.left+' '+ui.offset.top) } });
