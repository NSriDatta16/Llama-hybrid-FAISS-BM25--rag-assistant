[site]: stackoverflow
[post_id]: 2505911
[parent_id]: 
[tags]: 
Add remove field in JavaScript

I want to add and remove some html input in JavaScript. I have done some code for the add function and it work properly, but I'm unable to remove. My code is following: fields = 1; function addInput() { if (fields != 10) { document.getElementById('text').innerHTML += " Add | Remove "; fields += 1; } else { document.getElementById('text').innerHTML += " Only 10 upload fields allowed."; document.form.add.disabled=true; } } fields1=10 function removeInput() { if (fields1 !=1) { document.getElementById('text').innerHTML += ""; fields -= 1; } } and my PHP function is: function addSession() {?> Add Session Datum Tijd Module Organisatie Category Action Add | Remove Can anyone give me a solution?
