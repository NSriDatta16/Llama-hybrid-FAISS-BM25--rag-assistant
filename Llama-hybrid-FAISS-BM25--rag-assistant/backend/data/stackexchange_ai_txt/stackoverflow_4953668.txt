[site]: stackoverflow
[post_id]: 4953668
[parent_id]: 4953258
[tags]: 
C++03 and C++0x: #include int count = sizeof(arr)/sizeof(arr[0]); int sum = std::accumulate (arr,arr + count,0); double average = (double)sum/count; Online Demo : http://www.ideone.com/2YXaT C++0x Only (using lambda) #include int sum = 0; std::for_each(arr,arr+count,[&](int n){ sum += n; }); double average = (double)sum/count; Online Demo : http://www.ideone.com/IGfht
