[site]: datascience
[post_id]: 114523
[parent_id]: 114520
[tags]: 
Some models, like XGBoost, accept NaN values. So if you use them, you could just keep them like that. I wouldn't use impossible values, like -1, because any type of model WILL interpret it as a valid value (at least it's the case of GB trees, GLMs, Naive Bayes and SVMs)
