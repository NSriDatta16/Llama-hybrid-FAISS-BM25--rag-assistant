[site]: crossvalidated
[post_id]: 206536
[parent_id]: 190937
[tags]: 
You can use either the mnist autoencoder example as a semi-supervised approach to extract features or if you have a labeled data you can train a regular CNN (e.g., Alexnet) then extract the output of each layer as an output feature. For the latter approach you can have a look at this post and this tutorial .
