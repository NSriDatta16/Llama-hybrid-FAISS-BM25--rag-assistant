[site]: crossvalidated
[post_id]: 184514
[parent_id]: 
[tags]: 
Forecasting with ARMA models - how do you estimate the error terms for use with the MA coefficients?

I've seen versions of this asked before, but haven't seen a satisfactory answer. Lets say you have fitted an ARMA model: $$Z_t = \psi_1Z_{t-1}+\psi_2Z_{t-2}+\theta_1\varepsilon_{t-1}+\theta_2\varepsilon_{t-2}+\varepsilon_{t}$$ We know the coefficients and we know the $Z_t$ time series values. However, we don't know the error terms $\varepsilon_i$. My confusion is how to actually use the above model to make forecasts when I cannot plug in the $\varepsilon$ values? How would I forecast $Z_t$ using this model given a set of prior observations $Z_q, q
