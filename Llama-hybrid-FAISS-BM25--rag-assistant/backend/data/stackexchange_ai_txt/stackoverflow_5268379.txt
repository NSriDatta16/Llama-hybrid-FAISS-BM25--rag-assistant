[site]: stackoverflow
[post_id]: 5268379
[parent_id]: 
[tags]: 
Activity not finalizing?

Is there any reason why an activity might not be finalized (i.e. have its finalize() called) and therefore garbage collected, even though its onDestroy() is called? Is this just your average everyday memory leak?
