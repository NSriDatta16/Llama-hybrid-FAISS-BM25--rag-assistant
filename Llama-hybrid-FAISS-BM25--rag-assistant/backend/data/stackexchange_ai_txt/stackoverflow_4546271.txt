[site]: stackoverflow
[post_id]: 4546271
[parent_id]: 
[tags]: 
pre-populating DOB fields in Magento registration form

I am trying to pre-populate some fields in Magento's customer registration form. I was able to get the formData from the register block and do things like ->setFirstname() which works great, but I am having trouble doing the same with the DOB widget. Any idea on how this can be done? Eyal
