[site]: stackoverflow
[post_id]: 3758231
[parent_id]: 3757693
[tags]: 
A couple of thoughts. 1/ You seems to be using the string comparison operators (le, ne) to compare numbers. You should use the numeric comparison operators ( 2/ The value you get back from the LWP::UserAgent::get call is an HTTP::Response object. Judicious use of that class's "is_foo" method might make your code a bit cleaner. I don't know if either of these will solve your problem. But they'll improve the quality of your code.
