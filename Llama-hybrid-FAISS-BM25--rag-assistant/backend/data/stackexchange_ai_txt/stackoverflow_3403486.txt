[site]: stackoverflow
[post_id]: 3403486
[parent_id]: 3403442
[tags]: 
There are a number of ways: You can add `@SuppressWarnings("unchecked") before the assignment to tell the compiler to ignore the warning Use raw types List[] arrayOfList = new List[10]; notice that in Java you usually put the [] after the type not the variable when declaring it - though using raw types is discouraged by Sun since they might be removed in a future version. Don't use arrays, it's usually a bad idea to mix collections with arrays: List > listOfList = new ArrayList >;
