[site]: stackoverflow
[post_id]: 1869209
[parent_id]: 1869107
[tags]: 
You can try leveraging locking reads to allow concurrency to ensure that you're operating as a singleton. NOTE: This will block all other calls and they will execute serially. Make sure this is the behavior you're after.
