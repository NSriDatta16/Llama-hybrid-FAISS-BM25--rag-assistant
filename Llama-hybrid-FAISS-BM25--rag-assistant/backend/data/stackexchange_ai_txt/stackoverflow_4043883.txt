[site]: stackoverflow
[post_id]: 4043883
[parent_id]: 4043837
[tags]: 
Your HTML is invalid. You cannot have an element inside a element. When you start the you end the paragraph (implicitly as end tags are optional for ). The browser then sees as an error and silently discards it. Get rid of the , whatever that data is, it isn't a sub-sub-heading anyway. is nonsense as well. You probably mean . It looks like you would do well to learn to use http://validator.w3.org/ — it does a very quick and easy first pass QA for markup.
