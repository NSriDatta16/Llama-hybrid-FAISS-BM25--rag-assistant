[site]: stackoverflow
[post_id]: 5736704
[parent_id]: 
[tags]: 
What is an elegant way to convert IDictionary to IDictionary

I have a method I am not happy with, can you please show me how to do this better. public Foo WithBar(IDictionary parameters) { var strStrDict = new Dictionary (parameters.Count); foreach(var pair in parameters) { strStrDict.Add(pair.Key, pair.Value != null ? pair.Value.ToString() : (string)null); } // Call overload which takes IDictionary return this.WithBar(strStrDict); } This code works but I'm sure there is a nice linq'y way of doing this I am missing.
