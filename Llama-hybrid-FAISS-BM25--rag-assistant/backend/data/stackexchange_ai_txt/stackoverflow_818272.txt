[site]: stackoverflow
[post_id]: 818272
[parent_id]: 818243
[tags]: 
You won't escape scanning all files, but that is limited to reading the directory sectors. Still takes a while for an average drive. Aside from the file-date there is an Archive bit on every file entry. Don't forget to clear that after backing up a file. So the complexity will be O(n), where n is the number of files.
