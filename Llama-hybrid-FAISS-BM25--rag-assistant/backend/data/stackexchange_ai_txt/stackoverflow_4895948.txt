[site]: stackoverflow
[post_id]: 4895948
[parent_id]: 4598404
[tags]: 
You have 3 choices: Make the outer class serializable as well. Make your serializable inner class static. Create another class and make that class serializable (don't serialize an inner class). First one is actually discouraged. And as Mayra mentioned, it will work faster if you implement Parcelable.
