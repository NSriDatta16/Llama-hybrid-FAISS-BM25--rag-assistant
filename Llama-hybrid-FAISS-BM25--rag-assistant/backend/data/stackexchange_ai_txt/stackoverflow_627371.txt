[site]: stackoverflow
[post_id]: 627371
[parent_id]: 627316
[tags]: 
Here's a way to do it - though I hope there's a more elegant way. :execute ':e ' . substitute(substitute(expand('%'), ' \zs\d\+', \=submatch(0) + 1', ''), ' ', '\\ ', '') I have to add a second substitute() command to escape the space in the filename.
