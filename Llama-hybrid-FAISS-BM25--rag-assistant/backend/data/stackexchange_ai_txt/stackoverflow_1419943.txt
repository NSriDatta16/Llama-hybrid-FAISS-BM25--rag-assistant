[site]: stackoverflow
[post_id]: 1419943
[parent_id]: 1419919
[tags]: 
The abstract model does not exist as a table in your database, and it can't be queried. You will probably have to write two queries separately on the Bar and Baz models, and then merge the results. You could wrap this into a method in your Foo class for organizational purposes.
