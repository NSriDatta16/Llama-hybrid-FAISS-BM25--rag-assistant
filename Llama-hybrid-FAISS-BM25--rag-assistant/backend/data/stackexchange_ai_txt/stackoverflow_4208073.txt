[site]: stackoverflow
[post_id]: 4208073
[parent_id]: 2524301
[tags]: 
I know it's a bit verbose, but here's how we solved it: SOAPFactory factory = OMAbstractFactory.getSOAP12Factory(); OMElement requestElement = request.getOMElement(RegisterIntegrationAgent.MY_QNAME, factory); LOGGER.debug(requestElement);
