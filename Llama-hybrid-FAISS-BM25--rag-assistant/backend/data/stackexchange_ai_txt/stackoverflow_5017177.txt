[site]: stackoverflow
[post_id]: 5017177
[parent_id]: 5017099
[tags]: 
How about: #ifndef NDEBUG #define DEBUG_VAR(T) static nullclass #endif Now no additional storage is added to a class where DEBUG_VAR(T) is used as a member, but the declared identifier can still be used as though it were a member.
