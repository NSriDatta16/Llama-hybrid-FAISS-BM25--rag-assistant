[site]: stackoverflow
[post_id]: 4857725
[parent_id]: 3704293
[tags]: 
VTDNav.getContentFragment returns a long, the upper 32 bits is the length of the fragment, lower 32 bit is the offset, to retrieve the document text between tags, see the example below ... VTDNav vn = vg.getNav(); long l = vn.getContentFragment(); FileOutputStream fos = new FileOutputStream("out.txt"); fos.write(vn.getXML().getBytes(), (int)l, (int)(l
