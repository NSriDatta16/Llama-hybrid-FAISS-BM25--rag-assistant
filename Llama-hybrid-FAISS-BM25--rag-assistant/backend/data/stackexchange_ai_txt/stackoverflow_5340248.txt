[site]: stackoverflow
[post_id]: 5340248
[parent_id]: 5340168
[tags]: 
Don't assign your function directly to the event handler, but to a local variable ans assign that to both: var myHandler = function() { ... }; $('#new_average, #max_occupancy').keyup(myHandler); $(".industry_picker').change(myHandler);
