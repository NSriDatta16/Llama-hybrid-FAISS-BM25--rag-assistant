[site]: stackoverflow
[post_id]: 1594597
[parent_id]: 1594543
[tags]: 
The easiest approach would be nearest-neighbor downsampling, like this: for (int x = 0; x But this doesn't give nice results, so you might need other approaches that use several input pixels and average them to get a more exact color for the original region.
