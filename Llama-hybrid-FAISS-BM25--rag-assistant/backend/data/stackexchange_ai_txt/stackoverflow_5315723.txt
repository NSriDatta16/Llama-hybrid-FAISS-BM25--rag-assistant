[site]: stackoverflow
[post_id]: 5315723
[parent_id]: 5310605
[tags]: 
Use the if module. It uses goto to hide its own stack frame, so it's as if the pragma was called directly. The solutions given previously may work for bigint and most pragmas, but they will fail for import functions that use caller .
