[site]: stackoverflow
[post_id]: 3446752
[parent_id]: 3446693
[tags]: 
First of all: your problem is realy very basic and you should consider reading a book ;-). What you can do is to use the normal binding on the page... If you are using plain old sql your code might look like this. But if you are using Linq-To-Sql or event Entity Framework it is a different story. Attention : in this example is realy straight forward. protected DataRow _currentRow = null; protected void Page_Load(object sender, EventArgs e) { using (SqlConnection connection = new SqlConnection(ConfigurationManager.ConnectionStrings["pubs"].ConnectionString)) { using (SqlDataAdapter adapter = new SqlDataAdapter("SELECT * FROM authors", connection)) { DataTable table = new DataTable(); adapter.Fill(table); _currentRow = table.Rows[0]; } } Page.DataBind(); } Testpage '>
