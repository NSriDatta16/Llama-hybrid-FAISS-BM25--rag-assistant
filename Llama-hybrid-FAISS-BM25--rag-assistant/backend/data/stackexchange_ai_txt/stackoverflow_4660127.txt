[site]: stackoverflow
[post_id]: 4660127
[parent_id]: 
[tags]: 
Can't drop jquery ui helper on droppable

Why will this code not let me drop the helper onto the droppable region? $(".product").draggable({ revert: 'invalid', cursorAt: { top: -12, left: -20 }, helper: function(event) { return $(' '); } }); $(".droppable").droppable({ accept: '.product_helper', drop: function(event, ui) { $(this).append( ui.helper ); } }); Is it even possible to drop a helper onto a droppable?
