[site]: datascience
[post_id]: 114686
[parent_id]: 
[tags]: 
What is the Bayesian distribution of two p.d.f's?

I whant to make this: Theese are the inputs I'm traying to replicate this for my app, but don't know how its done. As you can see, there must be a preset value for the sigma with the inputs of type of game, format of game and stake, that with the mean value of bb/!00 can give the blue p.d.f. The red p.d.f. one can be a preset mean malue for all the players with those inputs, but don't know how to do the green one (bayessian). How can I paint the green bayessian with those inputs? How do I get the hight of the bayessian and the p.d.f's? This is the code I think I'm going to use for the p.d.f's (or similar) import math import numpy import pandas as pd import plotly.express as px import plotly.graph_objs as go import numpy as np ############################################################### mu es el valor medio y sigma standard desv ################################################################# def normal_pdf(x, mu=7, sigma=152): sqrt_two_pi = math.sqrt(math.pi * 2) return math.exp(-(x - mu) ** 2 / 2 / sigma ** 2) / (sqrt_two_pi * sigma) x1= pd.DataFrame(numpy.arange(-10, 10, 0.1)) y1= pd.DataFrame(map(normal_pdf, numpy.arange(-10, 10, 0.1))) #df = pd.DataFrame[x1,y1] #print(df) print(x1) print(y1) x1max=x1.max() y1max=y1.max() print("maxx",x1max) print("maxy",y1max) #Figure=px.scatter(x=x1, y=y1, data_frame=df) x11=np.array(x1).squeeze() y11=np.array(y1).squeeze() print(x11) print(y11) fig=go.Figure() fig.add_trace(go.Scatter(x=x11, y=y11)) but don't know how to do the bayessian. How can I make it?
