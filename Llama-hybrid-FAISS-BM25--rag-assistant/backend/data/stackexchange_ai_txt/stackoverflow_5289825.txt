[site]: stackoverflow
[post_id]: 5289825
[parent_id]: 5289791
[tags]: 
Use .net to call the web services, deal with security etc This forms the input into a ReportViewer control (in local mode) for presentation This avoids dealing with custom SSRS extensions and leverages the SSRS rendering.
