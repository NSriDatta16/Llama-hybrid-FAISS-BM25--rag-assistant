[site]: stackoverflow
[post_id]: 4791324
[parent_id]: 
[tags]: 
Selecting Text through JavaScript

I want to select text thats is on a Html page and make it BOLD, I am using the following Code function getSelectedText(){ if(window.getSelection){ ; return window.getSelection().toString(); } else if(document.getSelection){; return document.getSelection(); } else if(document.selection){ ; return document.selection.createRange().text; } } $(document).ready(function(){ $("*").live("mouseup", function() { selection = getSelectedText(); alert(selection); if(selection.length >= 3) { $(this).html($(this).html().replace(selection, " " + selection + " ") ); } } ); }); This Code works Fine But when the text is in two different paragraphs/ Div or if there is a link between the text then it doesnt seem to work. How Could i Make it Work ?
