[site]: crossvalidated
[post_id]: 46887
[parent_id]: 
[tags]: 
Example of time series prediction using neural networks in R

Anyone's got a quick short educational example how to use neural networks ( nnet in R for example) for the purpose of prediction? Here is an example, in R, of a time series T This just an example but what I have is jumpy-seasonal data.
