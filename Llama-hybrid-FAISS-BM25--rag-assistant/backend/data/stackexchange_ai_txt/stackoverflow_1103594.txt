[site]: stackoverflow
[post_id]: 1103594
[parent_id]: 
[tags]: 
instead of

I've been wondering if I can use &nbsp; (just space in paragraph) instead of Because I love to keep my code semantic and thought if this is right has been bothering me for a while now. I have seen WYSIWSG editors (TinyMCE) use this, but I still rather ask then do it wrong.
