[site]: crossvalidated
[post_id]: 46161
[parent_id]: 
[tags]: 
Is it preferable to normalize the data between [0-1] before running PCA

I'm interested to know if it is recommended to scale a matrix from some integer range (e.g 0-255) to [0.0-1.0] before running a PCA. Does it affect the floating point error?
