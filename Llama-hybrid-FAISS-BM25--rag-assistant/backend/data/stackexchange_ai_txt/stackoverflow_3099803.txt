[site]: stackoverflow
[post_id]: 3099803
[parent_id]: 
[tags]: 
Using a rails partial in a jquery dialog box

I'm trying to insert a partial into a jquery dialog box. The dialog box is working but no partial.. Here is my code : application.js $(document).ready(function(){ $("#add_note").click(function(){ $("#notes").html(" @note)) %>").dialog({title: 'Basic Dialog'}) }) }) my view Historique : Ajouter une note my partial in myview/_note.html.erb Type de note "Type" %> Titre : Description : @project_id %> @project.organization_id %> current_user.id %> current_user.domain_id %> If you have a clue, it would be very helpful! Thanks Fabien
