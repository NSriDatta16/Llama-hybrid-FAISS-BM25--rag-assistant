[site]: stackoverflow
[post_id]: 4307136
[parent_id]: 
[tags]: 
Pulling all the form values from a posted array

I have this form: + Product Code (e.g 66203) mtrs sq Required (e.g 10) Preview Image Submit I am trying to receive the post and print the associate product value with its meterage...as below ( this only seems to return the last result) any ideas? $number_of_products=count($_POST['prodcode']); for ( $i=0; $i "; }
