[site]: stackoverflow
[post_id]: 4976325
[parent_id]: 4976273
[tags]: 
Your last property has two types, I asume it's IEnumerable. I didn't try it, but this should work, although the "efficient" part is questionable. AgentsNotselected = AvaliableAgents.Where(a => !AvailableAgentSelected.Contains(a.Value));
