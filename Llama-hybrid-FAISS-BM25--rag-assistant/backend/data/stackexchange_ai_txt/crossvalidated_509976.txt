[site]: crossvalidated
[post_id]: 509976
[parent_id]: 509970
[tags]: 
That is correct. And that's too short for an answer, so notice in the xgboost paper, section 2.1 they initially refer to $w_i$ as scores , and calculate the final prediction by summing up the score in the corresponding leaves (given by $w$ ). They first refer to them as "leaf weights" in section 2.2.
