[site]: stackoverflow
[post_id]: 2021834
[parent_id]: 2021377
[tags]: 
Instead of using live() you can use event delegation, which is much more elegant: $('table').hover(function(e) { $(e.target).closest('tr').addClass('highlight'); }, function(e) { $(e.target).closest('tr').removeClass('highlight'); })
