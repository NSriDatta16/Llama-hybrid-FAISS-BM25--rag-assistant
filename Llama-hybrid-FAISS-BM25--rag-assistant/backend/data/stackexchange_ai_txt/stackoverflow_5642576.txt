[site]: stackoverflow
[post_id]: 5642576
[parent_id]: 
[tags]: 
Scriptaculous Drag and Drop

Basically i am dragging certain elements to many droppable areas. Once i have dragged all my respective elements i wish to save these drag conditions and update a database using php. But for the purpose of understanding how to do it i have one droparea in my example here. The drag and drop works. I just cant get it to do anything. for example $A($('ee').getElementsByTagName('div')).each(function(item) { new Draggable( item, {revert: true,}); }); Droppables.add( 'droparea', { hoverclass: 'hoverActive', accept:'g', onDrop: moveItem } ); function moveItem( drag,drop){ drag.parentNode.removeChild(drag); drop.appendChild(drag); } I want to once i drop the elements into the droparea i can the press save and "do" something in the php script. $playername){?> " class = 'g'> Take it that droparea is only a normal at the minute. Once i drag and drop $pname i want to send that to a php script but dont know how??? Please help.
