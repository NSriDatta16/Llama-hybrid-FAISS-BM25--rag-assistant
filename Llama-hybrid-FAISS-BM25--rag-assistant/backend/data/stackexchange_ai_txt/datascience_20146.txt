[site]: datascience
[post_id]: 20146
[parent_id]: 20118
[tags]: 
Pandas is an in-memory data storage tool. This allows you to do very rapid calculations over large amounts of data very quickly. SQL (usually) persistently stores data and is a database. It is also possible to run an in-memory SQL db which may be faster than using pandas, such as SQLite.
