[site]: stackoverflow
[post_id]: 4213888
[parent_id]: 
[tags]: 
How to return a DECIMAL(4,0) in TEXT keeping the 0000 format?

I want to return in a Stored Function a value as TEXT but I want to be 0001 , and not 1 I have this code fragments: DECLARE _RESTRICTEDROUTE DECIMAL(4,0); RETURN(_RESTRICTEDROUTE); I tried RETURN(CAST(_RESTRICTEDROUTE AS TEXT)); but failed.
