[site]: stackoverflow
[post_id]: 2360353
[parent_id]: 2360326
[tags]: 
If the first code fragment works, there is no logical reason for the second not to work. They are exactly the same thing from a PHP perspective. You do have to register the session before any calls to the session global, see http://php.net/manual/en/function.session-register.php
