[site]: crossvalidated
[post_id]: 169891
[parent_id]: 
[tags]: 
Same data, different PACF? [R vs Stata]

So I was playing around with R, trying to figure out how to do basic time series stuff. Using the Box-Jenkins Series G ("airline passengers") data, I wanted to plot ACF and PACF. So here's my code: library(foreign) bjg But then compare the same graphs from Stata, using the same data set. While the ACFs look alike, the PACFs are clearly different between R and Stata. How can that be? Where's my mistake? P.S.: Here's the Stata code: webuse air2 tsset t ac air pac air
