[site]: stackoverflow
[post_id]: 5665842
[parent_id]: 
[tags]: 
What is the most efficient way to search a blob of text for an array of regular expressions?

I'm looking for the most efficient way to search a blob of text (± 1/2KB) for many regular expressions stored in an array. Example code: patterns = [/patternA/i,/patternB/i,/patternC/m,...,/patternN/i] content = "Lorem ipsum dolor sit amet, consectetur... officiam id est laborum." r = patterns.collect{ |pattern| pattern unless ( content =~ pattern ).blank? }.compact Where r now contains patterns that matched the content string.
