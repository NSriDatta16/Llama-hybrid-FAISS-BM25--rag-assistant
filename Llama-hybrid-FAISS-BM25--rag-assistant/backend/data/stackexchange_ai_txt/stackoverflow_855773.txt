[site]: stackoverflow
[post_id]: 855773
[parent_id]: 
[tags]: 
Which algorithm does Ruby's sort method use?

When I sort an Array using the native sort method, which algorithm does Ruby use? Is it data-dependant, i.e., if the data is small it uses X algorithm else it uses Y algorithm? Is it a stable sort? What is the average time complexity?
