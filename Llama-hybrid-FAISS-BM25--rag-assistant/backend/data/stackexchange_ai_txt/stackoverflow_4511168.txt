[site]: stackoverflow
[post_id]: 4511168
[parent_id]: 4511128
[tags]: 
It would be nice, but that's the way CSS is designed and organized doesn't support that. However, there are CSS preprocessors which allow you to code this way, and then process it into real, valid CSS - such as LessCSS .
