[site]: stackoverflow
[post_id]: 879216
[parent_id]: 878869
[tags]: 
Routing constraints (global.asax) Action filters (via action attributes) Linq to SQL C# 3.0 features like lambda expressions, anonymous initializers, and implicit typing (var) DataContractSerializer or JavascriptSerializer for serialization stuff Donut caching, authentication, CSRF and a few other concepts that will need your attention when building the app
