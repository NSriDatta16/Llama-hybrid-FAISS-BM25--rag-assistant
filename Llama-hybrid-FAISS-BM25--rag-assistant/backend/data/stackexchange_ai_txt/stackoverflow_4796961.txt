[site]: stackoverflow
[post_id]: 4796961
[parent_id]: 4787893
[tags]: 
There is a bug in PrimeFaces, when is nested in any data repeating element the dropListener does not get called. I also tried using the Facelets repeat tag but the dropListener was not called just like dragging to the . In the PrimeFaces showcase all of the drag an drop examples have static droppable areas. PrimeFaces ShowCase
