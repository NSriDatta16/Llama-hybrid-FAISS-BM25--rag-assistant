[site]: stackoverflow
[post_id]: 4945874
[parent_id]: 
[tags]: 
Change cursor over HTML5 Canvas when dragging the mouse

I have made a paint brush type of application using the Canvas Tag . I wanted that when the mouse is on the canvas the Cursor Changes , I have accomplished this but i cant figure out how shall i change the cursor while I Drag the mouse for drawing the image
