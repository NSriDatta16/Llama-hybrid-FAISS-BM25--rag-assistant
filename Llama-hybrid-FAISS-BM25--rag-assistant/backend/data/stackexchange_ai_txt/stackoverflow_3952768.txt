[site]: stackoverflow
[post_id]: 3952768
[parent_id]: 3952750
[tags]: 
I would do something like this: var files = []; $('a.load-fragment').each(function(){ var loc = $(this).attr('href'); loc = loc.substring(loc.lastIndexOf('/')+1); files.push(loc); }); alert(files); You can then access each of them from the files array, files[0], files[1] , etc.
