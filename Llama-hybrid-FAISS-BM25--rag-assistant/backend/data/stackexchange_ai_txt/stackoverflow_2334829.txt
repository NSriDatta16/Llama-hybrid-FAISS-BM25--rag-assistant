[site]: stackoverflow
[post_id]: 2334829
[parent_id]: 2334535
[tags]: 
it's better to represent Booked/Available as binary numbers (e.g. 1-free, 0-booked). If you do so, you can elegantly make use of aggregate functions: select seat as n from seats where $num_seats = (select sum(status) from seats where seat between n and n + $num_seats - 1)
