[site]: stackoverflow
[post_id]: 1602679
[parent_id]: 1601933
[tags]: 
An easy approach is to leverage this code: Link Title This approach doesn't force a page refresh, so the scrollbar stays in place. Also, it allows you to programmatically change the onclick event and handle client side event binding using jQuery. For these reasons, the above solution is better than: Link Title Link Title where the last solution will avoid the scroll-jump issue if and only if the myClickHandler method doesn't fail.
