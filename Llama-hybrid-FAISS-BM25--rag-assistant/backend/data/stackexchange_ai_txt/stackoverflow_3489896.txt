[site]: stackoverflow
[post_id]: 3489896
[parent_id]: 3233428
[tags]: 
Add in an instruction so that the user agent knows the location of your proxy: $ua->proxy("http", "http://localhost:8888"); As to the comment about POST being a valid bareword (which I probably would not have noticed), my module has something like this in it: use HTTP::Request::Common qw( POST GET );
