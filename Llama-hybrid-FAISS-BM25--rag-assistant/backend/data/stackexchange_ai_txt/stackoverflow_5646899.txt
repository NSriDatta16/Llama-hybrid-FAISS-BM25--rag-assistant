[site]: stackoverflow
[post_id]: 5646899
[parent_id]: 5607594
[tags]: 
It is actually quite a simple answer. All you need to do is add this line in readZippedXML() : $xmldata = str_replace(" ", "\r\n", $xmldata); This is because is what word uses to mark the end of a paragraph. E.g. This is a paragraph. And a second one.
