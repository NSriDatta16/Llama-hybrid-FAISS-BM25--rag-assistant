[site]: stackoverflow
[post_id]: 296905
[parent_id]: 296797
[tags]: 
Anything between "sqlplus -s $1 You should make all "ifs" outside of the here-doc and use results in it. Like this: if [ "$7" -eq "AAA" ] ; then attr="attr2" elsif [ "$7" -eq "BBB" ] ; then attr="attr1" else echo "Bogus $7 - need AAA or BBB" fi .... sqlplus -s $1 Pay attention to "${attr}" and read a decent tutorial on shell scripting.
