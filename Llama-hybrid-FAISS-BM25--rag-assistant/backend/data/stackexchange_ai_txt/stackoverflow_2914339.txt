[site]: stackoverflow
[post_id]: 2914339
[parent_id]: 
[tags]: 
RegEx: h1 followed by h2 without p in between

I need a regular expression to find out whether or not a h1 tag is followed by a h2 tag, without any paragraph elements in between. I tried to use a negative lookahead but it doesn't work: (\s|(?!
