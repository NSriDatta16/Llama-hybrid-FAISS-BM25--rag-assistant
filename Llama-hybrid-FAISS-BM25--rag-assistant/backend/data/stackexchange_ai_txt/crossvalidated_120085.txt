[site]: crossvalidated
[post_id]: 120085
[parent_id]: 
[tags]: 
k-means cluster, How to re-calculate centroid when using cosine similarity?

I have a requirement using k-means cluster method with cosine similarity instead of Euclidean distance. for example: data a: a1 a2 a3 a4 ... data b: b1 b2 b3 b4 ... cosine similarity: $\displaystyle \frac{\mathbf{a}\cdot\mathbf{b}}{ |\mathbf{a}|\cdot|\mathbf{b}|}$ My problem is how I can re-calculate the centroid vector for each iteration base on cosine similarity? Can I still use average e.g.: $\displaystyle \frac{(a_1 + b_1 + c_1)}{3}$ ?
