[site]: stackoverflow
[post_id]: 1593823
[parent_id]: 1129343
[tags]: 
Assuming that Organization has a IList the HasMany (Reveal.Property ("_trips")); code should work. Check that it's a property and that you have protected getters and setters (privates will not work, since NHibernate will want to proxy the collection for lazyloading).
