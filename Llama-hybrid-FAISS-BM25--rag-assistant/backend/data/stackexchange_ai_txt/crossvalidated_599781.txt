[site]: crossvalidated
[post_id]: 599781
[parent_id]: 
[tags]: 
Facing problem getting output for sentiment analysis the code always labels all articles as 0.0 irrespective of there type

from textblob import TextBlob from newspaper import Article url = 'https://en.wikipedia.org/wiki/Mathematics' article = Article(url) article.download() article.parse() article.nlp text =article.summary print(text) blob = TextBlob(text) sentiment = blob.sentiment.polarity # -1 to 1 print(sentiment)
