[site]: crossvalidated
[post_id]: 603726
[parent_id]: 
[tags]: 
Specifying a terminal node in a Bayesian Network

I am writing R script using bnlearn package for learning a DAG from a dataset. Is it possible to define a terminal node beforehand? ie I want a node Y to be the final one in the graph, which will not affect any other node.
