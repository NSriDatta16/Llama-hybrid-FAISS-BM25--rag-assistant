[site]: datascience
[post_id]: 107706
[parent_id]: 107703
[tags]: 
Word2vec in most cases performs better than one hot encoding with lesser dimension. You can try using word2vec Embeddings only problem i see is that word2vec are generic bedding and drugs name can be very specific to medical field. Due to which you can face two problems A lot of words in your vocabulary may not be present is word2vec Embedding as they are generic may not do very good in medical context.
