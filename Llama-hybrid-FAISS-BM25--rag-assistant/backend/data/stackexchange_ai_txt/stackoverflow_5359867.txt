[site]: stackoverflow
[post_id]: 5359867
[parent_id]: 5350921
[tags]: 
Just use File -> New -> Library to create the library. Then any subsystem you put in the library can be used as a link in your model. The subsystem does not have to be masked. To use one, just drag the subsystem from the library into your model, and it will automatically create a link.
