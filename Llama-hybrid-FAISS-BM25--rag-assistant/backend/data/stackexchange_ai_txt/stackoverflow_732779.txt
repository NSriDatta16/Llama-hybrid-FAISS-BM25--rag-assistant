[site]: stackoverflow
[post_id]: 732779
[parent_id]: 732513
[tags]: 
If you are looking for data, I would just remove all html and handle it as line-by-line raw input. You can use the strip_tags function. $clean = strip_tags($input); // example: Test paragraph. Other text // returns: Test paragraph. Other text
