[site]: stackoverflow
[post_id]: 4630606
[parent_id]: 4630437
[tags]: 
As far as I'm aware, you can't - /tmp isn't configured as something that you can serve items out of (it's intended for temporary storage). Can you access images at the /tmp URLs in your browser? Do they work?
