[site]: datascience
[post_id]: 28316
[parent_id]: 27664
[tags]: 
@DPascal Here is something you could definitely try doing: Using the features you generated, you could add a label to each of the column for these features. You could generate these feature value for different time slices. Once that is done, you can then run something as simple as Random Forest Classifier on this data.
