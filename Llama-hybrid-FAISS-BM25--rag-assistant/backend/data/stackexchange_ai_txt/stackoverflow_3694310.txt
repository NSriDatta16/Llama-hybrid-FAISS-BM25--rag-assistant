[site]: stackoverflow
[post_id]: 3694310
[parent_id]: 3694263
[tags]: 
That sounds like maybe it is reacting to a different cookie value or similar. WebBrowser is IE (to most purposes). A 500 is at the server, but WebBrowser is the client. You might be able to hook WebBrowser to supply a different user-agent, but by default it should appear the same.
