[site]: stackoverflow
[post_id]: 2958462
[parent_id]: 
[tags]: 
JQuery date picker does not firing in ajax page using Rails

Here I have using datepicker from JQueryUI in my public/javascript folder as effects,prototype,control,dragdrop js files. in my public folder contains jqueryui development buddle. (css,js,development-bundle) in layout/application.rhtml $(document).ready(function(){ var $j=jQuery.noConflict(); $j( '#date' ).datepicker({ dateFormat: 'dd-mm-yy' }); }); in home/index.rhtml "add"%> "add" , :url=>{ :action=>"add" }%> in home/add.rhtml "create" do%> Name : 15%> DOB : "date"%> the datepicker works when I run home/add.rhtml directly but the datepicker not work when i run ajax page home/index.rhtml Any solutions for that,????
