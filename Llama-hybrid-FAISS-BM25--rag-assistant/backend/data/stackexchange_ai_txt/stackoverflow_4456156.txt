[site]: stackoverflow
[post_id]: 4456156
[parent_id]: 
[tags]: 
How do I use union within while?(SQL)

I have the following code and I want to use the union command to combine them.I tried using the union command with and without the last table(12).Thank you in advance. DECLARE @COUNTER as int SET @COUNTER = 0 WHILE @COUNTER = @COUNTER) GROUP BY Clients.Clients_Code)f WHERE f.Code=e.Code AND e.Month=f.Month AND f.AVERAGE>e.AVERAGE END
