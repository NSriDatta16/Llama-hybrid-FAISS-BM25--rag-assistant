[site]: stackoverflow
[post_id]: 4835237
[parent_id]: 4835192
[tags]: 
By far the most efficient way to solve this problem, assuming doSomethingThreadSafe(i) is basically a repeated instruction (single instruction on multiple data) is to use OpenMP as chrisaycock says. #pragma omp parallel for for ( i = 0; i It can't get much more simple than that, really.
