[site]: stackoverflow
[post_id]: 2141022
[parent_id]: 
[tags]: 
Passing parameters in jQuery

What i have is working, so I'm just asking if there's a more elegant way to do this. I have: If the changes either A or B, then concatenate the two fields together and place them into C. What I have is: function doit(A,B) { return A+B; }; $(function() { var A = $('#A'); var B = $('#B'); $('#A, #B').change(function() { $('#C').val(doit(A.val(),B.val())); }); }); I wonder if I should write $('#C').val(doit(A,B)) instead, or is it acceptable to pass A.val(), B.val()?
