[site]: stackoverflow
[post_id]: 2699853
[parent_id]: 2697336
[tags]: 
An HTML frameset reference other documents. You don't create them all in one go and send them all to the user-agent in a single response. Print the frameset and frame references only, and the browser will do the extra work to fetch each frame individually. Read up on how framesets work: Frames in HTML documents Frames tutorial
