[site]: stackoverflow
[post_id]: 3846738
[parent_id]: 3846722
[tags]: 
int aveScore = (int)((float)startScore / (([p1.arrayThrow count] - 1) + winPane.throws)); EDIT: If you want a floating point average score then it would just be: float aveScore = (float)startScore / (([p1.arrayThrow count] - 1) + winPane.throws);
