[site]: stackoverflow
[post_id]: 2032766
[parent_id]: 2032518
[tags]: 
Or you can use the following (this is probably faster and more elegant): But that sends the as-typed value back in the form data, so use either of the following to store it as all-caps in the database: MySQL: UPPER(str) PHP: strtoupper()
