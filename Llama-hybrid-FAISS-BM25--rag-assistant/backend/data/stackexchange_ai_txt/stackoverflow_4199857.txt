[site]: stackoverflow
[post_id]: 4199857
[parent_id]: 4199826
[tags]: 
This is the jQuery equivalent to your code. There are more elegant solutions, though. var values = []; $(lb).children('option').each(function () { values.push($(this).text()); }); $(txtfield).val(values.join(','));
