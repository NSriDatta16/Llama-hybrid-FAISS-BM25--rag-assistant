[site]: stackoverflow
[post_id]: 634592
[parent_id]: 
[tags]: 
How to Upload file using WPF only

Greetings to all, I wonder if anyone knows or has the sample code to upload a file to a server (web server using ISS) without using ftp and using WPF. Besides I wanted to ask if the file you wish to upload multimedia is possible that before being uploaded to the server you can do a preview before you upload it using the player archivosde average WPF Thank you very much.
