[site]: stackoverflow
[post_id]: 4926201
[parent_id]: 
[tags]: 
Make a window draggable within a certain boundary WPF

I have a wpf child window that I allow dragging by using the DragMove() method. However, I need to allow the window to be dragged only within the bounds of its Parent window control. Can anyone suggest a way to achieve this? Thanks!
