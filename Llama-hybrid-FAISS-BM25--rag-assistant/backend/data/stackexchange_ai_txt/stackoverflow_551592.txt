[site]: stackoverflow
[post_id]: 551592
[parent_id]: 551579
[tags]: 
std::vector does not actually contain bools (i.e.bytes) , it contains bits! This is mostly a missfeature and you are advised to use std::deque , which doesn't have this "feature" instead. And if you want the storage to be contiguous, use std::vector .
