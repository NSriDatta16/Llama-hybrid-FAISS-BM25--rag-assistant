[site]: stackoverflow
[post_id]: 813649
[parent_id]: 813625
[tags]: 
You can drag any file to the right bar of the Dock (where the Trash and folders are), and execute it by clicking it. To execute a shell script, you can save it as a .command file and the OS will pick it up. Another alternative would be to create an Applescript application for your script, and use it like any application.
