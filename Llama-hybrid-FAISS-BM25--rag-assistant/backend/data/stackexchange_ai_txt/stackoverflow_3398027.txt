[site]: stackoverflow
[post_id]: 3398027
[parent_id]: 3397994
[tags]: 
Storing the token in the Users table rather than a seperate table will mean that you don't have to join them during each query, which will be slightly quicker. Also, you're not storing the userIds and creating a new Id for that tokens table, which will save on the data storage.
