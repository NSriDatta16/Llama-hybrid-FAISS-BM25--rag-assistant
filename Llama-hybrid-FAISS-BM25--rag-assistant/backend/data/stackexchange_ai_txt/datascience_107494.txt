[site]: datascience
[post_id]: 107494
[parent_id]: 
[tags]: 
zero padding problem

i need to implement this code by using padding PAD=0 embeddings = [] for sent_idx in sentences: # pad sentence to correct size while len(sent_idx) but got AttributeError: 'str' object has no attribute 'append' sent_idx returns restaurant has modern wooden tables and chairs len(sent_idx) returns 46
