[site]: stackoverflow
[post_id]: 5257636
[parent_id]: 
[tags]: 
Rails 3 urls with subdomains

I have a rails app that has members that belongs to an organisation. When members login they now get a url like this www.DOMAIN/organisations/ORGANISATION_ID I want to change this to www.organisation_name.DOMAIN (What do we call a url like this?) How can I do this? A small add
