[site]: stackoverflow
[post_id]: 3295384
[parent_id]: 3295223
[tags]: 
Damn, those people are nasty browser-sniffers. I didn't do it all in vain though, this works: array( 'header' => 'User-Agent: Firefox/3.5.9\r\n' ) ); $context = stream_context_create($opts); libxml_set_streams_context($context); $d = simplexml_load_file('http://eu.wowarmory.com/character-achievements.xml?r=Sunstrider&cn=Secrezy&c=168'); var_dump($d->xpath("//achievement[@id='4602']")); The __extremely_ sad thing is, at first I just hoped it would be at least Accept or at the most Accept-Encoding - headers. But no, the "shouldn't mean anything significant" - User-Agent -header. A fine example why browser sniffing is bad if I ever saw one.
