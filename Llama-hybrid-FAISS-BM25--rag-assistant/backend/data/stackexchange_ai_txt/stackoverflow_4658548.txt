[site]: stackoverflow
[post_id]: 4658548
[parent_id]: 4656169
[tags]: 
I know I could find the value by testing for every combination using xsl:if or xsl:choose but is there a more elegant way that would work similar to the following SQL and also wouldn't need changes to code being made if other prefixes were added. WHERE label LIKE '%Mileage' In XPath 2.0 there is a standard function ends-with() In XPath 1.0 use : /*[Label[substring(., string-length(.) - 6) = 'Mileage']]/value Note : contains(Label, 'Mileage') is not equivalent to the SQL clause above . For example, it will select this node: xxx Mileage yyy
