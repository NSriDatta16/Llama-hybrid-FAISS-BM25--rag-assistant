[site]: stackoverflow
[post_id]: 5629633
[parent_id]: 
[tags]: 
SilverStripe CMS Unity3D Web Player Embedding Issue

I am trying to add the Unity3D (www.unity3d.com) web player into my site with SilverStripe CMS version 2.4.5. What seems to happen is that SS is dynamically removing my code that contains the web player. It leaves a closing div tag. it is really simple for a simple static page to embed the web player. You need the following code and it works perfectly. HTML:
