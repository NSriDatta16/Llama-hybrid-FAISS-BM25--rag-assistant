[site]: stackoverflow
[post_id]: 2527777
[parent_id]: 2527765
[tags]: 
Because a paragraph tag is a block element, the background will be painted between lines. If you change it to an inline element, or wrap the text in an inline element, you should be able to get the effect you want. This is an example of what I have span { background: #fff; }
