[site]: stackoverflow
[post_id]: 635418
[parent_id]: 635022
[tags]: 
Try this example , which just sets it to black or white depending on the average of the r,g and b values. Also good for testing! Substitute a better algorithm in there if you find cases that don't work very well. function rgbstringToTriplet(rgbstring) { var commadelim = rgbstring.substring(4,rgbstring.length-1); var strings = commadelim.split(","); var numeric = []; for(var i=0; i (3*256/2)) { newtriplet = [0,0,0]; } else { newtriplet = [255,255,255]; } var newstring = "rgb("+newtriplet.join(",")+")"; someelement.style.color = newstring; return true; } function randomiseBackground(someelement) { var vals = []; for(var i=0; i Some text
