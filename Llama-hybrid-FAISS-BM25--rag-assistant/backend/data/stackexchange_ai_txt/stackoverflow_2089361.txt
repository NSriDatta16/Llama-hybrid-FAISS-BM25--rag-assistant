[site]: stackoverflow
[post_id]: 2089361
[parent_id]: 2046122
[tags]: 
If you really want to convert an index path to an absolute row number, you could do this: // Start with row offset int absoluteRow = indexPath.row; // Add count of rows from all earlier sections int s; for (s = 0; s But it's probably better to organize your data to match your table structure.
