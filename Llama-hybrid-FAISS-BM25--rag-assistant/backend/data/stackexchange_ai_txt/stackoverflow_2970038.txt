[site]: stackoverflow
[post_id]: 2970038
[parent_id]: 2969403
[tags]: 
There are two aspects: If you want to implement it quickly with an RDBMS, use an embedded DB like SQLite. This is used by many desktop applications like Firefox . If performance is a concern, then use go for embedded key-value pairs kind of storage solution.
