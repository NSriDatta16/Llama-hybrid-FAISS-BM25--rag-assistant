[site]: stackoverflow
[post_id]: 109446
[parent_id]: 109432
[tags]: 
Data access is possible because you can set up a test database. Generally the 'untestable' stuff is FTP, email and so forth. However, they are generally framework classes which you can rely on and therefore do not need to test if you hide them behind an abstraction. Also, 100% code coverage is not enough on its own.
