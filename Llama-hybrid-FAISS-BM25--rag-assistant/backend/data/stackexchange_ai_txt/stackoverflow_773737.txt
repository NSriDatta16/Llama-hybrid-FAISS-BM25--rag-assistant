[site]: stackoverflow
[post_id]: 773737
[parent_id]: 773253
[tags]: 
If you have a gigantic data set (ie much larger than available memory) you likely have your data on disk/tape/something-with-expensive-random-access, so you need an external sort. Merge sort works well in that case; unlike most other sorts it doesn't involve random reads/writes.
