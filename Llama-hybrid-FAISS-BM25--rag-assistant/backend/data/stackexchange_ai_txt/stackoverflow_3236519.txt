[site]: stackoverflow
[post_id]: 3236519
[parent_id]: 3235265
[tags]: 
Your example cannot work, see other's answer which explain why. Furthermore using $_POST superglobal as a data storage is a pretty bad idea imho. Use specific variable sharing solution if you need (like database, im memory registry, session, cookie, etc)
