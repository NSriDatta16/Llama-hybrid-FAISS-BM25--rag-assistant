[site]: stackoverflow
[post_id]: 299945
[parent_id]: 299831
[tags]: 
Drag and drop is more suited to standard ASP.NET webforms, not MVC. I think you can still do it in MVC, but only if the controls do not rely on viewstate (which MVC does not support). try Googling on "asp.net mvc user controls"
