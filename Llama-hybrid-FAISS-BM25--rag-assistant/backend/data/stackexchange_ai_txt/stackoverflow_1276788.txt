[site]: stackoverflow
[post_id]: 1276788
[parent_id]: 1276752
[tags]: 
The second paragraph of your question would be a good introduction to your interface document. Then I would add two sections, one for the header, the other for the footer. The header section contains a description of what is expected on each line. The foorter section is a table where each line describes one field and the columns are: field name, offset, length, purpose
