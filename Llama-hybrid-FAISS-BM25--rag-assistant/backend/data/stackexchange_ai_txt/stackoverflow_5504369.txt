[site]: stackoverflow
[post_id]: 5504369
[parent_id]: 5503284
[tags]: 
BlockingCollection uses ConcurrentQueue by default. Should be a good fit for your application. It might be easier if you used F# with a Mailbox and async blocks. I made a sample post of a common implementation earlier. Map/reduce with F# Agent or MailboxProcessor
