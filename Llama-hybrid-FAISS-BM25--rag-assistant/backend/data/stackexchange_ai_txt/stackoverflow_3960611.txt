[site]: stackoverflow
[post_id]: 3960611
[parent_id]: 3960508
[tags]: 
Use smart class names to greater effect. The following example has a set of links which display a corresponding paragraph when clicked. $('.togglers a').live('click', function(e) { e.preventDefault(); // which number list item are we? var block = $(this).closest('ul').find('li').index( $(this).parent() ); console.log(block); $('.blocks p').removeClass('visible').eq(block).addClass('visible'); }); .blocks p { display: none; } .visible { display: block !important; } Block 1 Block 2 Block 3 Block 4 This is block 1. This is block 2. This is block 3. This is block 4. See the edit history for a version that is slightly less abstracted.
