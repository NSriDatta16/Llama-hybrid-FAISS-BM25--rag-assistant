[site]: stackoverflow
[post_id]: 4590287
[parent_id]: 4590264
[tags]: 
Here is one approach. Have both Teacher and Student inherit from a base class, let's call it Person . This Person class will have two properties, Name and Salt , and it can also have a public method called GetEncryptedName() , which will keep your implementation organized in a single place.
