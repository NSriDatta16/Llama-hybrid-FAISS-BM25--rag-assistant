[site]: crossvalidated
[post_id]: 371229
[parent_id]: 
[tags]: 
Conjugate priors for dynamic model $x_{t+1}=Ax_{t}+\eta_t$

What conjugate priors do we have for the model(multivariate) $x_{t+1}=Ax_{t}+\eta_t$ , where $\eta_t\overset{iid}\sim N(0,\Sigma)$ ? I was thinking of using $\tilde{x}=Diag[x_1,...,x_{n-1}]$ , $\tilde{y}=Diag[x_2,...,x_{n}]$ and simply use the usual conjugate priors for the Bayesian multivariate linear regression. Will there be any problem, if I do this?
