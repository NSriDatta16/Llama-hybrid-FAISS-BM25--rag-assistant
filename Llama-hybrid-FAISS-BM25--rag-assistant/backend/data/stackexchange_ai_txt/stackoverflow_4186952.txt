[site]: stackoverflow
[post_id]: 4186952
[parent_id]: 
[tags]: 
Socket Programing By C# in ASP.net

Is this Possible that write a Socket by C# in ASP.net ? for Example Can i write a Code Like this Perl Code in C# and asp.net ? : > use HTTP::Request::Common qw(POST); > use LWP::UserAgent; $ua = new > LWP::UserAgent(agent => 'Mozilla/5.0(Windows; U; Windows NT 5.1; en-US; rv:1.8.0.5)Gecko/20060719 Firefox/1.5.0.5'); > $ua -> timeout(20); > my $req = POST 'http://Example.com/', > [ login_username => 'mehdi' , login22 => '654321' , go => 'submit']; > my $content = $ua->request($req); please Give me an example or Convert the above code to C# and asp.net . Thanks in Advance .
