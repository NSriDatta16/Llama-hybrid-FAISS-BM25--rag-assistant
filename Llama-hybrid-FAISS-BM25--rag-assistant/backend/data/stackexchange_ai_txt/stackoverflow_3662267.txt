[site]: stackoverflow
[post_id]: 3662267
[parent_id]: 3661302
[tags]: 
I don't know if it is more elegant or not, but the poly function can take multiple vectors: z ~ poly(x, y, degree=2) This will create all the combinations that you asked for, without additional ones. Do note that you need to specify degree=2, not just 2 when doing it this way.
