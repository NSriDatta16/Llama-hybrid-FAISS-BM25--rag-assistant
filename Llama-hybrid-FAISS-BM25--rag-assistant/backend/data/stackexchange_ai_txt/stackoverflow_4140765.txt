[site]: stackoverflow
[post_id]: 4140765
[parent_id]: 3672273
[tags]: 
The problem is in event, that you are observing. Because Magento session wasn't initialized at that moment, so cookie has different name, than core one. Use controller_action_predispatch for setting up some session data from request.
