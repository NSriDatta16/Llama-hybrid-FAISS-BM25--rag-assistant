[site]: datascience
[post_id]: 122843
[parent_id]: 
[tags]: 
Error in SVM classifier RBF kernel in MATLAB?

I had written MATLAB function for RBF kernel in SVM classification but I am getting the following error: Kernel function returns kernel product of incorrect size. This is what I had tried: Mdl_SVM_Rbf = fitcsvm(feat_training,label_train,'KernelFunction','myrbf','Standardize',true); function G = myrbf(U,V) [n,d] = size(U); nms = sum(U'.^2); gamma = 0.016; sig = 1/sqrt(gamma); c = 0.18; G = exp(-((nms'*ones(1,n) -ones(n,1)*nms + 2*U*U')/(2*sig^2))); end Also I am not getting where should we include kernel parameter C.
