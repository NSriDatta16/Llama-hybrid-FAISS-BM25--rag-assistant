[site]: stackoverflow
[post_id]: 1253819
[parent_id]: 1253801
[tags]: 
a) find median or average values of all distances. = avgAll For each p, find average distance to other machines. = avgP(i) Pick the closer one as center. avgAll ~= avgP(i) b) no idea for now.. maybe for each p, find the closer machine. by this logic make a graph. than somehow (i dont know yet) divide the graph
