[site]: stackoverflow
[post_id]: 3505036
[parent_id]: 3504999
[tags]: 
Because one of your users does not have any organizations so organizations.first is nil You can prevent this by doing User.find(:all).select {|u| u.organizations.first.name unless u.organizations.size == 0}
