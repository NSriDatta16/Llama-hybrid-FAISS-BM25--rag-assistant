[site]: stackoverflow
[post_id]: 4271229
[parent_id]: 4269141
[tags]: 
In HTML 2.0, 3.2, 4.x and XHTML 1.x: In theory: It allows the user agent to decode entities (e.g. &hellip; to …) It allows a validator to determine which tags, attributes and attribute values are allowed where In practice: It allows a validator to determine which tags, attributes and attribute values are allowed where It allows browsers to make a guess as to if the author has a clue about what they are doing and switch between standards mode and quirks mode In HTML 5: It is a mystic string used to start a document so that standards mode is triggered
