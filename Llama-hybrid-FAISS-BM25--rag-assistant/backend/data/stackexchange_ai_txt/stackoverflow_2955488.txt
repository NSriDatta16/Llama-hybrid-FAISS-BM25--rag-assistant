[site]: stackoverflow
[post_id]: 2955488
[parent_id]: 1930454
[tags]: 
Why so many complicated long answers. Here is the simplest way to find the running average till now without any need to know how many elements or size etc.. long int i = 0; double average = 0; while(there are still elements) { average = average * (i / i+1) + X[i] / (i+1); i++; } return average;
