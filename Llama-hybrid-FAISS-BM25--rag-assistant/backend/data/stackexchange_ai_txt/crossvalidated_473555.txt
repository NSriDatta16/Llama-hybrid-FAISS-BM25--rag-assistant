[site]: crossvalidated
[post_id]: 473555
[parent_id]: 
[tags]: 
Accuracy in DNN

How does number of batch size and steps per epoch affects the accuracy of the model? Edit: Training and validation accuracy both. I trained a model using CNN where the accuracy is changing relatively to the step per epochs.
