[site]: stackoverflow
[post_id]: 2301797
[parent_id]: 2301453
[tags]: 
If you don't want to see the parameters in the query string, use the POST request instead GET request. Also to ensure greater security, you can use a HTTPS connection instead HTTP connection. Also, it may helps: Secure HTTP Post in Android Https Connection Android Other way, is using http request headers for transfering data (but is not an elegant, not good solution and unsuitable in most cases).
