[site]: stackoverflow
[post_id]: 2507141
[parent_id]: 2507114
[tags]: 
Seeing as it's routing rules, and you may need all the pieces at some point, you could also split the string the classical way: $path_exploded = explode("/", $path); foreach ($path_exploded as $fragment) if ($fragment[0] == "%") echo "Found $fragment";
