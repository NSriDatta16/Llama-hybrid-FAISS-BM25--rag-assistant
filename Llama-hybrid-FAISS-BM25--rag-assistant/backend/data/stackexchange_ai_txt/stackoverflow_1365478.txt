[site]: stackoverflow
[post_id]: 1365478
[parent_id]: 1365309
[tags]: 
i just test the public methods (and no i don't care about coverage metrics, i care about features that work). note that if the public methods don't use the internal methods then the internal methods do not need to exist!
