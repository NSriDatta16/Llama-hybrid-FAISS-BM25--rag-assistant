[site]: crossvalidated
[post_id]: 448757
[parent_id]: 
[tags]: 
Difference between Feature engineering and hyperparameter optimizations?

Hyperparameter optimizations and feature engineering can(in my understanding) both be used to create a machine learning model. But what is the difference? And what is done to the y = wx + b formula in both cases? In my understanding, the w is a hyperparameter, that is "tuned"...
