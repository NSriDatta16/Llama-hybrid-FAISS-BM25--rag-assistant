[site]: stackoverflow
[post_id]: 3135954
[parent_id]: 3135946
[tags]: 
You need to pass the address of these variables. Make sure to pay special attention to function signatures because you can end up with very strange results. Also, turn all warnings on for your compiler. scanf("%d %d", &hour, &min);
