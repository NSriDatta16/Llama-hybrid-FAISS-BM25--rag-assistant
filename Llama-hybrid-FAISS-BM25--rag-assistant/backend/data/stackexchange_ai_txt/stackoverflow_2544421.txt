[site]: stackoverflow
[post_id]: 2544421
[parent_id]: 2544381
[tags]: 
From what I can see here: http://opencv.willowgarage.com/documentation/dynamic_structures.html CvSeq seems to be some form of data container and can not be directly cast to a CvPoint . This might be what you want: char* cvGetSeqElem(const CvSeq* seq, int index) I guess it's used like this: CvPoint* point = reinterpret_cast (cvGetSeqElem(sequence, some_index));
