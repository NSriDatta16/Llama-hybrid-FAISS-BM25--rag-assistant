[site]: stackoverflow
[post_id]: 3063756
[parent_id]: 3054716
[tags]: 
Maybe you could check that the page in which the script is injected is not inside a frame: if (window == window.parent) { /* you're not inside a frame! */ } I'm not sure if it works from inside tags embedding HTML though. Chances are that yes.
