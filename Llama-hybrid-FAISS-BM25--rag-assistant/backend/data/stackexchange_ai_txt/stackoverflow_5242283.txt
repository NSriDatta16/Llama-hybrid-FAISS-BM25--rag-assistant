[site]: stackoverflow
[post_id]: 5242283
[parent_id]: 
[tags]: 
Remove Isolated files for an application

We have a windows application, where we make use IsolatedStorage to save some application settings. And recently user asked us to provide option to reset the application means, it should as newly installed application. So, we have built a small utility app to delete all application data, as Isolated storage created by framework, is there any way we can delete on our application Isolated storage.
