[site]: stackoverflow
[post_id]: 1435451
[parent_id]: 1435442
[tags]: 
If you can't use STL then your best bet is to use an array of News*. News*[] GetAllNews( int& count ) { News*[] arr = new News*[3]; count = 3; arr[0] = new Cnn(); arr[1] = new Msnbc(); arr[2] = new Bbc(); return arr; } int count; News*[] arr = GetAllNews(count); for ( int i = 0; i get(); } // Now for the awkward cleanup for ( int i = 0; i
