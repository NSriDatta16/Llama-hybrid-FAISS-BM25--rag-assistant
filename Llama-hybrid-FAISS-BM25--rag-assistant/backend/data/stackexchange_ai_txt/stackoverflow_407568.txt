[site]: stackoverflow
[post_id]: 407568
[parent_id]: 407550
[tags]: 
How about feeding the javascript from php? So instead of heaving: do And then in the php file replace all outputted text with their associated constants. Be sure to output the correct caching headers from within PHP code. EDIT These are the headers that I use: header('Content-type: text/javascript'); header('Cache-Control: public'); header('expires: '. date("r", time() + ( 7 * 24 * 60 * 60 ) ) ); // 1 week header("Pragma: public");
