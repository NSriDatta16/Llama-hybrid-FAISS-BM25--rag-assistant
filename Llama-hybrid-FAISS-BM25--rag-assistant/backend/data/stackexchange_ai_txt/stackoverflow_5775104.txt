[site]: stackoverflow
[post_id]: 5775104
[parent_id]: 5774918
[tags]: 
You should debug as Larry says, but I bet this would help adverage = total / lstBox.Items.Count; One of the the most important things in programming is knowing the api (or in the case the common runtime library). There is no reason to count the number of items when the object itself will tell you.
