[site]: stackoverflow
[post_id]: 5652840
[parent_id]: 5652800
[tags]: 
SELECT SalesAgentID, SUM(SalesAmount) FROM Sales GROUPBY SalesAgentID But there's something wrong here... Why your table have BOTH SalesAgentId and SalesAgentName? It should contain only the ID, the name should be in a SalesAgent table. Then you would retrieve the name with a join
