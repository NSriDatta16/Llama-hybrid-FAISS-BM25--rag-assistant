[site]: stackoverflow
[post_id]: 4464244
[parent_id]: 4464236
[tags]: 
AJAX is asynchronous . The getJSON() call will return immediately, and your draggable() call will run before the server replies. You need to move the draggable() call into the callback. This is why the callback exists in the first place.
