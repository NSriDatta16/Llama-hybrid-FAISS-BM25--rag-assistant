[site]: stackoverflow
[post_id]: 229490
[parent_id]: 229395
[tags]: 
I will consider using a pick list instead of a combo box for performance. A general rule of thumb to improve performance when inserting many item in a UI list is to call before the insertion and setting it back to true after. Here's the correct syntax #define NB_ITEM 1000 #define ITEM_LENGTH 10 void CMFCComboDlg::InitMyCombo() { CString _strData; m_cbMyCombo.SetRedraw( FALSE ); m_cbMyCombo.Clear(); m_cbMyCombo.InitStorage(NB_ITEM, ITEM_LENGTH); for( int i = 0; i EDIT: Improve the solution to include smacl suggestion
