[site]: stackoverflow
[post_id]: 3916497
[parent_id]: 3916476
[tags]: 
They are mixed. However, it depends on the actual programming language / compiler. I can image a compiler allocating space for local variable on the heap and keeping a pointer to the storage on the stack. There is one stack per thread in each process. Hence, for example, a process with 20 threads has 20 independent stacks.
