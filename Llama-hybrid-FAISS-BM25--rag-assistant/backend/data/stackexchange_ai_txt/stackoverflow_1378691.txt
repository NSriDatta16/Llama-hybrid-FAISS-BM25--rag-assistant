[site]: stackoverflow
[post_id]: 1378691
[parent_id]: 
[tags]: 
Removing the text from drupal custom menu

I'm trying to style a custom menu within drupal. I've sucessfully styled to display a background image, but the problem is that the menu item title still displays. So I get a nice image, with blazoned all over it. Is there a template function I could use to format the custom menu and remove the text portion from the hyperlink? I've done something similar on my primary links (see below) but I could do with some help figuring out how to do so on the custom menus. function primary_links_add_icons() { $links = menu_primary_links(); $level_tmp = explode('-', key($links)); $level = $level_tmp[0]; $output = " \n"; if ($links) { foreach ($links as $link) { $link = l($link['title'], $link['href'], $link['attributes'], $link['query'], $link['fragment']); $cssid = str_replace(' ', '_', strip_tags($link)); $link = preg_replace('#( ).*?( )#', '$1$2', $link); $output .= ' ' . $link .' '; }; $output .= ' '; } return $output; } And then this called within the page.tpl.php print primary_links_add_icons(); Thanks for any help!
