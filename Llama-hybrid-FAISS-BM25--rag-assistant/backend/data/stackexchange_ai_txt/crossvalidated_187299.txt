[site]: crossvalidated
[post_id]: 187299
[parent_id]: 70099
[tags]: 
Yes. If you want to make sure that its unit root here, you can use Dickey-Fuller test with $$ H_0 \colon \text{the presence of unit root}. $$ (thats mean time series is not stationary). For example, in R: library(tseries) adf.test(YourTimeSeries)
