[site]: stackoverflow
[post_id]: 1422824
[parent_id]: 1422807
[tags]: 
Adding a bit to AlbertEin's response, I don't believe String.Format can do this for you out-of-the-box. You'll need to dynamically create the format string prior to using the String.Format method, as in: var builder = new StringBuilder(); for(var i = 0; i This isn't exactly readable, though.
