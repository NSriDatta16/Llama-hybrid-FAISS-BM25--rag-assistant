[site]: stackoverflow
[post_id]: 4098187
[parent_id]: 4098124
[tags]: 
h2, p { display: inline; } div.p { /* whatever margins/paddings you would normally apply to p */ } and This is the heading. This is the paragraph You would need to enclose all in a block level element (like ) to avoid that consecutive paragraphs collapse.
