[site]: crossvalidated
[post_id]: 533393
[parent_id]: 533392
[tags]: 
If your neural network is a binary classifier, then it's likely true that the last layer is a sigmoid unit which outputs a number between 0 and 1. Since the normal distribution isn't bounded between 0 and 1, the output can't be normally distributed.
