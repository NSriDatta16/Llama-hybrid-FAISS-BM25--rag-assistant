[site]: stackoverflow
[post_id]: 3125054
[parent_id]: 
[tags]: 
jquery missing { after property

I keep getting an error, missing { after property for the following code. $(function(){ var storage = new Array("hi","h2","hi3","h4","h5","6","7","hi8","hi9","10"); // Slider $('#slider').slider({ max: 10, orientation: 'vertical' slide: function(e,ui) { $('#storage').html(storage[ui.value-1]); } }); });
