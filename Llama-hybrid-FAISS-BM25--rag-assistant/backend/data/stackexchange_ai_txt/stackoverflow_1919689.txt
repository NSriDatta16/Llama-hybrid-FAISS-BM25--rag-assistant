[site]: stackoverflow
[post_id]: 1919689
[parent_id]: 1919632
[tags]: 
I am not sure if passing strings is an elegant solution. I would rather send the Type of entity as an argument to a method. Something on these lines : var table = _dataCont.GetTable(typeof(Customer)); Here is the MSDN documentation.
