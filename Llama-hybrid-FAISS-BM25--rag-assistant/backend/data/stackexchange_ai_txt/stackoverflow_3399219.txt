[site]: stackoverflow
[post_id]: 3399219
[parent_id]: 3398237
[tags]: 
Delete your "build" directory (icons.plist might still exist) Remove the app from device, or do a full install by dragging the .app to the Organizer ("build and run/debug" apparently does an incremental install, which means it might not delete an existing icons.plist)
