[site]: stackoverflow
[post_id]: 4634576
[parent_id]: 
[tags]: 
Convert string field to datetime field in Azure table storage

I have a field in an Azure table that holds date times in string format. I want to replace this with a DateTime field and convert the values in the string column. What's the best way to approach this? In a SQL I'd do this by creating a new column and run an update statement...
