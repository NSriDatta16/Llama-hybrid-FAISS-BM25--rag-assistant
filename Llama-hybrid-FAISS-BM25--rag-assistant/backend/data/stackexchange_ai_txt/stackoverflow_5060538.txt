[site]: stackoverflow
[post_id]: 5060538
[parent_id]: 5055233
[tags]: 
Your three points are answered by: the bindings documentation (especially the part that regards value transformers ); Formatters ; and You will have to produce a custom action that creates that object, sets it as the selection (see the Selection binding of your array controller and the bindings reference above), and then messages the NSTableView to enter editing mode.
