[site]: stackoverflow
[post_id]: 2934661
[parent_id]: 2934603
[tags]: 
You can check that the position isn't changing when the drag event is occuring, like this: var position = {}; $("#draggable").draggable({ containment: 'document', drag: function(event, ui) { if(position.top == ui.position.top && position.left == ui.position.left) { alert("Constrained"); return false; } position = ui.position; } });​ You can play with a demo of this here
