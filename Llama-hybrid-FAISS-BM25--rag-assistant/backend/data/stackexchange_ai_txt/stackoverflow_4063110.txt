[site]: stackoverflow
[post_id]: 4063110
[parent_id]: 
[tags]: 
jboss 5.1 does not work after redirect from HTTP to HTTPS connection

the jboss server does not work if redirect to HTTPS url(port 8443), and here is WAR package web.xml configuration: HtmlAdaptor Security for all users /all/* GET POST role01 CONFIDENTIAL if i change CONFIDENTIAL to NONE the server can accept HTTP request at 8081 port. what configurations should i pay attention for SSL connection? any additional configurations?
