[site]: stackoverflow
[post_id]: 4131082
[parent_id]: 
[tags]: 
Is there a simple JavaScript plugin that can let users click on a Google map (or drag an icon), and then alert the lat/long?

I want people to easily click the embedded map, and then the la/long gets alerted.
