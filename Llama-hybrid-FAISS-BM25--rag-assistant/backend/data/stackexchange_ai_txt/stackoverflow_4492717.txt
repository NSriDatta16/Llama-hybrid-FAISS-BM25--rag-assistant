[site]: stackoverflow
[post_id]: 4492717
[parent_id]: 4491981
[tags]: 
You're trying to access the parent element from within the child iframe. I haven't personally tested it, but try to set context for your jquery selector. Otherwise, it'll look only inside the current iFrame. $('#draggables img', parent).draggable();
