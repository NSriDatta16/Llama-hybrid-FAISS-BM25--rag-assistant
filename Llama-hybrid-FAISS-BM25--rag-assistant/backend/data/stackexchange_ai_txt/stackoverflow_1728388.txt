[site]: stackoverflow
[post_id]: 1728388
[parent_id]: 1728379
[tags]: 
Why do you need to migrate this ? Because of drag-n-drop ? Perhaps you should look at running it as a stand-alone Java application and use Java Web Start to serve it up. I think the coding changes should be minimal to convert it from an applet.
