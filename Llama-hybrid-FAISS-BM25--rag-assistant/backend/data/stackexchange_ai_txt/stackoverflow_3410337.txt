[site]: stackoverflow
[post_id]: 3410337
[parent_id]: 3410307
[tags]: 
Isn't ui.helper what you need? From the docs: ui.helper - the jQuery object representing the helper that's being dragged So you'll have something like $( ".selector" ).draggable({ start: function(event, ui) { ui.helper.modify(the_way_I_want_to_modify_it); } });
