[site]: stackoverflow
[post_id]: 4691366
[parent_id]: 4691245
[tags]: 
If I understood you correctly, you need something like this. If not, please let me know. char separator = ' '; int length = 10; var splitted = paragraph.Split(separator); List arrays = new List (); for (int i = 0; i y >= i * length && y word + separator).ToArray()); } foreach (var arr in arrays) { foreach (var cell in arr) { Console.Write(cell); } Console.WriteLine(); }
