[site]: stackoverflow
[post_id]: 2115301
[parent_id]: 
[tags]: 
where command line arguments are stored?

I have a doubt @ the storage of command line arguments. myprog.exe -cfgfile myconfig.cfg The command line args are passed when the process gets created, so are they stored outside the process? Where does the OS stores it?
