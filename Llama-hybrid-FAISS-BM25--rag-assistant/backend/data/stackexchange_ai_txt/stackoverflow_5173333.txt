[site]: stackoverflow
[post_id]: 5173333
[parent_id]: 5173012
[tags]: 
It may be that you're overriding a param element that you're not intending to: $('#gallery_content object param[name="movie"]').attr(...); might do the trick for you. Embed elements don't work in IE, and possibly webkit (I can never remember which works where). I would suggest using the satay method of flash embedding. It's DRY er
