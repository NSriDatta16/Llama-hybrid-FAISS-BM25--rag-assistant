[site]: stackoverflow
[post_id]: 5432226
[parent_id]: 5432153
[tags]: 
select text from table1 where id in ( select id from table2 where agentid = ) This will give you all text rows for given agentid. The same can be done using join too - select t1.text from table1 t1 inner join table2 t2 on t1.id = t2.id where t2.agentid =
