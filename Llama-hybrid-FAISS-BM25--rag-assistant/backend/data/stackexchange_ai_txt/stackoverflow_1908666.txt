[site]: stackoverflow
[post_id]: 1908666
[parent_id]: 1775790
[tags]: 
preserve (visually, at least) the item in the palette from start of drag (rather than restoring it after dropping). I presume you've already tried setting the palette's sortable helper to 'clone'? var SortParams = { ..., helper: "clone", }
