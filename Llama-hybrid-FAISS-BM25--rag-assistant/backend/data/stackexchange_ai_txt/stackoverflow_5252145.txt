[site]: stackoverflow
[post_id]: 5252145
[parent_id]: 
[tags]: 
Would like to use .fadeTo elegantly

Here's what I've got far, which works: var X = $('#myID input:checked').length; if (X) { $('input[name=Save]').show(); } else { $('input[name=Save]').hide(); } What I'd like to do is write this elegantly using the .fadeTo method. $('input[name=Save]').fadeTo(Y) Where Y=1 if X > 0 AND Y=0 if X = 0.
