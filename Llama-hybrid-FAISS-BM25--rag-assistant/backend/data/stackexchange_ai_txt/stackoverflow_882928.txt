[site]: stackoverflow
[post_id]: 882928
[parent_id]: 869107
[tags]: 
It may be this line that is in error: [Column(Storage = "_Security_USER_ID;", DbType="VarChar(15) NOT NULL")] I would double check the DbType of the attribute ( VarChar(15) NOT NULL ) against what is actually being returned from the database. Most likely there is a mismatch between the two.
