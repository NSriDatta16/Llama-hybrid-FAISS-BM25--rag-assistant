[site]: stackoverflow
[post_id]: 703297
[parent_id]: 703217
[tags]: 
Can you change the charge code field to an array of fields? Something like: for all your agents would let you do: $divide = count($_POST["agent"]); foreach($_POST["agent"] as $agent) { $sum = $_POST["amount"] / $divide; //do other stuff }
