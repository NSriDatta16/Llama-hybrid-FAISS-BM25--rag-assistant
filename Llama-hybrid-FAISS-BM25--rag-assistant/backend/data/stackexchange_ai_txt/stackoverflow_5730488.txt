[site]: stackoverflow
[post_id]: 5730488
[parent_id]: 5730317
[tags]: 
I think Facebook blocks any request if it comes from (or looks like its coming from) a script/bot. You need to add in a user-agent string to make it seem like it's a real request. I modified your proxy script to this and it works (returns a result).
