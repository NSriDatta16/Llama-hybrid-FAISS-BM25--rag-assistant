[site]: datascience
[post_id]: 118761
[parent_id]: 92630
[tags]: 
you just need to save each section to a target path model.model.save_pretrained('model1') model.tokenizer.save_pretrained('model1') model.config.save_pretrained('model1/') then you can load like this model = ClassificationModel('roberta','model1')
