[site]: stackoverflow
[post_id]: 2762714
[parent_id]: 2762698
[tags]: 
It might be a simple user-agent blockage. There might be some script identifying the user-agent and it handles only the stated browsers. Try accessing it using other browsers. You can use http://browsershots.org/ to check how the site behaves on other browsers.
