[site]: stackoverflow
[post_id]: 2197874
[parent_id]: 2197865
[tags]: 
snprintf() will use the same format string and parameters as the printf() call you already have working. char NewString[] = "00:00:00:00:00:00"; // initializing with this string is just a lazy // way to get the right amount of storage snprintf( NewString, sizeof( NewString), "%2.2x:%2.2x:%2.2x:%2.2x:%2.2x:%2.2x", var[0], var [1], var[2], var[3], var[4], var[5]);
