[site]: stackoverflow
[post_id]: 5575758
[parent_id]: 5575269
[tags]: 
Just remove them from the backstack using the popBackStack() methods. Each fragment you pop is exactly the same as if the user had pressed the BACK button, and you don't do any special cleanup when the BACK button is pressed, right?
