[site]: stackoverflow
[post_id]: 5530764
[parent_id]: 5526477
[tags]: 
You can't use utils.httpcache.getFromCache(url) until you add this to your code: var utils = {}; That's because how the author created his function, it's called JavaScript module pattern and it's generally used to structure the code.
