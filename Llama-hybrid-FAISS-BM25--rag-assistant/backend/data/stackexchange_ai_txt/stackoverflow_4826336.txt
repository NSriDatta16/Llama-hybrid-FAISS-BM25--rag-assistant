[site]: stackoverflow
[post_id]: 4826336
[parent_id]: 4826069
[tags]: 
If you're worried about blocking calls, you can start the Outlook instance in a separate thread, then your main thread can Thread.Sleep() in that while loop so that it's not checking as often. My solution isn't elegant, but you're probably not too worried about a whole lot of deadlocking, so it should work.
