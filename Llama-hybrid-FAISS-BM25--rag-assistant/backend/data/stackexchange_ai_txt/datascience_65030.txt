[site]: datascience
[post_id]: 65030
[parent_id]: 
[tags]: 
K-Fold Cross Validation for NNs

When using K-Fold CV, is it still useful to have a Train/Validation/Test split? Or simply just a Train/Test? I.e. split up data into k bins, and leave one out for testing, train on the rest, and take average of the scores.
