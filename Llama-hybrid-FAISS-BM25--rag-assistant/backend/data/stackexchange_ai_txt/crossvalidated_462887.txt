[site]: crossvalidated
[post_id]: 462887
[parent_id]: 443242
[tags]: 
For the first question, I think the authors just exchange the notations $\psi$ and $\theta$ . For the second question, the weights of neural networks are modeled as Gaussian variables with distribution $\mathcal{N}(\theta, \sigma^2I)$ .
