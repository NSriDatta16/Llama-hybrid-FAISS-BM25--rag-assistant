[site]: stackoverflow
[post_id]: 2999517
[parent_id]: 2999446
[tags]: 
I'm assuming you don't know if the file is missing until the AJAX call returns, so at that point you can attempt another lookup. function getDate(timestamp) { month = timestamp.getMonth() + 1, day = timestamp.getDate(), year = timestamp.getFullYear(); return year + '-' + ((month = 3) // Maximum number of attempts return; $jq.ajax({ type: "GET", url: "_xml/carousel/home/carousel_large_"+dateObject+".xml", dataType: "xml", success: HPCarousels.heroCarousel.parseXML, error: function(){ if (/* file is missing */) { attemptGet(timestamp - 24 * 60 * 60, attempt + 1); } else { alert('Error Loading XML Content'); } } }); } attemptGet(0);
