[site]: crossvalidated
[post_id]: 399987
[parent_id]: 399971
[tags]: 
This model is not a decision tree, it is a random forest with ntree = 100 trees. Here is one general way to do cross validation with the modelr package. library("tidyverse") library("modelr") library("uplift") # Use example in upliftRF's documentation set.seed(123) dd $treat treat == 1, 1, 0) ### fit uplift random forest uplift_model % mutate(model = map(train, uplift_model)) %>% mutate(pred = map2(model, test, predict_uplift)) #> # A tibble: 3 x 5 #> train test .id model pred #> #> 1 1 #> 2 2 #> 3 3 Created on 2019-03-28 by the reprex package (v0.2.1)
