[site]: stackoverflow
[post_id]: 4787795
[parent_id]: 4787746
[tags]: 
They are probably both strings. You should convert them to numbers first: var admin = Number($('input[name="diskStorage"]').attr('data-adminstorage')); // 2097152000 $('#new-user input[name="diskStorage"]').keyup(function(){ if(admin
