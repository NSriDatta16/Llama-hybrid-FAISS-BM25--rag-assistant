[site]: stackoverflow
[post_id]: 4539283
[parent_id]: 4539242
[tags]: 
I think you're confusing string size with character representation. For instance, you could have a character that takes 4 bytes to represent it, and put it inside of string whose max storage size requires only one byte to hold the length since there's less than 255 characters in it.
