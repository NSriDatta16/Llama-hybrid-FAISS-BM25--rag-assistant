[site]: stackoverflow
[post_id]: 2556364
[parent_id]: 2539441
[tags]: 
I recommend Tapestry . Examples at Jumpstart (see "Creating Components"). If you can't get to port 8080, the home page is here . Tapestry components and pages are made up of an XHTML file and a Java source file (like CustomerList.java and CustomerList.tml). The component is self-contained, and can do just about anything a page can do. Then you add your component to your pages' HTML like this: . You can also define your own arguments, like . In my opinion this is a more elegant solution than JSF, and I think Tapestry's IoC container makes it more powerful than Wicket. We are using Tapestry for an intranet with a few thousand users.
