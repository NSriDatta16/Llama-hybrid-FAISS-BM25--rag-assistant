[site]: datascience
[post_id]: 69108
[parent_id]: 69050
[tags]: 
Check the Date column type if it is a character you need to convert it into the date format and then change the dataframe to time series object using xts function from xts package. library(lubridate) df $Date Date) library(xts) #used to convert dataframe to ts object tsdf
