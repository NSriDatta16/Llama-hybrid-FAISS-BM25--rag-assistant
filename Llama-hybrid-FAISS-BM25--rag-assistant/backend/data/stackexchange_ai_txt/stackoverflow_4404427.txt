[site]: stackoverflow
[post_id]: 4404427
[parent_id]: 4404361
[tags]: 
I already found a way. Maybe not better than the scope wrapper, but I think it's neater: for ( var i=0; i There is now a much easier (apparently faster) way (sync!) to get a file's data URL: img.src = URL.createObjectURL(file); Demo on http://jsfiddle.net/rudiedirkx/ZWMRd/8/show/ of both methods, and the original problem illustrated (drag multiple images and check the title tooltips).
