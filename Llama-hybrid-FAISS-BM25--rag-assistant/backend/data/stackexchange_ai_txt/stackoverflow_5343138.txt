[site]: stackoverflow
[post_id]: 5343138
[parent_id]: 5339083
[tags]: 
You could always throw jQuery at it. Have your theme enqueue jquery, and add this to your header. jQuery(document).ready( function () { jQuery(".size-thumbnail").wrap(" "); jQuery(".size-thumbnail").after(" BLABLA "); }); Not the most elegant solution, but it would have the desired outcome...
