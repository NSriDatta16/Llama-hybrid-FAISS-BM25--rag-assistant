[site]: stackoverflow
[post_id]: 4171764
[parent_id]: 4171732
[tags]: 
There may be a more elegant way but I would do something like this: var inputHasFocus = false; $("#some_input_id").focusin(function () { inputHasFocus = true; }).focusout(function () { inputHasFocus = false; }); and then use that in your case statement.
