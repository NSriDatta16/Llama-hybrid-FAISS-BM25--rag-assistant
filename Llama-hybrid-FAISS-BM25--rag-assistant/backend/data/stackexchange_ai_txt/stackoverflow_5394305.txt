[site]: stackoverflow
[post_id]: 5394305
[parent_id]: 
[tags]: 
JQuery Drag & Reorder

I have an image upload script that batch uploads loads of images. I want a page where the admin user can go in and re-order these images easily. Is there a sort of click and drag and re-order JQuery plugin? I then want to read the order and assign a weight value to each.
