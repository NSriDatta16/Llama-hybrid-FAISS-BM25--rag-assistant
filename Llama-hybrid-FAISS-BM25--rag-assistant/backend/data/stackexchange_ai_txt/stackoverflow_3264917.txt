[site]: stackoverflow
[post_id]: 3264917
[parent_id]: 
[tags]: 
draggable() and droppable() not functioning in IE6,7

Hello everyone, the drag and drop app that I have built functions perfectly in chrome and firefox, but when I try it on IE I get "Object required" as an error. So the draggable() does not work at all. Can anyone please help me solve this? function readJSON(json, i, end) { myarray = eval(json); $.each(myarray, function (index, value) { if (i '); var $tooltip = $(' ').html($str[0]).addClass('tooltiphidden'); var $newDiv = $(' ').addClass('items').html($str[1] + '&nbsp;&nbsp;' + $str[2]); $newDiv.append($tooltip); $newDiv.mousedown(function () { $newDiv.contents("span:last-child").css({ display: "none" }); moveditem = $str[0] + ' ' + $str[2]; }).draggable({ helper: 'clone', opacity: 0.5 }); $('div.selectedItemlist').droppable({ accept: 'div.items', drop: function (e, ui) { $newSelectedItem = $(' ').html(ui.draggable.html()).addClass('dropitems'); $tooltip = $(' ').html(moveditem).addClass('tooltiphidden'); $('input#inrs').val($('#inrs').val() + ui.draggable.attr('id') + ','); $('div.selectedItemlist').append($newSelectedItem); ui.draggable.remove(); $('div.selectedItemlist').css({ backgroundColor: 'white', opacity: 1 }); }, over: function (e, ui) { $('div.selectedItemlist').css({ backgroundColor: 'red', opacity: 0.2 }); }, out: function (e, ui) { $('div.selectedItemlist').css({ backgroundColor: 'white', opacity: 1 }); } }); $newDiv.appendTo('div.suggestions'); i++; } }); }
