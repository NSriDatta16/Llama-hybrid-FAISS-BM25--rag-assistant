[site]: stackoverflow
[post_id]: 3759882
[parent_id]: 3759861
[tags]: 
did you mean? $_SERVER['HTTP_REFERER'] The address of the page (if any) which referred the user agent to the current page. This is set by the user agent. Not all user agents will set this, and some provide the ability to modify HTTP_REFERER as a feature. In short, it cannot really be trusted.it cannot really be trusted. Take a look also at Determining Referer in PHP
