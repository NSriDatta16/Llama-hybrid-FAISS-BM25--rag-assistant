[site]: stackoverflow
[post_id]: 2380757
[parent_id]: 
[tags]: 
Explain this fragment of jQuery to me

I am newbie to jQuery, can someone explain what this code does: $(document).ready(function() { var order = null; $("#order-list").load(location.href+" #order-list>*",""); $("#order-list").sortable({ handle : '.handle', update : function (e, ui) { order = $(this).sortable('serialize'); $("#info").load("process-sortable.php?"+order); } }); });
