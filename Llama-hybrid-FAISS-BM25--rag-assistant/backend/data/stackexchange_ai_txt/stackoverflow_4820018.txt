[site]: stackoverflow
[post_id]: 4820018
[parent_id]: 
[tags]: 
#pragma deprecate a function based on signature?

in Visual Studio is it possible to #deprecated a function, based on the signature of the function and not simply the name? In my case we're in C++ and don't want to deprecate all flavors of the function int foo();
