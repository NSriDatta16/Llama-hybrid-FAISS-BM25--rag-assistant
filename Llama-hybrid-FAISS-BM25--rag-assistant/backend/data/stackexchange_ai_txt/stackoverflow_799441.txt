[site]: stackoverflow
[post_id]: 799441
[parent_id]: 799325
[tags]: 
Using /etc/passwd and /etc/shadow for password storage? Are the UIDs in sync between systems? You could simply copy/overwrite the line in the /etc/shadow file for the particular user. And the "don't do it" answer would be to use a NIS server and change the password only once.
