[site]: stackoverflow
[post_id]: 4363400
[parent_id]: 4363348
[tags]: 
Use the replace_if function in algorithm . (See the documentation and example code here ). Your problem could be solved like this: #include // for replace_if #include // for isdigit std::replace_if(data.begin(), data.end(), isdigit, 'x'); // => Santa Claus xxx-xx-xxxx sclaus passwordy std::replace_if(data2.begin(), data2.end(), isdigit, 'x'); // => Morgan Freeman xxx-xx-xxxx mfree passwordx
