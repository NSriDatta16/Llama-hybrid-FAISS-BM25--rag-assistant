[site]: stackoverflow
[post_id]: 4144017
[parent_id]: 
[tags]: 
'module' object has no attribute 'FieldStorage'

It doesn't make sense to me, because the code is very simple and "FieldStorage" exist in documentation. #!/usr/bin/env python print 'Content-type: text/html' print import cgi def Main(): form = cgi.FieldStorage() print ' ' print ' ' print 'ola' print ' ' print ' ' if __name__ == '__main__': Main()
