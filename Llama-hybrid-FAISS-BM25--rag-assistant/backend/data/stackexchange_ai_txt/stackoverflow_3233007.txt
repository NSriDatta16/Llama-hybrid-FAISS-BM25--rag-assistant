[site]: stackoverflow
[post_id]: 3233007
[parent_id]: 3232861
[tags]: 
Would this work? SELECT SUM(t.time) as sum_of_series_of_averages FROM ( SELECT avg(elapsedTime) as time FROM statisticsTable WHERE connectionId in ([lots of values]) AND updateTime > [startTime] AND updateTime By converting your averages into an inline view, you can SUM() the averages. Is this what you are looking for?
