[site]: stackoverflow
[post_id]: 3790672
[parent_id]: 3790619
[tags]: 
You should try something like this: if (navigator.userAgent.match("/MSIE/")) { // code for IE here } else { // code for everyone else } I don't normally use regex, so it could be wrong. Any regex pattern will work for the match() method.
