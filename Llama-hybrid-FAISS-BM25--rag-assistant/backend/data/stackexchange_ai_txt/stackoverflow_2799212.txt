[site]: stackoverflow
[post_id]: 2799212
[parent_id]: 2799063
[tags]: 
Well, instead of using a in your hbm.xml, did you try to use a ? Note that you'll need an index column in the collection table to use (since List is an ordered collection). Try something like that: Refer to the section 6.2. Collection mappings in the documentation for full details. Pay a special attention to the section 6.2.3. Indexed collections .
