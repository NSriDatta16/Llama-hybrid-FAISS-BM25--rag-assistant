[site]: crossvalidated
[post_id]: 176863
[parent_id]: 
[tags]: 
Do linear regression classifiers suffer from the curse of dimensionality?

I am aware that Bayesian models and K-nearest neighbor both suffer in prediction results, especially when the dataset size does not scale exponentially to the number of dimensions. However, does the curse of dimensionality apply to linear regression classifiers as well?
