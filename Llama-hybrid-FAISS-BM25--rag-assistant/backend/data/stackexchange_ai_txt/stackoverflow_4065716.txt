[site]: stackoverflow
[post_id]: 4065716
[parent_id]: 4065674
[tags]: 
There are plenty of ways, I usually do: if (!isset($argv)) die('Not allowed'); Otherwise, a lot of vars in $_SERVER can be checked (HTTP_HOST, USER_AGENT, etc.), but the $argv method definitely works for me.
