[site]: stackoverflow
[post_id]: 1343629
[parent_id]: 1343450
[tags]: 
ContainerStartTime is the timestamp for when a task's immediate parent container (such as a Sequence Container or a For Each Loop) has started executing. CreationDate is whenever the package was created. StartTime is the time that the package began executing. As described here .
