[site]: crossvalidated
[post_id]: 300804
[parent_id]: 
[tags]: 
Nonlinear dimensionality reduction in Java/on JVM

Is there a (decently) fast implementation of Manifold Learning algorithms on JVM? I tried Smile library but it takes dozen minutes to run its Isomap on my computer, while scikit-learn implementations are couple to ten times faster.
