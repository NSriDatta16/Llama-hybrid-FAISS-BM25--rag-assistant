[site]: stackoverflow
[post_id]: 4562951
[parent_id]: 4562578
[tags]: 
The code you linked to is what you need. However, rather than: StatFs stat = new StatFs(Environment.getExternalStorageDirectory().getPath()); use something like: StatFs stat = new StatFs(getFilesDir().getPath());
