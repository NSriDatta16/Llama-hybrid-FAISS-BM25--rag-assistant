[site]: stackoverflow
[post_id]: 3725410
[parent_id]: 3725325
[tags]: 
As far as I know that isn't possible. The easiest option would be to define a custom type, that uses an int32_t as the backing storage, and implements appropriate maths as override operators. For better storage density, you could store 3 int20 in a single int64_t value.
