[site]: stackoverflow
[post_id]: 484289
[parent_id]: 484170
[tags]: 
There's no built-in facility in .NET to list all functions called, although you can print the call-stack at a specific point (like exceptions do). I'd suggest looking for a code-coverage profiler if this is the route you want to take.
