[site]: stackoverflow
[post_id]: 4890312
[parent_id]: 4890290
[tags]: 
length = password.length(); while (length You never update length , even after obtaining a new password. Here's a better way to organize your code: public static String enterPassword() { //gets a string and returns it } public static boolean checkPasswordLength(String password) { //if too long return false } //... String password = enterPassword(); while ( !checkPasswordLength(password) ) { password = enterPassword(); }
