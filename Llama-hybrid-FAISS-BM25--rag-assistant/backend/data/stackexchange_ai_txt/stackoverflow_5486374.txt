[site]: stackoverflow
[post_id]: 5486374
[parent_id]: 5484378
[tags]: 
jQuery('#wrap1').draggable().resizable({handles: 'e, w,ne,nw,se,sw', resize: function(event, ui) { $(this).css('height', 'auto'); } }); that code made it draggable and then in resize it updated the div height :) We also got the resize boxes to imlement by appending html code into the wrap1 div...then on deselect it removed this code
