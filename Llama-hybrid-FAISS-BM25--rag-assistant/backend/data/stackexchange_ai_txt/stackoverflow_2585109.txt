[site]: stackoverflow
[post_id]: 2585109
[parent_id]: 2583883
[tags]: 
You cannot use the .live() function with .draggable() directly, but you can use .live() with the mouseover event and re-attach .draggable() on mouseover like this. $('.draggable').live('mouseover',function(){ $(this).draggable(); });
