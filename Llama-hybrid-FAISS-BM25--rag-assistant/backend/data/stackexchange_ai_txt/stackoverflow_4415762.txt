[site]: stackoverflow
[post_id]: 4415762
[parent_id]: 4415745
[tags]: 
Right now you're mixing your data structure (linked list) with what you intend to use it for. Consider splitting your logic into: Your I/O code. The linked list implementation. A function that takes a linked list, and computes the average.
