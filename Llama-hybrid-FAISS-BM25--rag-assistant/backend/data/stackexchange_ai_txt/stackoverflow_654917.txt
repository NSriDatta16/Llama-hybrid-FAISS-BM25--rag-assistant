[site]: stackoverflow
[post_id]: 654917
[parent_id]: 654906
[tags]: 
The simple solution would be creating an array (or a List ) and than randomize its indexes. EDIT: static IEnumerable Randomize (this IEnumerable source) { var array = source.ToArray(); // randomize indexes (several approaches are possible) return array; } EDIT: Personally, I find the answer of Jon Skeet is more elegant: var results = from ... in ... where ... orderby Guid.NewGuid() select ... And sure, you can take a random number generator instead of Guid.NewGuid() .
