[site]: stackoverflow
[post_id]: 3408951
[parent_id]: 3408839
[tags]: 
What I would do: Check that the URL is valid using a very open regex or filer_var with FILTER_VALIDATE_URL . Do an file_get_contents on the url and check that $http_response_header[0] contains a 200 HTTP resoponse. Now, that's dirty, sure there is some more elegant version using curl and stuff.
