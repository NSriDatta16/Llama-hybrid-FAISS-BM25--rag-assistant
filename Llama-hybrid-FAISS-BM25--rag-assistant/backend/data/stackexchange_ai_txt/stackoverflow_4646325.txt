[site]: stackoverflow
[post_id]: 4646325
[parent_id]: 4646012
[tags]: 
You can check current request object - it holds all data about module, controller, action and params which is currently in use. After that you could compare it with product page ones: does the module, controller and action the same and does product id matches But you should aware of third-party customizations which could replace standard Magento product view controller with theirs ones
