[site]: stackoverflow
[post_id]: 4442090
[parent_id]: 4022860
[tags]: 
Not exactly sure if this is your solution, but have you looked at ? BitmapFactory.Options.inTempStorage The way you use it is: BitmapFactory.Options options = new BitmapFactory.Options(); options.inTempStorage = new byte[16*1024]; Bitmap bitmap_origin = BitmapFactory.decodeFile(path, options);
