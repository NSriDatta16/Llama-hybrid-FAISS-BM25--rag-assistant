[site]: stackoverflow
[post_id]: 939876
[parent_id]: 938984
[tags]: 
This basic setup appears to do what you want. $(function() { $(".drag").draggable({ helper: 'clone' }); $("#dropArea").droppable({ drop: function(event, ui) { //ui.draggable.appendTo(this); $(this).append(ui.draggable); } }); }); If this doesn't work for you, can you show us some code?
