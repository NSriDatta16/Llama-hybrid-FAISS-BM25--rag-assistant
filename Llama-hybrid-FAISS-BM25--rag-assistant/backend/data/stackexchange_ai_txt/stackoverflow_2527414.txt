[site]: stackoverflow
[post_id]: 2527414
[parent_id]: 
[tags]: 
Segmentation fault while matrix multiplication using openMp?

My matrix multiplication code is int matMul(int ld, double** matrix) { //local variables initialize omp_set_num_threads(nthreads); \#pragma omp parallel private(tid,diag,ld) shared(i,j,k,matrix) { /* Obtain and print thread id */ tid = omp_get_thread_num(); for ( k=0; k I assume that it is because of matrix object only but why will it be null even though it is passed as a parameter..
