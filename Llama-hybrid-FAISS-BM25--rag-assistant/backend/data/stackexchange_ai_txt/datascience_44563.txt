[site]: datascience
[post_id]: 44563
[parent_id]: 44561
[tags]: 
Yes it affects the overall performance. As you say it is time series data, I suggest replacing the missing values with geometric mean of the populated values. Alternatively, you can delete the variables that contain missing values if they are correlated with fully populated variables.
