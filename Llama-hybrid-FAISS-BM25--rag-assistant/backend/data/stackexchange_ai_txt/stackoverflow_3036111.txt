[site]: stackoverflow
[post_id]: 3036111
[parent_id]: 3035844
[tags]: 
The xaml code below : And on the slidervaluechangedevent : this.contentScale.ScaleX = this.contentScale.ScaleY = e.NewValue; this.layout.ApplyLayoutTransform(); LayoutTransformer is ther in toolkit, add its reference: System.Windows.Controls.Layout.Toolkit Without having a toolkit, u can do it but it doesn't work properly(it will never update the scrollviewer)...try it the below one : And on the slidervaluechangedevent : `this.contentScale.ScaleX = this.contentScale.ScaleY = e.NewValue`; Hope it helps :)....
