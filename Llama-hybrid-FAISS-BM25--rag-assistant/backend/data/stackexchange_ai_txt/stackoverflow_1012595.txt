[site]: stackoverflow
[post_id]: 1012595
[parent_id]: 1012571
[tags]: 
If you don't want to drag in all of boost, go with strtod(3) from - it already returns a double. #include #include #include #include using namespace std; int main() { std::string num = "0.6"; double temp = ::strtod(num.c_str(), 0); cout Outputs: $ g++ -o s s.cc $ ./s 0.6 0.6 $ Why atof() doesn't work ... what platform/compiler are you on?
