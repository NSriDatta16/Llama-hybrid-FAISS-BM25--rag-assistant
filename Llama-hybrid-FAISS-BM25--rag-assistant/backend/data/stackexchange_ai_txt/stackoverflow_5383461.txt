[site]: stackoverflow
[post_id]: 5383461
[parent_id]: 
[tags]: 
Problem Adding Navigation Controller to MainWindow Xcode 4

I'm sure this is something obvious, but how do I add a nav controller to the main window in xcode 4 - when I drag and drop it won't let me drop the nav controller on the window. Thanks, Martin
