[site]: stackoverflow
[post_id]: 5413529
[parent_id]: 5413498
[tags]: 
I think you have 2 options: Use an SSH key (certificate) without a passphrase and protect it with filesystem permissions. Use an SSH agent. The problem with this approach is that you need to enter a passphrase at some point so using it with cron jobs is a bit trickier.
