[site]: datascience
[post_id]: 3766
[parent_id]: 
[tags]: 
predict with Multinomial Logistic Regression

If I execute the following code I have no problem: require(foreign) require(nnet) require(ggplot2) require(reshape2) ml but if I try: require(caret) ml it returns Error in eval(expr, envir, enclos) : object 'sesmiddle' not found , why?
