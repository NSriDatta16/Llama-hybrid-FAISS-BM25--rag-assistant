[site]: stackoverflow
[post_id]: 3271812
[parent_id]: 3271765
[tags]: 
"Bad" as in invalid? No. "Bad" as in unsemantic? Yes. A better way of doing this would be to wrap your label elements in p elements. They are paragraphs, after all. (Besides, labels are not something that should be block elements, it is an inline element because it gives purpose to inline text, the same way em or the HTML5 time element does — a block element does more than that. Style it as a block element using CSS) Edit: See also the answer by Alohci, explaining why is being used incorrectly here.
