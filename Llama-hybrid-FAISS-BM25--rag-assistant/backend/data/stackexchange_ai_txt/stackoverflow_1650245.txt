[site]: stackoverflow
[post_id]: 1650245
[parent_id]: 1650236
[tags]: 
Make sure the content encoding of your input HTML page is UTF-8. That way, PHP will know that the content that it is receiving is in UTF-8. You can do this by embedding the following in the head of your document: Update: Also make sure that the HTML that you're generating (echo'ing from) also reports UTF-8 as the content encoding. Once you get the echo working, the next step is to ensure that you're DB is also expecting/storing UTF-8.
