[site]: stackoverflow
[post_id]: 5398816
[parent_id]: 5398761
[tags]: 
1) first five elements? $('.block:lt("5")'); // zero-based indexing. 2) number of visible blocks (they can be invisible). $('.block:visible').length; // gets all visible '.block' elements. $('.block:visible:lt("5")'); // the first five elements $('.block:visible:gt("2")'); // all elements after the third (zero-based indexing) 3) number of all elements? $('.block').length; 4) number of invisible elements? $('.block').not(':visible'); $('.block:hidden'); // thanks @strager 5) make visible element(n)? $('.block').eq(n).show(); 6) make invisible elements from n to n? $('.block').each( function(i){ var lowBound = 2, highBound = 10; if (i > lowBound && i References: lt() . gt() . not() . eq() . show() . each() . index() .
