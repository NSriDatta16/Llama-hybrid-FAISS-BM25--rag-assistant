[site]: datascience
[post_id]: 73999
[parent_id]: 73997
[tags]: 
You can add features as you create them and check if the scoring improves (and by how much). Always measure the score on test data so you can detect overfitting. When you are done with feature engineering you can perform feature selection to reduce the number of features or understand their importances in the model.
