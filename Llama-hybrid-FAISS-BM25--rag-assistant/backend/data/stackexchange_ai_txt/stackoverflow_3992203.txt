[site]: stackoverflow
[post_id]: 3992203
[parent_id]: 3991209
[tags]: 
Is your plugin run on the admin side? In that case you could consider tapping into the built in AJAX processer. So you could do something like this... var calData = { month: currentMonth, year: currentYear - 2000, days: daysInMonth action: update_calender //notice we send an action here... } //The callback function I just copied and pasta from what you had... jQuery.post('ajaxurl',calData,function(data){ jQuery("#Calender").html(data); jQuery( "#eventDialog" ).dialog({ autoOpen: false, resizable: false, width: 300, minHeight: 200 }); jQuery( "#eventDialog" ).bind( "dialogopen", function(event, ui) {}); }) http://codex.wordpress.org/AJAX_in_Plugins Or for a less elegant, all js solution you could try... jQuery.post(location.protocol+"//"+location.hostname+"/wp-content/plugins/yourpluginname/scriptname", data, function...
