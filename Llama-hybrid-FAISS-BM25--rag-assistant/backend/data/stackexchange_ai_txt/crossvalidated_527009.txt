[site]: crossvalidated
[post_id]: 527009
[parent_id]: 
[tags]: 
How does Shapley obtain values in the probability space in tree classifiers?

By default, the Shapley values for a tree explainer (e.g. based on xgboost) are in the log odds space (where they are additive). However, there's a functionality in the package to obtain values in the probability space through the argument model_output='probability'. How does it work under the hood?
