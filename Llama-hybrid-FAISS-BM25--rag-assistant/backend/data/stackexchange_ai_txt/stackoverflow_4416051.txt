[site]: stackoverflow
[post_id]: 4416051
[parent_id]: 
[tags]: 
In C++,is it possible to define a class used as: SomeObj obj1; SomeObj obj2;

I want to implement a string class which has an option to create the inner buffer on stack or heap. So that I think about the appearance of that string like: String str_on_heap; String str_on_stack; will be elegant. But, latter I find that it is hard to implement such an interface in C++. template class String { ... // Codes to implement String on stack. }; template <> class String { ... // Codes to implement String on heap. }; String str_on_heap; // Compile error, should be "String<> str_on_heap;" String str_on_stack; // OK. Does any one has an idea or other C++ tricks to offer such an interface?
