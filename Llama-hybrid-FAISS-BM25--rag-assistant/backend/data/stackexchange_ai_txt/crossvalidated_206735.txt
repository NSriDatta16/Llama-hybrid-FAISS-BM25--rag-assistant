[site]: crossvalidated
[post_id]: 206735
[parent_id]: 
[tags]: 
logistic regression - class created with non significant coefficients?

I've created an example table (just in order to create a function) with: ex Then I run a logistic regression model (MASS library) fm As you can see coefficients are not significant, but I've created the class using them. So I don't understand why this is happening.
