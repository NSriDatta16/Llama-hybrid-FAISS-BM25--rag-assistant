[site]: stackoverflow
[post_id]: 2914689
[parent_id]: 2887614
[tags]: 
ok now i got it! Everything was fine but one mistake: in my own validation handler, i set form_set_error and if there is an error, and $form_state['rebuild'] = true, the function drupal_get_form will not rebuild the form until there is no form error. I found it in form.inc line 141 if ((!empty($form_state['storage']) || !empty($form_state['rebuild'])) && !empty($form_state['submitted']) && !form_get_errors()) {
