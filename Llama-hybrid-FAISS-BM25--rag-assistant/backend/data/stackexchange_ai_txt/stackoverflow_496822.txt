[site]: stackoverflow
[post_id]: 496822
[parent_id]: 
[tags]: 
In SQL Server, how do you determine the number of rows per page?

In SQL Server, how do you determine the (average or expected) number of rows per page? I know know how to determine the average and max row size, but I don't know how that relates to actual rows/page.
