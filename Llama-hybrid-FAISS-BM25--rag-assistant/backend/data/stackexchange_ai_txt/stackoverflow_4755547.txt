[site]: stackoverflow
[post_id]: 4755547
[parent_id]: 4755471
[tags]: 
I am going to propose ... do nothing . This assumes the serialized result is returned with the page and/or an additional HTML fragment. // In some JavaScript area somewhere in the ASP page var myObject = ; This works and is valid because JSON is a subset of JavaScript literals . Note that I did not put quotes around the . If the de-serialization is the result on an AJAX call returning JSON, etc, then see Zack's answer.
