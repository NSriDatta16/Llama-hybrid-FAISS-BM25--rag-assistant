[site]: crossvalidated
[post_id]: 28124
[parent_id]: 25400
[tags]: 
See if something like this works: proc mcmc data = /* your data here */ nmc = 20000, nbi = 1000; /* iterations and burn-in */ /* Specify parameters -- separate statements means they're proposed separately */ parms a; parms b; parms theta; /* Specify priors */ hyper a ~ gamma(1.5, scale = .25); hyper b ~ gamma(1.5, scale = .25); prior theta ~ gamma(a, scale = b); /* Sampling model */ lam = theta*time; model fail ~ poisson(lam); run;
