[site]: stackoverflow
[post_id]: 5534691
[parent_id]: 5534655
[tags]: 
For small apps, I use to keep the state of this kind of UIs in a class (e.g. in your case a list of booleans for the checkboxes), and then serialize/deserialize the class into an XML file. BTW, I usually save it the the IsolatedStorage.
