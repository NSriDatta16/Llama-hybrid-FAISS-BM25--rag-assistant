[site]: stackoverflow
[post_id]: 4531048
[parent_id]: 
[tags]: 
Textarea and monospace fonts

In case cols textarea attribute "specifies the visible width in average character widths", why it doesn't mean, in fact, that if one uses monospace font, he'll get exactly as many letters to fit textarea rect, as it actually defined in cols? upd:
