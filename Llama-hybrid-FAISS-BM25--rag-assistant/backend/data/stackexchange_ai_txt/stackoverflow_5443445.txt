[site]: stackoverflow
[post_id]: 5443445
[parent_id]: 2692859
[tags]: 
When you say "the fragment part is stripped", do you mean your web server sees a request with no fragment identifier? That's normal since the UA (browser) is supposed to strip that part. See https://en.wikipedia.org/wiki/Fragment_identifier#Processing (pun intended!)
