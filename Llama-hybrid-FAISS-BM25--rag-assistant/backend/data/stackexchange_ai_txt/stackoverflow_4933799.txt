[site]: stackoverflow
[post_id]: 4933799
[parent_id]: 4933710
[tags]: 
use strict; use warnings; my $file_name = shift @ARGV; open(my $file, ' Always use strict and warnings . If either of them complains, fix the code, do not comment out the pragmas. You can also use autodie to avoid the explicit or die after open, see autodie .
