[site]: datascience
[post_id]: 104750
[parent_id]: 104730
[tags]: 
One option is training an embedding space that includes both the strings and the labels as entities. Example algorithms are doc2vec (where the doc is the label) and StarSpace. The result is a dense vector representation that can be used with common distance metrics like L2 and cosine distance.
