[site]: datascience
[post_id]: 74103
[parent_id]: 74000
[tags]: 
This is the closer answer I can find. # Input inputs = Input(shape=(max_length,), name="Input") # Embedding embed = Embedding(input_dim=n_words+1, output_dim=embedding_size, input_length=max_length, name="Embedding")(inputs) # Bi-LSTM encoder = Bidirectional(LSTM(units=hidden_state_encoder_size, return_sequences=True, dropout=dropout_rate, name="LSTM"), name="Bi-LSTM") hidden_states = encoder(embed) # Average average = GlobalAveragePooling1D(name="Average")(hidden_states) # Outputs outputs = Dense(n_tags, activation="softmax", name="Output")(average) ```
