[site]: datascience
[post_id]: 115741
[parent_id]: 115677
[tags]: 
Not a complete solution, but in R: as_tibble(iris) %>% rename(foo = any_of(c("Petal.Length", "OtherName", "OtherOtherName"))) will rename to foo anything that's in that vector of variable names. h/t Robert On
