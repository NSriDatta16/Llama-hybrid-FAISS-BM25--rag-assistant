[site]: stackoverflow
[post_id]: 3998998
[parent_id]: 3998961
[tags]: 
You can certainly do that ArrayList ar1 = new ArrayList(); ar1.add("a"); ar1.add("b"); ar1.add("c"); ar1.add("d"); for (int i = 0; i Only pay attention that after you remove first element, other elements shift. Thus, calling ar1.remove(0); ar1.remove(1); will effectively remove first and third elements from the list. This will delete first two elements, though: ar1.remove(0); ar1.remove(0);
