[site]: crossvalidated
[post_id]: 578958
[parent_id]: 
[tags]: 
Confusion about consistency of time series model parameter

Can someone clear this confusion. Lets say I have a time series model: $$X_t \text{ follows Poisson}(\lambda_t)$$ $$\lambda_t=a*X_{t-1}+b*\lambda_{t-1}$$ Then I find estimators for $a$ and $b$ called: $\hat{a}$ and $\hat{b}$ . If the sample size increases: I take for example x data for 10 days calculate $\hat{a}$ and $\hat{b}$ then I take x data for 50 days calculate $\hat{a}$ and $\hat{b}$ then I take x data for 100 days calculate $\hat{a}$ and $\hat{b}$ The mean of $\hat{a}$ and $\hat{b}$ should approach the true value of $a$ and $b$ to be consistent? Am I correct? Does it being a time series matter? Also to calculate the means of $\hat{a}$ and $\hat{b}$ , does the number of sample used matter? Like 100 different 10 days samples or 500 different 10 days samples. I will get the samples using a computer simulation using the true values of $a$ and $b$ . Edited for context: I will explain. I will use a computer simulation using value a and b to generate 500 different 10 days long data. Then calculate 500 different $\hat{a}$ s and $\hat{b}$ s. Then get mean( $\hat{a}$ ) and ( $\hat{b}$ ). I will use a computer simulation using value a and b to generate 500 different 50 days long data. Then calculate 500 different $\hat{a}$ s and $\hat{b}$ s. Then get mean( $\hat{a}$ ) and ( $\hat{b}$ ). I will use a computer simulation using value a and b to generate 500 different 100 days long data. Then calculate 500 different $\hat{a}$ s and $\hat{b}$ s. Then get mean( $\hat{a}$ ) and ( $\hat{b}$ ). Then get 3 differents means for ( $\hat{a}$ ) and ( $\hat{b}$ ) for 1),2) and 3). Will mean( $\hat{a}$ ) and mean( $\hat{b}$ ) approach $a$ and $b$ ?. And, thus will I be able to say that $\hat{a}$ and $\hat{b}$ are consistent estimators.
