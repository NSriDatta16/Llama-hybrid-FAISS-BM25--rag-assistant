[site]: stackoverflow
[post_id]: 2873095
[parent_id]: 2873037
[tags]: 
You should be able to do something like this ( using System.Linq; ): foreach( Country c in countries) { int average = c.city.Sum( city => city.sqkm)/c.city.Count(); // display it, or save it, or something }
