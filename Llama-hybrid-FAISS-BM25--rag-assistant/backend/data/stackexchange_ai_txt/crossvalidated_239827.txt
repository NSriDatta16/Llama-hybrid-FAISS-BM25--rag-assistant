[site]: crossvalidated
[post_id]: 239827
[parent_id]: 239814
[tags]: 
SVM does not "find features", the features are pre-specified (either directly, or via a kernel function). Moreover, SVM is a linear technique, while your "reduction" introduces a new nonlinear feature $a/b$. So the result is perfectly sensible.
