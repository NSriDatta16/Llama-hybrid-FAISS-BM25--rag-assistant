[site]: stackoverflow
[post_id]: 880543
[parent_id]: 878852
[tags]: 
I'm not quite sure what you're asking, so clarification would be helpful. Here's my crack at what your looking for: Javascript $( function() { // Add number from as an to the $('#add_number').click( function() { // Get Number from var numberToAdd = $('#number_to_add').val(); // Make sure it's not a duplicate; if so, don't add var match = false; $('#mySelect option').each( function() { if (numberToAdd == this.value) match = true; }); if (match) return false; // Add the number to the $('#mySelect').append( $(' ').html(numberToAdd).val(numberToAdd) ); // Show the remove button $('#remove_selected').show(); return false; }); // Remove the currently selected in the $('#remove_selected').click( function() { $('#mySelect option:selected').remove(); return false; }); }) HTML Number: Add Remove
