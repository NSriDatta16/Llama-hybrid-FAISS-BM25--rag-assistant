[site]: stackoverflow
[post_id]: 5038504
[parent_id]: 
[tags]: 
xml with same element with different value need a schema

For the following xml need a schema. Listing all the products of a company powesystem APC 50K$ 2001 Electrical 2 avialble false sync transformer Search the products of a company from the repository high-voltage powesystem APC Electrical 2 avialble false sync setup-transformer Here different elements with operation like list_products , search_products and so on. Each element will have some common attributes such as ops_description , module and so on. Also some of the unique attributes for each element such as prod_price , manf_date etc. I want to have a xml schema to validate. Some of the attributes also optional. I tried using abstract and derived but could not make it work.
