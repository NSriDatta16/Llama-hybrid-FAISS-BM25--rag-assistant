[site]: stackoverflow
[post_id]: 1847600
[parent_id]: 1847493
[tags]: 
do you have the APPLICATION.JNLP file that is the same (has the same exact content) of your jnlp file that is included in the signed jar? it would be located in your resources/JNLP-INF directory. also- are you using maven? maven has plugins to help with signing jnlps
