[site]: stackoverflow
[post_id]: 5561285
[parent_id]: 5561222
[tags]: 
I think you might be looking for the glorious Sticky Footer . Scale the preview box vertically to see what the design does. The footer sits at the bottom of the page when the middle doesn't push it down, but it floats down with the page when the content pushes it. The method is a bit inelegant-looking, but it's easy to do: Foo Foo Foo The trick is all in the duct tape: http://www.cssstickyfooter.com/ .
