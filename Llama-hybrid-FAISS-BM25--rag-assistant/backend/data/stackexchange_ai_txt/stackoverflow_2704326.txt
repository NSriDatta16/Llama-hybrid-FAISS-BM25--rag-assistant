[site]: stackoverflow
[post_id]: 2704326
[parent_id]: 2704284
[tags]: 
An elegant method for doing that in C# is using extension methods ( Link ). You can then only import the relevant namespace if you need your utility functions. On the technical side this is basically just a static utility class as you described it, but in C# you get nice syntactic sugar for using them.
