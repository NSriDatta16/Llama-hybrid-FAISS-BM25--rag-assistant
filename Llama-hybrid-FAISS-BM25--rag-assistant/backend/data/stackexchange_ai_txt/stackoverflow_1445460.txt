[site]: stackoverflow
[post_id]: 1445460
[parent_id]: 
[tags]: 
appending last node in drag and drop

I am using the following code: function deleteImage2($item) { $item.fadeOut(function() { var $list = $('ul',$albumcover).length ? $('ul',$albumcover) : $(' ').appendTo($albumcover); $item.appendTo($list).fadeIn(); }); } "albumcover" is name of DIV and this code is working fine, but I need to append last node, how can I modify this query? Kind Regards
