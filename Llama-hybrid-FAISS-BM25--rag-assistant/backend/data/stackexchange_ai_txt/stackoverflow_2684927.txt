[site]: stackoverflow
[post_id]: 2684927
[parent_id]: 
[tags]: 
XPath to find text node that is a sibling of other nodes

Given the following fragment of html: My Legend Some text Text to capture Is there an xpath expression that will return only the 'Text to capture' text node? Trying /fieldset/text() yields three nodes, not just the one I need.
