[site]: stackoverflow
[post_id]: 5648835
[parent_id]: 5648752
[tags]: 
The default binding behaviour of TextBoxes is to update when focus is lost, so when you drag away this is triggered. You could change the UpdateSourceTrigger of the binding to Explicit to avoid the automatic update but then of course you need to trigger the update manually at some time.
