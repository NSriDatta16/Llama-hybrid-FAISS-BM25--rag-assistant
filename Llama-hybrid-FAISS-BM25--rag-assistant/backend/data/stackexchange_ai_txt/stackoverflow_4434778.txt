[site]: stackoverflow
[post_id]: 4434778
[parent_id]: 
[tags]: 
jQuery-UI - .append from .droppable into p.array (almost there!)

The site is using .draggable and .droppable UI to append the players ID from the span tag. This is appended into the following paragraphs: From these divs DROP PLAYERS INTO AREAS BELOW Goals Assists Yellow card Red card Captain By this javascript (cut down to needed code) $(function() { $(".droppable").droppable({ activeClass: 'dragactive', hoverClass: 'drophover', drop: function(event, ui) { $(".array").append(ui.draggable.children("span").text() + ', ') } }); }); I need the dropped items to get into the right p.array based on the second class. Now, the result from .droppable is appended to EVERY array class. This is an example of the draggable player's mark-up: 58 17 Traoré Hope you can help me with this. Thanks in advance.
