[site]: stackoverflow
[post_id]: 2674084
[parent_id]: 2673969
[tags]: 
AFAIK there are no events in jQuery that are fired when the dom changes. You would need to run code on the newly attached element which becomes fully queryable as soon as it is passed to jquery eg var $span = $(' my new document fragment '); $span.css('what','ever'); $('div').append($span);
