[site]: stackoverflow
[post_id]: 5524214
[parent_id]: 5524182
[tags]: 
It depends on the storage engine. MyISAM will not allow concurrent updates to the same table even if different rows are affected because MyISAM always locks the whole table. Whereas InnoDB will allow to update two different rows concurrently as it does row level locking.
