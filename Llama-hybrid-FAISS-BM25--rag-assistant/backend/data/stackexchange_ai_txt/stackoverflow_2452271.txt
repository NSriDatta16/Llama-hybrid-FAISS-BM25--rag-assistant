[site]: stackoverflow
[post_id]: 2452271
[parent_id]: 
[tags]: 
Can Nokogiri use a SAX parser to parse an HTML fragment?

I have this code. class MyParser Run on an HTML fragment like this, Hello Hi. the output shows that only the first element is processed: start_element h1 characters Hello end_element h1 If I wrap the fragment in html and body tags, the whole input is parsed. Is there a way to use a SAX style parser on HTML fragments?
