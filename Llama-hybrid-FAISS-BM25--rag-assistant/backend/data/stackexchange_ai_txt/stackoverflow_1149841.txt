[site]: stackoverflow
[post_id]: 1149841
[parent_id]: 1149534
[tags]: 
There will only be one user per installation at the moment; however, I'd like to get an idea of how storage methods change if it were multiple users per installation. Format is irrelevant as long as you only save data within the user's Home directory by default. Your options include property lists, Core Data (more an architectural decision—you either base your app on Core Data or don't use it), SQLite, NSKeyedArchiver, and your own custom format.
