[site]: stackoverflow
[post_id]: 5775070
[parent_id]: 5772206
[tags]: 
var d = document.getElementById("box"); function fadeOut(r, s) { if (!this instanceof Element) return false; this.style.opacity = ".90"; var opacity = this.style.opacity; var t = setInterval(function() { if (!r) r = 500; opacity = (opacity) - s; if (opacity == ".0") return; }, r); }; fadeOut.apply(d, [100, 5]); There are many things wrong with this code. First, CSS properties are strings, so your opacity addition line should be: opacity = parseInt(opacity) - 0.05 + ''; Opacity is turned into an int, then adds negative 0.05, then is casted back into a string using + '' Secondly, if (!r) r = 500; should be defined outside the anonymous function. You should have: var d = document.getElementById("box"); function fadeOut(fadeScaler, hertz) { if (!this instanceof Element) return false; hertz = (!hertz) ? 60 : hertz; // Approx 60 hertz refresh rate var opacity = this.style.opacity opacity = ".9"; var t = setInterval( function() { opacity = parseInt(opacity) - fadeScaler + ''; if (parseInt(opacity) Pay more attention to the legibility and style of your code. An elegant coder is a smart coder. Also, in IE opacity is the equivalent to the following: this.filters.alpha.opacity=100 and it goes from 0 - 100.
