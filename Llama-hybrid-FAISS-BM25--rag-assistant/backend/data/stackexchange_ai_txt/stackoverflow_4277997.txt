[site]: stackoverflow
[post_id]: 4277997
[parent_id]: 4277882
[tags]: 
What if your database will have several console clients? It looks like the storing of the LastAppRun is a client-side task. You may serialize LastAppRun into a file and store it in IsolatedStorageFile. Other (more ugly) option is to go for a registry key.
