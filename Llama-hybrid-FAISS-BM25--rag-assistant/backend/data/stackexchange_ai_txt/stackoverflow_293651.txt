[site]: stackoverflow
[post_id]: 293651
[parent_id]: 293602
[tags]: 
Does it work successfully on other pages? Also is the Topstyle.asp page a ASP.NET page? If not it will not work as you can't mix the two technologies like that. In ASP.NET the include syntax is discouraged. Try using a user control instead, or the . If you are using ASP 2.0 look at using Master Pages, but that might be a big change for your app.
