[site]: stackoverflow
[post_id]: 5199786
[parent_id]: 5178979
[tags]: 
If you set Windows Explorer on the server to show Hidden Files, you'll probably see a temp file created by Word. I must say, however, that using SQL Server to call an .exe (security risk on the server) which then spawns Word sounds like a very fragile architecture and prone to problems.
