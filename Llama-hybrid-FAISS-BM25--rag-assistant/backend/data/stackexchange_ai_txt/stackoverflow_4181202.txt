[site]: stackoverflow
[post_id]: 4181202
[parent_id]: 
[tags]: 
Problem with jQuery Draggable

I'm having trouble getting the very last draggable item in the loop below to become draggable (all the other items before the last item are fine, they are draggable). The html it's outputting to my browser for the last draggable item looks fine when I view source exept for the very last draggable, the "ui-draggable" class isnt being attached to the div (when looking with firebug). Any ideas? Thanks result() as $idx => $row): ?> Header ?> Teaser ?> PID; ?>/ FID; ?>">Click Here for more information products_model->get_category_slider_images($row->PID); ?> result() as $idx2 => $row): ?> IID ?>" style="position: absolute; top: SliderTop ?>px; left: SliderLeft ?>px;"> Name ?>" /> $(function () { $("#catdraggable IID ?>").draggable({ stop: function (event, ui) { p = $("#catdraggable IID ?>").position(); $.post("/admin/set_slider_image_position", { id: ' IID ?>', top: p.top, left: p.left, context: 'cat' }) } }); });
