[site]: crossvalidated
[post_id]: 299926
[parent_id]: 
[tags]: 
Create a neural network with a matrix output

Is it possible to create a neural network that has a matrix as its output instead of a vector? Or do I have to vectorize the matrix, i.e. make make one long vector instead of a matrix.
