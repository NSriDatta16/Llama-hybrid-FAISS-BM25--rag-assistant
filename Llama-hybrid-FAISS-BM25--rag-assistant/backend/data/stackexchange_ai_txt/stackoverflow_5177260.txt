[site]: stackoverflow
[post_id]: 5177260
[parent_id]: 5177210
[tags]: 
If $json is a integer-indexed array (although I doubt it) for ($i=0;$i data->from->name." "; } otherwise a less elegant solution would be $i = 0; foreach($json->data as $v) { echo $v->from->name." "; if(++$i == 3) {break;} } finally foreach(slice($json->data, 0, 3, TRUE) as $v) { echo $v->from->name." "; }
