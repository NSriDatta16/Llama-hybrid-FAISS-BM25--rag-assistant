[site]: stackoverflow
[post_id]: 2496836
[parent_id]: 2496716
[tags]: 
You can use Boost bind for member functions, for example creating a thread on a member function: class classA { public: void memberThreadFunc(int i); }; void main() { classA a; boost::thread( boost::bind(&classA::memberFunc, &a, 123) ); }
