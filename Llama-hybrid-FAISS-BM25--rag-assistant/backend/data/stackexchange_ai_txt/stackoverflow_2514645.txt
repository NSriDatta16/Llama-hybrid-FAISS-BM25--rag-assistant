[site]: stackoverflow
[post_id]: 2514645
[parent_id]: 2514624
[tags]: 
You could use a class: ... in the css .elegance{ overflow:hidden; max-width:400px; /* or just width, depending on what you want*/ display:inline-block; } ... on document ready $("label").addClass("elegance"); Or just wrap a div around it using $("label").wrap(" "); If you don't want to use separate css, you can set it on the fly using .css()
