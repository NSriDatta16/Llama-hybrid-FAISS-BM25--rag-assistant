[site]: stackoverflow
[post_id]: 2796055
[parent_id]: 2796004
[tags]: 
Since you already have jQuery referenced on the page, why not leverage that power? Assuming you've followed their convention and assigned a class of "datepicker" to your input element, try this: function allow_submit() { if($(".datepicker").val() == "") return false; return true; }
