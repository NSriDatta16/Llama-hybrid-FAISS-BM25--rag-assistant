[site]: stackoverflow
[post_id]: 5717474
[parent_id]: 
[tags]: 
rapidxml parse error with url attribute

I'm getting a strange error with rapidxml when parsing a xml file like It throws "expected >". Im using a code like the following to parse the data std::fstream file("./test.xml"); std::istream_iterator eos; std::istream_iterator iit (file); std::vector xml(iit, eos); xml.push_back('\0'); xml_document<> doc; doc.parse (&xml[0]); the "/" symbol in the IMG rag seems t be the problem. Is this a rapidxml bug or am I doing something wrong?
