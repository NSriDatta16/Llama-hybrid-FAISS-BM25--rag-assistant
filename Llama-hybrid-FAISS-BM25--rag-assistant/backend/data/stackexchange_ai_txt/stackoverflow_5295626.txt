[site]: stackoverflow
[post_id]: 5295626
[parent_id]: 
[tags]: 
Hibernate, how to count with condition

I am using oracle and hibernate for mapping. I want to count with a condition in count() function. my code is: select count(case when st.averageMark the code return me nothing. I used sum instead of count it returned a result but it is wrong, the result is bigger than I suppose it to be. thank in advance.
