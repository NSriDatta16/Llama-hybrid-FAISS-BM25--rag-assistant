[site]: crossvalidated
[post_id]: 307468
[parent_id]: 270206
[tags]: 
You can solve this problem using numerical methods by applying MCMC sampler. Or if you can design your experiments, set $X$ to zero, record data sample, then estimate $\mu$ using LS; set $X$ to 1, record other data sample, then $Var(Y) = \sqrt{1+\beta}$ .
