[site]: stackoverflow
[post_id]: 3742324
[parent_id]: 
[tags]: 
Strange Behevior when trying to create a Template containing maps

I am new to Templates and I tried to implement a template class which uses a map (which is a member in private). The map can only do 3 things: insert, swap, and operator =. And I don't get the full functionality of map.. Here's the code: #include using namespace std; template class GarageDataBase { public : GarageDataBase(); virtual ~GarageDataBase(); const T& Top() const; bool Add(T data,SortKey key2, SearchKey key2); T Remove(SearchKey toRemove); T Find(SearchKey toFind) const; bool isEmpty()const; private: multimap firstMap; multimap *> secondMap; }; template GarageDataBase ::GarageDataBase() { } template GarageDataBase ::~GarageDataBase() { } template const T& GarageDataBase ::Top() const { firstMap. } In the last function, when trying to get into firstMap methods, all I get is: insert, swap or, operator = . How do I get to "first" or "second" in the map?
