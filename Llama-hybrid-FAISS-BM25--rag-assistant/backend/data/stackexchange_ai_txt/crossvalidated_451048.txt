[site]: crossvalidated
[post_id]: 451048
[parent_id]: 
[tags]: 
Reinforcement Learning, why using data from behaviour policy can be used to optimise the target policy?

In off-policy RL, data generated by behaviour policy is used to optimise the target policy. However, the data generated by the behaviour policy can be good or even very bad. Why using off-policy (based on importance sampling) can guarantee the optimization of the target policy?
