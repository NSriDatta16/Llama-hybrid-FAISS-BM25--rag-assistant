[site]: crossvalidated
[post_id]: 186649
[parent_id]: 
[tags]: 
Can I use Convolution Neural Network (CNN) as a two class classifier?

I have two class data sets . I want to classify them using Convolution Neural Network . So Is it possible? Is there any build in functions available. I don't have any idea about CNN so any kind of help is welcome.
