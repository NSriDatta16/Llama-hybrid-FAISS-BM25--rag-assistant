[site]: stackoverflow
[post_id]: 4042003
[parent_id]: 4041777
[tags]: 
I have solved this three ways. Trim the unwanted stuff after the loop. Use a "started" bool, e.g., // Warning -- Air code fragment bool started = false; foreach (...) { if (!started) started = true; else sb.Append(","); // Rest of the loop } Created a wrapper for StringBuilder that does the "started" trick.
