[site]: stackoverflow
[post_id]: 4443122
[parent_id]: 
[tags]: 
How I can read a paragraph from the keyboard and stored it in a linked list (each node has a one word)?

#include #include #include using namespace std; class node { private: string data; node *next; public: node*head,*current,*tail; node() { head=current=tail=NULL; } void read() { string x; tail=new node; cin>>tail->data; tail->next=NULL; head=current=tail; cin>>x; while(x!=".") { current=new node; current->next=NULL; stringstream ss;//("plez help me"); string s; //getline(cin,aa); while (getline(ss, s,' ')) { cout data=s; tail->next=current; tail=tail->next; //cin>>x; } } void print() { current=head; while(current!=NULL) { cout data next; } } };
