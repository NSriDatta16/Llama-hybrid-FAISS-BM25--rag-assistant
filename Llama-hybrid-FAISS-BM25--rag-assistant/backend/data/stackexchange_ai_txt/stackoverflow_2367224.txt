[site]: stackoverflow
[post_id]: 2367224
[parent_id]: 2222302
[tags]: 
Tough call there! The 5.x OS is being made availably only for a limited set of legacy devices. The choice of which model gets the upgrade and which doesn't seems quite random. Further, only a small percentage of (perhaps I would keep SQLite for version 2.0 of your app. If there is a way, abstract out the storage layer so that you can bring in SQLite relatively easily. Again, tough call because perhaps the application you want to develop absolutely requires a more decent storage mechanism than the data structures available in JDE.
