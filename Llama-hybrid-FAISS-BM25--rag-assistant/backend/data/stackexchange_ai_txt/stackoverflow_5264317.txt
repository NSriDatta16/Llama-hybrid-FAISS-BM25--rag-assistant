[site]: stackoverflow
[post_id]: 5264317
[parent_id]: 
[tags]: 
Trying to run one command in "Execute Shell" and a python script in Jenkins (formerly hudson) under "build"

These run on the command line in linux (ubuntu) but when trying to run them via hudson, only the first one (first one) shows up in the console Output From comment: nosetests -s --with-coverage --cover-package prus find . -name "*test.py" pylint --rcfile scripts/pylintrc -f parseable prus > pylint.txt echo "pylint complete" sloccount --wide --details prus > sloccount.sc
