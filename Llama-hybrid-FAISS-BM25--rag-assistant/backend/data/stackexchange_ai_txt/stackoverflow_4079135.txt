[site]: stackoverflow
[post_id]: 4079135
[parent_id]: 
[tags]: 
Splitting a string with uppercase

Possible Duplicates: Split a PascalCase string into separate words is there a elegant way to parse a word and add spaces before capital letters Is there a simple way to split this string "TopLeft" to "Top" and "Left"
