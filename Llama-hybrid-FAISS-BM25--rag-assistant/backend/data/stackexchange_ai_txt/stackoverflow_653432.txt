[site]: stackoverflow
[post_id]: 653432
[parent_id]: 653420
[tags]: 
Split the string , then you'll receive an array containing the values. string[] firstnames = Request.Form["firstname"].Split(","); string[] lastnames = Request.Form["lastname"].Split(","); Although I must admit that I find this a bit an ugly solution... It's fragile imho.
