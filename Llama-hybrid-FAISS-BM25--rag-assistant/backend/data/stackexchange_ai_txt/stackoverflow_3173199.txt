[site]: stackoverflow
[post_id]: 3173199
[parent_id]: 3173158
[tags]: 
iframe s are for embedding other HTML pages within a page. object s are for embedding non-HTML content objects (like Flash) within a page. Since YouTube is using Flash to play videos, it uses object .
