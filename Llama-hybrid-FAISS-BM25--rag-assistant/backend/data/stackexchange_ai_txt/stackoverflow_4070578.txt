[site]: stackoverflow
[post_id]: 4070578
[parent_id]: 4070423
[tags]: 
You can get the path using Environment.getExternalStorageDirectory() and then open a file using that path. You may have to have the following permission set: uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
