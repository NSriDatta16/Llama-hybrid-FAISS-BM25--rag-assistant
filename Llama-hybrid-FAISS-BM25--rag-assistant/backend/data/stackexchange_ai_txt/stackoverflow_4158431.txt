[site]: stackoverflow
[post_id]: 4158431
[parent_id]: 
[tags]: 
Php sql script issue

Please take a look at this code.... what is i have instead of average, above average , a two letter word? >Petite >Athletic >Average >Above average $selectedCategories = array(); foreach(array('petite', 'average', 'athletic') as $category) { if (get_option($category) == $category) { $selectedCategories[] = $category; } } $qry = mysql_query( "SELECT performerid,pic0 FROM ".$table." WHERE build IN('" . implode("', '", $selectedCategories) . "') ORDER BY RAND() LIMIT 20;");
