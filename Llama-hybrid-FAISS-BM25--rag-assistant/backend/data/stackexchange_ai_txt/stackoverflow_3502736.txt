[site]: stackoverflow
[post_id]: 3502736
[parent_id]: 3502612
[tags]: 
There is no way to specify a storage engine for a certain database, only for single tables, as others have pointed out. You can, however, specify a storage engine to be used during one session with SET storage_engine=InnoDB; so you don't have to specify it for each table.
