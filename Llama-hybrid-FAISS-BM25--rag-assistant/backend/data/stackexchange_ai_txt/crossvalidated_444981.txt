[site]: crossvalidated
[post_id]: 444981
[parent_id]: 
[tags]: 
the difference between GLM and fisher's exact test

I want to compare the enrichment or overlapping between 2 lists. I found some paper use fisher's exact test, or other papers use GLM (logistic regression). Does anyone know what the difference between two methods aforementioned is?
