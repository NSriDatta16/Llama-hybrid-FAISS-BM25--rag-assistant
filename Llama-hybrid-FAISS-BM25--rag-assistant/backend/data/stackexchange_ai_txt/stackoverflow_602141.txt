[site]: stackoverflow
[post_id]: 602141
[parent_id]: 602074
[tags]: 
A TIMESTAMP will take slightly less storage space (4 bytes) than a DATETIME field. Performance-wise they should be roughly the same. If you're thinking about doing GROUP BY DAYOFYEAR(...) then you might be better storing the DAYOFYEAR value in its own column to get better performance.
