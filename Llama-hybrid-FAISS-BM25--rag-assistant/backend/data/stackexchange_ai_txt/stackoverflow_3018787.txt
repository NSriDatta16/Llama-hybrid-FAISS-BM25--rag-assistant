[site]: stackoverflow
[post_id]: 3018787
[parent_id]: 3018739
[tags]: 
As long as it makes sense for all derived classes to have the member it makes good sense to put it in the Base class. Reasons I see for not putting it there are: If your interface (the abstract Base class) is frozen, then you would be forced to use that storage or break compatibility for your users. To avoid forcing your users to #include and .
