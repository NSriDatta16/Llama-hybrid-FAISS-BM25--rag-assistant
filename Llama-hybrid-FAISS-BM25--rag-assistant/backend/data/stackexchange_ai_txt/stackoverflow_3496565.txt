[site]: stackoverflow
[post_id]: 3496565
[parent_id]: 3496523
[tags]: 
NO. Yes. Mysql is a little more intelligent storage system than a filesystem. It has it's own privileges system, far more flexible than just r-w-rw. There is no sense in controlling it from the client side. One who can execute a query, can change access level as well.
