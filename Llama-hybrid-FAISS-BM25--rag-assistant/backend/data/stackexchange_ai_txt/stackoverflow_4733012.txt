[site]: stackoverflow
[post_id]: 4733012
[parent_id]: 4732972
[tags]: 
not that elegantly, no. But you can run the query then compare the result: $case = mysql_real_escape_string($case); // make sure to escape values going in queries if (($r = mysql_query("SELECT * FROM cases WHERE passenger=4 AND case='{$case}'")) !== false) { if (mysql_num_rows($r)) { // your code here. } }
