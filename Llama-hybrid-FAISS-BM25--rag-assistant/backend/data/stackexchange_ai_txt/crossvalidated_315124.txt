[site]: crossvalidated
[post_id]: 315124
[parent_id]: 
[tags]: 
Which is the Recommended variants of neural network for tree like data structures?

Consider I have a training set where each data sample is a tree and each node has(including leaf node) its own feature vector. For example, single data sample will look this Now I have to classify leaf nodes to either of two categories(category_1 or category_2). Can some one please recommend feasible neural network variant for this kind of data.
