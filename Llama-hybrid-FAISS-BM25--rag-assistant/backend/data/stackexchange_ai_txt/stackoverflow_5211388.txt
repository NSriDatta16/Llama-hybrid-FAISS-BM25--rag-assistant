[site]: stackoverflow
[post_id]: 5211388
[parent_id]: 
[tags]: 
jQuery error in IE: Unexpected call to method or property

i got a message same as this here is my page No problem with chrome I suspect the following code which may be causing the problem function update_cost(){ var cost = $( "#amount_CPU" ).val() * CPU_per_unit + $( "#amount_RAM" ).val() * RAM_per_unit + $( "#amount_storage" ).val() * storage_per_unit + $( "#amount_traffic" ).val() * traffic_per_unit; $( "#estimated_cost" ).text( "$" + cost ); }
