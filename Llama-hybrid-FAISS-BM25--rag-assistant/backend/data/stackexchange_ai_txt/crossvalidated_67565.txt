[site]: crossvalidated
[post_id]: 67565
[parent_id]: 
[tags]: 
Should weights of neural network without hidden layer match logistic regression?

Should the weights of a neural network without hidden layer and a logistic activation function be the same as the parameters of a logistic regression? Mine are not the same? nnallnohidden=nnet( PartialPrepayzo~FIXPER+MEDSAL2+DREL+LEEFTIJD+HH2CRED+LTV_curr+ rate1Y+rate5Y+CIremFIRP+URB+WELSTAN2+OutNot+mover+SavRate+CRate, data=test, size=0, skip=T) log
