[site]: stackoverflow
[post_id]: 5354395
[parent_id]: 5324510
[tags]: 
i found the solution: if (Enumerable.Any(ctx.Organizations.AsEnumerable(). Where(s => s.CountryId == Organization.CountryId && s.ProvinceId == Organization.ProvinceId && s.CityId == Organization.CityId && s.Name == Organization.Name))) befor using where condition i used AsEnumerable() method.
