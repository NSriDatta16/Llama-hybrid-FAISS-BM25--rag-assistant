[site]: stackoverflow
[post_id]: 2894823
[parent_id]: 2565142
[tags]: 
The best way is not to use multipart, but to embed the file's binary content in your resource's XML, which then goes in the request body. This is easy to do using ActiveResource I've created a plugin to do that in Rails 3: http://github.com/nragaz/encoded_attachment
