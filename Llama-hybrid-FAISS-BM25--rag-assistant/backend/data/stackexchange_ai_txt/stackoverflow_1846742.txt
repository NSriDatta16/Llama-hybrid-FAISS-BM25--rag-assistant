[site]: stackoverflow
[post_id]: 1846742
[parent_id]: 1846679
[tags]: 
There is the inverse to everyones answer in that you can call a function that doesnt appear to have parameters in the signature with parameters. You can then access them using the built in arguments global. This is an array that you can get the details out of it. e.g. function calcAverage() { var sum = 0 for(var i=0; i
