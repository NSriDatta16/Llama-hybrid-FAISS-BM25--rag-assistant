[site]: stackoverflow
[post_id]: 1920423
[parent_id]: 1920325
[tags]: 
require LWP::UserAgent; my $ua = LWP::UserAgent->new; my $response = $ua->get('http://search.cpan.org/'); if ($response->is_success) { print $response->decoded_content; # or whatever } else { die $response->status_line; } response->decoded_content will return the body of the response.
