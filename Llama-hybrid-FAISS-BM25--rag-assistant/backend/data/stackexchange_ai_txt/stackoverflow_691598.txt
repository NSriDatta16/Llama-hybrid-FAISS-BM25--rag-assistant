[site]: stackoverflow
[post_id]: 691598
[parent_id]: 690295
[tags]: 
Use View > Source. Is the coming from the https server as well, or is it coming from http ? IE8 may not be loading the script because it isn't coming from the same secure source the rest of the page is coming from. Take the (if it does not include the protocol and server, use the same one the page is coming from) and paste it into the address bar of a new tab, does the script load/download? Is the tag that loads the appropriate library even listed in the source? Maybe it isn't being added because ASP.NET doesn't recognize the User Agent and doesn't think it is capable or something.
