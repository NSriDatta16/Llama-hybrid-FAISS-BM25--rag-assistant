[site]: crossvalidated
[post_id]: 183371
[parent_id]: 183364
[tags]: 
Yes that is correct, in that case the input is mapped through the output via a single weight matrix (10 x 10) and a bias of (10 x1). If you choose your activation function as a sigmoid function then the Network that you are describing is equivalent to logistic regression.
