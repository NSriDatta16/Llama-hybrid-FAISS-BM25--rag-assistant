[site]: stackoverflow
[post_id]: 5534296
[parent_id]: 5533092
[tags]: 
I can't figure out why sleeping has solved anything. But I think python in sending 5 bytes and C++ is reading 8 bytes. ? in python takes on byte. BOOL I believe is typedefed as an int and takes probably 4 bytes. In general reading/writing structs to sockets is discouraged.
