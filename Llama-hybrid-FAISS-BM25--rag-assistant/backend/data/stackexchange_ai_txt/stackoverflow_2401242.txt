[site]: stackoverflow
[post_id]: 2401242
[parent_id]: 2401217
[tags]: 
The real question should be: Is it reasonable to assume that MySQL's usage would have died out a long time ago if it couldn't work with an integer index on 500,000 rows of data? Providing there isn't any RAM or CPU or storage engine constraints it should work fine.
