[site]: stackoverflow
[post_id]: 3507399
[parent_id]: 3507321
[tags]: 
A loaded collection doesn't know anything about storage (which handles auto increment values). If you want to know the highest id in a loaded collection you can use an aggregate linq query and select max id. Or you can use Linq-To-Sql to run a stored procedure and retrieve SCOPE_IDENTITY.
