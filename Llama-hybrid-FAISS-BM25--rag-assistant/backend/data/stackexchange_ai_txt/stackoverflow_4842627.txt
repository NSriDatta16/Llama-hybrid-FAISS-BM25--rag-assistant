[site]: stackoverflow
[post_id]: 4842627
[parent_id]: 4840732
[tags]: 
If the app. is deployed using Java Web Start , a file association can be declared. Double click any file of that type and the path of the file is supplied to the main(String[]). BTW - here is a demo. of the JNLP file API . Note well the 2nd bug report, which would mean a minimum version of 1.6.0_10+ in order not to delete a file dropped on the application's icon.
