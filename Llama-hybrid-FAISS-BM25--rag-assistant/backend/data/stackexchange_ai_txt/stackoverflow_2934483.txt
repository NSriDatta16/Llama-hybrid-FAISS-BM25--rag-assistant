[site]: stackoverflow
[post_id]: 2934483
[parent_id]: 130734
[tags]: 
allml (now Ragtag ) and Omni-completion ( ) doesn't work in a file like .py or .java. if you want to close tag automatically in those file, you can map like this. imap F $a ( ^R is Contrl+R : you can type like this Control+v and then Control+r ) (| is cursor position ) now if you type.. abcde| and type ^j then it close the tag like this.. abcde |
