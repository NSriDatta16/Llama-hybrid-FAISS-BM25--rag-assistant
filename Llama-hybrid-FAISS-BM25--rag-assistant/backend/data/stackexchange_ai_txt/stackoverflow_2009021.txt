[site]: stackoverflow
[post_id]: 2009021
[parent_id]: 2001978
[tags]: 
Workaround for above problem: var scopeProvider = new Microsoft.Scripting.ScopeStorage(); scopeProvider.SetValue("B", true, 2); var scope = engine.CreateScope(scopeProvider); This gets rid of the performance overhead with the previous code.
