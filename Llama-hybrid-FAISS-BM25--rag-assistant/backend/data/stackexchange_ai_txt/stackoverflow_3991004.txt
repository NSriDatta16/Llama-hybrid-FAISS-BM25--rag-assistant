[site]: stackoverflow
[post_id]: 3991004
[parent_id]: 
[tags]: 
Access of file on desktop using C++

I am using Windows 7 and I have created on the desktop a file named test.txt . How can I access this file using C++? For example, consider following code: #include #include #include using namespace std; int main(int argc, char *argv[]) { fstream inout("test.txt", ios::in | ios::out | ios::binary); if(!inout) { cout In the fragment fstream inout("test.txt", ios::in | ios::out | ios::binary) , what should I change to access my test.txt on desktop?
