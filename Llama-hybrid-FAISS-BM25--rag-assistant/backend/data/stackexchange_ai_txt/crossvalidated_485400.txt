[site]: crossvalidated
[post_id]: 485400
[parent_id]: 
[tags]: 
What is the loss function for regression using artificial neural network?

For classification, one of the most common loss functions for artificial neural networks (ANN) is cross-entropy. What about in ANN for regression? and why is cross-entropy hardly even discussed in ANN's encyclopedia entries ?
