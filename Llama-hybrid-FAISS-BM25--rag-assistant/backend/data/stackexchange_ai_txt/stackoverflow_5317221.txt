[site]: stackoverflow
[post_id]: 5317221
[parent_id]: 5317134
[tags]: 
@adamjford is correct, it now returns a MvcHtmlString instead of a string which is your proplem. But I'd say that you're trying to do something that the HtmlHelpers weren't really designed to do. Which is cool because they're only there as helpers and writing raw html is fine and encouraged for situations like this. I'd suggest you try something like this: ">
