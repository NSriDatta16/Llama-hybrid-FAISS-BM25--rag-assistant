[site]: datascience
[post_id]: 103116
[parent_id]: 103109
[tags]: 
import spacy nlp = spacy.load('en_core_web_trf') def identify_futuristic(sentence): sentence_doc = nlp(sentence) if any((token.morph.get('Tense') == [] and token.morph.get('VerbForm') == ['Fin'] and token.morph.get('Mood') == []) or (token.morph.get('Tense') == ['Pres'] and token.morph.get('VerbForm') == ['Fin'] and token.morph.get('Mood') != ['Ind']) for token in sentence_doc): return 'F' else: return 'NF' sentences_dataframe["F/NF"] = sentences_dataframe['Sentences'].apply( lambda s: identify_futuristic(s))
