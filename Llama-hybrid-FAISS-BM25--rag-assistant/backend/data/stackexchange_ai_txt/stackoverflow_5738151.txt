[site]: stackoverflow
[post_id]: 5738151
[parent_id]: 5715096
[tags]: 
To force disconnection you may kill the process. Not elegant but there's no way of closing the connection from monkeyrunner. ... pid = int(filter(lambda p: len(p) == 9 and p[8] == 'com.android.commands.monkey', map(lambda l: l.split(), device.shell('ps').splitlines()))[0][1]) print "killing %s" % pid device.shell("kill %d" % pid) device = None ...
