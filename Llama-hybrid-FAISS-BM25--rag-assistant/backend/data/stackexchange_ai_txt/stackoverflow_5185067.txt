[site]: stackoverflow
[post_id]: 5185067
[parent_id]: 
[tags]: 
JQuery Select 1st, 2nd & Third Link in LI

Hi Have a menu which look like this beauty fragrence at home I would like to use jquery to change the CSS background image of the main div based on the hover of one of the links. for example when a user hovers over the Beauty link then change the css bg-imge to x and if the user hovers over fragrance then chaneg the css bg-image to y. Any help would be great Thanks
