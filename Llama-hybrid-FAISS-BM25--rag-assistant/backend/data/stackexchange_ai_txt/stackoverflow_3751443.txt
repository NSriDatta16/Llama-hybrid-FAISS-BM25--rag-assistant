[site]: stackoverflow
[post_id]: 3751443
[parent_id]: 3751289
[tags]: 
As @frag says, main() knows nothing about the type of f(s) except that it is a pointer to int . It doesn't have any reason to look for 19 other ints. Your main function should look more like this: int main(){ size_t s=20; int *p = f(s); for (size_t i=0; i
