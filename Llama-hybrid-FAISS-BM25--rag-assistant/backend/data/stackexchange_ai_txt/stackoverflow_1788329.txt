[site]: stackoverflow
[post_id]: 1788329
[parent_id]: 1788011
[tags]: 
If you want to generate the query for each call or use a hardcoded max-column-count, then you can do something like that: WITH tab AS ( SELECT table_name, column_name FROM user_tab_cols WHERE column_id If it is sufficient to get it in that form TABLENAME1|COL1,COL2 TABLENAME2|COL1,COL2,COL3 have a look at Tom Kyte's stragg .
