[site]: crossvalidated
[post_id]: 521063
[parent_id]: 353245
[tags]: 
I'd use Cohen's Kappa, treating each category as an independent question (the fact that it's multi-class multi-label makes me think you have to compute agreement for each). Then when aggregating it, I'd probably use a linear average across all categories.
