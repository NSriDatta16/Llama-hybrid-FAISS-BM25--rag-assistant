[site]: crossvalidated
[post_id]: 188455
[parent_id]: 186933
[tags]: 
1) Using SMOTE method to oversample would make a better classification on future prediction even the training data set doesn't consist of balanced result. 2) The class.weight argument in svm() doesn't perform well, one could use SMOTE or other method to create a balanced data instead of using weight argument. Alternatively, using a tree rpart might be a option where it can specify the loss matrix.
