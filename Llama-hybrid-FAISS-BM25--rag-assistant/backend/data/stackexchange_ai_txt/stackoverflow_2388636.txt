[site]: stackoverflow
[post_id]: 2388636
[parent_id]: 2385510
[tags]: 
So you want your entire output XML on 1 line? If so this might to the trick: String separator = System.getProperty("line.separator"); System.setProperty("line.separator", ""); transformer.transform(source, result); // Remember to re-set it to it's original value! System.setProperty("line.separator", separator);
