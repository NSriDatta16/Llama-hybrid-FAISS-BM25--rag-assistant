[site]: stackoverflow
[post_id]: 5731010
[parent_id]: 5730968
[tags]: 
You add matricies according to this rule: A[i,j] + B[i, j] = C[i,j] The two matricies being added must have the same number of rows and columns. How you do it for your storage scheme depends on how you map rows and columns. But the rules are unchanged, because that's how matricies work.
