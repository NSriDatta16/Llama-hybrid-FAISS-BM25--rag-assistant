[site]: crossvalidated
[post_id]: 440170
[parent_id]: 440096
[tags]: 
It is capable of catching them if they are statistics of the other variables, but you should still add them if you think those statistics are relevant IRL. The neural network might not choose to use them and go for overfitting other features instead.
