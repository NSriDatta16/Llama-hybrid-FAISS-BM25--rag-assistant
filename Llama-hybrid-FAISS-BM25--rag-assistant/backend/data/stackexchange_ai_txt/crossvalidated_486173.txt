[site]: crossvalidated
[post_id]: 486173
[parent_id]: 
[tags]: 
Logistic Regression, continuous feature is categorical

I'm using logistic regression for a simple binary classification. I have a feature, x, and looks something like this if x = 20, then it tends to be class A if 10 is the standard procedure to one hot encode this feature?
