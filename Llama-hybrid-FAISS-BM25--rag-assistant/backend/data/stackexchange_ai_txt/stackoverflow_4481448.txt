[site]: stackoverflow
[post_id]: 4481448
[parent_id]: 4481394
[tags]: 
Applets have the following methods : init(), start(),stop(),destroy() An applet can live inside the browser and outside the browser with the draggable feature . An application is executed outside the browser.
