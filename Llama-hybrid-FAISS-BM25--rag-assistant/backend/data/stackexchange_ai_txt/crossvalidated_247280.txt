[site]: crossvalidated
[post_id]: 247280
[parent_id]: 
[tags]: 
Constant in Ordinary Least Squares

Suppose, I estimate the model $y_i = \beta_0 \,\mathrm{cons}_i$ with simple OLS, where $\mathrm{cons}_i$ is a variable that takes the value $1$ for each $i$ (classical constant). Why is the estimate $\widehat{\beta}_0$ the sample average of $y_i$?
