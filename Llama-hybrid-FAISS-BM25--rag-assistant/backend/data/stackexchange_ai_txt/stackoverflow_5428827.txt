[site]: stackoverflow
[post_id]: 5428827
[parent_id]: 5428702
[tags]: 
In pure C, you can use pointer to store data of different types , and pay attention to avoid memory leaking. like this: struct NODE { void *data; struct NODE *next; }; How about considering using C++ template? template struct NODE { T data; struct *next; };
