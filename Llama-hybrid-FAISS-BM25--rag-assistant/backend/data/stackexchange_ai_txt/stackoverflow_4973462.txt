[site]: stackoverflow
[post_id]: 4973462
[parent_id]: 
[tags]: 
R: Best way to convert a mts to a non-time series dataframe with time indexes

I'm currently using the following method to convert a mts dataset to a data frame with time indexes as columns. Is there a more elegant way to do this? z
