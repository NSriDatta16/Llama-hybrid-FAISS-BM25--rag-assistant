[site]: stackoverflow
[post_id]: 2437158
[parent_id]: 
[tags]: 
What's the best way to move "a child up" in a .NET XmlDocument?

Given an XML structure like this: Firebird Altima Prius I want to "move" the Prius node "one level up" so it appears above the Altima node. Here's the final structure I want: Firebird Prius Altima So given the C# code: XmlNode priusNode = GetReferenceToPriusNode() What's the best way to cause the priusNode to "move up" one place in the garage's child list?
