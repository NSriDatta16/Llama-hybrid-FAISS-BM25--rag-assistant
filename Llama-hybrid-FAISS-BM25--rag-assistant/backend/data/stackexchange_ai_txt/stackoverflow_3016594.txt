[site]: stackoverflow
[post_id]: 3016594
[parent_id]: 3014525
[tags]: 
The reason is really quite simple. It's because the browser isn't rendering your error, but if you right click -> View Source, you can see the error in your HTML source code. ">paragraph would output paragraph or something similar. The browser simply thinks that those words are a collection of CSS classes, so you don't see anything on your screen. It's contained within an HTML attribute.
