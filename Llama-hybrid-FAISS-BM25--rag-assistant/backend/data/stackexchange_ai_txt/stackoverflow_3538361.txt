[site]: stackoverflow
[post_id]: 3538361
[parent_id]: 3538345
[tags]: 
I haven't tried it but this should work: function New_Text() { $(" Drag me ").appendTo("div#drag_border"); setTimeout(function() { ... }); }, 10); } You want to create the div, let the browser add it to the DOM tree, then call the draggable function.
