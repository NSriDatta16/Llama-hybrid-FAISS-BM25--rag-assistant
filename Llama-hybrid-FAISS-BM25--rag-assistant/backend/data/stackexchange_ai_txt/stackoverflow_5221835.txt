[site]: stackoverflow
[post_id]: 5221835
[parent_id]: 5219757
[tags]: 
VS2010 does support SQLite (I'm using it now). If you have the Full Version you can use the GUI otherwise you need to set the database up manually with SQL. I have just started to use the CoolStorage ORM with SQLite, it is great to work with. You may require the following code in your app.config file
