[site]: crossvalidated
[post_id]: 49836
[parent_id]: 49833
[tags]: 
ScaleData is probably needed as this supplies normalizing info for unseen data. Support vector indices is needed to reference the data inside the SupportVectors variable. Alpha is also needed as it supplies the weights for the support vectors. Shawe-Taylor and Cristianini's book provides some background on the equations for an SVM.
