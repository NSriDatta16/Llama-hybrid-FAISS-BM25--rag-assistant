[site]: stackoverflow
[post_id]: 1699372
[parent_id]: 1698453
[tags]: 
Here is the documentation for l It dosn't look like it will by default use the current page when no path is defined. So you should call it like this: l('link', 'currentpage', array('fragment' => 'namedanchor'));
