[site]: stackoverflow
[post_id]: 4396899
[parent_id]: 
[tags]: 
asp.net mvc converting list<> so to loop

I am using this List JobsMeta= _db.JobsMetas.Where(m => m.int_OrganizationId == null || m.int_OrganizationId == OrganizationInfo.OrganizationId).OrderBy(m => m.int_SortId).ToList(); ViewData["JobMeta"] = JobsMeta; now using the view i want to loop through ViewData["JobMeta"]
