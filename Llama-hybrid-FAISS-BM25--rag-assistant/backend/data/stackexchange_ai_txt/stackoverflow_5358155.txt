[site]: stackoverflow
[post_id]: 5358155
[parent_id]: 5358066
[tags]: 
If you use jQueryUI it would look something like this: var count = 1 $("#copy_button").click(function() { copy = $("#original_rectangle").clone() copy.removeAttr("id").addClass("dragMe ui-widget-content").html("copy " + count).appendTo("body") count += 1 }) $("dragMe").draggable();
