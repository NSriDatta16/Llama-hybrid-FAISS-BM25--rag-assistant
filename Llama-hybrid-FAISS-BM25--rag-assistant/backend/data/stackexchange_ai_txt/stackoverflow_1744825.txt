[site]: stackoverflow
[post_id]: 1744825
[parent_id]: 1744802
[tags]: 
Do this: Run .Skip(1000).Take(100) on a LINQ to SQL datacontext and look at the SQL output. It will generate a SQL statement for you that does what you're describing. It won't be as elegant but it gets the job done.
