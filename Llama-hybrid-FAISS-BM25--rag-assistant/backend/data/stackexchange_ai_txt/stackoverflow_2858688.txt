[site]: stackoverflow
[post_id]: 2858688
[parent_id]: 2858443
[tags]: 
If you need raw performance (=>no recursion) and length of the combination is known only at runtime, there's this code of mine that you can adapt. Otherwise, there are more elegant solutions like the ones linked by KennyTM in his comment.
