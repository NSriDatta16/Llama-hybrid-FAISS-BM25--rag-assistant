[site]: stackoverflow
[post_id]: 5257935
[parent_id]: 
[tags]: 
NSArray life cycle

I create an NSArray object in .h file: NSArray *tempArray; and than I alloc it in .m file in viewDidLoad() method: tempArray = [[NSArray alloc] init]; //initilaize tempArray = [connect connectSeriesJSonBack]; //fill it (I try it works) But I want to use this array in another method like: - (void)scrollViewDidEndDecelerating:(UIScrollView *)_scrollView How can i organize array lifecycle because I use alloc, retain etc. no way the reach array data again.
