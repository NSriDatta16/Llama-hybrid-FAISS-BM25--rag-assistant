[site]: stackoverflow
[post_id]: 3805690
[parent_id]: 3805611
[tags]: 
Consider that happens in CreateInfoObject . Lets say there is another subclass of AbstractInfo , call Foo . Inside CreateInfoObject we create a new Foo and assign it to *info . (Allowed upcast). But outside we now have Foo inside a MyInfoObject** which is wrong.
