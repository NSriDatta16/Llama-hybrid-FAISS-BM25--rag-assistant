[site]: stackoverflow
[post_id]: 1692282
[parent_id]: 1692273
[tags]: 
I can see 3 possibilities make the browser load resources more faster. the browser would open a fixed number of connection to same domain not to load the server they are using more then one server so they can share the load between servers separate some content to a separate virtual host or server. some kind of organization ...
