[site]: stackoverflow
[post_id]: 1334418
[parent_id]: 1334394
[tags]: 
You might need to concatenate that with System.AppDomain.CurrentDomain.BaseDirectory. I'd guess that FromFile is relative to the current working directory which is prone to change. The other thing to consider would be embedding the images in the assembly
