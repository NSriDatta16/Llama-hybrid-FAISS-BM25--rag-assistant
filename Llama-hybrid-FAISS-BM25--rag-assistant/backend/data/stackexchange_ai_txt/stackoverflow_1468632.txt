[site]: stackoverflow
[post_id]: 1468632
[parent_id]: 1468596
[tags]: 
On Windows, you can just do this: DWORD dwTickCount = GetTickCount(); // Perform some things. printf("Code took: %dms\n", GetTickCount() - dwTickCount); Not the most general/elegant solution, but nice and quick when you need it.
