[site]: stackoverflow
[post_id]: 4947077
[parent_id]: 4946873
[tags]: 
Here's a way that gets you what you want, but there may very well be a more elegant solution. rbind(x, data.frame(Language = "Total", t(colSums(x[, -1])))) For the record, I prefer Chase's answer if you don't absolutely need the Language column.
