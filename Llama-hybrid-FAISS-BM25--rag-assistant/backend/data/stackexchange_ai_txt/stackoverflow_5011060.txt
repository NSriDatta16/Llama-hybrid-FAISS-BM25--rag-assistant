[site]: stackoverflow
[post_id]: 5011060
[parent_id]: 2445010
[tags]: 
I think @Anurag was on the right path. Check out this working version... http://jsfiddle.net/Y23ec/ Same idea just a different way of doing it. jQuery(document).ready(function () { $.validator.addMethod('validate-checkbox-oneormore', function (value) { return $('.require-one:checked').size() != 0; }, 'need a value'); jQuery('#itemForm').validate({}); });
