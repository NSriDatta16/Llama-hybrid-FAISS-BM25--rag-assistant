[site]: stackoverflow
[post_id]: 2720251
[parent_id]: 1093992
[tags]: 
You could try: // Get the dropIndex from the dragEvent var index:int = this.calculateDropIndex(event); // Get the itemRenderer from the index var renderer:IListItemRenderer = this.indexToItemRenderer(index); // Get your item from the data property of the itemRenderer var item:Object = renderer.data;
