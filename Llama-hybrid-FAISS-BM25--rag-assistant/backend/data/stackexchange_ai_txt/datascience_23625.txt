[site]: datascience
[post_id]: 23625
[parent_id]: 23623
[tags]: 
Just a proposal of a method to try out. Stage $1$: Use one-class SVM to assign those images that do not belong to the set of predefined classes as the $9$-th class. Stage $2$: For those images that passes through your filter, let the multi-class SVM assign them to one of the $8$ classes.
