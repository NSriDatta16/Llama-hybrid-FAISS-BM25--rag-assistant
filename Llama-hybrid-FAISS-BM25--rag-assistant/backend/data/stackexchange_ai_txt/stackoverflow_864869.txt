[site]: stackoverflow
[post_id]: 864869
[parent_id]: 864852
[tags]: 
If you're referring to embedding your JavaScript in a URL rather than attaching it as an event handler, you could do something like this: Using the void() function ensures that nothing is returned so that the browser does not navigate away from the current page.
