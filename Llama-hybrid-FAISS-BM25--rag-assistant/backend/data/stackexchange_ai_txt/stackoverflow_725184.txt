[site]: stackoverflow
[post_id]: 725184
[parent_id]: 725171
[tags]: 
Are there instances where one ViewModel services two Views Skinned applications may leverage this ability. The Model is pretty dumb but the model itself doesn't handle validation The model can be as smart as you like. And it can include "validation" to ensure integrity, but that validation won't include messages that are surfaced in the UI.
