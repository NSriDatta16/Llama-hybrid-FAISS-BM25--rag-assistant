[site]: stackoverflow
[post_id]: 4661772
[parent_id]: 4661650
[tags]: 
The most elegant, flexible and safe way to do this would be to download msxsl.exe and then use a tiny XSLT stylesheet to modify only the value you want in the XML: Call that on the command line with: msxsl.exe input.xml DaysToRetainRecordedData.xsl –o output.xml newValue=0 The command line parameter newValue will show up in the XSL program.
