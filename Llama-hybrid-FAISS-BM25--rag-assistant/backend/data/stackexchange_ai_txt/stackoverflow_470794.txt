[site]: stackoverflow
[post_id]: 470794
[parent_id]: 470495
[tags]: 
So I made a little change to get it working. Not sure if this is the most elegant solution. $(document).ready(function() { $("input[name='ticketStatus']").bind("click", radioClicks) }); function radioClicks() { window.location = "/Tickets/" + $(this).val(); } Thanks.
