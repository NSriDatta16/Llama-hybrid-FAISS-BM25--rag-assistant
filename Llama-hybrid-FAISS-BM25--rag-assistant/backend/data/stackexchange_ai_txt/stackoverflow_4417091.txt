[site]: stackoverflow
[post_id]: 4417091
[parent_id]: 
[tags]: 
access bitmaps from speficied folder in android

I have successfully stored bitmap in sdcard/Pictures folder in android 2.X , using following code File path = Environment.getExternalStoragePublicDirectory( Environment.DIRECTORY_PICTURES); File file = new File(path, "pic"+index+".jpg"); now, i want to retrieve all the images stored in that folder with out specifying name of the file.
