[site]: stackoverflow
[post_id]: 2504345
[parent_id]: 2497279
[tags]: 
not sure but a have a feeling this is what you need. start: function(event, ui) { var docID = ui.find('.fax16'); //somehow access the dragged element //ui.helper = $(" " + docID + " "); //Set drag Helper }
