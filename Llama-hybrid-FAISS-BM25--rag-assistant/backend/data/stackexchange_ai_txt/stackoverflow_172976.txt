[site]: stackoverflow
[post_id]: 172976
[parent_id]: 172946
[tags]: 
This is pretty easy to do in WinForms. Check out basic drag and drop . It's targeted toward 2.0. You'll use the DoDragDrop() method and capture data in drag and drop events. It requires some verbosity, but it gets the job done. If you're keen on using WPF, take a look at MSDN's topic .
