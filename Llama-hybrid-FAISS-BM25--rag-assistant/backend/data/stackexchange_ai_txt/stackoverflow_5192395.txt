[site]: stackoverflow
[post_id]: 5192395
[parent_id]: 
[tags]: 
Nokogiri extract data from xml

i try to extract data from a xml in rails application with the Nokogiri gem, the xml : text, bla bla... ]]> Actually i do something like this to extract data from the xml : def test_content @return = Array.new site = 'http://www.les-encens.com/modules/feeder/rss.php?id_category=0' @doc = Nokogiri::XML(open(site, "UserAgent" => "Ruby-OpenURI")) @doc.xpath("//item").each do |n| @return Could you show me how extract just the src attribute from the img tag ? Edit : I have replace the xml by the correct one.
