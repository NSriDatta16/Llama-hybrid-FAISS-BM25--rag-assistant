[site]: stackoverflow
[post_id]: 4807847
[parent_id]: 
[tags]: 
"integer constant overflow" with GLSL (OpenGL ES2)

I need to pass texture data to a shader program without using OpenGL textures because I'm not using a power of two texture but I get the error with this fragment shader. varying highp vec2 texcoord; uniform ivec4 texdata[172800]; void main(){ int pixel = int(360.0 * texcoord.y + texcoord.x); gl_FragColor = vec4(texdata[pixel].x,texdata[pixel].y,texdata[pixel].z,1); } How can I pass this data?
