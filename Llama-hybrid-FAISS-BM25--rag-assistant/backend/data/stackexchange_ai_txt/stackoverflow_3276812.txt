[site]: stackoverflow
[post_id]: 3276812
[parent_id]: 
[tags]: 
Broadcast a UDP message in LAN with fake source IP with delphi

I want to broadcast a UDP message in my LAN with fake source IP ( spoofing ) 1. Do winpcap able to do this ? 2. Do this work on winxp, win7 ? 3. i'm using delphi, is there any good components available ? Thanks.
