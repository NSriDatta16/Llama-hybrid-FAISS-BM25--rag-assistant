[site]: stackoverflow
[post_id]: 3815438
[parent_id]: 
[tags]: 
Remove XML Nodes as per user input

I am trying to write all the dropped elements prperties in a xml document to save in the database.When the user delete a dropped element ,i need to remove that element from the Xml doc. XmlDocument oXmlDocument = new XmlDocument(); oXmlDocument.Load(@"D:\VanithaApps\SenMail\DiagramData.xml"); Boolean nodeExits = false; XmlNode oXmlRootNode = oXmlDocument.SelectSingleNode("records"); XmlNodeList xmlnode = oXmlDocument.GetElementsByTagName("record"); if (delete=="1") { if (xmlnode.Count > 0) { for (int i = 0; i 0) { for (int i = 0; i I haven't used xml extensively before 774 173 drag595 1 i want to delete whose child 'Element' value is equal to tempVar.Here if the Element value is equal to drag595 ,i want to remove that entry from my XML .
