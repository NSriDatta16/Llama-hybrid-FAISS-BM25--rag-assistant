[site]: stackoverflow
[post_id]: 542740
[parent_id]: 542564
[tags]: 
This seems to do what you want: final String srcOne = " \r\n \r\n \r\n "; final String srcTwo = " \r\n \r\n \r\n "; final String tag = " \r\n \r\n \r\nfunction() {\r\n\talert('hi');\r\n}\r\n \r\n "; final String tagAndSrc = " \r\n \r\n \r\nfunction() {\r\n\talert('hi');\r\n}\r\n \r\n "; final String[] tests = new String[] {srcOne, srcTwo, tag, tagAndSrc, srcOne + srcTwo, tag + srcOne + tagAndSrc}; final String regex = " ]*src=['\"]([^'\"]*)['\"][^>]*>|[^>]*>([^ )"; final Pattern pattern = Pattern.compile(regex, Pattern.CASE_INSENSITIVE | Pattern.DOTALL); for (int testNumber = 0; testNumber That being said, you would probably be better off using something like Tag Soup if it is at all possible.
