[site]: stackoverflow
[post_id]: 3262644
[parent_id]: 3262627
[tags]: 
To simplify matter. You can always return a T[] , Pair , Set , etc in these languages to emulate returning multiple values. That takes care of most multiple return values scenarios out there. It's not worth changing the semantics of the language so much for a gain so little. Related questions Using a java method to return multiple values? Returning multiple values from a C++ function Returning multiple values from a C function Method should return multiple values Elegant ways to return multiple values from a function
