[site]: stackoverflow
[post_id]: 2681496
[parent_id]: 2681467
[tags]: 
When you append, bind drag event. It should look like this: $("#button").click(function(){ //custom function for exists() if ($("#menu").exists()){ $("#menu").remove(); } else { $.get("data.html", function(data){ $("body").append(data); $("#menu").draggable(); }); } });
