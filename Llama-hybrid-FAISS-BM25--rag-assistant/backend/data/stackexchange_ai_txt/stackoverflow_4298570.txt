[site]: stackoverflow
[post_id]: 4298570
[parent_id]: 
[tags]: 
how to catch this event that when i left click and then move some where (not drag some div ) on a div

this is my code: $('#handle').mousedown(function(e){ if( (!$.browser.msie && e.button == 0) || ($.browser.msie && e.button == 1) ) { alert("Left Button"); } }) this event is like to drag , but not drag , the left button Has been pressed, no released until mouseup, so how to catch it using jquery , this is my demo : http://jsfiddle.net/ATZNW/1/ thanks
