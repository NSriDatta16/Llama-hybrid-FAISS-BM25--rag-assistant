[site]: datascience
[post_id]: 100228
[parent_id]: 100114
[tags]: 
You need at least a few labelled vaccine tweets (positive, neutral, negative) to train a BERT model so that it starts to understand the domain. For VADER you don't need any labelled data. However, when we compare the accuracies, the BERT model always performs better.
