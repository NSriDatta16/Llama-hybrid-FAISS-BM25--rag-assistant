[site]: stackoverflow
[post_id]: 2825656
[parent_id]: 2824493
[tags]: 
Right now, your code makes #move1 and #move2 draggable, but nothing droppable. Try this: $(document).ready(function(){ $('#move1').draggable(); $('#move2').draggable(); $('form div').droppable({ drop: function(e, ui) { alert(ui.draggable[0].id + ' dropped on ' + this.id); //example alert: "move1 dropped on name2" } }); });
