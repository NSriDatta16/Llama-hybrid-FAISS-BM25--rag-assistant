[site]: stackoverflow
[post_id]: 4731499
[parent_id]: 4725823
[tags]: 
For this, I created a scalar function as follows: CREATE FUNCTION [dbo].[MaxOrNull](@val1 int, @val2 int) returns int as begin if @val1 >= @val2 RETURN @val1 if @val1 It's the most elegant solution and can be used anywhere in your SQL code.
