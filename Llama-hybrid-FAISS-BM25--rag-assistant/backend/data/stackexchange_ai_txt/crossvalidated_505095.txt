[site]: crossvalidated
[post_id]: 505095
[parent_id]: 25203
[tags]: 
The facebook prophet package supports multiple seasonalities . Yearly, weekly and daily seasonalities are built-in but custom seasonalities can be specified. Here is a custom monthly seasonality: df If predict() is called without passing in a data frame, then it will decompose the time series used to build the model.
