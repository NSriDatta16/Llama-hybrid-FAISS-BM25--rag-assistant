[site]: stackoverflow
[post_id]: 5297166
[parent_id]: 
[tags]: 
Boolean equation

Why this fragment of code has two different outputs (GCC 4.5.1) (I've commented important lines): int main() { bool a = 1; bool b = 1; bool c = 1; bool a_or_b = (a || b); bool not_a_or_b = !a_or_b; bool not_a_or_b__c = not_a_or_b || c; cout
