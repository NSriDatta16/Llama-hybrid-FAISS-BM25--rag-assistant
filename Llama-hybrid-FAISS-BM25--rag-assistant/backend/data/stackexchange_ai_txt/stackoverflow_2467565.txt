[site]: stackoverflow
[post_id]: 2467565
[parent_id]: 2467552
[tags]: 
inelegant perl one-liner which should do the trick, though not particularly quickly. cat file | perl -e ' $x=0; while(<>){ s/^\s*(\S*(?:\s+\S+)*)\s*$/$1/g; print; $x++; if($x==3){ print"\n"; $x=0; } }' > output
