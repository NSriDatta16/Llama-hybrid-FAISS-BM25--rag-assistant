[site]: stackoverflow
[post_id]: 4850922
[parent_id]: 4850843
[tags]: 
If you just need it for a single table, containing a single row, then are you sure a database is the right choice? I think saving the password to SharedPreferences might be a better fit. Have a look at the data storage options.
