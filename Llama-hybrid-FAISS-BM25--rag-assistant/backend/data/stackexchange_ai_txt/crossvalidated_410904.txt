[site]: crossvalidated
[post_id]: 410904
[parent_id]: 
[tags]: 
What is the difference between random-effects models, multilevel models and hierarchical models?

In the Bayesian paradigm, I have found examples of models that could be called any of the following: random-effects models multilevel models hierarchical models. Each of these categories even has its own tag on this site! So what are the important differences, then? How much of this is just rewording?
