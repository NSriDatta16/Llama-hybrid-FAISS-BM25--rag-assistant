[site]: stackoverflow
[post_id]: 3351407
[parent_id]: 3351293
[tags]: 
You need to get every color compound from RRGGBB, revert it and then glue back: function numberToHex(nValue, nLength/* =2 */) { var sValue = Math.abs(Math.floor(nValue)).toString(16); if (!nLength) nLength = 2; if (sValue.length
