[site]: stackoverflow
[post_id]: 5144308
[parent_id]: 5141979
[tags]: 
All messaging in Chrome and all(?) API methods are asynchronous. So your toolbar.js should look like: chrome.extension.sendRequest({getStatut : "none"}, function(response) { console.log('yourToolbar : ' + response.statut); localStorage['activated'] = response.statut; //inside a callback if (localStorage['activated'] == "show") { // injection } });
