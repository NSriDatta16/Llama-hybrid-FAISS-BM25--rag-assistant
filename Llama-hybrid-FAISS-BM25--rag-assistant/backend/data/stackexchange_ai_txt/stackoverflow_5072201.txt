[site]: stackoverflow
[post_id]: 5072201
[parent_id]: 5070704
[tags]: 
Use actions. In your touch began method create an action: CGPoint vector; vector.x = ball.position.x - touchLocation.x; vector.y = ball.position.y - touchLocation.y; float vectorLength = sqrt(vector.x*vector.x + vector.y*vector.y); if (fabs(vectorLength)
