[site]: stackoverflow
[post_id]: 2119866
[parent_id]: 
[tags]: 
Create a range or html fragment in IE from a string

How do I take a string and create an html fragment with it in IE. This works fine in non-ie browsers. var str = " Foo "; var range = document.createRange(); var frag = range.createContextualFragment(str); var e = document.getElementById("element"); e.appendChild(frag);
