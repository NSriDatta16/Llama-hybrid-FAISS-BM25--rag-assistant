[site]: stackoverflow
[post_id]: 1047455
[parent_id]: 1003341
[tags]: 
Im not sure you can dynamically select the method on the fly, but you could setup a destination per MessageType and use filters to ensure that a message is routed to the correct destination (method). I will look into the dynamic select. Perhaps the method is available via a javascript transformer object.
