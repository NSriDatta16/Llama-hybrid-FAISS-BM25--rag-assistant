[site]: stackoverflow
[post_id]: 1956222
[parent_id]: 1956145
[tags]: 
It should not be made public to avoid problems like the one you're having. See the Gentoo autotools best-practices document , specifically, the paragraph that starts with The config.h header file should be considered to be an internal header file . In your case, I would do exactly the same thing that you did: add a #define that prevents processing that file.
