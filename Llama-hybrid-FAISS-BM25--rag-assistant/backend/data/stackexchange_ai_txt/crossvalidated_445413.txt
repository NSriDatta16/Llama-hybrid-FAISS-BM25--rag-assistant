[site]: crossvalidated
[post_id]: 445413
[parent_id]: 444966
[tags]: 
That looks like a bug. I tweaked the same repo, and used the standard toy "8-gaussians" dataset, and trained a VAE. Red points: samples from the 8-gaussians dataset Blue points: sampled from the trained model It's nowhere near perfect (and 8-gaussians is actually a non-trivial task for many other generative models), but the mass is concentrated in the right places, roughly speaking.
