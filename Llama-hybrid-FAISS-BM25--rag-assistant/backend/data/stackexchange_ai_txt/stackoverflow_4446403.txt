[site]: stackoverflow
[post_id]: 4446403
[parent_id]: 4446367
[tags]: 
Where I work, we make an archive form Xcode (Build and Archive) and then we export that archive on the disk from the Organizer Window (using the Share button). This will generate an .ipa file. The size of that file is what you're looking for.
