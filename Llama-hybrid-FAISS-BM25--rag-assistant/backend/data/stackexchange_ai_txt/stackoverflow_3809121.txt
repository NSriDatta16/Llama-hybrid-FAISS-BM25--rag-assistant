[site]: stackoverflow
[post_id]: 3809121
[parent_id]: 3809108
[tags]: 
use this regex to remove empty paragraph / ]*> ]*>/ example dd non-empty "; $pattern = "/ ]*> ]*>/"; //$pattern = "/ ]*>([\s]?)* ]*>/"; use this pattern to remove any empty tag echo preg_replace($pattern, '', $html); // output //abc dd non-empty ?>
