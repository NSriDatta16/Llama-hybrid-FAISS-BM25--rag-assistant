[site]: stackoverflow
[post_id]: 5494501
[parent_id]: 5494349
[tags]: 
If you have a matrix as opposed to a data frame, there's no reason not to use t . Note that stack uses as.list and lapply , so is probably less elegant in this case. However, if what you want is just a vector, you can omit the matrix(*, ncol=1) bit; just use as.vector .
