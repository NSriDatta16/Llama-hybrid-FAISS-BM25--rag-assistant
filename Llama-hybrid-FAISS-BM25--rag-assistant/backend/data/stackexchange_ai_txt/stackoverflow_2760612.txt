[site]: stackoverflow
[post_id]: 2760612
[parent_id]: 2760502
[tags]: 
§6.3.1.3 Signed and unsigned integers, paragraph 2: if the new type is unsigned, the value is converted by repeatedly adding or subtracting one more than the maximum value that can be represented in the new type until the value is in the range of the new type. So yes, x == UINT_MAX .
