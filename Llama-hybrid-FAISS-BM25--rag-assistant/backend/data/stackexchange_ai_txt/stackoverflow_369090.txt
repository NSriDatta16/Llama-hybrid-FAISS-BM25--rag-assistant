[site]: stackoverflow
[post_id]: 369090
[parent_id]: 
[tags]: 
iFrame causes scriptaculous dragging issues in IE7 (full code included)?

When I drag a link that is inside a draggable div over an iframe in IE7, I get very strange results. Try the code below and let me know if you have any suggestions about how to fix this. --> --> blah function blah(){ //blackbird.debug("blah"); } var dummy = new Draggable("test", {scroll:window,scrollSensitivity: 20,scrollSpeed: 25, revert: true, onStart: onDragStart, onEnd: onDragEnd }); var temp; function onDragStart(drgObj,mouseEvent){ temp = mouseEvent.target.onclick; mouseEvent.target.onclick = function(e){ mouseEvent.target.onclick = temp; return false; } } function onDragEnd(drgObj,mouseEvent){ }
