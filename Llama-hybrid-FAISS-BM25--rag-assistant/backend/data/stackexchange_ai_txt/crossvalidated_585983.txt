[site]: crossvalidated
[post_id]: 585983
[parent_id]: 
[tags]: 
Calculating odds ratios and respective confidence intervals for multilevel multinomial logistic regression using brm package

I am predicting a 4 category variable using a multinomial regression and allowing intercepts to vary by country. Is there a way to calculate odds ratios and their confidence intervals for each variable (var 1, var 2 and var 3) using the brm package (or another package)? Model: model = brm(outcome ~ var1 + var2 + var3 + (1 | country), data, family=categorical(link="logit"), control=list(adapt_delta=0.95)) summary(model) yields a table like this: Odds ratios for a given variable (e.g., var1) are obtained by exponentiating the Estimate column. How can I get the confidence interval for these odds ratios?
