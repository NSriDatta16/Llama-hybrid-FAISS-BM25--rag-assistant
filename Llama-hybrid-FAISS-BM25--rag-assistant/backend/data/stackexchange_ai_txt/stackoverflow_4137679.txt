[site]: stackoverflow
[post_id]: 4137679
[parent_id]: 4137616
[tags]: 
Robert made a good comment, but also note that closure.del is not a function, but undefined. Take another look at your code. The function is automatically executed and returns nothing, so del will be nothing. var del = function(modMe) { modMe = secretVar; modMe.slice(1,1); console.log(modMe); }(secretVar); It's generally this form: var a = function(b){ console.log(b) }('input'); console.log( typeof a ); // undefined a(); // error: a is not a function
