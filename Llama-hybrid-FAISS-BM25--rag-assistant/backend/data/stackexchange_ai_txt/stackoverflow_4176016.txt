[site]: stackoverflow
[post_id]: 4176016
[parent_id]: 4175952
[tags]: 
If you want to enclose the html content into paragraph maybe you can do it with an oneliner: var resulting_html = ' ' + content_array.join(' ') + ' '; assuming that the array containing the html is called content_array. You will have the html ready to incorporate it into the target element.
