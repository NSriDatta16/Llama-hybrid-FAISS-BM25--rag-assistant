[site]: crossvalidated
[post_id]: 71615
[parent_id]: 
[tags]: 
Support Vector Machine(SVM) and log transformation

Why may log(natural logarithm) transformation improve results of SVM prediction( regression , eps-svm)? Is SVM based on the assumption of normal distribution or something else? update1. I use Radial basis function kernel.
