[site]: stackoverflow
[post_id]: 3506960
[parent_id]: 3506863
[tags]: 
First off, total abuse of the paragraph element . Instead, use the folling: HTML 15000 10 // In percentage 1000 // Grandtotal will be calculated and displayed here using jquery CSS #subtotal-and-taxes var { display:block margin-top:5px; margin-bottom:5px; } Next, you can use jQuery to get references to those elements and read their contents with the html() function: var subtotal = $("#subtotal-and-taxes #subtotal").html(); var tax = $("#subtotal-and-taxes #tax").html(); var discount = $("#subtotal-and-taxes #discount").html(); and then use non-jQuery JavaScript to calculate the value for grandtotal . Unfortunately, your equation is a little confusing so I won't spell it out for you.
