[site]: stackoverflow
[post_id]: 2232456
[parent_id]: 2228817
[tags]: 
It looks like you're asking if WordPress can handle using an external JS file instead of embedding it between script tags. If that's your question, then yes, just open your theme's header.php and include the file using: Same as any other script include. WordPress will work fine with that.
