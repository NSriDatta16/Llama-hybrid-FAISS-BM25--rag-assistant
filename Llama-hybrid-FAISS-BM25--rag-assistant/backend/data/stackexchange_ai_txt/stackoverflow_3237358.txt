[site]: stackoverflow
[post_id]: 3237358
[parent_id]: 3236960
[tags]: 
You can use db4o and configure it to use a MemoryBin as the backing storage and then send the bytes over the wire. PS: the link points to Java documentation because db4o .Net documentation is in CHM format.
