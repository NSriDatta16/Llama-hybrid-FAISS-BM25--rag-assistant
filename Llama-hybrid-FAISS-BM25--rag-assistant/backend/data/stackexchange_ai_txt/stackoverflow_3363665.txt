[site]: stackoverflow
[post_id]: 3363665
[parent_id]: 3363633
[tags]: 
Might not be the best, but define a new class that contains these properties and XML serialize this to a file. When you load the application, look for the file and deserialize it if it exists. Optionally, you could serialize this to user-specific isolated storage, so that different users' settings don't conflict.
