[site]: stackoverflow
[post_id]: 3095343
[parent_id]: 3095254
[tags]: 
The line you identified: var cName = $(response).find("fragment cName"); assigns the jQuery object to the variable cName , not it's text content which it looks like you want. Try changing it to var cName = $(response).find("fragment cName").text();
