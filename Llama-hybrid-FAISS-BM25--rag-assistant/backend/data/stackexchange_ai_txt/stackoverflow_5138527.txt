[site]: stackoverflow
[post_id]: 5138527
[parent_id]: 
[tags]: 
SQLite check if at max id

I'm using SQLite to store data for a small mobile app. When the user presses the 'next' button, I would like data to be loaded from the next record. I have that part done, but how do I check and see if the next record is the last (max) one? And if it is, is it as simple as setting the id = 0 to start at the beginning? My Current 'next'(): private function nextMoveRPart(event:EffectEvent):void { var cardNumber:int = parseInt(cardNumberLabel.text); stmt.sqlConnection = conn; conn.open(File.applicationStorageDirectory.resolvePath("FlashCards.db")); stmt.text = "SELECT * FROM cardItems IF id resultHandlerNext() function resultHandlerNext(event:SQLEvent):void // result handler next { var result:SQLResult = stmt.getResult(); var numResults:int = result.data.length; for(var i:int = 0; i Thank you!
