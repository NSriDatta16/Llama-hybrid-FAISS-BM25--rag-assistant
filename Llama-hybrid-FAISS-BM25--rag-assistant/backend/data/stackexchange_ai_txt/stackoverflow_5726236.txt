[site]: stackoverflow
[post_id]: 5726236
[parent_id]: 5726145
[tags]: 
I'd do away with the function altogether and just use: int employeeAge = (int)employees.Average(e => e.age); int studentAge = (int)students.Average(e => e.age); Edit: Added the return and the cast to an int (Average returns a double).
