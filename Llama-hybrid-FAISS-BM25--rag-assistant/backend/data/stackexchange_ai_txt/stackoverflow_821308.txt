[site]: stackoverflow
[post_id]: 821308
[parent_id]: 820694
[tags]: 
From the Subversion 1.6 CHANGES file: identical files share storage space in repository (issue #2286) So, it appears that even though the diff is applied, upon commit, the code should discover identical files and just use pointers to the first version of the files.
