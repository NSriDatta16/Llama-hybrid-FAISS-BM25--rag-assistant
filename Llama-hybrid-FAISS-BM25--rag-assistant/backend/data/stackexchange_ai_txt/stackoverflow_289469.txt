[site]: stackoverflow
[post_id]: 289469
[parent_id]: 289225
[tags]: 
IE8 beta 2 supports two APIs from HTML5: cross-document messaging and non-SQL storage . IE8 beta 2 doesn’t implement the HTML5 parsing algorithm or the new elements (no or support). There are also bug fixes that align IE8 better with HTML5.
