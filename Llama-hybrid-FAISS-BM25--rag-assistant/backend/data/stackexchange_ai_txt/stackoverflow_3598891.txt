[site]: stackoverflow
[post_id]: 3598891
[parent_id]: 
[tags]: 
Add rows to table even if not in array

Not sure how to title this, so I apologize if it's misleading or not understandable. What I have is an array, that array will have 1-4 arrays within it. I need a HTML table to output 4 columns, so even if the array only has one array in it, it still needs to output for columns. The issue I am hitting is that the columns need to match up. Inside of the arrays there is a key called 'Deductible' and that key matches up with the HTML table's columns. The Array, PHP, current HTML output and wanted HTML output are all below and a link to PasteBin in case this post doesn't display it all correctly. // OUTPUT OF $estimateOutput array ( 0 => array ( 'AdminCode' => 'NASC', 'AdminName' => array ( ), 'NewUsed' => 'Used', 'CoverageCode' => 'NCGUGOLD', 'CoverageName' => 'GOLD USED COMPONENT 1-10', 'GenericCoverage' => array ( ), 'Term' => '24/24', 'TermInMonths' => '24', 'TermInMilesKM' => '24000', 'Deductible' => '100', 'RetailCost' => '1377.0', ), 1 => array ( 'AdminCode' => 'NASC', 'AdminName' => array ( ), 'NewUsed' => 'Used', 'CoverageCode' => 'NCGUGOLD', 'CoverageName' => 'GOLD USED COMPONENT 1-10', 'GenericCoverage' => array ( ), 'Term' => '24/24', 'TermInMonths' => '24', 'TermInMilesKM' => '24000', 'Deductible' => '50', 'RetailCost' => '1462.0', ), ) $estimateOutput .= ' '.$term_length.' Months '; if($rateEstimate != ""){ foreach($rateEstimate as $rate) { $ratesByMiles[$rate["TermInMilesKM"]][] = $rate; } foreach($rateEstimate as $key=>$value){ if($TermInMilesKM != $value['TermInMilesKM']){ $estimateOutput .= ' '; $estimateOutput .= ' '.number_format($value['TermInMilesKM']).' '; foreach($ratesByMiles[$value['TermInMilesKM']] as $newval){ $estimateOutput .= ' '; $estimateOutput .= ' $'; $estimateOutput .= number_format($newval['RetailCost']); $estimateOutput .= ' '; $estimateOutput .= ' '; } $estimateOutput .= ' '; $TermInMilesKM = $value['TermInMilesKM']; } } } else { $estimateOutput .= ' Not Available '; } echo $estimateOutput; // CURRENTLY OUTPUTTING 24 Months 24,000 $1,462 $1,377 // NEED IT TO OUTPUT 24 Months 24,000 -- // If $newval['Deductible'] == 0 this should show data, otherwise -- $1,462 // If $newval['Deductible'] == 50 this should show data, otherwise -- $1,377 // If $newval['Deductible'] == 100 this should show data, otherwise -- -- // If $newval['Deductible'] == 200 this should show data, otherwise -- http://pastebin.com/y41MA8VZ
