[site]: stackoverflow
[post_id]: 1752272
[parent_id]: 
[tags]: 
Magento - Is it possible to feed IE6 a different template?

Just wondering if, in the config/design section of the Magento admin - where you specify templates - if it’s possible to add IE6 as an exception - so it doesn't get the custom template and falls back to using the default one? If so, what code do you put there? Cheers
