[site]: stackoverflow
[post_id]: 4130701
[parent_id]: 
[tags]: 
SmallChange is a property of ScrollBar,But it can not change scrollBar

I expect ScrollBar value changed by 1, so I set SmallChange is 1,But it is invalid to change value of ScrollBar. private void ScrollBar_ValueChanged(object sender, RoutedPropertyChangedEventArgs e) { double t = (sender as ScrollBar).Value; } Drag thump,But t is not 0,1.0,2.0,3.0......,
