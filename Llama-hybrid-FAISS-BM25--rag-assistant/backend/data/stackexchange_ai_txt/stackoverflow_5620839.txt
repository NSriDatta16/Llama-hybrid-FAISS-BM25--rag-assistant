[site]: stackoverflow
[post_id]: 5620839
[parent_id]: 5620746
[tags]: 
you need to write if/else if statements. There is no direct way string numberToFormat = //state your number here string FormattedString = null; if(numberToFormat >2) { //Format 1 //e.g. FormattedString = String.Format("{0:0.00}", numberToFormat); } else if(numberToFormat This is the shortest way. But of course you can get your own conditional formatter.
