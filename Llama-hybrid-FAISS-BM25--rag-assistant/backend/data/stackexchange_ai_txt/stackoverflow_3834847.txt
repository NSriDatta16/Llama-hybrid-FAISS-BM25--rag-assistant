[site]: stackoverflow
[post_id]: 3834847
[parent_id]: 3834646
[tags]: 
How about double AvgDays = dates.Where(d => d.Date.HasValue) .Average(dd => (dd.Date.Value-dd.OtherDate).Days); but maybe something like this is more useful var ticks = (long)dates.Where(d => d.Date.HasValue) .Average(dd => (dd.Date.Value - dd.OtherDate).Ticks); var avg = new TimeSpan(ticks); Console.WriteLine("It's " + avg.Days + " days and " + avg.Hours + " hours and " + avg.Seconds + " seconds.");
