[site]: stackoverflow
[post_id]: 774878
[parent_id]: 774844
[tags]: 
I'm a bit rusty, but you should be able to do a query like. SELECT * FROM Bases WHERE Site = 111 Return this to a record set, then simply loop through the record set adding items to an array for storage and future use, depending on what you need. If you are not familiar with recordsets, have a look at this article .
