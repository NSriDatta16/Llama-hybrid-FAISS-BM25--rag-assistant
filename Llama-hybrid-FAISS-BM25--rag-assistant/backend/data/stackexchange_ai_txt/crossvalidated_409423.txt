[site]: crossvalidated
[post_id]: 409423
[parent_id]: 
[tags]: 
Parametrs of the Uniform Likelihood

Consider linear scalar model $y_k = x_k + w_k$ where $w_k \sim U(a,b)$ . What we can say about the probability distribution $p(y_k|x_k)$ ? We can show that $p(y_k|x_k)$ have uniform density, but I caanot determine the parameters of this density. $$\begin{array}{l} {p_{{Y_k}|{X_k}}}\left( {{y_k}|{x_k}} \right) = \frac{{{p_{{X_k},{Y_k}}}({x_k},{y_k})}}{{{p_{{X_k}}}({x_k})}} = \frac{{{p_{{W_k},{X_k}}}({y_k} - {x_k},{x_k})}}{{{p_{{X_k}}}({x_k})}} = \frac{{{p_{{X_k}}}({x_k}) \times {p_W}({y_k} - {x_k})}}{{{p_{{X_k}}}({x_k})}} = {p_W}({y_k} - {x_k})\\ {p_W}({w_k})\sim U(a,b) \Rightarrow {p_W}({y_k} - {x_k})\sim U(???) \end{array}$$ Edit: Please pay attention what we know about $x_k$ is the density $p(x_{k}|y_{1:k−1}) \sim \mathcal{N}(x_{k}; 2, 4)$ ; but as we are conditioning on $x_k $ , it now is a fixed value.
