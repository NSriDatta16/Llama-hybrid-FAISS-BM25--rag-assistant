[site]: stackoverflow
[post_id]: 1474250
[parent_id]: 1474068
[tags]: 
If you use the JNLP Download Servlet you can pass the $$codebase macro into your application as a system property and this will give access to the codebase URL. In our webstart application configuration is stored to the server using SQL but that could be a bit heavyweight for your needs.
