[site]: stackoverflow
[post_id]: 5115707
[parent_id]: 5105987
[tags]: 
There is no way done it. Only override product model and extends getProductUrl() function getProductUrl($params, $useSid = null) { $url = pagent::getProductUrl($useSid); // now parse url, add our parameters from array $params // generate new url return $url }
