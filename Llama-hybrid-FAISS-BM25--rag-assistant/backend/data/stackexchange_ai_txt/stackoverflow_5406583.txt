[site]: stackoverflow
[post_id]: 5406583
[parent_id]: 5406351
[tags]: 
It depends on how the remote sites increment their counters. If they take into consideration User-Agent headers you could take advantage of that. If their counters are incremented via javascript (like Google Analytics) then Net::HTTP shouldn't trigger changes.
