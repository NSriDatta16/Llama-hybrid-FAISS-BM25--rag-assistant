[site]: stackoverflow
[post_id]: 2974018
[parent_id]: 2973993
[tags]: 
You can use fragment id messaging, which is a hack that uses window.location.hash (the part of the URL following #) to communicate. This test page has an example. There are also libraries such as Dojo that facilitate this. If you only need to support recent browsers, you can use postMessage .
