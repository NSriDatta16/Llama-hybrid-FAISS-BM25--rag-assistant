[site]: stackoverflow
[post_id]: 1398197
[parent_id]: 1398070
[tags]: 
It has been a couple of years (3-4) since I have been programming with C# but isn't this simple & elegant enough: string randomPick(string[] strings) { return strings[random.Next(strings.Length)]; } You should also check if the input array is not null .
