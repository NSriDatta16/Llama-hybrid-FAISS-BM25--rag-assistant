[site]: stackoverflow
[post_id]: 3390644
[parent_id]: 3390113
[tags]: 
with foreign characters for example 'FelvidÃƒÂ©k Ma' That's where your real trouble starts, there's little you can do afterwards to fix this problem. It isn't clear how you got the string, but it was created from the http stream without paying attention to the web page encoding. HttpResponse.ContentEncoding for example. Once you get this right, everything else is simple. No need to convert anything, what you write to the dbase is the actual text. If you cannot figure this out, be sure to update your question with details that describe how you got the _html string value.
