[site]: datascience
[post_id]: 17679
[parent_id]: 
[tags]: 
R, caret: does caret automatically create dummy variable for random Forest?

I created a two class data set, dat . This data set only contains 25 variables. 24 are numeric and 1 is categorical (with 26 levels). The result of the auto-tuned model gives a mtry of 2, 14, 26, 38 and 50. So is this true that the caret created the dummy variable for the categorical variable? What other models does caret create dummy variable automatically? Please see following example code. pacman::p_load(R.utils, caret, doParallel) cl
