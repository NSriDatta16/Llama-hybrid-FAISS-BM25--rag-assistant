[site]: stackoverflow
[post_id]: 3359856
[parent_id]: 3359050
[tags]: 
Since you seem to want to use C++, we could write it slightly more elegantly as: #include #include int main() { int const colLeft = 36; // or 26 int const colRight = 60; // or 48 std::ifstream input("input.txt"); std::ofstream output("output.txt"); std::string line; std::getline(input,line); output
