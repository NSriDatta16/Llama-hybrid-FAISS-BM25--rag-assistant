[site]: stackoverflow
[post_id]: 4756627
[parent_id]: 4756602
[tags]: 
To put it simply : // code fragment int Add(int x, int y) { return x + y; } var x = Add(int x, int y); // the compiler will know that the result of the call will be of type int class X { var x; // illegal, the compiler has no idea what's the target type of X }
