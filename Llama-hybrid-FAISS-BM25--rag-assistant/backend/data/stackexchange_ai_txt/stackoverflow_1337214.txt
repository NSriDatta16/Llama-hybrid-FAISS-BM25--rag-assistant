[site]: stackoverflow
[post_id]: 1337214
[parent_id]: 1337160
[tags]: 
Why can't I assign myNullable yourAge to int myAge and get an exception if yourAge is null? Of course you can. Just use the Value property without checking first: int myAge = yourAge.Value; If yourAge contains null you will get an exception.
