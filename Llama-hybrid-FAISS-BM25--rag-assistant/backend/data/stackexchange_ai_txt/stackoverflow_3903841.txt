[site]: stackoverflow
[post_id]: 3903841
[parent_id]: 3903819
[tags]: 
you might consider OpenMP if your compiler supports it (e.g. Visual C++ does). You don't have to create threads by yourself but rather #include omp_set_num_threads(10); #pragma omp parallel { #pragma omp for for (int i=0;i
