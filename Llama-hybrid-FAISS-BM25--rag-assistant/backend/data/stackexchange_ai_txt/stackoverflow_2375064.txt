[site]: stackoverflow
[post_id]: 2375064
[parent_id]: 2374963
[tags]: 
Passing function with lambda is probably most elegant, but if you just want a simple solution to your problem private void callingMethod(HttpContext context) { Foo myFoo = new Foo(); int myAge = myFoo.Age; setFromQueryString(ref myAge, "inputAge", context); myFoo.Age = myAge; } private void setFromQueryString(ref int age, String queryString, HttpContext context) { ... }
