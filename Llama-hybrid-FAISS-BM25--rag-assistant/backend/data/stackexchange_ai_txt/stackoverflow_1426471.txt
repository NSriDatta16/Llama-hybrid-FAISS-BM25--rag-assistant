[site]: stackoverflow
[post_id]: 1426471
[parent_id]: 1426189
[tags]: 
I can think of lots horrible messy ways to implement it but I assume (perhaps wrongly) that there is a simple, clean and maybe even elegant way of doing this Yepp. Take a look at the Path::Class module. From your question it is not entirely clear what you are trying to do, but Class::Path let's you manipulate paths in a cross-platform way.
