[site]: stackoverflow
[post_id]: 1717738
[parent_id]: 1496469
[tags]: 
gcov can be used (and is used) in maemo platform and tool called lcov can then be used to generate "pretty" reports. However, in order use gcov in maemo sdk, you need to disable compiler cache during the build time when you are creating binaries for coverage execution..
