[site]: stackoverflow
[post_id]: 1053705
[parent_id]: 
[tags]: 
Help me optimize this average calculation snippet

Is it possible to speed up this snippet? firstSample and lastSample is the part of the array I'm interested in this iteration. It's when this interval reaches > 3000 that I get a noticeable slowdown. The _average array can contain 6-60 million int values. minY and maxY is the result I use after this calculation is completed. int minY = Int32.MaxValue; int maxY = Int32.MinValue; int Y = 0; int sample = firstSample + 1; while (sample
