[site]: stackoverflow
[post_id]: 472204
[parent_id]: 471935
[tags]: 
Guard them with #if statements. Look for a symbol that's defined by one compiler but not the other. #ifdef _MSC_VER #pragma NOTE(my warning here) #else #warning(my warning here) #endif Kind of ugly, but I don't see another way.
