[site]: stackoverflow
[post_id]: 1793479
[parent_id]: 1793457
[tags]: 
For radio buttons, the value attributed is never rendered by the user agent (unless it does something rather weird). Typically, if you need a radio button with a label, you explicitly specify one, ideally using the tag.
