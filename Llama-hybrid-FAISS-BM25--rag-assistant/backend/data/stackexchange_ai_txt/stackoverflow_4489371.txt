[site]: stackoverflow
[post_id]: 4489371
[parent_id]: 4488784
[tags]: 
Are you redrawing on every resize event? If so, you might be redrawing too often, and might need to decouple your render from the resize event (e.g. while resizing and mouse down, don't render). I've had similar problems before, but on dragging objects in a window ...
