[site]: stackoverflow
[post_id]: 988329
[parent_id]: 988177
[tags]: 
You could start from the entity P, provided this has a 'back-link' to the containing entity C, a so called bidirectional mapping. You can achieve this with HQL by specifying a select or Criteria by using a ResultTransformer. e.g. select p.C from P p where p.r.property like 'BLAH%'; or s.createCriteria(P.class) .createAlias("c","toSelect") .createCriteria("r") .add(Restrictions.like("property","BLAH%") .setResultTransformer(new ResultTransformer() { public Object transformTuple(Object[] tuple, String[] aliases) { for (int i = 0; i
