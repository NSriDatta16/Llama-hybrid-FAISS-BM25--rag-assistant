[site]: stackoverflow
[post_id]: 1713544
[parent_id]: 1709970
[tags]: 
Signed cookies that take into account the user agent and source IP are OK, if you really need to be that tough. To combat proxies, use HTTPS. If you're talking about security, always use HTTPS before other measures. To combat check for different source IP-s (if you really need to) you could try whois and AS numbers.
