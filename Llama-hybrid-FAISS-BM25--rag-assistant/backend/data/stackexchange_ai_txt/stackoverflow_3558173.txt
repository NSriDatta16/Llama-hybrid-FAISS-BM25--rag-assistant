[site]: stackoverflow
[post_id]: 3558173
[parent_id]: 3557900
[tags]: 
1 - I agree. They are most likely separate Activities . To open another activity, you just use an intent, like this: Intent intent = new Intent(MyActivity.this, NewActivity.class); startActivity(intent); 2 - It sounds like the SharedPreferences class is the way to go, but read through the Android documentation for storing data, since there are a number of ways to go about saving data: http://developer.android.com/guide/topics/data/data-storage.html http://developer.android.com/reference/android/content/SharedPreferences.html
