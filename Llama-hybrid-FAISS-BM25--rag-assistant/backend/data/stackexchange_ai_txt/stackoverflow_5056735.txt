[site]: stackoverflow
[post_id]: 5056735
[parent_id]: 4920050
[tags]: 
You mean something like do_put() method here ? Just pay attention to the section "CACHE CONTENT"... The output cache file looks like: It uses serialize() and can also store cache in Db... Source here .
