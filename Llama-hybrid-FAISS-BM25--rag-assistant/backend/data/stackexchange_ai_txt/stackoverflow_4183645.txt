[site]: stackoverflow
[post_id]: 4183645
[parent_id]: 4183391
[tags]: 
As it is done periodically, have you considered using SSIS for this task? You can add multiple data connections, and then have a series of data sources (each connected to one of the connections) feeding into a data destination (also connected to one of the data sources) You could then schedule this as part of a SQL Agent Job. Something like this:
