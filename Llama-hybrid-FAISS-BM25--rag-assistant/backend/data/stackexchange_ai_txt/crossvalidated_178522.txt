[site]: crossvalidated
[post_id]: 178522
[parent_id]: 
[tags]: 
Why KNN is a non linear classifier ?

How do we decide if a classifier is linear or non linear ? What property/characteristic makes a classifier linear or non linear ? Eg: Why SVM is a linear classifier ? Why Logistic Regression is linear classifier even though it uses logistic function which is a non linear function ?
