[site]: stackoverflow
[post_id]: 528621
[parent_id]: 528610
[tags]: 
They're called Anonymous Types . The main reason for their use is to keep the data from a query in a single object, so the iterators can continue to iterate over a list of objects. They tend to work as temporary types for storage in the middle of a large or multi-part LINQ query.
