[site]: stackoverflow
[post_id]: 4102985
[parent_id]: 4099813
[tags]: 
The SpeedLimit prefpane by mschrag does what you want: http://mschrag.github.com/ I'm not sure if it works for localhost, but it works great for everything else, and it's incredibly easy to use, so it's definitely worth a try.
