[site]: datascience
[post_id]: 116042
[parent_id]: 
[tags]: 
Keras Custom Layer Aware of Position in Network

I'm writing a custom Layer in TF/Keras ( tf.keras.layers.Layer ) for a CNN, and I need each layer to know its own depth in the model. Is there any way I can do this, without explicitly passing it as an argument?
