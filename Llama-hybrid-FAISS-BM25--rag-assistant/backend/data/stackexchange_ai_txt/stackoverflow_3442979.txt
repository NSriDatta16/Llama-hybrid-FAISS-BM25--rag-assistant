[site]: stackoverflow
[post_id]: 3442979
[parent_id]: 3442492
[tags]: 
Using the code below I've found myVec = new Vector. () is the fastest way. Output (average times in ms to empty a Vector. with 100000 random entries): 0.679 // using splice() 0.024 // using new Vector. () 0.115 // using .length = 0; Code: var myVec:Vector. = new Vector. (); var emptyTime:int; var startTime:int; var totalEmptyTime:Number = 0; // Need to empty and repopulate vector const NUM_TRIALS:int = 1000; var j:int; for(j = 0; j (); emptyTime = getTimer() - startTime; totalEmptyTime += emptyTime; } trace(totalEmptyTime/NUM_TRIALS); totalEmptyTime = 0; // OR for(j = 0; j
