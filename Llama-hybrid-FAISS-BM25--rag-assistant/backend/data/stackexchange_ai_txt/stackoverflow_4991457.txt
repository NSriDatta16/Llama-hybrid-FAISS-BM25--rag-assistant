[site]: stackoverflow
[post_id]: 4991457
[parent_id]: 
[tags]: 
Why use serialisation/Deserialisation functions when we can directly do it using IsolatedStorageSettings.ApplicationSettings?

In Windows phone we can store and retrieve anything using IsolatedStorageSettings.ApplicationSettings.Add("foo", bar); IsolatedStorageSettings.ApplicationSettings["foo"] Then why people are using "sharpSerializer" kind of things ?
