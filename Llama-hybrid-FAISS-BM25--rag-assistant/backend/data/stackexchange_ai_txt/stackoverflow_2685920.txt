[site]: stackoverflow
[post_id]: 2685920
[parent_id]: 
[tags]: 
How do I use QXmlQuery properly? (Qt XQuery/XPath)

I'm using the following code to load in an XML file (actually an NZB): QXmlQuery query; query.bindVariable("path", QVariant(path)); query.setQuery("doc($path)/nzb/file/segments/segment/string()"); if(!query.isValid()) throw QString("Invalid query."); QStringList segments; if(!query.evaluateTo(&segments)) throw QString("Unable to evaluate..."); QString string; foreach(string, segments) qDebug() With the following input, it works as expected: alt.binaries.cd.image waWdnZFHevdBeZTUnZ2dnUVZ8uOdnZ2d@giganews.com However, with the following input no results are returned. This is how the input should be formatted, with attributes: alt.binaries.cd.image waWdnZFHevdBeZTUnZ2dnUVZ8uOdnZ2d@giganews.com waWdfhrgfnZFHevdBeZTUnZ2dnUVZ8uOdnZ2d@giganews.com Please can someone tell me what I'm doing wrong?
