[site]: crossvalidated
[post_id]: 597490
[parent_id]: 
[tags]: 
Logistic regression, log-odds score interpretation

If I model glm2.out Where non_def is a column of values with $1$ if I do not default on a loan, and $0$ if I do default. The question is, does a higher score indicate a greater association with defaulting or non-defaulting?
