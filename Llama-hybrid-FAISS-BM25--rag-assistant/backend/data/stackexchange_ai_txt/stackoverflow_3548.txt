[site]: stackoverflow
[post_id]: 3548
[parent_id]: 3486
[tags]: 
If you're trying to prevent search engine bots from accessing certain pages, make sure you're using a properly formatted robots.txt file. Using HTTP_REFERER is unreliable because it is easily faked . Another option is to check the user agent string for known bots (this may require code modification).
