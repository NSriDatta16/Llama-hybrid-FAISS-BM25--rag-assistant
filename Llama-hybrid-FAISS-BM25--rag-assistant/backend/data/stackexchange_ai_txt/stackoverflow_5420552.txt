[site]: stackoverflow
[post_id]: 5420552
[parent_id]: 
[tags]: 
Magento - Programmatically Disable Automatic Indexing

In Magento 1.9 Enterprise (which is essentially the 1.4 Community Edition), what is the correct way to disable the index programmatically so that it wont reindex after every product update? We have a complex product import procedure, so we can't use the built-in catalog import.
