[site]: crossvalidated
[post_id]: 468713
[parent_id]: 
[tags]: 
Is is possible that 2 classifier give me the exact same results?

I'm trying to classify an expression dataset composed by 23 control person and 15 patient, I'm using LDA and RF that actually give me the same result(Sensitivity = 1 Specificity = 0.95 and accuracy 0.94) (Lasso give me different results), is it possible and if why? library(GEOquery) library(pca3d) library(ggplot2) library(devtools) library(ggbiplot) library("genefilter") library("randomForest") library("GEOquery") library(ggplot2) gse $p.value, method = "bonferroni") AFFECTED ### No cross ##S ## control
