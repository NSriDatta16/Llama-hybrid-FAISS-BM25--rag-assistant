[site]: stackoverflow
[post_id]: 1649888
[parent_id]: 
[tags]: 
.NET Reflection: Detecting IEnumerable

I'm trying to detect if a particular instance of a Type object is a generic "IEnumerable"... The best I can come up with is: // theType might be typeof(IEnumerable ) for example... or it might not bool isGenericEnumerable = theType.GetGenericTypeDefinition() == typeof(IEnumerable ).GetGenericTypeDefinition() if(isGenericEnumerable) { Type enumType = theType.GetGenericArguments()[0]; etc. ...// enumType is now typeof(string) But this seems a bit indirect - is there a more direct/elegant way to do this?
