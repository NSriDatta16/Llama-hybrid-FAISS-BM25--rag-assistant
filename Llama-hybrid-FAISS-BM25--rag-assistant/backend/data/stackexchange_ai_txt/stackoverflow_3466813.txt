[site]: stackoverflow
[post_id]: 3466813
[parent_id]: 3465895
[tags]: 
I've figured out by myself: Dim q3 = (From a In dc.Movies Where a.actlist.Contains(actor.name) Select a).Average(Function(r) r.ranking) But this one throws an execption if an actor isn't in any movie. So I had to put it in try..cast.
