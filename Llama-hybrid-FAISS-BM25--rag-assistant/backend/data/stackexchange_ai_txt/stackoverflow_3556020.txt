[site]: stackoverflow
[post_id]: 3556020
[parent_id]: 3550341
[tags]: 
A simple ggplot2 gantt chart. First, we create some data. library(reshape2) library(ggplot2) tasks Now draw the plot. ggplot(mdfr, aes(value, name, colour = is.critical)) + geom_line(size = 6) + xlab(NULL) + ylab(NULL)
