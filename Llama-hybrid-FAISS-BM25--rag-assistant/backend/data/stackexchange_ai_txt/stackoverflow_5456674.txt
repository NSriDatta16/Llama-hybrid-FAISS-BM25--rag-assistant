[site]: stackoverflow
[post_id]: 5456674
[parent_id]: 
[tags]: 
Qt set custom drag cursor?

How can I set the cursor used in Qt while performing a drag operation? I am using the QDrag class. The function setCursor takes a pixmap and has no way to specify the hotspot, nor do the docs specify that it could override the "no action" cursor. I'm happy if I can just do an explicit cursor in mouseMoveEvent but I'm not sure how during a drag operation.
