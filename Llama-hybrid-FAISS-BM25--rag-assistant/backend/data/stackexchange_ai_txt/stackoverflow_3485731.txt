[site]: stackoverflow
[post_id]: 3485731
[parent_id]: 3485701
[tags]: 
Further to Robert's answer -- and to cover the second part of your question -- you can combine the separate components back to an integer using the (left-shift) and | (bitwise OR) operators: int combined = (alpha
