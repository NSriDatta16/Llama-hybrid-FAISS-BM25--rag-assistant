[site]: stackoverflow
[post_id]: 3837639
[parent_id]: 3837626
[tags]: 
It will get lost on every request. If it is very small, you might be able to put it in a cookie and re-read it (and evel it) on every reload. With HTML5 you should be able to persist it using web/local storage .
