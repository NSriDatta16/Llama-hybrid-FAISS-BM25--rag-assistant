[site]: stackoverflow
[post_id]: 951577
[parent_id]: 
[tags]: 
How to include js files in asp.net MVC and have a valid path on all routes

I created a default ASP.net MVC project. In the Master page I have the following at the top I then need to add a javascript file and added the line as follows by dragging the file from the solution explorer to the page: When I access a the site and look at the html from the browser I see this: Index The CSS file relative path was fixed but the JS file not. The site will be deployed to a different folder on the server than the root which I get on my development box. Is there a proper way to do this?
