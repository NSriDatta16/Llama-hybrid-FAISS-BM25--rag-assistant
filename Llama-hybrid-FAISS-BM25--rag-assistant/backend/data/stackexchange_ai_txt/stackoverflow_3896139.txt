[site]: stackoverflow
[post_id]: 3896139
[parent_id]: 3895028
[tags]: 
It fails because a doesn't print anything, the c:redirect tag isn't properly closed and possibly also because the value isn't enclosed in quotes. You rather want this: " /> Note that the cast is unnecessary. However, using old fashioned scriptlets is discouraged since a decade. Rather use EL. It's then as easy and nice as:
