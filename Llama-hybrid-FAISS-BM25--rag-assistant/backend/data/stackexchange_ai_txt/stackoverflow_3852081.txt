[site]: stackoverflow
[post_id]: 3852081
[parent_id]: 3851526
[tags]: 
ok here's what worked... Not loving this bec it prevent the user from REFRESHing ajax content. I'd love it if you can one up me with something better and more elegant... $('a').live('click', function() { // Page Jump Prevention - Does the clicked on Hash differ from the currently loaded hash, if not return false if (window.location.hash.split('#')[1] == this.href.split('#')[1]) { //alert('Dude we already loaded this!') return false; } });
