[site]: crossvalidated
[post_id]: 348324
[parent_id]: 348319
[tags]: 
Unfortunately, I do not have your data to work with. I'll solve your problem using iris dataset: library("FactoMineR") library("factoextra") res.pca Notice that I just used axes = c(2, 3) argument inside fviz_pca_var function in order to plot the principal components I want.
