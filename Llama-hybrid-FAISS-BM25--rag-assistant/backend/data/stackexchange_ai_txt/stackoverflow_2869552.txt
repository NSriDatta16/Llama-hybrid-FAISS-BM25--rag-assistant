[site]: stackoverflow
[post_id]: 2869552
[parent_id]: 
[tags]: 
Simple average calculation

I'm trying to write program calculating average of given numbers stored in an array. Amount of numbers should be not more than 100, and user should input them until a !int variable is given : #include #include using namespace std; double average(int tab[], int i){ int sum=0; for(int j=0; j > number; tab[n]=number; number=0; ++n; } else{ break; } } while( !isdigit(number) ); cout Why after giving char, it prints me 'Give n number:' for all empty cells of my array ? It should end and use only given numbers.
