[site]: stackoverflow
[post_id]: 3809403
[parent_id]: 
[tags]: 
Rendering my own tag instead of the one provided by JSF

I have page which consists of couple fragments and in the "header" fragment I have this tag . This is rendered in HTML as . This is fine and it is working as expected. Now I need to force JSF somehow to return URL to the javascript with current version of app. This is known technique for forcing the reload of resource (javascript, css and images) in case they are cashed on client's side. I need to render something like . Please note the ver parameter in the URL. Thanks. Tomas
