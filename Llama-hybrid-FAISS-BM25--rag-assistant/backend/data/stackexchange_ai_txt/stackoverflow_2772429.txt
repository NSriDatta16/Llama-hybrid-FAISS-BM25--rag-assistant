[site]: stackoverflow
[post_id]: 2772429
[parent_id]: 
[tags]: 
weird space in IE - Any suggestions?

The below table is the only element inside a body tag - This displays fine in Firefox 3 as i expect it to be but it does not look good in IE7. There is a weird space just between the nested table and the row above. Can you please suggest some way to remove that weird space? - Thanks Search Relationships This is working second row just above the table new row inside table Select: User Name User Id IBD/Office /IP APA# IBD OFF IP Average Price Account# Enter multiple separated by commas
