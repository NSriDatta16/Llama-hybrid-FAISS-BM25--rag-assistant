[site]: stackoverflow
[post_id]: 377768
[parent_id]: 
[tags]: 
String concatenation in Ruby

I am looking for a more elegant way of concatenating strings in Ruby. I have the following line: source = "#{ROOT_DIR}/" Is there a nicer way of doing this? And for that matter what is the difference between and + ?
