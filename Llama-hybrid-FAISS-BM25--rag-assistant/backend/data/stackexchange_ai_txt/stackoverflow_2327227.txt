[site]: stackoverflow
[post_id]: 2327227
[parent_id]: 2326943
[tags]: 
The lifecycle is controlled by the application/user. From the standard : User agents should expire data from the local storage areas only for security reasons or when requested to do so by the user. User agents should always avoid deleting data while a script that could access that data is running.
