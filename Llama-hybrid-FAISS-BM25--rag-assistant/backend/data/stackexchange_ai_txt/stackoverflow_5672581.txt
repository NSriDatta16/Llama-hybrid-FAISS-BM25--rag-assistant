[site]: stackoverflow
[post_id]: 5672581
[parent_id]: 190759
[tags]: 
Creepy. Try if (!isset($_SERVER['HTTP_USER_AGENT'])) { instead. PHP Client Binary dont send it. Term Type just works when PHP is used as module (ie apache) but when running php through CGI interface, use the example above!
