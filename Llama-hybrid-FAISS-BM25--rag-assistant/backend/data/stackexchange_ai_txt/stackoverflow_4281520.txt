[site]: stackoverflow
[post_id]: 4281520
[parent_id]: 4280413
[tags]: 
If the segment from (x1,y1) to (x2,y2) is short, then you can approximate its direction vector simply by: (x2-x1) *i + (y2-y1) *j . Otherwise, you could use PCA to estimate the direction vector as the principal axis of individual points forming the segment,
