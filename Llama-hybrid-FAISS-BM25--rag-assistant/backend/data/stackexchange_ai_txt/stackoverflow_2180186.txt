[site]: stackoverflow
[post_id]: 2180186
[parent_id]: 2180143
[tags]: 
You can always get the class object from an instance via instance.getClass() . Therefore the following should work: instance.getClass().findBySomethingAndSomething(type.identifier, type.otheridentifier) as long as the instance's class supports findBySomethingAndSomething
