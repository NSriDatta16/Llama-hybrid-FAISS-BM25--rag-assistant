[site]: stackoverflow
[post_id]: 3102199
[parent_id]: 
[tags]: 
Lookup table/palette shader on the iPhone

I have a fragment shader with the following: const lowp float colors[8] = float[8]( // line 12 0,0,0,1, 1,0,0,1 ); but it fails to compile: ERROR: 0:12: 'array of float' : array type not supported here in glsl How can I define a lookup table in GLSL? The input texture has values from an enum, each value of which I want to be mapped to a different color.
