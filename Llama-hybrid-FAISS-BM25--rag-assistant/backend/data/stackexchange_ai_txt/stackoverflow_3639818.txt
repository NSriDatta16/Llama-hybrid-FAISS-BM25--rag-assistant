[site]: stackoverflow
[post_id]: 3639818
[parent_id]: 
[tags]: 
search by attribute_like_any using multiple words in one field [searchlogic]

My form ... Controller @search = Product.search @search.name_like_any(params[:search][:name_like_any].split(/\s+/)) @products = search.all This returns the correct result, but now my form shows the name as ["foo", "bar"] instead of what the user input ( "foo bar" ). What's the elegant way to handle this? Appreciate any feedback
