[site]: stackoverflow
[post_id]: 5554160
[parent_id]: 5554069
[tags]: 
There are several StackOverflow postings on these issues. You want to use Environment.getExternalStorageDirectory() to get the path to the SD Card. You can then do standard Java File I/O. Android how to use Environment.getExternalStorageDirectory() You must use the following permission. Permission to write to the SD card
