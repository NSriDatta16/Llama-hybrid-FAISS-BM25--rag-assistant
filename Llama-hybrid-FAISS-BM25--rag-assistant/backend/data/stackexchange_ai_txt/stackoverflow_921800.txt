[site]: stackoverflow
[post_id]: 921800
[parent_id]: 
[tags]: 
type safe and nhibernate

Is it possible to create 100% type safe compile time checking code? Even criteria seems to reference column names with a string reference "Username", but if you put "Usernnameaa" it will fail at runtime right?
