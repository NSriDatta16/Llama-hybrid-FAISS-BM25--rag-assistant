[site]: stackoverflow
[post_id]: 4399424
[parent_id]: 
[tags]: 
Using $(this) after .replaceWith()

Please consider the below HTML and Javascript. In the script, I am replacing an a tag with a p tag. I am expecting the alert() function to return the contents of the p tag but instead it returns the contents of the original a tag which no longer exists. How can I reference the new element? HTML: This is a link Javascript: $(document).ready(function() { $("a").each(function() { $(this).replaceWith(' New Paragraph '); alert($(this).text()); }); });
