[site]: stackoverflow
[post_id]: 3943232
[parent_id]: 3943203
[tags]: 
If you DIV has a specific ID or class you can use the next() function (documented here ). $('#dragdiv').next() Then you can directly work with that jquery element or use attr() to obtain it's attributes (ID, etc)
