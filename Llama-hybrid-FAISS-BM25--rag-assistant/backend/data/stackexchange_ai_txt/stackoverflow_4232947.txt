[site]: stackoverflow
[post_id]: 4232947
[parent_id]: 4232843
[tags]: 
wouldn't it be sufficient to declare it like this? const CarCollection& Cars() { return m_Cars; } then CarCollection::const_iterator it = garage.Cars().begin(); should work but CarCollection::iterator it = garage.Cars().begin(); would give error.
