[site]: stackoverflow
[post_id]: 400326
[parent_id]: 400314
[tags]: 
It's a symbol for a generic type parameter . It could just as well be something else, for example: public class SomeBase where GenericThingy : SomeBase , new() Only T is the default one used and encouraged by Microsoft.
