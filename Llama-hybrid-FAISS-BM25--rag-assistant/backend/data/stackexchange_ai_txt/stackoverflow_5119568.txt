[site]: stackoverflow
[post_id]: 5119568
[parent_id]: 5119165
[tags]: 
Not completely sure what you are doing, where is the data source you make the search on, so just guessing: string sCorpFilter = m_sc.get_Corp(); IEnumerable corps = null; if (! string.IsNullOrEmpty(sCorpFilter)) { corps = sCorpFilter.Split(",".ToCharArray(), StringSplitOptions.RemoveEmptyEntries).Select(s => s.Trim()); } [...] if (corps != null && corps.Contains(AgentID)) { found = true; }
