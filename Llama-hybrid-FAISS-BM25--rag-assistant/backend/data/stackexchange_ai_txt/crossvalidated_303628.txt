[site]: crossvalidated
[post_id]: 303628
[parent_id]: 
[tags]: 
Neural Network with only inputs

Is there a neural network that only requires inputs to train on? I'm new at this, sorry :P. I need a neural net that gets feeded a lot of inputs, and an output base off the sequence of inputs.
