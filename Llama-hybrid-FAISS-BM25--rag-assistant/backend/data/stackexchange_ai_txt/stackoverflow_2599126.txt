[site]: stackoverflow
[post_id]: 2599126
[parent_id]: 
[tags]: 
Net::HTTP::Unauthorized - how do I get at the WWW-Authenticate header?

Given the code below ... Net::HTTP.start('localhost', 4000) do |http| # # usual stuff omitted for clarity # @response = http.request(req) end ... if a (well behaved) server returns a 401 (Unauthorized) response, how do I get at the WWW_Authenticate header? The best solution I've got isn't at all good ... class Net::HTTPUnauthorized def get_header(h) _return = nil target = h.upcase self.header.each_header do |k, v| if k.upcase == target _return = v break end end _return end end Chris
