[site]: stackoverflow
[post_id]: 5151792
[parent_id]: 5151718
[tags]: 
You forgot to return false $(function() { $("dt.title").click(function(){ $("div.info").slideUp("slow"); $(this).next("div.info:hidden").slideDown("normal" ); return false; //do it here }); }); About Work Bol Annie Imran Contact Links If you are planning to load the content of about.html in content element then use load method. Your function will be something like $("dt.title").click(function(){ $("div.info").slideToggle("slow"); $('#content').load($(this).find('a').attr('href')); return false; }); But this will not work for cross-domain url. [UPDATE] //no use for this var var loader = $(" ").html('Loading') $(document).ready(function() { $(".title").click(function(){ $(this).find("ul").slideToggle("slow"); //do all the animations here //but using $.get, you will get more animation effect //I guess $('#content').load($(this).find('a').attr('href')); return false; }); //similarly for inner link $(".title ul a").click( function () { $('#content').load($(this).attr('href')); return false; }); }); About Bol Annie Khalid Imran Work Bol Annie Khalid Imran Contact Bol Annie Khalid Imran Links Bol Annie Khalid Imran Welcome to my site!!!!!!!!!!!!!!!!!!!!!!! In this tutorial we will be taking your average everyday website and enhancing it with jQuery. We will be adding ajax functionality so that the content loads into the relevant container instead of the user having to navigate to another page. We will also be integrating some awesome effects...
