[site]: stackoverflow
[post_id]: 3043891
[parent_id]: 3043846
[tags]: 
I haven't tested this, but queryForObject with Double.class as the last parameter might work. public double getAverageScore() { return jdbctemplate.queryForObject("select avg(score) from test", Double.class); }
