[site]: datascience
[post_id]: 84364
[parent_id]: 72598
[tags]: 
Since you have label data (i.e., sale amount), you can apply supervised machine learning. After a model has been fit, the features that contributed to predicting targets can be found. Decisions trees would be a relatively simple option because it easily handle the different types of features and can a yield a decision path.
