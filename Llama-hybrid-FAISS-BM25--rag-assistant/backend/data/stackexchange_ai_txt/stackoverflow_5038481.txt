[site]: stackoverflow
[post_id]: 5038481
[parent_id]: 5037810
[tags]: 
This is JPA standard. q.organization.key resolves to an inner join on q.organization, not to a left join! You have to do this: SELECT q from SurveyQuestion q left join q.organization o where o is null OR o.key = :organizationKey
