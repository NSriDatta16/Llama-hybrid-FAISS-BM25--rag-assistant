[site]: crossvalidated
[post_id]: 26878
[parent_id]: 26873
[tags]: 
SVM-RFE is a wrapper because it features training a classifier. Filters use some other criteria (mostly dependence tests) and the only classifier trained is the final one, built on the selected features.
