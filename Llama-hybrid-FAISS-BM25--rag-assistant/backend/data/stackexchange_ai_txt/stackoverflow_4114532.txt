[site]: stackoverflow
[post_id]: 4114532
[parent_id]: 
[tags]: 
iOS -- configuring a UITableViewCell after the cell learns its size

Suppose I am creating a UITableViewCell subclass. The cell will not know what its size is going to be until the controller tells it. What is the proper way to organize configuration of the subviews? They will want to determine their frames based on the size of the cell.
