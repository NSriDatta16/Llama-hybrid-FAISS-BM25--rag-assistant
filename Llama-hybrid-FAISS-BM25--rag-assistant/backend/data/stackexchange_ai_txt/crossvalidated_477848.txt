[site]: crossvalidated
[post_id]: 477848
[parent_id]: 
[tags]: 
Distribution of Data in GAN

Will Generator and Discriminator learns the Distribution of Data when it was trained? If So Both Generator and Discriminator learns same distribution or different distributions ? What can we interpret about the distribution of data for trained GAN ?
