[site]: stackoverflow
[post_id]: 3959616
[parent_id]: 3959497
[tags]: 
For XSLT 1.0 and XSLT 2.0, Pattern in rules should match nodes. In XSLT 1.0 shouldn't have variable/param reference. node-set extension function converts a Result Tree Fragment into a node: the node root of the RTF. So, in order to do what you want, you should use:
