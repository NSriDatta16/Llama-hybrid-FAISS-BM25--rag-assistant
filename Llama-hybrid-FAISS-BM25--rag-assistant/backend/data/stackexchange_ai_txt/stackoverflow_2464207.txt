[site]: stackoverflow
[post_id]: 2464207
[parent_id]: 2429433
[tags]: 
To get a handle to the File object representing the root of the SD card's filesystem you can use, Environment.getExternalStorageDirectory() . Listing the files in a directory is relatively straight forward using the File object's listFiles() method.
