[site]: stackoverflow
[post_id]: 1872477
[parent_id]: 1846938
[tags]: 
I'm not very familiar with the GWT plugin for Grails but it looks more like a standard GWT issue. From the GWT documentation about how to organize projects : The default source path is the client subpackage underneath where the Module XML File is stored. You package name is es.prueba.domain so GWT does not find it when it looks for source. You have to specify it in your module file (let's say Application.gwt.xml) as specified below:
