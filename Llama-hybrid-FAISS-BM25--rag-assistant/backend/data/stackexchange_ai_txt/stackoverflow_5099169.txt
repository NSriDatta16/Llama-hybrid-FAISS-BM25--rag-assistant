[site]: stackoverflow
[post_id]: 5099169
[parent_id]: 5099150
[tags]: 
I would make a variable, that switches depending on which checkbox is checked. (Perhaps an int value) And then, when you click 'continue' you can say If (click == 1) { window.open(www.google.com") } or something to that effect. Probably not the most elegant solution, but it certainly should work.
