[site]: stackoverflow
[post_id]: 1461439
[parent_id]: 1460737
[tags]: 
I have a very simple demo for testing HTML5's webstorage / localstorage. http://www.codebase.es/test/webstorage.html You can store whatever you want, not only strings. To store an image, copy the image into a canvas and save the data using toDataURL() method. But don't expect it to work on IE...
