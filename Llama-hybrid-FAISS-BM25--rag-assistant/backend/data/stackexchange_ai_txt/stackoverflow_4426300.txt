[site]: stackoverflow
[post_id]: 4426300
[parent_id]: 4426290
[tags]: 
Not sure about VS2010... but the debug and release modes probably have different library settings... You can inform the compiler to link thru code by specifying the following #pragma comment(lib, "library.lib") // no ; is needed That will make it link in both debug and release
