[site]: stackoverflow
[post_id]: 4352952
[parent_id]: 
[tags]: 
Binding to IsolatedStorageSettings

Binding={Binding Source={x:Static ...}} doesn't seem to work, so my static wrapper class doesn't work. How can we make a two-way binding to a key in WP7 IsolatedStorageSettings ?
