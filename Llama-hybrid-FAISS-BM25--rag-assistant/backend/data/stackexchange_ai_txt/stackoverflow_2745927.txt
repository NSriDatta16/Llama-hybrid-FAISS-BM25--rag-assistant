[site]: stackoverflow
[post_id]: 2745927
[parent_id]: 1051312
[tags]: 
xRootdfs from someone at slac seems to do this now. "rootdFS is a Posix filesystem for an Xrootd storage cluster. It is based on FUSE (Filesystem in Userspace) and runs in user space." but I expect that you need root access to load the FUSE module.
