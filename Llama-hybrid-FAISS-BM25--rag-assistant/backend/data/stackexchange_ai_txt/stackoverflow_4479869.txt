[site]: stackoverflow
[post_id]: 4479869
[parent_id]: 4479205
[tags]: 
One way to do it (albeit with hardcoded file names): BEGIN { FS="\t"; readfile(ARGV[1], s1); readfile(ARGV[2], s2); ARGV[1] = ARGV[2] = "/dev/null" } END{ for (k in s1) { if ( s2[k] ) printpair(k,s1,s2); } print "file1:" for (k in s1) { if ( !s2[k] ) print s1[k]; } print "file2:" for (k in s2) { if ( !s1[k] ) print s2[k]; } } function readfile(fname, sary) { while ( getline x) z = y; return z; } Not particularly elegant, but it handles many-to-many cases.
