[site]: stackoverflow
[post_id]: 1503260
[parent_id]: 
[tags]: 
Format XMl Document by adding "/" at the end of tag

So, i have some not well-formated xml document, some empty tags() don't have "/" at the end, example: And how can i elegant(using regex:)) add for each "loader" tag "/" at the of this tag(using Java); must be:
