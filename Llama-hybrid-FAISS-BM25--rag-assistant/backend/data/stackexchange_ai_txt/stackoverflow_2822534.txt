[site]: stackoverflow
[post_id]: 2822534
[parent_id]: 2822521
[tags]: 
For each of your task, you have to implement separate algorithm for check. Check extension before upload (see possible duplicate question) Check that uploaded zip is really an archive (mime type, libmagic, etc) Check that uploaded archive doesn't contain exe (unzip it, see previous paragraph)
