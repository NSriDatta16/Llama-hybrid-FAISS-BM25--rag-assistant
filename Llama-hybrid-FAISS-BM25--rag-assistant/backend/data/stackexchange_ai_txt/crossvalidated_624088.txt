[site]: crossvalidated
[post_id]: 624088
[parent_id]: 
[tags]: 
how to derive standard error for ROC and how to compare 2 ROC area under curve meta-analytic estimates in R using metamisc (Bayesian random effects)

I am trying to do a meta-analysis to pool many ROC curves together and get an estimate of cumulative AUC using 2 different techniques as logistic regression( LR ) and artificial intelligence ( AI ). I have 95% confidence interval (95%CI) for only some of the included studies. I found a formula to calculate the standard error for AUC SE(AUC) here I want to be sure this is the right approach I want to obtain the P-value for subgroup difference between a summary ROC estimate for AUC for LR and then for AI i.e. compare estimates between both groups. Here is the data sample and my code so far: df $Q1 AUC/(2-df $AUC);df$ Q1 df $Q2 AUC^2/(1+df $AUC);df$ Q2 df $SE_AUC AUC*(1-df $AUC)+ (df$ PPI-1)*(df $Q1-df$ AUC^2)+ (df $No.PPI-1)*(df$ Q2-df $AUC^2)) / (df$ PPI*df $No.PPI)) );df$ SE_AUC ################################################################################################################### ##============= Bayesian random effects meta-analysis of the c-statistic ========================================## ################################################################################################################### est1 then Any advice will be greatly appreciated.
