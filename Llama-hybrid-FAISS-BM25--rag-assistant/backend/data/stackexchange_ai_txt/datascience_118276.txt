[site]: datascience
[post_id]: 118276
[parent_id]: 20535
[tags]: 
We need a replay buffer for 2 reasons Q-Learning updates are incremental and slow to converge. Multiple passes through the Q-function are needed for convergence. When the input is highly correlated in a neural network, the gradient is high in one direction, causing the network to overcorrect.
