[site]: stackoverflow
[post_id]: 4627632
[parent_id]: 4627472
[tags]: 
Looks like you could have a container div that would be centered, set the background-image to point to your bg image, set the proper amount of padding and would contain all other elements. It could have a relative position . Text title looks like an h1 to me. Top right image would be an image floated to the right. Your paragraph would be inside a p . Clickable image could have an absolute position with left and bottom set to the proper amount. Something like this: text title a paragraph clickable button Fiddle demo with css applied
