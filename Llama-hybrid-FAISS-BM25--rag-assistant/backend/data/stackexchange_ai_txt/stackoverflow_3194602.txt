[site]: stackoverflow
[post_id]: 3194602
[parent_id]: 
[tags]: 
How do I neatly remove nodes from an XDocument using LINQ and Lambda?

What's the most elegant way of going from this: to this: Ideally, this is a function taking a "date" parameter. The obvious creation of and looping through to add each from an expression that returns IEnumerable seems clunky so there's probably a nicer way.
