[site]: stackoverflow
[post_id]: 4780694
[parent_id]: 4779790
[tags]: 
Indeed, ThiefMaster 's answer is the most elegant one. Now just to fix your code, taking into account poke 's remarks: def end_other(a,b): s1=a.lower() s2=b.lower() if len(s1)>len(s2): if s1[-len(s2):]==s2: return True elif len(s1) I just used simple rules to simplify it: use if / elif / else block instead of 3 if s since the 3 cases are mutually exclusive s[-len(s):] is equivalent to s
