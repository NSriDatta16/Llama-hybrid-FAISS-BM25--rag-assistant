[site]: stackoverflow
[post_id]: 1019483
[parent_id]: 1019474
[tags]: 
$('input[name=SomeName]').parent().text(); If you want to hide that text, you would be best off enclosing it in a paragraph, span or label tag, so you could do something like this: $('input[name=SomeName]').parent().find('span').hide();
