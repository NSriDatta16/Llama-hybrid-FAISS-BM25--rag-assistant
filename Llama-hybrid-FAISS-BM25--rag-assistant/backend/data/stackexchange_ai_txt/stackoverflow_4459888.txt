[site]: stackoverflow
[post_id]: 4459888
[parent_id]: 
[tags]: 
OpenMP C and C++ cout/printf does not give the same output

I am a complete noob in OpenMP and just started by exploring some simple test script below. #pragma omp parallel { #pragma omp for for(int i=0;i I tried the C and C++ version and the C version seems to work fine whereas the C++ version gives me a wrong output.
