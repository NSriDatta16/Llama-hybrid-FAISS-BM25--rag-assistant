[site]: stackoverflow
[post_id]: 1200754
[parent_id]: 1200519
[tags]: 
Since you are working in Managed C++, you can access the OpenGL API directly using something like this: #pragma comment(lib, "opengl32.lib") #pragma comment(lib, "glu32.lib") #include #include This article from the code project gives an excellent overview. It provides source code for including an OpenGL rendering context in a win forms application.
