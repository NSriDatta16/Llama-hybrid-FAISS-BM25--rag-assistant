[site]: stackoverflow
[post_id]: 3904595
[parent_id]: 3903668
[tags]: 
Try turning the index on agent.status into a covering index by adding agent_id as a second component: create index idx2 on agent( status, id) This might enable the query to eliminate more of the agent rows before having to retrieve records from the data table.
