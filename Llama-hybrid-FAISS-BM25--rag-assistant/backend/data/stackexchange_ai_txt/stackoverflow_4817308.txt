[site]: stackoverflow
[post_id]: 4817308
[parent_id]: 
[tags]: 
Choice of Machine Learning Platform

I have a data set of users and their loan repayment metrics (how long they took, how many installments etc). Now I want to analyse a user's past loan history and say, "If we loan them X they will most likely repay over Y installments, over Z days" Here is my take The algorithm is a Clustering algorithm to group all users according to their repayment habits I want to use a SOM or K-Means So my question is, what platforms are good for this? I have had a look at Mahout so far.
