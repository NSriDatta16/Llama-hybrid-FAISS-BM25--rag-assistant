[site]: crossvalidated
[post_id]: 286371
[parent_id]: 286359
[tags]: 
Looks likely to be perfect separation. There's just not a lot of data in some of those blocks. And while you are fitting an additive model, which this plot doesn't quite capture, it does seem quite likely that adding that third variable is causing this. Note also how huge your estimates are. For more on perfect separation, see for example, Will the p value become useless in such case: logistic regression with perfect separation? library(ggplot2) d
