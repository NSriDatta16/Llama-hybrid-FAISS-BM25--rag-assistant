[site]: stackoverflow
[post_id]: 4270053
[parent_id]: 
[tags]: 
jQuery: Element offset() coordinates relative to another element

I'm trying to use jQuery and jQuery UI to drag a element horizontally located inside a . I'd like to get the coordinates of the buttons located inside the elements to center them inside #the_parent , but the position I get changes every time I resize the browser window, how I can get a position relative to #the_parent ? $(document).ready (function () { $(".button").click (function () { var offset = $(this).offset(); alert (offset.left); }); }); #dragme li { float:left; list-style:none; } #the_parent { width:100%; } .spacer { width:100px; } .button div { padding:5px; background-color:darkgrey; color:white; } click click
