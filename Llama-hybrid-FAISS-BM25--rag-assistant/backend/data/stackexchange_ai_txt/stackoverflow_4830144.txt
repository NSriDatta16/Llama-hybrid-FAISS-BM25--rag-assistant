[site]: stackoverflow
[post_id]: 4830144
[parent_id]: 4830061
[tags]: 
There may be better way to store data, but if you really want this can be done this way: file_put_contents($storage_file, ' This way all data will be well formatted and well escaped. Then you can retrieve the data with a code like this: $data = include $storage_file;
