[site]: stackoverflow
[post_id]: 1686959
[parent_id]: 1686803
[tags]: 
To do this correctly you need to find the width that is outside the screen (extraWidth), and then calculate the position accordingly. I would also subtract the width of the button from the stageWidth when you calculate the percentage on which the button is placed: private function checkDrag(event:Event):void { if (LISTmc.width Hope it helps you
