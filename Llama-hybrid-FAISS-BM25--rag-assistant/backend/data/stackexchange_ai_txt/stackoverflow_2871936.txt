[site]: stackoverflow
[post_id]: 2871936
[parent_id]: 2869515
[tags]: 
When you use IDropTarget, it simulates a drag&drop (XP and later, you must use DDE or whatever on older systems) As luck would have it, Raymond Chen recently did a blog post about IDropTarget with sample code.
