[site]: stackoverflow
[post_id]: 5004984
[parent_id]: 5003396
[tags]: 
It sounds like VC is flushing denormals. This can be controlled by the following: #include #pragma fenv_access (on) int main() { _controlfp(_DN_SAVE, _MCW_DN); float f = pow (2.f, -127); double d = pow (2.0, -1023); }
