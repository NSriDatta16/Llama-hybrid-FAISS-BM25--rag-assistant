[site]: stackoverflow
[post_id]: 4665231
[parent_id]: 
[tags]: 
How to specialize this template (paying attention to return type)

I'd like to specialize following template: template T item(size_t s); into something like that: template T item (size_t s) { //for all numeric types return static_cast (rand()); } template string item (size_t s) { return "asdf"; }
