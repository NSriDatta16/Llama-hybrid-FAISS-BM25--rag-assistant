[site]: stackoverflow
[post_id]: 4435653
[parent_id]: 4435635
[tags]: 
A more idiomatically C++ way of doing this (vs. your original approach of array-of-arrays) would be to have a vector of vectors, ie. std::vector > and then invoke std::sort on the top-level vector. You can pass sort a custom predicate that compares two rows based on their average.
