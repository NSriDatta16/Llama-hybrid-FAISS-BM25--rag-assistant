[site]: stackoverflow
[post_id]: 5500557
[parent_id]: 5500469
[tags]: 
You need to call this line: datepicker.updateDate(timestampCalendar.get(Calendar.DATE), timestampCalendar.get(Calendar.MONTH), timestampCalendar.get(Calendar.DATE)); When you are using the plus1 button. This will update the date picker. You could actually just move this line of code into updateDateButton(); to stop repetition.
