[site]: stackoverflow
[post_id]: 4166881
[parent_id]: 
[tags]: 
Update Input with select in CakePHP and FullCalendar

Hey all, I'm making a booking calendar for a rental house and I'm trying to make a selectable calendar update two form fields after selection. I want users to be able to drag across available days and populate the start and end date of my form. This is what I have so far: $(document).ready(function() { $('#calendar').fullCalendar({ events: "bookings/feed", selectable: true, unselectAuto: false, select: function(startDate, endDate, allDay, jsEvent, view) { **I just don't know what to put here to edit the inputs bolded below** } }); }); create('Booking'); ?> ** input('start', array( 'label' => 'Check In:', 'type' =>'text')); ?> input('end', array( 'label' => 'Check Out:', 'type' =>'text')); ?>** input('firstName'); ?> input('lastName'); ?> Cheers!
