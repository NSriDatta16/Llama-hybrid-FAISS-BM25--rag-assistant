[site]: crossvalidated
[post_id]: 298858
[parent_id]: 223983
[tags]: 
The determinant of a confusion matrix probably isn't useful as an accuracy metric. Determinants are usually used when the matrix is an operator, and confusion matrices aren't operators. Yes, they are matrices, but that's just because matrices provide a convenient way to organizing the classification information.
