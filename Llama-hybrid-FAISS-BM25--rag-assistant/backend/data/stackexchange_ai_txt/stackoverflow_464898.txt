[site]: stackoverflow
[post_id]: 464898
[parent_id]: 
[tags]: 
Debugging my windows DLL that might be invoked from a Windows Service

I have my SNMP Extension Agent DLL that is called by the Windows SNMP Service (snmp.exe) everytime i do an snmpwalk (another console application) .I want to step into my DLL code that is called from the above Windows Service. How do i go about doing that? Thanks Som
