[site]: stackoverflow
[post_id]: 4487949
[parent_id]: 4487929
[tags]: 
You want the condition to evaluate on each iteration through the loop at the end, like this: s = ''.join('%s: %s ' % (a,getattr(user, a)) for a in dir(user) if '__' not in a ) Edit: Sorry, fixed the parentheses to the appropriate nesting. Edited: Changed the conditional (didn't even pay attention to it before, thanks Falmarri.
