[site]: stackoverflow
[post_id]: 2170458
[parent_id]: 
[tags]: 
Cancel fragment caching on Exception

If I am caching an rss response like so: how could I prevent it from caching if the rss doesn't return correctly? i.e. if the fetch_rss returns an exception or returns 'Not Found'
