[site]: stackoverflow
[post_id]: 3238317
[parent_id]: 3238121
[tags]: 
Create the directory with the Finder, and drag it to XCode. In the dialog, choose «Folder reference», instead of «group». It will tell XCode it's a real directory. Finally, also ensures the directory is added to the «Copy files» build phase of your application's target.
