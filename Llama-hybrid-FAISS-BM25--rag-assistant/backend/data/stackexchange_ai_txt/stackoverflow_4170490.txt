[site]: stackoverflow
[post_id]: 4170490
[parent_id]: 
[tags]: 
How provide several options (not just one) after initialization?

Can several options (after init) be provided at once? $(' Dialog ').dialog().parent().draggable( "option", "containment", false ); If I also want scroll: false , how do I do it?
