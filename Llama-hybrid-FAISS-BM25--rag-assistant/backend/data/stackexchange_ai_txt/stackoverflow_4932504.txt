[site]: stackoverflow
[post_id]: 4932504
[parent_id]: 
[tags]: 
Refresh BitmapCache?

How do I refresh a BitmapCache in WPF? When I apply a BitmapCache, it stretches the window content I cached when I change the window size. I need to know in either C# or VB .NET.
