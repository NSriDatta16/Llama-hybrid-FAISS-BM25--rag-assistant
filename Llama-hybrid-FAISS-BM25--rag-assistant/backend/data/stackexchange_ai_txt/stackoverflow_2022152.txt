[site]: stackoverflow
[post_id]: 2022152
[parent_id]: 2019607
[tags]: 
I did some digging into source code and it seems that iText explicitly ignores BaseFont.EMBEDDED flag for certain fonts and Helvetica is one of them. Embedding probably works if you provide font file (e.g. TrueType .ttf) for Helvetica.
