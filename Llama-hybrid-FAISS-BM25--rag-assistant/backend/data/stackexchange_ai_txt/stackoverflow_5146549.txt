[site]: stackoverflow
[post_id]: 5146549
[parent_id]: 5146199
[tags]: 
You can change the integer array to a string ArrayList and then use the ArrayList in your Adapter You can change your integer array to string array to like this int storage [] = new int[db]; int stringstorage[] = new String[db]; for (int i = 0; i Now Change the stringstorage to your ArrayList List l = Arrays.asList(stringstorage); ArrayList list = new ArrayList (l); Now use the list as your need. Hope this will help you.
