[site]: stackoverflow
[post_id]: 4373533
[parent_id]: 286533
[tags]: 
Slightly more elegant is the following... using (var fileStream = new FileStream(file, FileMode.Open, FileAccess.Read)) { using (var streamReader = new StreamReader(fileStream)) { while (!streamReader.EndOfStream) { yield return reader.ReadLine(); } } }
