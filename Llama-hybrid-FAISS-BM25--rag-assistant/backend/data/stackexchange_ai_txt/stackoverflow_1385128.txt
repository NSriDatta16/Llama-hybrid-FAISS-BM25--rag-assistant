[site]: stackoverflow
[post_id]: 1385128
[parent_id]: 1385081
[tags]: 
It's possible to create an instance of the other controller and call methods on it, but you should probably reevaluate your organization first (look into helpers, modules, etc.) @my_other_controller = MyOtherController.new @my_other_controller.some_method(params[:id])
