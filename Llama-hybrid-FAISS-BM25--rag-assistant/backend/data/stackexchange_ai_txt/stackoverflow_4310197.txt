[site]: stackoverflow
[post_id]: 4310197
[parent_id]: 4309966
[tags]: 
the colornames sortable update handler should include a reinitialization of the draggable. the thing is you're adding new bars that didn't exist on document ready. so, it should look something like this: $( ".colorNames" ).sortable({ handle: '.handle', update : function () { var barsOrder = $('.gantt').sortable('serialize'); var namesOrder = $('.colorNames').sortable('serialize'); barsTemp = $(' '); colorNames = namesOrder.split('&'); for (i = 0; i
