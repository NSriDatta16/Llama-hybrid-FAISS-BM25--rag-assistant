[site]: stackoverflow
[post_id]: 1292341
[parent_id]: 1292181
[tags]: 
We only need to add $grades = new Default_Model_Grade( );//create object of grade table in my db $code = $grades->getGradesByOrganizaton( 1 );//call method of grade class $form->populate($result[0]->toArray());//fetch all rows from db and then populate in the controller file. But the form fields id must be same as defined in database.
