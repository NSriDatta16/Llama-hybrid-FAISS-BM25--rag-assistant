[site]: stackoverflow
[post_id]: 3030006
[parent_id]: 3028067
[tags]: 
The correct way to manage the caching is to adjust the server end so that correct values for HTTP headers that affect caching are sent. For example in ASP.NET you might use the CacheControl property on the Response object Response.CacheControl = HttpCacheability.NoCache;
