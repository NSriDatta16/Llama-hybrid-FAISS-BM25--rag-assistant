[site]: stackoverflow
[post_id]: 5315491
[parent_id]: 5315227
[tags]: 
Indeedy, because it seems to work fine if I do this: //wingdi.h does not set the proper pack setting, causing structures to have the wrong size due to our /Zp1! //32-bit: sizeof(BITMAP)==24 //32-bit: sizeof(BITMAP)==28 but GetObject says it wants 32, so: #ifdef _WIN64 #pragma pack(push,8) #endif # include #ifdef _WIN64 #pragma pack(pop) #endif
