[site]: datascience
[post_id]: 113020
[parent_id]: 
[tags]: 
Can ML or DL Predict output vector target?

I have output data as follows: Then I encode into : Then I convert into vector: The input of model is word embedding of sentence. My question is that: Can ML or DL return a vector output above? If possible, how can it calculate the performance?
