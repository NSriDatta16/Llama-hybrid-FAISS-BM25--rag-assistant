[site]: stackoverflow
[post_id]: 1028190
[parent_id]: 1028117
[tags]: 
However, it doesn't stop e.g. the ASP.NET MVC Team you use them as a Hashtable-style parameter object... Url.Action(new{controller="Home", action="Index"}) Which I wouldn't encourage, though, because of heavy use of magic strings. I almost exclusively use anonymous types in LINQ-statements where in the same method you iterate over the resultset. That is pretty much why they were introduced
