[site]: stackoverflow
[post_id]: 2669045
[parent_id]: 2669033
[tags]: 
There is no problem in reallocation a git repo ... just mv it. Pay attention to surrounding jobs that could use it (maybe you gave git-daemon a path to it?). But the repository itself can safely be moved around.
