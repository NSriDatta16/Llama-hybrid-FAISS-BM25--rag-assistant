[site]: stackoverflow
[post_id]: 4338116
[parent_id]: 4318183
[tags]: 
I have killed a bit of time to expand on your original message dialog button modifier. You can just cut an paste this all and it will run. //--------------------------------------------------------------------------- #include #pragma hdrstop #include "frmMsgDlg.h" #include //--------------------------------------------------------------------------- #pragma package(smart_init) #pragma resource "*.dfm" int MsgDlg( const String Msg, TMsgDlgType DlgType, TMsgDlgButtons Buttons, TStringList* Captions); TForm1 *Form1; //--------------------------------------------------------------------------- __fastcall TForm1::TForm1(TComponent* Owner) : TForm(Owner) { } //--------------------------------------------------------------------------- int MsgDlg( const String Msg, TMsgDlgType DlgType, TMsgDlgButtons Buttons, TStringList* Captions) { TForm* aMsgDlg = CreateMessageDialog(Msg, DlgType, Buttons); int i, j; TButton *DlgButton; aMsgDlg->Color = clWindow; for (i = 0; i ComponentCount - 1; i++) { DlgButton = dynamic_cast (aMsgDlg->Components[i]); if (DlgButton) { switch (DlgButton->ModalResult) { case mrOk: j = Captions->IndexOfObject(reinterpret_cast (mbOK)); break; case mrCancel: j = Captions->IndexOfObject(reinterpret_cast (mbCancel)); break; case mrAbort: j = Captions->IndexOfObject(reinterpret_cast (mbAbort)); break; case mrRetry: j = Captions->IndexOfObject(reinterpret_cast (mbRetry)); break; case mrIgnore: j = Captions->IndexOfObject(reinterpret_cast (mbIgnore)); break; case mrYes: j = Captions->IndexOfObject(reinterpret_cast (mbYes)); break; case mrNo: j = Captions->IndexOfObject(reinterpret_cast (mbNo)); break; case mrAll: j = Captions->IndexOfObject(reinterpret_cast (mbAll)); break; case mrNoToAll: j = Captions->IndexOfObject(reinterpret_cast (mbNoToAll)); break; case mrYesToAll: j = Captions->IndexOfObject(reinterpret_cast (mbYesToAll)); break; case mrClose: j = Captions->IndexOfObject(reinterpret_cast (mbClose)); break; default: j = -1; } if (j != -1) { DlgButton->WordWrap = true; DlgButton->Caption = Captions->Strings[j]; DlgButton->Width = 56; DlgButton->Height = 28; DlgButton->Cancel = false; } } } return aMsgDlg->ShowModal(); } //--------------------------------------------------------------------------- void __fastcall TForm1::FormShow(TObject *Sender) { int rc; std::auto_ptr capt(new TStringList()); capt->AddObject("mbYes", reinterpret_cast (mbYes)); capt->AddObject("mbNo", reinterpret_cast (mbNo)); capt->AddObject("mbOK", reinterpret_cast (mbOK)); capt->AddObject("mbCancel", reinterpret_cast (mbCancel)); capt->AddObject("mbAbort", reinterpret_cast (mbAbort)); capt->AddObject("mbRetry", reinterpret_cast (mbRetry)); capt->AddObject("mbIgnore", reinterpret_cast (mbIgnore)); capt->AddObject("mbAll", reinterpret_cast (mbAll)); capt->AddObject("mbNoToAll", reinterpret_cast (mbNoToAll)); capt->AddObject("mbYesToAll", reinterpret_cast (mbYesToAll)); capt->AddObject("mbClose", reinterpret_cast (mbClose)); ::MsgDlg("Tre tentime dështuan, ju lutem startoni programin nga ikona!!!", mtInformation, TMsgDlgButtons()
