[site]: stackoverflow
[post_id]: 2925354
[parent_id]: 2925332
[tags]: 
Edit: Now I notice that you're getting the "if prefix should be there or not" from somewhere else, in which case my original solution is probably a bit less elegant. Instead, what I'd suggest is something like this: $parts = explode('=', $str); $key = array_shift($parts); if($has_prefix) { $key .= '=' . array_shift($parts); } $val = implode('=', $parts); . My original response: $parts = array_reverse(explode('=', $str)); $val = array_shift($parts); $key = implode('=', array_reverse($parts));
