[site]: stackoverflow
[post_id]: 4715013
[parent_id]: 4714814
[tags]: 
Here's the sort of approach I'd take. ... // read result from executed query while ( row = query.NextRow() ) { LibClass *ki = new LibClass; ki->email = new (string); //Needed to create storage // That's all the setup you need on your object //Here's what I'd do differently ki->email = get_column_data(ki->email, "email"); ki->landId = get_column_data(ki->landId, "landId"); ... } template void get_column_data(T target, string column_name){ //Code to grab column data based on target type and column name } ...
