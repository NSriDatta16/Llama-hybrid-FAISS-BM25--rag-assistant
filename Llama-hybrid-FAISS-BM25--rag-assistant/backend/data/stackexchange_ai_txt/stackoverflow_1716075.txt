[site]: stackoverflow
[post_id]: 1716075
[parent_id]: 1715918
[tags]: 
I think a more elegant way is to write a superclass of URLLoader like: class MyURLLoader extends URLLoader { public var book:String; function MyURLLoader(aBook:String){ book = aBook; super(); } } And then use this class in your loop, like: var gospels : Array = ["john", "mark", "matthew", "paul"]; for each (var book : String in gospels) { var loader : MyURLLoader = new MyURLLoader(book); loader.load(new URLRequest("http://example.com/" + name)); trace(book) // outputs current value of array loader.addEventListener(Event.COMPLETE, function(e : Event) : void { var myUrlLoader:MyURLLoader = e.target as MyURLLoader; trace (myUrlLoader.book); }); } P.S. I'm currently not behind a computer with flash so I did not have the opportunity to check the code.
