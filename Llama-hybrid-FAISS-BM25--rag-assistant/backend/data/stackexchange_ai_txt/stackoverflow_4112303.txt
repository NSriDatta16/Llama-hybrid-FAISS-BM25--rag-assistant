[site]: stackoverflow
[post_id]: 4112303
[parent_id]: 4112225
[tags]: 
I guess it is because they control different aspects of encoding: The -C flag controls the default encoding of input and output streams read or written by the program. The use utf8 pragma tells the Perl parser if the source code is UTF-8. This can also be detected by a BOM in the source file.
