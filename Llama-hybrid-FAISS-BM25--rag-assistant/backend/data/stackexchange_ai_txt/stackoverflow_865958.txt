[site]: stackoverflow
[post_id]: 865958
[parent_id]: 865891
[tags]: 
The speediest solution would be to have a hidden block with the button/drag handle and whenever you mouse-over your element you .show() that div and position it accordingly (with css) .append() 'ing and removing html code on each mouse-over is certainly possible but will most definitely have some performance hit.
