[site]: stackoverflow
[post_id]: 2764520
[parent_id]: 
[tags]: 
Not enough arguments for format string

I have such code in Python: def send_start(self, player): for p in self.players: player["socket"].send_cmd(' '%(self.players.index(p)+1, p['name'], int(player["pid"]==p["pid"]), 0)) player["socket"].send_cmd(' '%(self.turnnow)) player["socket"].send_cmd(" ") And the error is in the title of this post. What's wrong?
