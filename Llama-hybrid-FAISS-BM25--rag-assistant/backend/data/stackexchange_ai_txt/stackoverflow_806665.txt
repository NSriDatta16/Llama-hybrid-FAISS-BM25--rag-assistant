[site]: stackoverflow
[post_id]: 806665
[parent_id]: 806647
[tags]: 
Presumably from the parser. I would say a stack trace would be helpful, but Xerces/Xalan has a tendency for screwing those up. Obvious steps to take: Try looking at the result as a string. Try parsing with a parser, ignoring the transformer for now. Try finding out exactly what the error was.
