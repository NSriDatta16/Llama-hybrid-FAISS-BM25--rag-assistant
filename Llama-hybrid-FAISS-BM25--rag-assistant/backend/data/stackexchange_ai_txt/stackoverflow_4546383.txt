[site]: stackoverflow
[post_id]: 4546383
[parent_id]: 4546356
[tags]: 
If you read the rest of that paragraph, you will see: However, nowadays one should never use mknod() for this purpose; one should use mkfifo(3), a function especially defined for this purpose. so what POSIX.1-2001 says here is already outdated. I would just ignore it. mknod is still being used by the init scripts in the Linux systems.
