[site]: stackoverflow
[post_id]: 3989200
[parent_id]: 3988470
[tags]: 
To answer your first question about gcc . No, it seems that gcc easily accepts unsigned or size_t loop variables in something like #pragma omp parallel for for (size_t i = 0; i at least mine (gcc v 4.4 on a 64bit ubuntu) doesn't complain and does the right thing.
