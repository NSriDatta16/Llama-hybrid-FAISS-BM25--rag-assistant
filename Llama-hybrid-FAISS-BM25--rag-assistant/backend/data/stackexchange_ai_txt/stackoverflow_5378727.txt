[site]: stackoverflow
[post_id]: 5378727
[parent_id]: 
[tags]: 
Get row total price from a configurable product in Magento

I'm writing a payment module, and after the checkout process I'm doing the following to get the price for each product. foreach($order->getAllVisibleItems() as $item) { //... $price = (float)$item->getData('row_total_incl_tax'); //... Afaik it works with any kind of product except configurable ones. How can I retrieve the row total for a configurable product?
