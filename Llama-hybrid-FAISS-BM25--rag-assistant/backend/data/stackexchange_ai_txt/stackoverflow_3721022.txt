[site]: stackoverflow
[post_id]: 3721022
[parent_id]: 
[tags]: 
jQuery Error regarding .text

$(document).ready(function() { $('.taskStatus').text(function(n, oldcontent) { if (oldcontent == "Done") return " Click Here "; }); }); Done Why does the above code display the following on my webpage Screenshot below: Browser: Chrome 6.0.472.55 jQuery: 1.4.1 Edit: Apparently, it works for p (paragraph) tags, but not for divs and spans. Edit: Using mcgrailm's answer, I ended up using the following code: $('.taskStatus').each(function() { if ($(this).text() == 'Done') $(this).html(" Click Here "); });
