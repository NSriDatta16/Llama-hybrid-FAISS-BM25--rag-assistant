[site]: crossvalidated
[post_id]: 108102
[parent_id]: 
[tags]: 
Logistic regression and error terms

In logistic regression, if we considered residuals, could they only take on the values $0$ or $1$? The data points themselves take on only $1$ or $0$. The logistic curve can take on any value between $0$ and $1$. What would the distribution of the residuals look like?
