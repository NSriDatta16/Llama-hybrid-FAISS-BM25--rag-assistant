[site]: stackoverflow
[post_id]: 5630908
[parent_id]: 
[tags]: 
multiple belongs_to relationship to three model

The situation is this way.. class Organization :role_memberships, :uniq => true end class User has_many :role_memberships has_many :organizations, :through => :role_memberships, :uniq => true has_many :roles, :through => :role_memberships, :uniq => true end class RoleMembership :role_memberships, :uniq => true end The QUESTION is how do I populate all the three foreign-keys in rolemembership table..when I do org.users.push(u) this create a record but role_id is left out...
