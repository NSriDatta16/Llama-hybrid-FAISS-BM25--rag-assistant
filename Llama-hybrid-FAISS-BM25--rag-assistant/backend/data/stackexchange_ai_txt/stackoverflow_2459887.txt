[site]: stackoverflow
[post_id]: 2459887
[parent_id]: 2459859
[tags]: 
pragmas are almost all compiler-specific. GCC doesn't implement init , but you can get the same effect using the constructor function attribute: static __attribute__((constructor)) void my_init() { //do-something } There's also a corresponding destructor attribute.
