[site]: stackoverflow
[post_id]: 1349716
[parent_id]: 1349694
[tags]: 
I'm not saying this is an elegant solution, but you could try doing something like this: select name from customers order by replace(name, '_', '-+') asc; Just replace the + with whatever will put the prefixed underscore values after the dash.
