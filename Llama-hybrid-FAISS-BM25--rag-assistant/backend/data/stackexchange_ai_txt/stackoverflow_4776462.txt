[site]: stackoverflow
[post_id]: 4776462
[parent_id]: 4776426
[tags]: 
You have to add this permission: By the way, not sure how you are getting the SDcard directory... but it must be this way: File sdDir = Environment.getExternalStorageDirectory(); It's important, because some programmers use to think that the SDCard is always /sdcard, but it could change.
