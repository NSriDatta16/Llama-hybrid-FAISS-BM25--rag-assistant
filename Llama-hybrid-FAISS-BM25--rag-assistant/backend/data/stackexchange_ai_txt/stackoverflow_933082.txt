[site]: stackoverflow
[post_id]: 933082
[parent_id]: 933077
[tags]: 
The line: int returnNunmber = RandomNumber(1, 10); does nothing, because in the next line returnNumber is used as an output variable and will be whatever number is in textBox1. Remove the int.TryParse(textBox1.Text, out returnNumber); line and it will add a random number from 1 to 10 to your listbox. EDIT:::: To answer you additional question, try: private void btnOk_Click(object sender, EventArgs e) { string yourNumber; yourNumber = textBox1.Text.Trim(); int returnNumber; int.TryParse(textBox1.Text, out returnNumber); if( returnNumber 10) { returnNumber = RandomNumber(1, 10); } listBox1.Items.Add(returnNumber); }
