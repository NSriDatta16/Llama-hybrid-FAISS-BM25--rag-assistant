[site]: stackoverflow
[post_id]: 1450865
[parent_id]: 
[tags]: 
magento price getPrice() value

function getDescriptionHtml($tpl, $p){ $out = ""; $pr = $p["product"]; if(Mage::getStoreConfig('featuredproducts/displayoptions/title') == 'description'){ $out .= " {$pr->getName()} "; } $out .= " {$p['description']} "; $out .= " ". (Mage::getStoreConfig('featuredproducts/displayoptions/price') ? " {$pr->getPrice()} " : "" ) ."". (Mage::getStoreConfig('featuredproducts/displayoptions/bnb') ? " {$tpl->__('Buy Now')} " : "") ." "; return $out; } Per the code shown, when I'm using $pr->getPrice() its output looks like 299.0000, but I want it to be like 299.00. How can I do this?
