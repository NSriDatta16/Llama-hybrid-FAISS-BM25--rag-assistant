[site]: datascience
[post_id]: 61045
[parent_id]: 
[tags]: 
Cat2Vec embedding a categorical value columns with respect to multiple y's

I try to do some embeddings on categorical columns using Keras. Here is the code: embedding_size = 7 model = k.models.Sequential() model.add(k.layers.Embedding(input_dim = 13, output_dim = embedding_size, input_length = 1, name="embedding")) model.add(Flatten()) model.add(Dense(50, activation="relu")) model.add(Dense(15, activation="relu")) model.add(Dense(1)) model.compile(loss = "mse", optimizer = "adam", metrics=["accuracy"]) model.fit(x = df['Release Cause'].as_matrix(), y=df['Jitter'].as_matrix() , epochs = 50, batch_size = 4) Here y is only one column of the dataset. I want to do embedding with respect to multiple columns. Is it possible and how to achieve it.
