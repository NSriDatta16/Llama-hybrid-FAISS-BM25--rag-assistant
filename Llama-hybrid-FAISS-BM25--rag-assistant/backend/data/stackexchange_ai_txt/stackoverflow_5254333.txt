[site]: stackoverflow
[post_id]: 5254333
[parent_id]: 5254276
[tags]: 
The page URL query-string cannot be modified without a new request, but its " fragment identifier " (the part after the #) can, with javascript (thus Ajax). You can even leverage that using ScriptManager's EnableHistory property. and protected void Button_Click(object sender, EventArgs e) { //Do something. // ... //Then "remember the state". ScriptManager1.AddHistoryPoint("SomeState", "SomeStateValue"); } protected void ScriptManager1_Navigate(object sender, HistoryEventArgs e) { //Get the state back. String statevalue = e.State["SomeState"]; // ... }
