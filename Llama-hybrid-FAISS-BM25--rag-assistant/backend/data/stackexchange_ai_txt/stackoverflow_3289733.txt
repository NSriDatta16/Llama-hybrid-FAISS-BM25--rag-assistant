[site]: stackoverflow
[post_id]: 3289733
[parent_id]: 3289676
[tags]: 
You should suppress line breaks in erb output by adding a minus sign before the closing tags. $("# ").append(" " + $(ui.draggable).text() + " "); $("# SPAN.draggable").draggable({ appendTo: 'body', helper: 'clone', scroll: false, drag: function() { $('# ').val("DimensionBox"); } }); Always have a look at the JavaScript console. It's a great tool to find bugs.
