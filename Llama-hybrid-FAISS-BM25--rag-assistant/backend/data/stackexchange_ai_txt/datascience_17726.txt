[site]: datascience
[post_id]: 17726
[parent_id]: 17640
[tags]: 
Yes, I am asking about Backpropagation algorithm. I want to understand how the weights get assigned to the neural network. If we want to assign a weight matrix to the neural network, how will we assign it?
