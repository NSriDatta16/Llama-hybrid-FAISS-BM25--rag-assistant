[site]: stackoverflow
[post_id]: 4681028
[parent_id]: 4680136
[tags]: 
Found it, you want "IsEqual". Did this by accessing list item using powershell as described. $web = get-spweb(weburl); $list = $web.lists["Content Organizer Rules"]; $list.items[0] >> c:\arule.txt You can then look in the file for the condition.
