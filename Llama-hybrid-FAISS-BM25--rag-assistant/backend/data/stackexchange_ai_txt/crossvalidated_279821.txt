[site]: crossvalidated
[post_id]: 279821
[parent_id]: 
[tags]: 
What are the benefits of using CNNs for NLP?

In computer vision, CNNs have definitely proven themselves useful in learning feature maps for tasks such as image classification. When applied to images, the role of convolutional and max-pooling layers is easily interpretable as image filtering and aggregation. However, what does it mean to apply CNNs for textual data? What is the interpretation of CNN layers in this case?
