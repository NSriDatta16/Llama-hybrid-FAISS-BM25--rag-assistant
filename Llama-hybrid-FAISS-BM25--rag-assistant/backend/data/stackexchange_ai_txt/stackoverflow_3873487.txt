[site]: stackoverflow
[post_id]: 3873487
[parent_id]: 3873377
[tags]: 
you can use the object cache class (framework 4) according ms documentation is defined this way: public abstract class ObjectCache : IEnumerable>, IEnumerable The class belogs to System.Runtime.Caching. In order to Use it you have inherit it into a concrete implementation. This object works in the same way the httpcache application works, so you can define lets say a sliding expiration and/also an absolute expiration time as well
