[site]: stackoverflow
[post_id]: 4409590
[parent_id]: 4408222
[tags]: 
applying for data frame on row converts numeric to character if one or more column of the data frame is character. So easy workaround is to convert again in vol: vol This is not elegant. I can not recall just now the more elegant way, probably using d*ply or something... Probably this is more elegant: library(plyr) vol
