[site]: stackoverflow
[post_id]: 4221334
[parent_id]: 4219600
[tags]: 
Being a pragmatic language, D has the ability to cast away const if you really need to. I think the following should work: class M { bool set; real val; real D() const { if(!set) { M m = cast(M)this; m.val = this.some_fn(); m.set = true; } return this.val; } }
