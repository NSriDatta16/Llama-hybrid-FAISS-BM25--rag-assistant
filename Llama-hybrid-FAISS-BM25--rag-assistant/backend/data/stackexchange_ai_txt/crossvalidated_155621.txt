[site]: crossvalidated
[post_id]: 155621
[parent_id]: 
[tags]: 
Logistic Regression: How to know if we lack of predictors?

In the variable selection, we only have the different methods for excluding the explanatory variables. But after all, how can we know if we need to search for a new predictor to add into the model or not?
