[site]: stackoverflow
[post_id]: 3134733
[parent_id]: 3134681
[tags]: 
Return 0 from the handleDragon method if the dragon slayer is available: int handleDragon() { if (dragonSlayer.on_vacation()) { cout Then back in the monster method, if the return value was greater than zero, return that value, otherwise carry on: // some statements ... int handleDragonResult = handleDragon(); if (handleDragonResult > 0) { return handleDragonResult; } // rest of long method... You should also document the handleDragon method, to explain the value that gets returned.
