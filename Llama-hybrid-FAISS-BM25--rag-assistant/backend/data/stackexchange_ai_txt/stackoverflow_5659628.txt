[site]: stackoverflow
[post_id]: 5659628
[parent_id]: 
[tags]: 
HTML5 Local Storage vs JSON Object

Are there performance benefits to storing in Local Storage over a JSON object in js? I would assume that it takes (slightly) longer to access, but bogs down the browser (slightly) less. Any thoughts?
