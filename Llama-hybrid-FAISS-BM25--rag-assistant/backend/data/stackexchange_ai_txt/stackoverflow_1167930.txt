[site]: stackoverflow
[post_id]: 1167930
[parent_id]: 1167852
[tags]: 
I don't think you can multiply a matrix by itself in-place. for (i = 0; i Even if you use a less naïve matrix multiplication (i.e. something other than this O(n 3 ) algorithm), you still need extra storage.
