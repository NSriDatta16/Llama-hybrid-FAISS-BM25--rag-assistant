[site]: stackoverflow
[post_id]: 1508065
[parent_id]: 1487684
[tags]: 
Starting with Windows Vista a new memory manager is enabled by default, called the low fragmentation heap (m2). MS Description For Windows XP you can enable the low fragmentation heap using the following code: HANDLE heaps[1025]; DWORD nheaps = GetProcessHeaps((sizeof(heaps) / sizeof(HANDLE)) - 1, heaps); for (DWORD i = 0; i
