[site]: crossvalidated
[post_id]: 288413
[parent_id]: 288412
[tags]: 
There may be a lot of models that uses softmax function. The simpler one is the multinomial logistic regression . Given an input vector, this model assigns a score to each class : $$ score(X_i,class_k)=\beta_k \cdot X_i$$ These score are then converted to probabilities using a softmax layer
