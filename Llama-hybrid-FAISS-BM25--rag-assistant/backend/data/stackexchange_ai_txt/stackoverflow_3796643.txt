[site]: stackoverflow
[post_id]: 3796643
[parent_id]: 3796344
[tags]: 
The obvious simpler way would be to use std::vector: #include #include #include #include #include #include int main() { std::vector test; std::ifstream reader("in.txt"); // read the data into the vector: std::copy(std::istream_iterator (reader), std::istream_iterator (), std::back_inserter(test)); // show the data in the vector: std::copy(test.begin(), test.end(), std::ostream_iterator (std::cout, "\t")); // Just for fun, display the average of the numbers: std::cout
