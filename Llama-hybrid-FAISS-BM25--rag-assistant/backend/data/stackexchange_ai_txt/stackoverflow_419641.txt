[site]: stackoverflow
[post_id]: 419641
[parent_id]: 419575
[tags]: 
You should read up on the TcpClient class, network coding isn't nearly as drag and drop as it was in vb6, but you do have a lot more control over the protocol. Also, there are quite a bit of WinSock clone libraries out there that act and interface about the same as the vb6 equivalent.
