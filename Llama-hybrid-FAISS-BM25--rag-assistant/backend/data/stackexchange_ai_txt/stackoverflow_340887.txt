[site]: stackoverflow
[post_id]: 340887
[parent_id]: 340881
[tags]: 
There's always the User Agent string. But that means keeping up with all the latest browsers, and differentiating between them isn't always simple. Instead of redirecting to a completely different page, try using a media selector in your style sheets to tweak your normal content for the smaller screen.
