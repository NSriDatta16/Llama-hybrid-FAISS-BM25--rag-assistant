[site]: stackoverflow
[post_id]: 5595209
[parent_id]: 5595157
[tags]: 
Since they abstracted away the data retrieval from the data itself. While for a sqlite data source, data is on the phone, you can easily implement a content provider which takes its data from a cloud service. So you can code against the same interface and easily switch your data retrieval implementation.
