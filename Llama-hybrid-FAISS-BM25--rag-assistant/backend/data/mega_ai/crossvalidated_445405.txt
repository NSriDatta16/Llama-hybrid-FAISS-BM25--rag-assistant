[site]: crossvalidated
[post_id]: 445405
[parent_id]: 445250
[tags]: 
It doesn't have or need a target variable, because it is just used as a component of the larger transformer model. When the transformer model is trained (on say a translation task, where the target variable is the target sentence), all the parameters, including those of the feed-foward network will also be trained.
