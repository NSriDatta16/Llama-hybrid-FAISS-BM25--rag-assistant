[site]: datascience
[post_id]: 32521
[parent_id]: 32478
[tags]: 
Yes you can do sensor fusion, given that you have access to ground truth during training. You should not try to model the errors with your RNN. Instead you should directly build a MISO RNN model (multiple inputs -> your sensors | single output -> the estimation of ground truth).
