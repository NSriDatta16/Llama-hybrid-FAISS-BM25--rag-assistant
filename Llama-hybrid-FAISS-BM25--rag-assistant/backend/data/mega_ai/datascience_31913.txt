[site]: datascience
[post_id]: 31913
[parent_id]: 8120
[tags]: 
It should be noted that XGBoost makes optimal splits assuming it has access to the entire dataset, so you would likely be losing out on some predictive power by updating rather than retraining (though of course this may be worth the lessened computational cost). See an interesting discussion on this XGBoost github issue
