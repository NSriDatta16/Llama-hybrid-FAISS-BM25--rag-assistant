[site]: stackoverflow
[post_id]: 669778
[parent_id]: 669745
[tags]: 
Mechanize cannot handle javascript, so basically you have two options: use scrubyt and firewatir: it's a way to script your browser (so Firefox handles the javascript part) manually check the base url and dynamically add the page number something like: base_url = 'http://example.com/gvw_offcies&page=' links.each do |link| page_number = ... #get the page number from link agent.get base_url+page_number end
