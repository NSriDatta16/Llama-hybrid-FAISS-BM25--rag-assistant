[site]: crossvalidated
[post_id]: 409986
[parent_id]: 409971
[tags]: 
As a pragmatic solution, for each bootstrapped dataset, Fit the regression model Calculate the predicted values at every desired observation level Calculate the bootstrap interval estimates for each set of predictions at an observation level Voilà As always, R code seems to convince people more than anything any more so a small working example: set.seed(123) x gives
