[site]: stackoverflow
[post_id]: 4663400
[parent_id]: 3038770
[tags]: 
You're genius but also when retrieving the new directories you have to combine their path with the initial directory. // Add all directories at this directory foreach (string directoryName in isolatedStorageFile.GetDirectoryNames(directoryPath)) { stack.Push(Path.Combine(dir,directoryName)); }
