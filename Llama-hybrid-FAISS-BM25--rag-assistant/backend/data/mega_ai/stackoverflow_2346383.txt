[site]: stackoverflow
[post_id]: 2346383
[parent_id]: 
[tags]: 
Chrome doesn't render span in div properly

When a span is nested in a div with a different background there's a small gap above and below it. FF doesn't render like that. Here is the html : Nested Can you see that magenta line ? Does anyone has experienced this ? Thanks PS: I'm running chrome 5.0.307.9 beta under Xubuntu 9.10
