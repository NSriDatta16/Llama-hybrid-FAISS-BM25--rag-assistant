[site]: stackoverflow
[post_id]: 4673808
[parent_id]: 4673787
[tags]: 
You could simply invert the whole expression: if (!($is_private && 'root' != $role)) Or you solve it using De Morgan’s laws to this: !($is_private && 'root' != $role)) !$is_private || !('root' != $role) !$is_private || 'root' == $role
