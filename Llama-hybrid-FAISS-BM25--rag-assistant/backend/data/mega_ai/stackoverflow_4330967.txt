[site]: stackoverflow
[post_id]: 4330967
[parent_id]: 4330795
[tags]: 
Why don't try contributed modules instead? A more elegant solution I think will be using the CCK module which provides the ability of adding new fields to content types (as well as creating new content types), you can install then the Date module provides you with the ability to attach date fields to content types.
