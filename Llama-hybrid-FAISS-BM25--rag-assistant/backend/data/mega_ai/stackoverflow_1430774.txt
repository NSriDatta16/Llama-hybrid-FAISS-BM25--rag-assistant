[site]: stackoverflow
[post_id]: 1430774
[parent_id]: 1430757
[tags]: 
Definitely not as elegant as Python, but nothing quite is as elegant as Python in C++. You could use a stringstream ... #include //... std::stringstream ss; for(size_t i = 0; i You could also make use of std::for_each instead.
