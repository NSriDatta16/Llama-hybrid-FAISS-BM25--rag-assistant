[site]: stackoverflow
[post_id]: 2143350
[parent_id]: 2037151
[tags]: 
FYI this actually does not average or pick the dominant color. doing it this way is relying on flash to shrink the image down to one pixel and then decide which color to use. I'm not sure how this color is chosen, but through tests it is NOT the average NOR is it the dominant color.
