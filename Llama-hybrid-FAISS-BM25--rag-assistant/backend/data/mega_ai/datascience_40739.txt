[site]: datascience
[post_id]: 40739
[parent_id]: 36431
[tags]: 
LightGBM is the answer you are looking for. It uses less memory than xgboost, catboost and the fastest GBM library. Also it has a RF mode which is faster than sklearn implementation. It works on both R and Python. Also it has some parameters to control memory management. You can check this benchmark repo.
