[site]: stackoverflow
[post_id]: 4213227
[parent_id]: 4213181
[tags]: 
Use jQuery's post method: $.post( '/url/to/post/login/details/to/', $('#login-form').serilaize(), function(data) { // hide the existing div and add a paragraph containing the data // returned form the server $('#some-div') .hide() .after(' ' + data + ' '); }); assuming that data just contains the logged in users username.
