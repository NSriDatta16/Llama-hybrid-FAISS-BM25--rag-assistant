[site]: stackoverflow
[post_id]: 3789763
[parent_id]: 3789678
[tags]: 
for small size all containers are pretty efficient; just use set unless you know that you have a performance problem in your case using vector trades functionality (sorting, uniqueness) for storage size using set does the opposite If you need sorting and uniqueness then choose the container with that feature unless you are sure its a bad trade
