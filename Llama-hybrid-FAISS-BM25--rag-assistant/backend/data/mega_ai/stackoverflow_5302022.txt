[site]: stackoverflow
[post_id]: 5302022
[parent_id]: 
[tags]: 
Convert this var to a list of longs?

I have a screen attached here and I need to know how I can convert the list to a list of Longs? var x = SchedulerMatrixStorage.Resources.Items.Select(col => col.Id); Where, Id is of type Object
