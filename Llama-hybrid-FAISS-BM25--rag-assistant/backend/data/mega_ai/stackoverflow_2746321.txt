[site]: stackoverflow
[post_id]: 2746321
[parent_id]: 2744660
[tags]: 
Here the solution I came up with. If there's a more elegant way, let me know. :-) Test URL "); } if(!$("#testpath_"+value).length) { //add field 2 $("#fieldcontainer").append(" TEST PATH "); } }); //remove all not selected fields var preg = ''; $.each(values, function(k, v) { preg = preg + " #testurl_"+v + ","; preg = preg + " #testpath_"+v + ","; }); //remove trailing comma preg = preg.slice(0,preg.length-1); $("#fieldcontainer > :not("+preg+")").remove(); } $("#testselector").change(changeVals); changeVals(); }); //-->
