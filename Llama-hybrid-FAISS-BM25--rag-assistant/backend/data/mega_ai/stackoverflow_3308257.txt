[site]: stackoverflow
[post_id]: 3308257
[parent_id]: 
[tags]: 
Array unique values

While dealing with JSON returned from YQL, I found myself looking for a way extract all unique values from an array. function uniqueArrayValues(o){ var items = o.query.results.row, output = []; function check(val){ for(var c=0; c The code looks a bit too 'busy' and i was wondering if there is a more elegant way of extracting unique values from an array.
