[site]: stackoverflow
[post_id]: 5292463
[parent_id]: 
[tags]: 
Something in this function is crashing my program!

Howdy, When my program begins executing the code in case N of the switch statement it crashes. I am not sure why. Anyone care to take a gander? Code: (I suspect the problem is occurring somewhere in case N of the switch statement) #include "header.h" void findSeats(int& FC_Row, int& FC_Col, int& EconRow, int& EconCol, int& ticketNum, int& rowNum, char& ticketType, char& seatType, int airplane[][6]) { int aisle, col; char letterCol; if (ticketType = 'F') { switch (seatType) { case 'W': if (airplane[rowNum - 1][0] == 0) { airplane[rowNum - 1][0] = 1; cout > rowNum; while (rowNum > (FC_Row)) { cout > rowNum; } findSeats(FC_Row, FC_Col, EconRow, EconCol, ticketNum, rowNum, ticketType, seatType, airplane); } break; case 'A': aisle = (FC_Col / 2); if (airplane[rowNum - 1][aisle - 1] == 0) { airplane[rowNum - 1][aisle - 1] = 1; cout > rowNum; while (rowNum > (FC_Row)) { cout > rowNum; } findSeats(FC_Row, FC_Col, EconRow, EconCol, ticketNum, rowNum, ticketType, seatType, airplane); } break; case 'N': col = 0; while (airplane[rowNum - 1][col] == 1) { for (col; airplane[rowNum - 1][col]; col++) { if (col > 3) { cout > rowNum; } while (rowNum > (FC_Row)) { cout > rowNum; } ticketType, seatType, airplane); } } airplane[rowNum - 1][col] = 1; switch (col) { case 0: letterCol = 'A'; break; case 1: letterCol = 'B'; break; case 2: letterCol = 'C'; break; case 3: letterCol = 'D'; break; } cout removing the function call to itself solved the issue.
