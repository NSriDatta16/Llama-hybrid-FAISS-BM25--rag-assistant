[site]: stackoverflow
[post_id]: 5005512
[parent_id]: 
[tags]: 
Magento - adding a widget instance to a template file

I know that you can call a cms block directly from a template file using the following: getLayout()->createBlock('cms/block')->setBlockId('footer_links')->toHtml() ?> Is there some way to do this with widget instances?
