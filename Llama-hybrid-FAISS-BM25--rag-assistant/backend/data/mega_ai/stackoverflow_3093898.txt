[site]: stackoverflow
[post_id]: 3093898
[parent_id]: 3093564
[tags]: 
Every 5.02 ms (~200Hz) according to this page (which has loads of great info on it, mostly in links to other articles). Data from with tk.wii object is 8-bit data reported every 5.02 milliseconds on average (with a 4.90 median and 5.86 in one test). The data is represented as a 0.0-1.0 value the tilt of the controller occupying 21% of that range – tilt values for the X-and Y-axis range from 0.396 to 0.608 with sharp accelerations using the entire range. It may depend not only upon the device's capabilities, but your connection to it.
