[site]: stackoverflow
[post_id]: 5181142
[parent_id]: 
[tags]: 
remove empty div in jquery if no class or id attached

Hi I have an empty div in my code well magento's and i want to remove it via jquery and tried: $('.col-main > div:empty').remove(); $('div:not([class])'.remove(); $('div[class!=""]').remove(); also tried: $('.col-main > div[class!=""]').remove(); $('.col-main > div:not([class])'.remove(); $('.col-main > div[class!="clr"]').remove(); - im guessing here tha last two removed all my divs on the page, but none of them work. I want to keep all divs which are empty which have a class or id attached e.g. clr's.
