[site]: stackoverflow
[post_id]: 118873
[parent_id]: 118774
[tags]: 
You probably don't want to undefined near and far everywhere. But when you need to use the variable names, you can use the following to undefine the macro locally and add it back when you are done. #pragma push_macro("near") #undef near //your code here. #pragma pop_macro ("near")
