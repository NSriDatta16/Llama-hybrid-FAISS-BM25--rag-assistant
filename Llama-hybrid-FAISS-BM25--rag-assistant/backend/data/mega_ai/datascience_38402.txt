[site]: datascience
[post_id]: 38402
[parent_id]: 
[tags]: 
Should I use rescale parameters for data augmentation?

I am using Keras library to build a CNN model. I want to use data augmentation for training data. Should I use rescale parameters for data augmentation? ImageDataGenerator(rescale=1./255)
