[site]: stackoverflow
[post_id]: 3029590
[parent_id]: 3029553
[tags]: 
I'm assuming that Freeagent::Base.site is retuning 'https://XXXXX.freeagentcentral.com' . In which case, you just need to add the / into your string between the site and project parts. Try the following: Edit: Looking at this more closely, gsub(/\A\//, '') is removing a / from the start of the string returned by Freeagent::Project.element_path(@kase.freeagent_id) . Assuming that element_path does actually return a string with a leading / , the better answer would be to remove the first gsub call:
