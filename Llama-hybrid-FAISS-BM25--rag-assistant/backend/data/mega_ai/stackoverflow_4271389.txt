[site]: stackoverflow
[post_id]: 4271389
[parent_id]: 4271060
[tags]: 
Here's my take. I find it easier to understand, if not more elegant. public static IEnumerable WalkRanges (IEnumerable source, List > ranges) { if (ranges == null) return source; Debug.Assert(ranges.Count > 0); return WalkRangesInternal(source, ranges); } static IEnumerable WalkRangesInternal (IEnumerable source, List > ranges) { int currentItem = 0; var rangeEnum = ranges.GetEnumerator(); bool moreData = rangeEnum.MoveNext(); using (var sourceEnum = source.GetEnumerator()) while (moreData) { // skip over every item in the gap between ranges while (currentItem
