[site]: stackoverflow
[post_id]: 4485785
[parent_id]: 4485649
[tags]: 
I figured it out: function isInQueue(id) { var result = false; var queue = localStorage["queue_rss"]; if (queue != undefined) { var items = JSON.parse(queue).items; if (items) { for (var i = 0; i I should have done it that way in the first place. Thanks guys. :D
