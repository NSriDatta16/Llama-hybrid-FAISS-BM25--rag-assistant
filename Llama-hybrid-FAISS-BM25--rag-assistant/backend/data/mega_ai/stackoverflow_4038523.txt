[site]: stackoverflow
[post_id]: 4038523
[parent_id]: 4038250
[tags]: 
Found the answer. The clone() function works fine when the template is encapsulated within and elements. As stated below .innerHTML (which jQuery uses in clone()) does not work with table fragments. That means jQuery will not clone a if it is a root element (whose parent element is ). Therefore, the template should look like this(of course with an appropriate id on the to be selected by jQuery): Request Info Save
