[site]: stackoverflow
[post_id]: 5252393
[parent_id]: 5239117
[tags]: 
That depends from the browser, not from the API/SDK the device uses. It uses the local storage functionality of the HTML5 standard of the browser. see this example A Simple TODO list using HTML5 WebDatabases
