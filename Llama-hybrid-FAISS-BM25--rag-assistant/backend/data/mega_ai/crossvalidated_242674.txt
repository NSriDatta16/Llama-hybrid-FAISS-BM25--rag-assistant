[site]: crossvalidated
[post_id]: 242674
[parent_id]: 
[tags]: 
Difference between logistic regression and logistic neuron

What is the difference between a logistic neuron in a neural net and a logit regression? They both follow the Sigmoid function plotted below Here is the R Code for reasons of reproducibility f They are both binary classifiers Both classifiers are symmetric and give the same output for the same input. But what is the difference between the two?
