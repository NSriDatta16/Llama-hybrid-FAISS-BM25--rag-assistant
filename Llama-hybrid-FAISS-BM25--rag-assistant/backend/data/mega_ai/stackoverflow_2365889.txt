[site]: stackoverflow
[post_id]: 2365889
[parent_id]: 2365663
[tags]: 
Would it be possible to store the compile options (or, the full command line, if there is such) in a text file while invoking the build, and making sure to ship that file along with the executable? [This is certainly not elegant, but just wanted to check.]
