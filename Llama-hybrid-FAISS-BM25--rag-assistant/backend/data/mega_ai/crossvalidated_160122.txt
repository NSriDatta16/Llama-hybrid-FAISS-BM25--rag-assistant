[site]: crossvalidated
[post_id]: 160122
[parent_id]: 160118
[tags]: 
Yes, it is possible to use an SVM for such data. In fact it could even be more extreme such as this: image source An SVM handles this kind of data with an appropriate kernel which transforms the data into a higher dimensional space in which it is linearly separable.
