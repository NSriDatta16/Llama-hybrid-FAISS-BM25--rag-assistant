[site]: stackoverflow
[post_id]: 2557369
[parent_id]: 2557352
[tags]: 
One could mergesort the rows individually, and then merge the sorted rows. That would at least leverage some of the special structure of the 2D array, i.e. the fact that monotonic runs will typically start and stop at the edge of the array. It also exposes another couple levels of parallelism.
