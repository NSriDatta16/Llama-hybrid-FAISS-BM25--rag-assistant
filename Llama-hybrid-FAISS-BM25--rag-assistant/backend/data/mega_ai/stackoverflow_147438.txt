[site]: stackoverflow
[post_id]: 147438
[parent_id]: 147391
[tags]: 
In the comments, Robert Gould asked for a working version for posterity: #include #include #include #include struct bar : std::unary_function { boost::mt19937 &_state; unsigned operator()(unsigned i) { boost::uniform_int<> rng(0, i - 1); return rng(_state); } bar(boost::mt19937 &state) : _state(state) {} }; void foo(std::vector &vec, boost::mt19937 &state) { bar rand(state); std::random_shuffle(vec.begin(), vec.end(), rand); }
