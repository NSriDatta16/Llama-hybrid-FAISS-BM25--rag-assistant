[site]: stackoverflow
[post_id]: 4917750
[parent_id]: 4917677
[tags]: 
Please read the docs . In order to return a list of objects from a query that selects specific properties, you need to use an Alias To Bean transformer. Example (for SQL queries, but HQL works the same)
