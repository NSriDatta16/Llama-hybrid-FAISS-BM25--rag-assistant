[site]: stackoverflow
[post_id]: 3120647
[parent_id]: 2760162
[tags]: 
Something like this should do it: add_definitions(-DWPCAP -DHAVE_REMOTE) find_path(WINPCAP_INCLUDE_DIR pcap.h ${WINPCAP_HINT_INCLUDE_DIR}) find_library(WINPCAP_LIBRARY NAMES "wpcap" PATHS ${WINPCAP_HINT_LIB_DIR}) ... include_directories(${WINPCAP_INCLUDE_DIR}) target_link_library(my_lib ${WINPCAP_LIBRARY})
