[site]: stackoverflow
[post_id]: 672999
[parent_id]: 672927
[tags]: 
jQuery can do this very elegantly: //only when the DOM has been loaded $(document).ready(function() { //bind to button id="myButton" click event $('#myButton').click(function() { //populate div id="myDiv" with specific div (div id="someDiv") from another page $('#myDiv').load('/anotherPage.html #someDiv'); }); }); See jQuery Ajax/load
