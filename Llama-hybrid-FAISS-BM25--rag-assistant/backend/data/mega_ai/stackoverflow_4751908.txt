[site]: stackoverflow
[post_id]: 4751908
[parent_id]: 4750798
[tags]: 
If your implementation were to use DOM , I believe you could do the following: //given $node is $frag = $node->ownerDocument->createDocumentFragment(); foreach($node->childNodes as $child){ $frag->appendChild($child->cloneNode(true)); } $string = $node->ownerDocument->saveXML($frag);
