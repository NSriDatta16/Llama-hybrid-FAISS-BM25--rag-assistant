[site]: datascience
[post_id]: 120821
[parent_id]: 120807
[tags]: 
If I understand correctly, you can do this with the line target = torch.abs(torch.ceil((torch.zeros_like(x) - 0.5) - x)) with no need to train a neural network. If this isn't what you're looking for please clarify what the desired output looks like. thanks.
