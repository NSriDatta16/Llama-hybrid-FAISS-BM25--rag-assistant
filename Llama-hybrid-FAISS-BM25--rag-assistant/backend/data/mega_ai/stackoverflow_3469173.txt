[site]: stackoverflow
[post_id]: 3469173
[parent_id]: 
[tags]: 
Closing open XML tags with regex

Basically I want to do the same as here which is done in Python. I'd like to replace all self-closed elements to the long syntax. Example becomes Full example: //void //void //void swfobject.registerObject('id_G12564763'); function getFlashObject() { var object; if (navigator.appName == 'Microsoft Internet Explorer' || navigator.userAgent.indexOf("Chrome")!=-1) { object = document.getElementById('id_G12564763'); } else { object = document['flash_id_G12564763']; } return object; }
