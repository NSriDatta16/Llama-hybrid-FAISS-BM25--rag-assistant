[site]: stackoverflow
[post_id]: 4405874
[parent_id]: 4405827
[tags]: 
Found a solution that does the trick - I make every cell in my column: =ROUND(ROW()/3,0) which makes it always give 111222333 no matter how I sort the data. But if anyone has a more elegant way of creating the de-duped list from several columns I'd love to hear it. Thanks, Rob
