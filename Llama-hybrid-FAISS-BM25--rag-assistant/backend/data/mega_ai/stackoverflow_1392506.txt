[site]: stackoverflow
[post_id]: 1392506
[parent_id]: 1392393
[tags]: 
First, an shouldn't be wrapped in a paragraph - this could account for some weird behavior you're seeing. Also, the "selected" attribute might not be expressed in the HTML markup but it is being set where it counts - i.e. the DOM "selected" property. var option = $(' something '); option.removeAttr('selected').attr('selected', 'selected'); option[0].selected === true;
