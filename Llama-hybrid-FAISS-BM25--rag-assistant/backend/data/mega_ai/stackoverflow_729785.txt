[site]: stackoverflow
[post_id]: 729785
[parent_id]: 729634
[tags]: 
To answer your question (even though I agree with Marc, you shouldn't be using XML as a data storage layer). Ids and hrefs are commonly used for creating relations in XML (which is hierarchical and not relational, again, you shouldn't use XML). As a small example:
