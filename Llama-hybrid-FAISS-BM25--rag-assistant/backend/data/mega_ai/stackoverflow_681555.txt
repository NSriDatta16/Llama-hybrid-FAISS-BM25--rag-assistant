[site]: stackoverflow
[post_id]: 681555
[parent_id]: 681500
[tags]: 
Take a look at Narrange .You'll probably need to automate these things as part of the build. Not sure if it meets all your requirements though. To quote: NArrange is a .NET code beautifier that automatically organizes code members and elements within .NET classes.
