[site]: stackoverflow
[post_id]: 711991
[parent_id]: 
[tags]: 
Flatten array in PowerShell

Assume we have: $a = @(1, @(2, @(3))) I would like to flatten $a to get @(1, 2, 3) . I have found one solution : @($a | % {$_}).count But maybe there is a more elegant way?
