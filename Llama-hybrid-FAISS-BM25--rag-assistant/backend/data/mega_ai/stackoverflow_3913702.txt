[site]: stackoverflow
[post_id]: 3913702
[parent_id]: 3913525
[tags]: 
The only legal way to make a type have zero size, is to make it the base of a type that has a size. struct A { }; int cba = sizeof(A); // 1 struct B : A { int32_t b; }; int cbb = sizeof(B); // 4 - implying no storage for A.
