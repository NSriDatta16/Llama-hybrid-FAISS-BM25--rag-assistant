[site]: stackoverflow
[post_id]: 5092645
[parent_id]: 5092571
[tags]: 
You should remove 'not-editable-cell' class from the cell ( element) td.removeClass('not-editable-cell'); You should select all cells ( element) which you want make editable. I made the demo which demonstrate how to do this. The most important code fragment from the demo is var grid = $("#list"); var getColumnIndexByName = function(gr,columnName) { var cm = gr.jqGrid('getGridParam','colModel'); for (var i=0,l=cm.length; i tr.jqgrow > td:nth-child("+(pos+1)+")",gr[0]); for (var i=0; i In the demo the cells from the 'Client' column having the text "test" will be marked as "non-editable". Later one can make the cells "editable" or "non-editable" be clicking on the corresponding button.
