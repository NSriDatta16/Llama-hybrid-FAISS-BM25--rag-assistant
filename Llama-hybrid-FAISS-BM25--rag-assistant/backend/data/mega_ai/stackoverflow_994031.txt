[site]: stackoverflow
[post_id]: 994031
[parent_id]: 994005
[tags]: 
You could store their IP and User Agent string as their identifier, however it will fail in somewhat common circumstances like large intranets. You could use a cookie as an identifier, and log their last access to a page... then you could implement logic like if (dateDiff($lastAccessedTime, time()) If they don't have cookies enabled - well that's up to you.
