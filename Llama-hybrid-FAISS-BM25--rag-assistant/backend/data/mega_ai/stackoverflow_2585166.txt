[site]: stackoverflow
[post_id]: 2585166
[parent_id]: 
[tags]: 
Add handler multiple times in jquery

How does jquery handle event assignment when it comes to assigning the same handler multiple times? Let's say I have Some Text Are there any side effects (performance or otherwise) from calling the following multiple times? $('.draggable').draggable();
