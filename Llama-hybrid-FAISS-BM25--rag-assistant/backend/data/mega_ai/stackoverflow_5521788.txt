[site]: stackoverflow
[post_id]: 5521788
[parent_id]: 5521549
[tags]: 
After random position is selected and current range is partitioned we know whether k-th element is on the left side or on the right side. Then we recourse algorithm on one side. So we have complexity on average T(n) = n + T(n/2), (n for partitioning). Thus we have O(n) on average
