[site]: stackoverflow
[post_id]: 4822803
[parent_id]: 4822634
[tags]: 
I think you should do something like this: $(".postCommentDIV form").submit(function() { var comment = $(this).find(".commentBody").val(); $(' ').text(comment).appendTo('.commentsDIV .commentsBlock'); return false; }); Pay attention in using class for performance issues, better to use id to identify a unique block, as the containing the comments, so you could do .appendTo('#commentsBlock').
