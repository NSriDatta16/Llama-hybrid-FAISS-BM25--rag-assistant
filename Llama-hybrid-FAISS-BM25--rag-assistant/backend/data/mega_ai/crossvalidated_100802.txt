[site]: crossvalidated
[post_id]: 100802
[parent_id]: 100799
[tags]: 
One way to do it would be logistic regression: logit(female) ~ year + state + year*state After being sure to treat both as categorical variables. With N this large, everything will be significant, so you want to look at effect sizes (odds ratios).
