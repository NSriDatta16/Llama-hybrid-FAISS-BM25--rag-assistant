[site]: datascience
[post_id]: 122401
[parent_id]: 122400
[tags]: 
First approach would be some moving average smoothing, with something like: df.rolling(50).mean() Where the 50 is the lag and need to be tuned. More complex approach would involve filtering high frequency with a low pass filter. You might want to look at the scipy.signal filters (butter ?).
