[site]: stackoverflow
[post_id]: 5339583
[parent_id]: 5339428
[tags]: 
I solved it saving the previous left offset and comparing it with the new left offset. var previousOffset = null; $( "#draggable" ).draggable({ axis: 'x', drag: function(event,ui){ if(previousOffset == null) previousOffset = ui.offset.left; else{ if(previousOffset Here's working: http://jsbin.com/ubine3/2
