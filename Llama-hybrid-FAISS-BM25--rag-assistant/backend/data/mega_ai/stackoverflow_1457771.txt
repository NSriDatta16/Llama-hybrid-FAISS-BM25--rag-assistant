[site]: stackoverflow
[post_id]: 1457771
[parent_id]: 1457753
[tags]: 
The traditional approach is Session backed by SqlSessionStateProvider. The modern approach seems to be to use the database for storage and to use a cache (memcached) in front of the database to speed up lookups. But Session is out, as are encrypted cookies (except for the auth cookie).
