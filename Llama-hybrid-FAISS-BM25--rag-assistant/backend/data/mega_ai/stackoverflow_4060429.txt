[site]: stackoverflow
[post_id]: 4060429
[parent_id]: 4060175
[tags]: 
Try this: setTimeout(function() { $('#loading').hide(0,rotator); }, 3000); function rotator() { var $paragraphs = $('#latest-news').find('p.latest'); // the paragraphs $paragraphs.first().show(); var i = 0; var looper = setInterval(function() { var showpara = i++ % $paragraphs.length; // choose next one to show $paragraphs.not(':eq(' + showpara + ')').hide() // hide others .end() // back to first selector .eq(showpara).fadeIn(); // show the next }, 3000); } Edit: Updated to handle loading message, encapsulate rotator in a function, and trigger that function when the loading message is hidden.
