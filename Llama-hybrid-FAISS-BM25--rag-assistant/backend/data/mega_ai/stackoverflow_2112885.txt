[site]: stackoverflow
[post_id]: 2112885
[parent_id]: 2112850
[tags]: 
It depends on if you need persistent storage of that object. If you need to reference the same instance declare it as a class level variable otherwise declare it in the methods so that you can decrease coupling between your methods. Additionally if those functions are always using Class A think about whether they should belong to Class A instead of Class B
