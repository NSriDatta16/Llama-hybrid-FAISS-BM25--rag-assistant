[site]: stackoverflow
[post_id]: 3553720
[parent_id]: 3551746
[tags]: 
This may or may not apply in your case, but when I’ve needed to create routes like this, I usually try something along these lines: %w(main other).each do |uri| get "/#{uri}" do @res = "hello" @res.upcase! if uri == "other" @res end end
