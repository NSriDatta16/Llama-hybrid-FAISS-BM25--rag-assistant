[site]: stackoverflow
[post_id]: 1754736
[parent_id]: 1754712
[tags]: 
As this XSD-fragment only defines a custom simple type, what exactly do you want to know? The given type defines a string that can either be bar or baz . As the definition is only a type -definition, you'll have to use some kind of element -definition that actually uses the type , e.g.: This will allow the following tags in your XML: bar baz
