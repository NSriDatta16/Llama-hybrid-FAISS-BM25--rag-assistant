[site]: stackoverflow
[post_id]: 2515182
[parent_id]: 
[tags]: 
getElementsByClassName not working on parsed html data in greasemonkey

var xhReq = new XMLHttpRequest(); xhReq.open("GET", linksRaw, false); xhReq.send(null); var serverResponse = xhReq.responseText; var tempDiv = document.createElement('div'); tempDiv.innerHTML = serverResponse.replace(//g, ''); var plzWork = tempDiv.getElementsByClassName('organizationID').innerHTML; console.log(plzWork); The value of 'plzWork' :-) which is logged to the firebug console is always 'undefined' while the link code is Partner Organisations I'm writing this script in the latest versions of Greasemonkey and FF 3.6 Thanks
