[site]: stackoverflow
[post_id]: 2117016
[parent_id]: 2116919
[tags]: 
It's messing up because you are using the same class for everything, it doesn't look if its a child or parent or what, it only sees if you drag a level 3 item to a level 1 list that the classes are the same and that it should be able to drag that item into that list. edit: I used source code from your website: $(document).ready(function(){ $('.level-1, .level-2, .level-3').sortable({ update: function(e, ui) { update(ui.item); } }); }); var id_prefix = "id_"; function update(item) { var parentID = $(item).parent().attr('id'); var order = $('#'+parentID).sortable('serialize'); parentID = parentID.substring(id_prefix.length); } .level-2 {background-color: lime;} .level-3 {background-color: #ff6fcf;} .level-4 {background-color: #6FF;} div {margin: 5px 0; min-height: 5px; padding: 10px;} x 1017 y 1020 z 1041 i 1030 z 1021 iii 1032 1 1018 2 1029 1 1022 z 1042 iv 1033 Your php code should then look something like this getContentAssoc("","AND content.contentType='mainsection'",""); foreach($level1 as $row1){ ?> " class="level-2"> getContentAssoc($row1['contentAssocID'],"AND content.contentType='mainsection'",""); foreach($level2 as $row2){ ?> " class="level-3"> getContentAssoc($row2['contentAssocID'],"AND content.contentType='mainsection'",""); foreach($level3 as $row3){ ?> " class="level-4"> Note that I removed the name attributes, what do you need them for if the name is in the id?
