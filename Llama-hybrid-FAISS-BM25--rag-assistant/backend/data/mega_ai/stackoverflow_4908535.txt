[site]: stackoverflow
[post_id]: 4908535
[parent_id]: 4899881
[tags]: 
You could set up a continuous integration build server (there are lots of excellent, free build servers ). One of the build steps would be running code coverage. You could set it up to ignore legacy code and calculate coverage only for non-legacy code. Then you set it up to fail the build if coverage
