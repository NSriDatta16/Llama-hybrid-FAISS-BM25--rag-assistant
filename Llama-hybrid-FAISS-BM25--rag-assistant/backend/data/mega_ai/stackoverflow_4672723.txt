[site]: stackoverflow
[post_id]: 4672723
[parent_id]: 4672622
[tags]: 
Since this is a demo, can you use HTML5? If so, you can use local storage: link text . Another option is to use AJAX to load the other HTML page (replace whole body of the current document). Your storage variables would be stored in the . This is a tightly coupled design, but again you're making a quick and dirty demo.
