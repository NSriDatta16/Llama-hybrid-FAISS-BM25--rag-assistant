[site]: stackoverflow
[post_id]: 4430284
[parent_id]: 4430175
[tags]: 
Place the DIV outside the FORM element and you should be fine. Do it with script if you are unable to do it from the backend. Let the toggle callback put the DIV in place if needed, like this I presume: // show form_elm.appendChild(div_elm); div_elm.style.display = 'block'; // hide form_elm.parentNode.appendChild(div_elm); div_elm.style.display = 'block'; You can do this more elegant in your JavaScript library of choice.
