[site]: crossvalidated
[post_id]: 132603
[parent_id]: 132596
[tags]: 
If the library you're interested in supports weighted samples, use those. Here's how to weight samples in LibSVM . Most libraries I've encountered offer something similar. If the library doesn't support weighted samples, then as you say you can either replicate datapoints (ugh) or you can dig into the code and add that functionality.
