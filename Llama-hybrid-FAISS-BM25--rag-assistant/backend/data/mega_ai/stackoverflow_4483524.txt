[site]: stackoverflow
[post_id]: 4483524
[parent_id]: 4483433
[tags]: 
You need to add public static to the inner class. public because it needs to be visible from outside. static because if not you will need to have CupcakeMessage to instantiate it. All the same I would recommend doing the View in a separated class.
