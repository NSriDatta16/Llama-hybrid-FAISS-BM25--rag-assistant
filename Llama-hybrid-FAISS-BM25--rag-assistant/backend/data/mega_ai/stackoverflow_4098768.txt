[site]: stackoverflow
[post_id]: 4098768
[parent_id]: 4098751
[tags]: 
width and height are semantic in the case of img es and object s. They provide metadata for images so it is completely fine IMO to rely on those attributes in order to tell the user agent that it should presize the element to X dimensions prior to the image fully rendering.
