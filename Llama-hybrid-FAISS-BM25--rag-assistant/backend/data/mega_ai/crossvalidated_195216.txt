[site]: crossvalidated
[post_id]: 195216
[parent_id]: 
[tags]: 
What kind of pooling with non ReLU non-linearities?

in a Convolutional Neural Network it is customary to have a ReLU, then max pooling. If we would have a symmetric non-linearity, e.g. $max(w_1 x, w_2x)$, would it make sense to have a max absolute pooling instead of max pooling layer?
