[site]: crossvalidated
[post_id]: 262435
[parent_id]: 
[tags]: 
what is the minimum sample size to train RNN to do embedding?

I have about 1600 sequences, each 35 data points long (Its actually multivariate, so each data point is a vector). is this sufficient to train an RNN (LSTM) to do embedding?
