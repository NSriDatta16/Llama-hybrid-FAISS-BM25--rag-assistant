[site]: stackoverflow
[post_id]: 5085219
[parent_id]: 5083623
[tags]: 
There is a number of PHP libraries that can parse HTML, even invalid one. PHPQuery Simple HTML DOM Zend DOM Query Here is a PHPQuery example that prints all img tags appear on StackOverflow home page. find('img') as $img) { echo pq($img)->attr('src') .' '; } ?> Another example that extracts text of all paragraphs: foreach ($pq->find('p') as $p) { echo pq($p)->text() .' '; }
