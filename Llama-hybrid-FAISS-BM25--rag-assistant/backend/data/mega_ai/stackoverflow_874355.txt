[site]: stackoverflow
[post_id]: 874355
[parent_id]: 874262
[tags]: 
It looks to me like you can replace it by using the $res->content( $bytes ) member. By the way, I found this stuff by looking at the source of LWP::UserAgent, then HTTP::Response, then HTTP::Message .
