[site]: crossvalidated
[post_id]: 111642
[parent_id]: 69449
[tags]: 
When using the SVM for novelty detection the actual distribution of data does not matter. WHat the algorithm actually does, is to estimate the support of the distribution from which the data is sampled, i.e. those regions of the feature space for which $p(x) > 0$. You can use it out of the box.
