[site]: stackoverflow
[post_id]: 4661542
[parent_id]: 
[tags]: 
How do I select xml child nodes in jQuery?

Right now this code parses the XML file fine, however, in the XML file I have multiple author nodes, I'd like to be able to put a comma in between each author. The XML varies from one to from one to four authors. Thank you ahead of time. /* Load XML File */ $.ajax({ url: "xml/ajax-response-data.xml", cache: false, success: libraryXML }); function libraryXML (xml) { $(xml).find('book').each(function(){ /* Parse the XML File */ var id = $(this).attr('id'); var checked = $(this).attr('checked-out') var title = $(this).find('title').text(); var isbn = $(this).find('isbn-10').text(); var authors = $(this).find('authors').text(); /* Spit out some books */ $(' ').html(' ' + id + ' ' + title + ' ' + authors +' ' + isbn + ' ').appendTo('.library'); }); } David Flanagan JavaScript: The Definitive Guide 0596101996 John Resig Pro JavaScript Techniques (Pro) 1590597273 Erich Gamma Richard Helm Ralph Johnson John M. Vlissides Design Patterns: Elements of Reusable Object-Oriented Software 0201633612
