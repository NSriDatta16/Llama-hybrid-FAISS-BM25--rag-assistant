[site]: stackoverflow
[post_id]: 943205
[parent_id]: 
[tags]: 
intercept hyperlink of C# webbrowser

I have a webbrowser control in C#.NET CF. When the user clicks on a hyperlink, instead of attempting to navigate to the specified URL how would I instead display a fragment of html content stored in memory? I've tried the following //page doesnt refresh private void webBrowser_Navigating(object sender, WebBrowserNavigatingEventArgs e) { if (e.Url.Host != String.Empty) { e.Cancel = true; webBrowser.DocumentText = " some text "; } } //some text appears but then the original page is loaded up private void webBrowser_Navigating(object sender, WebBrowserNavigatingEventArgs e) { if (e.Url.Host != String.Empty) { webBrowser.DocumentText = " some text "; } }
