[site]: stackoverflow
[post_id]: 1632920
[parent_id]: 1632796
[tags]: 
If you look at the source code for that page, you'll see that it calls $("div, p").disableSelection(); ... which explicitly disables selection on all div s and p s on the page, including those beneath the draggable elements.
