[site]: stackoverflow
[post_id]: 4889501
[parent_id]: 4889218
[tags]: 
You could write your own manipulator . The elegance is of course debatable. It would more or less be what you've all ready proposed though. Example: struct myfloat { myfloat(float n) : _n(n) {} float n() const { return _n; } private: float _n; }; std::ostream & > result; if (result.length() > 1) return out
