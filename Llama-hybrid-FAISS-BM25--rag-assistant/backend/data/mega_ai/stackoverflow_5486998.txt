[site]: stackoverflow
[post_id]: 5486998
[parent_id]: 5486895
[tags]: 
when i==2, it will become 0 at the end of the current iteration. Then i==0, rear==0, i==rear, the loop terminates. EDIT: This works but it doesn't look elegant int i = front; for(; i != rear; i= (i + 1) % SIZE ) { System.out.println(i +":> " + queue[i]); } System.out.println(i +":> " + queue[i]);
