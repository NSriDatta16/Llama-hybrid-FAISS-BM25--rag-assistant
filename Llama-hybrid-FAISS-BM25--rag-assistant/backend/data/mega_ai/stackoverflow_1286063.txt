[site]: stackoverflow
[post_id]: 1286063
[parent_id]: 1286046
[tags]: 
The only way is to download each page and compare them. Really, this shouldn't be too much trouble, since your average HTML file is fairly small (normally well under 100KB's at the most). You don't need to download all the referenced files.
