[site]: stackoverflow
[post_id]: 3693527
[parent_id]: 3693511
[tags]: 
try randomize random generator without casting it to in /* initialize random seed: */ srand ( time(NULL) ); Holly molly... this one is not easy. I remember long time ago in my stochastic models I had this problem. The solution was to use some pragma derective, as far as I remember. anyways below is solution and it works. Random numbers every time... kind of cheezy though. #include #include #include int main(void) { double array[10]; double rand_max = RAND_MAX; srand(time(NULL)); for (int iii = 0; iii
