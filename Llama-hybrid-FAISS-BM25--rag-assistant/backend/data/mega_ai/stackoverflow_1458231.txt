[site]: stackoverflow
[post_id]: 1458231
[parent_id]: 1458215
[tags]: 
Since you have an array and want to separate the values with a , you can simply join the values: var arr = ["foo", "bar", "baz"], elem = /* refers to the paragraph */; elem.innerHTML = arr.join(" "); And with DOM methods only: var i = 0, n = arr.length; if (n) { elem.appendChild(document.createTextNode(arr[i++])); while (i
