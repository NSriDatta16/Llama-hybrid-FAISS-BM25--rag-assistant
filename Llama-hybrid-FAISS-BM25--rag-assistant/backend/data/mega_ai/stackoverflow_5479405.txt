[site]: stackoverflow
[post_id]: 5479405
[parent_id]: 5479200
[tags]: 
I do something similar in an app with code roughly along these lines. Though I am serializing objects in an array to json. Same rough idea though. using (IsolatedStorageFile file = IsolatedStorageFile.GetUserStoreForApplication()) { for (int i = 0; i Note this is just typed straight in, I may have a mistake in there :)
