[site]: stackoverflow
[post_id]: 650193
[parent_id]: 452013
[tags]: 
Looks like only partitioning by key is supported for cluster, whether in memory or on disk: http://dev.mysql.com/doc/refman/5.1/en/partitioning-limitations-storage-engines.html Range partitioning (and Hash, and List) are possible if MySQLD is started with the --new option, so data can be stored on disk, partitioned by range, . Cluster
