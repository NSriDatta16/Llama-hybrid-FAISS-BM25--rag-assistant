[site]: stackoverflow
[post_id]: 3820325
[parent_id]: 
[tags]: 
overlay opaque div over youtube iframe

How can I overlay a div with semi-transparent opacity over a youtube iframe embedded video? CSS #overlay { position:fixed; top:0; left:0; width:100%; height:100%; background:#000; opacity:0.8; /*background:rgba(255,255,255,0.8); or just this*/ z-index:50; color:#fff; } edit (added more clarification): HTML5 is approaching us, with more and more devices that use it instead of flash, which complicates the embedding of youtube videos, thankfully youtube provides a special embeddable iFrame with handles all of the video embedding compatibility issues, but now the previously working method of overlaying a video object with a semi-transparent div is no longer valid, I am now unable to add a to the object, because it is now a iFrame, so how do I add a opaque div on top of the iframe embedded video?
