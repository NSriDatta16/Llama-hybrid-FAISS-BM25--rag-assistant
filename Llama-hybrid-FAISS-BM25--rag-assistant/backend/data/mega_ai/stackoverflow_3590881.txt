[site]: stackoverflow
[post_id]: 3590881
[parent_id]: 3590870
[tags]: 
E-mail can be stored in databases quite efficiently. Alternatively you can store it in the file system (i.e. on the disk) or in the virtual file system which supports storing of metadata. We recently published an article on storing data in different storages . Outlook uses custom storage, similar to virtual file system.
