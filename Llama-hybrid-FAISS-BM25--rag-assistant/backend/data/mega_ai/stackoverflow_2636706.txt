[site]: stackoverflow
[post_id]: 2636706
[parent_id]: 2636655
[tags]: 
You can use the range operator in scalar context to extract the whole paragraph: while ( ) { chomp; if (/AceView summary/ .. /Please quote/) { print "$_\n"; } print "$_\n" if /^The closest human gene/; }
