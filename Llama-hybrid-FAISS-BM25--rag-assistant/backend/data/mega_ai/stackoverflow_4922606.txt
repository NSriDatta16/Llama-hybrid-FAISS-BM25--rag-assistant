[site]: stackoverflow
[post_id]: 4922606
[parent_id]: 
[tags]: 
Blank jQuery Dialog Window with ColdFusion

I'm trying to use a jQuery UI dialog window The dialog opens with the title displayed, but it is blank. I know that it is hitting the requested link because it throws an error that I placed in one of the pages. However, the page just never loads. Any ideas? $(document).ready(function() { $('#theKids td a').each(function() { var $link = $(this); var $dialog = $(' ') .load($link.attr('HREF') + ' #content') .dialog({ autoOpen: false, draggable: false, title: $link.attr('title'), modal: true, width: 500 }); $link.click(function() { $dialog.dialog('open'); return false; }); }); }); Home X Y #homename# #contract# #meds#
