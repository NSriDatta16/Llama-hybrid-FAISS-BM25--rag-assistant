[site]: stackoverflow
[post_id]: 4857393
[parent_id]: 4856600
[tags]: 
An example from BS is this from BeautifulSoup import BeautifulSoup doc = [' Page title ', ' This is paragraph one .', ' This is paragraph two .', ' '] soup = BeautifulSoup(''.join(doc)) soup.findAll('b') [ one , two ] As for a regular expression, you can use aa = doc[0] aa ' Page title ' pt = re.compile('(? ).*?(?= )') re.findall(pt,aa) ['Page title']
