[site]: datascience
[post_id]: 66973
[parent_id]: 
[tags]: 
Why does my GridSearchCV always break up?

GridSearchCV for my Random Forest breaks up. I need to know the reason and the solution to make it work: # Grid-Search for Random Forest param_grid = { 'bootstrap': [True], 'n_estimators': [100, 200, 300, 400, 500], 'max_depth': [50, 100, None], 'max_features': ['auto', 200], 'min_impurity_decrease':[0], 'min_samples_split': [2, 5], 'min_samples_leaf': [2, 5], 'oob_score': [True], 'warm_start': [True] } # Base-Model for improvement rf_gridsearch = RandomForestRegressor(random_state=42) # Grid-Search initiation rf_gridsearch = GridSearchCV(estimator = rf_gridsearch, param_grid = param_grid, scoring = 'neg_mean_absolute_error', cv = 5, n_jobs = -1, verbose = 5) # Perform the grid search for the model rf_gridsearch.fit(X_train, y_train) ```
