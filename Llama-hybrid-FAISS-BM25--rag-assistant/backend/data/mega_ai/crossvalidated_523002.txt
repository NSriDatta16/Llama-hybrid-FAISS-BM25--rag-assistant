[site]: crossvalidated
[post_id]: 523002
[parent_id]: 4517
[tags]: 
For Bayesian multivariate regression, one can use R package BNSP . For example, the dataset ami that comes with the package includes 3 responses and 3 covariates. # First load the package and dataset require(BNSP) data(ami) # Second, centre and scale variables - # this is specific to the dataset sc $ratio ami)-log(ami $tot)) ami$ tot $tot)) ami$ amt $amt)) ami$ pr $pr) ami$ qrs $qrs) ami$ bp Results are shown below. For the correlation matrix, the plot of the left shows posterior means and the one on the right posterior credible intervals.
