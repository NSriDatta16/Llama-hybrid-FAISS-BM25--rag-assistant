[site]: stackoverflow
[post_id]: 3083202
[parent_id]: 3083142
[tags]: 
If you have create two groups, allowing two documents open side by side you can just drag tabs from one group to the other (or use the tab's context menu). When the last tab is closed or moved to another group the whole group will close.
