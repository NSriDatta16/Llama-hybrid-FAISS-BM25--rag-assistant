[site]: stackoverflow
[post_id]: 2841029
[parent_id]: 
[tags]: 
Listing localstorage

I did my own feature using this: function save(title, url) { for (var i = 1; i save() happens when someone clicks on this: onclick="save(\'' + title + '\', \'' + tab.url + '\');"> ' + title + ' '; However ... it doesn't show the saved localStorages, how am I supposed to make it work?
