[site]: stackoverflow
[post_id]: 724781
[parent_id]: 724754
[tags]: 
It is hard to come up with an elegant solution when you only provide foo-bar examples instead of actual code, but my guesswork tells me that you probably want something like this: def som_setter=(val) self.value = val end def method_two return unless self.value # perform calculations with 'self.value'. end
