[site]: stackoverflow
[post_id]: 2490754
[parent_id]: 2490098
[tags]: 
A query that uses double-ended wild-cards (e.g. '%Babe%') cannot leverage any index, so the table will result in a sequential scan instead of an index scan. If you were searching for 'Babe%', then your index should work.
