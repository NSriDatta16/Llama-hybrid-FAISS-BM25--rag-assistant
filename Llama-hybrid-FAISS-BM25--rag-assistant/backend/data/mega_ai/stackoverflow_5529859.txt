[site]: stackoverflow
[post_id]: 5529859
[parent_id]: 5527869
[tags]: 
The problem is that you're using a narrow stream with a wide string. Use std::wofstream instead of std::ofstream and it should work fine. That being said, I agree with @jonsca -- why drag iostreams into a C++/CLI app?
