[site]: stackoverflow
[post_id]: 3134594
[parent_id]: 3134578
[tags]: 
You can extract it from the User-Agent HTTP headr. String userAgent = request.getHeader("User-Agent"); Here is a useful resource on how to extract the OS from the User-Agent (it's PHP, but the data is important). Update: this provides a much better way to get info from the user agent. (thanks to BalusC for the link)
