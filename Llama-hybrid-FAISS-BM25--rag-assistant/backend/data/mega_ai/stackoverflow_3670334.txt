[site]: stackoverflow
[post_id]: 3670334
[parent_id]: 3670183
[tags]: 
The brute-force solution is to dump your input into a std::string, then loop over the string and check if each character is between 0 and 9. It's not the most elegant approach. But it's simple and stupid. :-) bool isnum(char c) { if(! ( c = '0')) { return false; } return true; } bool has_int(std::string &s) { for( int i = 0; i
