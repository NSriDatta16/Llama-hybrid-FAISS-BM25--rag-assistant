[site]: crossvalidated
[post_id]: 544955
[parent_id]: 492010
[tags]: 
This approach often used in NLP models, so called via tuning and it is what you called transfer learning . A recent debate on this so called Foundation models actually revolves around this concept. transfer learning is even really used on autoencoders? Yes, it is possible, as these new foundation models approach training a large vision models or autoencoders. However, VGG or Resnet are very tiny models to be used as a foundational model compare to language models like GPT-3 or similar in size and training data coverage.
