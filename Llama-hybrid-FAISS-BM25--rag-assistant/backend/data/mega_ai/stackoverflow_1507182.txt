[site]: stackoverflow
[post_id]: 1507182
[parent_id]: 834027
[tags]: 
The apparently is a way that doesn't require the file to be in the downloads, but in the svn To do it you need to set the "svn:mime-type" property for the uploaded jnlp, otherwise the link will not work. I think this works because the svn apache module (that google must use) sets the right content type if it finds this property.
