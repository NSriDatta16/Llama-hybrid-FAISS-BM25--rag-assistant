[site]: stackoverflow
[post_id]: 2382305
[parent_id]: 2332946
[tags]: 
I think your instinct is correct. The back of the brain is remembering seeing somewhere that the W3C's guidance is to not use block elements (div) inside of inline elements (span). It is goofy from a semantics perspective. It's a bit like saying I have a paragraph inside a sentence. I think you'll get what you want if you change that div to span. It's possible is more correct semantically depending on what the point of that last text is. some text here
