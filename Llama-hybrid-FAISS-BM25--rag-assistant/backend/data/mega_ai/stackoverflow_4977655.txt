[site]: stackoverflow
[post_id]: 4977655
[parent_id]: 
[tags]: 
Postback on RadioButtonFor in MVC

I have 2 radio buttons in my mvc webpage. // some html codes m.Agents, "A", new { Checked = "checked" })%> //radio button 1 m.Agents,"AG") %> //radio button 2 // some html codes the aspx page is shown below. I also have button in my page.and if i clicked on that button,then there is a postback occures. I need another postback if i changed radiobutton selection. How can i make postback on that event.But there is no postback if i changed the selection of radio buttons. How can i achieve this?
