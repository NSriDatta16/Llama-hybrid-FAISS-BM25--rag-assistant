[site]: stackoverflow
[post_id]: 5085467
[parent_id]: 5084954
[tags]: 
Some problems I noticed : What if in accounts[x].term is also 1 ? Ans: You would never get in to that loop. Are you actually computing the average of all bank accounts? Ans: No You said account must be of 4 digits. So, 1000 & 9999 are valid account numbers. So, your condition should have been - while( accounts[x].accountNum 10000) ) { /.... } Assuming that, user will always enter intergers only between 1 to 10 for term, try this - for( int x=0; x Also, if had set the warnings on while compilation, you should have got useful messages regarding array out of bounds.
