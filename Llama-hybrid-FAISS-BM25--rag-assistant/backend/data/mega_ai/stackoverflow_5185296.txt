[site]: stackoverflow
[post_id]: 5185296
[parent_id]: 
[tags]: 
How to disable jquery connected sortables on the go?

Given two simplest connected sortables, Problem (updated). when a draggable item leaves its container, the container should not accept it back while the item is still being dragged (as if the container is disabled). On the DOM level, the placeholder should not move back to the original container. How can this be accomplished? The code in this jsfiddle is not working as expected, but should be a good starting point.
