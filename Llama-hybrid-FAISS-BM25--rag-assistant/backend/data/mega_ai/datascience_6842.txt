[site]: datascience
[post_id]: 6842
[parent_id]: 
[tags]: 
Is there a way of performing stratified cross validation using xgboost module in python?

I am training and predicting on the same data-set, but I want to perform 10-fold cross-validation and predict on the left out fold and thus predict on the whole data set. How can I do this? The libraries which I am using are: from sklearn import cross_validation import xgboost as xgb
