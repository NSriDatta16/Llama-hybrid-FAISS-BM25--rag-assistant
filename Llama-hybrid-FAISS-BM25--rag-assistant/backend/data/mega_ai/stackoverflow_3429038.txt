[site]: stackoverflow
[post_id]: 3429038
[parent_id]: 3428870
[tags]: 
private sub doStuff(depth as integer) for i as integer = 1 to 2 initialize(work_item(i)) if depth > 1 then doStuff(depth - 1) doSomething(work_item(i)) next end sub Far more elegant, shorter, less tricky than any iterative solution you can come up with. Downvote me all ya want, doesn't make it less true.
