[site]: stackoverflow
[post_id]: 4458626
[parent_id]: 
[tags]: 
Way to fill List<> with elements

I have in my code List retVal = new List (somesize); Match later in the code if some condition works I need to fill this entire List with same value. Of course I can do it via foreach loop and set values , Is there more elegant way to do so ? I just to learn something new here .
