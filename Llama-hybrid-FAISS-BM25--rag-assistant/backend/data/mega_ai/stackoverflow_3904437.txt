[site]: stackoverflow
[post_id]: 3904437
[parent_id]: 3904423
[tags]: 
Alternative syntax for control structures : if ($a == $b) { echo $a; } // is same as: if ($a == $b): echo $a; endif; This syntax was introduced to make embedding PHP in HTML easier. By telling which block to close the code becomes more understandable.
