[site]: stackoverflow
[post_id]: 5249270
[parent_id]: 5249122
[tags]: 
Not tested but maybe something like edit threw it into LinqPad, works just fine for what you need. public static string ToXmlFragment(this object input, string element) { //extension method, place in a static class somewhere return string.IsNullOrEmpty(input.ToString()) ? string.Format(" ",element) : string.Format(" {1} ",element,input); } reasonFrag.InnerXml = reason.ToXmlFragment("ReasonForPayment");
