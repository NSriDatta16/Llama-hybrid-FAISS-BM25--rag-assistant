[site]: stackoverflow
[post_id]: 4221960
[parent_id]: 4220652
[tags]: 
May be a good approach to quickly try something, but on the long run that gateway object will become so gigantic and the relation between the different objects so many that it will fall under its own weight. Also, one of the points of object orientation is to encapsulate the access of parts of the code to only those objects that require it.
