[site]: stackoverflow
[post_id]: 805823
[parent_id]: 805802
[tags]: 
The load indicates how man processes are waiting in the queue to be executed. You would have a load of 1 if your have that much processes that at least one process has always to queue up before it gets executed (is not executed immediately). The numbers in uptime are average over some period.
