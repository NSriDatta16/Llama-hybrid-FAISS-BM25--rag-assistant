[site]: stackoverflow
[post_id]: 4374026
[parent_id]: 4373580
[tags]: 
Static members persist across multiple requests, so you could do this: public static class HttpHandlerStorage { // this should still be here, unless the HttpApplication reloads. public static IHttpHandler StoredHandler { get; set; } } However, your HttpContext will get destroyed at the end of the request.
