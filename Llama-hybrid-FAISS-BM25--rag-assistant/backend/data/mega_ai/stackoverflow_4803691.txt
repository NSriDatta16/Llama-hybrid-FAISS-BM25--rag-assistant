[site]: stackoverflow
[post_id]: 4803691
[parent_id]: 4802864
[tags]: 
Sorry, no, you could only do that with a regex engine that supports infinite-length-lookbehind, and ECMAScript engines don't do that. You'll have to process each entry separately, and if it starts with Albert_ , then do the replacements on it.
