[site]: stackoverflow
[post_id]: 4376784
[parent_id]: 4376377
[tags]: 
I have no idea why you would want to do this legitimately, but, as the question was asked... # Determine if the User Agent is a Targetted Spider or Bot SetEnvIfNoCase User-Agent "^Start of the Spider User-Agent String" is_bot # Add as many lines as are needed here... # Set the 403 Forbidden location as the Real Visitor Site ErrorDocument 403 http://www.real-visitors-go-here.com/ # Apply the Targetted Spider or Bot Check to Access order deny,allow deny from all allow from env=is_bot Never done it myself, but that kind of makes sense.
