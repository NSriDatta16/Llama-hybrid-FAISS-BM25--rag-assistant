[site]: stackoverflow
[post_id]: 787097
[parent_id]: 787075
[tags]: 
GUI method: in SQL Server Management Studio if you right click the table and choose "Design" you can then drag the column up to the top and hit save. Note : Be aware that by doing this, SQL Server drops the table and creates it again. You won't lose your data though.
