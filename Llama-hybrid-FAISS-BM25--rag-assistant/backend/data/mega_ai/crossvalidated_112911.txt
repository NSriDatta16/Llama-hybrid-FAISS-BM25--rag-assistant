[site]: crossvalidated
[post_id]: 112911
[parent_id]: 
[tags]: 
Epoch vs Incremental Update over Entire Dataset

In the context of neural networks, what is the difference between training like this: foreach observation in dataset: compute_error(observation) update_weights() and foreach observation in dataset: compute_error(observation) update_weights() Both train over the entire dataset.
