[site]: stackoverflow
[post_id]: 2921466
[parent_id]: 2919696
[tags]: 
Concatenate both result using the Concat operator.So its basically a single query IEnumerable priorityAgents = (from pri in builtAgents where pri.OptimPriority == 1 select pri).Concat((from oth in builtAgents where oth.OptimPriority == 0 select oth).Take(5));
