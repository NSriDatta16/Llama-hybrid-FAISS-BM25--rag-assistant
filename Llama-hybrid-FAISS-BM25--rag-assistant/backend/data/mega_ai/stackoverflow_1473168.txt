[site]: stackoverflow
[post_id]: 1473168
[parent_id]: 1473139
[tags]: 
.draggable p { white-space: nowrap; } You shouldn't need the explicit auto width/height, that's the default. You normally wouldn't use display: inline either as a float is never inline, but I guess this is a workaround for an IE bug?
