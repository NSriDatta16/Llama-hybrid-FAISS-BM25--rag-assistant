[site]: datascience
[post_id]: 126572
[parent_id]: 
[tags]: 
How to add lagging Features for Forecasting with a random lag range without adding a new column per lag?

The common way of adding lagging features in time series forecasting problems is adding lag columns with pandas.shift(). While it is a fine method but what about when wanting to use a random integer (within a range) for lags per observation without adding a new column per lag?
