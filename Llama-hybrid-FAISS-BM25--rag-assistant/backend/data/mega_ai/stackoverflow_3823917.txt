[site]: stackoverflow
[post_id]: 3823917
[parent_id]: 3822148
[tags]: 
The relative path works, but we need to track up from the excuting binary, which is buried away in a test project. string solutionPath = Directory .GetParent(Assembly.GetExecutingAssembly().Location) .Parent.Parent.Parent.FullName; string pluginPath = Path.Combine(solutionPath, "TestPlugins"); Needs the following using System.IO; using System.Reflection; Not the most elegant solution, but it works.
