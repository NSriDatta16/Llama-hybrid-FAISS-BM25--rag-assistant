[site]: stackoverflow
[post_id]: 255732
[parent_id]: 255714
[tags]: 
If you want "def something" to to accept a block, you need to yield data to that block. For example: #to uppercase string def something(my_input) yield my_input.upcase end # => "HELLO WORLD" something("hello world") { |i| puts i}
