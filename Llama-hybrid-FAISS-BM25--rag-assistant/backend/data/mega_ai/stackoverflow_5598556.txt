[site]: stackoverflow
[post_id]: 5598556
[parent_id]: 
[tags]: 
C: Reading in a file of strings into an array of char *s

I have something like this (without the spacing issues), where I'm reading a file with strings that are separated by the # sign, and trying to read each of them into an array of char * s. However, each char* that holds a string much be the exact length of the string. This is a little buggy though. char * fragments[1000]; char temp[20]; while (i Thoughts?
