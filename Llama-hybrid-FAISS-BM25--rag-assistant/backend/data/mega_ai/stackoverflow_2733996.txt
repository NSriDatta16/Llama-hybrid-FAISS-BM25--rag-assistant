[site]: stackoverflow
[post_id]: 2733996
[parent_id]: 2732930
[tags]: 
With Windows Installer, no. You'll need two setups. However NSIS is quite capable of handling both platforms in a single setup with runtime detection. It really depends if you're targeting Enterprise users or not, Enterprise customers will require Windows Installer (MSI) packages while your average internet user doesn't care :)
