[site]: stackoverflow
[post_id]: 4885968
[parent_id]: 4885891
[tags]: 
Something like this should work on all tags: replace(/ /, ' ') For paragraphs only, just replace the \w : replace(/ /, ' ') The \s* in the beginning allows for whitespace before the tag name, so if you for some reason have , it works on that too.
