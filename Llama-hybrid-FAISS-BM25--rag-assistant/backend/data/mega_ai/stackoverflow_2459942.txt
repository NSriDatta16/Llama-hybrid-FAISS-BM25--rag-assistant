[site]: stackoverflow
[post_id]: 2459942
[parent_id]: 2459662
[tags]: 
As you have seen, you can't do this in CSS; however, you can instruct your JavaScript to not update an element's style, but it requires some manual attention. Consider the following: HTML A B C D JavaScript (vanilla) var nodes = document.getElementById("some-list").getElementsByTagName("LI"); for ( var i=0, l=nodes.length; i
