[site]: stackoverflow
[post_id]: 4215632
[parent_id]: 4215524
[tags]: 
Check the inline comments. Simplified the initial selection. To load the new page use the slideDown callback. function slideUp(){ allDivs.slideUp(500, slideDown); } function slideDown(){ $("#introductie").slideDown(500, function() { /* load the new page here */ }); } $(function() { //you can select multiple elements like this var allDivs = $("#introductie, #agenda, #werk, #onderwijs-organisatie, #publicaties, #links, #contact"); console.log(allDivs); $("li.page-item-11 a").click(slideUp); });
