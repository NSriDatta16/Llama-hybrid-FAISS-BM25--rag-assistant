[site]: stackoverflow
[post_id]: 3778335
[parent_id]: 
[tags]: 
How to retrieve the display property of a DOM element?

a { display: none; } a paragraph google var a = (document.getElementById('a')).style; alert(a.display); var p = (document.getElementById('p')).style; alert(p.display); p.display = 'none'; alert(p.display); The first and the second alert display nothing other than a empty string, which I thought should be none and block . However after the intensionally display setting, the third alert finally alert none . But Why? How could I retrieve the display property correctly? Thanks.
