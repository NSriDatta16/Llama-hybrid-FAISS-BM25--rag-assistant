[site]: datascience
[post_id]: 90411
[parent_id]: 90403
[tags]: 
I think I was able to get it working now. What I did was to change the assigned nan value to 25 instead of 0 (which would be about the average) and normalize the values within the interval (-1,1) with a scaled sigmoid shifted on 25. This is how it looks now:
