[site]: stackoverflow
[post_id]: 300079
[parent_id]: 300077
[tags]: 
This is my solution, untested as of yet: // this will be our storage of the new z-order int *tmpZ = new int[GetCount()]; int currentZ = INT_MIN; int smallestIdx = -1; int newZ = 0; for (int passes = 0; passes m_zOrder > currentZ && GetAt(i) m_zOrder; } } tmpZ[smallestIdx] = newZ; // prepare for the next item currentZ = smallestZ; newZ++; smallestIdx = -1; } // push the new z-order into the array for (int i = 0; i m_zOrder = tmpZ[i]; It is O(n^2), as you can see.... :(
