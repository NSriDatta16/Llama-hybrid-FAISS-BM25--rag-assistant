[site]: stackoverflow
[post_id]: 3754531
[parent_id]: 2225692
[tags]: 
One way to do this -- there might be others -- is to check to see whether it's listed in the navigator.plugins array before creating the object tag. Something like this (some sample code cut-and-pasted from a recent project): function createVncServer(id) { console.log(navigator.userAgent); var vncDiv = document.getElementById("vncDiv"); if (navigator.userAgent.indexOf("MSIE") > -1) { vncDiv.innerHTML = " "; } else { // On Mozilla browsers, check to make sure the plugin is installed before we try to instantiate it, to avoid the weird "plugins required" message. var pluginInstalled = false; for (var i = 0; i "; } else { promptForDownload(); return null; } } var vncServer = document.getElementById(id); try { if (vncServer.Port > 0) { clearDownloadPrompt(); return vncServer; } else { promptForDownload(); } } catch (ex) { promptForDownload(); } }
