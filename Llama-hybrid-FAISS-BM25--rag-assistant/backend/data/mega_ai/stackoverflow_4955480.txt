[site]: stackoverflow
[post_id]: 4955480
[parent_id]: 4955437
[tags]: 
They're unsafe depending on the type of storage. A secure method would be to use custom session handlers (via session_set_save_handler ) and store the information "somewhere safe" (i.e. not as session files in the globally readable /tmp directory).
