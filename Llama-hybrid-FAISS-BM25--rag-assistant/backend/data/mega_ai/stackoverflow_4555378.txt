[site]: stackoverflow
[post_id]: 4555378
[parent_id]: 
[tags]: 
Converting paragraph tags with RegEx

I need to replace all tags with tags within a string. The problem is that the tag can have attributes in it , such as so I want to delete all occurrences of an opening tag of a paragraph, no matter what attributes are in it, and replace them with . I am using PHP and had no success getting to the right expression with the preg_replace function. Any help would be appreciated! Joel
