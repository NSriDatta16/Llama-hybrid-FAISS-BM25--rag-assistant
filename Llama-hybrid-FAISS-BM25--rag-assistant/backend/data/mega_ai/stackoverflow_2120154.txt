[site]: stackoverflow
[post_id]: 2120154
[parent_id]: 2119896
[tags]: 
You'll need to collect the user agent string and then do some kind of a lookup table against that list to see if an update is needed. You can then populate a DIV or open a dialog, etc. I'm not aware of any "automatic" service that tells you if you are out of date, aside from the browser's built-in mechanisms.
