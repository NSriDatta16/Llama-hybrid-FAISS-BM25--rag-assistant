[site]: stackoverflow
[post_id]: 3982662
[parent_id]: 3982633
[tags]: 
One way to approach this problem is to break the function foo into 2 functions float foo(Container* aContainer){ if(aContainer!=NULL) { return foo_core(aContainer->storage); } else { vector workingStorage; return foo_core(workingStorage); } float foo_core(vector & workingStorage) { ... // rest of original foo }
