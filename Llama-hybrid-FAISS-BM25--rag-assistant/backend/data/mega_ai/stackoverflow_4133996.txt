[site]: stackoverflow
[post_id]: 4133996
[parent_id]: 4133907
[tags]: 
you can use the following class and the build a new table from it. class AgentsData { public static DataTabe ProcessDataTabe(DataTable dt) { Dictionary data = new Dictionary (); for (int i = 0; i (); } public void Add(int weekNumber, int score) { WeekScore[weekNumber] = score; } public void Process() { int min = WeekScore.Keys.Min(); int max = WeekScore.Keys.Max(); for (int i = 0; i WeekScore { get; private set;} }
