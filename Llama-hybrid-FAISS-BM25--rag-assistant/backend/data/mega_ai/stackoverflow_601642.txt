[site]: stackoverflow
[post_id]: 601642
[parent_id]: 600597
[tags]: 
Here's my solution. It might not be as elegant as you'd expect: public class Report { //... void PostLoad() { foreach(Section s in Sections) { s.Report = this; } } public static Report Load(string filename) { // Load using an XmlSerializer Report report = ...; report.PostLoad(); return report; } }
