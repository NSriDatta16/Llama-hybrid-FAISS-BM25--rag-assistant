[site]: stackoverflow
[post_id]: 573767
[parent_id]: 573751
[tags]: 
How about: count = foldl (\i v -> if v then i + 1 else i) 0 Another way to do it without foldl : count list = sum $ map fromEnum list Credit to Logan for pointing out fromEnum . Hadn't heard of that one before.
