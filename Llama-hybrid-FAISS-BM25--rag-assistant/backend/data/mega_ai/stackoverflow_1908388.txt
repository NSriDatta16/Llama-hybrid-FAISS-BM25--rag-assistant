[site]: stackoverflow
[post_id]: 1908388
[parent_id]: 1908325
[tags]: 
Just a hunch, but check the mime-type of the data being returned via the ajax function. If you request xml, the script is expecting content encoded as text/xml or some other derivative . You may also be running into the same origin policy, in which case you'll need to refactor everything to json and leverage jsonp .
