[site]: crossvalidated
[post_id]: 198997
[parent_id]: 
[tags]: 
Estimating parameters of a linear equation using MCMC: Why so much variability in the c parameter?

I'm currently trying to understand parameter estimation using MCMC via estimating the two parameters of a linear equation ( m and c ). My code works and produces histograms representing a probability distribution for the m parameter but not for the c parameter. My questions is: why? Is it because the c parameter represents a structural non-identifiability(to use ODE terminology)? or is there something else wrong with my code? Some example output m parameter"> c parameter "> My code: Three matlab scripts, one objective function, one model function and one run script. The objective function function chi2 = SSR(sim,exp,sigma) %% % sim= simulated data % exp= experimental data % sigma = standard deviation chi2 =sum( ( ([sim]-[exp])./ ( 2*sigma^2) ).^2); The model function function y=mx_plus_c(m,x,c) dim=size(x); if dim(1)==1, if dim(2)==1, y=m*[x]+c; else y=m*[x]+c; end end The run code: %% Simulate some data from a linear equation interval=1:100; y=mx_plus_c((38),interval,(4)); % y=log10(y); %% Attempt at estimating the parameters using MCMC methodology n=1e5; %iterations %create a storage vector vec=zeros(n,6); % Define starting point for both estimated parameters m=(50); c=(50); %U~(1,100) for both parameters %matlab way of generating samples from uniform % r = a + (b-a).*rand(N,1) a1=1; b1=100; a2=1; b2=100; chi2_current=SSR( mx_plus_c( m,interval,c) , y, 0.0001); count=0; for i=1:n, m_prop = ( (a1+(b1-a1).*rand(1,1) ) ); c_prop = ( (a2+(b2-a2).*rand(1,1) ) ); chi2_prop=SSR( mx_plus_c( m_prop,interval,c_prop) , y, 0.0001); aprop=min(1,chi2_current/chi2_prop); vec(i,4)=aprop; % ratio=exp((-chi2_prop+chi2_current) / 2*0.001^2); % end random_value=rand; vec(i,5)=random_value; if random_value
