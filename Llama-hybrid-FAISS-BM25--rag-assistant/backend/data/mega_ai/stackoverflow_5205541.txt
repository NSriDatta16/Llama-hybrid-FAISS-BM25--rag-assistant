[site]: stackoverflow
[post_id]: 5205541
[parent_id]: 5205475
[tags]: 
You have to use a SSH-agent that will store the deciphered key in memory. This is really easy under unix, just launch eval $(ssh-agent) and then ssh-add this will ask you for your password and then store the key. For the cron problem, try entering the full path, you can obtain it with which git on any shell.
