[site]: stackoverflow
[post_id]: 1023125
[parent_id]: 1023108
[tags]: 
Table Storage would be the most logical place. Other server farm type setups also use a database table to store session info. Take a look at the AspProviders project in the Windows Azure SDK samples. It has a SessionState provider that uses Azure Table Storage.
