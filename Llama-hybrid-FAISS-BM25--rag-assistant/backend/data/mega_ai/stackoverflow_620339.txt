[site]: stackoverflow
[post_id]: 620339
[parent_id]: 620276
[tags]: 
A probably more reliable way of doing this is to perform a regex on the $_SERVER['HTTP_USER_AGENT'] string. You are not guaranteed to get a valid and useful user-agent string, so make sure that the else statement contains a reasonable alternative.
