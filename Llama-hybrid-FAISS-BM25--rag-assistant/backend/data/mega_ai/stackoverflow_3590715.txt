[site]: stackoverflow
[post_id]: 3590715
[parent_id]: 3590698
[tags]: 
Use this to trigger it instead: .trigger('dragstop') If you want it to behave completely as a normal event, use .bind('dragstop', function) to attach it as well, the start option behaves slightly differently.
