[site]: stackoverflow
[post_id]: 4356416
[parent_id]: 4356329
[tags]: 
If we're going to abstract it into a function anyway, it's not too hard to write "from scratch": def pairs(iterable): iterator = iter(iterable) while True: try: yield (iterator.next(), iterator.next()) except: return robert's recipe version definitely wins points for flexibility, though.
