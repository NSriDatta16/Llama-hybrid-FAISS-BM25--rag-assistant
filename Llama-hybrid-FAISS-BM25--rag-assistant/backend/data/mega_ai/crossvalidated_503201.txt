[site]: crossvalidated
[post_id]: 503201
[parent_id]: 
[tags]: 
obtaining proximity matrix from random forest for unsupervised scenario in R

I recently came across the concept of proximity matrix in random forests (see for example this great StatQuest video ). This can easily be obtained in the regression or classification scenario like so: library(dplyr) library(randomForest) library(datasets) data(iris) model I am just wondering, if one could also obtain it without specifying a target as such? I tried: library(dplyr) library(randomForest) library(datasets) data(iris) temp % select( Sepal.Length , Sepal.Width , Petal.Length , Petal.Width ) model without success. I read somewhere that a fudge like this may be the solution: library(dplyr) library(randomForest) library(datasets) data(iris) temp % select( Sepal.Length , Sepal.Width , Petal.Length , Petal.Width ) %>% mutate( fudge = 1 ) model Not sure, if this is acceptable? Thanks! PS: I followed this and the results using the "fudge approach" actually make sense: library(ggplot2) library(cowplot) library(dplyr) library(randomForest) library(datasets) data(iris) temp % select( Sepal.Length , Sepal.Width , Petal.Length , Petal.Width ) %>% mutate( fudge = 1 ) model $eig/sum(mds.stuff$ eig)*100, 1) ## now make a fancy looking plot that shows the MDS axes and the variation: mds.values $points mds.data Species) ggplot(data=mds.data, aes(x=X, y=Y, label=Sample)) + geom_text(aes(color=Status)) + theme_bw() + xlab(paste("MDS1 - ", mds.var.per[1], "%", sep="")) + ylab(paste("MDS2 - ", mds.var.per[2], "%", sep="")) + ggtitle("MDS plot using (1 - Random Forest Proximities)") PPS: The section: Unsupervised learning Here: https://www.stat.berkeley.edu/~breiman/RandomForests/cc_home.htm explains it but TBH I do not fully understand what it means. Could someone please help?
