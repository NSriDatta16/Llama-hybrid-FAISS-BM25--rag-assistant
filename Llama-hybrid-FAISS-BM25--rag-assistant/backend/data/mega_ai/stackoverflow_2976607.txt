[site]: stackoverflow
[post_id]: 2976607
[parent_id]: 2976485
[tags]: 
I think if you remove the join, the collection of associated People objects will be lazy loaded when you access them. If you want the collection to be loaded in the same query then this should work: select o from Organization o join fetch o.Persons as p where o.OrgId = :orgId
