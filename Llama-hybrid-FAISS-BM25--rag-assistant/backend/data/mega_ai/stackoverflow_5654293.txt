[site]: stackoverflow
[post_id]: 5654293
[parent_id]: 5653855
[tags]: 
Completely rewritten (thanks for the example). I rewrote your example data to be slightly more compact. df1 Alternatively, if you don't want to fill in spaces with NAs but want to have a "ragged" list, you can do this: lapply(split(df1,df1$date),function(x) subset(x,select=-date))
