[site]: stackoverflow
[post_id]: 4481187
[parent_id]: 4481174
[tags]: 
I thing they banned the IP is your system IP not your connection IP who can be change but system IP can not. some application set their agent like Firefox's user agent then server can't find that the request not come from browser they make them because they need that server can't find somebody going of their site by code not just using browser. the answer i put nothing match to your question. Fake identity means i tell about the request make by code inside a application not by browser user use. the user-agent change make them as normal request that they can't find the request they have come by code not by browser.
