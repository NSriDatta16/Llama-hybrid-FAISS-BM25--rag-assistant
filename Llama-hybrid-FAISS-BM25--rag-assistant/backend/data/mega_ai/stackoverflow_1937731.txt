[site]: stackoverflow
[post_id]: 1937731
[parent_id]: 1937685
[tags]: 
It is not guaranteed that when using @"foo" in multiple places the runtime uses the same storage for them, and certainly may not be the case across compilation unit or library boundaries. I would rather use static NSString *string = @"foo" , especially with a lot of literal strings.
