[site]: stackoverflow
[post_id]: 2362198
[parent_id]: 
[tags]: 
Implementing a parallel algorithm to compute pi

I would like to implement a parallel version of the code below using threads in OpenMP,is there any better way to do this? /* Program to compute Pi using Monte Carlo methods */ #include #include #include #include #include #define SEED 35791246 int main(int argc, char* argv) { int niter=0; double x,y; int i,count=0; /* # of points in the 1st quadrant of unit circle */ double z; double pi; clock_t end_time, start_time; printf("Enter the number of iterations used to estimate pi: "); scanf("%d",&niter); start_time = clock(); /* initialize random numbers */ srand(SEED); count=0; #pragma omp parallel for for ( i=0; i
