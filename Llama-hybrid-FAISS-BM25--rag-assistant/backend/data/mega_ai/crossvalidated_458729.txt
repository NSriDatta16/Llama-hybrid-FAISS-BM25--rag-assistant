[site]: crossvalidated
[post_id]: 458729
[parent_id]: 458721
[tags]: 
you can create your own model so basically you redefine the parameters for evtree, basically redefining the existing one for evtree , the elements of the list to change are under parameters, grid and fit (where you pass in evtree.control) : evtree_ext $control$ alpha $alpha ctl control theDots $control alpha,maxdepth=param$alpha) ## pass in any model weights if(!is.null(wts)) theDots$weights Using an example (sorry not familiar with this package, might be nonsense): library(MASS) library(caret) library(evtree) library(rBayesianOptimization) mod
