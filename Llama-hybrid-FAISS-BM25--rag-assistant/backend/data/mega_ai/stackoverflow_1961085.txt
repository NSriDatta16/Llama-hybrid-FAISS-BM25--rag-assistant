[site]: stackoverflow
[post_id]: 1961085
[parent_id]: 1958954
[tags]: 
I ended up with the following solution List propertyList = new ArrayList(); CollectionUtils.collect(list,new Transformer() { @Override public Object transform(Object input) { return ((LogMessage)input).getLogRunIds(); } },propertyList); Unfortunately, Transform interface doesn't allow using generics, so i have to use explicit casts Many thakns to Brian Agnew for an insight
