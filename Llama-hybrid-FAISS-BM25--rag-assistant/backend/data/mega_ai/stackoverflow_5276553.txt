[site]: stackoverflow
[post_id]: 5276553
[parent_id]: 
[tags]: 
html stack order

Consider the following code: HTML h1 { font-size: 2em; font-family: Verdana; font-weight: bold; } p { border: 3px solid blue; margin-top: -50px; background-color: green; color: white; } QUESTION The header text in the preceding h1 element is behind this paragraph's text (as expected), but on top of this paragraph's background and border (not expected). See the example here: http://jsfiddle.net/ZKHc9/ Why isn't the paragraph's background and border rendered on top of the header like the content is?
