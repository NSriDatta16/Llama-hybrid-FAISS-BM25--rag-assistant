[site]: datascience
[post_id]: 96346
[parent_id]: 90330
[tags]: 
One standard way to represent data for both supervised and unsupervised learning is the tidy dataframe . Tidy dataframe is putting data into tabular form where each row is an observation and each column is a feature. Once data is in that form, machine learning modeling can take place.
