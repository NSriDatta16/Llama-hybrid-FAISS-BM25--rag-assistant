[site]: stackoverflow
[post_id]: 3901313
[parent_id]: 
[tags]: 
The most elegant way to write an abstraction layer

I'm curious how to write an abstraction layer. By abstraction layer, I mean a wrapper above one or more 3rd party libraries. Or do I have to solve it like this? #include #include class layer { private: an3rdpartyl* object1; another3rdpartyl* object2; public: //... int loadModel(char* file) { return object2->LoadMeshFromFile(file); } //... };
