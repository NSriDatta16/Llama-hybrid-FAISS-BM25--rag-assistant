[site]: stackoverflow
[post_id]: 4172507
[parent_id]: 4172480
[tags]: 
Capacity is the size of the internal storage of the objects. The internal storage is always greater than or equal to the size() of the list (so that it can contain all elements). public class Main { public static void main(String[] args) throws Exception { ArrayList arr = new ArrayList<>(); System.out.println("initial size = " + arr.size()); // 0 System.out.println("initial capacity = " + getCapacity(arr)); for (int i = 0; i l) throws Exception { Field dataField = ArrayList.class.getDeclaredField("elementData"); dataField.setAccessible(true); return ((Object[]) dataField.get(l)).length; } } Running this gives: initial size = 0 initial capacity = 10 size = 11 capacity = 16
