[site]: stackoverflow
[post_id]: 5677208
[parent_id]: 5677115
[tags]: 
Yes there most certainly is a more elegant way of doing this ( ... else if (typeid(T) == ... ? Yuck!). You could write some operator s for your struct s. This makes your debugPrintMe() function nice and generic and also allows you to stream your structs to cout , cerr , a logger, an ostringstream , ... Here's an example to get you started: std::ostream& operator void debugPrintMe(const T& myvar) { if (DEBUG) { std::cout
