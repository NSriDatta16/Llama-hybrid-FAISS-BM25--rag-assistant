[site]: stackoverflow
[post_id]: 868244
[parent_id]: 867522
[tags]: 
Here is the solution from python mailing list , not very elegant but will work if can't avoid the need for such hack import sys try: raise "a string exception" except: e, t, tb = sys.exc_info() if not isinstance(e, str): raise print "caught", e
