[site]: stackoverflow
[post_id]: 3583951
[parent_id]: 3583928
[tags]: 
You should use a foreach loop . function mediaType(){ $userAgents = array("iPhone", "Chrome"); $browser = $_SERVER['HTTP_USER_AGENT']; foreach ($userAgents as $agent) { if (strpos($browser, $agent) !== false) return 1; } return 0; }
