[site]: stackoverflow
[post_id]: 968800
[parent_id]: 
[tags]: 
How do you check the browser's user agent in a JSP page using JSTL, EL?

I need to check the browser's user-agent to see if it is IE6. However I shouldn't use scriptlets (we have a strict no scriptlets policy) to do this. Currently I use What is the cleanest way to do this using JSTL, EL, etc and not scriptlets?
