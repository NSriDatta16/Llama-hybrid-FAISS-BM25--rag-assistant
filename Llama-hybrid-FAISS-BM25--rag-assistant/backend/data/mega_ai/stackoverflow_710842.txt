[site]: stackoverflow
[post_id]: 710842
[parent_id]: 710834
[tags]: 
Essentially No there is no way to retrieve a windows account password. You can change them with various tools but retrieval is highly unlikely. You can always create a proxy account on a domain controller and setup an app pool to run under a proxy account so permissions can span across workstations.
