[site]: stackoverflow
[post_id]: 5431658
[parent_id]: 5427531
[tags]: 
Shouldn't that line read either import com.facebook.graph.*; or import com.facebook.graph.SomeClass; ? I always rely on Flex Builder to organize my import statements. That way I never really have to deal with these errors.
