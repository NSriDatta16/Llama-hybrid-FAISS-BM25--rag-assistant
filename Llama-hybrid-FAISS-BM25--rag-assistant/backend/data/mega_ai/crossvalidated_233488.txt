[site]: crossvalidated
[post_id]: 233488
[parent_id]: 233484
[tags]: 
So, did anyone find a sequence model that can incorporate continuous variables? Yes. You can use continuous variables as input (e.g. when using word embeddings, example ) or as output (e.g., regression, example )
