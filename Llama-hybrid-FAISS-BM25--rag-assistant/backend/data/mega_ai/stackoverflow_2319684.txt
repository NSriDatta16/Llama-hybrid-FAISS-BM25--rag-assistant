[site]: stackoverflow
[post_id]: 2319684
[parent_id]: 2317515
[tags]: 
Organize your data into arrays. For example: T=0.3:0.1:0.6; Price=yourfunction(T); Then if you want a price vs T graph, plot(T,Price) If you've got a large amount of data, try to avoid for loops, as they're slower than vectorized code.
