[site]: crossvalidated
[post_id]: 229714
[parent_id]: 
[tags]: 
How do you deal with a categorical variable that has multiple levels?

Suppose we want to model whether somebody makes $> \$50,000$ or $ For example, in a random forest or logistic regression, it could take a long time for these algorithms to run if you include a lot of dummy variables.
