[site]: stackoverflow
[post_id]: 4413565
[parent_id]: 
[tags]: 
Magento 1 - get category ID from product ID

In magento how to get the category id of each product from its product ID. $items = $request->getAllItems(); $c = count($items); for ($i = 0; $i getProduct() instanceof Mage_Catalog_Model_Product) { if ($items[$i]->getProduct()->getId()) { $this->_dhlAllowed = false; } } } Here $items[$i]->getProduct()->getId() returns product ID. I want its category ID.
