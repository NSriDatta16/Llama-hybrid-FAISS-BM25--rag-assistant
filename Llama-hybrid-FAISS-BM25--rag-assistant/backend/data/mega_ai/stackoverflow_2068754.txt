[site]: stackoverflow
[post_id]: 2068754
[parent_id]: 2068735
[tags]: 
Although not technically a stateful event mechanism, Windows Workflow Foundation contains many features to handle these specific types of situations. In particular, it has routines in place for serializing the work state to storage, handling failures, and recovery of long running processes with external events.
