[site]: crossvalidated
[post_id]: 221139
[parent_id]: 
[tags]: 
Distribution of classes in neural network batches

I'm creating a neural network for classifying input data. When using batches, do I need to ensure a somewhat uniform distribution of classes per batch? Or can I simply split up my data in any which way?
