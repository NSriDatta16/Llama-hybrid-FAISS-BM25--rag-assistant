[site]: stackoverflow
[post_id]: 61042
[parent_id]: 60950
[tags]: 
Turn on quickedit mode (but selection is still rectangular instead of line-wrapped) Resizing by dragging works for me You can change the buffer size which will impact when scrollbars appear pushd \\server\share Even with those, cmd.exe isn't a great console. See all the other replies and the earlier stackoverflow questions on the same subject. The "Console" project from sourceforge looks pretty good.
