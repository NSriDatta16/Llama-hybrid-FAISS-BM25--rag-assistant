[site]: stackoverflow
[post_id]: 4307415
[parent_id]: 
[tags]: 
how to write a custom panel displaying categories in magento

I have a category called "Top Products". I wish to display this in a panel on my home page. What is the best way to do this in magento. ** edit ** Thanks sdek, i now have the following. In Home Page / Design. And it is displaying products. However i have the following issues. - it's not displaying products from category_id 13, It seems like this value is not being passed thru - it's only display 2 products. I wish to display all. featured.phtml getLoadedProductCollection(); $_helper = $this->helper('catalog/output'); ?> count()): ?> __('There are no products matching the selection.') ?> Featured Products count() ?> getColumnCount(); ?> first last "> getProductUrl() ?>" title=" stripTags($this->getImageLabel($_product, 'small_image'), null, true) ?>" > helper('catalog/image')->init($_product, 'thumbnail')->resize(85); ?>" class="product-img" alt=" stripTags($this->getImageLabel($_product, 'small_image'), null, true) ?>" style="padding-bottom:20px; margin-bottom:20px;" /> productAttribute($_product, $_product->getName(), 'name') ?> getShortDescription(); ?> getPriceHtml($_product, true) ?> getProductUrl() ?>" class="more">More details
