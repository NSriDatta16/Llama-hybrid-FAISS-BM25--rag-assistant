[site]: stackoverflow
[post_id]: 4211930
[parent_id]: 4211909
[tags]: 
You can like this... document.getElementById('my-image').ondragstart = function() { return false; }; See it working (or not working, rather) It seems you are using jQuery. $('img').on('dragstart', function(event) { event.preventDefault(); });
