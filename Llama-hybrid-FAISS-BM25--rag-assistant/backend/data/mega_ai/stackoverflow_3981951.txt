[site]: stackoverflow
[post_id]: 3981951
[parent_id]: 
[tags]: 
Most elegant way to express random choice between two options in JavaScript

Given an Array of two elements what is the most elegant way to express a random coin toss to choose between them? var choices = ['#00f', '#ff0']; document.bgcolor = choices[Math.floor(Math.random() * choices.length)];
