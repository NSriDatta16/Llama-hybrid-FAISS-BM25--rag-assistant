[site]: stackoverflow
[post_id]: 2346752
[parent_id]: 2346744
[tags]: 
You can read Generic Drag article. It works with "pure" JavaScript. However, I still recommend jQuery UI because it's more feature-rich and probably cross-browser. In jQuery UI this is easily done using $('#divId').draggable();
