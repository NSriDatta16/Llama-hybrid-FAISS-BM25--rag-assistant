[site]: stackoverflow
[post_id]: 4787004
[parent_id]: 4276437
[tags]: 
I don't think there's such a library but you could solve it in the following way (if your xml fragment starts with an element). I assume you are familiar with XMLUnit. public boolean isSubset(Document document, Element element) { NodeList list = document.getElementsByTagName(element.getNodeName()); for (int i = 0; i
