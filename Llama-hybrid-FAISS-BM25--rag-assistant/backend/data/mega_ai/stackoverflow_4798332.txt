[site]: stackoverflow
[post_id]: 4798332
[parent_id]: 4798231
[tags]: 
Keep state around, which can be done via fragment URLs or HTML5 history . e.g., make opening up the skills tab change the fragment to #skills , which will remain across a refresh. Then check window.location.hash in your onLoad to determine what initial state your page should be in. function switchToTab(tabName) { // DOM/CSS manipulation etc. here } var tabs = ['bio', 'skills', 'creds']; var initialTab = 'bio'; for (var i = 0; i Untested, and there's plenty of JS libraries out there that abstract this away for you.
