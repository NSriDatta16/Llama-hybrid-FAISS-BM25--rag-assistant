[site]: datascience
[post_id]: 19647
[parent_id]: 
[tags]: 
Bahdanau Attention

In the original paper, the author says that the annotation are the concatenation fo the forward states and the backward states at each time step. In the tensorflow implementation (memory param), the memory field is said to be populated with the output (not hidden state) of an RNN encoder. What am I missing?
