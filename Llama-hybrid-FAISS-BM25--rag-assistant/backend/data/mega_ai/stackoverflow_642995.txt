[site]: stackoverflow
[post_id]: 642995
[parent_id]: 548352
[tags]: 
You can avoid changing file settings by enabling and disabling optimisations using #pragma optimize in code: eg.cpp: // top of the file, after headers #if defined(DEBUG) && defined(PARTIAL_OPTIMISATION) #pragma optimize( "g", on ) #endif void MyFunc() {...}
