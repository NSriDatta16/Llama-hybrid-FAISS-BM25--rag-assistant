[site]: stackoverflow
[post_id]: 340231
[parent_id]: 340217
[tags]: 
Although I cannot exactly understand, what you need and how are tables related, I'd try someting similar: select a.id, a.agent_name, a.ticket_id, a.category, a.date_logged, b.department from table1 a inner join table2 b on b.agent_name=a.agent_name Currently I'm assuming that you need to link tables on agent_name. BTW, joins are the simplest end of the SQL spectrum :)
