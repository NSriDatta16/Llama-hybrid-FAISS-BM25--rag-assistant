[site]: stackoverflow
[post_id]: 939798
[parent_id]: 939689
[tags]: 
You should leverage Hibernate's lazy loading features for this. You can annotate a collection like: @OneToMany(fetch=FetchType.LAZY) In that case Hibernate only loads the collection if you actually access it in code.
