[site]: crossvalidated
[post_id]: 428838
[parent_id]: 
[tags]: 
Feature vector formulation for a Neural Network

So I'm implementing a simple ANN where I have a massive input data set. The input data contains all kinds of stuffs like eg: categorical values : button,table, image...; binary values : true-false...; continuous variables : length, width...; tuple values : rgb(1,0,0),...; text values : file name, absolute location... So my question is how do I take care of this different types of input data, like for binary values- we can directly encode it to o-1, categorical use 1hot encoder to encode to 0-1 matrix. But how do I take care of continuous variables, tuple values and texts ?? And can I parse the date to one single Neural network?
