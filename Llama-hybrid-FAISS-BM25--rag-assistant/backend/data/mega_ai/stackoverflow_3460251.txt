[site]: stackoverflow
[post_id]: 3460251
[parent_id]: 1876507
[tags]: 
At the mentioned Java bug page you will find a workaround that is reported to work using ProcessBuilder and wrapping the parameters in environment variables. Here is the source code from Parag Thakur: String[] cmd = new String[]{"yourcmd.exe", "Japanese CLI argument: \ufeff\u30cb\u30e5\u30fc\u30b9"}; Map newEnv = new HashMap (); newEnv.putAll(System.getenv()); String[] i18n = new String[cmd.length + 2]; i18n[0] = "cmd"; i18n[1] = "/C"; i18n[2] = cmd[0]; for (int counter = 1; counter env = pb.environment(); env.putAll(newEnv); final Process p = pb.start();
