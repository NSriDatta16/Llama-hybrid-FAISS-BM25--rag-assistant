[site]: stackoverflow
[post_id]: 1529209
[parent_id]: 1528037
[tags]: 
from rd in dc.RouteDispatches join r in dc.Routes on rd.RouteID equals r.RouteID join s in dc.References on rd.StatusCodeReferenceID equals s.ReferenceID join e in dc.Employees on rd.CreatedByEmployeeID equals e.EmployeeID from md in ( from md in dc.MachineDispatches where md.RouteDispatchID == rd.RouteDispatchID && md.IsSelected == 1 select md ).DefaultIfEmpty() from mdp in ( from mdp in dc.MachineDispatchPicks where mdp.MachineDispatchID == md.MachineDispatchID select mdp ).DefaultIfEmpty() from mh in ( from mh in dc.MachineHistories where mh.MachineID == md.MachineID && mh.ReadDate > m.LastServiceDate select mh ).DefaultIfEmpty() group .... etc...
