[site]: stackoverflow
[post_id]: 4758702
[parent_id]: 4758672
[tags]: 
I am not sure if this is what you need. function renderDropDown($name, $opts, $valueIsKey=true){ $out = array(); $out[] = ' '; $out[] = ' '.ucfirst($name).' '; foreach($opts as $key => $val){ if($valueIsKey){ $out[] = ' '.$val.' '; } else { $out[] = ' '.$val.' '; } } $out[] = ' '; return implode("\n", $out); } This is simple, but you'll have the plural s at the end of each name. You may want to add an argument $title to the function, so it will become: function renderDropDown($name, $opts, $title, $valueIsKey=true){ [...] $out[] = ' '.$title.' '; [...] } You can call that function with something like: renderDropDown('days', range(1,31), "Day", false); Edit : you may want to look at this . Hope that helps. —Alberto
