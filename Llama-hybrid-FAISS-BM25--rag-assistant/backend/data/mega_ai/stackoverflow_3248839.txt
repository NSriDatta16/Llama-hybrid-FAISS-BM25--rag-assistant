[site]: stackoverflow
[post_id]: 3248839
[parent_id]: 3248745
[tags]: 
yeah, what Klinger said ((Textbox)sender).Text.Trim() except you shouldn't need to test for null, since even if you set it to null, on retrieval it will return string.empty anyway. [note that (sender as Textbox) is VB.net format]
