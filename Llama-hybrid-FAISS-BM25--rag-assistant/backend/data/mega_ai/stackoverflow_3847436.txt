[site]: stackoverflow
[post_id]: 3847436
[parent_id]: 
[tags]: 
Spring Framework filter, bean not injected

There are 2 entries for a Servlet Filter, one in web.xml and one in Spring applicationContext.xml I added the filter into applicationContext.xml because I wanted to inject creditProcessor bean into it. The only problem is that the entry in web.xml got picked up by JBoss and then used, so creditProcessor is null. Do I have to use Spring's delegatingFilterProxy or similar so I can inject stuffs into the bean, or can I tweak the web.xml? web.xml: CreditFilter credit.filter.CreditFilter CreditFilter /coverage/* Spring-applicationContext.xml:
