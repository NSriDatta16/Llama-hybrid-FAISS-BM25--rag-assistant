[site]: datascience
[post_id]: 112642
[parent_id]: 
[tags]: 
Why XGboost does not work on small dataset

Here I am using Xgboost for classification for a simple small dataset, when x = 0 then y = 1 elif x = 1 then y = 0. Then I use the xgb.XGBClassifier() but the resulting probability is just 0.5. I wonder why this happens.
