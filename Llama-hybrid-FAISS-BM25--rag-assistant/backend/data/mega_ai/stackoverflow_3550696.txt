[site]: stackoverflow
[post_id]: 3550696
[parent_id]: 3550350
[tags]: 
As dmags said, I prefer to use array (and I use this regularly). It's more elegant (IMHO) and works also if you have different variable names (i.e. name, address, city, country, zip, etc...) $vars= array('sub1','sub2','sub3','sub4','sub5','sub6','sub7','sub8','sub9','sub10'); foreach ($vars as $getvar) if (isset($_GET[$getvar])) { $$getvar= $_GET[$getvar]; } }
