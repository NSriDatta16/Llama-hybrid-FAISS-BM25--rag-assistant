[site]: stackoverflow
[post_id]: 5133279
[parent_id]: 
[tags]: 
Cancel dragging of a sortable item

An absolutely common sortable case: $(function() { $("#sortable").sortable(); }); Item 1 Item 2 Item 3 Problem. Need to cancel dragging of an item on some condition and there is a good advice of Andrew Whitaker , but that approach is working only for jquery-ui-draggable and fails for sortables: $("#sortable").sortable({ start: function() { return false; // will still cause `this.helper is null` } }); Will be greateful for suggestions.
