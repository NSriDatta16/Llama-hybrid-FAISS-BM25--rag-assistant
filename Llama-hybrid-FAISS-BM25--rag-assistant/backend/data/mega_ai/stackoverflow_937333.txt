[site]: stackoverflow
[post_id]: 937333
[parent_id]: 540034
[tags]: 
Look at Direct Attached Storage and Storage Area Network . From what I can tell, the DAS is cheaper as it runs over existing network infrastructure, while a SAN essentially has its own dedicated fiber network that is parallel to the regular network. actually that wikipedia link is old.. see dell's DAS options for a better example.
