[site]: stackoverflow
[post_id]: 3225325
[parent_id]: 3225298
[tags]: 
Not exactly elegant, but this brute-force solution is the first to come to mind. Split, and then combine if short: var lines = content.Split(...); string header[] = lines[0].Split(...); int numberOfColumns = header.Length; var parsedLines = new List (); for (int i = 1; i
