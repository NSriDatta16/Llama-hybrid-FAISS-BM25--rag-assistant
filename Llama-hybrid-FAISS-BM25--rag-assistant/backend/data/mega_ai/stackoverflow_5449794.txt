[site]: stackoverflow
[post_id]: 5449794
[parent_id]: 
[tags]: 
problem getting my program to work

I'm having a novice problem with my program here.... I want it to print a list of cities in both ascending and descending order, so far i'm only getting the descending part implemented. #include #include #include #include int main(void) { int i, j, ch, sort=0; printf("HOW WOULD YOU LIKE TO SORT?\n\nASCENDING\n\nDESCENDING\n\n"); ch=getc(stdin); if(toupper(ch)=='A'&&tolower(ch)=='a') sort=1; if(sort==1) printf("\nSORT ASCENDING...\n"); else printf("\nSORT DESCENDING...\n"); printf("\nHOW MANY CITIES WOULD YOU LIKE? : - \n"); scanf("%d",&i); char NAMES[i][20]; j=0; while(j 0) { strcpy(swapNAME,NAMES[r]); strcpy(NAMES[r],NAMES[k]); strcpy(NAMES[k],swapNAME); } } } printf("\n THE SORTED LIST IS NOW : \n"); for(r=0;r please any efforts made will greatly be appreciated aganin thankz
