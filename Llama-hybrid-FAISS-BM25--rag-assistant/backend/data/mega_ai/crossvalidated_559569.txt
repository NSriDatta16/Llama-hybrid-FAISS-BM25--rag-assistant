[site]: crossvalidated
[post_id]: 559569
[parent_id]: 
[tags]: 
Finding lagged time series effects

I have a time series with the number of customers and discounts. I suspect that the percentage of discount has a postponed effect. I tried just lagging the variable and calculating the correlation, but looking for other suggestions to increase my knowledge about this kind of problem. My data set library(lubridate) library(tidyverse) date $discount, lead(df$ n_customers, i), use = "complete.obs") } cors
