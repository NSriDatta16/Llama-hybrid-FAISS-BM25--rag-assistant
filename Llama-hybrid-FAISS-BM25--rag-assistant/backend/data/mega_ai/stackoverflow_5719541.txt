[site]: stackoverflow
[post_id]: 5719541
[parent_id]: 
[tags]: 
Magento:constructing product URL

I have been able to get attributes like name, image URL, description from a magento store. However getting the product URL is tedious. Any help?? Thanks. include_once 'app/Mage.php'; umask(0); Mage::app(); $products = Mage::getModel('catalog/product')->getCollection(); $products->addAttributeToSelect('*'); $products->load(); $baseUrl = Mage::getBaseUrl(Mage_Core_Model_Store::URL_TYPE_WEB); foreach ($products as $id => $product): $output .= ' '. $product['url_key'] .' '. $baseUrl ."media/catalog/product". $product['image'] .' ';
