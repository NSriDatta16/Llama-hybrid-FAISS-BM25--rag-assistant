[site]: stackoverflow
[post_id]: 5519514
[parent_id]: 
[tags]: 
copy and paste on textarea won't work on mysql

if i copy a text paragraph and paste it on a textarea for an update content my mysql database won't update the content but when i type it manually on the textarea, the mysql database will then be update the content..how can i update the content in the mysql using copy and paste method if (isset($_POST['submit'])) { $main = $_POST['main']; $result = mysql_query("UPDATE tbl_content SET main ='$main' WHERE users = '$name' }
