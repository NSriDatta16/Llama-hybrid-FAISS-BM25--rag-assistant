[site]: stackoverflow
[post_id]: 1564552
[parent_id]: 
[tags]: 
Anchor tag onclick to call a code behind method

I use Register in my aspx page. Once true is returned from the function orgvalid() I want to call a code behind function, insertorganization() . Is it possible to do so? If so, how?
