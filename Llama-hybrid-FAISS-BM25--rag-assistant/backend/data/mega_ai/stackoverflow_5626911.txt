[site]: stackoverflow
[post_id]: 5626911
[parent_id]: 
[tags]: 
How do I left join onto a mysql range case group by query?

How do I LEFT JOIN an average onto this query? select t.range, count(*) as num from (select case when price = 50000 and price = 100000 and price I've tried select t.range, count(*) as num, avg(b.val) from (select case when price = 50000 and price = 100000 and price And various other feeble attempts to no avail.
