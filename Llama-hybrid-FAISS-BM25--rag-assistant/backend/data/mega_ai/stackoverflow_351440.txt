[site]: stackoverflow
[post_id]: 351440
[parent_id]: 
[tags]: 
Determine record in multi record html form

In a html form, I'm displaying multiple records from a table, ready for update. Right now I use: name= _ value= for the fields. Then in my python-script I go for: for key in form.keys(): if key.startswith(' _'): update set = where pk= if key.startswith(' _'): update set = where pk= Is there a more 'organic' way to handle multi-record forms?
