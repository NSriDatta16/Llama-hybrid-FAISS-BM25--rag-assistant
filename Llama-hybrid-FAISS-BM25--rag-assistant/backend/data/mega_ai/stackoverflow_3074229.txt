[site]: stackoverflow
[post_id]: 3074229
[parent_id]: 3074154
[tags]: 
Probably not the most elegant solution, but how about this? //TreeSet with reversed natural ordering (big integers first) Map > h = new TreeMap >(Collections.reverseOrder()); //and use TreeSet for the set... // ... // for(Map.Entry > entry : h.entrySet()){ for(String str : entry.getValue()){ System.out.println(str + " has occured " + entry.getKey() + " times."); } }
