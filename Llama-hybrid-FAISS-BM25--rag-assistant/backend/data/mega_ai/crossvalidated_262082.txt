[site]: crossvalidated
[post_id]: 262082
[parent_id]: 
[tags]: 
In a finite state Markov Chain $p_{ij}^{(n)}\le M\lambda^n$

In a finite state Markov Chain , show that there exists $M>0$ and $0 I guess $\lambda$ should be something like $\max\limits_{i,j\in S}p_{ij}$ (I might be wrong) , but I have no clue abut $M$. $S$ is set of transient states
