[site]: stackoverflow
[post_id]: 3308765
[parent_id]: 3308433
[tags]: 
we have aysnc webservice, that runs the long running job, we use a secondary method on the webservice to query the database and return the value to the UI, so we could have multiple jobs running and return the value. Not elegant but it works. Cheers Iain
