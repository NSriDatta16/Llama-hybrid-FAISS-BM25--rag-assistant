[site]: stackoverflow
[post_id]: 1397202
[parent_id]: 1397190
[tags]: 
You put the #pragma once before the #include "stdafx.h" which I think is causing the compiler to ignore the #pragma once directive. Also, I don't think you should be putting the #include "stdafx.h" line into the header files at all.
