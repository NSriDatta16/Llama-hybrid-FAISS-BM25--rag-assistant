[site]: stackoverflow
[post_id]: 5098266
[parent_id]: 5098230
[tags]: 
Your list is null , thus null[i] throws NullPointerException You should consider either initialize it as ChrisJ describes or use a List List list = new ArrayList (); .... list.add( rs.getString("product_name")); .... edit This solves two parts, your NullPointerException and the dynamic fetch of the data. You mention you need to put this in a JList, in that case, you can still use this approach, but separate the data retrieval from the displaying code. So, create a method list this: public String[] fetchOptions() { List list = new ArrayList (); ... db code here while .... etc. et list.add( rs.getString("product_name")); ... // convert it to String[] return list.toArray( new String[list.size()] ); } And then call this method where you create your JList String [] options = fetchOptions(); JList aList = ... use it normally
