[site]: stackoverflow
[post_id]: 2535014
[parent_id]: 2534998
[tags]: 
In a turn-based situation (game) you actually don't need threads, fibers/coroutines will do (better). 'Paying attention' does not require a thread, just access to the state (-changes) when you are ready to act again.
