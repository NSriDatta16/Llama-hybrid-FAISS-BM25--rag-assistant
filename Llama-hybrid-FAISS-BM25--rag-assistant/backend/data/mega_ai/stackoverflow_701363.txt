[site]: stackoverflow
[post_id]: 701363
[parent_id]: 701322
[tags]: 
Just to give you an alternative, you could repeatedly divide the integer by 10, and then rollback one value once you reach zero. Since string operations are generally slow, this may be faster than string manipulation, but is by no means elegant. Something like this: while(curr>=10) curr /= 10;
