[site]: stackoverflow
[post_id]: 2315629
[parent_id]: 2315465
[tags]: 
I use position:relative; so that superscript characters can still ascend above vertical-align: top; but doesn't allow them to mess with the leading of my paragraphs. sup { font-size: .7em; vertical-align: top; position: relative; top: -.1em; }
