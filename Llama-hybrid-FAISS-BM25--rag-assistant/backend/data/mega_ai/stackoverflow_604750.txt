[site]: stackoverflow
[post_id]: 604750
[parent_id]: 575435
[tags]: 
It looks like the column would have to be an UltraTreeNode for that to work. Presumably you are putting the column data into a (derived) UltraTreeNode of some sort, so you might want to try decanting it and using that object in the DoDragDrop call. -- MarkusQ
