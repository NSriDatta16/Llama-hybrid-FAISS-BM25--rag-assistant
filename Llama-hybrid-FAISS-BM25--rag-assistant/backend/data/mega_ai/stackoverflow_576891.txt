[site]: stackoverflow
[post_id]: 576891
[parent_id]: 576749
[tags]: 
Why not pass the entire list of as a template parameter? Not necesarily the most elegant... but it would avoid adding conditional logic to the template (ie. "is this item the currently selected item? Yes output "selected", no output nothing).
