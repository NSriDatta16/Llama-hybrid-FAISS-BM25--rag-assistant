[site]: stackoverflow
[post_id]: 4035612
[parent_id]: 4035570
[tags]: 
you can't have more than one element with the same id not only is it invalid html but if you trageted it using jquery it would only effect the first one. you should use a class $('.class_name').blur();
