[site]: stackoverflow
[post_id]: 2353980
[parent_id]: 
[tags]: 
At what exact moment is a local variable allocated storage?

Suppose we have the following: void print() { int a; // declaration a = 9; cout Is the storage for variable a allocated at the moment function print is called in main or is it when execution reaches the declaration inside the function?
