[site]: stackoverflow
[post_id]: 2501850
[parent_id]: 2500467
[tags]: 
Support vector machines ? libsvm can be used from Python, and is quite speedy. Handles sparse vector inputs, and won't mind if some of the features are continuous, where others are just -1/+1. (If you've got an n-way discrete feature, the standard thing to do is expand it into n binary features.)
