[site]: stackoverflow
[post_id]: 3779355
[parent_id]: 3774755
[tags]: 
I don't have this working using sortable, but I did using draggable & droppable. I don't know if I covered all the functionality you need, but it should be a good start ( demo here ): HTML Available Boxes (click to select multiple boxes) Box #1 Box #2 Box #3 Box #4 My Boxes Script $(document).ready(function(){ var selectedClass = 'ui-state-highlight', clickDelay = 600, // click time (milliseconds) lastClick, diffClick; // timestamps $("#draggable li") // Script to deferentiate a click from a mousedown for drag event .bind('mousedown mouseup', function(e){ if (e.type=="mousedown") { lastClick = e.timeStamp; // get mousedown time } else { diffClick = e.timeStamp - lastClick; if ( diffClick
