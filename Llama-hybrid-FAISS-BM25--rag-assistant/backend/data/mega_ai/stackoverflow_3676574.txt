[site]: stackoverflow
[post_id]: 3676574
[parent_id]: 3676426
[tags]: 
Just for your information, but there is a better syntax then to prepend the whole thing as a huge HTML fragment. You can construct the whole thing with jQuery from scratch: var anchor = $(' ', { 'class': 'booknow2 sidelink sidelinkNew', // Try to use event handlers instead - inline event handlers are *bad* href: "javascript:__doPostBack('SetSess','')" }).prependTo(this); $(' ', { 'width': '4', 'height': '35', 'alt': 'book now', 'src': '../../images1/button/rightEdge.png' }).css({ // Try to use CSS instead - add a class // and define the CSS in a separate stylesheet 'float': 'left', 'margin': 0, 'padding': 0, }).appendTo(anchor); $(' ', { html: 'Check availability &raquo;' }).appendTo(anchor);
