[site]: stackoverflow
[post_id]: 2042818
[parent_id]: 2042804
[tags]: 
It's a callback/upcall implemented with an anonymous inner class. "Inline class" is some made up terminology (where did it come from??). The new version of closures that should appear in JDK7 should make this sort of thing much less verbose. The idiom is known as Execute Around .
