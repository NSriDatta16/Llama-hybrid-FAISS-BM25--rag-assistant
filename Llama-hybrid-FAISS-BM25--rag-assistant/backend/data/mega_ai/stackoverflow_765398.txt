[site]: stackoverflow
[post_id]: 765398
[parent_id]: 
[tags]: 
C#: No casting within Generics?

While I can upcast a string to an object, I cannot upcast an IList of strings to an IList of objects. How come? What to do now other that coping all items to a new IList? static void ThisWorks() { IList list = new List (); list.Add("I can add a string since string : object"); } static void ThisDoesNotWork() { // throws an invalid cast exception IList list = (IList ) new List (); list.Add("I'm never getting here ... why?"); }
