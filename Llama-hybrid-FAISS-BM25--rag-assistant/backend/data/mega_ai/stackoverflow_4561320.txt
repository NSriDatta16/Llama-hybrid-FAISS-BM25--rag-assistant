[site]: stackoverflow
[post_id]: 4561320
[parent_id]: 4561249
[tags]: 
You can try to avoid hijacking by testing user agent. Something like that : if (isset($_SESSION['userAgent'])) { if ($_SESSION['userAgent'] != md5($_SERVER['HTTP_USER_AGENT'])) { // HACK !!! // Kill the process or ask for authenticating } } else { $_SESSION['userAgent'] = md5($_SERVER['HTTP_USER_AGENT']); }
