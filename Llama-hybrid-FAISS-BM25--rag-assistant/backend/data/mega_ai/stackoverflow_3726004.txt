[site]: stackoverflow
[post_id]: 3726004
[parent_id]: 3725970
[tags]: 
You would need to check the user agent. You will need to define what is a mobile platform to you. One hack may be to include a media type mobile stylesheet, and have it set an element to something specific that you could check for using JavaScript. This is hacky though, but it will mean you won't need to parse user agents.
