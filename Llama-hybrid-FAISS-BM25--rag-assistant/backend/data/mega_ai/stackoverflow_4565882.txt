[site]: stackoverflow
[post_id]: 4565882
[parent_id]: 4565863
[tags]: 
closest() searches ancestors , not siblings . Remove it, prev() will find the previous sibling for you (you can always pass in the p selector to ensure it only selects the previous paragraph): $(this).prev("p").toggleClass("showComment");
