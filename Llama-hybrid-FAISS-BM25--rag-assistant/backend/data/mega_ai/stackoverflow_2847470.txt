[site]: stackoverflow
[post_id]: 2847470
[parent_id]: 1539152
[tags]: 
Read this: link It seems server 2008 has a known error on this (IIS7 has a max. upload restriction you have to manually edit via the web.config of the specific web app I'm currently working on this fix, locally now the upload works (respecting the set max. upload limit), except via Internet (passed the ISA) it still doesn't. I'm trying to figure out if I also have to edit the WFE's to make it work via the Internet, but not sure if this fixed my problem or has anything to do with it. Roberto
