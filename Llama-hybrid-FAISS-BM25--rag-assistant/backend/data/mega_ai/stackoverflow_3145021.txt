[site]: stackoverflow
[post_id]: 3145021
[parent_id]: 3144989
[tags]: 
$score_arr = array(4.0, 3.2, 2.2); function getGPA($score_arr) { $count = count($score_arr); $sum = array_sum($score_arr); $gpa = $sum / $count; return $gpa; } Add: Could be modified to accept letter grades, compare via the chart, and sum scores appropriately. The details were missing when I began the post.
