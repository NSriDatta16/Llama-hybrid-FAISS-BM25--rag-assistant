[site]: crossvalidated
[post_id]: 239461
[parent_id]: 
[tags]: 
cost function which does not have a min/max point

In machine learning, is it meaningful to have a cost function which does not have a min/max point, i.e., the cost function's min/max point is negative/positive infinity? Any examples?
