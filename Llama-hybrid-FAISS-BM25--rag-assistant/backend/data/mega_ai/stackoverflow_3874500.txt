[site]: stackoverflow
[post_id]: 3874500
[parent_id]: 
[tags]: 
How to keep certain values in an array in R?

Suppose I have a data array, dat Q1: What I do if I want to get a new data array, datnew in which, all the positive value remain and the negative value changed to NA ? Q2: What I do if I want to get a new data array, datnew by averaging with the third dimension, but only averaging every 8 values? Thanks a lot.
