[site]: stackoverflow
[post_id]: 2938821
[parent_id]: 2938806
[tags]: 
A code fragment running concurrently by multiple threads has the potential of causing race conditions , regardless of where this code is placed (static/non-static method). You must make sure that the data manipulated by the code tolerates concurrent accesses, and synchronize the code properly if needed.
