[site]: stackoverflow
[post_id]: 5664375
[parent_id]: 5664263
[tags]: 
This uses the jquery library and is borrowed heavily from this answer on SO to a related question . What it does is break up each word in a paragraph tag ( ) and wrap it with a tag with a class of highlightable and then attach an event on hover to the tags. If you want only one paragraph to be used, then give it an ID and bind only to those items in that given ID. If a few paragraphs, use a class or multiple IDs. Each word will be wrapped in a span. A second paragraph here. $(document).ready(function() { // wrap words in spans $("p").each(function() { $(this).text(). replace(/\b(\w+)\b/g, " $1 "); }); // bind to each highlightable span $('.highlightable').hover( function() { $(this).css('background-color','#ffff66'); }, function() { $(this).css('background-color',''); } ); });
