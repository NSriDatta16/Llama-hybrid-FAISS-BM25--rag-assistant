[site]: stackoverflow
[post_id]: 2913986
[parent_id]: 2913894
[tags]: 
An iframe is a frame in an otherwise normal document. Normal frames are placed in a document which consists entirely of frames (plus, hopefully, some fallback content from user agents that don't support them).
