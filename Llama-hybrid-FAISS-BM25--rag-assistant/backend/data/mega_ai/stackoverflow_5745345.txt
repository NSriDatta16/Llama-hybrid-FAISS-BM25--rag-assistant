[site]: stackoverflow
[post_id]: 5745345
[parent_id]: 
[tags]: 
Passing unspecialized template as a template parameter

Can i do something like template , template class Allocator = allocator > class mymap { typedef map > > storageMap; typedef vector > storageVector; } So template is passed to the class unspecialiazed and instantiated later.
