[site]: stackoverflow
[post_id]: 3952211
[parent_id]: 3951213
[tags]: 
As an alternative you can go here: http://wiki.eclipse.org/MDT-UML2-Tool-Compatibility and download one of the tools that extend the UML2 component (all of them use exactly the same storage format but add a different user interface). For instance, take a look at MOSKitt. Some of these tools come prepackaged so there´s nothing to install, just download and execute them
