[site]: stackoverflow
[post_id]: 4007187
[parent_id]: 4007176
[tags]: 
You could always use If ipAddress.ToString().StartsWith("172.16) Then ' Now you've got a match ipAddress = tmpAddr(i).ToString() End If Of course, my code is bad because you're calling .ToString() twice, which could be a performance drag if there are a lot of addresses to parse, so you may want to modify it slightly...
