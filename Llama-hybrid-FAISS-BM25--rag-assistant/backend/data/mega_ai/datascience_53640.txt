[site]: datascience
[post_id]: 53640
[parent_id]: 
[tags]: 
Y-axis output is not order

I am currently attempting to output some data based on user agent strings. The strings are too messy to place into a plot similar to the one shown below, so I plotted their index instead. Why is my plot outputting out of order? I would like to have my $y$ -axis be in ascending order. For context, I am plotting 6 plots with a for loop and none of them are producing the y-axis in order. Edit: I added ' uas_id ' dplyr code with solution. gbuas_large % mutate(uas_id = sort(strtoi(rownames(gbuas_large))))%>% select(user_agent_string,uas_id,everything()) col
