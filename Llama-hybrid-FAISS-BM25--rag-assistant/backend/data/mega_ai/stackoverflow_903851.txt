[site]: stackoverflow
[post_id]: 903851
[parent_id]: 903345
[tags]: 
One idea is to minimize compile time by reducing the permutations (different version for each browser) during development. In your module.gwt.xml force an user agent, for example gecko1_8 (Firefox 2). If you're using i18n you can also limit the locales used during development.
