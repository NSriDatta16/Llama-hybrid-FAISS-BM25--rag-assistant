[site]: datascience
[post_id]: 103817
[parent_id]: 
[tags]: 
Confusion matrix with different levels

I want to print a confusion matrix, but data and reference have not the same level. How can I do? this is my actual code: library(xgboost) library(tidyverse) library(caret) library(readxl) library(data.table) library(mlr) data $Species ts_label Species new_tr 0.5,1,0) #confusion matrix library(caret) confusionMatrix (xgbpred, ts_label)
