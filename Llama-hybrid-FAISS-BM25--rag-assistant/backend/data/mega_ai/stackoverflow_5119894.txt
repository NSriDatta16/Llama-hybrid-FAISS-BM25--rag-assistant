[site]: stackoverflow
[post_id]: 5119894
[parent_id]: 
[tags]: 
Importing directories of resources into a JNLP?

Can I import an entire directory of jar files at once, or must I specify each jar individually when creating a JNLP? If the latter, are there any workarounds?
