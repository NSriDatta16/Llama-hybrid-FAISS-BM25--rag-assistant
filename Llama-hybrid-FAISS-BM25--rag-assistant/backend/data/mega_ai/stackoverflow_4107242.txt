[site]: stackoverflow
[post_id]: 4107242
[parent_id]: 3558347
[tags]: 
I'm not sure how detailed your concurrent needs are, but OpenMP works fine (i.e., you can combine options /clr and /openmp ) array ^ model ....; #pragma omp parallel for for(int i=0;i Length;i++) { model[i] = ComputeModelFor(i); }
