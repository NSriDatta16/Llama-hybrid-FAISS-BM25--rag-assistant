[site]: stackoverflow
[post_id]: 1977599
[parent_id]: 1532172
[tags]: 
Yes, vacuum is a recognized SQL statement in SQLite. It can be used as a normal query, or so it says . Beware, though, as it can lead to excessive file system reads and writes—the bottleneck of practically any system—not to mention server file system fragmentation on Windows servers.
