[site]: stackoverflow
[post_id]: 467816
[parent_id]: 467807
[tags]: 
It means you have to balance them; don't get trapped by either/or thinking. On the other hand, if you do need configuration files, don't be trapped into thinking they must be XML. XML is a relatively heavyweight representation; JSON, YAML etc is widely available and easier to use for most configuration file sorts of storage.
