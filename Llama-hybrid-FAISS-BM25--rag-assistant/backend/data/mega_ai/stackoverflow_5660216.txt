[site]: stackoverflow
[post_id]: 5660216
[parent_id]: 
[tags]: 
local storage in IE

When I tried to get item from the local storage value , it returns "undefined" in IE8 I'm using the following codes for : Set item : var value = document.getElementById('test').value; var key = "i" + x + "test" + y; localStorage[key] = value; And get Item: var test = localStorage["i" + x + "test" + y]; alert(test); It return undefined I followed the same code for firefox 4 and chrome .. it returns the same value which i have set in the local storage
