[site]: crossvalidated
[post_id]: 575296
[parent_id]: 575238
[tags]: 
The answer is in the code that you present, in particular, the following function call: cph(S ~ ps) that fits a Cox proportional hazards model for survival outcomes S against a set of sparse PCA scores called ps . You can do the same for any type of regression. Earlier code shows where that predictor ps comes from. pcs with ps just a subset of pcs : for(i in 1:10) { ps
