[site]: crossvalidated
[post_id]: 510987
[parent_id]: 
[tags]: 
Do negative weights affect learning when using ReLUs in neural networks?

When we use ReLU as our activation function can we say that negative weights don't have any effect on learning process of the neural network?
