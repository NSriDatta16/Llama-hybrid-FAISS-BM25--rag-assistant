[site]: stackoverflow
[post_id]: 1071062
[parent_id]: 1070425
[tags]: 
very simple: i don't like your paths for your files, so i changed it. ^_^ create the link to go to the main storage immediately. http://www.domain.com/file/filename.jpg on the main server use a ruleset like so RewriteCond %{REQUEST_FILENAME} !-f RewriteCond %{REQUEST_URI} /file/(.+) RewriteRule ^/file/.+ /temp_content/%1 [L,R]
