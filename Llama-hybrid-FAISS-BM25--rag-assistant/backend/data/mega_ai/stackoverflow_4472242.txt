[site]: stackoverflow
[post_id]: 4472242
[parent_id]: 
[tags]: 
Add-member in iteration doesn't work in PowerShell

Here is the code: I get projects list Iterate through the list and add member on the current value doesn't work [psobject] $tfs = get-tfs "http://myserver:8080/tfs/defaultcollection" [psobject] $projects = $tfs.CSS.ListAllProjects() foreach($pro in $projects){ $pro | add-member -membertype noteproperty -name TFS -value $tfs $test = $pro.TFS }
