[site]: stackoverflow
[post_id]: 1032001
[parent_id]: 1031945
[tags]: 
Maybe you could set the location's fragment to an empty string and store the old value upon page load, then reset location's fragment when all's done. ASP.Net probably can't help because the fragment won't be sent to the server.
