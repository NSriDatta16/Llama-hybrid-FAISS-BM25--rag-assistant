[site]: stackoverflow
[post_id]: 2754469
[parent_id]: 
[tags]: 
R library for discrete Markov chain simulation

I am looking for something like the 'msm' package, but for discrete Markov chains. For example, if I had a transition matrix defined as such Pi for states A,B,C. How can I simulate a Markov chain according to that transition matrix?
