[site]: datascience
[post_id]: 117735
[parent_id]: 
[tags]: 
problem on designing a custom_loss function

I am using CNN to solve a regression problem in a supervised manner. i have input data(X_train) and the target data(y_train). I allow the network to train and during training process in each batch of the corresponding epoch
