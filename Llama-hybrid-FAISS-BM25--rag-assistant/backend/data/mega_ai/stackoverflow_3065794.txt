[site]: stackoverflow
[post_id]: 3065794
[parent_id]: 3065708
[tags]: 
This is a version close to what @GMan did, but it doesn't accept precedding spaces. e.g. " 101" : #include #include #include #include #include long strict_conversion(const std::string& number_string) { long number; std::stringstream convertor; convertor > number; if( convertor.fail() || !convertor.eof() ) throw std::runtime_error("The string didn't pass the strict conversion!"); return number; } After one minute, here is the generic one: template NumberType strict_conversion(const std::string& number_string) { NumberType number; std::stringstream convertor; convertor > number; if( convertor.fail() || !convertor.eof() ) throw std::runtime_error("The string didn't pass the strict conversion!"); return number; }
