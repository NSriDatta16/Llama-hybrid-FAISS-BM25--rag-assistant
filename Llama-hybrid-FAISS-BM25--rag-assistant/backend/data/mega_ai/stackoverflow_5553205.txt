[site]: stackoverflow
[post_id]: 5553205
[parent_id]: 5552989
[tags]: 
The easiest solution is probably to construct an array during the loop and average it at the end. // Before the loop $ratings = array(); // During the loop $ratings[] = $post_rating; // After the loop $average = array_sum($ratings) / count($ratings);
