[site]: stackoverflow
[post_id]: 4511314
[parent_id]: 
[tags]: 
Filter Magento collection but not products, using distinct

In my magento store I am trying to retrieve a list of youtube videos from a DB table, some videos are duplicated. I'm looking to filter videos by using distinct video "value" but can't seam to find any information about using distinct with addFieldToFilter() // get video collection $collection = Mage::getModel('video/video')->getCollection(); $collection->addFieldToFilter('provider', 'youtube'); // filter by video value $collection->addFieldToFilter('value')->distinct(true); By removing $collection->addFieldToFilter('value')->distinct(true); it works, but retrieves all the videos.
