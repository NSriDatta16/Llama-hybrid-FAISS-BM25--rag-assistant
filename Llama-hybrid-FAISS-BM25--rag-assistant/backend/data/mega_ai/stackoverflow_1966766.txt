[site]: stackoverflow
[post_id]: 1966766
[parent_id]: 1966750
[tags]: 
I'm not sure about this, I've never used it, but from what I've read it should be something like this: .... t = Timer("raw_queries(queries, nlp)", "from __main__ import raw_queries") print t.timeit() I took this from http://docs.python.org/library/timeit.html (if this helps).
