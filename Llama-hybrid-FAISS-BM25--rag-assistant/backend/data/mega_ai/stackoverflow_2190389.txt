[site]: stackoverflow
[post_id]: 2190389
[parent_id]: 2190378
[tags]: 
if the two tables are identical (columns, etc) you might want to go through all rows of one trable and append them to the other one. For convenience you could use an extension method. Maybe there's a more elegant version, but that's a first thought. -sa
