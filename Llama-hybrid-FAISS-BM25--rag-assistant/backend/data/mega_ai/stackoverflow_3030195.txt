[site]: stackoverflow
[post_id]: 3030195
[parent_id]: 
[tags]: 
how can i get MC in stage function?

i create three circles. if a circle is dragged to right side of the stage, it becomes invisible and vice versa. when MOUSE_UP is invoked, it must stay in its last position. so in the appear() function how can i assign the selected circle to current_mc? function createCircles(evt:Event):void { for(i=0; i stage.width/2) current_mc.visible=false; else current_mc.visible=true; stage.addEventListener(MouseEvent.MOUSE_UP, upFNC); } function upFNC(evt:MouseEvent):void { stage.removeEventListener(Event.ENTER_FRAME, appear); }
