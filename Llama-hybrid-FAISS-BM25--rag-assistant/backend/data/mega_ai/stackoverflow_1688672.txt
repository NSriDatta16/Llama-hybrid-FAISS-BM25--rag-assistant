[site]: stackoverflow
[post_id]: 1688672
[parent_id]: 1688591
[tags]: 
I personally use the jQuery ui sortable plugin. The client can drag and drop the items from a list, changing the order. Next I use jQuery again to loop through the list of items, compile CSV list of id's and send it to the server. var imageList; function init() { imageList = $("#imagelist"); if(imageList.length > 0) { imageList.sortable({stop:onStopSorting}); } } function onStopSorting() { var datas = imageList.find("span.data"); var result = new Array(); for(var i = 0; i and on the server: if(isset($_REQUEST["imageSort"]) && $_REQUEST["imageSort"] != "") { $imageIdList = explode(",", $_REQUEST["imageSort"]); for($i = 0; $i setImagePriority($imageIdList[$i], $i+1); } } It might be more "quick 'n dirty" than a nice Design Pattern, but it works.
