[site]: crossvalidated
[post_id]: 32935
[parent_id]: 32901
[tags]: 
Here is some R code to complement previous answers ( pca[["sdev"]] is usually written pca$sdev , but it causes misformatting in the snippet below). # Generate a dummy dataset. set.seed(123) x So, as @Max points out, working with the variance instead of the standard deviation and not forgetting to divide by the total variance solves the issue.
