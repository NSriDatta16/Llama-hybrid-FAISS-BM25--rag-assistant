[site]: stackoverflow
[post_id]: 2413729
[parent_id]: 2413144
[tags]: 
import pickle import datetime ... delta = end_time - start_time for_storage = pickle.dumps(delta) #now you have a string representation of your timedelta object that you can store #sometime later... delta = pickle.loads(from_storage) You'll still need to convert the delta to a time resolution of your choice using the days, mins, seconds, and microseconds attributes of the time delta.
