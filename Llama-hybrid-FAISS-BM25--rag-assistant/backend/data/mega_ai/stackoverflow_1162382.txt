[site]: stackoverflow
[post_id]: 1162382
[parent_id]: 1162245
[tags]: 
Looping: foreach(keys %my_hash) { print "$_ / $my_hash{$_}\n"; } Functional map {print "$_ / $my_hash{$_}\n"; } keys %my_hash; But for sheer elegance, I'd have to choose wrang-wrang's. For my own code, I'd choose my foreach. Or tetro's Dumper use.
