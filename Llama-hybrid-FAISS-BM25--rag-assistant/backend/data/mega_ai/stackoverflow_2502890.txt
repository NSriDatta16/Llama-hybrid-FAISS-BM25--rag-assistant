[site]: stackoverflow
[post_id]: 2502890
[parent_id]: 1644668
[tags]: 
function: $.fn.splitUp=function(splitBy,wrapper) { $all= $(this).find('>*'); var fragment=Math.ceil($all.length/splitBy); for(i=0; i usage: $('ul#slides').splitUp(4,'&lt;li class=splitUp&gt;&lt;ul&gt;') or: $('div#slides').splitUp(3,'&lt;div/&gt;')
