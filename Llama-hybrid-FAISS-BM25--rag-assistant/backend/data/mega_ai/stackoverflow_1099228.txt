[site]: stackoverflow
[post_id]: 1099228
[parent_id]: 1099183
[tags]: 
You can find out where you were downloaded from with BasicService and then use HTTP to transfer the file. You could also just add them to a jar. You can store a limited amount of information (I think it currently defaults to 128K/muffin) with PeristenceService . http://java.sun.com/javase/6/docs/jre/api/javaws/jnlp/
