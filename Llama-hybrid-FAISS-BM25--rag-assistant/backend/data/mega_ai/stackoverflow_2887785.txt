[site]: stackoverflow
[post_id]: 2887785
[parent_id]: 2887720
[tags]: 
#include static void count_to_five(void) { static int i = 0; while (i So, yes. Note, static storage for i means it retains its value with each call of count_to_five() . However, count_to_five() need not be defined as static . Its very hard to tell what you're asking.
