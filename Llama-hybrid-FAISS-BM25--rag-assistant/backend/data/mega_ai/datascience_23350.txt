[site]: datascience
[post_id]: 23350
[parent_id]: 
[tags]: 
Do xgboost and random forests in general handle multiple splits of the same numeric feature in a single branch?

For example, let's say that the age (say $x$) of a person for $12 12$. Does xgboost, or other decision tree learning algorithms in general, handle multiple splits within the same branch?
