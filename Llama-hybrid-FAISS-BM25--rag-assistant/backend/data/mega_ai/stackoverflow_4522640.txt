[site]: stackoverflow
[post_id]: 4522640
[parent_id]: 
[tags]: 
Parse an XML fragment stored in a string into nodes in XSLT with SAXON for Java

In my XSLT, I have a variable which contains a string. This string contains a fragment of XML. This is coming in programmatically, but its the equivalent of this: this is it '"/> Is there a way to parse this XML and add it to the output? We're using SAXON for Java.
