[site]: stackoverflow
[post_id]: 3686146
[parent_id]: 3686043
[tags]: 
Script elements are not binded to document when you do this. You need to insert the script yourself. One way to do that is by calling a callback function that parsers your return html and insert the script manually. Like this one: ` _insertScripts: function(html) { var sfrag = ' ]*>([\\S\\s]*?) '; var matchAll = new RegExp(sfrag, 'img'); var matchOne = new RegExp(sfrag, 'im'); if(html.match(matchAll)) { $.map(html.match(matchAll), function(script) { eval( (script.match(matchOne) || ['', ''])[1] ); }); } } ` I've seen some solutions that use the live() function from jQuery also, but I'm not sure on how it works.
