[site]: datascience
[post_id]: 124099
[parent_id]: 79669
[tags]: 
Push the texts through a sentence transformer (without any preprocessing if it is natural language), obtain an embedding and add a classification head which classifies into low, medium and high (or any other binned version of the target). Looking at it as a classification problem you will directly see that you will have to think about class imbalances.
