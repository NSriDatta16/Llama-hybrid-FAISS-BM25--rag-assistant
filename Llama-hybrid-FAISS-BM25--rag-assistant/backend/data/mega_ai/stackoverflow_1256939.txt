[site]: stackoverflow
[post_id]: 1256939
[parent_id]: 1256908
[tags]: 
Keep in mind you can use "," in your selector to attach a handler to multiple types of elements, for example: $("a, div.heading, p").click(function() { // this click event will fire for all anchors, // paragraphs and divs with the class "heading" });
