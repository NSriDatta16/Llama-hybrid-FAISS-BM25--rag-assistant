[site]: crossvalidated
[post_id]: 455538
[parent_id]: 
[tags]: 
What happens when the filter size is same as that of image size in a CNN?

In Convolutional Neural Networks (CNNs), theoretically speaking, if the filter dimensions are same as that of the image (training example) dimensions, will CNNs boil down to normal Fully Connected Deep Neural Networks?
