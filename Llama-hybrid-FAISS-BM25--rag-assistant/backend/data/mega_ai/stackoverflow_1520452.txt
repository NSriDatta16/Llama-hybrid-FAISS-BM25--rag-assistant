[site]: stackoverflow
[post_id]: 1520452
[parent_id]: 1520409
[tags]: 
Not really with the markup you have currently, I mean theoretically you can, but the markup you seem to want to wrap consists of header elements and you can't place block elements (which header is) inside paragraph elements. (I assumed you wanted the tag inside the , if you actually want to wrap the itself in then that will actually throw an error in IE).
