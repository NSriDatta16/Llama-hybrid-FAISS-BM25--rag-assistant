[site]: crossvalidated
[post_id]: 213825
[parent_id]: 213471
[tags]: 
PCA decomposes the covariance matrix into rotation and scaling . If you only use rotation, you should get the exact same result with k-means. So you gained nothing. Two ways of using the scaling information: scale every projected attribute to unit variance discard attributes with low variance both.
