[site]: stackoverflow
[post_id]: 3516814
[parent_id]: 3516323
[tags]: 
If you're using jQuery, you can do it with something like this: = select(@organization, "tabs", @organization.tabs.collect { |t| [ t.title, edit_organization_tab_url(t) ] }, { }, :onchange => "window.location.href=$(this).val()") The value for each item in the select list should be the URL you want to redirect the person to. I'm not sure what you meant by putting a Rails render call inside a JavaScript block.
