[site]: stackoverflow
[post_id]: 2821056
[parent_id]: 2820964
[tags]: 
To make sure everything is indexed create a sitemap with links to all pages. I would detect spiders via $_SERVER['HTTP_USER_AGENT'] and treat them differently than regular users, always showing default navigation.
