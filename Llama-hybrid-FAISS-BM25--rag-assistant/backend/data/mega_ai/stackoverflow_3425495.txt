[site]: stackoverflow
[post_id]: 3425495
[parent_id]: 
[tags]: 
PHP include file in foreach Loop

Will I be ok doing this? foreach ($item as $val) { include('external_script.php'); } Where external script is about 800 lines of code I want to keep separate for organizational sakes. Gracious!
