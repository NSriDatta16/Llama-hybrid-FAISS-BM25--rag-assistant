[site]: stackoverflow
[post_id]: 3535778
[parent_id]: 3535765
[tags]: 
The best you can do is to omit the error message with @ , check the return value and, if false , call error_get_last . You could also write your own wrapper around pcre_compile . It receives pointers for storage of error codes and strings. Shouldn't be too difficult; preg_match is a thin wrapper.
