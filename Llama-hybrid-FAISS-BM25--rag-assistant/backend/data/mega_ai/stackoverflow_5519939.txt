[site]: stackoverflow
[post_id]: 5519939
[parent_id]: 
[tags]: 
10 Month average

I'm replacing an Excel spreadsheet with a Microsoft SQL Server 2008 database and need to calculate this: =AVERAGE(IF((A:A>A4-304)*(A:A Where column A is the date and column G is the value. The heading for this field is: 10 month simple moving average I suppose I will use something like: SELECT RawData.* ,(SELECT Sum(X.AdjClose) AS SumAdjClose FROM RawData X WHERE DATEDIFF(d,X.RawDate,RawData.RawDate)
