[site]: crossvalidated
[post_id]: 413685
[parent_id]: 413683
[tags]: 
DNNs and CNNs aren't exclusive terms. A network can be deep, convolutional, both or neither. State-of-the-art results in computer vision are deep networks (having many layers) and are also convolutional neural networks (using convolutional filters).
