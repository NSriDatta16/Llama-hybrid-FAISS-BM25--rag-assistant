[site]: stackoverflow
[post_id]: 5641601
[parent_id]: 5641565
[tags]: 
No, you shouldn't. jQuery supports all major versions of all major browsers. Even if some behavior isn't available in older browsers(e.g. localStorage), you should use Modernizr to smooth out any problems. Which, if you're only using jQuery, you shouldn't have in the first place.
