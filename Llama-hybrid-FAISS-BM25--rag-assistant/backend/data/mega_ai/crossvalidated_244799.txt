[site]: crossvalidated
[post_id]: 244799
[parent_id]: 
[tags]: 
Can I do Principal Component analysis as a first step for linear regression?

If I have many predictor variables, instead of performing backward or forward selection, can I do PCA as a first step and then go ahead with linear regression?
