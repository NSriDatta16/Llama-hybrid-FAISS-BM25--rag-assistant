[site]: crossvalidated
[post_id]: 624817
[parent_id]: 
[tags]: 
Sudden drop to zero for precision recall curve

I am training a neural network classifier with 250k training samples and 54k validation samples. The output activation is sigmoid. I noticed a sudden drop in the precision for the very top probability scores. The graph looks as below. Any advice about a possible cause?
