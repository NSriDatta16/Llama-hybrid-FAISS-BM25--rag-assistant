[site]: stackoverflow
[post_id]: 3124535
[parent_id]: 
[tags]: 
Delete a div or span with a id=".xxx"

I am unable to delete a div with an id with a period or an asterix. I have the jquery code, which deletes the but not the above. Not looking for the jQuery code, but does it need a forward slash? ----req by anurag-------- // JavaScript $('.s').click(function (e) { var wd_del = (e.target.id); var yy = wd_del.substr(1, 100); $(document.getElementById("" + yy)).remove(); $(document.getElementById("s" + yy)).remove(); }); // HTML " class="ul_shh" style="cursor:pointer;"> " class="s"> x Thanks Jean
