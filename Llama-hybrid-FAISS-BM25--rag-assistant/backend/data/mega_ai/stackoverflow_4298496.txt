[site]: stackoverflow
[post_id]: 4298496
[parent_id]: 
[tags]: 
Graph/chart that supports dynamic pie charts?

Does anyone know of a good charting/graphing tool that supports dynamic pie charts, i.e. where a user could change the values by dragging, etc? It would need an event API so I could get access to the changed values. And, jQuery > vanilla JS > Flash.
