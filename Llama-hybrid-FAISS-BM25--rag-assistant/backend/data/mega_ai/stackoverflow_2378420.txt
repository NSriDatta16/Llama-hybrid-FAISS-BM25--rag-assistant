[site]: stackoverflow
[post_id]: 2378420
[parent_id]: 2372815
[tags]: 
public static function getByFilerOrganisedChildren($filer_id, $parent = '') { $table = new Filer_Interface_Table(); $where[] = $table->getAdapter()->quoteInto('ifc_system_id = ?', $filer_id); $where[] = $table->getAdapter()->quoteInto('ifc_parent = ?', $parent); $rows = $table->fetchAll($where, 'ifc_parent ASC'); foreach ($rows as $row) { $data[$row->ifc_name] = array(); /* self::getByFilerOrganisedChildren($filer_id, $row->ifc_name); */ } Zend_Debug::dump($data); return (isset($this->data) ? $this->data : false); } With the commented line above, the output by Zend_Debug::dump() is: array(7) { ["cifs-80"] => array(0) { } ["e0M"] => array(0) { } ["lo"] => array(0) { } ["vif1"] => array(0) { } ["vif1-81"] => array(0) { } ["vif1-82"] => array(0) { } ["vif1-83"] => array(0) { } } These are all the "parents". But as expected (since the call to the table is "ifc_parent = ''") none of the children (and their possible children) is returned. However when I uncomment the line: self::getByFilerOrganisedChildren($filer_id, $row->ifc_name); The output is: NULL I guess it's because of: abstract class Filer_Interface_Abstract extends Filer_Db_Class But I'm unable to change this - since it's how the current application is built. Suggestions?
