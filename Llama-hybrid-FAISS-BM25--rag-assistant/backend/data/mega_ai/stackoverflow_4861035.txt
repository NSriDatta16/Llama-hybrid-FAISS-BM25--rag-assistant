[site]: stackoverflow
[post_id]: 4861035
[parent_id]: 4860961
[tags]: 
You can indeed use preg_replace, but I'd suggest a regex that matches the 4. and 5. .* substrings. A less elegant (but simpler, if you don't know regex) solution is using strpos multiple times, using the result of one as the offset of the next.
