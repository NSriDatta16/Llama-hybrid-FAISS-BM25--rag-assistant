[site]: stackoverflow
[post_id]: 2307083
[parent_id]: 2307059
[tags]: 
Don't do that. upcase! returns either the upcased string or nil if no changes were made, so the result of that comparison will always be false after the first time. But it's a very bad idea to depend on side-effect semantics that way.
