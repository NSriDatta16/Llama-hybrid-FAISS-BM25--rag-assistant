[site]: stackoverflow
[post_id]: 990833
[parent_id]: 990767
[tags]: 
I didn't have time to fully test this, but this should work for you. I removed your temporary variable (you shouldn't need it) and I'm using the hasClass to see if the span has readless or readmore defined. $(document).ready(function(){ // Toggle a paragraph $('.togglebutn a').click(function(){ var $parentpara = $(this).parent().prev(); if ($(this > span).hasClass('readless')) { $(this).html(' read more '); } else { $(this).html(' read less '); } return false; }); });
