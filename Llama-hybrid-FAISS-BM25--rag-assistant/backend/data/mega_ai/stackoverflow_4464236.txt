[site]: stackoverflow
[post_id]: 4464236
[parent_id]: 
[tags]: 
Jquery - adding a dynamically added control from getJSON

I am having a problem assigning a control to dynamically added elements (inside the JSON function). The code is working fine but when I add $('div.prnt').append("hello"); inside the JSON it then doesn't assign the $("div.text").draggable(); to it. When I take the JSON function away and i'm using just $('div.prnt').append("hello"); it works straight away. I imagine the JSON function is stopping the elements from being added before the draggable control is called. div.prnt div.test { border: 1px solid #000; width: 200px; height: 200px; } $(document).ready(function () { var serviceurl = "WebServices/BusinessCardStudio.svc/gettextobjects/3457/17/17/1064/709/600/399"; $.getJSON ( serviceurl, function (data) { $.each(data, function (i, item) { $('div.prnt').append(" hello "); }); } ); $("div.text").draggable(); }); Does anyone know a way to get around this? Thanks in advance
