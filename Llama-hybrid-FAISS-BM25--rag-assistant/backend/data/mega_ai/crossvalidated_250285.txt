[site]: crossvalidated
[post_id]: 250285
[parent_id]: 249924
[tags]: 
Sequence to sequence models consist of an encoder and a decoder. The encoder takes the input and has no target. The decoder is a language model that is conditioned on the output from the encoder. It's targets are the probability of x(t+1) just like a language model.
