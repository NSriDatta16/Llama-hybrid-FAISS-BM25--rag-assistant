[site]: stackoverflow
[post_id]: 3049055
[parent_id]: 3048988
[tags]: 
Assuming your container supports it, use EL ! Java side: HttpSession session = request.getSession(true); session.setAttribute("foo", "bar"); JSP side: ... ... ${foo} ... And that will give you a paragraph containing the string bar .
