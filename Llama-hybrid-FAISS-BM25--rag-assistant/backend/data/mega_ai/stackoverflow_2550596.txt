[site]: stackoverflow
[post_id]: 2550596
[parent_id]: 2196470
[tags]: 
At the risk of actually answering the question that was asked (and assuming for a moment the asker has a good reason to specifically detect IE6): if (/\bMSIE 6/.test(navigator.userAgent) && !window.opera) { // yep, browser claims to be IE6 }
