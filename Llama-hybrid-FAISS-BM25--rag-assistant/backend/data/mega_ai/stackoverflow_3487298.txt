[site]: stackoverflow
[post_id]: 3487298
[parent_id]: 
[tags]: 
Drag function error

document.onmouseup = stopdrag(); document.getElementById('drag').onmousedown =drag(); // Just example It works. but here is the error: movedown on 'drag' div ->I move the pointer outside the browser ->mouseup -> move inside browser -> then you can know what the error . Help me to fix it.
