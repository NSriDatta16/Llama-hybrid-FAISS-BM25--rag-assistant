[site]: stackoverflow
[post_id]: 2831482
[parent_id]: 2831465
[tags]: 
Yep jPtr is pointing to the same memory address as iPtr at the end of the given fragment. So, you can change i using *jPtr or *iPtr . Since jPtr holds the memory address of i , if you change *jPtr , you are changing i and as a direct result, changing the value of *iPtr .
