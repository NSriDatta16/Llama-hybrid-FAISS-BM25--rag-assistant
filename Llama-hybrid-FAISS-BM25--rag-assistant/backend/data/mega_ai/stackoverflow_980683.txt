[site]: stackoverflow
[post_id]: 980683
[parent_id]: 980664
[tags]: 
When you call resize() the vector implementation reallocates the buffer to the size enough to store the requested number of elements - it can't be less than you require but the implementation is free to make it larger to reduce memory fragmentation and make reallocations less often. The only way to avoid such errors is to only loop within a valid range of indices in your code. For the code you provided the following would do: for ( int i = 0 ; i
