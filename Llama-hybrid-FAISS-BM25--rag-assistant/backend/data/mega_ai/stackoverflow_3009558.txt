[site]: stackoverflow
[post_id]: 3009558
[parent_id]: 2995673
[tags]: 
Don't use #pragma managed - it's considered "evil" and may cause problems with DLL init/shutdown. I would recommend explicitly compiling some files as managed (/clr), and some as native (without /clr). You can also have two pre-compiled headers - one for managed, one for native.
