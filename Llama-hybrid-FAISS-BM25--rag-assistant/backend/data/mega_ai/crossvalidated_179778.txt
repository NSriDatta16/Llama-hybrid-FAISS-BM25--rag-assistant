[site]: crossvalidated
[post_id]: 179778
[parent_id]: 
[tags]: 
optimization approach in logistic regression

In logistic regression we need to maximise the log likelihood which boils down to minimising a function which is sum of multiple log functions. We normally use gradient descent approach there. Can we use normal differentiation approach ?
