[site]: stackoverflow
[post_id]: 1287368
[parent_id]: 1287349
[tags]: 
An array is a data storage. An iterator is just an iterator over some other data storage or method that produces a sequence. Thus if a method returns an array you can modify it and its elements. You cannot do that via an iterator. Also, an array is of limited size. An iterator could be infinite.
