[site]: datascience
[post_id]: 54861
[parent_id]: 
[tags]: 
Version of Perceptron

If we change the $ywx condition (for performing update) to $ywx like in SVM (but without adding regularization to maximize the margin), is there any difference from the basic perceptron (the one with the aforementioned $ywx condition)?
