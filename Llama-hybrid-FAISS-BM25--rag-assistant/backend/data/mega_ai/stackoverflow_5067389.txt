[site]: stackoverflow
[post_id]: 5067389
[parent_id]: 5067323
[tags]: 
It depends exactly what the trigger does. The trigger won;t be the performance hit, the logic the trigger contains will have a read at the second last paragragh in this article for more details: http://msdn.microsoft.com/en-gb/magazine/cc164047.aspx What you could do is set up a test case. Another thing to watch is the size of the log file (.ldf) as it will grow quite large depending on how often an insert is done. WHy would you want to this anyway?
