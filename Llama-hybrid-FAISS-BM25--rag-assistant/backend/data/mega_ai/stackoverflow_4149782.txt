[site]: stackoverflow
[post_id]: 4149782
[parent_id]: 4149308
[tags]: 
It is invariably too late after it is loaded. The JIT compiler would quite likely already have converted the method bodies to machine code. Especially on Mono. The DLL is locked, you can't hack it anymore. Using the profiler is pretty difficult to justify. Leverage Edit + Continue if this is important.
