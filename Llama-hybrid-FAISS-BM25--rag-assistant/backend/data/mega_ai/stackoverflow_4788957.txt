[site]: stackoverflow
[post_id]: 4788957
[parent_id]: 4786555
[tags]: 
You could try something like this: put this span somewhere on your page: Superdefaultnumber And then this code in a script tag in the head of your document: var setNumber = function(){ var spanNode = document.getElementById("phoneNumber"); if (!spanNode) {throw new Error("Unable to find span");} if (location.href.indexOf("?provider=") > -1) { spanNode.innerHTML = "paid phone number"; } //Check userAgent to serve organic number to bots if(navigator.userAgent.toLowerCase().indexOf("googlebot") -1) { spanNode.innerHTML = "google tracking number"; } else if (last_referrer.indexOf("yahoo") > -1) { spanNode.innerHTML = "yahoo tracking number"; } else { spanNode.innerHTML = "default number"; } }; window.addEventListener("load", setNumber);
