[site]: stackoverflow
[post_id]: 5290171
[parent_id]: 
[tags]: 
jQuery Rotate Plugin

I'm using jQuery Rotate plugin to rotate items. It works, but I experienced some problems. I have that code: np_angle?>" onchange="$('#np_drag').rotate(this.value)" /> np_color?>; font-size: np_size?>px;position:absolute;top: np_y?>px;left: np_x?>px" class="draggable np_drag"> np_text?> $("#np_drag").rotate( np_angle?>); The problem is on input onchange. When I change it, #np_drag don't rotate. But when value is static (e.g. onchange="$('#np_drag').rotate(45)" It works. Why? How can I solve my problem?
