[site]: stackoverflow
[post_id]: 5675390
[parent_id]: 5675349
[tags]: 
If we define memory leak as a condition where a memory that can be used for creating objects, cannot be used or a memory that can be released does not then Memory leaks can happen in: Events in WPF where weak events need to be used. This especially can happens in Attached Properties . Large objects Large Object Heap Fragmentation http://msdn.microsoft.com/en-us/magazine/cc534993.aspx
