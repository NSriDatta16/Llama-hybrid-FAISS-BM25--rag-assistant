[site]: stackoverflow
[post_id]: 920114
[parent_id]: 
[tags]: 
JQuery - Hardcoding HTML in Javascript - Is there a better way to dynamically create dom elements?

I have a lot of messages, and raw HTML that I hard code in my javascript code, such as: var subjectId = $(subject).attr('subjectId'); var subjectName = $(subject).attr('subjectName'); var html = " "; $(subject).find('group').each( function() { var groupId = $(this).attr('groupId'); var groupName = $(this).attr('groupName'); var ownerId = $(this).attr('ownerId'); var owner = findStudentNameById( subjectId, ownerId ); html += " " + " " + " " + subjectId + " " + subjectName + ' ' + groupId + " - " + groupName + " Created By " + owner + " " + " " + " " + " " + " " + " " + " " + " "; }); html += " "; return html; or ... $('#inviteform').submit( function (){ var invitesSent = false; var html = ' Invitations have been sent to the following people '; $('#inviteform').find('input:checkbox').each( function() { if ( $(this).is(':checked')){ html += ' + ' + $(this).val() +' '; invitesSent = true; } }); html += ' '; if ( !invitesSent ){ html = ' You did not select any invitees, no invitations have been sent. '; } $('#content').hide().fadeOut(2000); $('#content').html(html).slideDown("slow"); $('#slogan').html('Congratulations! Your group is now complete. ').fadeIn(2000); return false; }); This is because I have a 2 main areas of the page (i.e title div, and content div), which I need to change the contents of on the fly. Unfortunately I cant separate this content into different files. So, is there a best practices behind creating the text and DOM elements for these divs instead of hard coding so much stuff. Im looking for the most elegant solution, so I don't have so much hard coding. Thanks
