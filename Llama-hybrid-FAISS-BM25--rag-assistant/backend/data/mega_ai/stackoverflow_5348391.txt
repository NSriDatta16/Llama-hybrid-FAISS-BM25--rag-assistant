[site]: stackoverflow
[post_id]: 5348391
[parent_id]: 5348380
[tags]: 
If you mean exclusively in Firefox, it can't be done . The best you can do is hide it from other browsers by sniffing the user agent if the browser is not Firefox. But a different browser can easily present itself as Firefox (by spoofing the user agent). Either way, it is not a good idea.
