[site]: stackoverflow
[post_id]: 1082941
[parent_id]: 1082867
[tags]: 
boost::lambda can do it vector v; ... using boost::lambda::_1; using boost::lambda::bind; for_each(v.begin(), v.end(), bind(&B::blah, *_1)); No need to upcast. A member pointer to a base-class member can be applied to a derived class too. #include #include
