[site]: stackoverflow
[post_id]: 2926361
[parent_id]: 2925136
[tags]: 
I have figured out that I must use a different syntax var storageAccount = new CloudStorageAccount( new StorageCredentialsAccountAndKey("my_account_name", "my shared key")); var blobClient = storageAccount.CreateCloudBlobClient(); var blobContainer = blobClient.GetContainerReference("images"); blobContainer.CreateIfNotExists(); Notice how the end points are omited. Evidently, the CloudBlobClient can figure out the appropriate URIs automatically.
