[site]: stackoverflow
[post_id]: 1928754
[parent_id]: 
[tags]: 
MySQL select highest rated

I have 2 columns, one called rating_average and one called rating_count. I need to select the 3 rows with the highest rating with the rating count factored into the equation as well.
