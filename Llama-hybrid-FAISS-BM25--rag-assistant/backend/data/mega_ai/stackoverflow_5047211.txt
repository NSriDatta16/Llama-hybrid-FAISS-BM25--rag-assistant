[site]: stackoverflow
[post_id]: 5047211
[parent_id]: 2893981
[tags]: 
Yes, the question is probably too old to answer it but just for the Google effect: Create a custom swig class (e.g. derive it from JPanel) Drag the source class file listed in the tree diagram on the left and drop it on its supposed parent widget
