[site]: stackoverflow
[post_id]: 3233399
[parent_id]: 
[tags]: 
IE jquery takes two clicks to work

I am trying to setup a checkbox that checks all the child checkboxes, like a tree view. The following codes works great in Firefox and Chrome but in IE, it takes two clicks to get it to work. What am I missing? HTML Organizations Org 1 Org 2 Javascript/Jquery $(document).ready(function () { $("#orgs").change(function () { if ($(this).attr("checked")) { $("#navform #orglist :checkbox").attr("checked", true); } else { $("#navform #orglist :checkbox").attr("checked", false); } }); });
