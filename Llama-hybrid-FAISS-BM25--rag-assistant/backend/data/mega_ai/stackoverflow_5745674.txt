[site]: stackoverflow
[post_id]: 5745674
[parent_id]: 
[tags]: 
Drag and drop div using jquery

I got a Main Div where i can drop any text and this will be displayed as in a form shown in the below script. I need to drag and drop the dynamically generated div to move it up or down ,from within the current div. Is this possible? $(init); function init() { function addColumn(column) { var iHtml; //Labeling and Tool Tip the Checkbox iHtml = " '> "+ " ' type='checkbox' name=' ' />"+ " '>MyCheckBox "; return iHtml } }
