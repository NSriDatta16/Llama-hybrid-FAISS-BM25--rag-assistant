[site]: stackoverflow
[post_id]: 4910687
[parent_id]: 
[tags]: 
"Use of unassigned local variable" in a Generic Method

Use of Unassigned local variable 'model'. Is what error message I'm getting. Its right where I say if (model == null). I'm not sure why its giving me a compile time error there.. someone please help. public static T TryGet (string fileName) where T : new() { T model; using (var storageFile = IsolatedStorageFile.GetUserStoreForApplication()) { using (var stream = new IsolatedStorageFileStream(fileName, FileMode.Open, storageFile)) { if (stream.Length > 0) { var serializer = new DataContractSerializer(typeof(T)); model = (T)serializer.ReadObject(stream); } } } if (model == null) { model = new T(); } return model; }
