[site]: stackoverflow
[post_id]: 5565029
[parent_id]: 
[tags]: 
Check if full path given

Is there a method to check if given path is full path? Right now im doing this: if (template.Contains(":\\")) //full path already given { } else //calculate the path from local assembly { } But there must be more elegant way for checking this?
