[site]: stackoverflow
[post_id]: 3651304
[parent_id]: 3651198
[tags]: 
I wouldn't claim this to be the most elegant and pretty solution out there, but it gets the job done. Notice that each dataframe row carries its own row name, which becomes a problem when inserting new lines. That being said, you can mend this with row.names (see below). my.df
