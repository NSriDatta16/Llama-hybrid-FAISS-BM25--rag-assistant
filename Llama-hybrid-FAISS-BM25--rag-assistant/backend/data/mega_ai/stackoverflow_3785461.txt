[site]: stackoverflow
[post_id]: 3785461
[parent_id]: 3785366
[tags]: 
Why dont you just disable optimization for the code in question? #pragma optimize( "", off ) // Code, not to optimize goes here #pragma optimize( "", on ) This sample of #pragma optimize is specific to MSVC, but other compilers support it as well.
