[site]: crossvalidated
[post_id]: 74497
[parent_id]: 
[tags]: 
Finding the right model

I have a data frame with several predictors, lets call them pred1 through pred3 , and a result column. Now I need to specify the right model. I could randomly try: svm.model But there must be a better automatic approach in R to model selection?!
