[site]: stackoverflow
[post_id]: 3329698
[parent_id]: 3328040
[tags]: 
This transformation finds if an ascii character is in the (inclusive) range of any two ascii characters : !"#$%&amp;'()*+,-./0123456789:;&lt;=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\]^_`abcdefghijklmnopqrstuvwxyz{|}~ = string-length(substring-before($vAscii, $pStarting)) and string-length(substring-before($vAscii, $pEnding)) >= string-length(substring-before($vAscii, $pChar[1])) "/> when applied on the following XML document (that contains exactly the provided XML fragment): H &#x20; z produces the wanted result : true When applied on this XML document : H A G again the correct result is produced : false
