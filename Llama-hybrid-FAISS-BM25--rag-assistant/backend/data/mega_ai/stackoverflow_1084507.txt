[site]: stackoverflow
[post_id]: 1084507
[parent_id]: 1079705
[tags]: 
Thank you g. You really helped me although I have not found more elegant solution. From *.aspx side it looks so: Where RenderControl ia a helper function: public string RenderControl(Control ctrl) { StringBuilder sb = new StringBuilder(); StringWriter tw = new StringWriter(sb); HtmlTextWriter hw = new HtmlTextWriter(tw); ctrl.RenderControl(hw); return sb.ToString(); }
