[site]: stackoverflow
[post_id]: 1516390
[parent_id]: 1516358
[tags]: 
This is a simple way, just replace cout with printf ! Although I like C++ streams more because they are more elegant IMO: #include #include template void printMatrix(double (&matrix)[rows][columns], int dec) { std::cout
