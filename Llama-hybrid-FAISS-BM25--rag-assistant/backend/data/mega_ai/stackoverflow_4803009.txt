[site]: stackoverflow
[post_id]: 4803009
[parent_id]: 4802869
[tags]: 
One can not programmatically set (from within an app) user's browser to display a custom user-agent string. However, users can do this by hand via some fiddling, but then this is set system wide. If you need to send custom user-agent, you can embedded browser into your app via WebView . Then you can set custom user-agent .
