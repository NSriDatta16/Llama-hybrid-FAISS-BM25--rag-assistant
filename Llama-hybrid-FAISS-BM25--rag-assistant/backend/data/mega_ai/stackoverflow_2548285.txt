[site]: stackoverflow
[post_id]: 2548285
[parent_id]: 2548190
[tags]: 
Yes, you can do something like "running offline" by embedding broker into JVM . This is usually done for (unit) testing. BrokerService broker = new BrokerService(); // configure the broker broker.addConnector("tcp://localhost:61616"); broker.start();
