[site]: stackoverflow
[post_id]: 5680940
[parent_id]: 5679724
[tags]: 
I prefer the following: #define VERBOSE 1 // or 0, obviously if (VERBOSE) { // Debug implementation } This is a little more readable since VB_S doesn't mean anything to the average user, but if (VERBOSE) does.
