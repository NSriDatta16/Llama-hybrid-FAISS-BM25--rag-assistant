[site]: stackoverflow
[post_id]: 3354715
[parent_id]: 3354666
[tags]: 
This assumes that your timestamp column only contains information about the day, but not the time. That way, the dates can be grouped together: select timestamp, AVG(value) as average from TABLE_NAME group by timestamp
