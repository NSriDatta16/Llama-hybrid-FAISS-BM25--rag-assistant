[site]: stackoverflow
[post_id]: 2950436
[parent_id]: 2948660
[tags]: 
This will remove all HTML tags from the given String. String html = //... html = html.replaceAll(" ", ""); But if you're looking to screen-scrape, you can use XPath to pull out specific parts of the HTML: StreamSource source = new StreamSource(new StringReader(html)); DOMResult result = new DOMResult(); Transformer transformer = TransformerFactory.newInstance().newTransformer(); transformer.transform(source, result); Node root = result.getNode(); XPath xpath = XPathFactory.newInstance().newXPath(); String value = xpath.evaluate("/the/xpath/expression", root);
