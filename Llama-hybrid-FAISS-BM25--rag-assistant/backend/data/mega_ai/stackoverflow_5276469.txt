[site]: stackoverflow
[post_id]: 5276469
[parent_id]: 5276447
[tags]: 
Yes, that is linear in the number of nodes in the tree. Can you cache the height with the subtree, so only the portion of the tree that you mutate needs to have height recomputed (mutated nodes and their ancestors)? That should get you back to O(log n) at a cost of O(n) storage.
