[site]: stackoverflow
[post_id]: 2569139
[parent_id]: 2569120
[tags]: 
Dojo has it. If you do not have the constraint of using jquery, then maybe you can investigate it. http://ajaxian.com/archives/dojostorage-offline-access-and-permanent-client-side-storage Alternately, you could also probably just store it in an array that other plugins / functions can access it.
