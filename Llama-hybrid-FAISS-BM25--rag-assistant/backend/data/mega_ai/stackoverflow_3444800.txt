[site]: stackoverflow
[post_id]: 3444800
[parent_id]: 3444395
[tags]: 
No, the storage array will only contain pointers to the actual Pair objects existing somewhere else on the heap. Yet, remember to instantiate 8 Pair objects and make each element of the array point to these objects. You need to have something like this after the code that you have written: for(int i=0;i Only then will the Pair objects be created and correctly referred to by the storage array.
