[site]: stackoverflow
[post_id]: 1063686
[parent_id]: 1063660
[tags]: 
You could wrap your paragraph in a container that has padding at the top, so it doesn't matter what's above it. For example, wrap it in a div with the following css div.finaltext { clear: both; margin-top: 10px; }
