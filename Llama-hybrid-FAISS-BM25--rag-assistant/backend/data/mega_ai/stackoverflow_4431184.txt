[site]: stackoverflow
[post_id]: 4431184
[parent_id]: 
[tags]: 
Dual Inner Join Query taking forever to run

i would like to runa a query to get some simple stats on our database but it returns the following error. can someone advise me on how to fix it? query = SELECT tblSchemes.Clientid, count(clientid), count(insscheme) FROM tblSchemes INNER JOIN tblclaims_liberty ON tblClaims_liberty.AgentCode = tblSchemes.ClientID INNER JOIN tblPolicys_liberty ON tblSchemes.Scheme = tblPolicys_liberty.InsScheme GROUP BY tblSchemes.Clientid Error returned = Msg 8115, Level 16, State 2, Line 1 Arithmetic overflow error converting expression to data type int. Many thanks in advance. Adam
