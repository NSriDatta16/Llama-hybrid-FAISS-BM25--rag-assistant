[site]: stackoverflow
[post_id]: 3431324
[parent_id]: 
[tags]: 
Simple Moving Average for stock prices

I was playing with ActiveQuant FinancialLibrary SimpleMovingAverage function SMA() below: Is there an error in the algo below, where it calculates the average by looking "into the future" ( as it does i public static double SMA(int period, double[] vals, int skipdays) { double value = 0.0; for (int i = skipdays; i The for loop can be replaced with that below, where it looks backward. for (int i = skipdays - period; i Am I missing something?
