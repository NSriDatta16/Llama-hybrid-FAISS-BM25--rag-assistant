[site]: stackoverflow
[post_id]: 3044757
[parent_id]: 2872430
[tags]: 
I encount the same problem. Though the query string is escaped with Uri.EscapeDataString, and the property AbsoluteUri do reprent it correctly, but WebBrowser send the Uri in unescaped format. currentUri = new System.Uri(ServerAgent.urlBase + "/MailRender?uid=" + Uri.EscapeDataString(uid); webBrowser.Navigate(currentUri); Plus sign ('+') is convert to %2B, but the server still get + in URL, which is then convert to space (' ') via HttpServeletRequest.getParameter() call
