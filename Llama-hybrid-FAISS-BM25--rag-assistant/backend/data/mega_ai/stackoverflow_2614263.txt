[site]: stackoverflow
[post_id]: 2614263
[parent_id]: 
[tags]: 
take values from table cells and turn into array

using jquery I need to retrieve an array from table cells, format the data and pass it into a js function. the code i am using is this: var l1 = new Array(); $('table#datatable tbody td:first-child').each(function() { l1.push($(this).text()); }); this is the table fragment Age: 0-4 0 9.7 5-17 23.6 18.0 Total 0-17 20.6 16.1 the table's id is "datatable". i want to return an array of the contents of each first td and then format it like this: 0,23.6,20.6 i am very new to using arrays...
