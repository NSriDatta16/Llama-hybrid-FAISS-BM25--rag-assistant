[site]: stackoverflow
[post_id]: 2110280
[parent_id]: 2110198
[tags]: 
I think I would start by just averaging the pixel values: for x = start_x to end_x for y = stary_y to end_y total += getPixel(x,y) shade = total / (xlen*ylen) Its going to be more complicated if you're doing it in color.
