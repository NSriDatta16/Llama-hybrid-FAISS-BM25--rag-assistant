[site]: stackoverflow
[post_id]: 3130026
[parent_id]: 3129575
[tags]: 
I love jQuery (and functional programming in general) but sometimes native JavaScript is the way to go: function ProcessParagraphs(elem) { var children = elem.getElementsByTagName('p'); if(children.length If you have list of elements to process then I'd give them a class, say 'foo', and process them with jQuery $('.foo').each(ProcessParagraphs);
