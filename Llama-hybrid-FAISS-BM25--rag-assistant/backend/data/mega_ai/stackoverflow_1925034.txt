[site]: stackoverflow
[post_id]: 1925034
[parent_id]: 1923239
[tags]: 
I would just loop through the Lists dataProvider that you dragged the items onto... while looping store the new order (i) in a variable in your ArrayCollection populating the dataProvider... then convert the ArrayCollection to an array to be passed to your server side for processing. something like: for(var i:int = 0; i
