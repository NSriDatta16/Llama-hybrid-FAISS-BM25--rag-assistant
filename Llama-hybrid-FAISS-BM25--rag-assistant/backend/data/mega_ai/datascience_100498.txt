[site]: datascience
[post_id]: 100498
[parent_id]: 46779
[tags]: 
It could also be that the problem is very hard to learn. I've had this and actually after 6 hours of identical outputs in each batch (which happens because the 'average' answer is the easiest to minimize loss), the network finally started learning: Some things I plan on doing to make the learning happen earlier: changes in learning rate using features from earlier in the network
