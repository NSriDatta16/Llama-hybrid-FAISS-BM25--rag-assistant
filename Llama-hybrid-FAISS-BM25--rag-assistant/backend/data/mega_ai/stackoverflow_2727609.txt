[site]: stackoverflow
[post_id]: 2727609
[parent_id]: 
[tags]: 
Best way to create IPEndpoint from string

Since IPEndpoint contains a ToString() method that outputs: 10.10.10.10:1010 There should also be Parse() and/or TryParse() method but there isn't. I can split the string on the : and parse an IP address and a port. But is there a more elegant way?
