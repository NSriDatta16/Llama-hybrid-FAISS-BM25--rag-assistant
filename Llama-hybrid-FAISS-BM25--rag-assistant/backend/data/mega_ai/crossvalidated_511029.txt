[site]: crossvalidated
[post_id]: 511029
[parent_id]: 
[tags]: 
Can glmmTMB be used without a random effect?

I'm curious whether glmmTMB can be used without a random effect. Both glmer , lmer , and lme throw an error about lack of random effects, but glmmTMB runs fine. However, when I test a logistic regression using glmmTMB and regular glm , I get very different p values. Toy example below. library(glmmTMB) options(contrasts=c("contr.sum","contr.poly")) data(mtcars) mtcars $Bin mpg > 20) # create a binary variable m
