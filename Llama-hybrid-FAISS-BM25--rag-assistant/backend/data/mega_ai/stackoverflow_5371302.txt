[site]: stackoverflow
[post_id]: 5371302
[parent_id]: 
[tags]: 
Fivestar widget doesn't refresh after voting

I have a question about using the Fivestar module. When I vote a node the vote is processed, but the widget showing the average doesn't reload until I reload the node. Does anybody know what is happening?
