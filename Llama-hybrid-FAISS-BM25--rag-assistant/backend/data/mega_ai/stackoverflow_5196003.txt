[site]: stackoverflow
[post_id]: 5196003
[parent_id]: 5195930
[tags]: 
It doesn't work because the drop element doesn't present the 'valid' class until after the droppable fires the drop event. A way to correct this is to change the drag event to the start event in the drag element, so the 'valid' class is added to the element before the drop event is fired.
