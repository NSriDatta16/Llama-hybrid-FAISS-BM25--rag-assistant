[site]: stackoverflow
[post_id]: 5238156
[parent_id]: 
[tags]: 
Trouble with colorbox and jQuery validation in WebKit browsers

I'm using JQuery 1.4.2 with Colorbox( http://colorpowered.com/colorbox/ ) and Validation( http://bassistance.de/jquery-plugins/jquery-plugin-validation/ ) plugins in my project. I've got problem in Chrome(10.0.648.127 beta) and Safari(5.0.1) - content of my modal window don't refresh after validation. What i mean is that, when I enter wrong value in form(which is in this modal window), and validate it, error message doesn't appear. Validation is working, because i see in console, that valid() method returns good values. And when I close my modal, and open it again (without refreshing page), then I see error message. And of course to make it dissapear, i have to type in good value, and agian close&open again my modal window. In Firefox 3.6.13, Opera 11.01 and IE9 RC everything is OK. My JS code: $.colorbox({ width: 1000, scrolling: false, inline: true, href: '#userForm', onComplete: function() { $('#userForm').validate({ rules: { name: { minlength: 4, required: true, remote: "ajax/ajaxUsers.php?ajaxMode=userExists" }, pass: { minlength: 6, required: true }, pass_2: { equalTo: "#i_pass" }, email: "email" }, messages:{ name: { minlength: 'Nazwa użytkownika powinna mieć conajmniej {0} znaki', required: "Nazwa użytkownika jest wymagana", remote: "Ta nazwa jest już zajęta" }, email: "Proszę wpisać prawidłowy adres email", pass:{ minlength: 'Hasło powinno mieć conajmniej {0} znaków', required: "Proszę wpisać hasło" }, pass_2: "Proszę powtórz hasło z powyższego pola" }, errorClass: "invalid", errorElement: "em" }); } }); My HTML: Dodaj Nowego Użytkownika Dane podstawowe: Nazwa użytkownika* Pełna nazwa Adres email Aktywny? Autoryzacja Hasło* Powtórz hasło* Dostęp do Fotoware Użytkownik Fotoware Hasło Fotoware Poziom dostępu Wybierz poziom dostępu {foreach $userLevels as $level} {$level} {/foreach} {foreach $userPermissions as $perm} {$perm} {/foreach} Pola oznaczone gwiazdka są obowiązkowe. Zapisz Anuluj Any ideas why does it happen that way?
