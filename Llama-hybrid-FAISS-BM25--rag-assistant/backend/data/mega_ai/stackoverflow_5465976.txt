[site]: stackoverflow
[post_id]: 5465976
[parent_id]: 5465890
[tags]: 
I don't know for sure, but really doubt that this is possible. I see why you would want to thought...It's not so elegant to insert the row, then immediately update it. I would consider if you really need to store the hash in the DB or if you can calculate it during the select: SELECT MD5(CONCAT( id , salt )) FROM table WHERE ...
