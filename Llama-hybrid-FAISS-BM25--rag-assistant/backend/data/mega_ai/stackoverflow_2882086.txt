[site]: stackoverflow
[post_id]: 2882086
[parent_id]: 2882058
[tags]: 
var classNames = $('#dv').attr('class').split(' '); if you just need to know if an element owns a class you might want to use if( $('#dv').is('.green') ){ alert('its green!'); } or if( $('#dv').hasClass('green') ){ alert('its green!'); } which is a derived function. Keep attention to the different notation ".className" and just "className"
