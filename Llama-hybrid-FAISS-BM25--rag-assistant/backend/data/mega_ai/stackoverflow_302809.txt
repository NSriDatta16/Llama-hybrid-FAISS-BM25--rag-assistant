[site]: stackoverflow
[post_id]: 302809
[parent_id]: 302736
[tags]: 
Lower level code should check use from higher level code. Usually this means checking arguments, but it can mean checking return values from upcalls. Upcall arguments need not be checked. The aim is to catch bugs in immediate and obvious ways, as well as documenting the contract in code that does not lie.
