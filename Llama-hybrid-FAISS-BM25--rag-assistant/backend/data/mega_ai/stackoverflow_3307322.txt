[site]: stackoverflow
[post_id]: 3307322
[parent_id]: 3298929
[tags]: 
Not that this would be any faster, just cleaner: var dragable = this; $("#timeline div.timeline").each(function(i) { SelectClosestKeyframes(dragable, event, ui, $('#layer'+i), $('#layer'+i+'Go')); }); In fact the each() will be a little slower than your for() but on a very minute scale.
