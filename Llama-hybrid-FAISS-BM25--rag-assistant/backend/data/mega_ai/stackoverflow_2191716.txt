[site]: stackoverflow
[post_id]: 2191716
[parent_id]: 
[tags]: 
zend framework : set imap mail to read, or move it to a folder

After reading a message's state in Zend_Mail_Storage_Imap with $message->getHeaders(). How do I then set that mail as read? setHeaders(Zend_Mail_Storage::FLAG_SEEN); Thanks, Dan
