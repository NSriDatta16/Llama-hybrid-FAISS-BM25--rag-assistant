[site]: stackoverflow
[post_id]: 835213
[parent_id]: 47177
[tags]: 
The following code is Linux (maybe Unix) only, but it works in a real project. private double getAverageValueByLinux() throws InterruptedException { try { long delay = 50; List listValues = new ArrayList (); for (int i = 0; i
