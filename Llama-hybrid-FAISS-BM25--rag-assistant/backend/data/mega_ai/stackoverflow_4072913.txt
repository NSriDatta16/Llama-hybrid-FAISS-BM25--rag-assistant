[site]: stackoverflow
[post_id]: 4072913
[parent_id]: 4072551
[tags]: 
There's no little phone icon next to the method, so it's likely not available, according to the documentation: http://msdn.microsoft.com/en-us/library/system.io.isolatedstorage.isolatedstoragefile(VS.95).aspx Uggh...maybe keep a smaller file with only the last write time, so you know whether you need to load the big one or ignore it? Or even some fancy datetime in the file name itself?
