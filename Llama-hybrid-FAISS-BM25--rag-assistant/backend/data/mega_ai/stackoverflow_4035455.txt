[site]: stackoverflow
[post_id]: 4035455
[parent_id]: 4033502
[tags]: 
This is finally how I've done it. I've added a errorMessage field to the HTTP 500 response in this way. 500 ${exception.message} and in the ajax request I recover the message like this. error: function (XMLHttpRequest, textStatus, errorThrown) { var errorMessage = XMLHttpRequest.getResponseHeader('errorMessage'); .... } Maybe there's a more elegant way to do this, but at least it works.
