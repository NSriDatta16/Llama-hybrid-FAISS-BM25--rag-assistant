[site]: stackoverflow
[post_id]: 934576
[parent_id]: 934536
[tags]: 
Without LINQ, no, there's no built-in helpers for this. You could write one though: // I forget if you need this delegate definition -- this may be already defined in .NET 2.0 public delegate R Func (T obj); public static Dictionary BuildDictionary (IEnumerable objs, Func kf, Func vf) { Dictionary d = new Dictionary (); foreach (T obj in objs) { d[kf(obj)] = vf(obj); } return d; } Dictionary fooDict = BuildDictionary(foos, new Func (delegate(Foo foo) { return foo.Name; }), new Func (delegate(Foo foo) { return foo.StreetAddress; })); It doesn't look nearly as elegant as the LINQ-based answers, does it...
