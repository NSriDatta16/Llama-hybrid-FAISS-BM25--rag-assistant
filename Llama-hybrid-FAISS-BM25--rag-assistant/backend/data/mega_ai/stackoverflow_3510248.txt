[site]: stackoverflow
[post_id]: 3510248
[parent_id]: 3417808
[tags]: 
I found a workaround for this, override the methods for touch gestures in my custom UITableView in order to make it scroll programMatically as I drag upon it an object. Here is the 'solution'. I still believe there is another simpler way to do this but I did not find it, so posting this and marking it as an 'answer' might help someone else.
