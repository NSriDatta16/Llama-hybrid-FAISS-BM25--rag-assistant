[site]: stackoverflow
[post_id]: 5030325
[parent_id]: 1764881
[tags]: 
That should work. Not so elegant but still: select count(mycol - (mycol mod 10)) as freq, mycol - (mycol mod 10) as label from mytable group by mycol - (mycol mod 10) order by mycol - (mycol mod 10) ASC via Mike DelGaudio
