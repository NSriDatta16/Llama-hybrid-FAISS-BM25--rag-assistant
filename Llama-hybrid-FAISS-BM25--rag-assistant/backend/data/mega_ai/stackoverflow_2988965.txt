[site]: stackoverflow
[post_id]: 2988965
[parent_id]: 2988885
[tags]: 
I think the simplest thing to do would be to have the function return if it detects that it has been less than 5000 milliseconds since the last time it ran. var func = (function() { var ts = new Date(); return function(force) { var now = new Date(); if (!force && now - ts In the event handler, the function is called with a parameter that forces it to ignore the timer. This mechanism is a little fragile of course.
