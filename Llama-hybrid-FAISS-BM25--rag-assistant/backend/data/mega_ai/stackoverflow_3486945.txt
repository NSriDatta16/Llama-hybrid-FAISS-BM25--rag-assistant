[site]: stackoverflow
[post_id]: 3486945
[parent_id]: 3485066
[tags]: 
Wrote auto-complete a week ago :) You need to use Spark text components and pass custom TextFlow to them: private function init():void { var textFlow:TextFlow = new TextFlow(); var paragraph:ParagraphElement = new ParagraphElement(); textFlow.addChild(paragraph); var elements:Vector. = highlight("Pet Sematary", "Se"); var n:int = elements.length; for (var i:int = 0; i { var result:Vector. = new Vector. (); // since we need to compare ignore-case we can not use split() // and have to collect indexes of "query" matches in "text" var indexes:Vector. = new Vector. (); var index:int = 0; var textLowerCase:String = text.toLocaleLowerCase(); var queryLowerCase:String = query.toLocaleLowerCase(); var queryLength:int = query.length; while (true) { index = textLowerCase.indexOf(queryLowerCase, index); if (index == -1) break; indexes.push(index); index += queryLength; } // now add SpanElement for each part of text. E.g. if we have // text="aBc" and query is "b" then we add "a" and "c" as simple // span and "B" as highlighted span. var backgroundColor:uint = 0xFFCC00; var n:int = indexes.length; if (n == 0) // no matches { addSpan(result, text); } else { var startIndex:int = 0; for (var i:int = 0; i , text:String, backgroundColor:* = "transparent"):void { var span:SpanElement = new SpanElement(); span.text = text; span.backgroundColor = backgroundColor; vector.push(span); } MXML code: P.S: If you will have troubles with popup taking focus - add it manually to the toolTipChildren : systemManager.toolTipChildren.addChildAt(child, 0);
