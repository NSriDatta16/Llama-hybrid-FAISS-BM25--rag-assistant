[site]: stackoverflow
[post_id]: 1573188
[parent_id]: 1572275
[tags]: 
Try adding this: " /> Further... I took the liberty of changing how you do your for loop so that you no longer need to do the incrementing in the code, as I always find this a bit convoluted. This means that you would call the current item as: And you access the current index of the loop with: So your view would look like this (although you never seem to use the current item anyway, just the index of it): new { Current = x, Index = i }) { %> Item Title: " /> Item Price:
