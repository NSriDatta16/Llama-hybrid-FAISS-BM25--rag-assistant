[site]: stackoverflow
[post_id]: 1531748
[parent_id]: 1531706
[tags]: 
Make sure you are not assigning pointer to the auto_ptr, this will not work. However, all these fragments compiling just fine: #include #include using namespace std; int main(int argc, char * argv[] ) { auto_ptr pX; pX.reset(pX.get() ? new int(1) : new int(2)); pX = auto_ptr (pX.get() ? new int(1) : new int(2)); pX = auto_ptr ((pX.get()==NULL) ? new int(1) : new int(2)); return 0; }
