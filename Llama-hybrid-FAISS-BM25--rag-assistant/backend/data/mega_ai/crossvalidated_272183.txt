[site]: crossvalidated
[post_id]: 272183
[parent_id]: 
[tags]: 
what are mixed variables in data mining?

I read that neural networks, SVM and neuro-fuzzy don't support "mixed variables." So what are those exactly? Does it refer to mixed types (numeric and non-numeric)? And if so, does that mean the input has to be all numeric? what about the output?
