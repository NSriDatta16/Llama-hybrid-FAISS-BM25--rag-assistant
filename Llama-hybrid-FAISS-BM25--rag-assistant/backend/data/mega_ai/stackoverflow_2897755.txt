[site]: stackoverflow
[post_id]: 2897755
[parent_id]: 2897615
[tags]: 
Your second example should probably be: constraint PK_GroupCategory primary key clustered(GroupID,CategoryID,PermissionID) Your first example would violate normal form (and string parsing might not be a good use of your processing time), but that doesn't mean it's necessarily wrong for your application. It really depends how you use the data.
