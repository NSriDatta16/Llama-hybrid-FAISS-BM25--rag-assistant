[site]: stackoverflow
[post_id]: 4943854
[parent_id]: 3838539
[tags]: 
As a workaround: open the solution configuration manager next to build is a deploy column, uncheck your project press F5 This will launch the app that is already on the device without overwriting it (and deleting its storage).
