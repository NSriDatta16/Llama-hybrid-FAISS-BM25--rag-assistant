[site]: stackoverflow
[post_id]: 4649602
[parent_id]: 4649547
[tags]: 
your code will need to leverage the "not exists" operator pretty heavily delete from table1 where not exists ( select 1 from colType ct where ct.colType = table1.colType ) and not exists ( select 1 from colsubtype cst where cst .colSubType = table1.colSubType )
