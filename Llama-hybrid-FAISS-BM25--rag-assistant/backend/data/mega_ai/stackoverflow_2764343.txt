[site]: stackoverflow
[post_id]: 2764343
[parent_id]: 
[tags]: 
jQuery sortable (how to customize the clickable area inside the sortable box)

I have this jQuery code: $(".right_box_holder").sortable({ update : function () { var order = $('.right_box_holder').sortable('serialize'); $.get("right_menu_functions.php?change_sortorder&"+order); } }); and this HTML code: // sort box 0 // sort box 1 // sort box 2 As it is now, I can click anywhere inside .right_box and move it. I want to disable this and make a button / icon inside .right_box which the user have to click on to drag the box. Is this possible?
