[site]: stackoverflow
[post_id]: 3005824
[parent_id]: 3005006
[tags]: 
First, when are you executing the query? Second, that SP doesn't return anything, what are you adding that ResultTransformer for? The code should look like this: session.GetNamedQuery("Procedure") .SetParameter("Id", "222") .SetParameter("Name", "testsp") .ExecuteUpdate()
