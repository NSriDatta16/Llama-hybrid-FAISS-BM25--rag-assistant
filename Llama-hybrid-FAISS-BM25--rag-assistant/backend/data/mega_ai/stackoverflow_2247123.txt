[site]: stackoverflow
[post_id]: 2247123
[parent_id]: 2247092
[tags]: 
You could approach this with a distinct syntax that clearly discourages the usage of such properties. You could borrow the python syntax of starting the method name with an underscore to define it as private. This doesn't block anyone from using it, of course, but its usage will be discouraged.
