[site]: stackoverflow
[post_id]: 1250359
[parent_id]: 1250245
[tags]: 
Semantic code is just code that makes sense when you read it. Let's take the most obvious example, a dictionary entry. Any given word in a dictionary will likely have multiple definitions. Lists when you're listing things, paragraphs when you're writing a paragraph, etc. In short, it should be fine.
