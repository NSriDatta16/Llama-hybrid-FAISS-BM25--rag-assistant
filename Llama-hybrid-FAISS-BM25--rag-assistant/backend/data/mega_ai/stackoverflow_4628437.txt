[site]: stackoverflow
[post_id]: 4628437
[parent_id]: 4628301
[tags]: 
This is called a custom server control. You can read an indepth walkthrough about them. You will need to have the server control reside in an assembly that is separate from your project and then reference the assembly into your project. The syntax you want to use to put the control on the page is: Then you can reference the control: An easy way to do this is to add the control to your toolbox. Then you can drag the control from the toolbox onto any page. The Register will be created automatically. To add the control to the toolbox Right click on the toolbox tab where the control should reside, select choose items. (You can create a new tab or use an existing one). Select Browse Browse to and select your assembly, click Open Your control should now appear in the toolbox tab.
