[site]: stackoverflow
[post_id]: 5247420
[parent_id]: 5247347
[tags]: 
Aggregate functions such as average require another column containing result of the aggregate function when combined with GROUP BY . So try: SELECT Student_ID, avg(POINTS) AS avg_points FROM Exams GROUP BY Student_ID ORDER BY avg_points desc
