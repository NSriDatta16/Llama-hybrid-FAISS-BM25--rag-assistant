[site]: stackoverflow
[post_id]: 4768988
[parent_id]: 
[tags]: 
Get the value from an Array

I have some code I need help with: var bob = [10,30,50,20,8]; // Call the function foo(bob); var foo = function(bob){ var max = 0, min = 0, average = 0, sum = 0; max = Math.max.apply( null, bob); // Get the max value. min = Math.min.apply( null, bob); // Get the min value. for( var i = 0; i This function returns an array with max, min and average values. How can I choose which value to print in different divs?
