[site]: stackoverflow
[post_id]: 998340
[parent_id]: 998303
[tags]: 
addItems() has to allocate memory: void addItems(listItem *l) { listItem* one = (listItem*)malloc(sizeof(listItem)); listItem* two = (listItem*)malloc(sizeof(listItem)); listItem* three = (listItem*)malloc(sizeof(listItem)); l->left = 0; l->right = one; one->left = l; one->right = two; two->left = one; two->right = three; three->left = two; three->right = 0; } int main () { listItem l; addItems(&l); } I'm assuming you are out to create a double linked list, so I liberty in setting the left/right pointers accordingly. If I'm wrong in my assumption, please adjust so it suits your needs. Cheers
