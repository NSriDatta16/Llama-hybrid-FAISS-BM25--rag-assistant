[site]: stackoverflow
[post_id]: 3124757
[parent_id]: 
[tags]: 
Magento : Variable well displayed in javascript. How call it in php in the same file?

I've built one variable (horaires_lundi) in this file : app/code/community/Unirgy/StoreLocator/controllers/Adminhtml/Locationcontroller.php ->setTitle($this->getRequest()->getParam('title')) ->setNotes($this->getRequest()->getParam('notes')) ->setStreetAddress($this->getRequest()->getParam('street_address')) ->setHorairesLundi($this->getRequest()->getParam('horaires_lundi')) All variables are well displayed in JavaScript in this file : app/design/frontend/default/default/template/unirgy/storelocator/map.phtml (function(){ var storeLocator = new UnirgyStoreLocator({ mapEl: $('map'), sidebarEl: $('sidebar'), searchUrl: ' getUrl('ustorelocator/location/search')?>', generateSidebarHtml: function(m) { return ' ' + m.title + ' (Distance : ' + parseFloat(m.distance).toFixed(1) + ' ' + m.units + ') ' + m.street_address + '&nbsp;' + m.test + '&nbsp;' + m.city + ' ' + m.notes + ' '; }, generateMarkerHtml: function(m) { var url = m.website_url.replace(/\s/,''); var dataForm = new VarienForm('form-validate', true); return ' ' + m.title + ' ' + ' getHorairesLundi())!=""): ?>Lun&nbsp;'+ m.horaires_lundi +' No horaire lundi ' variable m.horaires_lundi in JavaScript is well displayed in frontend. But I can't make a condition in PHP to display m.horaires_lundi only if m.horaires_lundi exists... Above I made a try with getHorairesLundi())!=""): ?> Is it a goog method? what else must I do? Is there an other method with something like: I've found a tutorial about Magento but can't find my solution.
