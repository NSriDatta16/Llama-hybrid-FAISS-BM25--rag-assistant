[site]: stackoverflow
[post_id]: 3631974
[parent_id]: 3631960
[tags]: 
Your for() order is mixed up, this: for (x = 0; x++; x Should be: for (x = 0; x You can use .toggleClass("class", bool) to shorten it a bit though, like this: function SelectButton(id) { for (var x = 0; x An even better approach would be to cache the selectors so they're not run each time, like this: var ToggleButtons = [$("#Click"), $("#DoubleClick"), $("#Drag"), $("#RightClick"), $("#MiddleClick")]; function SelectButton(id) { $(".ToggleButtonSelected").removeClass('ToggleButtonSelected'); ToggleButtons[id].addClass('ToggleButtonSelected'); }
