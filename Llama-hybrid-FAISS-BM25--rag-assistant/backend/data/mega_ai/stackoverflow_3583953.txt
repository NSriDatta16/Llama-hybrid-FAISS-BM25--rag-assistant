[site]: stackoverflow
[post_id]: 3583953
[parent_id]: 3583928
[tags]: 
Here's your sweet and simple method and no need for a separate $var : function mediaType() { $userAgents = array("iPhone", "Chrome"); $browser = $_SERVER['HTTP_USER_AGENT']; $var = 0; foreach($userAgents as $agent) if(strpos($browser, $agent) !== FALSE) return 1; return 0; }
