[site]: stackoverflow
[post_id]: 1749746
[parent_id]: 1749730
[tags]: 
As a simple example, you could do this: or perhaps your users can be in multiple roles, in which case something like this logic might be more appropriate: Or a more elegant approach for the latter using an extension method: with public static void RenderPartialIfInRole (this HtmlHelper html, string control, string role) { if (HttpContext.Current.User.IsInRole(role) html.RenderPartial(control); }
