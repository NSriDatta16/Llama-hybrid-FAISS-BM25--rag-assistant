[site]: stackoverflow
[post_id]: 1406000
[parent_id]: 1405947
[tags]: 
var qry = BenefitCodes .Where(b => b.Code != '1001' && b.Code != '1009' && b.BenInterest != 'E' && ( b.BenProductLine == CoverageProductLine || b.BenProductLine == null)) .OrderBy(b => b.BenDesc); You don't even need the "Select" when you aren't using the LINQ syntax. as the Where and OrderBy methods already return your IQueryable.
