[site]: stackoverflow
[post_id]: 4539615
[parent_id]: 
[tags]: 
Magento topSearch doesn't work in template

I want to take topSearch from the header and copy it to a content page, so I cut from header.phtml and added to 3columns.phtml : getChildHtml('topSearch'); ?> It doesn't work, though. Any ideas?
