[site]: stackoverflow
[post_id]: 3739554
[parent_id]: 3739458
[tags]: 
I don't see what would be wrong with just doing this: float calcWeightedAverage( float v1, float v2, float w1, float w2 ) { static const float eps = FLT_MIN; //Or some other suitably small value. v1 = fabs( v1 ); v2 = fabs( v2 ); if( v1 + v2 Sure, no "fancy" stuff to figure out your division, but why make it harder than it has to be?
