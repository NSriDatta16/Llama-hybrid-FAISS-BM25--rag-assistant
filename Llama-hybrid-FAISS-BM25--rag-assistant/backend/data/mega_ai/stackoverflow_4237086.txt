[site]: stackoverflow
[post_id]: 4237086
[parent_id]: 
[tags]: 
Retrieving links using jquery

I'm trying to create a list links using jquery. I'm using this code to retreive the date $.getJSON('/chapersRetrival.php', function(data) { $.each(data, function(index, array) { $(" ").attr({href: 'http://www.mangamanga.com/chapNo='+array['chapterID']+'&#pageNo=1>', title:array['mangaName'] + array['chapterName']} + array['chapterNumber']).appendTo("#mangaChpaters"); }); }); just wondering if there is anything wrong with regards to who the code is written ?
