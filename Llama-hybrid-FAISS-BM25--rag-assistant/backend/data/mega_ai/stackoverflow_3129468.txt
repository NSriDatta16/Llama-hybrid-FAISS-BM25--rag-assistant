[site]: stackoverflow
[post_id]: 3129468
[parent_id]: 3129451
[tags]: 
Use LABjs . Embedding tags work because the browser loads and executes whatever script in it or referenced by it upon seeing any of them, but that also means the browser will block until it is done with the script.
