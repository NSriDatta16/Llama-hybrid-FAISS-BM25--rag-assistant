[site]: stackoverflow
[post_id]: 1178996
[parent_id]: 1178937
[tags]: 
So, to build on the two answers from AlbertoPL and Yishai, try this: class y implements x { A getValue(){ return new B();} } B extends A, so that's fine. The caller doesn't need to know what subclass of A is returned, only that it extends A. So there we go :)
