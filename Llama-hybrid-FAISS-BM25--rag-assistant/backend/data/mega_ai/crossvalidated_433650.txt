[site]: crossvalidated
[post_id]: 433650
[parent_id]: 
[tags]: 
AR model throws ValueError on a constant time series

Here's my code: from statsmodels.tsa.ar_model import AR model = AR(np.array([1, 1, 1, 1, 1, 1, 1])) model = AR(arr) results = model.fit(method='mle', maxlag=10) model.predict(results.params) The error I'm getting: ValueError: x contains a constant. Adding a constant with trend='c' is not allowed. Why can't the AR model deal with a constant series?
