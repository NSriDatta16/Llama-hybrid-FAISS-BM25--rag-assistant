[site]: stackoverflow
[post_id]: 4939541
[parent_id]: 4939415
[tags]: 
for (MetadatRetrievalDTO dto : dtos) { for (Map.Entry entry : queryParams.entrySet()) { if (dto.getMetadataName().equals(entry.getKey()) && dto.getMetadataValue().equals(entry.getValue())) { // dto matches } } } Not sure if this what you mean... But you should consider indexing or something like that...
