[site]: stackoverflow
[post_id]: 1795845
[parent_id]: 1795812
[tags]: 
Here are some snippets of code that we use to assign members to a project, hopefully this helps you out! In the view we have: Select project members: " class="inline"> In the controller we have: // update project members foreach (var key in collection.Keys) { if (key.ToString().StartsWith("Member")) { int userId = int.Parse(key.ToString().Replace("Member", "")); if (collection[key.ToString()].Contains("true")) this.ProjectRepository.AddMemberToProject(id, userId); else this.ProjectRepository.DeleteMemberFromProject(id, userId); } } The main thing to remember when working with the Html Checkbox Helper is to use contains() to determine true or false.
