[site]: stackoverflow
[post_id]: 1114414
[parent_id]: 1114340
[tags]: 
As you haven't specified any thing about language or technologies you are using, I'm taking liberty to assume that you want to accomplish things in HTML/JavaScript. So here is the solution. Chk1 Chk2 function chkchanged(a) { if(document.getElementById(a).checked) { var s=document.getElementById("sel"); var o = document.createElement('option'); o.text = a; o.value = a; try { s.add(o, null); } catch(ex) { s.add(o); } } else //if you want to remove also on unchecking { var s=document.getElementById('sel'); var i; for( i=0; i
