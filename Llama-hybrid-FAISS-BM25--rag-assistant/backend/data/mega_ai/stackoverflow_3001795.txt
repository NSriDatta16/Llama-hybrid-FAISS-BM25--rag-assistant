[site]: stackoverflow
[post_id]: 3001795
[parent_id]: 2994448
[tags]: 
As above introduced you shouldn use regex to parse html, or xml. I would do your example with str_replace(); if its all time the same. $str = ' This is a paragraph with an image '; $str = str_replace('id="paragraph" class="green"', "", $str); $str = str_replace('width="50" height="75"',"",$str);
