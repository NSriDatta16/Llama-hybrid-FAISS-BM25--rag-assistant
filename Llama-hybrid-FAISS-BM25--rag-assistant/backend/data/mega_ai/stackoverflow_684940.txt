[site]: stackoverflow
[post_id]: 684940
[parent_id]: 
[tags]: 
Embedding .NET usercontrol in IE8

I have a simple webpage containing a .NET usercontrol embedded using the OBJECT tag. In IE7, the page displays and I can use the usercontrol. However, in IE8, the usercontrol does not even load. Any thoughts / ideas?
