[site]: stackoverflow
[post_id]: 5242637
[parent_id]: 5236919
[tags]: 
people should use magento methods for getting the path for controller class and then it would't break the compiler as the methods would return the correct path. require_once Mage::getModuleDir('controllers', 'Mage_Checkout').DS.'OnepageController.php';
