[site]: stackoverflow
[post_id]: 5448273
[parent_id]: 5447317
[tags]: 
This question is intriguing. Google returns no results for implementing SPCache A search through unobfusticated code; reveals only two internal classes (DataCache and DBCache) in the User Profile system that use SPCache. It sounds like you should not use SPCache (but usually the msdn page would say "not intended to be used by user code".)
