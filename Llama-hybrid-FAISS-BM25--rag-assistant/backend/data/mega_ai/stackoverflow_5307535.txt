[site]: stackoverflow
[post_id]: 5307535
[parent_id]: 5306821
[tags]: 
In main.h you need to declare ArrowKey as extern "C" std::map ArrowKey; and in main.cpp after the includes you should define it: std::map ArrowKey; BTW, I would also replace #ifndef/#define/#endif with #pragma once in the headers.
