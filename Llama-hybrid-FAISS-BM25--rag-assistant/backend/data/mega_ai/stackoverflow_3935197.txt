[site]: stackoverflow
[post_id]: 3935197
[parent_id]: 3933612
[tags]: 
I cannot use the orderfile attribute as we specify the as c:\SqlScripts*.sql. Obviously, this will only work if you don't have strict ordering requirements. If you do... How do I overcome this problem? ...use a setup that allows to deal with ordering in a deterministic way. I see 3 solutions (...). Any more elegant solutions? I think you nailed. My preference goes to option 3.
