[site]: stackoverflow
[post_id]: 5759520
[parent_id]: 5759221
[tags]: 
From your comments it seems you can't use the normal check for include guards or #pragma once s. Another way is to save the absolute (or maybe relative) path for every file you include in a string and if you encounter on of the include pathes again, don't include the file.
