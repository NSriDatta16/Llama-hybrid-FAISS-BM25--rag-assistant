[site]: stackoverflow
[post_id]: 4496216
[parent_id]: 
[tags]: 
how to apply background color to the found text

hello i have following code and it works fine for finding and replacing string... but it is not working globally..how to make it to work as globally and after replacing i want to highlight that string into green color.. how..? New Document $(document).ready(function(){ $('#replace').click(function(){ var oldstr=$('#inputstring').val(); var newstr=$('#newstring').val(); var para=$('#para').html(); var result=$('p:contains('+oldstr+')'); if(result) { var x=para.replace(new RegExp(oldstr, 'i','g'), newstr); $('#para').empty().html(x); } }) }) Enter the string to find: Enter string to replace: This is the new paragraph written to test how to replace the a string with desired string
