[site]: stackoverflow
[post_id]: 668798
[parent_id]: 668750
[tags]: 
var htmlFragment = " alert('hi') "; var style = " .foo { font-size: 14pt } "; // ... ... --> Good luck getting a regular expression to figure that out. You're using JavaScript, so I'm guessing you're probably running in a browser. Which means you have access to the DOM, giving you access to the browser's very capable HTML parser. Use it.
