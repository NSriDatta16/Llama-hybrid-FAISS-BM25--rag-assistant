[site]: stackoverflow
[post_id]: 5298070
[parent_id]: 5298049
[tags]: 
The selected attribute has to go on the HTML element and it should only be set when the option value matches. Most elegant way is to use the conditional operator ?: . Dogs Cats Others Better would be if you have the items in some List or Map . E.g. a List where Category has id and name properties. ${category.name} This way you don't need to repeat the same thing for all options.
