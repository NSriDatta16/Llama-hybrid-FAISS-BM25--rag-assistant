[site]: stackoverflow
[post_id]: 4815730
[parent_id]: 
[tags]: 
Display messages using JavaScript

I wonder if there is JS snippet/library for displaying messages from Django messages framework, when CookieStorage is used. It would be awesome for caching purposes, because it would be possible to cache whole views, even if they displays messages.
