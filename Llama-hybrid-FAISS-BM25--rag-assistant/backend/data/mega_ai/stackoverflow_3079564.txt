[site]: stackoverflow
[post_id]: 3079564
[parent_id]: 3079532
[tags]: 
You can't simply mixup JavaScript and PHP code Like this. JavaScript Code is executed in the Users Browser and the PHP code is executed on your server. So you have to make clear which code is to be executed on you server. In PHP you do this with the PHP Tags & ?> : function hinzu(pizza) { NeuerEintrag = new Option(pizza, pizza, false, false); document.getElementById("warenkorbfeld").options[document.getElementById("warenkorbfeld").length] = NeuerEintrag ; pizza_id as $tmp): ?> gesamtbetrag=gesamtbetrag + pizza_preis[$tmp] ?>; document.getElementById('gesamtbetrag').innerHTML = gesamtbetrag; } or if I'm interpreting your heredoc strings correctly: echo pizza_id as $tmp) { echo "gesamtbetrag=gesamtbetrag+" . $this=>pizza_preis[$tmp]; // The important differnces: ^ ^ } echo
