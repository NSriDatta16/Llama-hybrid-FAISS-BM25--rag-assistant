[site]: stackoverflow
[post_id]: 4618687
[parent_id]: 3624880
[tags]: 
You can get Silverlight interop to invoke a JS method and get its return value using the following: // Returning a String string stringValue = (string)HtmlPage.Window.Invoke("myJSMethod"); where myJSMethod returns the embedded key. But if you're embedding the key anyway, why not just put in into the s? ...
