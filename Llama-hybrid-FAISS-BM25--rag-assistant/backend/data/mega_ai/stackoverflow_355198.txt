[site]: stackoverflow
[post_id]: 355198
[parent_id]: 355087
[tags]: 
Since D has 80-bit reals built in (as far as I can tell), why would you not just use the standard approach of comparing with an epsilon value. This can be a fixed value if you know the rough range in advance, such as US currency: if (abs (a - b) or an acceptable relative error, such as 1 part per million of the average: if (abs (a - b) Keep in mind I don't know D, the code above is for demonstrative purposes only.
