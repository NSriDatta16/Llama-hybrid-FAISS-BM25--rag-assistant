[site]: stackoverflow
[post_id]: 3296567
[parent_id]: 3296519
[tags]: 
It's hard to diagnose without code, but try something like this: $("div span,div br").remove(); You can replace div with a selector that better describes your div element, like an ID: $("#mydiv span, #mydiv br") // ... Hope this helps! Edit Based on your new code, try this: $("# span#" + draggedNodeID).remove();
