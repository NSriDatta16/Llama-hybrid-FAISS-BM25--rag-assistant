[site]: crossvalidated
[post_id]: 157331
[parent_id]: 
[tags]: 
Random Forest: Predictors have more than 53 categories?

What is the solution when we want to apply the Random Forest function in R to a predictor with more than 53 categories? > RandomForestPrediction=function(alpha){ + d = sort(sample(nrow(MPS), nrow(MPS)*alpha)) + train RandomForestPrediction(0.7) Error in randomForest.default(m, y, ...) : Can not handle categorical predictors with more than 53 categories.
