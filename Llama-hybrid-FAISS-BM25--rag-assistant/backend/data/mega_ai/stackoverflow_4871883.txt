[site]: stackoverflow
[post_id]: 4871883
[parent_id]: 4871826
[tags]: 
just save the x/y coordinate of the point where the drag started, and the point where the finger is after a few milliseconds, then calculate the direction vector http://en.wikipedia.org/wiki/Direction_vector with the calculated formula, always just fetch the fingers x or y position, and use it to calculate the counterpart
