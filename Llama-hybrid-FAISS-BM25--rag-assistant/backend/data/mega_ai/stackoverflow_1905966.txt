[site]: stackoverflow
[post_id]: 1905966
[parent_id]: 1905954
[tags]: 
Actually MySQL does include support for transactions through the InnoDB storage engine which is bundled with MySQL. It's not the default storage engine but that setting can be changed. Or you can simply specify the engine when creating your tables. create table mytable (...) engine=innodb;
