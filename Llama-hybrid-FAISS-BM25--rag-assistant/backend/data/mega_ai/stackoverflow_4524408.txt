[site]: stackoverflow
[post_id]: 4524408
[parent_id]: 
[tags]: 
how can I write lock {} elegantly?

In my multi-threaded project(C# 3.5), I have many codes simliar to this: Map map; lock ( _maps ) { map = _maps.First( i => i.ID == arg.MapID ); } It looks ugly to me. Takes too many lines for simple retrieval... What I want is one line code as simple as this: Map map = _maps.First( i => i.ID == arg.MapID ); But with lock: (not working) Map map = lock( _maps ){ _maps.First( i => i.ID == arg.MapID ) }; Map map = delegate(){ lock( _maps ) return _maps.First( i => i.ID == arg.MapID ) }; Any help will be greatly appreciated :) Thanks
