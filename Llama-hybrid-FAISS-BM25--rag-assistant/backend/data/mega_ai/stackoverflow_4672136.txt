[site]: stackoverflow
[post_id]: 4672136
[parent_id]: 4672035
[tags]: 
Do you actually need this global variable? If it's used only to control the depth of recursion, you can rewrite it in more elegant way, as follows: void foo() { fooHelper(0); } void fooHelper(int var) { if (var == 3) return; else fooHelper(var + 1); }
