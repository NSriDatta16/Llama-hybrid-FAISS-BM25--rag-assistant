[site]: stackoverflow
[post_id]: 1498948
[parent_id]: 1497712
[tags]: 
Just to throw in another possibility, you could use Apache Commons' Transformer and collect to create a new collection or transform to modify the existing one: Collection productIds = CollectionUtils.collect(products, new Transformer() { public Long transform(Object o) { return ((Product) o).id; } });
