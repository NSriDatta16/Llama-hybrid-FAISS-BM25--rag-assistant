[site]: stackoverflow
[post_id]: 3732299
[parent_id]: 3725739
[tags]: 
You could use VBA and ordinary file I/O to inspect the file, but what I'd do is load it into a hidden sheet, verify it, then copy the data from the hidden sheet to the user-visible sheet. This also gives you an opportunity to reorganize the presentation if that's useful.
