[site]: stackoverflow
[post_id]: 4549891
[parent_id]: 
[tags]: 
Can I use variables to replace If conditions?

Since I need j%3 and j%5 to use in three places, I used it like a variable, I just want to know, is this reduce any memory storage and increase efficiency, private void forloo() { bool a = j % 3 == 0; bool b = j % 5 == 0; for (int i=0; i
