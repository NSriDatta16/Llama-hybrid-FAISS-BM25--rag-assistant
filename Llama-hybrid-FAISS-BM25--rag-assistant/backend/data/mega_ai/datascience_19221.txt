[site]: datascience
[post_id]: 19221
[parent_id]: 19207
[tags]: 
Have you tried using randomForest instead of rpart ? For example, let's assume you have two data.frames: train_data and test_data In my example, the last column is the class (and is a factor variable), and all other variables (all other columns) are numeric . For example, one training example might look like: 4 5 10 12 1 0 where 0 is the class label. You can do the following to run a random forest with 50 trees: library(randomForest) model_pl_1
