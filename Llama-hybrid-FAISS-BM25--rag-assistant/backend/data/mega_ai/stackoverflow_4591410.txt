[site]: stackoverflow
[post_id]: 4591410
[parent_id]: 
[tags]: 
Jquery Resizable problem after class is added with live()

resizable() no longer works when the created div is clicked. All i'm doing is adding a class with live(). What is the solution to this problem. .aaa { width:100px; height:100px;background-color:#ccc;margin-bottom:5px;} p{widht:500px; height:500px; margin:0px; padding:0px;border:2px solid red;color:#000;} Click to create divs var dividcount = 1; $("button").click(function() { var myDiv = $(' '); $(myDiv).resizable().draggable().appendTo('#ptest'); dividcount++; }); $('div').live('click', function() { $('div').removeClass('selected'); $('div').html(''); $(this).addClass('selected'); $(this).html('div #' + this.id); });
