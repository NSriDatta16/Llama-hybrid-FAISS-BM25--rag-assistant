[site]: stackoverflow
[post_id]: 1558016
[parent_id]: 
[tags]: 
jQuery accordion, clicking an already opened accordion item just toggles it

I have a semi-working example that you can look at. This appears to work as most would desire, though it shares a bug I am seeing in many other accordions, which is if you click on the an already opened header link, it will be closed, and then opened again. Any elegant solutions? Here is the jQuery $(document).ready(function() { // Simple Accordion Style Menu Function $('h2.question').click(function() { $('div.answer').slideUp('normal'); $(this).next().slideDown('normal'); }); // Closes All Divs on Page Load $("div.answer").hide(); // Opens the first div var Current = $('.question:first'); Current.next().show(); }); And here is the basic markup I am looking to use: Header 1 some body content 1 some body content 2 some body content 3 Header 2 some body content a some body content b some body content c Header 3 some body content x some body content y some body content z
