[site]: stackoverflow
[post_id]: 3357416
[parent_id]: 3357382
[tags]: 
You can either store it in an instance variable (like you're doing with MaxSize ) or make Mean() return the average value instead of just printing it out. If you make Mean() return the average, then you can call it in your Show() function like this: cout
