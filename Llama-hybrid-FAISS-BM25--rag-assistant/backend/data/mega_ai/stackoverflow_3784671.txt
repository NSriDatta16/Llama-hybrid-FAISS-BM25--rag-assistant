[site]: stackoverflow
[post_id]: 3784671
[parent_id]: 3784553
[tags]: 
You can try something like : TCHAR szValue[TEMP_STR_SIZE] = {0}; DWORD dwStorageSize = TEMP_STR_SIZE; LPBYTE lpStorage = reinterpret_cast (szValue); // In case of registry value retrieval failure if (ERROR_SUCCESS != RegQueryValueEx(hRegKey, _T("NDS"), 0, 0, lpStorage, &dwStorageSize)) { // Prompt error } Edit: Just noticed there are no subkey, changed my sample code
