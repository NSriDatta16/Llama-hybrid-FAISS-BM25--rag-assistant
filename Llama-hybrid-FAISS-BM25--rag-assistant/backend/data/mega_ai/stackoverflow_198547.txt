[site]: stackoverflow
[post_id]: 198547
[parent_id]: 198535
[tags]: 
Maybe the page is being cached when in a virtual directory. Try adding this to the Page_Load Response.Cache.SetCacheability(HttpCacheability.NoCache) I think caching can be applied to directories using a config file. So may be this is the difference.
