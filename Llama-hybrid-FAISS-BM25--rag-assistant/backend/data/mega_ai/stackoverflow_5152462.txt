[site]: stackoverflow
[post_id]: 5152462
[parent_id]: 5152172
[tags]: 
try like this: _points.execute("""SELECT * FROM points WHERE strftime('%H', checkintime) == ? AND strftime('%w', checkintime) == ?""" % (time, day)) pay attention to %(time, day) I'm not sure about the %H and %w , don't know what time and day format is
