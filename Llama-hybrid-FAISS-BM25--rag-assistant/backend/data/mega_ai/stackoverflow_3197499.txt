[site]: stackoverflow
[post_id]: 3197499
[parent_id]: 3197461
[tags]: 
With a narrower table, the database will fit more records in a single IO page, and will therefore require fewer hard disk reads. The rule of thumb is to always use the data type that will require the least storage size.
