[site]: stackoverflow
[post_id]: 3665975
[parent_id]: 3665662
[tags]: 
You could handle the drag/drop events in a parent control like the Border. And then you probably need a reference to your viewbox in your drag/drop methode. Try this: and in your drag/drop methode ViewBox v = (sender as Border).Tag as ViewBox;
