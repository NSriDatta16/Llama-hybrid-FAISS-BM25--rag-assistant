[site]: stackoverflow
[post_id]: 4554773
[parent_id]: 
[tags]: 
How to handle an Event of a programmatically created object?

That's a very basic question. How can I catch for example the "touchesBegan" event of a UIWindow instance? So that whenever the user is touching the window a method I set is called? Thank you! F.
