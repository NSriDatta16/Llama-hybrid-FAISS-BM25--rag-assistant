[site]: stackoverflow
[post_id]: 1307938
[parent_id]: 1307933
[tags]: 
The pound character (#) is used in URLs to indicate where a fragment identifier (bookmarks/anchors in HTML) begins. You'll probably want to URL encode it as %23. Or, as Andrew noted in a comment, you could have the user pass the parameter without the pound sign and add it in later.
