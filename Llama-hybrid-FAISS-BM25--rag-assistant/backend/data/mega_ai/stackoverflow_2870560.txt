[site]: stackoverflow
[post_id]: 2870560
[parent_id]: 2870473
[tags]: 
When a large table is deleted or truncated InnoDB storage engine for MySQL will leave the empty space, and use it for subsequent insertion of new row versions. There are ways to shrink database. Check Re: how to shrink a MySQL database
