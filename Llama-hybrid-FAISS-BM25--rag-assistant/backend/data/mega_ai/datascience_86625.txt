[site]: datascience
[post_id]: 86625
[parent_id]: 86209
[tags]: 
You unroll the same RNN multiple times, but not in parallel. The RNN needs both an input and the previous step, therefore, you unroll the same RNN for the first time step, then for the second, and so on. For each step it is always the same RNN, so the weights are the same each time:
