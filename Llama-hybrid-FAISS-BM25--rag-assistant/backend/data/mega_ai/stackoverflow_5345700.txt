[site]: stackoverflow
[post_id]: 5345700
[parent_id]: 5345281
[tags]: 
That's what the Tag is for. It's very useful by itself but even more if combined with the springcache plugin and using page fragment caching. Since the include calls a regular action in a controller you can also consume it by an ajax call.
