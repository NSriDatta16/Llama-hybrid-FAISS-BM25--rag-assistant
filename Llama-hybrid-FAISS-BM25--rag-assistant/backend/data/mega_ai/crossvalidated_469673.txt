[site]: crossvalidated
[post_id]: 469673
[parent_id]: 469672
[tags]: 
You messed up with the columns. Scikit-learn assumes that features are in columns, numpy uses rowvar=True as default: np.random.seed(42) nDim = 20 nData = 1000 def naive_pca(d): return np.sort(np.linalg.eig(np.cov(d))[0])[::-1] data = np.random.uniform(0,1,(nDim, nData)) pca = PCA(n_components=nDim) pca.fit(data.T) #
