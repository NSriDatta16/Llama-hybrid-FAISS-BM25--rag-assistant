[site]: stackoverflow
[post_id]: 4390520
[parent_id]: 
[tags]: 
Convert a SQL query into a Linq to SQL instruction

I need to convert the below SQL query into a Linq query select * from bancos where codigobanco in (select distinct codigobanco from headerarquivo where (MONTH(datahorageracao)=12) and (YEAR(datahorageracao)=2010)) How could I do to transform this SQL query in a Linq query ?
