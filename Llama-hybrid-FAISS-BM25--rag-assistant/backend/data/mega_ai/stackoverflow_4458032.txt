[site]: stackoverflow
[post_id]: 4458032
[parent_id]: 4457390
[tags]: 
There is no special control to render this type of relationship. However you can try to use standard CheckBoxList control. Children will be shifted a little using jquery. Maybe it is more elegant solution to shift cells using jquery. jQuery(document).ready(function () { var cells = $('# ').find('td'); for (var i = 1; i Parent Child You know that parent is first node, the rest are children.
