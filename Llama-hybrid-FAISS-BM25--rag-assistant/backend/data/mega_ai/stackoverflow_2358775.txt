[site]: stackoverflow
[post_id]: 2358775
[parent_id]: 218733
[tags]: 
I found that the most elegant way to do this is to use jQuery to wire the onClick event: $(".deleteLink").click(function() { return confirm('Are you sure you wish to delete this record?'); }); ... Notice that I use an arbitrary CSS class to identify the link button.
