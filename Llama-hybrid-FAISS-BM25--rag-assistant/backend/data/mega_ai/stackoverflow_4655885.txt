[site]: stackoverflow
[post_id]: 4655885
[parent_id]: 4655865
[tags]: 
Upcasts are normal because every Derived is Base . When you write a class B that inherits from a class A, then B is a subclass of A. That means that you can use an object of type B anywhere that you can use an object of type A. You should read about inheritance and how powerful it is.
