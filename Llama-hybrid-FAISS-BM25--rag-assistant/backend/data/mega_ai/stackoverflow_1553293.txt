[site]: stackoverflow
[post_id]: 1553293
[parent_id]: 843730
[tags]: 
Seems like context.Response.Cache.SetCacheability(HttpCacheability.NoCache); will only work when using IIS7. I changed it to context.Response.AddHeader("Cache-Control", "no-cache"); and it seems to be working with IE7 and IE8.
