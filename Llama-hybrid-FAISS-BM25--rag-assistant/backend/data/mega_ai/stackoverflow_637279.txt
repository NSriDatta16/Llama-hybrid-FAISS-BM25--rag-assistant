[site]: stackoverflow
[post_id]: 637279
[parent_id]: 637268
[tags]: 
XHTML 1 specification says: С.3. Element Minimization and Empty Element Content Given an empty instance of an element whose content model is not EMPTY (for example, an empty title or paragraph) do not use the minimized form (e.g. use and not ). XHTML DTD specifies script tags as: Hope it helps, This is from : Why don't self-closing script tags work?
