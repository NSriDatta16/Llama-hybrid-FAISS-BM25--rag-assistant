[site]: stackoverflow
[post_id]: 2887479
[parent_id]: 2882373
[tags]: 
Foreign key enforcement is off by default. You need to use a PRAGMA to switch it on: PRAGMA foreign_keys = ON; Moreover, the SQLite library must also have been built with foreign key support at all. It probably has been though.
