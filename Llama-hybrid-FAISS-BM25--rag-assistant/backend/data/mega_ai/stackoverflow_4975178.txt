[site]: stackoverflow
[post_id]: 4975178
[parent_id]: 
[tags]: 
help converting this table to divs and css

This is driving me insane. how can i achieve the same effect like the table below but with divs and css. label1 foo label2 bar EDIT If foo or bar are long paragraphs they should not flow under the label column
