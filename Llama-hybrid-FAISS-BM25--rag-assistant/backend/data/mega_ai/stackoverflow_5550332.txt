[site]: stackoverflow
[post_id]: 5550332
[parent_id]: 5550286
[tags]: 
status_items = models.StorageItem.objects.filter(client=client_id, itemstatushistory__isnull=False).distinct().count() {% for item in status_items %} {{item.itemstatushistory_set.latest|cut:"Destroyed"}} {% endfor %} You've set status_items to an integer, so you can't then do for item in status_items . What are you trying to achieve?
