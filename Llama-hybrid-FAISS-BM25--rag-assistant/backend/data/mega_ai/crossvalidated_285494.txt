[site]: crossvalidated
[post_id]: 285494
[parent_id]: 285490
[tags]: 
Of course! You can make use of recurrent neural networks such as LSTM and GRU . It requires some understanding about neural networks in general to understand what is going on though. You basically input your data sequentially , so the neural network will relate the previous inputs with the current input.
