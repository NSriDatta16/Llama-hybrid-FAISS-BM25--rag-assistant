[site]: stackoverflow
[post_id]: 2969652
[parent_id]: 2969521
[tags]: 
Here's an example with custom(-izable) drawing and handling of window operations (min, max, size, drag): WPF adventures (Part III) or Windows Style Window was set to remove any title or border; all window controls, including min/max/close buttons, were defined in the custom code. Edit : OpenNetCF Blogs seem to experience technical issues and the link doesn't work right now. As alternative one can use a wayback machine .
