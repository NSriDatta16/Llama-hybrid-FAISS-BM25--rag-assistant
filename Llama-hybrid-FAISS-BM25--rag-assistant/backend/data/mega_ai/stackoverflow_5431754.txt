[site]: stackoverflow
[post_id]: 5431754
[parent_id]: 5431704
[tags]: 
I think you are going to have to move away from inheritance which is already a rather fragile tool when applied across module boundaries. I'd point you towards COM. Managed code can expose their interfaces as COM objects and C++ can consume them. And vice versa.
