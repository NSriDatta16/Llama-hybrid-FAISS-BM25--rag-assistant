[site]: datascience
[post_id]: 48160
[parent_id]: 48156
[tags]: 
I think you would benefit from keeping your data as a time series and use a sequence model. Commonly you would use an RNN, the most popular flavor would be a LSTM. Here is an example using Keras that I think is relevant for you Multivariate example from same blog (but forecasting instead of classification) LSTM with regression
