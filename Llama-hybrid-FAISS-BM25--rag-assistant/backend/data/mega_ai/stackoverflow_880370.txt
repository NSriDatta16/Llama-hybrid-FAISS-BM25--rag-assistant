[site]: stackoverflow
[post_id]: 880370
[parent_id]: 880311
[tags]: 
When is applied to a tree fragment, it takes the text content of that tree. In your case, it looks like your XML file doesn't contain any text (other than whitespace) which isn't in an attribute value. I suspect that you mean to select the value of a particular attribute node within the document, e.g.: (Without knowing the structure of your XML and what you're trying to select, I don't know what the real path would be.)
