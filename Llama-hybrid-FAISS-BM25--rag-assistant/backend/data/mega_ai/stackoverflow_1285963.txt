[site]: stackoverflow
[post_id]: 1285963
[parent_id]: 1285906
[tags]: 
The classic answer is a hash table. Constant time insertion/replacement. But it's not entirely clear what you want. If it's just to keep the keywords neatly organized instead of peppered through your code a simple array would do; then use #defines to index and select them.
