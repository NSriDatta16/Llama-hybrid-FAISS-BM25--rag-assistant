[site]: stackoverflow
[post_id]: 3751387
[parent_id]: 
[tags]: 
What is wrong with this boost c++ regex code?

include #include #include #include #include #include #include using namespace std; using namespace boost; int main() { string robotsfile="User-Agent: *" "Disallow: /"; regex exrp( "^Disallow:(.*)$"); match_results what; if( regex_search( robotsfile, what, exrp ) ) { string s( what[1].first, what[1].second ); cout i need to get the disallowed path / from Disallow: / what is wrong with my regex??
