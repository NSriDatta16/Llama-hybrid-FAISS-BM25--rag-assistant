[site]: stackoverflow
[post_id]: 1970284
[parent_id]: 1970247
[tags]: 
Put tinyMCE's loading sequence into the onload event. There are more elegant ways to do this using one of the JS frameworks, but the basic way is document.onload = function() { tinyMCE.init ...... } in the part of your document.
