[site]: stackoverflow
[post_id]: 3359404
[parent_id]: 3359346
[tags]: 
(\d+:)?(\d{2})\.(\d{2}) 1 or more digits, and a colon (optional) 2 digits dot 2 digits For each matched group, ensure that the numbers make sense. Embedding 0-60 in regex is just silly. matching = regex.text( str ); matching |= $2
