[site]: stackoverflow
[post_id]: 2819322
[parent_id]: 2819113
[tags]: 
Assuming you have an HTML fragment in form of a string and are in a browser, trying to consume it with JavaScript: var str = " 33,7 100 20,0 "; var temp = document.createElement("tr"); temp.innerHTML = str; var tds = temp.getElementsByTagName("td") for (var i=0; i See? No regular expressions necessary - the browser has a perfectly capable HTML parser built-in, no need to make your own. When using a JavaScript framework like jQuery, the above gets even easier: $(" 33,7 100 20,0 ").find("td").each( function() { alert( $(this).text() ); });
