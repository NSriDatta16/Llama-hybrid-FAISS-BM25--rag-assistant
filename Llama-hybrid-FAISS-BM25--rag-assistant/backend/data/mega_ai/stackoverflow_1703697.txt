[site]: stackoverflow
[post_id]: 1703697
[parent_id]: 
[tags]: 
jnlp webstart nativelib: how to specify different architecture for os

in a java webstart file (jnlp) you can specify the nativelib tag to load binaries. you can also specify toload different ones for different operating systems by also specifying the os attribute. for example: but how can i specify even different binaries for different architectures? for example win32 and win64 or linux 32 and linux 64bit. operating systems. where can i find a list of options for the os-attribute?
