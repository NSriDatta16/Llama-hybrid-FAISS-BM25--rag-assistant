[site]: stackoverflow
[post_id]: 3172809
[parent_id]: 3172761
[tags]: 
.draggable({ containment: image.canvas, }); return this; Should be (in jquery.pinpoint.js): .draggable({ containment: image.canvas }); return this; And: area.resizable({ handles: 'all', containment:image.canvas, }) should be area.resizable({ handles: 'all', containment:image.canvas }) So, basically as Pointy has said, you've got a few commas that shouldn't be there.
