[site]: crossvalidated
[post_id]: 457815
[parent_id]: 
[tags]: 
What is the difference between "learnable parameters" and "weights" in neural networks?

I think the learnable parameters are the set of all weights and biases in the neural network, but is it true in general?
