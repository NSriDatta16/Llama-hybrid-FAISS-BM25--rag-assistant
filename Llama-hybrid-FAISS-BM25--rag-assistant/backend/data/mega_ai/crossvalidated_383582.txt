[site]: crossvalidated
[post_id]: 383582
[parent_id]: 383575
[tags]: 
If you don't have any other (control) variables, In general, ceteris paribus, go for the simpeler method (e.g. could be more robust in terms of interpretation). Logistic regression would be more appropriate if you actually need to make predictions (it is an actual model).
