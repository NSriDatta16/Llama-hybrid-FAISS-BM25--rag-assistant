[site]: stackoverflow
[post_id]: 4975787
[parent_id]: 4975628
[tags]: 
If you need an average only then you do not need to check all elements in the list. You need only the first one and the last one. var period = list.Last() - list.First(); var averageDays = (double)period.TotalDays / (list.Count() - 1);
