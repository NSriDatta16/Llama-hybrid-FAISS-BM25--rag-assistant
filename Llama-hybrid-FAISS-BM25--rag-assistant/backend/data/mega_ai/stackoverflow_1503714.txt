[site]: stackoverflow
[post_id]: 1503714
[parent_id]: 1493623
[tags]: 
Check if there's an alias clobbering your use of the "svn" command. Also check if the destination host allows password-mode authentification. Might also be worth to check that the desktop doesn't fool around with some handy "keyring agent", those can interfere with svn+ssh.
