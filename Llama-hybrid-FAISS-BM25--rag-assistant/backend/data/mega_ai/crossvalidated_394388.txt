[site]: crossvalidated
[post_id]: 394388
[parent_id]: 394364
[tags]: 
If you are trying to estimate if the consumer will pay or not then a logit model would help. A simple logistic regression model with the set factors should do the trick. The output of the model would be $P$ (Consumer will pay). This link should help in understanding multicollinearity in the regressors.
