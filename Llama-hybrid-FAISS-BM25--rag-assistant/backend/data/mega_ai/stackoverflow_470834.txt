[site]: stackoverflow
[post_id]: 470834
[parent_id]: 470768
[tags]: 
Inline style tags should be recognized. A styelsheet block will not, as they are loaded at the page's initial render time. You can dynamically add classes you the existing stylesheet, but you'll have to parse your HTML fragment to grab what's there and do it in script. see: Totally Pwn CSS with Javascript
