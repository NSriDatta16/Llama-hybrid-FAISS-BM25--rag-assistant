[site]: stackoverflow
[post_id]: 5320647
[parent_id]: 5320580
[tags]: 
Second one is not an option at all. You cannot use a PHP class to store anything between calls, as it just will die in less than second like all php scripts do. So, the only choice is persistent storage, like database or session.
