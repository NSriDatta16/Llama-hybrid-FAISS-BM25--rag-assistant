[site]: stackoverflow
[post_id]: 4458423
[parent_id]: 4440631
[tags]: 
An empty masked edit control sets its property to the template string with spaces instead of "#" and "?". For example, if "Text" is set to "#####" the empty value is " " If "Text" is set to "###-###" the empty value is " - " In your case you can use this condition: DataStoragePort <> " "
