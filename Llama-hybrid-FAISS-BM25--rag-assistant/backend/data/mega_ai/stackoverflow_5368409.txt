[site]: stackoverflow
[post_id]: 5368409
[parent_id]: 5368208
[tags]: 
Indeed, these functions work only in Makumba >= 0.9 (and in some versions A workaround in 0.8 is to do the date programmatically via JSP tags, e.g.: and then use the variable inOneHour (which is in the pageContext) as a named parameter in your query, e.g.: = $oneHourAgo"> ....
