[site]: crossvalidated
[post_id]: 79171
[parent_id]: 77914
[tags]: 
It is a "most popular" algorithm indeed. But if you are using data with no ratings, then the ratings are implicitly 1, and so every item's average rating is 1. The result is random. That is to say: don't use it without item ratings to average.
