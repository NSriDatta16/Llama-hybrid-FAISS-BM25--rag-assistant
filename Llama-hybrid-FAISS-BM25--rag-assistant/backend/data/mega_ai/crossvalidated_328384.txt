[site]: crossvalidated
[post_id]: 328384
[parent_id]: 326071
[tags]: 
Approximate Bayesian Computing using Sequential Sampling by Jessi Cisewski at Carnegie Mellon University 2014 seems to have some r code to do this: Mean of a Gaussian with σ2 known: Sequential R code # INPUTS n=25 #number of observations N=2500 #particle sample size true.mu = 0 sigma = 1 mu.hyper = 0 sigma.hyper = 10 data=rnorm(n,true.mu,sigma) epsilon = 1 time.steps = 20 weights = matrix(1/N,time.steps,N) mu=matrix(NA,time.steps,N) d=matrix(NA,time.steps,N) rho=function(y,x) abs(sum(y)-sum(x))/n for(t in 1:time.steps){ if(t==1){ for(i in 1:N){ d[t,i]= epsilon +1 while(d[t,i]>epsilon) { proposed.mu=rnorm(1,0,sigma.hyper) # epsilon[t]) { sample.particle
