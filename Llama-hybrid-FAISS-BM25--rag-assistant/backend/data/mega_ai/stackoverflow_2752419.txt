[site]: stackoverflow
[post_id]: 2752419
[parent_id]: 2752402
[tags]: 
You may want to check the user agent string as follows: var userAgent = navigator.userAgent; if (userAgent.match(/iPad/i) || userAgent.match(/iPhone/i)) { // iPad or iPhone } else { // Anything else }
