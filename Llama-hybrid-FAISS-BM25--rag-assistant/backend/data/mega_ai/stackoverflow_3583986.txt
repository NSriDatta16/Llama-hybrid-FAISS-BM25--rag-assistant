[site]: stackoverflow
[post_id]: 3583986
[parent_id]: 3583928
[tags]: 
function mediaType() { $userAgents = array("iPhone", "Chrome", ....); $browser = $_SERVER['HTTP_USER_AGENT']; foreach($userAgents AS $userAgent) { if(preg_match('#' . preg_quote($userAgent, '#') . '#i', $browser)) { return true; } } return false; } Edit: Hm I was too late :/ But in comaprison to the other answers I would use preg_match to find the browser :)
