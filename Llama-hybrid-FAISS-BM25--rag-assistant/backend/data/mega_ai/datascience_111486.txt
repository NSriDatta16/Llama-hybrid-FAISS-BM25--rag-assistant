[site]: datascience
[post_id]: 111486
[parent_id]: 6838
[tags]: 
CNNs outperform over SVMs. If data is not linearly separable, complex, and the dimension of the data is large. Though we can use kernel trick in SVM for nonlinear separable data, but computation time for large data sets remains where in CNNs computation time is reduced compared to SVMs.
