[site]: stackoverflow
[post_id]: 4316008
[parent_id]: 4315982
[tags]: 
Yes, blocks outside the are fine. In fact many encourage placing them at the end of the . Placing them just before , defers fetching/loading until after the page content...to speed up the render. From the W3C HTML4 spec : The SCRIPT element places a script within a document. This element may appear any number of times in the HEAD or BODY of an HTML document.
