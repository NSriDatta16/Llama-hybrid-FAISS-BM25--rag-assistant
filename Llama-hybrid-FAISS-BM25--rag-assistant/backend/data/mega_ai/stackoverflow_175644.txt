[site]: stackoverflow
[post_id]: 175644
[parent_id]: 175609
[tags]: 
The droppable API accepts a 'drop' callback in its options. Use it to change the src of the image, something like: $('#dropContainer').droppable({ drop: function(e,ui) { $(ui.draggable).attr('src','/path/to/different/image'); } });
