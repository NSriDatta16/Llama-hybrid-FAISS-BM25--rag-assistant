[site]: stackoverflow
[post_id]: 4891515
[parent_id]: 4891429
[tags]: 
The simplest/most elegant approach would be to chunk your list of countries $(this).find('country') into an an array of fixed length arrays. Then, nest a couple loops to iterate the arrays, building a list for each array. EDIT: So your code created a bunch of items in a single list as it was. What you need to do is find out when every 20th item happens, the simplest way, would be to break up your array, something like this. $(document).ready(function() { $.ajax({ type: "GET", url: "lang-select.xml", dataType: "xml", success: function(xml) { var countriesList = $('div.countries ul'); var countriesListItems = []; $(xml).find('dropdown').each(function(){ $(this).find('country').each(function(){ var url = $(this).attr('url'); var label = $(this).text(); var listItem = $(" " + label + " "); countriesListItems.push(listItem); }); }); var chunkedCountriesListItems = chunk(countriesListItems, 20); var currentList = countriesList; $.each(chunkedCountriesListItems, function() { $.each(this, function() { currentList.append(this); }); countriesList.after(currentList); currentList = $(' '); }); } }); function chunk(arr, len){ for(var x, i = 0, c = -1, l = arr.length, n = []; i
