[site]: stackoverflow
[post_id]: 4779407
[parent_id]: 
[tags]: 
JSONArray to string array

It looks like too much boilterplate to convert a json array to string[]. Is there any simpler and elegant way? final JSONArray keyArray = input.getJSONArray("key"); String[] keyAttributes = new String[keyArray.length()]; for(int i = 0; i
