[site]: stackoverflow
[post_id]: 2948882
[parent_id]: 2943041
[tags]: 
Thaks your help I got solution! That's my working script: var forma=$("form"); var elements=$("*", forma); $.each(elements,function(i){ var id = $(elements[i]).attr("id"); var tipo = $(elements[i]).attr("type"); var nome=$(elements[i]).attr("name"); var val=$(elements[i]).attr("value"); switch (tipo) { case "text" : $(elements[i]).val($(id, xml).text()); break; case "radio": $("input:radio[name='"+nome+"'][value='"+ $(nome, xml).text() +"']").attr('checked', true); break; case "checkbox": $(xml).find(nome).each(function(){ var value =$(this).text(); if (val==value) { $("#"+id).attr("checked", value); } }); break; case "select-one" : $(elements[i]).append(" "+$(nome ,xml).text()+" "); break; } $("#scheda_ris").append(id+" "+tipo+" "+nome+" "+val+" "); //that's just for debug }); If you have suggestions to make it more elegant I'll be really happy Thanks again ciao h.
