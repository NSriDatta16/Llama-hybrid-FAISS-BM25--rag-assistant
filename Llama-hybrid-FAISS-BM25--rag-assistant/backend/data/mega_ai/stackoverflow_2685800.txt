[site]: stackoverflow
[post_id]: 2685800
[parent_id]: 2684802
[tags]: 
Thanks to munch and mnemosyn. Munch - outrageous! That simplicity is what I was after from the get-go but just couldn't cobble it together ... and btw, there's no 'active' class by default so I was able to trim 2 lines of your solution - a thing of beauty! ... and I was happy as a clam that I'd previously found this solution: $(document).ready(function() { var containerClass = $('#container').attr('class'); $('#nav-primary li').each(function(){ var secondaryID = $(this).attr('id'); if ( secondaryID == containerClass ) { $(this).addClass('active'); $('#aux-left').find('div[id*="'+secondaryID+'"]').each(function(){ $(this).addClass('active'); }); // end #aux-left.find } // end if }); // end #nav-primary.each }); // end doc.ready.func ... but yours is even better! Huge thank you! cheers, svs
