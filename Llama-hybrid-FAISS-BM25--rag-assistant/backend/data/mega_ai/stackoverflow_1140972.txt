[site]: stackoverflow
[post_id]: 1140972
[parent_id]: 
[tags]: 
how can we resize a row in dojo grid

I want to resize the dojo grid row by dragging it as we can do with the column. I want it some thing like Excel row which we can resize(minimize or maximize its height), is there a way to do this in dojo grid? Thanks Faz
