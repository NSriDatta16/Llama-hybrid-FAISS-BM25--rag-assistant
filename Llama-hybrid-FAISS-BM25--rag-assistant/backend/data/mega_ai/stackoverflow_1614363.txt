[site]: stackoverflow
[post_id]: 1614363
[parent_id]: 1614332
[tags]: 
You can't declare a src and content in a single element. from the script element definition on the W3C site : If the src has a URI value, user agents must ignore the element's contents and retrieve the script via the URI Try separating them: var clear=" "; " type="text/javascript">
