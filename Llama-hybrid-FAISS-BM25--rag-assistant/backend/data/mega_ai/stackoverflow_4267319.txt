[site]: stackoverflow
[post_id]: 4267319
[parent_id]: 
[tags]: 
Jquery Draggable / Live

I have: $(document).ready(function () { $(".div1, .div2, .div3, .div4, .div5").draggable(); $("#menu").click(function () { $(" ").appendTo("#layout"); }); }); But the draggable function works just to the divs that already exist when the page is loaded. When I add dynamically, I could not drag them. My Html:
