[site]: stackoverflow
[post_id]: 5747688
[parent_id]: 5747648
[tags]: 
There might be a more elegant way but this should work: SELECT s.Name, MAX(Count) FROM (SELECT S.Name, COUNT(C.StateID) as 'Count' --NO Can do, please help! FROM tblStates as S JOIN tblCities as C ON S.StateID = C.StateID GROUP BY S.Name)
