[site]: stackoverflow
[post_id]: 3910086
[parent_id]: 
[tags]: 
how to do self rotating CALayer

When i Clicked on view its create a star shape path. Now i want to spin the star which is added to CALayer. Can any one help me out.... How to spin the CALayer on iphone. -(void)touchesBegan:(NSSet *)touches withEvent:(UIEvent *)event{ UITouch *touch=[touches anyObject]; currentPoint=[touch locationInView:self.view]; CGMutablePathRef starPath; rootLayer = [CALayer layer]; rootLayer.frame = self.view.bounds; [self.view.layer addSublayer:rootLayer]; starPath = CGPathCreateMutable(); CGPathMoveToPoint(starPath, NULL,currentPoint.x,currentPoint.y+15.0f); for(int i = 1; i
