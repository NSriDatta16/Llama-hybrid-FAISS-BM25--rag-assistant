[site]: crossvalidated
[post_id]: 296694
[parent_id]: 
[tags]: 
Variable Length Markov Chains is a computational approach for analyzing sequences, which allows for prediction, in the same way as ordinary Markov chains, but with an arbitrary length of the memory which the Markov chain can draw on in making its predictions.
