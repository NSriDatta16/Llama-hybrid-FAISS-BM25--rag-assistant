[site]: stackoverflow
[post_id]: 4103772
[parent_id]: 3229064
[tags]: 
You could log all the visits to the site using iis logging either to a file or directly into SQL. Then you could query the data to see the average time a user spends on the site, how many only go to one page and so on. If page load times are at the 10 second plus range your are in big trouble .
