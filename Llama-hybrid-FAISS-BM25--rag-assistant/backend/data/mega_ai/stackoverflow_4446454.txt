[site]: stackoverflow
[post_id]: 4446454
[parent_id]: 4446327
[tags]: 
I'll assume you're selecting elements with the class "zoom" . document.observe('dom:loaded', function() { if(navigator.userAgent.indexOf('Chrome')!=-1) { $$('.zoom').each(function(e) { e.addClassName( 'chrome' ); }); } }); In the code in the question, you're adding to Object.prototype . Never do that. It will only cause problems.
