[site]: datascience
[post_id]: 93957
[parent_id]: 72420
[tags]: 
You can use model='bert-base-uncased' with transformers.pipeline : pip install transformers from transformers import pipeline unmasker = pipeline('fill-mask', model='bert-base-uncased') unmasker("a [MASK] in the watergate saga") This works for me on Google Colab.
