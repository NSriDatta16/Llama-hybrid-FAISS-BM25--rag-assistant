[site]: crossvalidated
[post_id]: 620726
[parent_id]: 620134
[tags]: 
Yes, when training the VAE, you should freeze the weights of the discriminator and only update the VAE parameters ( $\theta$ ). Algorithm 2 provides a nice summary of how to jointly train the two models (take notice that the parameters ( $\theta, \psi$ ) are updated separately).
