[site]: stackoverflow
[post_id]: 5356002
[parent_id]: 5355971
[tags]: 
Heed the warning. buffer is a contiguous area of memory allocated on the stack . Once the call stack unwinds (your function returns), local storage is rendered garbage. If you want to return a pointer to a memory that survives the return of the function, allocate said memory in the heap.
