[site]: stackoverflow
[post_id]: 4566285
[parent_id]: 
[tags]: 
livequery keypress event

(Im using the before() function from jquery to append a new element to a div layer. $('#AddParagraphButton').click(function() { $('#TheLayer').before(' Some text... '); }); here I have set keypress function to insert tag. $('p').keypress(function(e){ if(e.which == 13){ e.preventDefault(); document.execCommand('insertHTML', false, ' '); } }); this works fine(br tag inserts) until the append function is called and a new is added. How do I get livequery to unbind the keypress event and bind again? EDIT: the tags have the contentEditable property. Im doing this because the tags are wrapped in divs and I only want tags
