[site]: stackoverflow
[post_id]: 4049721
[parent_id]: 4049485
[tags]: 
def entry_template content_tag(:div, :class => "item_title") do " " + if x == 'garage' entry.garage.name else 'Not garage' end + " " end end or, in my eyes somewhat nicer, since it doesn't switch styles all the time, def entry_template content_tag(:div, :class => "item_title") do content_tag(:h3) do if x == 'garage' entry.garage.name else 'Not garage' end end end end
