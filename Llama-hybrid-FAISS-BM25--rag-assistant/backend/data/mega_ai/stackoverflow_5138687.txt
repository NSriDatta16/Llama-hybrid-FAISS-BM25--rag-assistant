[site]: stackoverflow
[post_id]: 5138687
[parent_id]: 5138658
[tags]: 
You can use the Build class to get information about the device and OS. Update: See this code fragment; // connect to the servlet String location = "http://www.foo.com/servlet/TestServlet"; URL testServlet = new URL( servletLocation ); URLConnection servletConnection = testServlet.openConnection(); // inform the connection that we will send output and accept input servletConnection.setDoInput(true); servletConnection.setDoOutput(true); // Don't use a cached version of URL connection. servletConnection.setUseCaches (false); servletConnection.setDefaultUseCaches (false); // Specify the content type that we will send binary data servletConnection.setRequestProperty ("Content-Type", " "); // get input and output streams on servlet . . . // send your data to the servlet . . . See this article for details. Change it as per your needs.
