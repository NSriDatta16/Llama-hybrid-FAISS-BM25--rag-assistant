[site]: stackoverflow
[post_id]: 4376364
[parent_id]: 4376297
[tags]: 
You can't upcast like this in C# and expect valid results. If b adds new members, an a object will not suddenly have them, however you try to cast it. Say b adds a buzz() method. Suppose that your cast actually works. What happens when you call bar.buzz() ?
