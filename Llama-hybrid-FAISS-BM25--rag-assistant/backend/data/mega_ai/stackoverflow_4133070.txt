[site]: stackoverflow
[post_id]: 4133070
[parent_id]: 4132931
[tags]: 
Looking at the source code , I can't see how this is possible as -f and -file are treated in exactly the same way. else if (arg.equals("-buildfile") || arg.equals("-file") || arg.equals("-f")) { i = handleArgBuildFile(args, i); } I'd encourage you to look at the source too.
