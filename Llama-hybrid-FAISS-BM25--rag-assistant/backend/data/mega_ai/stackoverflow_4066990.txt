[site]: stackoverflow
[post_id]: 4066990
[parent_id]: 4066931
[tags]: 
you can use hash approach like this, my %h = map {$_ => 1 } @agentConfAssociationDeletionsList; if (exists $h{$agentConfAssociationDeletionsList}) { delete $h{$agentConfAssociationDeletionsList}; # like that }
