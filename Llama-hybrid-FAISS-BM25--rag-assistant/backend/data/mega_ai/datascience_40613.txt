[site]: datascience
[post_id]: 40613
[parent_id]: 40343
[tags]: 
It's just several time-series with same data. You should make preprocessing (if it's neccessary) and can provide as an input just for one RNN layes. for example model.add(GRU(units=512,return_sequence=True,input_shape=(None,number_of_inputs))
