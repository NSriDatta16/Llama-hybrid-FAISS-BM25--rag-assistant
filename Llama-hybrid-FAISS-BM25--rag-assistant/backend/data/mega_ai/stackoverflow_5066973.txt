[site]: stackoverflow
[post_id]: 5066973
[parent_id]: 3184670
[tags]: 
Magento uses TwoLevels backend for caching. Cache stores to both of them. And according to Zend's code, Magento reads from fast, and then from slow, if nothing returns. You have to flush memcache and remove /var/cache at the same time.
