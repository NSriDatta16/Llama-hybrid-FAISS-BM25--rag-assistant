[site]: stackoverflow
[post_id]: 5197838
[parent_id]: 5197801
[tags]: 
At the very least, you want your function call to happen only after the page is loaded. This can be done by embedding the function in the body tag during onload : The function call is going to execute on the client-side, after the entire page has been processed by PHP. You can't communicate back-and-forth between JavaScript and PHP during the process of the page.
