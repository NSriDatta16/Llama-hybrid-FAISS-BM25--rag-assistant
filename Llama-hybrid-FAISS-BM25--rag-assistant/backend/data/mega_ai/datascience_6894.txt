[site]: datascience
[post_id]: 6894
[parent_id]: 
[tags]: 
For logistic regression, Predict.glm() outputs $p$ or $ln(p/1-p)$?

I'm performing a logistic regression in R. I wanted to know if the function predict.glm outputs $p$ (probability of event occurring) or log odds i.e. $log(p/1-p)$?
