[site]: stackoverflow
[post_id]: 5103970
[parent_id]: 
[tags]: 
Issues with adding new table's row with JQuery

I follow the instruction for adding new table’s row from: Add table row in jQuery A new row was successfully added but it didn’t get the other rows’ feature (highlight, draggable,…). The table: col1 col2 col3 col4 +++ +++ The jQuery script: $(document).ready(function(){ $("#addPubTab").click(function(){ var id = "3"; var value = " "+ " +++ "+ " "+ " "+ " "+ " "; $('#pubTab tbody').append(value); }); }); The problem Problem: It added the new row but I cannot drag it (up/down) as I drag the other tow (there are no curser for dragging). Inspecting elements resolve with: _ _ _ _ Please note that I am using the jQuery script (dragging/sorting): $(function() { $("#pubTab:not(thead)").tableDnD(); }); Please assist.
