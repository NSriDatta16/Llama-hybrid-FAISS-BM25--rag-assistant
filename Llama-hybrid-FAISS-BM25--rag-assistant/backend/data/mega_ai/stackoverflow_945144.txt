[site]: stackoverflow
[post_id]: 945144
[parent_id]: 945074
[tags]: 
SQL server actually makes some indexes for you (e.g. on primary key). Indexes can become fragmented. Too many indexes will always reduce performance (there are FAQs on why not to index every col in the db) also there are s ome situations where indexes will always be slower .
