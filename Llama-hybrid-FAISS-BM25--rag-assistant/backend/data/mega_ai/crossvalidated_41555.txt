[site]: crossvalidated
[post_id]: 41555
[parent_id]: 41545
[tags]: 
I recommend using some form of cross-validation . It's even the name of the site! 10-fold CV is commonly used, but there are other, more sophisticated methods. Here is an example, based on the caret vignette : #Load Data set.seed(42) require(mlbench) data(BostonHousing) y In this example, the random forest has a lower cross-validated RMSE , so I would select that model.
