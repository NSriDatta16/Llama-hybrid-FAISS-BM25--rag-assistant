[site]: stackoverflow
[post_id]: 636491
[parent_id]: 634508
[tags]: 
You can do this through a file handle as well. Not as elegant as some solutions, but it would likely work. Something along the lines of: my $foo; open(READ, "env ps |"); while ( ) { print; $foo .= $_; } print $foo; close(READ);
