[site]: stackoverflow
[post_id]: 4612330
[parent_id]: 4612223
[tags]: 
1NF sets the very basic rules for an organized database: 1: Eliminate duplicative columns from the same table. 2: Create separate tables for each group of related data and identify each row with a unique column (the primary key). The problem with your Database tables is "Name"(duplicate column).
