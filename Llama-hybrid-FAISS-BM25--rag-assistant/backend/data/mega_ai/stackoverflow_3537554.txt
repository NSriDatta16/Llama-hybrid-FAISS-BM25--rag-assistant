[site]: stackoverflow
[post_id]: 3537554
[parent_id]: 3533631
[tags]: 
For anyone paying attention or having the same: This works Sinatra get '/:thisticket' do @ticket=Ticket.first(:thisticket=>params[:thisticket]) @comments=@ticket.comments.all(:order => [ :created_at.desc ]) if @ticket haml :showticket Haml @comments.each do |comment| comment.comment etc, etc, et. al.
