[site]: datascience
[post_id]: 115818
[parent_id]: 71804
[tags]: 
Use make_column_transformer and fit_transform for this: from sklearn.preprocessing import oneHotEncoder from sklearn.compose import make_column_transformer transformer=make_column_transformer(oneHotEncoder(),categorical_column,remainder="passthrough") transformed=transformer.fit_transform(data) transformed_df=pd.dataframe(transforme,columns=transformer.get_feature_names()) You will get the data frame which has both encoded categorical data and other numerical columns
