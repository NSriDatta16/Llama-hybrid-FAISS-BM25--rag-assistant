[site]: stackoverflow
[post_id]: 3461856
[parent_id]: 3461821
[tags]: 
You can do something like this: Organization.find(1140).events.all(:conditions => ["DATE(time) DATE() is a mysql function to parse the given value to Date format. And if you want to compare dates you should use Date.today instead of Time.now.beginning_of_day , it's much shorter and more readable.
