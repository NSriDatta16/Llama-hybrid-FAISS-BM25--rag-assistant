[site]: stackoverflow
[post_id]: 4719623
[parent_id]: 1183111
[tags]: 
I use urllib with a modified user-agent(since Timmy blocked the original) and BeautifulSoup. Anyway, heres the date generator: t1 = datetime.date(2002, 10, 22) t2 = datetime.date.today() while t2 > t1: t1 = t1 + datetime.date.resolution toon = t1.strftime("%Y%m%d") url1 = str(toon) main(url1) and my main(): looks like this (took the "Check if file exists" part out) def main(url): con = httplib.HTTPConnection("www.cad-comic.com") con.request("GET", "/cad/" + url) r1 = con.getresponse() soup = BeautifulSoup(r1) img = soup.findAll("img")[1] nImg = str(img) imgSplit = re.split('"', nImg) imgUrl = imgSplit[1] if imgUrl.startswith('http://www.cad-comic.com/comics/'): MeOpans().retrieve(imgUrl, url + ".jpg") print "Downloaded " + url "MeOpans()" looks like this: class MeOpans(urllib.URLopener): version = 'Mozilla/5.0 (Windows; U; Windows NT 5.1; it; rv:1.8.1.11) Gecko/20071127 Firefox/2.0.0.11'
