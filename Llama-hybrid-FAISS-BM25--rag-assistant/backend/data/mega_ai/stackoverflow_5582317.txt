[site]: stackoverflow
[post_id]: 5582317
[parent_id]: 5579783
[tags]: 
To use the delegate, you need to initialize the connection like this: NSMutableUrlRequest request = new NSMutableUrlRequest(new NSUrl(value),NSUrlRequestCachePolicy.ReloadRevalidatingCacheData,20); req["User-Agent"]=_UserAgent; var mydelegate = new EscozUrlDelegate("picture",(result)=> { Console.WriteLine(result); }); new NSUrlConnection(request, mydelegate, true); Let me know if it doesn't work.
