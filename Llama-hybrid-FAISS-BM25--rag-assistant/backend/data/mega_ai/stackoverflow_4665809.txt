[site]: stackoverflow
[post_id]: 4665809
[parent_id]: 
[tags]: 
Image deminsion issue with jquery resizable

The below code creates an image when the button is clicked on. You can see the image created in firebug DOM. But height and width is 0px. Image dimensions is not being detected. test var spanid = 1; $("button").click(function() { var elm = $(' '); elm.resizable().parent().draggable({ cursor: "move" }).appendTo('p'); spanid++; });
