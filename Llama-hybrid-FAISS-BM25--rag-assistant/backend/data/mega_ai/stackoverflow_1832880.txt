[site]: stackoverflow
[post_id]: 1832880
[parent_id]: 1832859
[tags]: 
XmlHTTPRequest uses the same Request-Architecture as the Browser, so there would be no difference. The only thing that might differ when using a Flash-Application is the User-Agent submitted in the header of the HTTP-Request. In the above scenario I assume that a simple GET-Request is made. Best wishes, Fabian
