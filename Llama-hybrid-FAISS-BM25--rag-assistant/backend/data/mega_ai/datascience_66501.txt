[site]: datascience
[post_id]: 66501
[parent_id]: 64750
[tags]: 
There is no rule of thumb for choosing best kernel. You should try simpler methods first (linear SVM) and go for more complex ones (RBF Kernel) if you don't get good results. As @Syenix said, you should try different SVM parameters, which can be done by the grid search method.
