[site]: stackoverflow
[post_id]: 3402877
[parent_id]: 
[tags]: 
Calling a function defined inside jQuery ready from outside of it

My aspx page:- $(document).ready(function () { //lots of other code here.... function showMessage(){ $("#msgDiv").dialog({ modal: true, buttons: { Ok: function() { $(this).dialog('close'); } }, resizable: true, show: "explode", position: "center", closeOnEscape: true, draggable: false }); } }); Another aspx pop up page which is triggered from the above page window.opener.document.getElementById("msgDiv").innerHTML = ; //works very well for me. window.opener.document.showMessage(); // I am unable to access it like this? window.close(); Basically I want to call showMessage() from the pop up window. I also have other logics to perform in both pages.
