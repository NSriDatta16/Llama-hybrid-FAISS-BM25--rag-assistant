[site]: stackoverflow
[post_id]: 1480228
[parent_id]: 
[tags]: 
jQuery ajax issue

I've been at this bit of code for a while, but am not sure what I'm doing wrong. Basically I want to create a loop that prints out all items in a cart. If I take the loop out, it prints the item just added, but when I add the loop in, it breaks. I'm not too familiar with jQuery/JSAON, so if anyone could point out where I might be going wrong, that would be great. (edit - full js files) var ShoppCartAjaxHandler = function (cart) { (function($) { var display = $('#shopp-cart-ajax'); display.empty().hide(); // clear any previous additions var item = $(' ').appendTo(display); $.each(cart.items, function() { $(" ").html(" "+this.quantity+" "+this.name).appendTo(item); }); //$(' ').html(' '+cart.Item.quantity+' x '+cart.Item.name).appendTo(item); $(' ').html(' Subotal '+asMoney(cart.Totals.subtotal)).appendTo(item); $(' ').html(' Shipping '+asMoney(cart.Totals.shipping)).appendTo(item); $(' ').html(' Total '+asMoney(cart.Totals.total)).appendTo(item); if ($('#shopp-cart-items').length > 0) { $('#shopp-cart-items').html(cart.Totals.quantity); $('#shopp-cart-total').html(asMoney(cart.Totals.total)); } else { $('.widget_shoppcartwidget p.status').html(' '+cart.Totals.quantity+' x '+cart.Item.name+' Subtotal '+asMoney(cart.Totals.subtotal)+' Shipping '+asMoney(cart.Totals.shipping)+' Total '+asMoney(cart.Totals.total)+' '); } display.slideDown(); })(jQuery) }
