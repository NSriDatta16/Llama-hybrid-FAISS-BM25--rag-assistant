[site]: stackoverflow
[post_id]: 3545454
[parent_id]: 3545066
[tags]: 
replace the glsl version by : #version 120 but in 1.2 the keyword in and out were not defined yet, it was attribute and varying. smooth varying vec4 vVaryingColor; You'll probably need to make similar changes in the fragment shader For vVertex and vNormal, these are custom names, which means they have been bound in the C++ code. The easiest way to work around this is to rename them gl_Vertex and gl_Normal
