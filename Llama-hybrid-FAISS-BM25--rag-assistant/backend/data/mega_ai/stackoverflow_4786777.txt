[site]: stackoverflow
[post_id]: 4786777
[parent_id]: 4784711
[tags]: 
Hey Robert, not sure if this is what is causing it, but in Example 1 it looks like you are trying to use a string literal on the JPG name, but if so, the @ symbol needs to be outside the quotes... @"good morning" // a string literal
