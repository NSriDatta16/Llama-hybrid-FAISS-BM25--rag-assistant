[site]: stackoverflow
[post_id]: 4858988
[parent_id]: 4858542
[tags]: 
Make it resizable when it's loaded: $(elm).load(function(){$(this).resizable();}).appendTo('#container').parent().draggable(); If you do it before it's not clear what size the image will have, so the initial size of the resizable is set to 0/0
