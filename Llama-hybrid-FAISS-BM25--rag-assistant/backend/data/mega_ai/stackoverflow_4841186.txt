[site]: stackoverflow
[post_id]: 4841186
[parent_id]: 4841160
[tags]: 
// first we grab the clone. We also want to change the 's id, as it's // not valid to have two elements within a page with the same ID. $('#ex').clone().attr({ id: 'newpid' // paragraph's ID // next, we alter the input inside it (change ID, clear value). }).find('input').attr({ value: '', id: 'newid' // 's ID // call end to stop the ".find", then add it to the #def element }).end().appendTo('#def'); Working Example: http://www.jsfiddle.net/F4rRQ/1/
