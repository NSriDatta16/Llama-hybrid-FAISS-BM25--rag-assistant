[site]: stackoverflow
[post_id]: 3294981
[parent_id]: 
[tags]: 
XHTML Markup Validation

i have my site [here][link removed] when i run it through validator , i get several errors regarding the: if i keep the and drop the then the code compiles fine. does this mean i no longer have to use the tag when im embedding multimedia in my page? thanks for your responses.
