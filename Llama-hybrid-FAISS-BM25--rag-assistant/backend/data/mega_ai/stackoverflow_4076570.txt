[site]: stackoverflow
[post_id]: 4076570
[parent_id]: 
[tags]: 
Is there any way to use a JS variable as input instead of jquery selector "#id"?

I want to know that, can we able to give a variable instead of #anyId inside a selector. Actually i am trying this code, Click me! $("p").live("click", function() { $(this).after(" Another paragraph! "); var number = this.id.match(/^abcd(\d+)$/)[1]; var iD = this.id; alert(number); alert(iD); var div = "#"+"aDiv"+number; alert(div) $(div).remove(); }); This is the fiddle link for the above code. Any suggestions about where i am doing it wrong!!!! Thanks!
