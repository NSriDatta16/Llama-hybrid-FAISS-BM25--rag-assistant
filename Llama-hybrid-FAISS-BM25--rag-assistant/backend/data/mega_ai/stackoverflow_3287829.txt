[site]: stackoverflow
[post_id]: 3287829
[parent_id]: 3287801
[tags]: 
Just make them both store pointers an explicitly delete the objects when you don't need them. std::vector storage; std::map map; for (int i=0; i getKey(), a)) } // map contains only valid pointers to the 'correct' elements of storage
