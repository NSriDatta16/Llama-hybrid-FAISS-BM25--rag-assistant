[site]: stackoverflow
[post_id]: 5365213
[parent_id]: 
[tags]: 
Get innerText from XElement

Hey guys, I have an XML file I need to parse, but only for the text, not the HTML. Here's an example of a node: Classy &amp; elegant purse hooks Choose from various styles Stable and reliable Makes a great gift! ]]> As you can see, the CDATA has HTML code in it, so when I do element.Value I also get the tags. Is there any simple way to get just the text? Thank you!
