[site]: crossvalidated
[post_id]: 508863
[parent_id]: 
[tags]: 
generator learns generating features in GAN

In Image generation using GAN, what if image features are given as input instead of Image to the discriminator? will generator generates features? Any kind of reference is helpfull
