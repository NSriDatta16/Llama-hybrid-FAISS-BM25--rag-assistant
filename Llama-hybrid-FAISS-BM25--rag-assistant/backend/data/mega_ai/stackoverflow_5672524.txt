[site]: stackoverflow
[post_id]: 5672524
[parent_id]: 5672490
[tags]: 
The portion of the URL after the # is the fragment and is never sent to the server. It is handled by the user agent only. You'll need to use JavaScript to interact with it. Though, in your example, you'll need to escape it so its meaning is taken literally. As others have pointed out, urlencode() is perfect for that.
