[site]: stackoverflow
[post_id]: 1018524
[parent_id]: 1018407
[tags]: 
Not elegant, but efficient - make sure indexes are sorted ... ICollection selected = new Collection (); var indexesIndex = 0; var collectionIndex = 0; foreach( var item in collection ) { if( indexes[indexesIndex] != collectionIndex++ ) { continue; } selected.Add( item ); if( ++indexesIndex == indexes.Count ) { break; } }
