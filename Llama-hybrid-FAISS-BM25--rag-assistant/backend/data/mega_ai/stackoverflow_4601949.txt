[site]: stackoverflow
[post_id]: 4601949
[parent_id]: 4601749
[tags]: 
Rather than a 'comparison' based storage/retrieval ( which will always be O(log(n)) ), You need to work on 'radix' based storage/retrieval . In other words .. extract nibbles from the uint32, and make a trie ..
