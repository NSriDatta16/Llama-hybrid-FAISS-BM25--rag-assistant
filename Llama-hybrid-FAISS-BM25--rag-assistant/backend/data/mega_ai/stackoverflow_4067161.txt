[site]: stackoverflow
[post_id]: 4067161
[parent_id]: 4067148
[tags]: 
You need to allocate some storage rather than use the temp temporary variable. At present you are re-using the temp variable each time round the loop. Use something like std_out_lines[i] = malloc(BUFFER_LEN); // check for null here too, malloc can fail.
