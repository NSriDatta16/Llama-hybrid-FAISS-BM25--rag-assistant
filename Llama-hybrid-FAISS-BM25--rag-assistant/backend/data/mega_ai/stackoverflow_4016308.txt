[site]: stackoverflow
[post_id]: 4016308
[parent_id]: 4015708
[tags]: 
You also have logics errors in else if conditions. There should be: if average >= 90, else if average >= 80, else if average >= 70, else if average >= 60, else. Replace public GradeReport() with public static void main(String[] args) . Try to put all the code in one method - main. After the method will do what you expect it to do decompose (refactor) it.
