[site]: stackoverflow
[post_id]: 5210395
[parent_id]: 
[tags]: 
Array value store to each treenode and then access them

TreeNode ns = new TreeNode("Main"); public void selectInput(string inp) { // values and input array length are same string[] values={"asd","qwe","qwer"}; string[] store_arr = inp.Split('|'); for(int i=0;i I want to store each treenode value separately and then i want to access these values by drag drop on listbox
