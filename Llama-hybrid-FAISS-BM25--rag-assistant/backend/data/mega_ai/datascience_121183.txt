[site]: datascience
[post_id]: 121183
[parent_id]: 120321
[tags]: 
A typical solution is to chunk the long text into smaller fragments, retrieve relevant pieces based on the search query, then send them via an API call. Here's a project available which can handle PDFs, txt and doc files, as well as web pages. I believe this implementation is straightforward to follow, if you're interested.
