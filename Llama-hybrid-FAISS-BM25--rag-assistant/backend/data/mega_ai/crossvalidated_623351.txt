[site]: crossvalidated
[post_id]: 623351
[parent_id]: 623350
[tags]: 
People have suggested using lasso regression to select trees from a random forest (eg here ) An alternative would be to just use boosted trees. Boosting isn't quite the same because each tree is grown to contain new information -- this also means that boosting is harder to parallelise.
