[site]: stackoverflow
[post_id]: 3838882
[parent_id]: 3838864
[tags]: 
Comparisons of NSStrings to @"" using equality are effectively pointer comparisons. Thus, you can't just use someString != @"" to compare against the empty string. Instead, use isEqualToString: or test against a zero length . Edit: Just for code readability, if you're using Objective C 2.0, you can replace your for loop and object retrieval by index with a foreach... for (NSArray *logbookItem in logBookItems) { NSLog(NSLog(@"%@, %@, %@", [[logBookItem objectAtIndex:1] class], ...
