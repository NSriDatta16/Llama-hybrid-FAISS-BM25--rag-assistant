[site]: stackoverflow
[post_id]: 3782582
[parent_id]: 3782545
[tags]: 
Short answer: yes, the static field is global to the AppDomain, so doing this for one user will step on the data for another user. You probably want to look into using Session storage instead, which is scoped per-user, e.g. var theExam = Session["exam"] as Exam;
