[site]: stackoverflow
[post_id]: 1666159
[parent_id]: 1666146
[tags]: 
Make sure that your PHP can output the f ollowing HTML code : $(document).ready(function(){ $('#example').calendar(); }); Here's a sample how you can do it, by using json_encode: $(document).ready(function() { $('#calendar').fullCalendar({ draggable: true, events: "json_events.php", eventDrop: function(event, delta) { alert(event.title + ' was moved ' + delta + ' days\n' + '(should probably update your database)'); }, loading: function(bool) { if (bool) $('#loading').show(); else $('#loading').hide(); } }); }); And here's the PHP code: 1, 'title' => "Event1", 'start' => "$year-$month-10", 'url' => "http://yahoo.com/" ), array( 'id' => 2, 'title' => "Event2", 'start' => "$year-$month-20", 'end' => "$year-$month-22", 'url' => "http://yahoo.com/" ) )); ?>
