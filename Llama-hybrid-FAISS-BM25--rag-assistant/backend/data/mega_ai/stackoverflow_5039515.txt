[site]: stackoverflow
[post_id]: 5039515
[parent_id]: 5039379
[tags]: 
That won't work. You can make an object though, storing the 'store'+i as a property. var storage = {},i=0; while(++i 'null' In a browser, you can also use the window namespace to declare your variables (avoiding the use of eval ): var i=0; while(++i 'null'
