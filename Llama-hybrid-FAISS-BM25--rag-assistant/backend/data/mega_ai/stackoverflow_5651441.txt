[site]: stackoverflow
[post_id]: 5651441
[parent_id]: 5651412
[tags]: 
It sounds like you're asking how to write a method to wrap that code. If so I think you want the following (assuming the type of agent is Agent ) void TheMethod(Agent& agent) { if (agent.GetWorldState().GetPlayMode() == PM_Before_Kick_Off) { agent.Move(Vector(-1, -1)); } }
