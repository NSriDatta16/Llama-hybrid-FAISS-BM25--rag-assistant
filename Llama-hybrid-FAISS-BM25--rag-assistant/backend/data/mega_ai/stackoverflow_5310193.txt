[site]: stackoverflow
[post_id]: 5310193
[parent_id]: 5308891
[tags]: 
POSIX::strftime is a thin wrapper around the real strftime C function call in time.h which uses the current locale. Perl doesn't go through the effort to make it conform. DateTime has a strftime equivalent implemented in Perl that will conform to Perl's locale pragma.
