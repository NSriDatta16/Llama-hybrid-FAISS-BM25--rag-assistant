[site]: stackoverflow
[post_id]: 722870
[parent_id]: 722801
[tags]: 
#include #include #include int main() { /* output storage */ std::string id; double d; /* convert input to a STL string */ std::string s("name:454"); size_t off = std::string::npos; /* smart replace: parsing is easier with a space */ if ((off = s.find(':')) != std::string::npos) { // error check: all or none s = s.replace(off, 1, 1, ' '); std::istringstream iss(s); iss >> id >> d; std::cout Though, I'd just write my own parser or use the C scanner function for speed.
