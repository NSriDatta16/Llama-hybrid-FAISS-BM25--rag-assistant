[site]: stackoverflow
[post_id]: 1492732
[parent_id]: 
[tags]: 
ASP.Net uploading an image for a users avatar

I dragged a file upload control to my Form, and I want to save the "image" the user chooses to an object[] Array so I can then save it. I only need to know how to "grab" the image the user selects and save it as a byte[] array.Thanks
