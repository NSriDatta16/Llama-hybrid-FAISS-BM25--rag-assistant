[site]: stackoverflow
[post_id]: 2983392
[parent_id]: 2983213
[tags]: 
for line in $(tcpdump -n -r $pcap) do command done This isn't exactly doing what I need. But it is close. And Shell compatible. I'm creating HTML tables from the tcpdump output. The for loop makes a new row for each word. It should make a new row for each line (\n ending). Paste bin script01.sh .
