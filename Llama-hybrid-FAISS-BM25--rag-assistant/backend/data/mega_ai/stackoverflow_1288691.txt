[site]: stackoverflow
[post_id]: 1288691
[parent_id]: 1288526
[tags]: 
Maybe you can do something like: var wattList = (from t in _table where t.Id == id && t.Time >= startTime && t.Time (int) ((x.Time - startTime).TotalSeconds / intervalInSeconds)) .Select(grp => grp.Average(x => x.Value));
