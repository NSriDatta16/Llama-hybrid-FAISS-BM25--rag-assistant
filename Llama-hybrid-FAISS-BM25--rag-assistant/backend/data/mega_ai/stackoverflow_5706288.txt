[site]: stackoverflow
[post_id]: 5706288
[parent_id]: 5706214
[tags]: 
You'll be better off using var updateLocalStorage; $(document).ready( function() { var localDb = null; updateLocalStorage = function () { // localDb is set here } } ); and then use it in your function later.
