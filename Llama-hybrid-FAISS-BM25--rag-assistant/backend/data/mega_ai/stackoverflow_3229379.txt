[site]: stackoverflow
[post_id]: 3229379
[parent_id]: 3228999
[tags]: 
if you have somthing like char buf[10]; long var; strcpy(buf, "ganz viel text"); you may or may not get an segmentation violation or strange behaviour with variable "var". if you put more debug text into your code, the linker may reallocate the variables, or the compiler may do other code optimization, and reallocation space allocation in memory.
