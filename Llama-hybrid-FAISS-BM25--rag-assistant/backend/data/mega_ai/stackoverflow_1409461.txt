[site]: stackoverflow
[post_id]: 1409461
[parent_id]: 1409449
[tags]: 
The reason it only takes the first one is because you're missing the g flag. Try this: function executeJs(html) { var scriptFragment = ' (.+?) '; var re = new RegExp(scriptFragment, 'gim'), match; while ((match = re.exec(html)) != null) { eval(match[2]); } } executeJs(' alert("hello") abc alert("world") ');
