[site]: stackoverflow
[post_id]: 3743795
[parent_id]: 3743722
[tags]: 
Do I need to look at using IsolatedStorage for this instead? yep, every app (except OS apps) needs to use IsolatedStorage to store data in physical memory OR you can use could service to sore the data. IsolatedStorage example
