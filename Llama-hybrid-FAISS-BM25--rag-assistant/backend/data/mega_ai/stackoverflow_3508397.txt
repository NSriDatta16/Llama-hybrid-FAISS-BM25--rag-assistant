[site]: stackoverflow
[post_id]: 3508397
[parent_id]: 3508388
[tags]: 
You can use indexOf to get the index in the array which contains the element you're looking for. So you could rewrite your if statement like this: if (userAgent.indexOf("msie7") > -1) {...} Note that IE (at least IE6) doesn't support indexOf for arrays (it does work on strings though), but you could always write your own version: if (typeof Array.prototype.indexOf == 'undefined') Array.prototype.indexOf = function(e) { for (var i = 0; i
