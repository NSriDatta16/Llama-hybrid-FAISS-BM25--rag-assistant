[site]: stackoverflow
[post_id]: 3023476
[parent_id]: 3023384
[tags]: 
Another way, more elegant: use ids (numbers) and then just make a loop, to check if the post number matches the current number, you can do it with an array: // 0 ,1, 2 $ids = ('Medmo','Paris','Imo'); $selected = $_POST['mpage']; for($i=0;$i '.$ids[$i].'.com '; }
