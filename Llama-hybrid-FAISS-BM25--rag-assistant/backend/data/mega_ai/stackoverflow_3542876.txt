[site]: stackoverflow
[post_id]: 3542876
[parent_id]: 3542856
[tags]: 
If you want to know how many elements are in the list then use the Count property. int numElements = list.Count; On the other hand if you want to know how many elements the backing storage of the List can currently handle then then use the Capacity property. int size = list.Capacity;
