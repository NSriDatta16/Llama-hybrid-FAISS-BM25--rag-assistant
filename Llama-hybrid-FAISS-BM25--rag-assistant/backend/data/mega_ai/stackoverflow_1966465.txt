[site]: stackoverflow
[post_id]: 1966465
[parent_id]: 1966449
[tags]: 
If you are not aware that your mail server will handle the number, you can split up the work into smaller groups of addresses and add them to a temporary storage (ie. MySQL table). Then on each page request, you can get some addresses, send the messages and remove them from the temporary table.
