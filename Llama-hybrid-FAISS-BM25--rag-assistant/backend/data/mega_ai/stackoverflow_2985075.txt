[site]: stackoverflow
[post_id]: 2985075
[parent_id]: 2981850
[tags]: 
The old samples are here . But here are the caveats: They are old and have not been updated in a while There are a few bugs (some the result of underlying changes in the Win Azure since then, other simply bugs) There are no unit tests, so you will need to either write your own or live with that. But, it is a starting point if you are interested in using Win Azure storage (tables + blobs).
