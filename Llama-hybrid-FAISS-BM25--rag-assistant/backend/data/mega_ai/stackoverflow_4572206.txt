[site]: stackoverflow
[post_id]: 4572206
[parent_id]: 4569306
[tags]: 
I'm using the Facebook API, too. And your code is wrong, the right way is: $info = $facebook->api( '/'.$memberfb.'/groups', 'GET', array( 'access_token=' => $membertoken ) ); $ct = count( $info['data'] );
