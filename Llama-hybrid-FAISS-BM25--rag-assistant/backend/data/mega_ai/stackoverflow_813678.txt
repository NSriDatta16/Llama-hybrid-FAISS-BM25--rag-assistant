[site]: stackoverflow
[post_id]: 813678
[parent_id]: 
[tags]: 
Java: Converting arrays of arrays to collections of collections and vice versa

Please suggest some elegant way to convert arrays of arrays to collections of collections and vice versa in Java. I suppose there's no convenience methods in the Java API, right? public static T[][] nestedCollectionsToNestedArrays( Collection > source){ // ... ? } public static Collection > nestedArraysToNestedCollections( T[][] source){ // ... ? } Additional question: What's with nested arrays of primitive types ? Do I have to declare methods for each primitive type, because of forbidden generic array creation ?
