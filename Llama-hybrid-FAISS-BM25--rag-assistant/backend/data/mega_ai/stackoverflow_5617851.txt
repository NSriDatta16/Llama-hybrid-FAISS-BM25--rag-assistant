[site]: stackoverflow
[post_id]: 5617851
[parent_id]: 
[tags]: 
Returning a value in Java

How do I write a method which returns a primitive datatype value when inside the method it has an if statement which returns a different datatype depending on the result? int minimum = 5; int students = 4; int studentGrades = 100; public double getAverage(){ if(students >= minimum){ return studentGrades / students; } else { System.out.println(null, "Sorry, you don't have enough students."); return false; } }
