[site]: stackoverflow
[post_id]: 4782151
[parent_id]: 4782051
[tags]: 
it = csvMap.find(commandList.at(lineCount)); if (it == csvMap.end()) { cout second.size() Either when command isn't found or command is the last No, the end iterator is not an item in the container. string c = (*it).first; Since this is after the iterator is the end iterator, you have undefined behavior when dereferencing it.
