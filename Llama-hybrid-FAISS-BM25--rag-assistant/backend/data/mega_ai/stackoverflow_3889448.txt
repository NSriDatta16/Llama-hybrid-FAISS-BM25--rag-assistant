[site]: stackoverflow
[post_id]: 3889448
[parent_id]: 3889349
[tags]: 
It is mostly answered that regex's in most cases are not suitable for HTML, so you should provide the language in which you plan to implement this. However a regex like this will replace the heading: // replace with The regex for the paragraph tag is analogous (replace 'h2' with 'p' and 'red' with 'green').
