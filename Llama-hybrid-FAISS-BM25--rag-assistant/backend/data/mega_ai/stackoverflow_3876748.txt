[site]: stackoverflow
[post_id]: 3876748
[parent_id]: 
[tags]: 
How to validate the Two Date fields on my view using jquery or javascript

I have two fileds in my View one is Effective data and one is Cancel date Effective date: Model.SelectedAccountGroup.GroupEffectiveDate, new { @class="datepicker"} )%> x.SelectedAccountGroup.GroupEffectiveDate )%> Cancel date: Model.SelectedAccountGroup.GroupCancelDate, new { @class = "datepicker" } )%> x.SelectedAccountGroup.GroupCancelDate )%> On Save button I should not allow user to Enter the Cancel date Before Effective data? can any body tell me how to do this?
