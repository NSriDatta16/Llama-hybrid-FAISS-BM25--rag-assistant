[site]: stackoverflow
[post_id]: 5029528
[parent_id]: 
[tags]: 
Magento webservice api expose blocks

We would like to expose some Magento blocks through the webservice API like Magebridge does. Actually Magebridge does exactly what we want but this is too tightly integrated with Joomla to re-use for another CMS. Thanks! Jasper
