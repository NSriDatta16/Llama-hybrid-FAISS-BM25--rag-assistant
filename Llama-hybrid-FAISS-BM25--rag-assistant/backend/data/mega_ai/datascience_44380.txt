[site]: datascience
[post_id]: 44380
[parent_id]: 44354
[tags]: 
I think you can use a regression setup, e.g. bad=0, good=0.5, very good = 1 for labels, and then postprocess output of XGBoost, such as pred_value prediction_label=bad, pred_value >= 0.25 and pred_value prediction_label=good and so on.
