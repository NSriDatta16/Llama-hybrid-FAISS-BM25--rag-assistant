[site]: stackoverflow
[post_id]: 2270752
[parent_id]: 
[tags]: 
jquery div contents real width

i have a div tag above script tag is third party url that may returns either image or flash object or any other of any size.. how to determine dynamic width and height occupied by div tag using jquery ? i tried $("#openxfrag").width() ,but it always return the same width that is defined in css
