[site]: stackoverflow
[post_id]: 5182075
[parent_id]: 5180772
[tags]: 
This should work in all Magento versions: $products = Mage::getModel('catalog/product')->getCollection(); //Magento does not load all attributes by default //Add as many as you like $products->addAttributeToSelect('name'); foreach($products as $product) { //do something }
