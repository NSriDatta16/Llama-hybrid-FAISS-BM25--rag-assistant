[site]: crossvalidated
[post_id]: 158854
[parent_id]: 
[tags]: 
What exactly are some fundamental differences between probit model and logistic regression

It seems that both these refer to cases where the regressed (dependent) variable can only take certain values, as opposed to a linear regression. So what is the difference between probit and logistic regression?
