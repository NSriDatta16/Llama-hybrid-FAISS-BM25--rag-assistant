[site]: stackoverflow
[post_id]: 4614986
[parent_id]: 
[tags]: 
Which of these storage methods is fastest?

1 million users, each only needing their OWN data set of 100 strings (approx 255 characters each). Each accesses once each day, doing 2 reads (all data) and 1 write per day (1 string) mysql amazon EC2 + simpleDB amazon EC2 + flat file system (text files + php) amazon EC2 + sqllite - removed as it can't handle it. Which is fastest?
