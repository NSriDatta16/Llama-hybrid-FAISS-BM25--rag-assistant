[site]: stackoverflow
[post_id]: 622090
[parent_id]: 622058
[tags]: 
Can you post the relevant XML fragment? We're looking at your code that parses it, but if we had the XML itself, it'd be easier to cons up an Xpath query. Most useful would be the bit that has elements like etc
