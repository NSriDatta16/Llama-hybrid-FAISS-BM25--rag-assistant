[site]: stackoverflow
[post_id]: 3167109
[parent_id]: 
[tags]: 
exceptions from boost::iostreams::copy()

In the below code, I have a corrupt "hello.bz2" which has stray characters beyond the EOF. Is there a way to make the boost::iostreams::copy() call to throw ? #include #include #include #include #include int main() { using namespace std; using namespace boost::iostreams; ifstream file("hello.bz2", ios_base::in | ios_base::binary); filtering_streambuf in; in.push(bzip2_decompressor()); in.push(file); boost::iostreams::copy(in, cout); } EDIT: Please ignore the line that is so far attracted most attention; the EOF. Please assume working with a corrupted bzip2 file. I used "EOF" suggesting the error I got when I run bzcat on the file bzcat hello.bz2 hello world bzcat: hello.bz2: trailing garbage after EOF ignored
