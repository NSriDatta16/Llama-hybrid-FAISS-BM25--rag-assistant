[site]: crossvalidated
[post_id]: 19024
[parent_id]: 
[tags]: 
What is the P(A|C) if we know B depends on A and C depends on B?

Given a Bayesian network that looks like the following: A->B->C How do we compute P(A|C)? My initial guess would be: P(A|C) = P(A|B) * P(B|C) + P(A|not B) * P(not B|C)
