[site]: stackoverflow
[post_id]: 3523088
[parent_id]: 3522978
[tags]: 
Have you considered adding a method to bundle A's API that loads and returns the resource? Many might consider this a better design as it allows the name or means of storage of the resource to change without breaking clients of bundle A.
