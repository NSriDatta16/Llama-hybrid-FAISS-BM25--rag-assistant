[site]: stackoverflow
[post_id]: 4050398
[parent_id]: 4050361
[tags]: 
If you're using just div s, using a className would be convenient and valid. Like this: Latest Oldest ... function divClickHandler(event) { var div = event.target; if (div.className == 'latest') // load latest else if (div.className == 'oldest') // load oldest } If you're using html5, an even better solution would be using custom data- attributes ; like this: Latest Oldest The most elegant solution would be using a tags instead of the div s and intercept their clicks: Latest Oldest ... function aClickHandler(event) { var a = event.target; // do your ajax call using a.href return false; }
