[site]: stackoverflow
[post_id]: 4587678
[parent_id]: 4587632
[tags]: 
else if(e.type == 'click') { $(window).unbind('mousemove') } But really you should name the event so you only unbind the appropriate event listener. Bind : $(window).bind('mousemove.dragging', function(){}); Unbind : $(window).unbind('mousemove.dragging', function(){});
