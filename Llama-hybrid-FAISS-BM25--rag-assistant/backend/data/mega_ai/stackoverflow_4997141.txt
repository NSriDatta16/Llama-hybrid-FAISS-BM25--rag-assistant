[site]: stackoverflow
[post_id]: 4997141
[parent_id]: 
[tags]: 
jQuery Draggable - Getting out of containment - Chrome Bug

I realized that I there is bug with jQuery Draggable in Chrome, that doesn't exist in Firefox and Internet Explorer. I have div #drag-container /450x80/ and 3 draggable objects inside. /150x0/ and when I move them to lowest point ,let them and drag again LOWER they get position 0,80 + 14 = 0,94 px. I let them and drag and they get 14 px lower again. Is there fix ? Working demo: http://jsfiddle.net/ucmqq/ Here is code: .draggable { width: 150px; height: 0px; cursor:move; } use_colors != '0'): ?> #drag-container { width: 450px; height:80px; background-color: background?>; } #drag-container { width: 450px; height:80px; background:url(/backgrounds/ image?>); } $(function() { $( "#draggable" ).draggable({ cursor: "move",containment: 'parent', scroll: false, stop: function() { document.settings.np_x.value = $(this).css('left'); document.settings.np_y.value = $(this).css('top'); } }); $( "#draggable2" ).draggable({ cursor: "move",containment: 'parent', scroll: false , stop: function() { document.settings.artist_x.value = $(this).css('left'); document.settings.artist_y.value = $(this).css('top'); } }); $( "#draggable3" ).draggable({ cursor: "move",containment: 'parent', scroll: false , stop: function() { document.settings.other_x.value = $(this).css('left'); document.settings.other_y.value = $(this).css('top'); } }); }); .::[ NowPlaying SIGnature ]::. Artist - title Album: (Year) Genre:
