[site]: stackoverflow
[post_id]: 5663817
[parent_id]: 5661848
[tags]: 
Embedding the parameter inside a perl script can lead to trouble if there are characters special to perl in the parameter value. Best to do something like crypt_pass() { echo $(perl -e 'print crypt($ARGV[0], "aa")' "$1") }
