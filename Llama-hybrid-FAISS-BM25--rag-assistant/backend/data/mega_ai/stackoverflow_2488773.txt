[site]: stackoverflow
[post_id]: 2488773
[parent_id]: 2488725
[tags]: 
Since you tagged the question with it, here's a jQuery solution. $("body").append("text"); Remember that the parameter can also be a DOM element. So you can do this : var p = $(" ").text("a paragraph"); $("body").append(p);
