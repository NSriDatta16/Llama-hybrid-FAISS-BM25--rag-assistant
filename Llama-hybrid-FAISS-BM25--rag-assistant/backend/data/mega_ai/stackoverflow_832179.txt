[site]: stackoverflow
[post_id]: 832179
[parent_id]: 832116
[tags]: 
This regex will find all first paragraphs after a h1, h2, or h3. If you want only the very first paragraph on the page, just keep the first match. (? \s*? )([\s\S]*?)(?= ) You will probably need to adjust the matches for the tags to account for attributes.
