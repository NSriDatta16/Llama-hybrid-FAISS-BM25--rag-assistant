[site]: stackoverflow
[post_id]: 2446615
[parent_id]: 2433773
[tags]: 
Okay so I found this plugin: http://ludo.cubicphuse.nl/jquery-plugins/treeTable/doc/index.html#example-1 This uses tables and allows it to be nested. Now I just need to sort it and post submit the table elements with a parent id so i can save it to the database. I hope this helps someone else. so this is what i came up with. I don't want more then 2 levels ( only parent and childs ) so i changed it to let parents only accept childs, and not made parents draggable. $(document).ready(function() { $("#dragndrop").treeTable(); // Drag & Drop Code $("#dragndrop .child").draggable({ helper: "clone", opacity: .75, refreshPositions: true, // Performance? revert: "invalid", revertDuration: 300, scroll: true } ); $("#dragndrop .parent").each(function() { $($(this).parents("tr")[0]).droppable({ accept: ".child", drop: function(e, ui) { $($(ui.draggable).parents("tr")[0]).appendBranchTo(this); setNewParent($($(ui.draggable).parents("tr")[0]).attr("id"), $(this).attr("id")); }, hoverClass: "accept", over: function(e, ui) { if(this.id != $(ui.draggable.parents("tr")[0]).id && !$(this).is(".expanded")) { $(this).expand(); } } }); }); function setNewParent(child, parent) { // do ajax call here alert(child); alert(parent); } }); Title Size Kind CHANGELOG 4 KB Parent images -- child doc -- Parent bg-table-thead.png 52 KB child folder.png 4 KB child To make the whole table sortable you can use jQuery's .sortable() function, but I left that out of here to make it more clear.
