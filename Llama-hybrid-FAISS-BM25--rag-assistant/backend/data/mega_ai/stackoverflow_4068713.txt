[site]: stackoverflow
[post_id]: 4068713
[parent_id]: 4068640
[tags]: 
Some things to check. your HTTP headers (check out, say, LiveHttpHeaders for Firefox) does the HTML code render in the broswer if loaded as a standalone file ? If the answer to 2) is no, then I would recommend chopping the HTML up and searching for the offending fragments that render your HTML broken.
