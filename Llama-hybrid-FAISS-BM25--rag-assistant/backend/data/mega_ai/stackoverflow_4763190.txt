[site]: stackoverflow
[post_id]: 4763190
[parent_id]: 
[tags]: 
.NET 4.0 MSBuild.exe not recognizing web deploy arguments

I have .NET 4.0 installed on a build server, along with Web Deploy 1.1 I've opened up a command window to test the web deployment, but whenever I run it, the build succeeds, and no publish happens. I try the exact same thing on my development machine, and the deploy fires, so I'm left wondering, why does msbuild not start the publish on my build server? Here's the command I'm runnning C:\WINDOWS\Microsoft.NET\Framework\v4.0.30319\msbuild.exe NerdDinner.sln /P:Configuration=Release /P:DeployOnBuild=True /P:DeployTarget=MSDeployPublish /P:MsDeployServiceUrl=http://someserver/MsDeployAgentService /P:AllowUntrustedCertificate=True /P:MSDeployPublishMethod=RemoteAgent /P:CreatePackageOnPublish=True /P:UserName=user /P:Password=password
