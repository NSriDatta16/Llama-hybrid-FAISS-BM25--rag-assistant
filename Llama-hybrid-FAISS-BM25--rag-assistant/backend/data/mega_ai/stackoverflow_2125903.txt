[site]: stackoverflow
[post_id]: 2125903
[parent_id]: 2125402
[tags]: 
What version of Jquery are you using? If you're using the new one (1.4), you should be able to leverage the new $.delay() function. Then all you'd have to change is one line within navBar_open() to: ddmenuitem = $(this).find('ul').delay(timeout).css('visibility', 'visible');
