[site]: crossvalidated
[post_id]: 500506
[parent_id]: 500489
[tags]: 
SVMs 's separate classes by a hyperplane, which is a form of clustering. Given a single example, each digit will likely reside somewhere near the cluster centroid for the class. Therefore, as mentioned in the comments, a single example should still learn better than random.
