[site]: crossvalidated
[post_id]: 297791
[parent_id]: 297790
[tags]: 
The Sigmoid is used to compress the data between 0 and 1 so you can use a percentage threshold. It also causes saturation of extreme values. If you want to use just a linear threshold it is possible but this won't be a logistic regression but a threshold model.
