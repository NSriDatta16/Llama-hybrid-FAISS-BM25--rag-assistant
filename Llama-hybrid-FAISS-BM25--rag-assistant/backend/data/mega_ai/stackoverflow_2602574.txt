[site]: stackoverflow
[post_id]: 2602574
[parent_id]: 2602543
[tags]: 
You may be corrupting memory if (*recordData).size() . Consider eliminating getNumFields and relying on (*recordData).size() to store that information. Or, don't preset the size of the vector at all and use push_back . Avoid the pointers, too, with references. int CCSVManager::ReadCSVLine ( fstream &fsInputFile, vector &recordData ) { string s; getline ( fsInputFile, s ); istringstream iss( s ); for ( unsigned int i = 0; i
