[site]: stackoverflow
[post_id]: 5307280
[parent_id]: 
[tags]: 
WordPress - How to pull data (title, thumb) from a specified page?

I've got a list of links being generated by wp_list_pages that are displaying all the children of a certain page. Each of these pages has the usual: title and a paragraph. Instead of loading the page separately, I was planning on setting each link to be ?id=XXX where XXX is the page's slug. From there, I would have a PHP function in the content area pulling the title and copy of a page depending if the slug matched the id=XXX. How would I go about querying a single page with specific arguments so I make sure that the page that loads has the slug that matches the id=XXX? Nav: Link 1 Link 2 Link 3 Content:
