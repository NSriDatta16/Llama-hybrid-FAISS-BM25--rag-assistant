[site]: stackoverflow
[post_id]: 911664
[parent_id]: 911326
[tags]: 
Assuming that the Id column is sequential, here's a simplified query for a table named "MyTable": SELECT b.Id, b.Number, ( SELECT AVG(a.Number) FROM MyTable a WHERE a.id >= (b.Id - 3) AND a.id 3 ) as Average FROM MyTable b;
