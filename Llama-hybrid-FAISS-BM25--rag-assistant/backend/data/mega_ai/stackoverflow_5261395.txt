[site]: stackoverflow
[post_id]: 5261395
[parent_id]: 5261283
[tags]: 
These two lines: boost::uniform_real<> uni(0.0,1.0); boost::variate_generator > uniRng(rng, uni); should be boost::uniform_real<> uni; boost::variate_generator > uniRng; and those two variables have to be initialized in the constructor, as Agent() : uni(0., 1.), uniRng(rng, uni) { rng.seed(static_cast (std::time(0))); }
