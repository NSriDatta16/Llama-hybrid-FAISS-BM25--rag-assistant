[site]: stackoverflow
[post_id]: 5132579
[parent_id]: 
[tags]: 
Why am I getting an access violation exception in a pointer operation?

I'm using C++ Visual Studio .Net 4.0 on Windows 7.0 x64. This happens just on the first loop of the while statement. int main() { char *string = new char[11]; string = "characters\0"; toUppercase(string); return 0; } void toUppercase(char *stringPtr) { while(*stringPtr != '\0') { if(*stringPtr >= 'a' && *stringPtr
