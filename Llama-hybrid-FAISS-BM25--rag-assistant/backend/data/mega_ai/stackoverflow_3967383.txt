[site]: stackoverflow
[post_id]: 3967383
[parent_id]: 3945702
[tags]: 
implement it as an image implement it as an iframe which is blocked via robots.txt implement it as a post load external javascript i recommend not doing it via user agent cloaking as this might be seen from google side as misleading and can lead to a penalty or demotion
