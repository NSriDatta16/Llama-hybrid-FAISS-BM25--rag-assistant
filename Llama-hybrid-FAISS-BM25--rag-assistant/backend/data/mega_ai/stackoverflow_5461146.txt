[site]: stackoverflow
[post_id]: 5461146
[parent_id]: 5461100
[tags]: 
Use jQuery live to keep the event's bound to the new elements. With live it binds the event to all present and future elements where just defining a handler for change will only bind for the current elements (you can also just reattach the events every time you create an element but why do it when live takes care of it for you) $tags.live("change",function(){ /* your stuff here */}); UPDATE Here is the change function and if block: $(document).ready(function() { $tags = $("select[name='tags']"); $tags.live("change",function() { $operands = $(this).parent().find("select[name='operands']"); $values = $(this).parent().find("select[name='values']"); if ($(this).val() == "agent") { $(this).parent().find("select[name='operands'] option").remove(); $(" = ").appendTo($operands); $(" != ").appendTo($operands); $(this).parent().find("select[name='values'] option").remove(); $(" excel ").appendTo($values); $(" msword ").appendTo($values); $(" ppt ").appendTo($values); $(" pdf ").appendTo($values); $(" image ").appendTo($values); $(" txt ").appendTo($values); $(" html ").appendTo($values); $(" csv ").appendTo($values); $(" ooxml ").appendTo($values); $(" flash ").appendTo($values); $(" wmf ").appendTo($values); }
