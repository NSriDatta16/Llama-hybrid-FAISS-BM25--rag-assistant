[site]: crossvalidated
[post_id]: 575813
[parent_id]: 575784
[tags]: 
Even if you don't use regularization, it is highly advised that you normalize your data before inputting to a neural network as it'll significantly affect the gradients. So, yes, you should normalize it.
