[site]: stackoverflow
[post_id]: 2563908
[parent_id]: 2559720
[tags]: 
I fixed this by subclassing UISwitch like so: @interface NamedUISwitch : UISwitch { NSString *name; } It seems elegant (no index arrays required) and the tag property is free to do whatever it wants. I read that you have to be careful with subclassing in Objective-C, though...
