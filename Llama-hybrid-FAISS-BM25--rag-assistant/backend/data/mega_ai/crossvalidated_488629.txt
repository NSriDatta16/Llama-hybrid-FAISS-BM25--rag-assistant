[site]: crossvalidated
[post_id]: 488629
[parent_id]: 488616
[tags]: 
where the variables came from does not really matter, so you treat them as any other predictors and fit a model using all these variables that is predicting your outcome, for example, (regularized) logistic regression.
