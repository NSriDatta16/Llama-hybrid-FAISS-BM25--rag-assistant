[site]: stackoverflow
[post_id]: 1287163
[parent_id]: 1287146
[tags]: 
Okay, how about this: var value = (from t in _table where t.Id == id && t.Time >= intervalStartTime && t.Time I believe that's logically what you want - changing {} to {0}, so making all averages achievable. I don't know if it'll do what you want in terms of SQL though.
