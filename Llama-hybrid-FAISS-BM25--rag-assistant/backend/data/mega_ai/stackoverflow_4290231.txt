[site]: stackoverflow
[post_id]: 4290231
[parent_id]: 4281798
[tags]: 
Here is a full example. It required access to the mx_internal namespace unfortunately but should hold up fine. The reason it had to use mx_internal is to get access to the IUITextField reference. tf.textWidth) { tf.appendText(draggedText[0]); } //Dropped at beginning of field (only happens when it is empty) else if(charIndex == -1 && mouseX
