[site]: stackoverflow
[post_id]: 2768353
[parent_id]: 
[tags]: 
Counting number of searches

I updated my main and sequetialSearch and now it crashes when it runs. It compiles okay, but then crashes. main.c #include #include #include #include #include "percentage.h" #include "sequentialSearch.h" #define searchAmount 100 int main(int argc, char *argv[]) { int numbers[100]; int searches[searchAmount]; int testAmounts[searchAmount]; int i; int where; int searchSuccess; int searchUnsuccess; int percent; int looker; int sum; int average; srand(time(NULL)); for (i = 0; i sequentialSearch.h bool seqSearch (int list[], int last, int target, int* locn, int* looker){ *looker = 0; while(*looker
