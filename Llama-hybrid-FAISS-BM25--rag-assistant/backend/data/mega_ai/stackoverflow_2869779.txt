[site]: stackoverflow
[post_id]: 2869779
[parent_id]: 2869552
[tags]: 
You could use lexical_cast from boost. You read the input into a string and the lexical_cast will check if it can be converted into a string. This will also make sure that your string is not to long to be converted, if it is a negative number or a number at all. #include #include #include using std::cout; using std::cin; using std::endl; using std::string; using boost::lexical_cast; using boost::bad_lexical_cast; double average(int tab[], int i){ int sum=0; for(int j=0; j > input; //read number into string tab[n]= lexical_cast (input); //conversion with lexical_cast //if not possible exception is //thrown } } catch(bad_lexical_cast &){ cout
