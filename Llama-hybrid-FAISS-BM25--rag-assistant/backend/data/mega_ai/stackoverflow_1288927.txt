[site]: stackoverflow
[post_id]: 1288927
[parent_id]: 1288344
[tags]: 
See the solution below. Cheers, Maras public static int[] Distribute(int[] weights, int boxesNo) { if (weights.Length == 0) { return new int[boxesNo]; } double average = weights.Average(); int[] distribution = new int[weights.Length]; for (int i = 0; i bestResult = new List (boxesNo); while (true) { List result = new List (boxesNo); for (int i = 0; i
