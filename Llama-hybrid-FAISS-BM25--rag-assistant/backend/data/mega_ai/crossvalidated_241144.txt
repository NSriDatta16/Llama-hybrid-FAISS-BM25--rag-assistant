[site]: crossvalidated
[post_id]: 241144
[parent_id]: 
[tags]: 
Explain what is meant by a deterministic and stochastic trend in relation to the following time series process?

Explain what is meant by a deterministic and stochastic trend in relation to the following time series process? $y_t = c + y_{t-1} + \varepsilon_t$ where $\varepsilon_t\sim iid(0, \sigma^2)$ this should be a random walk with drift, how should it relate to deterministic and stochastic trend?
