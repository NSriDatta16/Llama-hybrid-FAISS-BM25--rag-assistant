[site]: stackoverflow
[post_id]: 5682843
[parent_id]: 5682783
[tags]: 
The jQuery UI .draggable() handles the mousedown/mouseup events for you. All you need to do is call draggable on the items you want draggable when the document loads. Then, you can respond to events. Examples are on the API page for this: http://jqueryui.com/demos/draggable/#events
