[site]: stackoverflow
[post_id]: 4414730
[parent_id]: 
[tags]: 
What's wrong with this Javascript syntax?

google.maps.event.addListener(marker,"dragend",fuction(){ var newLat = marker.getPosition().lat(); var newLng = marker.getPosition().lng(); alert(newLat); }); If I delete this, everything is fine. If I keep this in here....it says "Missing argument after list", on the first line.
