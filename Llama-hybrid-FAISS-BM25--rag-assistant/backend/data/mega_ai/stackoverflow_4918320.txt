[site]: stackoverflow
[post_id]: 4918320
[parent_id]: 4918244
[tags]: 
$. This special variable will give you the input file line number, for more detail go to http://perldoc.perl.org/perlvar.html use strict; use IO::Handle; my $search = 'escapes'; while (my $line = ) { if ($line =~ /$search/){ my $line_number = DATA->input_line_number(); } } __DATA__ "How to Format ? put returns between paragraphs ? for linebreak add 2 spaces at end ? _italic_ or **bold** ? indent code by 4 spaces ? backtick escapes `like _so_` ? quote by placing > at start of line ? to make links [foo](http://foo.com)"
