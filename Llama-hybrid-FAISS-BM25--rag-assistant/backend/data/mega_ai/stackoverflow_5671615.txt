[site]: stackoverflow
[post_id]: 5671615
[parent_id]: 
[tags]: 
filter a dropdownlist

how to filter a dropdownlist? i tried to make a code but its not working.. please check my code. tnx DataSet ds = new DataSet(); DataTable dt = ds.Tables.Add("Source"); string category; category = drpCategory.SelectedValue; DataRow[] foundRows; foundRows = table.Select(category); for (int i = 0; i
