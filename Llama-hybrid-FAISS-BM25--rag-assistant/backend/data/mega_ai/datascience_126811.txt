[site]: datascience
[post_id]: 126811
[parent_id]: 126809
[tags]: 
It depends on tools. I guess in the most cases it will raise error. The input shape of data should be exactly the same as train data shape. It's impossible that a SVM model use a feature that it hasn't seen.
