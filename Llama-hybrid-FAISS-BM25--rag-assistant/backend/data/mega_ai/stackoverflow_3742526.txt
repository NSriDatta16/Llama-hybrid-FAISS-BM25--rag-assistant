[site]: stackoverflow
[post_id]: 3742526
[parent_id]: 
[tags]: 
Dynamic matrix with contiguous storage

I want a matrix container class that has similar functionality to vector >, but stores elements in contiguous memory. I bet there is none in the standard library (including C++0x); does Boost provide one?
