[site]: stackoverflow
[post_id]: 5417880
[parent_id]: 
[tags]: 
I want to save the results as variables instead of printing them in an array

I am using the following code that gets what is between and . Instead of displaying the result in an array how can I save every paragraph in a new variable ? Thank you!
