[site]: stackoverflow
[post_id]: 2453819
[parent_id]: 
[tags]: 
populating object collection property with Linq

I have an XML structure that has many doc nodes, and each node may have zero or more extract paragraphs ( paras ). 3 129826 sample title 2009-07-03T16:59:00 2009-07-03T16:59:23 sample summary 2 1 sample text 1 2 sample text 2 ... I also has some Linq code to populate some Document objects: List documentsList = (from doc in xmlDocument.Descendants("doc") select new Document { DocId = doc.Element("docid").Value, DocTitle = doc.Element("doctitle").Value, DocDateTime = DateTime.Parse(doc.Element("docdate").Value), DocSummary = doc.Element("summary").Value, DocParas = "" ///missing code to populate List } ).ToList (); Is it possible add all the paras nodes into the Document.DocParas List using Linq and Xpath, or should I do this task in a different way? Note: I'm using .NET C# 3.5
