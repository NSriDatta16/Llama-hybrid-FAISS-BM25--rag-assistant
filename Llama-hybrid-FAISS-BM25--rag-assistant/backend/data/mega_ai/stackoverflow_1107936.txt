[site]: stackoverflow
[post_id]: 1107936
[parent_id]: 1107919
[tags]: 
You can use variables within MySQL SELECT @query1:=(subquery) AS s1, @query2:=(subquery) AS s2, (@query1+@query2) AS s3 Still not that elegant. Perhaps you could elaborate on the subqueries so we could perhaps suggest better ways?
