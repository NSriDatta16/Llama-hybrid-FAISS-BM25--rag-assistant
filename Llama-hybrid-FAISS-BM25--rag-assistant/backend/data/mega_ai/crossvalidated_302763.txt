[site]: crossvalidated
[post_id]: 302763
[parent_id]: 
[tags]: 
Feature engineering after multi-imputation of missing data

So far I read that with multi-imputation, I put the new values of each set into a different data, run the training and than vote for a final results, like in the following schema: However, what should I do if I want to further engineer the imputed data? It sound unlikely to run each line of code multiple times for each dataset.
