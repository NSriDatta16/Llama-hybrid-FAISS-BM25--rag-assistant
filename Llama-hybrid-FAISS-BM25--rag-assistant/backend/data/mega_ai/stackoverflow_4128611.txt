[site]: stackoverflow
[post_id]: 4128611
[parent_id]: 4128582
[tags]: 
Shouldn't that be (SELECT x.agentid, SUM(services) FROM tblservices x INNER JOIN tblAgent t ON t.agentid = x.agentid GROUP BY x.agentid) if you're joining by agentid, or (SELECT SUM(services) FROM tblservices x INNER JOIN tblAgent t ON t.agentid = x.agentid ) if you're not?
