[site]: stackoverflow
[post_id]: 3740828
[parent_id]: 3740660
[tags]: 
Empty paragraphs looks like [spaces or newlines] (case-insensitive). You can use preg_replace (or str_replace) for removing empty paragraphs. The following will only work if an empty paragraph is : $oldHtml = file_get_contents('File With Empty Paragraphs.html'); $newHtml = str_replace(' ', '', $oldHtml); // and write the new HTML to the file $fh = fopen('File Without Empty Paragraphs.html', 'w'); fwrite($fh, $newHtml); fclose($fh); This will also work on paragraphs with attributes, like : $oldHtml = file_get_contents('File With Empty Paragraphs.html'); $newHtml = preg_replace('# ]*>\s* #i', '', $oldHtml); // and write the new HTML to the file $fh = fopen('File Without Empty Paragraphs.html', 'w'); fwrite($fh, $newHtml); fclose($fh);
