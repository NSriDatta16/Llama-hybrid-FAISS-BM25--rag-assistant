[site]: stackoverflow
[post_id]: 1328496
[parent_id]: 
[tags]: 
Is there a way to typedef this?

Simplified version of the code: Foo.h: class Foo { private: class Bar { // ... }; typedef std::map mapType; mapType _map; public: void method(mapType::iterator it); }; Foo.cpp: void Foo::method(mapType::iterator it) { // ... notMethod(it); } void notMethod(mapType::iterator it) { // ... } Unsurprisingly, I get the error 'mapType' is not a class or namespace name in VS2008 at notMethod 's definition. Is there any (elegant) way that I can avoid having to type out std::map everywere in notMethod 's definition without turning notMethod into a method?
