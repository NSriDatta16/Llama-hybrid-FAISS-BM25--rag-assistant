[site]: stackoverflow
[post_id]: 2081999
[parent_id]: 2081993
[tags]: 
If the user agent indicates it's an iPhone, don't load them. Update: According to the class you referenced, this is how it should accomplish your check - I say should simply because I can see that some users commented that this wasn't reliable for them on the blog you linked: $browser = new Browser(); $jQuery = ($browser->getBrowser() == Browser::PLATFORM_IPHONE) ? false : true ; See Also: iPhone User Agent (StackOverflow)
