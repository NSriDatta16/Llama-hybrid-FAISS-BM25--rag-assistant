[site]: stackoverflow
[post_id]: 2796901
[parent_id]: 2796891
[tags]: 
If you really want to, you can use De Morgan's laws to replace x || y in your code. One version says: !(x || y) == !x && !y If you want to have the same result, we just need to negate the entire expression twice: x || y == !!(x || y) == !(!x && !y)
