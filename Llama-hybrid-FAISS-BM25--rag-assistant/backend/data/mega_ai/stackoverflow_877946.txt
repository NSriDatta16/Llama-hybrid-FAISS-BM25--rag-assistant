[site]: stackoverflow
[post_id]: 877946
[parent_id]: 876847
[tags]: 
I believe the solution here is to create a DTO class and then use that as a result transform. public class OrderDTO { int Id; // Rest of Orders properties you want int LineTotal; // [quantity]*[unitPrice] public OrderDTO(int id, /* rest of constructor parameters */); } session.CreateCriteria(typeof(... /* criteria query here */)) .SetResultTransformer(new AliasToBeanConstructorResultTransormer(typeof(OrderDTO).GetConstructors()[0]));
