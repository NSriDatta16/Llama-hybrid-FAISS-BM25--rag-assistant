[site]: crossvalidated
[post_id]: 241625
[parent_id]: 241623
[tags]: 
remove stopwords from this data It depends on many things, such as the classifier and the features. E.g. it is quite common to remove stopwords when using bag-of-words, but very uncommon when using recurrent neural networks. remove low information features from this data People often use the validation set to explore feature selection.
