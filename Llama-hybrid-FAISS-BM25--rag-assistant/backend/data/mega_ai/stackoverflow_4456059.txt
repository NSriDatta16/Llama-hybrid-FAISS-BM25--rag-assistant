[site]: stackoverflow
[post_id]: 4456059
[parent_id]: 4316709
[tags]: 
Hey I have been messing around with something similar - let me know if this is sufficient to help you out - if not I can explain it some more. $data = " testing in a span 1 testing in a span 3 testing in a span 4 testing in a span 5 "; $dom = new Zend_Dom_Query(); $dom->setDocumentHtml($data); //Look for any links inside of paragraph tags $results = $dom->query('p a'); foreach($results as $r){ echo "Parent Tag: ".$r->nodeName." "; echo $r->nodeValue." "; $children = $r->childNodes; if($children->length > 0){ $children = $r->childNodes; foreach($children as $c){ echo "Child Tag: "; echo $c->nodeName." "; echo $c->nodeValue." "; } } echo $r->getAttribute('href')." "; } echo $data;
