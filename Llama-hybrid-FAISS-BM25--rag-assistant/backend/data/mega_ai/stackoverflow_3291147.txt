[site]: stackoverflow
[post_id]: 3291147
[parent_id]: 3290949
[tags]: 
@bradenkeith is probably correct with his answer but you might want to leverage jQuery a little more and do a jQuery dialog as well. $("a.confirm-delete").click(function(event) { var message = fConfirmDelete(deleteObj); var $dialog = $j(' ') .html(message) .dialog({ autoOpen: true, bgiframe: true, buttons: { 'Dismiss': function() { $j(this).dialog('close');} 'Delete': function() { //Do delete things } } modal: true // other parameters here }); }); Something like that might be easier to read and maintain in my opinion. Just an option. :)
