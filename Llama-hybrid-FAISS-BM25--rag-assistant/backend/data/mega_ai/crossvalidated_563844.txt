[site]: crossvalidated
[post_id]: 563844
[parent_id]: 563827
[tags]: 
Here is an implementation of the White test that works for me, at the time of writing (along with some manual calculations to illustrate the $nR^2$ format of the test statistic). library(skedastic) mtcars_lm Some things that also strike me in your post: Why use White if you have already done Breusch-Pagan? Both serve the same purpose. I do not think you use I correctly when generating the square of the log. Try the following for illustration: set.seed(1) y
