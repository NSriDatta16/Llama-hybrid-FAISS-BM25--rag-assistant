[site]: stackoverflow
[post_id]: 4365343
[parent_id]: 4364818
[tags]: 
@thing.id) %> and in the controller def new @parent = Thing.find(params[:Parent_id]) @thing = Thing.new @thing.parent_id = @parent.id respond_to do |format| format.html # new.html.erb format.xml { render :xml => @organizr } end end I guess my real question should have been how do I add parameters to a GET in Rails!
