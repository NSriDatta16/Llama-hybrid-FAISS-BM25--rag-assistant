[site]: stackoverflow
[post_id]: 2258220
[parent_id]: 2256623
[tags]: 
You can check the format of the request: @categories = Category.all unless request.format == "application/json" and fragment_exists?("JSON_CATEGORIES_ALL") respond_to do |format| format.html # @categories is available format.json # no database call if your cache fragment already exists end
