[site]: stackoverflow
[post_id]: 1437339
[parent_id]: 
[tags]: 
Merge Two DateTime types in C#

This is what I have so far. /// /// Gets the date. /// /// The date: 05/07/2009 /// private static DateTime GetDate(string date) { DateTime postDate = DateTime.Parse(date); postDate.AddHours(DateTime.UtcNow.Hour); postDate.AddMinutes(DateTime.UtcNow.Minute); postDate.AddSeconds(DateTime.UtcNow.Second); postDate.AddMilliseconds(DateTime.UtcNow.Millisecond); return postDate; } Is there a better way to merge two dates? I'm looking for a more elegant solution.
