[site]: stackoverflow
[post_id]: 3733230
[parent_id]: 3730167
[tags]: 
try this var comList= new Array("apple","banana","mango"); var list = new Array("orage", "apricot"); var val1selected = 0; function loadDropDown(option){ setSelectdVal(option) createDropDown(option) } function setSelectdVal(option){ if (option == 1){ val1selected = 1; } } function createDropDown(option){ html = ""; html = html + ' ' var arr = getArray(option); for (var i=arr.length-1; i>=0; --i ){ html = html + ' ' + arr[i] + ' ' } html = html + ' ' document.getElementById('selectTag').innerHTML = html; } function getArray(option){ var a = comList; if ((option == 2) && (val1selected == 0)){ a = comList.concat(list) } return a; } value1 value2
