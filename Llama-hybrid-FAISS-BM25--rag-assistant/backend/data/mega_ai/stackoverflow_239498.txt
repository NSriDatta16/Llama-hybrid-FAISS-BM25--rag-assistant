[site]: stackoverflow
[post_id]: 239498
[parent_id]: 
[tags]: 
Drag & drop images from a HTA file

From a HTML application ( .hta file ), you can drag any image and drop it into the desktop for example. Does anybody know how to disable this capability? I want the .hta files to have the same behavior as the .html files.
