[site]: stackoverflow
[post_id]: 2744178
[parent_id]: 2728872
[tags]: 
Sadly applying it to the iframe's body didn't work. But i found a plugin where you can apply it to any element on the site: http://benalman.com/projects/jquery-resize-plugin/ So that's my code(solution) now: function initFrame() { resizeAppFrame(); if (navigator.userAgent.indexOf("MSIE") > 0) $(function() { $(appFrame.document.body).resize(resizeAppFrame) }) //IE else document.getElementById("applicationFrame").contentWindow.addEventListener("overflow", resizeAppFrame, false); //FireFox } function resizeAppFrame() { $("#iFrameID").height($("#iFrameID").contents().find("body").outerHeight(true)); } ...
