[site]: stackoverflow
[post_id]: 4822439
[parent_id]: 4821906
[tags]: 
Rather than pulling it into PHP you can do this directly from the MySQL query, For example, the following query returns a the average age of users in days to the current date: SELECT AVG(DATEDIFF(NOW(), birthday)) AS AverageAge FROM `user_records`;
