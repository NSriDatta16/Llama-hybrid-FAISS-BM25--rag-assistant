[site]: stackoverflow
[post_id]: 3658090
[parent_id]: 3657817
[tags]: 
I'm sure it could be neatened up, but here's something that seems to work: function dropRow(event, ui) { var target = $(event.target); var draggable = ui.draggable; var tr = $(' ' + draggable.text() + ' '); target.before(tr); tr.droppable({ drop: dropRow, greedy: true }); } function dropTable(event, ui) { var target = $(event.target); var draggable = ui.draggable; var tr = $(' ' + draggable.text() + ' '); target.append(tr); tr.droppable({ drop: dropRow, greedy: true }); } $("#mytable").droppable({ drop: dropTable });​ This question may relate - part of your issue is hooking up the 'drop' event to elements that don't exist yet (and 'live' doesn't handle 'drop' either).
