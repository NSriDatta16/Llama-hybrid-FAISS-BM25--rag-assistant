[site]: stackoverflow
[post_id]: 3675531
[parent_id]: 3675390
[tags]: 
Using C++: //Splits the string into the fragments of given size //Returns the set of of splitted strings avaialble set split(string s, int frag) { set uni; int len = s.length(); for(int i = 0; i 1;--i) { set uni = split(s,i); if(uni.size() == 1) { out = *uni.begin(); break; } } cout
