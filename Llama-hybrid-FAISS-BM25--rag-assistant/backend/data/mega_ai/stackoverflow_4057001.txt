[site]: stackoverflow
[post_id]: 4057001
[parent_id]: 3797390
[tags]: 
The Jquery Tablesorter plugin would be appropriate if you don't need the sorting to persist across user sessions and its just reordering the same content (although you can use localStorage to save list orders). http://tablesorter.com/docs/
