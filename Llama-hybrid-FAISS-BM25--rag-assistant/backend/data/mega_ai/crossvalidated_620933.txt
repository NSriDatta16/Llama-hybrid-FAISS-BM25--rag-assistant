[site]: crossvalidated
[post_id]: 620933
[parent_id]: 620890
[tags]: 
In general, not just in neural networks, one common use of higher dimensional mappings is to make the data separable in the high dimensional space. For example, the whole idea of kernel SVM's is based on this.
