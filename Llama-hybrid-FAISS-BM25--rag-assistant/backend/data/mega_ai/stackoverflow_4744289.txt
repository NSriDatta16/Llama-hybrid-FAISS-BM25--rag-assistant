[site]: stackoverflow
[post_id]: 4744289
[parent_id]: 
[tags]: 
Problems moving buttons with javascript

Hello i have a division that contain buttons and i want to a group in a table inside the same division but when i try to make click on them they just the last buttons are aviable to be used. This is the function... function properties(){ //This function gives to all of the elements inside //the widgetsCol that create the pages. wdgBox = document.getElementById('widgetsCol'); $(wdgBox).first().before(" "); tr = document.createElement('TR'); $("#wdgBod").append(tr); cont = 0; $(wdgBox).children().each(function(index, elmnt){ td = document.createElement('TD'); $(tr).append(td) $(elmnt).appendTo(td); $(elmnt).attr('new', 'true');//Add the new attribute to the element. makeDrag(elmnt);//And make it draggrable. if (cont == 2){ cont = 0; tr = document.createElement('TR'); $("#wdgBod").append(tr); }else{ cont += 1; } });} KarinApp(Karina Application Web Maker) \\n window.onload = function(){ postLoad(); } function __init__(){ main(); } Agregar componente Guardar Pagina Crear PostData Crear Instrucciones Componentes Basicos close Contenedor: Body A AREA Audio BASE BDO Input(Button) Button Code Column Colgroup DL DIV Form H1 Image Input(Checkbox) Input(file) Input(Hidden) Input(Image) Input(Radio) Input(Text) Label Link Map Meta Object OL Input(Password) P PRE Input(Reset) Samp Script Select Span Style Input(submit) Table TBody TextArea TFoot THead UL Var Video Abrir Paleta
