[site]: stackoverflow
[post_id]: 5307036
[parent_id]: 
[tags]: 
What is difference between template and template . For me both are generating the same result

What is difference between template and template . For me both are generating the same result. for example template T Average(T *atArray, int nNumValues) { T tSum = 0; for (int nCount=0; nCount if I change it to template it's the same
