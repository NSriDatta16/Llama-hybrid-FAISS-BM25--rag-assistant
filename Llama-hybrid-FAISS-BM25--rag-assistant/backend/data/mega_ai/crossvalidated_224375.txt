[site]: crossvalidated
[post_id]: 224375
[parent_id]: 223258
[tags]: 
We can think of this as a hyperparameter optimization problem. We have a target X_mean which is the target value. We also have a loss function e.g. L2 (X_exponential - X_mean). We are searching for a hyperparameter (alpha) for the exponential moving average to minimize loss.
