[site]: stackoverflow
[post_id]: 4225347
[parent_id]: 4225305
[tags]: 
TO create a directory(for example on sd-card) you should use the other way: File file = new File(Environment.getExternalStorage() + "/folder-name"); file.mkdirs(); To write to external storage you should have the following permission in your manifest: To execute a system command usually used Runtime.getRuntime().exec("ls -l");
