[site]: stackoverflow
[post_id]: 1556918
[parent_id]: 1158518
[tags]: 
Good answers. My two cents is that there is no method of testing that catches all errors, but less testing will never catch more errors, so any testing is good. To my mind, coverage testing is not to show what code has been exercised, but to show what code has not been exercised, because that is where bugs love to lurk. If you combine it with single-stepping, it is a very good way to review code and catch bugs. Here's an example.
