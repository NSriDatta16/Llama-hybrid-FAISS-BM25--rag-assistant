[site]: stackoverflow
[post_id]: 1837690
[parent_id]: 1835660
[tags]: 
The quick and simple way, without using a cache is to do: class Script has_many :chapters has_many :paragraphs, :through => :chapters end script = Script.find(1) puts script.paragraphs.size #get the count
