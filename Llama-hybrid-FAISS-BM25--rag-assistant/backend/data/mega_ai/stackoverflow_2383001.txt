[site]: stackoverflow
[post_id]: 2383001
[parent_id]: 2382828
[tags]: 
No bug, SQL XML decodes XML escape sequences correctly: declare @x xml; set @x = ' &amp;some '; select @x.value(N'(node)[1]', N'varchar(max)'); What most likely is your problem is a misunderstanding on your part of the XML query method: it returns an XML fragment, and as such all content will be... escaped, since it's XML: declare @x xml; set @x = ' &amp;some '; select @x.query(N'/node/text()');
