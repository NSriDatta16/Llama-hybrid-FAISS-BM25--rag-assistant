[site]: stackoverflow
[post_id]: 2036089
[parent_id]: 2036045
[tags]: 
The fastest general way is to use ExecuteNonQuery. internal static void FastInsertMany(DbConnection cnn) { using (DbTransaction dbTrans = cnn.BeginTransaction()) { using (DbCommand cmd = cnn.CreateCommand()) { cmd.CommandText = "INSERT INTO TestCase(MyValue) VALUES(?)"; DbParameter Field1 = cmd.CreateParameter(); cmd.Parameters.Add(Field1); for (int n = 0; n Even on a slow computer this should take far less than a second for 1500 inserts. [reference]
