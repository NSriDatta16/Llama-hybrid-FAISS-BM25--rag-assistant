[site]: stackoverflow
[post_id]: 5251708
[parent_id]: 5244719
[tags]: 
use encoding is official discouraged. The module is deprecated because it causes very weird behaviour. Instead, you should use the following: use utf8; # Source code is UTF-8 use open ':std', ':encoding(UTF-8)'; # STDIN,STDOUT,STDERR are UTF-8.
