[site]: stackoverflow
[post_id]: 5693406
[parent_id]: 
[tags]: 
Pivot header-like Control?

Is it any separated Control for create something like Pivot header? I need horizontal list on top of page, binded to my Collection with same behavior like Pivot header - left-right drags(can be done with GestureListener, i think) and with event for select new item.
