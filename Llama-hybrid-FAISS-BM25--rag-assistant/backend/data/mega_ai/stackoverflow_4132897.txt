[site]: stackoverflow
[post_id]: 4132897
[parent_id]: 
[tags]: 
html DOM file referencing an XML file won't print in browser

I am totally new to this and I am taking a class for fun that has now turned into a big headache. I have this html DOM file that references an XML file that I need to have show up on the screen in a browser. It should only print the elements of, title, first, last, location, discription. The CSS and Schema both validate fine and work. Should I still leave both of them in the the xml file when I using the html file? I get a blank on the browser screen. Thanks, Liam Catalog var xmlDoc; function loadXMLDoc() { // XML loader for IE if (window.ActiveXObject) { xmlDoc = new ActiveXObject("Microsoft.XMLDOM"); xmlDoc.load("catalog.xml"); printCatalog(); } // XML loader for other browsers else { xmlDoc = document.implementation.createDocument("", "", null); xmlDoc.load("catalog.xml"); xmlDoc.onload = printCatalog; } } function printcatalog() { var titleNodes = xmlDoc.getElementsByTagName("title"); var firstNodes = xmlDoc.getElementsByTagName("first"); var lastNodes = xmlDoc.getElementsByTagName("last"); var locationNodes = xmlDoc.getElementsByTagName("location"); var descriptionNodes = xmlDoc.getElementsByTagName("description"); for (var i = 0; i " + titleNodes[i].firstChild.nodeValue + " "); document.write(" " + firstNodes[i].firstChild.nodeValue + " "); document.write(lastNodes[i].firstChild.nodeValue + " "); document.write(firstNodes[i].firstChild.nodeValue + " "); document.write(locationNodes[i].firstChild.nodeValue + " "); document.write(descriptionNodes[i].firstChild.nodeValue + " "); } } Programming 1 Bill Gates 3 100 101 Learn from the man himself, Mr. Microsoft. Must love PCs and not Macs. Government 1945 Karl Marx 3 200 202 Mr. Marx will explain his political theories Lecture course. Bring lots of coffee. Math 911 Albert Einstein 3 200 302 Find the answers to the universe in just one semester.
