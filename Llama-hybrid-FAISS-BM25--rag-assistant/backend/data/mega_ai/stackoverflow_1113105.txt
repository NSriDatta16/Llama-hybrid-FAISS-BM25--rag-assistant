[site]: stackoverflow
[post_id]: 1113105
[parent_id]: 1112974
[tags]: 
It might be possible to detect mouse events and dynamically make the content draggable ... You can get the selected text using: txt = document.getSelection(); It looks like this only works on text, not the actual HTML though. Full selection code is here: http://www.codetoad.com/javascript_get_selected_text.asp
