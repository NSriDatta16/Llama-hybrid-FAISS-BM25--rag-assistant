[site]: stackoverflow
[post_id]: 4913264
[parent_id]: 4910897
[tags]: 
Try the following code. Just put your if where you want. ' . join('|', $known) . ')[/ ]+(? [0-9.|a-zA-Z.]*)#'; // see how many we have $i = count($matches['browser']); if ($i != 1) { //we will have two since we are not using 'other' argument yet //see if version is before or after the name if (strripos($u_agent,"Version") $u_agent, 'name' => $bname, 'version' => $version, 'platform' => $platform, 'pattern' => $pattern ); } // now try it $ua=getBrowser(); $yourbrowser= "Your browser: " . $ua['name'] . " " . $ua['version'] . " on " .$ua['platform'] . " reports: " . $ua['userAgent']; echo $yourbrowser; ?>
