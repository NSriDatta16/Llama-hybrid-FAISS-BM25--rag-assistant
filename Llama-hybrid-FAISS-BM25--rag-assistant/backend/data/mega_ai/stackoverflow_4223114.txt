[site]: stackoverflow
[post_id]: 4223114
[parent_id]: 4223055
[tags]: 
function ranger(num) { //Establish limits and pre/post array storage var low = 0, high = 100, howMany = 5; var pre = []; var post = []; //Increment/decrement if appropriate for(x=1;x low) { pre.push(num-x); } if((num+x)
