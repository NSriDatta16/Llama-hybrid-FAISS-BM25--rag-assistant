[site]: stackoverflow
[post_id]: 3310110
[parent_id]: 
[tags]: 
A better way to "Clear" all tables using Entity Framework

Hi currently when I want to clear my tables I use brute force: inventario_dboEntities inv = new inventario_dboEntities(); foreach (var item in inv.espiromex_dampers) { inv.DeleteObject(item); } foreach (var item in inv.espiromex_detalles) { inv.DeleteObject(item); } foreach (var item in inv.espiromex_docs) { inv.DeleteObject(item); } I am sure there must be a better more elegant way to do this... how you guys do this kind of task?
