[site]: stackoverflow
[post_id]: 5075234
[parent_id]: 5073925
[tags]: 
Here is the code I use to save and load lists of objects from isolated storage. public class IsoStoreHelper { private static IsolatedStorageFile _isoStore; public static IsolatedStorageFile IsoStore { get { return _isoStore ?? (_isoStore = IsolatedStorageFile.GetUserStoreForApplication()); } } public static void SaveList (string folderName, string dataName, ObservableCollection dataList) where T : class { if (!IsoStore.DirectoryExists(folderName)) { IsoStore.CreateDirectory(folderName); } string fileStreamName = string.Format("{0}\\{1}.dat", folderName, dataName); using (IsolatedStorageFileStream stream = new IsolatedStorageFileStream(fileStreamName, FileMode.Create, IsoStore)) { DataContractSerializer dcs = new DataContractSerializer(typeof(ObservableCollection )); dcs.WriteObject(stream, dataList); } } public static ObservableCollection LoadList (string folderName, string dataName) where T : class { ObservableCollection retval = new ObservableCollection (); if (!IsoStore.DirectoryExists(folderName)) { IsoStore.CreateDirectory(folderName); } string fileStreamName = string.Format("{0}\\{1}.dat", folderName, dataName); using (IsolatedStorageFileStream stream = new IsolatedStorageFileStream(fileStreamName, FileMode.OpenOrCreate, IsoStore)) { if (stream.Length > 0) { DataContractSerializer dcs = new DataContractSerializer(typeof(ObservableCollection )); retval = dcs.ReadObject(stream) as ObservableCollection ; } } return retval; } }
