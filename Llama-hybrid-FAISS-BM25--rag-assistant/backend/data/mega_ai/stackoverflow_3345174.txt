[site]: stackoverflow
[post_id]: 3345174
[parent_id]: 3345159
[tags]: 
It's a trick to prevent the same header file being included more than once. The actual value you #define doesn't matter - so long as it's only defined in that header file, the convention is NAME_HEADER_FILE_H in capitals See also this discussion on #pragma once
