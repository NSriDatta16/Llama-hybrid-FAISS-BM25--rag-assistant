[site]: crossvalidated
[post_id]: 243538
[parent_id]: 243536
[tags]: 
First of, adding restrictions like that are a possibility and can work quite well in some case, however they certainly aren't mandatory. In the general case, they aren't there. If you do want them, you usually calculate the gradients separately, average them and then add that average to both. That ensures that both weights remain the same
