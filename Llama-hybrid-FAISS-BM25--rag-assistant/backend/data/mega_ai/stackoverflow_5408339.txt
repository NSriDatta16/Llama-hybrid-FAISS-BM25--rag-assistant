[site]: stackoverflow
[post_id]: 5408339
[parent_id]: 
[tags]: 
Change helper ID

I made a draggable, sortable div and what I want is to change the clone ID after dragging it, but my script changes the IDs of both the original and the clone. My code is as follows: var i = 0; $('#colum').draggable({ helper:'clone', connectToSortable:'#sort', start: function(event,ui) { i++; $(this).attr("id","colum"+i); } }); $('#sort').sortable({ connectWith: "#sort", containment: "#sort" )};
