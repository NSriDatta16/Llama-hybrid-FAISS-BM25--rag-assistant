[site]: crossvalidated
[post_id]: 8002
[parent_id]: 7999
[tags]: 
I for one would put everything to the list and then use Reduce . funToRepeat Then convert to data.frame you need after dividing by length of the list to get the average. Note that your function does not need whole of a data.frame to operate, only its dimensions. The conversion to data.frame is unnecessary cost here, so it is better not to use it.
