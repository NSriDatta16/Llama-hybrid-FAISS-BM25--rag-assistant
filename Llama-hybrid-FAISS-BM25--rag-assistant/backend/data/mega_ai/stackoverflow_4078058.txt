[site]: stackoverflow
[post_id]: 4078058
[parent_id]: 4077940
[tags]: 
if you want to check whether memory allocation failed do it this way: HGLOBAL hwndTitleValue = GlobalAlloc(GPTR,(sizeof(CHAR) * hwndTitleSize)); if (hwndTitleValue == NULL) return ALLOC_FAILED; I see no point working with strings here.
