[site]: stackoverflow
[post_id]: 5527022
[parent_id]: 5517513
[tags]: 
For high performance logging take a look into the Microsoft Message Queue (MSMQ). Each thread can write to the same message queue, while a separate service/process can read from the queue and store the data in your log storage format. This avoids any contention between the threads and maximizes performance.
