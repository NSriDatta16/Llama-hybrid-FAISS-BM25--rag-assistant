[site]: stackoverflow
[post_id]: 128418
[parent_id]: 128350
[tags]: 
The more elegant solution would be to employ SimpleXML . Or a third party library such as XML_Feed_Parser or Zend_Feed to parse the feed. Here is a SimpleXML example: item as $item) { echo "{$item->description}\n\n"; } ?> Keep in mind that RSS and RDF and Atom look different, which is why it can make sense to employ one of the above libraries I mentioned.
