[site]: stackoverflow
[post_id]: 3774183
[parent_id]: 3773962
[tags]: 
I am not sure but i think this code can help you (Adapted from JavaScript YUI YAHOO.util.DragDrop.js ) var initPosition = 0; var rangeLength = 500; var maxPosition = (initPosition + rangeLength); getTicks: function(bucketSize) { var axisTicks = []; var tickMap = {}; for (i = initPosition; i The drawback is because, sometimes, the axis ticks will not be evenly distributed ((initPosition + rangeLength) / bucketSize) does not return a plain integer
