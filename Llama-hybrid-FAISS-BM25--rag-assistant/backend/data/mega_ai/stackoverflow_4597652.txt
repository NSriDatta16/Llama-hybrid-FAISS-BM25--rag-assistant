[site]: stackoverflow
[post_id]: 4597652
[parent_id]: 4597606
[tags]: 
For per category Average Weightage SELECT Category, AVG(Value), SUM(CASE WHEN RN Average Weightage over the whole set SELECT M.Category, AVG(Value), foo.AvgWeightage FROM MyTable M CROSS JOIN (SELECT AVG(Weightage) As AvgWeightage FROM MyTable WHERE Rn
