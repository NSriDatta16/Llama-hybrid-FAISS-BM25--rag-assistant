[site]: stackoverflow
[post_id]: 3003961
[parent_id]: 3003903
[tags]: 
strip_tags Try using strip_tags . It will allow you to strip out all tags that you don't allow. Examples Example from the manual: Test paragraph. Other text '; echo strip_tags($text); echo "\n"; // Allow and echo strip_tags($text, ' '); ?> You would use something like this: Test paragraph. Other text '; // Allow some tags but not echo strip_tags($text, ' '); ?>
