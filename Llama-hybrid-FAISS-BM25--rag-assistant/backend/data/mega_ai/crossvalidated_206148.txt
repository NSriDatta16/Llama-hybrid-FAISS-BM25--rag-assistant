[site]: crossvalidated
[post_id]: 206148
[parent_id]: 206091
[tags]: 
There's another (pragmatic) reason. To calculate chi-square (using ML) you need to invert the implied covariance matrix. If the implied matrix isn't positive definite, you can't invert it so you can't do the calculation.
