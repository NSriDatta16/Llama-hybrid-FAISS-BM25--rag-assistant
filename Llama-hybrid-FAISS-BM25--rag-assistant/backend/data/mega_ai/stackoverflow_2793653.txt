[site]: stackoverflow
[post_id]: 2793653
[parent_id]: 2793588
[tags]: 
animal.h: class Cell; // fwd declaration // Cell is now an "incomplete type" class Animal { void Respirate( Cell & ); // reference: no need for complete type }; cell.h: #include "animal.h" class Cell { Animal organism; // conceptually, a reference or pointer would be better }; OR class Animal class Cell { Animal *organism; // Cell "knows about an" animal rather than "has an" };
