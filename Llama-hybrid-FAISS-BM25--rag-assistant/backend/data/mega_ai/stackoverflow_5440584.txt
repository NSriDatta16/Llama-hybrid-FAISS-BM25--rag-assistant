[site]: stackoverflow
[post_id]: 5440584
[parent_id]: 4917031
[tags]: 
I'm not sure why your Display attribute retrieval isn't working. Here's what I use. It's from a method which retrieves the Display attribute from enum field values, but it's the same basic pattern that I use to retrieve any attribute from an object: public static string GetDisplayName ( T toCheck ) { Type enumType = typeof(T); if( !enumType.IsEnum ) return null; MemberInfo[] members = enumType.GetMember(toCheck.ToString()); if( ( members == null ) || ( members.Length != 1 ) ) return toCheck.ToString(); foreach( MemberInfo memInfo in members ) { DisplayAttribute[] attrs = (DisplayAttribute[]) memInfo.GetCustomAttributes(typeof(DisplayAttribute), false); if( ( attrs != null ) && ( attrs.Length == 1 ) ) return attrs[0].Name; } return toCheck.ToString(); }
