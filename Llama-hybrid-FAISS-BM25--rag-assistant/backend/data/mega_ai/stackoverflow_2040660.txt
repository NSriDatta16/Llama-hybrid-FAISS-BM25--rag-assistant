[site]: stackoverflow
[post_id]: 2040660
[parent_id]: 1881643
[tags]: 
Here's what I made in Perl, I stumbled across this question when searching for a more elegant way of doing it (no luck so far). use strict; use warnings; my $guid; if (shift() =~ /\{([0-9A-F\-]+)\}/) { $guid = $1; } die "Usage $0: \n" unless $guid; my @sections = split /-/, $guid; die "Malformed guid $guid\n" if @sections != 5; my $str; for (my $i = 0; $i
