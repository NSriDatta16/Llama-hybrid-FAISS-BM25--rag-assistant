[site]: stackoverflow
[post_id]: 4156378
[parent_id]: 4156306
[tags]: 
Looping is pretty easy (and not inelegant). Iterate over the keys of the original Map and put it in them in the new copy with a value of zero. Set keys = original.keySet(); Map copy = new HashMap (); for(String key : keys) { copy.put(key, 0); } Hope that helps.
