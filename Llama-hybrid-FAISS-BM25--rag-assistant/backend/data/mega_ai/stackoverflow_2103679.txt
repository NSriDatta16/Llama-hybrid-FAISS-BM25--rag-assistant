[site]: stackoverflow
[post_id]: 2103679
[parent_id]: 2103530
[tags]: 
if i recall correctly #pragma omp threadprivate global variables are a bit tricky, if they has to be initialized, you have to use copyfirst directive (i think that is correct name). also threadprivate variables may carry a significant penalty, on some implementations variable access becomes a function call.
