[site]: stackoverflow
[post_id]: 2436803
[parent_id]: 2432130
[tags]: 
You don't need to pass any User-Agent header, unless you're using a service that explicitly requires it such as for tracking or debugging purposes etc. HTTP works perfectly well without the User-Agent header being present.
