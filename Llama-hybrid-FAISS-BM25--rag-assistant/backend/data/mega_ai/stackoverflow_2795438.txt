[site]: stackoverflow
[post_id]: 2795438
[parent_id]: 2795163
[tags]: 
Here is a C++ solution to your assignment, but you probably won't be allowed to hand that in ;-) #include #include #include #include #include int main() { const int N = 10; int ary[N] = {4, 32, 9, 7, 14, 12, 13, 17, 19, 18}; int max = *std::max_element(ary, ary + N); std::cout (std::cout, " "), std::bind2nd(std::less_equal (), 2 * average)); std::cout
