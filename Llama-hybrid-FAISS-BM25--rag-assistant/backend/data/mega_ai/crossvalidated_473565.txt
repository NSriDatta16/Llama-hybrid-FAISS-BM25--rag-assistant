[site]: crossvalidated
[post_id]: 473565
[parent_id]: 
[tags]: 
Whats the different between Logistic regression and perceptron?

In a binary classification problem, if both logistic regression and a single preceptron uses Sigmoid function, what's the difference in classification results, since they will have the same decision boundary ? their conceptual behaviour?
