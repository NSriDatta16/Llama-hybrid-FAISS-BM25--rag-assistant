[site]: stackoverflow
[post_id]: 2039137
[parent_id]: 2039052
[tags]: 
Silly question, but is the TextArea component still in the library ? Try to delete it and drag it again from the Components Panel. When exactly does the error occur ? As soon as you access the TextArea ? When you try to retrieve/set the text ?
