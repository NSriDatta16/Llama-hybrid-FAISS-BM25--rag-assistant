[site]: stackoverflow
[post_id]: 3223353
[parent_id]: 
[tags]: 
How to check UIButton position in a ScrollView?

I have a UIButton in a ScrollView. This button is hided by the NavigationBar, so it is visible only when the user drag down the ScrollView. I want to check the position of the UIButton, and if the y position is
