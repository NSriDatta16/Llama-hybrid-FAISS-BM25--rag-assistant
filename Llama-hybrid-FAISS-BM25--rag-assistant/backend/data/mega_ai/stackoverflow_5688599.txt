[site]: stackoverflow
[post_id]: 5688599
[parent_id]: 
[tags]: 
Add elements to canvas html5

It is possible to add elements inside HTML5's canvas? For example: Example That doesn't show anything... however, if I put that draggable div inside any other element in the html it works... What am I missing? Thx.
