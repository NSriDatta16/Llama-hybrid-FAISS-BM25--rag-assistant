[site]: stackoverflow
[post_id]: 628847
[parent_id]: 628790
[tags]: 
The number one priority of my hash table is quick search (retrieval). Well then you are using the right data structure, as searching in a hash table is O(1)! :) The CRC32 should do fine. The implementation isn't that complex, it's mainly based on XORs. Just make sure it uses a good polynomial.
