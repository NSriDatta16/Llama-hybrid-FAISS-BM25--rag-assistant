[site]: stackoverflow
[post_id]: 5366924
[parent_id]: 
[tags]: 
Jquery drag-n-drop

I have this script, which to display window on screen: $.ajax({ type: "POST", url: "test.php", data: dataString, cache: false, success: function(html){ $("#work-window").append(html); } }); This window: Add close Content I need to do, that when click a center class, i can drag and drop this div (#div-add). i tried make it with jquery Ul, but no result :( $(function() { $( "#div-add" ).draggable(); }); How create this ?
