[site]: stackoverflow
[post_id]: 3759841
[parent_id]: 729621
[tags]: 
...and if you're using purely XOM, something like this: String xml = " " + xml + " "; Document doc = new Builder( false ).build( xml, null ); Nodes children = doc.getRootElement().removeChildren(); for( int ix = 0; ix XOM uses fakeRoot internally to do pretty much the same, so it should be safe, if not exactly elegant.
