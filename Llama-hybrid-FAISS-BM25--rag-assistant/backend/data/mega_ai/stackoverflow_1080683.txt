[site]: stackoverflow
[post_id]: 1080683
[parent_id]: 1080652
[tags]: 
Why not use std::string ? This makes storage, concatenation, and modification much easier. If you need a c-style string after, use: my_string.c_str() Here is a hint: To make your input 3 characters long, use std::insert to prefix your number with 0's.
