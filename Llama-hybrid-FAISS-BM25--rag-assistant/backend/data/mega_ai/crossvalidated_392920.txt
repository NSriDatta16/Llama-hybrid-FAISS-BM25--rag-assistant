[site]: crossvalidated
[post_id]: 392920
[parent_id]: 
[tags]: 
PCA Marginal Distribution

In PCA, if I have a latent $\vec{y}$ with loading matrix $\Lambda$ , then the PCA models using: (1) $P(\vec{y}) \sim N(\vec{0}, I)$ , $P(\vec{x}|\vec{y}) \sim N(\Lambda \vec{y}, \psi I)$ (2) $P(\vec{y}) \sim N(\vec{0}, \Gamma), P(\vec{x}|\vec{y}) \sim N(\Lambda \vec{y}, \psi I)$ with $\Gamma_{ij}=0$ for $i \neq j$ and $\Lambda^T \Lambda = I$ lead to exactly the same marginal distributions. So far, I have attempted to compute the marginal of (1) by integrating out over $\vec{y}$ as follows (I am assuming the latent variable $\vec{y}$ has dimension $n$ and the normal variable $\vec{x}$ has dimension $D$ ): $P(\vec{x}) = \int_{\mathbb{R}^n} P(\vec{x}| \vec{y}) P(\vec{y}) d \vec{y}= \int_{\mathbb{R}^n} \frac{1}{\sqrt{(2 \pi)^n | I|}}exp(-\frac{1}{2} \vec{y}^T \vec{y}) P(\vec{x}|\vec{y}) d \vec{y} = \int_{\mathbb{R}^n} \frac{1}{(2 \pi)^{\frac{n+D}{2}}\psi^{\frac{1}{2}}|I|}exp(-\frac{1}{2} \vec{y}^T \vec{y}) exp \left(-\frac{1}{2 \psi} [\vec{x}^T \vec{x} - 2 \vec{x}^T \Lambda \vec{y} + (\Lambda \vec{y})^T (\Lambda \vec{y})] \right) d \vec{y}$ I am not sure if I am on the right lines? If I am, what should I do next? Can I simplify this marginal for (1) any more or should I go ahead and try and bring the marginal of (2) to this form? Thanks
