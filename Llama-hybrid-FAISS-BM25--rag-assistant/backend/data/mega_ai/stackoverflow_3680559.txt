[site]: stackoverflow
[post_id]: 3680559
[parent_id]: 
[tags]: 
Add Stylesheet reference to XML Document in Linq?

I create an XML Doc and wanted have a reference to the XSLT file. // to this XML generation: XElement xml = new XElement("ReportedOn", from dl in EL.DocumentLog.ToList() join o in EL.Organization on dl.OrganizationID equals o.OrganizationId where dl.ActionDate >= stDate & dl.ActionDate
