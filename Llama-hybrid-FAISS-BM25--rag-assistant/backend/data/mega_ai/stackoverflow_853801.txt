[site]: stackoverflow
[post_id]: 853801
[parent_id]: 
[tags]: 
Get list of titles from xml files

I am trying to get titles of xml files from a folder call "bugs". My code: public virtual List FillBugs() { string folder = xmlStorageLocation + "bugs" + Path.DirectorySeparatorChar; List bugs = new List (); foreach (string file in Directory.GetFiles(folder, "*.xml", SearchOption.TopDirectoryOnly)) { var q = from b in bugs select new IBug { Title = b.Title, Id = b.Id, }; return q.ToList(); } return bugs; } But I'm not geting out the titles from all the xml files in the folder "bugs". the biggest problem is to get eatch files to singel string and not string[].
