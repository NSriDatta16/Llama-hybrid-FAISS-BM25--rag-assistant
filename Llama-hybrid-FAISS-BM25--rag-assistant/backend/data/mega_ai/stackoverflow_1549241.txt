[site]: stackoverflow
[post_id]: 1549241
[parent_id]: 1549200
[tags]: 
How about only you only registring one document.ready() on the master page, and then let that one execute the code based on a variable? This solution is not that elegant, but a search for JavaScript thread synchronization didn't really turn op anything. You could change the code on the master page to: var runWysiwyg = false; $(document).ready(function() { if (runWysiwyg) { $('textarea.message-body').wysiwyg(); } $("input[type='text']:enabled:first", document.forms[0]).focus().select(); }); And put this script in you page: runWysiwyg = true;
