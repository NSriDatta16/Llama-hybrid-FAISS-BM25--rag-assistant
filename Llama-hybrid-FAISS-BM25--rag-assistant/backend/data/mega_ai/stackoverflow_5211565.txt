[site]: stackoverflow
[post_id]: 5211565
[parent_id]: 5211177
[tags]: 
The best example you can find is here: http://developer.android.com/guide/topics/media/index.html#capture I hope you tested it on a phone, because: Note that the emulator doesn't have hardware to capture audio or video, but actual mobile devices are likely to provide these capabilities, accessible through the MediaRecorder class. And have a look at this: /* * The application needs to have the permission to write to external storage * if the output file is written to the external storage, and also the * permission to record audio. These permissions must be set in the * application's AndroidManifest.xml file, with something like: * * * */
