[site]: datascience
[post_id]: 86840
[parent_id]: 86769
[tags]: 
The 2D feature map is extended to the output layer and hence throwing errors at the Softmax Layer. Add Flatten Or GlobalAveragePool after Concatenate. mergedOutput = keras.layers.Concatenate()([vgg.output, resnet.output]) mergedOutput = keras.layers.GlobalAveragePooling2D()(mergedOutput) # This line x = keras.layers.Dense(256, activation="relu")(mergedOutput)
