[site]: stackoverflow
[post_id]: 4509314
[parent_id]: 4509283
[tags]: 
int i = Convert.ToInt32(e); This will work regardless of the underlying storage of the enum, whereas the other solutions will throw an InvalidCastException if the enum is stored in anything other than int32 (say, a byte or short )
