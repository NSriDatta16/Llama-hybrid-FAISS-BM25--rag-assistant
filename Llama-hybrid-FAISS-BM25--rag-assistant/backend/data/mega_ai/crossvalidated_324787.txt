[site]: crossvalidated
[post_id]: 324787
[parent_id]: 
[tags]: 
Cache/Saved feature engineering result for reusing

I use python pandas for transforming a csv with panda dataframe for feature engineering (e.g., handle missing values, extract feature etc.) and pass it to machine learning algorithm for handling. Many times when I execute, it is to tune the algorithm. But now it always rerun the whole feature engineering codes make it very slow. Is there a convenient way where if I modify feature engineering codes then it rerun the codes, and otherwise it will save the results, and reuse the results?
