[site]: stackoverflow
[post_id]: 3921290
[parent_id]: 3921257
[tags]: 
you can do this: List errors = new List (); Func , bool>[] eval = { e => { bool ret = e != null; if (!ret) errors.Add("Null"); return ret; }, However a more elegant solution would be to List errors = new List (); Func , bool> EvaluateWithError = (test, message, collection) => { if (!test) collection.Add(message); return test; }; Func , bool>[] eval = { e => EvaluateWithError(e != null, "Null", errors),
