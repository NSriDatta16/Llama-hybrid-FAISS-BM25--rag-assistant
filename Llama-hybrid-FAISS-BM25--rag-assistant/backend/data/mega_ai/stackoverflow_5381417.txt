[site]: stackoverflow
[post_id]: 5381417
[parent_id]: 5379827
[tags]: 
There is no such thing as starting a Mac .app bundle as root on startup. However, you can install a daemon process that is executed at startup (authenticated) that launches your .app bundle. For more on Launch Agents/Daemons have a look here .
