[site]: stackoverflow
[post_id]: 3867817
[parent_id]: 3867594
[tags]: 
Since characters are not DOM-elements you need to build a new DOM: CharLoop $(document).ready(function() { $("p").each(function() { var $resultDom = $(" "); var $p = $(this); var text = $p.text(); // Loop through every character in paragraph text for (var i=0, l = text.length; i " + text.charAt(i) + " "); // Set class $char.attr("class", "color" + i); // Set inline style if you don't want to hardcode alot of colors $char.css("color", "rgb(" + (i * 10) % 255 + "," + (i * 20) % 255 + "," + (i * 30) % 255 + ")"); $resultDom.append($char); } $p.empty(); $p.append($resultDom.children()); }); }); I want to put a ding in the universe. Lorem ipsum Beware of that this method might not be fast, and you are emptying the paragraph ( ) so every information that is not text will be removed, for example links ( ) will not be preserved. Result:
