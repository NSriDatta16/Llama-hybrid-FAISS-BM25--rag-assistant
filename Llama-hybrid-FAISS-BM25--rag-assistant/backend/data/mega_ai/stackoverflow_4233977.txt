[site]: stackoverflow
[post_id]: 4233977
[parent_id]: 4233967
[tags]: 
var x = $("#comm_id").val(); EDIT: actually, you don't even need to select it by ID: you could just use var x = $( "input[type=hidden]", $( this ).parents(".pane") ).val(); this would solve the problem if you have several fragments of code like that.
