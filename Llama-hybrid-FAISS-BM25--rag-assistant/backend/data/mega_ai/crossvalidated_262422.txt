[site]: crossvalidated
[post_id]: 262422
[parent_id]: 261859
[tags]: 
Decision tree algorithms like CART (Classification and Regression Trees) can handle this sort of data and Random Forests can as well. These methods will not make use of the ordering of the bins. They will simply be treat the data as labels, but they can certainly be used for regression.
