[site]: stackoverflow
[post_id]: 3852631
[parent_id]: 3852589
[tags]: 
The following is the code that I use to determine if a cell lives or dies in the game of life . private static boolean lives(Point p, byte[][] squares) { int liveNeighboors = 0; for (int i = p.x - 1; i You could adapt this to sum the numbers and return the average instead of whether or not the cell should live or die. This would need to be called in a loop like so: for(int i = 0; i
