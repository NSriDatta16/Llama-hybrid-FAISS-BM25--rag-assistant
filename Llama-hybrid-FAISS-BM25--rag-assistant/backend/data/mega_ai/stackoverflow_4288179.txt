[site]: stackoverflow
[post_id]: 4288179
[parent_id]: 4287819
[tags]: 
You need the WRITE_EXTERNAL_STORAGE permission. Also, never never never never never hardcode the path to external storage the way you are doing. Your code will break on Android 2.2 devices. Your code will break on other devices. Use Environment.getExternalStorageDirectory() , please.
