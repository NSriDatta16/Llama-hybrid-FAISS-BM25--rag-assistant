[site]: stackoverflow
[post_id]: 4116561
[parent_id]: 4116550
[tags]: 
You could select d1 or d2: $("select[name='d1'], select[name='d2']").change(function(){xxx}); But it would be more elegant if you assigned them the same class and then selected based on that: ... ... $("select.d").change(function(){xxx});
