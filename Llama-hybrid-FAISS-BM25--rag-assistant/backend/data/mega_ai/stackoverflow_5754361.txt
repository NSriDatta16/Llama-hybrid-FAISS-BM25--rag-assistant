[site]: stackoverflow
[post_id]: 5754361
[parent_id]: 5754202
[tags]: 
Sorry don't really understand your question. Just send this as the URI path. Environment.getExternalStorageDirectory()+File.separator+"testFolder" Also Check if you have the permissions to write to the sd card.
