[site]: stackoverflow
[post_id]: 4088724
[parent_id]: 4088623
[tags]: 
std::map and map are different types and do not have the same iterator type, so you cannot compare two instances of those iterators. If you're going to use typedefs, you should use them consistently: FileNameToStorageClass::iterator iter;
