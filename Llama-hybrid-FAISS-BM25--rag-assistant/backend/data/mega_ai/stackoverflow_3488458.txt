[site]: stackoverflow
[post_id]: 3488458
[parent_id]: 3488373
[tags]: 
I'll probably get downvoted for recommending short tags, but here's what I do. I put all the and ?> tags at column 1 so that the code reads like a mix of PHP and HTML. No echo statements is the goal. foreach ($details as $detail) { $d = unserialize($detail); if ($ad) { print_r($d); // get_info('orphan')) { ?> orphan get_info('class ID') ?>" style="display:none"> get_info('class number') ?> - get_info('class description') ?>"/> options(+) get_info('interval') ?>" maxlength="5"/> get_info('num riders') ?>"/> Move to Ring: Select A Ring get_info('online sign up') != 'false') { ?> Sign-Up Sign-Up get_info('water and drag')) { ?> WATER AND DRAG COURSE CHANGE &amp; WALK OTHER get_info('water and drag') ?>" maxlength="2" class="wdInput"> min WATER AND DRAG COURSE CHANGE &amp; WALK OTHER min get_info('ghost riders')) { ?> Ghost Riders get_info('ghost riders') ?>"> Ghost Riders get_info('water and drag')) { ?> 08:33am - 08:34am get_info('water and drag description') ?>">
