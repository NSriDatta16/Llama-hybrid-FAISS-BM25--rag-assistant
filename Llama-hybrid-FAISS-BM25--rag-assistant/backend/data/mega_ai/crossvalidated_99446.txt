[site]: crossvalidated
[post_id]: 99446
[parent_id]: 
[tags]: 
How to learn the points inside a square with its boundary?

Is it possible to classify the points inside a square? i.e. if $a \le x \le b$ and $c \le y \le d$ then label is $+1$ otherwise $0$. Is that possible using SVMs for example?
