[site]: crossvalidated
[post_id]: 103462
[parent_id]: 103461
[tags]: 
Two suggestions: make sure your parameters are properly tuned (in the case of SVM you will probably have $C$ and $\gamma$ for the kernel) try without feature selection (SVM is fairly robust to useless features anyway).
