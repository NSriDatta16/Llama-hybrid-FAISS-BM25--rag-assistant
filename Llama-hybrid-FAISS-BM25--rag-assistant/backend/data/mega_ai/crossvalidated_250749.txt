[site]: crossvalidated
[post_id]: 250749
[parent_id]: 248316
[tags]: 
You would probably need to use an XML reader and Natural Language Programming techniques to create features from the data which would be useful in determining relevancy. You would also have to label the URLs as relevant or irrelevant. Both the class labels and the features would be the input into your [SVM].
