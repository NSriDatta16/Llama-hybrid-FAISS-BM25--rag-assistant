[site]: datascience
[post_id]: 105104
[parent_id]: 
[tags]: 
What do x and y mean when working with data in the machine learning domain?

We need train & test data, so what do x and y mean? Does it mean that it divides 15% to the 'x_train', 'x_val', 'y_train', 'y_val' ? x_train, x_val, y_train, y_val = train_test_split(x, y, test_size=0.15, shuffle=True)
