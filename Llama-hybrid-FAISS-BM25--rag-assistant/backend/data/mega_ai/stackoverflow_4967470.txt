[site]: stackoverflow
[post_id]: 4967470
[parent_id]: 4965757
[tags]: 
Because the / is considered a path separator, Rails encourage you to separate the ID from the slug using a - which is more "neutral". See this screencast . Otherwise, you need to pass a custom :id validation and let Rails know that :id can include / , but it's not that simple.
