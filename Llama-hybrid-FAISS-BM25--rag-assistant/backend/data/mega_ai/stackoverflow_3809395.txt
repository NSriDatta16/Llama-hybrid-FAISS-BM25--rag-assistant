[site]: stackoverflow
[post_id]: 3809395
[parent_id]: 3809357
[tags]: 
And what happens if you do: echo 'a\ b\ c\ d\ e\ f' > tmpfile cat tmpfile | ~/pltest/cmd1.csh If that does work you could organise your Perl script around pre-fetching input, saving it to a temp file and then doing the above cat /path/to/tempfile | /path/to/cmd1.csh trick.
