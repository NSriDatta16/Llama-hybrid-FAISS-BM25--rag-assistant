[site]: stackoverflow
[post_id]: 4795077
[parent_id]: 4795016
[tags]: 
Try this // Add all the carriage returns to the array that you wanted to replace $carrage_returns = array("\r\n", "\n\r", "\n", "\r", " "); $replace_with = ''; // empty string $content = str_replace($carrage_returns, $replace_with, $content);
