[site]: stackoverflow
[post_id]: 1530062
[parent_id]: 1526906
[tags]: 
In addition to JacobM's suggestions: 4) If you only have to store a small amount of data (e.g. A dozen or so row indicies) and the data doesn't have to persist for too long, you could just use cookies to store the row indicies between pages. 5) Depending on your target browsers, you may be able to use DOM Storage or something like PersistJS
