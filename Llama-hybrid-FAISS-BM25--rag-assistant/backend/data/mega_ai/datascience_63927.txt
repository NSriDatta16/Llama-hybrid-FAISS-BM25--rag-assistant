[site]: datascience
[post_id]: 63927
[parent_id]: 63924
[tags]: 
Features should be same for training and test/prediction sets . You should not use (actually can't for most packages) n features for training, n-k for test/prediction etc. If the column you mentioned have NaN s for not all observations, you can try models that robust to null values. (ie: xgboost )
