[site]: crossvalidated
[post_id]: 197996
[parent_id]: 
[tags]: 
A simple question about cross-validation

I want to use SVM using 10-fold cross-validation (CV). My code is as follows: svm.model I know that in k-fold CV, k different models are trained. Which of those k models is the final svm.model ? Is it the trained model with maximum accuracy?
