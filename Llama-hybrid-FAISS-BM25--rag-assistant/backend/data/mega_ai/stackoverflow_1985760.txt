[site]: stackoverflow
[post_id]: 1985760
[parent_id]: 
[tags]: 
How does the C# compiler optimize a code fragment?

If I have a code like this for(int i=0;i Does the compiler instantinate iTemp 10 times? Or it optimize it? I mean if i rewrite the loop as int iTemp; for(int i=0;i Will it be faster?
