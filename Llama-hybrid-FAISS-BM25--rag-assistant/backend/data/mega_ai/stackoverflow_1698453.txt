[site]: stackoverflow
[post_id]: 1698453
[parent_id]: 
[tags]: 
Drupal: Creating anchor only link with l()

I'd like to output this link using the l() function, so that the link just jumps to an anchor on the current page. I expected this to work l('link', '', array('fragment' => 'namedanchor')); but it creates an absolute link to www.example.com/#namedanchor instead of www.example.com/currentpage#namedanchor
