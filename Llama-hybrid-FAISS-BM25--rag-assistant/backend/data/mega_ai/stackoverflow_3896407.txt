[site]: stackoverflow
[post_id]: 3896407
[parent_id]: 3896386
[tags]: 
The .get() callback callback uses 3 arguments: the data the status the XMLHttpRequest So, you're method should work. Make sure you pay attention to capitalization, punctuation, and whitespace. I would try and get more info about data to pinpoint the problem: var params = $('form#myform').serialize(); $.get("/ajax/file.php?"+params, function(data){ alert("Type: " + typeof.data + "\nContents: ==>" + data + " " + $.trim(data) + " Note that jQuery has a $.trim() function
