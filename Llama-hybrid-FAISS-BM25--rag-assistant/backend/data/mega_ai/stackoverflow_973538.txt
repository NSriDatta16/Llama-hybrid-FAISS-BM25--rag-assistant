[site]: stackoverflow
[post_id]: 973538
[parent_id]: 972749
[tags]: 
The regex answer is extremely fragile. Here's proof (and a working BeautifulSoup example). from BeautifulSoup import BeautifulSoup # Here's your HTML html = ' item1 item2 item3 ' # Here's some simple HTML that breaks your accepted # answer, but doesn't break BeautifulSoup. # For each example, the regex will ignore the first tag. html2 = ' item1 item2 item3 ' html3 = ' item1 item2 item3 ' html4 = ' item1 item2 item3 ' # This BeautifulSoup code works for all the examples. paragraphs = BeautifulSoup(html).findAll('p') items = [''.join(p.findAll(text=True)) for p in paragraphs] Use BeautifulSoup.
