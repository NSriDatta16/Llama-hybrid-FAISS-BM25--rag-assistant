[site]: crossvalidated
[post_id]: 590952
[parent_id]: 590951
[tags]: 
The dimension of the latent representation is a hyperparameter, so it's chosen by the researcher. Typically, the latent space has dimension much smaller than the dimension of the input because a typical goal of VAEs is to create a low-dimensional representation of the input.
