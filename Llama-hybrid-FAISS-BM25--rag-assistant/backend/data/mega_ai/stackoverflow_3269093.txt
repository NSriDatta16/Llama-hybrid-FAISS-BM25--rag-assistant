[site]: stackoverflow
[post_id]: 3269093
[parent_id]: 3268986
[tags]: 
It's quite possible that SQLiteConnection.GetSchema is broken; most programs do not need this functionality. You can get what you need by executing a PRAGMA table_info(MYTABLE) command ; you should get a data reader with one row for each column.
