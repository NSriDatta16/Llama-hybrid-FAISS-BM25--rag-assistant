[site]: stackoverflow
[post_id]: 4156449
[parent_id]: 
[tags]: 
New value is not getting updated in the jsf backing bean when submit button is clicked

I am creating a user manager page which updates and adds users in the same page. Relevant parts of the view: I am using value change listener in the dropdownlist. The bean is request scoped. The value change listener code is below: public void updateDetails(ValueChangeEvent evt) { helper = new Helper(); System.out.println(evt.getNewValue()); UserManagerDAO userManagerDAO = new UserManagerDAO(); List userDetails = userManagerDAO.getUserDetails(evt.getNewValue().toString()); //setUserName(getExistingUser()); if (evt.getNewValue().toString().equalsIgnoreCase("select")) { passwordBinding.resetValue(); userNameBinding.resetValue(); checkBoxBinding.resetValue(); //setUserName("finland"); } else { passwordBinding.setValue(userDetails.get(0)); userNameBinding.setValue(evt.getNewValue()); checkBoxBinding.setValue(helper.splitStringAndAddToList(userDetails.get(1))); } } When I click the submit button, new username and password is not getting bind to the property, instead the value is returning null. If I remove the if-else block from the valuechange event code, everything works normal. How can I solve this problem?
