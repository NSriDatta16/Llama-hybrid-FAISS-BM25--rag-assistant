[site]: stackoverflow
[post_id]: 1622732
[parent_id]: 1622711
[tags]: 
The jQuery function can turn text into jQuery extended DOM elements. So: $.getJSON( 'ajax_popup.php', function( data ) { var popupDiv = decodeURIComponent( data.data ); $('body').append( $(popupDiv).draggable() ); } );
