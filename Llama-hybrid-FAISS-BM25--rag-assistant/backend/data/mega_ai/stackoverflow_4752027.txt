[site]: stackoverflow
[post_id]: 4752027
[parent_id]: 4750426
[tags]: 
I think Lord Torgamus is on the right track. I stole this example: http://apache-wicket.1842946.n4.nabble.com/Feedback-message-does-not-show-in-new-WebPage-td2993413.html HTML fragment Java fragment public MyPage(boolean failed) { super(); FeedbackPanel fbpnlFeedbackPanel = new FeedbackPanel("fbpnlFeedbackPanel"); add(fbpnlFeedbackPanel); if (failed) { // MySession extends WebSession MySession ssnSession = (MySession)getSession(); ssnSession.error("[Error text to put in FeedbackPanel] or use a model"); ... } Form frmForm = new Form ("frmForm") { ... } ... } The fact that the session.error() is called adds enough stuff to the page to know to (a) show the feedback panel and (b) use the provided text/model in that panel.
