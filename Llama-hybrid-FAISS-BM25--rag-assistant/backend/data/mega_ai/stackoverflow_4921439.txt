[site]: stackoverflow
[post_id]: 4921439
[parent_id]: 3997770
[tags]: 
There isn't any method that cancels only one "dragging", but you can set the position of the object using ui.position return false raises an error and it doesn't work effectively. The following piece of code worked for me well. $('#draggablediv').draggable({ drag: function (event, ui) { if (your_condition) { // for example: ui.position.left
