[site]: stackoverflow
[post_id]: 1567852
[parent_id]: 1567248
[tags]: 
If you insist on multiple form usage..use Javascript in a some function like this function InnerFormSubmitter(dataForm, actionForm) { actionForm.innerHTML = dataForm.innerHTML; actionForm.submit(); } Hope this helps! Addendum on jQuery usage for your scenario: Since you wanted a link: );">Check Availability function isValidUser(userId) { var url = " / /" + userId; $.post(url, function(data) { if (data) { // callback to show valid user } else { // callback to show error/permission } }); } And you controller should have: [AcceptVerbs("POST")] public bool IsValidUser(int id) { // check availability bool allow = CheckUser(); // if allow then insert if (allow) { //insert user return true; } else { return false; } } Further Update on jQuery: Instead of document.getElementById('UserIdent').value you can use $('#UserIdent').val(); Update on JSON usage The JsonResult class should be used in the Controller and $.getJson function in the View. $(function() { $('#yourLinkOrButton').click(function() { $.getJSON(" /GetUserAvailability/", null, function(data) { $("#yourDivorLablel"). (data); }); }); public JsonResult GetUserAvailability() { //do all validation and retrieval //return JsonResult type }
