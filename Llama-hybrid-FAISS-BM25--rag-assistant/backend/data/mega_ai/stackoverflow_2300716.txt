[site]: stackoverflow
[post_id]: 2300716
[parent_id]: 2300703
[tags]: 
If you're only adding to the end of the array and you're worried about memory fragmentation, you might want to use std::list instead. The only problem is, list accesses are O(n) time if they're not at the beginning. Are you calling free()/delete on your bullets when you're done with them after pulling them out of the vector?
