[site]: stackoverflow
[post_id]: 5549819
[parent_id]: 5549724
[tags]: 
For an actual closure , this is about the only way to do it: $obj = $this; call_user_func(function () use ($obj) { echo "Goodbye, " . $obj->name; }); It's probably more elegant to pass the object as parameter as suggested in the other answers (and probably as you're already doing).
