[site]: stackoverflow
[post_id]: 2168290
[parent_id]: 2168090
[tags]: 
Figured it out! The trick they don't mention on the developer site is that for list/grid type dynamic views you actually want to merge an existing view with any stubs, rather than inserting stubs into your view, like so; This of course leaves cupcake and donut users in the dust though.
