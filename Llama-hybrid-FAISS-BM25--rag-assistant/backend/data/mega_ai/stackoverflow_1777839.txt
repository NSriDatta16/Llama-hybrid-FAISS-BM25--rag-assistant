[site]: stackoverflow
[post_id]: 1777839
[parent_id]: 
[tags]: 
Parsing an XML string fragment out of document in Javascript

I'm trying to use DOMParser, or XPath to get a XML fragment out of a document. A element with either DOMParser or document.evaluate returns an element with a null nodeValue, how do I get the xml fragment back as a string? Here's the example that I can't get to work in WebKit. XML: Source: var parser = new DOMParser(); var filterXmlDoc = parser.parseFromString(xml, "text/xml"); var test = filterXmlDoc.getElementsByTagName("filter")[0]; test.nodeValue; // null!
