[site]: stackoverflow
[post_id]: 729621
[parent_id]: 
[tags]: 
Convert String XML fragment to Document Node in Java

In Java how can you convert a String that represents a fragment of XML for insertion into an XML document? e.g. String newNode = " value "; // Convert this to XML Then insert this node into an org.w3c.dom.Document as the child of a given node?
