[site]: stackoverflow
[post_id]: 3583900
[parent_id]: 
[tags]: 
Eclipse javadoc coverage plugin

i want to find out what classes need some javadoc love in my project. Is there a plugin for eclipse (or a standalone app) that would tell me what methods don't have a javadoc yet and perhaps even tells me if some are wrong (parameters changed etc). This is more or less the last plugin i need to create great code :D thx in advance
