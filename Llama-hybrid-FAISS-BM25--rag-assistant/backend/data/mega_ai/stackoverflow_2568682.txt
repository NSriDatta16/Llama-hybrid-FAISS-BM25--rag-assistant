[site]: stackoverflow
[post_id]: 2568682
[parent_id]: 2567218
[tags]: 
You need to set either LSUIElement (Application is Agent) or LSBackgroundOnly (Application is background only) in your app's Info.plist. LSUIElement is usually for status bar apps, and LSBackgroundOnly for apps that have no UI at all. You can Apple's documentation on these flags here .
