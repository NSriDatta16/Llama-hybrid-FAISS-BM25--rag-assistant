[site]: stackoverflow
[post_id]: 5647559
[parent_id]: 5344029
[tags]: 
For jQuery.bgiframe.js, would be a better solution to fix the wrong IE6 test? What about replacing this: if($.browser.msie&&/6.0/.test(navigator.userAgent) with something like this: if ($.browser.msie && $.browser.version=="6.0")
