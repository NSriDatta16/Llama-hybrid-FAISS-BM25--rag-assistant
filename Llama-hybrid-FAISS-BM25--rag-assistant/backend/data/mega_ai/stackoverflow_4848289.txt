[site]: stackoverflow
[post_id]: 4848289
[parent_id]: 4848266
[tags]: 
use stop() in this case...... $("#address p").each(function(index) { //use stop with id of paragraph or you can also use setTimeOut().. also you can use delay() ......here it's reference delay in jquery
