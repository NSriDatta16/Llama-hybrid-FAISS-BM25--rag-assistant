[site]: stackoverflow
[post_id]: 4077087
[parent_id]: 
[tags]: 
Color Library AS3

Does a library exist for AS3 that performs color conversions and transitions such as converting a hex with alpha component to a ColorTransform? I'm basically looking for a wrapper around the Color classes of a pure AS3 project to make dealing with them more elegant.
