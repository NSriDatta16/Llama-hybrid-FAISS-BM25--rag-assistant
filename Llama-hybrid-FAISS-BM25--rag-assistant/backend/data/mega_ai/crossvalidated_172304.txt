[site]: crossvalidated
[post_id]: 172304
[parent_id]: 
[tags]: 
how to calculate the evidence in a hybrid bayesian network

The wikipedia page on bayesian networks gives a clear example on bayesian network on discrete variables, its says that My question is how this will differ if S is continuous? Or more generally how one calculates the marginal probability of a hybrid bayesian network?
