[site]: stackoverflow
[post_id]: 3150821
[parent_id]: 3150678
[tags]: 
Your map is a multidimensional array--these do not support LINQ query operations (see more Why do C# Multidimensional arrays not implement IEnumerable ? ) You'll need to either flatten the storage for your array (probably the best way to go for many reasons) or write some custom enumeration code for it: public IEnumerable Flatten (T[,] map) { for (int row = 0; row
