[site]: stackoverflow
[post_id]: 2537514
[parent_id]: 2537157
[tags]: 
You can write something like this. If Date.Now.DayOfWeek = DayOfWeek.Sunday Then ) > ElseIf Not )> is Nothing Then )> and clear )>) End If So if it's sunday, you'll just store messages and if it's not you'll update DB with stored messages and clear that storage.
