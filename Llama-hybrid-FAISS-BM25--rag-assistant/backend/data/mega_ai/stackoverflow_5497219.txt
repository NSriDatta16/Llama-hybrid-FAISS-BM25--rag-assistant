[site]: stackoverflow
[post_id]: 5497219
[parent_id]: 5497100
[tags]: 
Reasonably elegant would be (not compiled/tested): struct TempReprPoints { TempReprPoints(size_t size) { x.reserve(size); y.reserve(size); z.reserve(size); } TempReprPoints(const vector &v) { x.reserve(v.size()); y.reserve(v.size()); z.reserve(v.size()); for (size_t i = 0; i x; std::vector y; std::vector z; }; So you can fill it with a loop or even try to make the std::back_inserter work with it.
