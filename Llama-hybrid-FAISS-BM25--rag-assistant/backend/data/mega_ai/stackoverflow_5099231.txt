[site]: stackoverflow
[post_id]: 5099231
[parent_id]: 5099187
[tags]: 
I think that there's probably a more elegant solution, but this should do the trick: def get_y_axis dates = "" ed = Date.today sd = Date.today - 30 i = 0 sd.upto(ed) do |date| if i % 2 == 0 dates Just added 'i' which is incremented every iteration. Dates are only appended when i is even.
