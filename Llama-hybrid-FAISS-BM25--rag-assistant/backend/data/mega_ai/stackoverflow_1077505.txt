[site]: stackoverflow
[post_id]: 1077505
[parent_id]: 1077457
[tags]: 
You use SMO to manage the SQL Server Agent, which is what you use to schedule an SSIS package (or packages). Use the JobServer object to get started. You may want to create the job manually in SQL Server Management Studio, then just use SMO to adjust the schedule.
