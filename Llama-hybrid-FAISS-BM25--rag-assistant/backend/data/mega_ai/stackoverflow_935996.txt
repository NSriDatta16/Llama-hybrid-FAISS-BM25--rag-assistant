[site]: stackoverflow
[post_id]: 935996
[parent_id]: 
[tags]: 
Calculating the Cartesian product of a list of numbers with F#

I am new to f# I am try to calculate the Cartesian products of a list of numbers. I "borrowed" this. let xs = [1..99] let ys = [1..99] seq {for x in xs do for y in ys do yield x * y} Is there a better or more elegant way? Gary
