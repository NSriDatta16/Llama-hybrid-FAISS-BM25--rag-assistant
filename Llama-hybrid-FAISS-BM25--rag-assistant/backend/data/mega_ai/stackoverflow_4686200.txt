[site]: stackoverflow
[post_id]: 4686200
[parent_id]: 4686019
[tags]: 
Off the top of my head in jQuery: $('.numbers').each(function() { $(this).html( $(this).html().substr(0, $(this).html().length-2) + " " + $(this).html().substr(-2) + " "); }); Here's a fiddle to demonstrate it. I'm sorry it's not plain JavaScript, but I'm sure one of the folks here can offer a native solution if this doesn't cut your corn. UPDATE: And here's a non-jQuery solution, and another fiddle demonstrating it. I also broke the style out into CSS. .unimportant { color: #3399ff; } var numberTargets = document.getElementsByClassName('number'); for(i=0; i " + numberTargets[i].innerHTML.substr(-2) + " "; }
