[site]: crossvalidated
[post_id]: 247942
[parent_id]: 247939
[tags]: 
Yes, images should be of the same size. If you will be using convolutional neural network like LeNet you should store each image in matrix (WxHxColorChannel). For simple algorithms like kNN images are reshaped to single column vector.
