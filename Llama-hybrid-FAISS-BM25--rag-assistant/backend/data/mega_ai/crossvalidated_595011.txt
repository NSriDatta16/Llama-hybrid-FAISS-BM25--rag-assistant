[site]: crossvalidated
[post_id]: 595011
[parent_id]: 
[tags]: 
Why is max pooling preferred over average pooling in convolutional neural networks?

In most CNN guides max pooling is explained and it is just mentioned that also average pooling exists. Why is max pooling preferred over average pooling? Average (or even median) seems to make more sense to me?
