[site]: stackoverflow
[post_id]: 800780
[parent_id]: 800762
[tags]: 
That seems OK to me. Client-side validation in this specific case sounds a little fuzzy, like maybe checking for some element you expect to have part of a server-generated ID. Very fragile. Usually I will go one extra step and actually create my own validator control which inherits from BaseValidator . Makes encapsulation and SoC cleaner, and the validation logic portable as well.
