[site]: stackoverflow
[post_id]: 2363351
[parent_id]: 2363337
[tags]: 
You could build the new element’s content in a DocumentFragment before inserting it into the actual document: var fragment = document.createDocumentFragment(); // build node in fragment var div = /* … */; // DIV that should be replaced div.parentNode.replaceChild(fragment, div);
