[site]: stackoverflow
[post_id]: 3536429
[parent_id]: 3535655
[tags]: 
Here you go, you can tweak as needed. $StartTime = Get-Date { } $TimeSpan = New-TimeSpan $StartTime (Get-Date) $AverageTimeSpan = New-TimeSpan -Seconds ($TimeSpan.TotalSeconds / $LoopCount) Seconds is the best unit to use for this.
