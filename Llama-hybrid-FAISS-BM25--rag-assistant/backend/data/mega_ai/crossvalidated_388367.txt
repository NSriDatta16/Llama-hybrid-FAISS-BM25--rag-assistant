[site]: crossvalidated
[post_id]: 388367
[parent_id]: 388364
[tags]: 
Typically, the gradients calculated using the error on each sample from the batch are accumulated, then averaged. The average of the accumulation of gradients is then what is used to update the weights.
