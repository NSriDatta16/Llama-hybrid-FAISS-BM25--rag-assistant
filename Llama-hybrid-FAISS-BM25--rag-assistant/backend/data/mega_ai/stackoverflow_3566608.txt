[site]: stackoverflow
[post_id]: 3566608
[parent_id]: 3566424
[tags]: 
Reorganizing @st0le's answer, to group the data that's used together, together: mcursor = getCursor(); //or whatever if (mcursor != null && mcursor.moveToFirst()) { do { Double tlat = mcursor.getDouble(5); Double tlng = mcursor.getDouble(6); GeoPoint point = new GeoPoint(tlat,tlng); String tname = mcursor.getString(1); String tmessage = mcursor.getString(2); OverlayItem overlayitem = new OverlayItem(point, tname, tmessage); itemizedoverlay.addOverlay(overlayitem); mapOverlays.add(itemizedoverlay); } while(mcursor.moveToNext()); } Which shows that we're not even using tlink & tsname, so I deleted them.
