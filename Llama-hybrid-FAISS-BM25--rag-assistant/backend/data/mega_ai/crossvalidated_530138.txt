[site]: crossvalidated
[post_id]: 530138
[parent_id]: 
[tags]: 
Find Bayesian estimator for $e^{\theta}$

Given $\{Y_i\}_n\sim U(\theta-1,\theta+1)$ and prior distribution $\theta\sim U(a,b),1\leq a is the posterior distribution conjugate? Find the absolute error estimator for $e^{\theta}$ and credible interval. I've found that $\pi(\theta|Y)=(\frac{1}{2})^{n}(\frac{1}{b-a})\mathbb{I}\{max(Y_{max}-1,a)\leq\theta\leq min(y_{min}+1,b)\}$ I think that the posterior is conjugate when $Y_i$ are given but not sure about how to find the estimator.
