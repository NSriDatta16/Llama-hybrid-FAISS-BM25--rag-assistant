[site]: stackoverflow
[post_id]: 5314388
[parent_id]: 5314313
[tags]: 
You are trying to pass a value of type multimap as a pointer to that type, i.e. multimap * . Either change the function signature to void calculateAverage(const multimap & q) and modify its code accordingly (replace -> with . ), or call it like this: calculateAverage(&movies) .
