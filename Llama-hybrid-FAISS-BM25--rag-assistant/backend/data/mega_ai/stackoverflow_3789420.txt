[site]: stackoverflow
[post_id]: 3789420
[parent_id]: 3784075
[tags]: 
val it = new Iterable[Int] { def iterator = Iterator.iterate(0)(_+1) override def toString: String = "Infinite iterable" } Don't try it out on REPL (except by embedding it inside an object or class), as REPL will try to print it, and it doesn't use toString .
