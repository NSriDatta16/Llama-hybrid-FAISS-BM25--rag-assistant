[site]: stackoverflow
[post_id]: 5438700
[parent_id]: 5438576
[tags]: 
is this all the code you have? the first frame has everything listed above and there is a second frame(which should exist) which has no code in it? EDIT: The code above works perfectly fine, you're error is coming from somewhere else. On the side, you shouldn't create a new thread for the same error: Tracing flash to a line which doesn't exist, flash CS5/AS3 Error #1034 while running flash application just put the entire question in one thread, or people will be discouraged from answering.
