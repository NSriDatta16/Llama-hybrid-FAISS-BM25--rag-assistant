[site]: stackoverflow
[post_id]: 5060397
[parent_id]: 5020695
[tags]: 
Thanks for figuring out a solution. However i really needed the destroy method... so after using your solution, i assigned the native destroy method to another method... so your solution with mine would be something like this; $.ui.draggable.prototype.destroy = function (ul, item) { }; $.ui.draggable.prototype.remove = function() { if(!this.element.data('draggable')) return; this.element .removeData("draggable") .unbind(".draggable") .removeClass("ui-draggable" + " ui-draggable-dragging" + " ui-draggable-disabled"); this._mouseDestroy(); return this; };
