[site]: stackoverflow
[post_id]: 3609417
[parent_id]: 3609364
[tags]: 
Try connecting to your database with the same connection string in SQL Server Management Studio. Does it connect? If not (and I doubt it will), you have the wrong connection string. Edit: Additionally, make sure the SQL Server and SQL Agent services are running.
