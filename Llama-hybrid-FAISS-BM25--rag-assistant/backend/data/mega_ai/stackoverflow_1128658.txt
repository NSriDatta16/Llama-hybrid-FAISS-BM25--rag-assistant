[site]: stackoverflow
[post_id]: 1128658
[parent_id]: 1128617
[tags]: 
you can specify the default encoding on the command line when the app is started: java -Dfile.encoding=UTF8 au.com.objects.MyClass If nothing is specified then the default is got from the underlying OS as AlbertoPL explains above.
