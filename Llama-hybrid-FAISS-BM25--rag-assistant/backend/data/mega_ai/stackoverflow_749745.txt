[site]: stackoverflow
[post_id]: 749745
[parent_id]: 746070
[tags]: 
I've achieved something similar using basic HTML fragments . You just leave an element with a known ID: And then either via script, on on the server side change the url: window.location += "#CONTROL-ID"; In the first case the page won't reload, it will just scroll down to the control.
