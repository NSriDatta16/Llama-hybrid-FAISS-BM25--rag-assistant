[site]: stackoverflow
[post_id]: 2500297
[parent_id]: 
[tags]: 
Current object value in jquery

All, in the below code How to edit the current added value.i.e, in the edit function how to get the value in td .Also how can we check for duplicates. function edit(obj) { // var ele= obj.parentNode ; alert(obj.innerHTML); var htm = ' '+ele.html+' '; $dialog.html(htm) .dialog({ autoOpen: true, position: 'center' , title: 'EDIT', draggable: false, width : 300, height : 400, resizable : false, modal : true, buttons: { "Save" : function() { if($('#modal').val() != ''){ obj.innerHTML=$('#modal').val() ;$dialog.dialog('close');} else {alert('i++'+$('#modal').val()+'++');} } } }); $dialog.dialog('open'); } $(document).ready(function() { var flag=0; if (flag == 0) { var html = ' &nbsp;&nbsp;&nbsp; '; $('#useroptions').append(html); flag=1; $('#add').click(function(){ var a = $('#val').val(); if (a == '') { alert('Enter options'); } else { var section= ' ' + a + '&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; '; $('#useroptions').append(section); } } } }); });
