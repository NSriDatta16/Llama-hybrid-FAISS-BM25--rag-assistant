[site]: stackoverflow
[post_id]: 3612489
[parent_id]: 3612343
[tags]: 
Please create methods for your clauses, you are scaring me. var query_loc = (from at in db.amenities_types join a in db.amenities on at.id equals a.amenities_type join u in db.unitInfos on a.unit_id equals u.id join l in db.locations on u.locations_id equals l.id join o in db.organizations on l.organization_id equals o.id join ot in db.organization_types on o.id equals ot.organization_id where PriceIsValid(u) && BedsAndBathsArevalid(u) && AtIdIsValid(at.id) && SomeCrazyDateConditionIsValid(ot, l) && TheOtTypeIsValid(ot) orderby o.name ascending, l.name ascending select new { l, o, u, ot, at }); And if you mean at.is = 29 OR at.id = 30 OR at.id = 40, then use an AtIdIsValid(at.id) predicate like: bool AtIdIsValid(int atId){ return (atId == 29 || atId == 30 || atId == 40); }
