[site]: crossvalidated
[post_id]: 390681
[parent_id]: 386677
[tags]: 
If I learned this right, you use the MCMC estimates essentially as replacement for the M step, in an EM like process. The idea is simple: if we cannot solve the maximum likelihood problem exactly, we use anumeric approximation instead.
