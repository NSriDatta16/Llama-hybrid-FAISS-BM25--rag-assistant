[site]: stackoverflow
[post_id]: 5494730
[parent_id]: 
[tags]: 
Jquery moving elements from one div to another acting buggy

I have 3 divs(#col1, #col2 and #col3) which contain keywords wrapped in span tags. When a user clicks on a keyword/span it moves out of #col1 or #col2 and into #col3. Then when the user clicks on #col3 span item it moves out of #col3 and back into it's original div. PROBLEM I can get the keywords to move to #col3 but I can't move them back out. #col3 keeps calling/using the #col1 or #col2 function. I tried using remove() but that didn't help. $("#col1 span, #col2 span").click(function(){ var tag = this.id; //$(this).remove(); $(this).appendTo('#col3'); $('#'+tag).wrap(" "); }); $('#col3 span').click(function(){ //move back to original div (#col1 or #col2) //I can't figure this part out either }); Unilevel Person-to-Person Retail Bonuses Multi-Level/Direct Sales Binary Matrix Hybrid Nutritional Supplements Super Juices Skin Care Cosmetics Fragrances Hair Care Jewelry
