[site]: stackoverflow
[post_id]: 933638
[parent_id]: 915366
[tags]: 
I ended up using NSURLRequests, and then storing the retrieved HTTP body and cookies to NSUserDefaults. After the re-entry to the app I read these and set them again on a UIWebView and in the NSHTTPCookieStorage, and then clearing the NSUserDefaults for these keys.
