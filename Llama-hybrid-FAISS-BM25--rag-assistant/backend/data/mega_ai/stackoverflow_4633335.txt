[site]: stackoverflow
[post_id]: 4633335
[parent_id]: 4632862
[tags]: 
Why not do it more elegantly: $sql = " SELECT title, created_date FROM todos WHERE list_id = ? AND checked = ? ORDER BY created_date DESC"; $stmt = $link->prepare($sql); $stmt->execute(array($_REQUEST['list_id'], '0')); # do stuff # and later... $stmt->execute(array($_REQUEST['list_id'], '1')); This avoids code duplication and (depending on the DB backend) should be faster too.
