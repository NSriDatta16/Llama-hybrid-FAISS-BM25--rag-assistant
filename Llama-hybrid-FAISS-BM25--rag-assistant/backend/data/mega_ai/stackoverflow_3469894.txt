[site]: stackoverflow
[post_id]: 3469894
[parent_id]: 3469299
[tags]: 
You're already using generics, so why not go all the way? void GetMemberType (Expression > path) { // Get the member ... var member = path.Body as MemberExpression; // ... Do whatever you want with the member ... // Now get the type var memberType = typeof(TProperty); }
