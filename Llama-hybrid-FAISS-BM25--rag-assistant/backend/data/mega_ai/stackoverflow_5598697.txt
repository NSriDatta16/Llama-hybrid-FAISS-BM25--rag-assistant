[site]: stackoverflow
[post_id]: 5598697
[parent_id]: 5598617
[tags]: 
or more fully: $(document).ready(function(){ $('#hdr').append($('#frag').html()); }); or without the script tag at all... //content ... $(document).ready(function(){ $('#hdr_add').load('frag.html'); }); .load() is a shortcut AJAX call that sweet and simply loads content from a give URL into the given element.
