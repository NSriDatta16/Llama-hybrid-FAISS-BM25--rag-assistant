[site]: stackoverflow
[post_id]: 2754279
[parent_id]: 2754230
[tags]: 
Window.Location.assign(newUrl) But in general, you want to avoid doing so, because you would loose all client state. You are better off changing the view via code, and then updating History (which updates the fragment url - the portion of the url after #).
