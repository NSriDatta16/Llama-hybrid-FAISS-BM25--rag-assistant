[site]: stackoverflow
[post_id]: 1787836
[parent_id]: 
[tags]: 
How to tell 'PowerShell' Copy-Item to unconditionally copy files

The following PowerShell script works if the resources are not there. Copy-Item $src_dir $dst_dir$agent_folder -recurse But if the resources are there, it will say: + Copy-Item What do I have to add to the command so that it will unconditionally copy the files?
