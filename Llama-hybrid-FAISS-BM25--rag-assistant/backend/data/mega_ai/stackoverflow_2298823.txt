[site]: stackoverflow
[post_id]: 2298823
[parent_id]: 2298763
[tags]: 
acceleration = (force / mass) velocity = (acceleration * time) (force from user's finger) = acceleration / mass = velocity / time give the view a mass (tweak it until things seem reasonable, and let the user tweak it) identify some new force (drag) give the new force (drag) a magnitude (tweak until reasonable, let the user tweak it) apply the new force to the object and watch it slow down
