[site]: crossvalidated
[post_id]: 129358
[parent_id]: 
[tags]: 
Can you generate confidence intervals for time series ETS forecast components?

Suppose you fit a time series with the ets function from the forecast package in R : library(forecast) fit Is it possible to generate confidence intervals for the parameters (e.g. alpha , beta , gamma )? If methods exist, are they restricted to certain taxonomies? That is, can this method be extended to ARIMA models?
