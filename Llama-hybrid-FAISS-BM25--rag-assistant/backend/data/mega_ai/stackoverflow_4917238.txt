[site]: stackoverflow
[post_id]: 4917238
[parent_id]: 4917188
[tags]: 
each line must be only one label While I think using 2 labels per "line" would be more elegant, you could solve this by having your labels use a fixed width font. This would make it so that each char was exactly the same width, and you could figure out how to "fill" appropriately.
