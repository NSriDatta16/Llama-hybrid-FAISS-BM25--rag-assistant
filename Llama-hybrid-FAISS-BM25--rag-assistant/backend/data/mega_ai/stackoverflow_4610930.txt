[site]: stackoverflow
[post_id]: 4610930
[parent_id]: 4573936
[tags]: 
I tried doing it by incrementally passing the width parameter in size to sizeToFit and looking at the resulting height to give cues in terms of whether the size is ok. But this is not a elegant solution for (int index = 100; index { label.width = x; if (label.height }
