[site]: stackoverflow
[post_id]: 4889223
[parent_id]: 4889075
[tags]: 
It should be easy with Perl and LWP::UserAgent : #!/usr/bin/perl use LWP::UserAgent; open my $fh, "links.txt" or die $!; my @links = ; my $ua = LWP::UserAgent->new; for my $link (@links) { my $resp = $ua->get($link); # automatically follows redirects if ($resp->is_success) { print $resp->request->uri, "\n"; } }
