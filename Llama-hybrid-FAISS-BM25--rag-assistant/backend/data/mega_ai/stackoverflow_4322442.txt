[site]: stackoverflow
[post_id]: 4322442
[parent_id]: 
[tags]: 
SVG Element Grouping without tag

The SVG tag doesn't work in chrome. How do I group svg shapes like rect,circle,path without using SVG use tag ? EDIT: But when i drag the element in iframe its not move all the element that contain is there any other way to get i all child element and drag them with loop?????
