[site]: stackoverflow
[post_id]: 1005841
[parent_id]: 1004866
[tags]: 
private methods are usually an indicator that they don't need to be directly tested because you will be testing methods that use these private methods. That method feels like it belongs in factory or builder class, not as a private method. It won't be elegantly testable if you keep instantiation code hidden as a private method.
