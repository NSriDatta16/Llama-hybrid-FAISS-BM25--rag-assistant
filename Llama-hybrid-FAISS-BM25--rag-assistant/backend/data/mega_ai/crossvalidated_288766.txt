[site]: crossvalidated
[post_id]: 288766
[parent_id]: 
[tags]: 
Finetunng a deep network

Can we perform fine tuning using the pre-trained weight file when the network is slightly changed? I have changed the CNN from classification to regression network and rest of the network is the same. I have a pretrained model file for classification.
