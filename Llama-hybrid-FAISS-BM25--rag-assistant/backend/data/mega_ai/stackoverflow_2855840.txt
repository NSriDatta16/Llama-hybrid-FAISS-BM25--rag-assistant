[site]: stackoverflow
[post_id]: 2855840
[parent_id]: 2651739
[tags]: 
I would port your css to Javascript, this enables you to get the figure caption and also you get greater browser coverage. Using jQuery you'd do something like this: $(function() { var section = 0; $(".section").each(function() { section++; var figure = 0; $(this).find("img.figure").each(function() { figure++; var s = "Fig. " + section + "." + figure; $(this).attr({alt:s}).after(s); }); }); }); Then you could do: blabla Lorem ipsum dolor sit amet see here $(function() { $("a.figurereference").each(function() { var selector = $(this).attr("href"); var $img = $(selector); var s = $(this).text() + " (" + $img.attr("alt") + ")"; $(this).text(s); }); }); Though I agree that doing this using CSS would be very neat.
