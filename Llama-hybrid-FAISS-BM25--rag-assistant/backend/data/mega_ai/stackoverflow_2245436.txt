[site]: stackoverflow
[post_id]: 2245436
[parent_id]: 
[tags]: 
Magento Checkout : Get Subtotal Value without iterating

getTotals() as $total) { if ($total->getCode() == 'subtotal') { $subtotal = $total->getValue(); break; } } echo $subtotal; ?> Any way to get subtotal directly ?
