[site]: stackoverflow
[post_id]: 708711
[parent_id]: 708684
[tags]: 
If your input file is blank, then the vector should be empty. You shouldn't go past the line: if (in.is_open()) -- your program (when I run it on my machine) quits. Can you explain why you have the following? #include using namespace System; #pragma hdrstop The easiest to implement is to read in the file contents in one go, keep the items in memory, edit them in-memory and write out to file once through with editing. This is of course not a very good technique from memory footprint or performance point of view. But then, the problem you are trying to solve is not trivial. Read FAQ 36 to gain a better understanding of your task.
