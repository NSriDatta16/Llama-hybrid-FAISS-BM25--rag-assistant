[site]: datascience
[post_id]: 76732
[parent_id]: 76710
[tags]: 
I think you should use a pre-trained neural network for image recognition and adjust the weights to detect the individual objects you need. Afterwards, you will need to combine this with some good old-fashioned scripts to manually calculate the score. Deep Learning doesn't do magic, even less with
