[site]: stackoverflow
[post_id]: 3351492
[parent_id]: 3350951
[tags]: 
Here is a recursive, inefficient Java solution: private static void findSolutions(Set fragments, String target, HashSet solution, Collection > solutions) { if (target.isEmpty()) { solutions.add(solution); return; } for (String frag : fragments) { if (target.startsWith(frag)) { HashSet solution2 = new HashSet (solution); solution2.add(frag); findSolutions(fragments, target.substring(frag.length()), solution2, solutions); } } } public static Collection > findSolutions(Set fragments, String target) { HashSet solution = new HashSet (); Collection > solutions = new ArrayList >(); findSolutions(fragments, target, solution, solutions); return solutions; }
