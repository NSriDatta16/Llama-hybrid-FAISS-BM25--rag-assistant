[site]: datascience
[post_id]: 90996
[parent_id]: 90994
[tags]: 
You can try transforming the target using MixMaxScaler so that both train and test set's target is between 0 and 1. from from xgb import XGBRegressor from sklearn.compose import TransformedTargetRegressor from sklearn.metrics import r2_score from sklearn.preprocessing import MinMaxScaler regressor = XGBRegressor() model = TransformedTargetRegressor(regressor= regressor, transformer = MinMaxScaler() ).fit(trainX,trainY) pred = model.predict(testX) print(pred)
