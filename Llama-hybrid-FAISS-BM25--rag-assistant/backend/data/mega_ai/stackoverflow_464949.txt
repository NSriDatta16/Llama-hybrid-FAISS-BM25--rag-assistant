[site]: stackoverflow
[post_id]: 464949
[parent_id]: 464862
[tags]: 
I assume you're having problems with Firefox, which ignores the object tag, and uses the embed tag instead. You need to add the flashvars parameter as an attribute there as well: Alternatively, you could use SWFObject to dynamically generate the embedding code.
