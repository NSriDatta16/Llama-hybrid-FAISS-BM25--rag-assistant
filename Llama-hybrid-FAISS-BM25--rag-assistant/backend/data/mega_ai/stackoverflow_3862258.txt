[site]: stackoverflow
[post_id]: 3862258
[parent_id]: 3862142
[tags]: 
I guess what you like to do ist this: var subcontainer = $(' ').attr({id:expandedRowContainer_id+'_'+child.subId}); createDocumentFragment() is a method of document. $(' ') ...will create a new DocumentFragment. If you provide the 2nd parameter, this parameter will be taken as the context(should be a document, what it's not actually). That causes the error.
