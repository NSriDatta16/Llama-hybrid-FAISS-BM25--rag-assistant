[site]: stackoverflow
[post_id]: 3941779
[parent_id]: 3941714
[tags]: 
Well, I need more info. How does the HTML look? What kind of data are you extracting? Also why C? Although DOM creating is fast in C, but afterwards string manipulations need some effort. Why not Python? Anyway here's the xpath you could try. //table[@class='table_class'] This gives all tables in your HTML page having classname as 'table_class'. You could change this to how your HTML is organized.
