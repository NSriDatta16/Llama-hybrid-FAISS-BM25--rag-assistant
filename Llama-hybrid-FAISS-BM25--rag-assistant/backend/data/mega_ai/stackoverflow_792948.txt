[site]: stackoverflow
[post_id]: 792948
[parent_id]: 792924
[tags]: 
Yes you can run the Python code from C by embedding the interpreter in your program. You can expose portions of your C code to Python and call your exposed C code from Python as if they were normal Python functions. A good start is the Embedding section in the Python docs. Also have a look at the article linked to by cobbal.
