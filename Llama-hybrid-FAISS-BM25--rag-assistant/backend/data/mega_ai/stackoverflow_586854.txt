[site]: stackoverflow
[post_id]: 586854
[parent_id]: 586836
[tags]: 
I don't see any reason you can't. CC.Net doesn't actually compile anything. It's just the engine that initiates the build process. If you can build via commandline then you can build it via CC.Net. You might want to look at Nant to do source code retrieval and the execution of the compiler.
