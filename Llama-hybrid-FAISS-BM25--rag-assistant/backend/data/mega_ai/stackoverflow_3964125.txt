[site]: stackoverflow
[post_id]: 3964125
[parent_id]: 3964075
[tags]: 
I'm assuming that Lowered User name is unique, otherwise the query would be fairly meaningless, so you can just use. context.Users .Where(u => u.LoweredUserName == userName.ToLower()) .Select(u => u.Organisations.Select(o => o.Name));
