[site]: stackoverflow
[post_id]: 301687
[parent_id]: 270623
[tags]: 
This works for embedding your ActiveX container in an html page and invoking a method: var activexObj = document.getElementById('myControl'); if(activexObj != null) { var result = myControl.myMethod(); document.write("Result: " + result + " "); } else { document.write("ActiveX component not found! "); }
