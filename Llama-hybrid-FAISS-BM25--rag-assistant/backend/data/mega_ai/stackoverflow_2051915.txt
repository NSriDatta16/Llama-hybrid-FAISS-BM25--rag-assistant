[site]: stackoverflow
[post_id]: 2051915
[parent_id]: 
[tags]: 
jQuery Drag Drop - how to fetch the dropped value in the server end?

I have an application which uses jQuery UI to drag a LI from one UL to another. the asp code for the same is as follows: $(function() { $("#selectedUsers").sortable({ connectWith: ["#userList"] }); $("#userList").sortable({ connectWith: ["#selectedUsers"] }); }); Tea Coffee Juice User list My question is.. How do I capture the dropped LI element at the server end? I wanted the jQuery to return the id/value of this dropped LI to the server end.. Thanks in advance
