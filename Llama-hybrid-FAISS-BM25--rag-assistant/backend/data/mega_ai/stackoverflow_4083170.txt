[site]: stackoverflow
[post_id]: 4083170
[parent_id]: 4083147
[tags]: 
Well you wouldn't render the partial like that, pass it a strongly-typed ViewModel, like this: contentModel is the ViewModel (just a POCO-like storage mechanism for your views), which the strongly typed partial view would bind to. So you can do this in your partial view: etc etc
