[site]: stackoverflow
[post_id]: 5243941
[parent_id]: 5243890
[tags]: 
Well, obviously you can do it in a C-like way. Right now I can't figure out more elegant solution. vector;%Your vector vec_length = length(vector); A = zeros(vec_length); for i=1:vec_length A(i,i) = vector(i); end;
