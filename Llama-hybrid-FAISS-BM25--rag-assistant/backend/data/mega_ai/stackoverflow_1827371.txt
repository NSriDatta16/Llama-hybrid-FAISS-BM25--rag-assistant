[site]: stackoverflow
[post_id]: 1827371
[parent_id]: 
[tags]: 
Why doesn't delegate/datasource show in IB?

I have a uiviewcontroller declared like this: @interface RootViewController : UIViewController I've dragged a tableview onto the rootcontroller's view. When clicking the File's Owner, all I see is tableview. Where is the delegate and datasource, which need to be connected for the tableview to work?
