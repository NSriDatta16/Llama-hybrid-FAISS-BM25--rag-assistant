[site]: stackoverflow
[post_id]: 3958090
[parent_id]: 
[tags]: 
Find out the Proxies WPAD is returning

our organization uses the WPAD method for setting proxies in IE . Is there a way to actually find out the actual proxy IE is using for a URL ? . i need this to manually set up the proxy and not use the WPAD option . Any help is appreciated . Thanks and Regards
