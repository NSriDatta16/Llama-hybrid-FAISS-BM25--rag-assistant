[site]: stackoverflow
[post_id]: 5078992
[parent_id]: 5014893
[tags]: 
I've had a similar problem and found this question in Silverlight forum. Maybe it could help you. How to Highlight a particular WRONG word in Textbox to make SpellCheck feature This is how I would implement the search function: private void Find(RichTextBox richTextBox, string term) { var builder = new StringBuilder(); var inlines = richTextBox.Blocks .OfType () .SelectMany(paragraph => paragraph.Inlines); foreach( var inline in inlines ) { builder.Append(((Run)inline).Text); } var regex = new Regex(term); var matchedStrings = regex.Matches(builder.ToString()); foreach( var item in matchedStrings ) { // Whatever you want to do. } }
