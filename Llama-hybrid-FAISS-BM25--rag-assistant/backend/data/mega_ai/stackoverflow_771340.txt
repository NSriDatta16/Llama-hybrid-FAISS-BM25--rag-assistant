[site]: stackoverflow
[post_id]: 771340
[parent_id]: 771182
[tags]: 
Sockets in .NET deals with packets as a continuous stream of data, not on packet-by-packet basis. That's why EndReceive will block until data is available, or until the connection is ended. If you want to capture the received data in a packet-by-packet basis, I think you need to use something like WinPcap library
