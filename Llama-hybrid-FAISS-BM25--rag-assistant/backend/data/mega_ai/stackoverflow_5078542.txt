[site]: stackoverflow
[post_id]: 5078542
[parent_id]: 5078444
[tags]: 
Well, the WHERE col IS NOT NULL may use the NULL bitmap but it's a clustered index scan. Or you just index the column and take the hit. Or use an indexed view with the IS NOT NULL filter. Unfortunately, this isn't resolved elegantly until " filtered indexes " SQL Server 2008
