[site]: stackoverflow
[post_id]: 3329623
[parent_id]: 3329598
[tags]: 
That is an old game, though the usual form is like struct foo { size_t size char data[1] } and then allocate the space as big as you want and use array as if it had the desired size. It is valid, but I would encourage you to find another way if possible: there are lots of chance to screw this up.
