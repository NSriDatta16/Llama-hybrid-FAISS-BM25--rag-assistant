[site]: stackoverflow
[post_id]: 3877504
[parent_id]: 3877484
[tags]: 
The issue is that once you exit the first while loop, you're never returning back to it. Try something like this: while true response = gets.strip if response == response.upcase puts msg1 else puts msg2 end end That'll run forever, until you decide to kill virtual-granny with Ctrl-C.
