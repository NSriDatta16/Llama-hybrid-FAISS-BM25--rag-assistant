[site]: stackoverflow
[post_id]: 5365254
[parent_id]: 5365194
[tags]: 
You can slice the result list with [1:] for line in results: scores = line.split(';')[1:] scores = map(float, scores) # Conversion to float average = sum(scores)/len(scores) that's also more general, because you won't depend on number of scores, just reject the first element :)
