[site]: stackoverflow
[post_id]: 3743944
[parent_id]: 3743902
[tags]: 
If it would make sense, clip long strings with '...' Otherwise, change the sample data to be the same length as maximum allowed values. You can even use multiple sample data sources: one for minimum lengths, one for average lengths, and one for maximum lengths. Ask the designer to check the layout using each one.
