[site]: stackoverflow
[post_id]: 5168977
[parent_id]: 5160754
[tags]: 
In Kohana 3.1 you should use Request->post(): Request::current()->post() or if in your controller: $this->request->post() Since Kohana is HMVC, you can call sub-requests with dedicated post data, so using the superglobal $_POST is discouraged, since it's not unique to the request.
