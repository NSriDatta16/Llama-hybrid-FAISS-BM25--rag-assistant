[site]: datascience
[post_id]: 108043
[parent_id]: 46873
[tags]: 
I'm a bit late but since I had the same problem, here is how I solved it. It's not the most elegant way but I just created new columns for all the dummy variables created with the dummyVars() function, and then assigned the values manually. So if "df" is our new data frame and x_train is the one we created with the dummyVars() function, I used: existing_cols
