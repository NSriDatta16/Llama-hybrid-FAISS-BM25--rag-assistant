[site]: crossvalidated
[post_id]: 171999
[parent_id]: 147225
[tags]: 
I'm not sure if you are still looking for an answer, but you can use a bigram (or n-gram) tokenizers. scores = c(2, 7, 5, 4, 4, 6, 8) feedback = c("Cant connect", "Excellent Service but i had trouble with login", "Need more locations", "Trouble with connection", "Cant connect", "Login issues", "You need more locations") df = data.frame(scores, feedback) # adjust the number for a different n-gram. NLPBigramTokenizer
