[site]: stackoverflow
[post_id]: 2895222
[parent_id]: 2895188
[tags]: 
Because certain rules make sense by default: Table cells have display:table-cell; makes text bold, makes text italic Paragraphs should have space in between them Unordered lists should be displayed as bulleted lists The problem isn't that browsers have defaults, it's that the defaults are all different.
