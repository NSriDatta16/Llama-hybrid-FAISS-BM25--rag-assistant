[site]: stackoverflow
[post_id]: 5194386
[parent_id]: 5193321
[tags]: 
I have a work around if anyone else is having similar difficulty. It ain't elegant but it works. Grab the JSON response from the JQuery request function and parse it and load it into the table as an Array. As I say it isn't elegant but it works.
