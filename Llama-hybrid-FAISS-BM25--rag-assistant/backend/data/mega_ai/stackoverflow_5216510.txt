[site]: stackoverflow
[post_id]: 5216510
[parent_id]: 
[tags]: 
In jquery sortable UI,What can we get from the event function's parameters like ui and event?

I know we can get the current dragged element's id by ui.item.attr('id') but what can we get from event parameter? Could it like javascript, have event.target property? or get anything else?
