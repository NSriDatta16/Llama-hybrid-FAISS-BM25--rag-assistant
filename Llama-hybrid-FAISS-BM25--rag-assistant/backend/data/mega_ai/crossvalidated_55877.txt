[site]: crossvalidated
[post_id]: 55877
[parent_id]: 
[tags]: 
Interpretation of probabilities from a mixed-model logistic regression

In the following model specification, which is a random intercept 2-level logistic regression: Would two lower level units ($i$) with the same value of $x_{1ij}$ and within the same higher level unit ($j$) but with different values for $y_{ij}$ have the same predicted $\pi_{ij}$?
