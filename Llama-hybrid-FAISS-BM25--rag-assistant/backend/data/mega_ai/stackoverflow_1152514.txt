[site]: stackoverflow
[post_id]: 1152514
[parent_id]: 1152500
[tags]: 
You can do this easily enough in the designer (SSMS) - just drag the columns around to the correct order and then save - in the background it will script out and re-create the table. But why do you need to do it? Order of columns has no impact other than making things look pretty in the designer.
