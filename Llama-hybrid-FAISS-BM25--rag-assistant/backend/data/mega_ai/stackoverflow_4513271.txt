[site]: stackoverflow
[post_id]: 4513271
[parent_id]: 4513078
[tags]: 
You can write (inc i) to increment an integer or long. (def i 1) (def i+1 (inc i)) If you need to assign (inc i) to i itself, then please tell why you want to do that. There will be a more elegant (or idiomatic) solution in clojure in most of the cases.
