[site]: stackoverflow
[post_id]: 5252011
[parent_id]: 5152686
[tags]: 
By the way, the more elegant solution to the self-invocation problem is to use AspectJ Load-Time Weaving for your transactional proxies (or whatever AOP-introduced proxy you're using). For example, with annotation-driven transaction management, you can use the "aspectj" mode as follows: Note that the default mode is "proxy" (i.e., JDK dynamic proxies). Regards, Sam
