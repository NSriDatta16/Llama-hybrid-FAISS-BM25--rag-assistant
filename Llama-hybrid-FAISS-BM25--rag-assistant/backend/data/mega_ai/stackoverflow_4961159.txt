[site]: stackoverflow
[post_id]: 4961159
[parent_id]: 4960519
[tags]: 
The most common sections to require local changes are connectionStrings and appSettings . For both of these sections, the .NET framework allows for storage in a separate file. You then simply keep these separate file out of source control (ignore, get once then cloak, disable versioning on file... whatever flavour source control you use). For example,
