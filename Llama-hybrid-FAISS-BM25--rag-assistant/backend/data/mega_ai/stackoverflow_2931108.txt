[site]: stackoverflow
[post_id]: 2931108
[parent_id]: 1583123
[tags]: 
I couldn't get Robert Koritnik's code to work, so I edited it to the following which seems to work: var CircularQueueItem = function (value, next, back) { this.next = next; this.value = value; this.back = back; return this; }; var CircularQueue = function (queueLength) { /// Creates a circular queue of specified length /// Length of the circular queue this._current = new CircularQueueItem(undefined, undefined, undefined); var item = this._current; for (var i = 0; i Pushes a value/object into the circular queue /// Any value/object that should be stored into the queue this._current.value = value; this._current = this._current.next; }; this.pop = function () { /// Gets the last pushed value/object from the circular queue /// Returns the last pushed value/object from the circular queue this._current = this._current.back; return this._current.value; }; return this; } To use: var queue = new CircularQueue(3); // a circular queue with 3 items queue.push("a"); queue.push("b"); queue.push("c"); queue.push("d"); alert(queue.pop()); // d alert(queue.pop()); // c alert(queue.pop()); // b alert(queue.pop()); // d alert(queue.pop()); // c
