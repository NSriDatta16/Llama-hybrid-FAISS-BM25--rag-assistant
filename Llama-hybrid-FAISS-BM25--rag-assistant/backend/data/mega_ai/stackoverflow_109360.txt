[site]: stackoverflow
[post_id]: 109360
[parent_id]: 61088
[tags]: 
Numbers are also objects. So you can do cool stuff like: // convert to base 2 (5).toString(2) // returns "101" // provide built in iteration Number.prototype.times = function(funct){ if(typeof funct === 'function') { for(var i = 0;i paragraph #'+i+' '; }); // adds 1000 parapraphs to the document
