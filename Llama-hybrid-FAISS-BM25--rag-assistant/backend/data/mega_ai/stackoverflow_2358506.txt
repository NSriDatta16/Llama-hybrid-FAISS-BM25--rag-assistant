[site]: stackoverflow
[post_id]: 2358506
[parent_id]: 2358471
[tags]: 
Quoting the 12.1.17. CREATE TABLE Syntax of the MySQL manual : The CHECK clause is parsed but ignored by all storage engines Maybe this explains that ? Purely as a suggestion, maybe a possible solution for you would be to use either : 10.4.4. The ENUM Type for your column, if it must only contain a few values -- note there are drawbacks to that approach, as an enum doesn't exactly work like a varchar. a Trigger to prevent invalid data to be inserted But those are just wild propositions that I didn't test in this kind of situation .
