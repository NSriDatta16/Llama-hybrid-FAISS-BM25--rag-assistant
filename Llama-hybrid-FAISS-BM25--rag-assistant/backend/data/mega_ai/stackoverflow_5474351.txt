[site]: stackoverflow
[post_id]: 5474351
[parent_id]: 5473858
[tags]: 
The answer is entirely dependent on the storage engine used by the table in question. In general, you will go through MySQL itself to get access to your stored data. If you specify ENGINE = CSV when creating the table, then MySQL will use the CSV engine , which does use delimiter-separated storage.
