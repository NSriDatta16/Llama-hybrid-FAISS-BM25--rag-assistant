[site]: stackoverflow
[post_id]: 3941640
[parent_id]: 3941593
[tags]: 
Not entirely clear, but sounds like you want something like... var detailsOfUserAccount = policySpecificationXml .Descendants("Agent") .Where(agent => agent.Descandants("Product") .Any(product => (string)product.Attribute("ID") == productId)) .FirstOrDefault();
