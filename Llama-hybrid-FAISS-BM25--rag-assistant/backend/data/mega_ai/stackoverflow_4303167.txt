[site]: stackoverflow
[post_id]: 4303167
[parent_id]: 
[tags]: 
Grouping posted values for echo after they're submitted

Whats the best way to print the meterage1 and product1 posts as a single related string value when i loop through the foreach? So for example, product1 input box value which is related to meterage1 and product2 relates to meterage2. Here is my form: + Product Code (e.g 66203) mtrs sq Required (e.g 10) Preview Image Submit My current PHP foreach: if ($_POST) { foreach($_POST as $key => $value) { $orderdetails = $key.'&nbsp;'.$value.'m 2 '; } } else { $orderdetails = 'No Order Details Selected'; }
