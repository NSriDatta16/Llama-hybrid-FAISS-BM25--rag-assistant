[site]: stackoverflow
[post_id]: 4792447
[parent_id]: 4792409
[tags]: 
You can store input in their browser's localStorage, or in a cookie if it's really small. That way users can save their stuff, but don't even have to sign up. Otherwise, "signing up" usually implies creating some sort of account, which must be handled server side.
