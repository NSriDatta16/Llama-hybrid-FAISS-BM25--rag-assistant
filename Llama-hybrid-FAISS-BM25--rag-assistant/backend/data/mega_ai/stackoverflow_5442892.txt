[site]: stackoverflow
[post_id]: 5442892
[parent_id]: 
[tags]: 
Can I use the > operator in a KRL query() selector?

I want to get a nested DIV tag using KRL query() but it complains with ERROR Rules.pm a8x40 show_xfers Ruleset a8x40 failed: html.query error - Invalid specification ">div" in query: div.recent-transfer>div Here's the HTML fragment (there are multiple in the file): ... ... ... Here's my function: recent = function() { t = http:get(the_url).pick("$..content"); t.query("div.recent-transfer>div") } I want to select the DIV marked * * * . Do I need to chain several query() statements to get the DIV?
