[site]: datascience
[post_id]: 121322
[parent_id]: 
[tags]: 
detect/generate possible tokens for the dataset (name,type/category,signatures)

I have a dataset in the following format: name, type, signature Eg1 : A, 2, abc123 Eg2 : A, 2, ab3 Eg3 : A, 2, addc1 If we need to train the following dataset using roberta or any other model how can we do it? Or is there any other way to train this model to detect possible signatures for the name?
