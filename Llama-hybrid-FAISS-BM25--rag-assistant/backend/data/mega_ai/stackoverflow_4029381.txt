[site]: stackoverflow
[post_id]: 4029381
[parent_id]: 
[tags]: 
Input after cin.getline() fails?

I am new to programming. In my textbook, the problem presented it to write a program that asks a user for the rainfall for three months and calculates the average. I used the cin.getline() function to read the user input into an array. The text states that there is no worry of the array being overflowed using the cin.getline() function. However, if I enter a string larger than the array, my program goes haywire. Why does this happen? #include "stdafx.h" #include #include using namespace std; int main() { const int SIZE = 10; char month1[SIZE], month2[SIZE], month3[SIZE]; double rain1, rain2, rain3, average; cout > rain1; cin.ignore(); cout > rain2; cin.ignore(); cout > rain3; cin.ignore(); average = (rain1 + rain2 + rain3)/3; cout
