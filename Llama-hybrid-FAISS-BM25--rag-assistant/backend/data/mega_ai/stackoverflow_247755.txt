[site]: stackoverflow
[post_id]: 247755
[parent_id]: 247718
[tags]: 
You could try declaring the parameters first (then set the values), and see if this make a difference. cm.Parameters.Add["@OrganisationName", SqlDbType.NVarChar, 256].Direction = ParameterDirection.Output cm.Parameters["@OrganisationName"].Value = name But to me there doesn't look like anything wrong with what you have posted. Incidently, if you shouldn't need the .Parse(.ToString()) you should only need to cast. visible = bool.Parse(cm.Parameters["@Visible"].Value.ToString()); becomes visible = (bool)cm.Parameters["@Visible"].Value;
