[site]: crossvalidated
[post_id]: 461776
[parent_id]: 
[tags]: 
Is error term in MA model in univariate time series the same as white noise

I what to know if the error term referred to in moving average model of time series the same as white noise? which is usually define in r as e If y[t] = e[t] + theta*e[t] where theta is a parameter provided makin the ma(1) model to be y[t] = e[t] + 0.8*e[t] if `t = 1,2,...,100. Can I figure it out in ar as: n=100 theta
