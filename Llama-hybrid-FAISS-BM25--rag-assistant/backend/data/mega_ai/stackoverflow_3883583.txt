[site]: stackoverflow
[post_id]: 3883583
[parent_id]: 3883490
[tags]: 
It would be better, for a matter of performance, if your aspx page would returns a html fragment without "doc" links, even if you could filter later on ajax callback. And it would be better if you would send a json with a simple object containing the url (to the non-doc file) because you have a lot of redundant code.
