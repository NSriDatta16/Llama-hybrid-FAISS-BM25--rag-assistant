[site]: stackoverflow
[post_id]: 4170788
[parent_id]: 
[tags]: 
Any ideas on what is wrong with this javascript code?

I have the code below. The purpose of the code is to grab all the values stored in the local storage and display them in two HTML elements with id s of 'title' and 'textLoc' . 'title' is an and 'textLoc' is a . I want the values to be stored in the and the keys to be stored in the . The values are being stored correctly but the keys are not. Any ideas on why this would be? var tests = []; var titles = []; var finalTests = ""; var key, value; for (var i = 0; i " + key + " "; } for (i=0; i
