[site]: stackoverflow
[post_id]: 4824936
[parent_id]: 
[tags]: 
how to show msgbox on button click event if i have some disabled checkboxes and user forgot to check checkboxes

i m using asp.net Usercontrols How to show alert msgbox if user doesn't select atleast minimun 1 checkbox from asp.net checkboxes if i have disabled some checkboxes on page load event then after user forgot to check checkboxes and click on button then still msgbox will popup "you haven't select checkboxes" please edit my this code ...to show msg box on button click event if i have disabled checkboxes and if user haven't select any of the checkbox &nbsp; &nbsp; Choose... " SelectCommand="SELECT DISTINCT [bd_time] FROM [boardingpt] WHERE ([service] = @service)"> Choose... " SelectCommand="SELECT DISTINCT [dp_time] FROM [droppingpt] WHERE ([service] = @service)"> " SelectCommand="SELECT TOP 1 [seats], [id], [dates], [service] FROM a1_holds WHERE (dates = @dates) AND (service = @service) ORDER BY [id] DESC"> " SelectCommand="SELECT DISTINCT sel_seat FROM a1_ticket WHERE (journey_date = @journey_date) AND (serv_code = @serv_code)"> function checkboxChecked(){ var checked=false; var allInputs = document.getElementsByTagName("input"); var noOfChkBox = 0; // number of check box var noOfDisabledChkBox = 0; // number of disabled check box for(var i=0; i
