[site]: crossvalidated
[post_id]: 227177
[parent_id]: 227133
[tags]: 
For a problem like this I would suggest modelling the defaults separately. So you'd have a binary outcome default/non-default (logistic regression say) and then a model for the non-defaults and one for the defaults. They might each be roughly (conditionally) gamma, say
