[site]: stackoverflow
[post_id]: 2078838
[parent_id]: 2078801
[tags]: 
Add this to your stylesheet: ul { clear: left; } It seems your (what I assume are p tags) paragraphs have a float: left and an explicit width specified. However, the ul is still attempting to keep the paragraph to its left, and producing that strange result.
