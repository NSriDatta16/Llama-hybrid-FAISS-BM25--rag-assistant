[site]: stackoverflow
[post_id]: 4617847
[parent_id]: 4616335
[tags]: 
Why do you need the table to be an IOT? a UNIQUE INDEX on column1,column2 with a heap table will probably be nearly as efficient for data retrieval. IOTs (similar to a table with a clustered index) are the exception, not the norm in Oracle.
