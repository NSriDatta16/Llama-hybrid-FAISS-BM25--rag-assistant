[site]: stackoverflow
[post_id]: 4964513
[parent_id]: 4963073
[tags]: 
Just look for those rows with XPath and remove them: $d = new DOMDocument(); $d->loadXml($f); $xp = new DOMXpath($d); foreach ($xp->query("//row[./PCatID != 2]") as $ev) { $ev->parentNode->removeChild($ev); }
