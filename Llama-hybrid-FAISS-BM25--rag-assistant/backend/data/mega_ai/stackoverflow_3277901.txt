[site]: stackoverflow
[post_id]: 3277901
[parent_id]: 3277847
[tags]: 
I would change the HTML structure to embed the link inside the paragraph itself to make it more semantic from 2 hours to 2 hours for each of the four links. Then assign a handler to each link: $('p.left a').click(function() { $('.buy').attr('href', this.href); return false; // don't follow the link });
