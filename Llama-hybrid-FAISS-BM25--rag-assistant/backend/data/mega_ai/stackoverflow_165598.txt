[site]: stackoverflow
[post_id]: 165598
[parent_id]: 73261
[tags]: 
I agree with Joe. I even developed helper structs and classes like this one: internal struct Argument { internal static object False = false; internal static object Missing = System.Type.Missing; internal static object True = true; } And this one: /// /// Defines the "special characters" /// in Microsoft Word that VSTO 1.x /// translates into C# strings. /// internal struct Characters { /// /// Word Table end-of-cell marker. /// /// /// Word Table end-of-row markers are also /// equal to this value. /// internal static string CellBreak = "\r\a"; /// /// Word line break (^l). /// internal static string LineBreak = "\v"; /// /// Word Paragraph break (^p). /// internal static string ParagraphBreak = "\r"; } And a few more...
