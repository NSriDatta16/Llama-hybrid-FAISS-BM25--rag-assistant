[site]: datascience
[post_id]: 96722
[parent_id]: 
[tags]: 
Does Keras MultiHeadAttention with 1 head equals normal self attention?

Keras multihead attention if used as single head num_heads=1 , then how is it different than Keras Attention ? Also, Is multihead attention by default self-attention type?
