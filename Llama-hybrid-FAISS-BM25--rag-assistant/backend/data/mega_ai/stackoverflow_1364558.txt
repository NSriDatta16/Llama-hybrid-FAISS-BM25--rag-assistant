[site]: stackoverflow
[post_id]: 1364558
[parent_id]: 1364094
[tags]: 
I'd do it exactly as you described. In fact, I actually did: The dog jumps over the log. function boxWords(textNode) { var frag = document.createDocumentFragment(), // trim leading and trailing whitespace to avoid empty elements words = textNode.nodeValue.replace(/(^\s+)|(\s+$)/g, '').split(' '); for(var i = 0, len = words.length; i
