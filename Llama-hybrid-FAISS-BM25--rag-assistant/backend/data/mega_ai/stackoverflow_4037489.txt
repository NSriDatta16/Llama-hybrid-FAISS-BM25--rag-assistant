[site]: stackoverflow
[post_id]: 4037489
[parent_id]: 4036036
[tags]: 
Regexes would make it a lot more elegant: // helo babe echo preg_replace('~.*?x~', '', $str); // Tuex helo babe echo preg_replace('~.*?y~', '', $str); But you can always try this: // helo babe echo str_replace(substr($str, 0, strpos($str, 'x')) . 'x', '', $str); // Tuex helo babe echo str_replace(substr($str, 0, strpos($str, 'y')) . 'y', '', $str);
