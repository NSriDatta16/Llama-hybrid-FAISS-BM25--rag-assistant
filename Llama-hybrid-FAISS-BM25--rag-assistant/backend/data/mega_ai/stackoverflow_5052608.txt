[site]: stackoverflow
[post_id]: 5052608
[parent_id]: 5052461
[tags]: 
I don't know how authoritative a resource xhtml.com is, but they do say that the a tag can only contain: Inline elements, except a, at any depth Text A possible solution would be to just reorganize your HTML so that it makes more sense (eg, not trying to put block-level elements in inline-level elements). Just have a single link for the product (maybe in the h2, or around the image). Then use JavaScript to detect a click anywhere on the li , and load the link. Does that make any sense? Here's an example.
