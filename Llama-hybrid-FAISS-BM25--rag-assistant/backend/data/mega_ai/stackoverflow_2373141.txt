[site]: stackoverflow
[post_id]: 2373141
[parent_id]: 2373088
[tags]: 
This is a perfectly valid way of doing things. As long as your selectors are quick this is certainly not fragile, nor slow. Once you JS file is loaded, it's cached. You could optimize this a bit as follows: $(document).ready(function() { if($("div.dostufftome")) { // dostuff } }
