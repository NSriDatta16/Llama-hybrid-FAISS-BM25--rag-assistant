[site]: stackoverflow
[post_id]: 4464265
[parent_id]: 4464236
[tags]: 
do it in the success of ajax, so that it will ensure its done after the ajax is completed $.getJSON ( serviceurl, function (data) { $.each(data, function (i, item) { $('div.prnt').append(" hello "); }); $("div.text").draggable(); } ); });
