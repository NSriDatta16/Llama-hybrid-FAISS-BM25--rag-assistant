[site]: stackoverflow
[post_id]: 4091995
[parent_id]: 4091721
[tags]: 
yes, just use UserID based storage of the image files. But, make sure you wrap your request for a userdir location in a function call in case you need to change it to deal with directory file limits. for instance assume $imageName is the name of the image you are trying to store or retrieve,. $userDir = getUserDir($userID); $imgLink = 'http://' .$hostname .'/' . $userDir . '/' . $imgName; function getUserDir($userID) { return 'images/' . $userID; }
