[site]: stackoverflow
[post_id]: 1490080
[parent_id]: 1490073
[tags]: 
SELECT b.bid, AVG(s.age) AS avg_age FROM sailor s JOIN boat b USING (sid) GROUP BY b.bid ORDER BY AVG(s.age) DESC LIMIT 1; This will return one row, and because of the ordering it will contain the maximum average age.
