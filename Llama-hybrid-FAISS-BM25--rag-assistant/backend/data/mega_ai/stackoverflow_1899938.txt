[site]: stackoverflow
[post_id]: 1899938
[parent_id]: 1899913
[tags]: 
var tip = " Most computers will open PDF documents "; tip += "automatically, but you may"; tip += "need to download Adobe Reader . "; $(document).ready(function(){ //IF NUMBER OF PDF LINKS IS MORE THAN ZERO INSIDE DIV WITH ID maincontent //THEN THIS WILL PUT TIP PARAGRAPH AS LAST CHILD OF DIV if($("div#maincontent a[href*='/pdf']").length>0){ $("div#maincontent").children(":last-child").after(tip); } }); check it here
