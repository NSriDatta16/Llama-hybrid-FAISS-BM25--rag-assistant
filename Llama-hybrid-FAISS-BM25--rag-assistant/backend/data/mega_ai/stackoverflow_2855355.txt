[site]: stackoverflow
[post_id]: 2855355
[parent_id]: 2855335
[tags]: 
You really do not need to check as File.ReadAllLines() will always return a string array regardless of the number of lines. You can leverage that behavior and simply join the returned array with your separator of choice. string singleLine = string.Join(" ", File.ReadAllLines("filepath"));
