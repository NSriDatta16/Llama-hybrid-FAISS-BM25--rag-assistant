[site]: stackoverflow
[post_id]: 5038683
[parent_id]: 
[tags]: 
JQuery - Drag & Drop - Id draggable element

I'm new in this forum I learned jquery from 2 week and i'm fairly inexperienced. My site is build with asp.net mvc and use jquery 1.4.1 this is the code html ... " idAmico=" "> .... script $(function() { $("div[id^='amico_']").draggable({ revert: "valid" }); $("#droppable").droppable({ activeClass: "ui-state-hover", hoverClass: "ui-state-active", drop: function(event, ui) { var index = $(".ui-draggable").attr('idAmico'); $(this) .addClass("ui-state-highlight") .find("p") .html("id = " + index); } }); }); the problem is that id attribute's value is always 1 (index variable) while are 1 and 2. (red row) where am i wrong? thanks so much for your replies Alberto
