[site]: stackoverflow
[post_id]: 3239208
[parent_id]: 3230935
[tags]: 
I solved the problem using Cached Composition. I had to place the Viewport3d into a canvas and use the following code: CacheMode = new BitmapCache() { EnableClearType = true, RenderAtScale = 1, SnapsToDevicePixels = true }, Doing the same thing on the viewport itself did not work...
