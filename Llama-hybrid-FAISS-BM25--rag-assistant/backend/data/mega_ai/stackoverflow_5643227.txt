[site]: stackoverflow
[post_id]: 5643227
[parent_id]: 5643210
[tags]: 
Rather than storing data in the database, you could easily use session or cookie storage. Create a stack of most recent references with a fixed size and manipulate this when a record is worked on. Your dashboard can then use this stack to display the records.
