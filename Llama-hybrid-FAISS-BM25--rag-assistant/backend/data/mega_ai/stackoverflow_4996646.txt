[site]: stackoverflow
[post_id]: 4996646
[parent_id]: 4996390
[tags]: 
You can simply place all your runs on a single line... ABC DEF GHI You can then access each run via the InlineCollection ... foreach (var run in ((Paragraph)content.Blocks.FirstBlock).Inlines) { //do stuff with run }
