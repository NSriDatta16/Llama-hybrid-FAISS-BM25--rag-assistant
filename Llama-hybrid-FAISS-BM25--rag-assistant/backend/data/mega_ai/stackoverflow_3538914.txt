[site]: stackoverflow
[post_id]: 3538914
[parent_id]: 3538632
[tags]: 
Nevermind, it worked now. I set the modified the config file as follows # Method 2: Compress all content, manually excluding specified file types # place filter 'DEFLATE' on all outgoing content SetOutputFilter DEFLATE # exclude uncompressible content via file type SetEnvIfNoCase Request_URI \.(?:gif|jpe?g|png|rar|zip)$ no-gzip # properly handle requests coming from behind proxies Header append Vary User-Agent Then did a browser cache reload and checked the log files and it was compressed. Yslow agreed.
