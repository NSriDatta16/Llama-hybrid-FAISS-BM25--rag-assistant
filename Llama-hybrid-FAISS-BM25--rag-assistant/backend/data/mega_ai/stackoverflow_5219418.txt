[site]: stackoverflow
[post_id]: 5219418
[parent_id]: 5218227
[tags]: 
Do the job in constructor or @PostConstruct method of the bean which is associated with the view and use the rendered attribute to render page fragment/include conditionally. @PostConstruct public void init() { someObject = someDAO.find(someParam); } with Found Not gound Or use ExternalContext#redirect() to redirect to another view. @PostConstruct public void init() { someObject = someDAO.find(someParam); if (someObject == null) { FacesContext.getCurrentInstance().getExternalContext().redirect("notfound.jsf"); } }
