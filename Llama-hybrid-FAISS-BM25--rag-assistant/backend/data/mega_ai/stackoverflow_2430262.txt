[site]: stackoverflow
[post_id]: 2430262
[parent_id]: 2430218
[tags]: 
You have 3 issues happening. The javascript include for jQuery should be javascript instead of ecmascript : The .active class won't override #drag (because it's less specific) you need to change the CSS to: #drop.active { background:#FFCC33; } And in jQuery don't use the . when using the addClass() method, just use the class name: $(this).addClass('active'); Here's your source with those changes, this should have the intended effect: #content { background:#CCCCCC; width:500px; height:500px; } #drop { height:200px; width:200px; background:#00FFFF; float:right; } #drag { background:#009966; width:100px; height:100px; float:left; } #drop.active { background:#FFCC33; } $(document).ready(function() { $('#drag').draggable({ containment: '#content', scrollSensitivity: 60, revert: true, cursor: 'move' }); $('#drop').droppable({ accept: '#drag', drop: function(event, ui) { $(this).addClass('active'); } }); }); Untitled Document
