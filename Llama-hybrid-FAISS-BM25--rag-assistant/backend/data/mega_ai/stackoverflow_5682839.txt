[site]: stackoverflow
[post_id]: 5682839
[parent_id]: 5682783
[tags]: 
Just run the .draggable() code on DOM ready. You'll need to use a different selector, though. $(function () { $('some selector').draggable({ scroll:true, scrollSensitivity:100, scrollSpeed:100 }); });
