[site]: datascience
[post_id]: 126879
[parent_id]: 126878
[tags]: 
You can get an embedded vector for each sample, then do dimensionality reduction and then plot the samples in a 2D plane using the category as colour. This will give you something like this: With that, you can start exploring the relationships among categories. These are the details: To get embedded vectors, you can use the sentence transformers library . To do dimensionality reduction, you can use U-MAP , as it is better than PCA and t-SNE.
