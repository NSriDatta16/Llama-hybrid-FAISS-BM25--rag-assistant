[site]: stackoverflow
[post_id]: 1426822
[parent_id]: 1426714
[tags]: 
It seems that the LIMIT operation is being performed before to the ORDER BY function. I'm no expert (far from it), but I'm guessing that nesting the nested query should work: ( (SELECT value ORDER BY datetime ASC) LIMIT 1) AS open But this isn't exactly an elegant way to do it. I'm sure other SO users will have a better solution.
