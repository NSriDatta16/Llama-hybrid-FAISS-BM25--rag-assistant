[site]: stackoverflow
[post_id]: 4915109
[parent_id]: 4914896
[tags]: 
Here is the jsfiddle: Using RegEx and If Else condition $('p').each(function() { var temp = this.innerHTML.replace(/^\s*|\s(?=\s)|\s*$/g, "").toLowerCase(); if (temp.substring(0, 5) == ' ' || temp.substring(0, 4) == ' ' || temp.substring(0, 6) == ' ') { $(this).addClass('diog-fail brbr'); console.log('start: true'); } else if (temp.substring(temp.length - 5, temp.length) == ' ' || temp.substring(temp.length - 4, temp.length) == ' ' || temp.substring(temp.length - 6, temp.length) == ' ') { $(this).addClass('diog-fail brbr'); console.log('end: true'); } else { console.log('none: ' + false); } }); First get all paragraphs and then use RegEx on the innerHTML and flatted it by removing whitespace, newline and etc. Then do if else condition to see if first and last few substrings matches , or
