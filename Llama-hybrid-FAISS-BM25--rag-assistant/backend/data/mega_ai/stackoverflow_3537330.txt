[site]: stackoverflow
[post_id]: 3537330
[parent_id]: 3537285
[tags]: 
In addition to what strager said, IMO you're going a bit wrong by templating HTML together using strings. PHP is a templating language, you might as well use it: (Note the HTML-encoding, to avoid the invalid unescaped ampersands in src .)
