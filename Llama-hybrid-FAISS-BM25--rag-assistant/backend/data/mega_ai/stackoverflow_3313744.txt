[site]: stackoverflow
[post_id]: 3313744
[parent_id]: 3312294
[tags]: 
Seems everyone's pointing you to code which references the query string. Assuming your parameters are in the fragment rather than the query string as in the example you gave, you can use the window.location.hash property to access them without parsing the entire URL yourself. From there you can use code similar to what others have mentioned. A simple loop like this would work. var values = {}, pairs = window.location.hash.substring(1).split('&'), pair; for (var i = 0, len = pairs.length; i
