[site]: crossvalidated
[post_id]: 116410
[parent_id]: 113987
[tags]: 
I think you are looking for: fit2 = lm(y ~ x, data = d.f, weights = 1/u^2) parameter.covariance.matrix = vcov(fit2)/summary(fit2)$sigma^2 there is probably a better way to get this information from the fit2 object. This works even if it isn't elegant.
