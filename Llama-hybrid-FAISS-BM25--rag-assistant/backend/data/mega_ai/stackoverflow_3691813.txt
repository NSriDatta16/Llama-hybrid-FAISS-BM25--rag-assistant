[site]: stackoverflow
[post_id]: 3691813
[parent_id]: 
[tags]: 
Drag and drop a file then POST to a URL using HTML5

I have figured out the drag and drop part but how would I then upload and POST the file to a URL with set parameters? If I had a form I would just How would I do above using the HTML5 drag and drop API???
