[site]: crossvalidated
[post_id]: 205661
[parent_id]: 66190
[tags]: 
My solution is, If it is acceptable, better to compose all the observations into a single data set and go for stratified sampling, which might not be possible when data is separately in different files. Then build SVM on it.
