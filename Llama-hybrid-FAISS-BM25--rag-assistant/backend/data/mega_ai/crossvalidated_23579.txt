[site]: crossvalidated
[post_id]: 23579
[parent_id]: 23578
[tags]: 
It depends on the classifier. Some need to have inputs that have the same magnitude, some not. Models that AFAIK need scaled data: SVM Most neural networks Fisher model --- I not really sure, but I think so. Models that don't need it: Decision trees
