[site]: stackoverflow
[post_id]: 3647140
[parent_id]: 3647126
[tags]: 
You just took it wrong. It is not PHP being embedded in HTML, but HTML being embedded in PHP. To run PHP code a PHP interpreter is required. And usual browser doesn't have one. Thus, you can add PHP code to HTML archive but it just won't work. While embedding itself is called Escaping from HTML
