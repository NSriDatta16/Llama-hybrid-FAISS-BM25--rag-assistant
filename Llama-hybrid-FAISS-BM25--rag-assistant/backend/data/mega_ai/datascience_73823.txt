[site]: datascience
[post_id]: 73823
[parent_id]: 
[tags]: 
Open source alternative to google's Auto ML

Auto ML receives raw(ish) data and generates a supervised learning model from it. That is, it automates feature engineering and model selection/tuning. What open source alternatives currently exist that when given raw data and told which variable to target, and will produce an ML model autonomously?
