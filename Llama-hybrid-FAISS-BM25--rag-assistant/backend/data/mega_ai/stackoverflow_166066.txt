[site]: stackoverflow
[post_id]: 166066
[parent_id]: 165175
[tags]: 
The "issue" is that this fragment relies heavyly on operator precedence (not bad per se, but sometimes it can be confusing). Here you can find a list of all java operators with their precedence, and here for comparison the same table for C/C++
