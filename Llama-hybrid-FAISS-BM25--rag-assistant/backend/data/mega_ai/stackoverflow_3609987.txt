[site]: stackoverflow
[post_id]: 3609987
[parent_id]: 
[tags]: 
Elegantly refactoring code like this (to avoid a flag)

I have a function running over an enumerable, but the function should be a little bit different for the first item, for example: void start() { List a = ... a.ForEach(DoWork); } bool isFirst = true; private void DoWork(string s) { // do something if(isFirst) isFirst = false; else print("first stuff"); // do something } How would you refactor this to avoid that ugly flag?
