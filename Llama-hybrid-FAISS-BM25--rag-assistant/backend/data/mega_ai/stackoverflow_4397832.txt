[site]: stackoverflow
[post_id]: 4397832
[parent_id]: 4396771
[tags]: 
The .Script() method returns a string collection. There's probably a more elegant way of doing it, but replacing $scripts.Set_Item ($instance, ) with $val = $scripts[$instance] $val.Add("text to add") $scripts.Set_Item($instance, $val) should work.
