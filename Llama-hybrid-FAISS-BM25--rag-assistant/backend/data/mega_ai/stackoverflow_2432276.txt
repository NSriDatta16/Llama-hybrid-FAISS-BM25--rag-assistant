[site]: stackoverflow
[post_id]: 2432276
[parent_id]: 2432159
[tags]: 
(Assuming the fact that in the for loop you always get results.rows.item(0), instead of indexing all the items, is a typo when pasting the code in the question) You might be hitting the "Infamous Loop Problem" with variable bindings (described by Robert Nyman and James Padolsey (original page disappeard, so link to Google cache)).
