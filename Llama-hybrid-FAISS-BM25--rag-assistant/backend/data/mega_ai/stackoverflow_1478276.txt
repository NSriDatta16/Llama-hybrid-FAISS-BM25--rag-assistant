[site]: stackoverflow
[post_id]: 1478276
[parent_id]: 1478083
[tags]: 
Maybe something like: my @interesting = ( qr/Failed in routing out/, qr/Agent .+ failed/, qr/Record Not Exist in DB/, ); ... for my $re (@interesting) { if ($line =~ /$re/) { print $line; last; } } You can try joining all your patterns with "|" to make one regex. That may or may not be faster.
