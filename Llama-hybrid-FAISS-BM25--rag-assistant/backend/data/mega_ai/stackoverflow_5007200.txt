[site]: stackoverflow
[post_id]: 5007200
[parent_id]: 5006656
[tags]: 
It is possible, but not neccessary. You should use the Tortoise client to export your working copy to another directory. You can drag and drop (with a right click) and then select export. This copy all versioned files, but creates no .svn directories. You could also drop the directory at itself.
