[site]: stackoverflow
[post_id]: 1648611
[parent_id]: 1648537
[tags]: 
I certainly agree with @Wogan above, that using the CSV moudle is a good approach. Having said that if you still want to try a regex solution give this a try, but you will have to adapt it to Python dialect string.split(/,(?=(?:[^\"]*\"[^\"]*\")*(?![^\"]*\"))/) HTH
