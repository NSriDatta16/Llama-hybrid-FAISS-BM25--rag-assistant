[site]: crossvalidated
[post_id]: 381802
[parent_id]: 
[tags]: 
Terminology for neural networks

The words “a neural network” in machine learning can refer to either of The architectural design of a neural network (number of layers, etc) The network with specific parameter values encoded in it. The first we can call “neural net architecture”, but how do we specifically refer to the second?
