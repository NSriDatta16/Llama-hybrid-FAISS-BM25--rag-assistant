[site]: crossvalidated
[post_id]: 64370
[parent_id]: 
[tags]: 
How to use SVD for dimensionality reduction

After reading several "tutorials" on SVD I am left still wondering how to use it for dimensionality reduction. Here is my confusion in an applied setting. If I limit svd to only considering the first two singular values / vectors and "recreate" the matrix, the dimensionality is still the same (4 columns). What should be done here to instead only use 2 columns? data(iris) s
