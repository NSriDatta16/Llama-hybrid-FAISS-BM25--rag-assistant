[site]: stackoverflow
[post_id]: 2446393
[parent_id]: 2446346
[tags]: 
You get this exception because the example you entered is a valid XML fragment (as a consequence readable by Firefox), but an invalid XML document, as it has more than one root node, which is forbidden by XML rules. Try to create one XML document for each tag, and SAX will be fine.
