[site]: stackoverflow
[post_id]: 5002196
[parent_id]: 5001426
[tags]: 
I think the fadeIn() should be applied to the appened paragraph. $.each(tweetsArray, function(i, tweet){ $(displayContainer).append( $(tweet).css('display', 'none').fadeIn() ); }); I'm not sure you're array is compatible with "$(tweet)", if not try to wrap the tweet in a like $('&lt;p&gt;', {'html': tweet}).css('display', 'none').fadeIn();
