[site]: crossvalidated
[post_id]: 140807
[parent_id]: 
[tags]: 
Problematic variables and number of factors in PCA

I have a problem with two variables in PCA. I obtain a certain number of factors (lets say n factors), but on the last factor I obtain only one variable (I will call it X) that loads high on that factor and another variable (Y) that has a loading
