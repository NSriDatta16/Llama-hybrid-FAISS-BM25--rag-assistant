[site]: stackoverflow
[post_id]: 5417040
[parent_id]: 5416975
[tags]: 
On js document.write(navigator.appCodeName); On php echo array_shift(explode(' ', $_SERVER['HTTP_USER_AGENT'])); Using these you can switch css, but have you ever looked in to css framework like Blueprint css It contains something like You can use this conditional statements, also using this framework isn't that bad either.
