[site]: stackoverflow
[post_id]: 75515
[parent_id]: 75487
[tags]: 
New separate types for Date and Time, instead of just Datetime New geographic types for lattitude/longitude Change Data Capture is pretty neat if you're doing anything where auditing is important Configuration Servers, for maintaining multiple databases. That's what caught my attention at the Heroes Happen Here launch back in April.
