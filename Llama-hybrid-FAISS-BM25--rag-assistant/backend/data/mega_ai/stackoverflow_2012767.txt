[site]: stackoverflow
[post_id]: 2012767
[parent_id]: 2011435
[tags]: 
Aside from the HTML problems (spans wrapping an li and a strange mixture of text and list) you should make one return function plus a calculate function to deal with the whole thing like this PHP function echoRatingAll () { list($value, $counter) = getRating2(); //function returns doesn't print echo "$value,$counter"; //we need to echo for the AJAX & we comma delimit } function getRating2(){ $sql= "select `value`, `counter` from vote"; $result=@mysql_query($sql); list($v,$c) = mysql_fetch_row($result); return array($v, $c); } JS success: function(result) { var values=result.split(",", result); var value = values[0]; var counter = values[1]; //do your jquery stuff from here with the two values } if you are unsure how to do the averaging in Javascript just make the PHP return 3 values rather than 2.
