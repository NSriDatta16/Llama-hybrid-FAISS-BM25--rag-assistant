[site]: stackoverflow
[post_id]: 942395
[parent_id]: 942357
[tags]: 
When it has captured the mouse, a control will receive mouse events even if the mouse pointer is no longer within its bounding area. Typically, it's used for: Drag and drop Buttons (to handle Mouse Up when you put the mouse down on the button and move the mouse before you release the button)
