[site]: stackoverflow
[post_id]: 3320262
[parent_id]: 
[tags]: 
ASP.Net - How to determine if web request is to a resource that allows anonymous users or not

I have denied anonymous access to the entire application using the following Web.Config setting: Then, for various paths, I have allowed anonymous access using Web.Config settings such as this: I'd like to be able to determine during the processing of a given request whether the requested URL is to path that allows anonymous users or whether the request is to a path that denies anonymous users. What is the most elegant way of determining this?
