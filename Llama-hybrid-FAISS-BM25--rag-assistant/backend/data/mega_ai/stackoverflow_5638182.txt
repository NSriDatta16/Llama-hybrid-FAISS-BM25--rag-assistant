[site]: stackoverflow
[post_id]: 5638182
[parent_id]: 5637954
[tags]: 
@NarfFlarf: You could just use WordPress queries to get your data -- $sql = "SELECT * FROM headerslogans ORDER BY RAND() LIMIT 1"; $result = $wpdb->get_results($sql); foreach ($myrow as $result) { echo $myrow->slogan; } See this WordPress Codex entry for further reference. Update Something else you can perhaps try then is to have and within slogan.php :
