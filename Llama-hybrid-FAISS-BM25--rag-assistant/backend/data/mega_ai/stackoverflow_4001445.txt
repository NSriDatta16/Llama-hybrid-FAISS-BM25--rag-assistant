[site]: stackoverflow
[post_id]: 4001445
[parent_id]: 3496209
[tags]: 
I had to return a rtf file and this worked for me. // create a byte array of the file in correct format byte[] docStream = createDoc(fragments); return Response .ok(docStream, MediaType.APPLICATION_OCTET_STREAM) .header("content-disposition","attachment; filename = doc.rtf") .build();
