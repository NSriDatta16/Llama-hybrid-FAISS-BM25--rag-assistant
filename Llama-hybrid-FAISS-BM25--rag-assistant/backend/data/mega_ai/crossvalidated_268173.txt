[site]: crossvalidated
[post_id]: 268173
[parent_id]: 267663
[tags]: 
You could train a binary classifier. Encode each entry in your data as $x_i = (\text{code, count, other description})$ and $y_i=1$ if the entry belongs to A and $y_i=0$ if it belongs to B. As a first step, you could try fitting a logistic regression .
