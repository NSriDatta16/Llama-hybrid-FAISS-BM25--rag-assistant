[site]: stackoverflow
[post_id]: 2112468
[parent_id]: 2111587
[tags]: 
In addition to the config file... You can define a variable which can be set by the command line or in a batch job or SQL Server Agent job. The variable can be substituted into the server name (by an expression, IIRC). This is useful for if you want a single file deployment. Edit: Example : (Sorry, SQL Server Central requires registration)
