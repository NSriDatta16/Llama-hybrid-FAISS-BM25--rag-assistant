[site]: stackoverflow
[post_id]: 2716410
[parent_id]: 2716306
[tags]: 
The way you've got is fine (call the setter and then check the get returns the expected value). Make sure you choose a selection of test values that exercise all the paths in that setter. A single set/get test isn't sufficient coverage.
