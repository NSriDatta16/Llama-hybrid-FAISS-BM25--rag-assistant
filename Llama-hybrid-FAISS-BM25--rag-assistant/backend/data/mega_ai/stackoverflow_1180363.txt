[site]: stackoverflow
[post_id]: 1180363
[parent_id]: 1180340
[tags]: 
PHP provides a function called get_browser that checks the user-agent header and tries to guess the browser against a whitelist. It's quite limited, but it works for the most common browsers. If it doesn't fit your application, then you might need to create a custom function.
