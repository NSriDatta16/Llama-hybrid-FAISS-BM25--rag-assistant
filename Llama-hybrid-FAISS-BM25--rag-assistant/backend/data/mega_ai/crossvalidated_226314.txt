[site]: crossvalidated
[post_id]: 226314
[parent_id]: 
[tags]: 
How do we compute the gradients of recurrent connections in Tensorflow?

As we unfold the network by time, the gradients of each time step is calculated. Are the recurrent gradients averaged or summed over the time steps in the Tensorflow implementation?
