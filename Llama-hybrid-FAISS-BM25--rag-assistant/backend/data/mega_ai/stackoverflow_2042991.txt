[site]: stackoverflow
[post_id]: 2042991
[parent_id]: 2042810
[tags]: 
As others already have noted it is not possible to change the URL without reloading the page. Note that you can the change the fragment identifier, i.e. the part in the URL after the hash (#) using document.location.hash, but that is probably not good enough for you.
