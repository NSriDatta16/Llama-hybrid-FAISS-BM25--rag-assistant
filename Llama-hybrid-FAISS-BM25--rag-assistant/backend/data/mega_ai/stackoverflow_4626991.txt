[site]: stackoverflow
[post_id]: 4626991
[parent_id]: 
[tags]: 
embed PHP in jQuery .append()

Is this possible? I know the code below looks a whole heap of mess but i want to make it more messy by embedding PHP into it. On each click i'm appending a row onto a table, but i need to include a dynamic dropdown into one of these 's by pulling results from a mysql db. Where it says this: Add/Edit Instead of p tags i'm going to have a PHP built dropdown...how can i achieve this? $('#items').append(' ' + supp_short_code_db + ' ' + omPartNo + ' ' + supPartNo + ' ' + cat + ' ' + desc + ' ' + manuf + ' ' + list + ' ' + disc + ' Add/Edit ' + priceEach + ' Add/Edit ');
